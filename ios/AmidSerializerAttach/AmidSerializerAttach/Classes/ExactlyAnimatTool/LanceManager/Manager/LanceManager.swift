
//: Declare String Begin

/*: "PARTY_STATUS_CHANGE_NOTIFICATION" :*/
fileprivate let app_businessValue:[UInt8] = [0x82,0x93,0x80,0x86,0x8b,0x8d,0x81,0x86,0x93,0x86,0x87,0x81,0x8d,0x91,0x9a,0x93,0x9c,0x95,0x97,0x8d,0x9c,0x9d,0x86,0x9b,0x94,0x9b,0x91,0x93,0x86,0x9b,0x9d,0x9c]

private func insertReach(communicate num: UInt8) -> UInt8 {
    return num ^ 210
}

/*: "redirectUrl" :*/
fileprivate let mainOldFemaleGirlValue:String = "bronzeedi"

/*: "VOICEROOM: 进房模型解析失败。" :*/
fileprivate let notiSoulValue:String = "VOICEcastO"
fileprivate let dreamOffImpressionUrl:String = "Ostretch"
fileprivate let userTypicalLetterValue:String = ": \u{8fdb}房模型"

/*: "Party room exception" :*/
fileprivate let main_radioMsg:String = "Party rhuge coat of"
fileprivate let main_animaRemainValue:[Character] = ["o","o","m"," ","e","x","c","e","p","t","i","o","n"]

/*: "male" :*/
fileprivate let kBossFormat:[UInt8] = [0x65,0x6c,0x61,0x6d]

/*: "female" :*/
fileprivate let show_ageKey:[Character] = ["f"]
fileprivate let appOvalMsg:[Character] = ["e","m","a","l","e"]

/*: "VOICEROOM: 群聊进房失败。groupId:  :*/
fileprivate let showSpendUrl:String = "voic"
fileprivate let show_breadPath:String = " demonstrate聊进"
fileprivate let mFatalIdent:String = "accept yet perfect arm broadroupId: "

/*: "VOICEROOM: 切房模型解析失败。" :*/
fileprivate let show_installId:[Character] = ["V","O","I","C","E","R","O","O","M",":"," "]
fileprivate let m_himKey:String = "切房"
fileprivate let k_commendPath:[Character] = ["模","\u{578b}","解","析","失","败","。"]

/*: "VOICEROOM: 群聊切房失败。groupId:  :*/
fileprivate let notiLastFormat:[Character] = ["V","O","I","C","E","R","O","O","M",":"," ","群","聊","\u{5207}"]
fileprivate let notiTakeName:String = "房失coal。"

/*: "errCode" :*/
fileprivate let mainSubmitMessage:[UInt8] = [0xda,0xcd,0xcd,0xfc,0xd0,0xdb,0xda]

private func addressFore(raw num: UInt8) -> UInt8 {
    return num ^ 191
}

/*: "custom_kickout_message" :*/
fileprivate let notiButteIdent:[Character] = ["c","u","s","t","o","m","_","k","i","c","k","o","u","t","_","m","e","s","s","a"]
fileprivate let mTickName:[Character] = ["g","e"]

/*: "closeRoom" :*/
fileprivate let showPersonGuiltyPath:String = "near component levelcloseRo"
fileprivate let kLetterLeadingId:String = "oresource"

/*: "adminCloseRoom" :*/
fileprivate let dreamPlanSupportiveFormat:String = "giftdmin"
fileprivate let noti_staffId:String = "Closegment sound computer"
fileprivate let m_componentIdent:String = "seRoombusy economic strike manage"

/*: "The room is closed, please go to another room" :*/
fileprivate let showDenyKey:[UInt8] = [0x63,0x5f,0x52,0x17,0x45,0x58,0x58,0x5a,0x17,0x5e,0x44,0x17,0x54,0x5b,0x58,0x44,0x52,0x53,0x1b,0x17,0x47,0x5b,0x52,0x56,0x44,0x52,0x17,0x50,0x58,0x17,0x43,0x58,0x17,0x56,0x59,0x58,0x43,0x5f,0x52,0x45,0x17,0x45,0x58,0x58,0x5a]

private func egoForward(closed num: UInt8) -> UInt8 {
    return num ^ 55
}

/*: "You were kicked out of the room" :*/
fileprivate let noti_additionalName:[UInt8] = [0xff,0xc9,0xd3,0x86,0xd1,0xc3,0xd4,0xc3,0x86,0xcd,0xcf,0xc5,0xcd,0xc3,0xc2,0x86,0xc9,0xd3,0xd2,0x86,0xc9,0xc0,0x86,0xd2,0xce,0xc3,0x86,0xd4,0xc9,0xc9,0xcb]

private func dynamicsRole(interrupt num: UInt8) -> UInt8 {
    return num ^ 166
}

/*: "VOICEROOM：进房失败，roomID:  :*/
fileprivate let showBuryStackText:String = "Vclothe"
fileprivate let kLiteralContent:[Character] = ["I","C","E","R","O","O","M","：","\u{8fdb}","\u{623f}","失","败","，","r","o","o","m","I","D",":"," "]

/*: , error:  :*/
fileprivate let mWitchData:[Character] = [","," ","e"]
fileprivate let noti_siblingValue:String = "rror: economic trigger cos psychological remaining"

/*: , reason:  :*/
fileprivate let k_okRiverValue:[Character] = [","," ","r","e","a","s","o","n"]
fileprivate let mRitePleaseValue:[Character] = [":"," "]

/*: , extendedData:  :*/
fileprivate let dream_signalIdent:[Character] = [","," ","e","x","t","e","n"]
fileprivate let main_patientConFormat:String = "dedDfade bright thick"

/*: "VOICEROOM: 当前用户推流失败，roomID:  :*/
fileprivate let user_soundId:String = "VroleICER"
fileprivate let noti_motivationData:String = " "
fileprivate let app_textName:String = "当\u{524d}用\u{6237}推"
fileprivate let noti_lieFormat:String = "rstandstandm"

/*: , errorCode:  :*/
fileprivate let notiSoleMsg:String = ", erpreserve literal"
fileprivate let dream_aliveUrl:String = "de: way lens"

/*: , streamID:  :*/
fileprivate let mMaximumUrl:String = "scenario listen before pending, stre"
fileprivate let app_okName:[Character] = ["a","m","I","D",":"," "]

/*: "An error occurred, please try again" :*/
fileprivate let userWeightContent:[UInt8] = [0xf4,0xdb,0x95,0xd0,0xc7,0xc7,0xda,0xc7,0x95,0xda,0xd6,0xd6,0xc0,0xc7,0xc7,0xd0,0xd1,0x99,0x95,0xc5,0xd9,0xd0,0xd4,0xc6,0xd0,0x95,0xc1,0xc7,0xcc,0x95,0xd4,0xd2,0xd4,0xdc,0xdb]

/*: "VOICEROOM: 拉流失败，roomID:  :*/
fileprivate let appAmberMessage:String = "VOtolerance"
fileprivate let appResponseKey:String = "M: 拉流失close pair course patient"
fileprivate let kHoweverId:String = "basic clear listener templateomID"
fileprivate let kCurrentlyMsg:[Character] = [":"," "]

/*: "VOICEROOM: 信令解析失败，command:  :*/
fileprivate let main_involveForestPath:String = "VconductCER"
fileprivate let userAcrossBalloonValue:[Character] = ["析","\u{5931}","败","，","c","o","m","m","a","n","d",":"," "]

/*: , roomID:  :*/
fileprivate let notiLapKey:String = ", roomtemporary log edit others"

/*: "data" :*/
fileprivate let notiWifeStr:[Character] = ["d","a","t","a"]

/*: "position" :*/
fileprivate let user_allIdent:[Character] = ["p","o","s","i","t"]
fileprivate let dream_arrowIdent:String = "iomove"

/*: "xxxx--  :*/
fileprivate let dream_termsId:String = "effectively error limitxxxx-- "

/*: "opType" :*/
fileprivate let app_rewardKey:[Character] = ["o","p","T","y","p","e"]

/*: "party:changeMike" :*/
fileprivate let m_towerValue:String = "party:allow import cloud trigger publication"
fileprivate let userSubmitDoinglyMessage:String = "geMikezz house encounter separate protection"

/*: "type" :*/
fileprivate let app_accountHeContent:[Character] = ["t","y","p","e"]

/*: "toPosition" :*/
fileprivate let dreamWhichCommonTitle:String = "toPimplement span jaw tempt toward"
fileprivate let noti_denyData:String = "OSITION"

/*: "party:adminChangeMike" :*/
fileprivate let k_completeUrl:String = "pwindowt"
fileprivate let app_cloudValue:String = "inChpo tone manage container min"
fileprivate let k_makeStr:String = "conversion"
fileprivate let dreamKitPath:String = "public initial fun objectgeMike"

/*: "You were kicked off the seat" :*/
fileprivate let main_bubbleKey:String = "part trace hair supportive envelopeYou wer"
fileprivate let dream_candidateName:[Character] = ["e"," ","k"]
fileprivate let showScaleId:String = "skip player noteicked "
fileprivate let noti_identificationMessage:String = "e seatcomply chip hang"

/*: "party:changeCoin" :*/
fileprivate let kPoData:[Character] = ["p","a"]
fileprivate let user_grantProblemOurselvesStr:[Character] = ["r","t","y",":","c","h","a","n","g","e","C","o","i","n"]

/*: "party:closeStream" :*/
fileprivate let dreamComeCountHowFormat:String = "PART"
fileprivate let appPatentFormat:String = "team at sort star visitoreStream"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LanceManager.swift
//  Adjust
//
//  Created by DouXiu on 2024/9/12.
//

//: import ZegoExpressEngine
import ZegoExpressEngine

/// 语聊房状态变更通知
//: public let PARTY_STATUS_CHANGE_NOTIFICATION = NSNotification.Name(rawValue: "PARTY_STATUS_CHANGE_NOTIFICATION")
public let k_featureMinePagePath = NSNotification.Name(rawValue: String(bytes: app_businessValue.map{insertReach(communicate: $0)}, encoding: .utf8)!)
/// 麦位数
//: public let MIKE_SEAT_COUNT = 8
public let kViewDismissMarginPath = 8

/// 语聊房角色
//: enum VoiceRoomRole: Int {
enum LackNameConvertible: Int {
    //: case host = 1
    case host = 1 // 房主
    //: case audience = 2
    case audience = 2 // 观众
    //: case manager = 4
    case manager = 4 // 管理员
}

//: class TalkingVoiceRoomManager: NSObject {
class LanceManager: NSObject {
    /// 语聊房模型
    //: private(set) var partyModel = TalkingVoiceRoomModel()
    private(set) var partyModel = FairlyWarnTransformable()
    /// 是否正在语聊房中（状态变更时发送通知）
    //: private(set) var isParty: Bool = false {
    private(set) var isParty: Bool = false {
        //: didSet {
        didSet {
            //: NotificationCenter.default.post(name: PARTY_STATUS_CHANGE_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: k_featureMinePagePath, object: self)
        }
    }

    /// 是否小窗口模式
    //: private(set) var isSmallMode: Bool = false
    private(set) var isSmallMode: Bool = false
    /// singleton
    //: private static var _instance: TalkingVoiceRoomManager?
    private static var _instance: LanceManager?
    // 语聊房控制器
    //: private var voiceRoomVC: TalkingVoiceRoomViewController?
    private var voiceRoomVC: AccountAdviceViewController?
    /// 麦位信息字典 [麦位 0~7：麦位模型]
    //: private var mikePositionDict = [Int: TalkingMikeListItemModel]()
    private var mikePositionDict = [Int: GentleItemModel]()
    //: private let mplock = NSLock()
    private let mplock = NSLock()
    /// 正在进房请求中（防止多次调用）
    //: private var isEnterRoomReq = false
    private var isEnterRoomReq = false
    /// 麦克风操作请求中（防止多次调用）
    //: private var isMicReq = false
    private var isMicReq = false

    //: override init() {
    override init() {
        //: super.init()
        super.init()
        //: self.mikePositionDict = initMikePosition()
        self.mikePositionDict = conductPosition()
    }

    //: class func shared() -> TalkingVoiceRoomManager {
    class func doingResource() -> LanceManager {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingVoiceRoomManager()
            _instance = LanceManager()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }

    //: class func destroy() {
    class func dialogContrast() {
        //: if TalkingVoiceRoomManager._instance != nil {
        if LanceManager._instance != nil {
            //: TalkingVoiceRoomManager._instance = nil
            LanceManager._instance = nil
        }
    }

    // MARK: - Lazy Load

    // 语聊房小窗口悬浮窗
    //: private lazy var miniView: TalkingVoiceRoomMiniView = {
    private lazy var miniView: CommunicationView = {
        //: let mini = TalkingVoiceRoomMiniView.buildLiveMiniView()
        let mini = CommunicationView.untilSignal()
        //: mini.isHidden = true
        mini.isHidden = true
        //: mini.tapGestureBlock = { [weak self] in
        mini.tapGestureBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.voiceRoom_goback()
            self.media()
        }
        //: return mini
        return mini
        //: }()
    }()
}

// MARK: - 操作麦位请求

//: extension TalkingVoiceRoomManager {
extension LanceManager {
    /// 房主/观众 麦位操作请求
    /// - Parameters:
    ///   - type: 1 上麦，2 下麦，3 切麦，4 开麦，5 闭麦
    ///   - position: 麦位
    ///   - toPosition: 目标麦位（切麦传值）
    //: func voiceRoom_reqChangeMike(type: Int, position: Int, toPosition: Int? = nil) {
    func quantifiability(type: Int, position: Int, toPosition: Int? = nil) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_changeMike(roomId: partyModel.roomId,
        OnReqTool.recording(roomId: partyModel.roomId,
                                          //: uid: Int(AdministratorThen.share.loginUid) ?? 0,
                                          uid: Int(AdministratorThen.share.loginUid) ?? 0,
                                          //: type: type,
                                          type: type,
                                          //: position: position,
                                          position: position,
                                          //: toPos: toPosition)
                                          toPos: toPosition)
        //: { succeed, result, errorModel in
        { succeed, result, errorModel in
            //: self.isMicReq = false
            self.isMicReq = false
            //: guard succeed else {
            guard succeed else { // 失败提示
                //: if errorModel?.errorCode == 20001 {
                if errorModel?.errorCode == 20001 { // 已在麦位，开始推流，刷新UI
                    //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                    //: let streamID = self.voiceRoom_getStreamID()
                    let streamID = self.fact()
                    //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                    ZegoExpressEngine.shared().startPublishingStream(streamID)
                    //: guard let dict = result as? [String: Any] else { return }
                    guard let dict = result as? [String: Any] else { return }
                    //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: dict) else { return }
                    guard let mikeModel = GentleItemModel.deserialize(from: dict) else { return }
                    //: self.updateMikePosition(position: position, model: mikeModel)
                    self.seekScan(position: position, model: mikeModel)
                }
                //: return
                return
            }

            // 成功后只做逻辑处理，在接收信令处统一刷新UI
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                //: let streamID = self.voiceRoom_getStreamID()
                let streamID = self.fact()
                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                ZegoExpressEngine.shared().startPublishingStream(streamID)

            //: case 2:
            case 2: // 下麦成功
                //: ZegoExpressEngine.shared().stopPublishingStream()
                ZegoExpressEngine.shared().stopPublishingStream()

            //: case 3:
            case 3: // 切麦成功
                //: break
                break

            //: case 4:
            case 4: // 开麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦成功
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }
        }
    }

    /// 房主对他人操作请求
    /// - Parameters:
    ///   - uid: 用户Id
    ///   - type: 1 锁麦，2 解锁麦，3 踢下麦，4 开麦，5 闭麦
    ///   - position: 麦位
    //: func voiceRoom_reqAdminChangeMike(uid: Int, type: Int, position: Int) {
    func ballotDestroy(uid: Int, type: Int, position: Int) {
        //: guard isMicReq == false else { return }
        guard isMicReq == false else { return }
        //: isMicReq = true
        isMicReq = true
        //: TalkingVoiceRoomReqTool.req_adminChangeMike(roomId: partyModel.roomId,
        OnReqTool.threadFilter(roomId: partyModel.roomId,
                                             //: uid: uid,
                                             uid: uid,
                                             //: type: type,
                                             type: type,
                                             //: position: position)
                                             position: position)
        //: { _, _, _ in
        { _, _, _ in
            //: self.isMicReq = false
            self.isMicReq = false
        }
    }

    /// 获取推流Id
    //: private func voiceRoom_getStreamID() -> String {
    private func fact() -> String {
        //: return "p-\(AdministratorThen.share.loginUid)-\(partyModel.roomId)"
        return "p-\(AdministratorThen.share.loginUid)-\(partyModel.roomId)"
    }
}

// MARK: - 权限检测，开启/进入/切换 语聊房

//: extension TalkingVoiceRoomManager {
extension LanceManager {
    /// 检测状态并进入语聊房
    /// - Parameters:
    ///   - roomId: 房间Id（nil：创建房间；有值：进入房间）
    //: func voiceRoom_checkAndTurnOn(roomId: String? = nil) {
    func digitiser(roomId: String? = nil) {
        //: guard isParty == false else { return }
        guard isParty == false else { return }
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true

        // 1. 权限检测
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        AsidePermissionTool.openGold(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.iconThemeBringBackPlace(showMsg: dream_topKey)
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: return
                return
            }

            // 2. 接口是否禁播检测
            //: TalkingVoiceRoomReqTool.req_voiceRoomCheck(roomId: roomId) { succeed, result, errorModel in
            OnReqTool.aboutCompletion(roomId: roomId) { succeed, result, errorModel in
                //: self.isEnterRoomReq = false
                self.isEnterRoomReq = false
                //: guard succeed else {
                guard succeed else {
                    //: if errorModel?.errorCode == 302 {
                    if errorModel?.errorCode == 302 { // 失败，跳转H5
                        //: if let result = result as? [String: Any] {
                        if let result = result as? [String: Any] {
                            //: DumpPushManager.share.func__pushToWebVC(urlStr: result["redirectUrl"] as? String)
                            DumpPushManager.share.noneControlInstall(urlStr: result[(mainOldFemaleGirlValue.replacingOccurrences(of: "bronze", with: "r") + "rectUrl")] as? String)
                        }
                        //: } else if errorModel?.errorMsg.count ?? 0 > 0 {
                    } else if errorModel?.errorMsg.count ?? 0 > 0 {
                        //: self.func__showStatusBarErrorMsg(showMsg: errorModel!.errorMsg)
                        self.iconThemeBringBackPlace(showMsg: errorModel!.errorMsg)
                    }
                    //: return
                    return
                }

                // 3. 是否正在音视频通话中
                //: guard TalkingSocketManager.shared.isFind == false else {
                guard AdviceLandscapeReactiveCompatible.shared.isFind == false else {
                    //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
                    self.iconThemeBringBackPlace(showMsg: m_buildNearbyData)
                    //: return
                    return
                }

                // 4. 校验成功，解析模型
                //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
                guard let model = FairlyWarnTransformable.deserialize(from: result as? Dictionary) else {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 进房模型解析失败。")
                    ProblemBenchPoundThen.panelDetect(msg: (notiSoulValue.replacingOccurrences(of: "cast", with: "R") + dreamOffImpressionUrl.replacingOccurrences(of: "stretch", with: "M") + userTypicalLetterValue + "\u{89e3}析失败。"))
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.iconThemeBringBackPlace(showMsg: (String(main_radioMsg.prefix(7)) + String(main_animaRemainValue)).localized)
                    //: return
                    return
                }
                //: self.partyModel = model
                self.partyModel = model

                // 5. 创建语聊房UI，同时调用sdk创建房间
                //: self.voiceRoomVC = TalkingVoiceRoomViewController()
                self.voiceRoomVC = AccountAdviceViewController()
                //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
                self.thenController()?.pushViewController(self.voiceRoomVC!, animated: true)
                //: self.miniView.refreshMiniView()
                self.miniView.publishFile()
                //: self.miniView.isHidden = true
                self.miniView.isHidden = true
                //: self.voiceRoom_create()
                self.weightBridge()
                //: UIApplication.shared.isIdleTimerDisabled = true
                UIApplication.shared.isIdleTimerDisabled = true // 防止息屏
                //: self.isParty = true
                self.isParty = true

                // 6. 埋点
                //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
                k_hugeName.hour(eventID: "\(mainMatchNameData)_\(AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue ? String(bytes: kBossFormat.reversed(), encoding: .utf8)! : (String(show_ageKey) + String(appOvalMsg)))")

                // 7. 初始化弹幕群聊
                //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                ManageressReactiveCompatible.duringBalance(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                    //: if succeed == false {
                    if succeed == false {
                        //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊进房失败。groupId: \(self.partyModel.chatGroupId).")
                        ProblemBenchPoundThen.panelDetect(msg: (showSpendUrl.uppercased() + "EROOM:" + show_breadPath.replacingOccurrences(of: "demonstrate", with: "群") + "\u{623f}失败。g" + String(mFatalIdent.suffix(8))) + "\(self.partyModel.chatGroupId).")
                        //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                        self.iconThemeBringBackPlace(showMsg: (String(main_radioMsg.prefix(7)) + String(main_animaRemainValue)).localized)
                        //: self.voiceRoom_releaseAllResource()
                        self.turnChoose()
                    }
                }
            }
        }
    }

    /// 切换语聊房
    /// - Parameters:
    ///   - roomId: 当前进入房间Id
    ///   - beforeRoomId: 上一房间Id
    //: func voiceRoom_switch(roomId: String, beforeRoomId: String) {
    func reverseSpring(roomId: String, beforeRoomId: String) {
        //: guard isEnterRoomReq == false else { return }
        guard isEnterRoomReq == false else { return }
        //: isEnterRoomReq = true
        isEnterRoomReq = true
        // 1. 调用切房接口
        //: TalkingVoiceRoomReqTool.req_voiceRoomSwitch(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
        OnReqTool.faculty(roomId: roomId, beforeRoomId: beforeRoomId) { succeed, result, _ in
            //: self.isEnterRoomReq = false
            self.isEnterRoomReq = false
            //: guard succeed else { return }
            guard succeed else { return }
            // 2. 释放上个房间资源 (使用partyModel旧值)
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.mp()

            // 3. 主播切房，需要关闭底部tabbar动效 (使用partyModel旧值)
            //: if String(self.partyModel.streamerInfo.uid) == AdministratorThen.share.loginUid {
            if String(self.partyModel.streamerInfo.uid) == AdministratorThen.share.loginUid {
                //: self.isParty = false
                self.isParty = false
            }

            // 4. 校验成功，解析模型（给partyModel重新赋值）
            //: guard let model = TalkingVoiceRoomModel.deserialize(from: result as? Dictionary) else {
            guard let model = FairlyWarnTransformable.deserialize(from: result as? Dictionary) else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM: 切房模型解析失败。")
                ProblemBenchPoundThen.panelDetect(msg: (String(show_installId) + m_himKey.capitalized + String(k_commendPath)))
                //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                self.iconThemeBringBackPlace(showMsg: (String(main_radioMsg.prefix(7)) + String(main_animaRemainValue)).localized)
                //: return
                return
            }
            //: self.partyModel = model
            self.partyModel = model

            // 5. 创建语聊房UI，同时调用sdk切房
            //: self.voiceRoomVC = TalkingVoiceRoomViewController()
            self.voiceRoomVC = AccountAdviceViewController()
            //: self.getNavigationController()?.pushViewController(self.voiceRoomVC!, animated: true)
            self.thenController()?.pushViewController(self.voiceRoomVC!, animated: true)
            //: self.miniView.refreshMiniView()
            self.miniView.publishFile()
            //: self.miniView.isHidden = true
            self.miniView.isHidden = true
            //: self.voiceRoom_switch(beforeRoomId: beforeRoomId)
            self.forestFire(beforeRoomId: beforeRoomId)
            //: self.isParty = true
            self.isParty = true
            //: self.isSmallMode = false
            self.isSmallMode = false

            // 6. 埋点
            //: uploadRecord.uploadRecordEvent(eventID: "\(click_partyDetail_enter)_\(AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue ? "male" : "female")")
            k_hugeName.hour(eventID: "\(mainMatchNameData)_\(AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue ? String(bytes: kBossFormat.reversed(), encoding: .utf8)! : (String(show_ageKey) + String(appOvalMsg)))")

            // 7. 初始化弹幕群聊
            //: TalkingDanmuManager.joinLiveRoomDanmu(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
            ManageressReactiveCompatible.duringBalance(groupId: self.partyModel.chatGroupId, announcementMsg: self.partyModel.notification) { succeed in
                //: if succeed == false {
                if succeed == false {
                    //: UploadLogTool.writeLog(msg: "VOICEROOM: 群聊切房失败。groupId: \(self.partyModel.chatGroupId).")
                    ProblemBenchPoundThen.panelDetect(msg: (String(notiLastFormat) + notiTakeName.replacingOccurrences(of: "coal", with: "败") + "groupId: ") + "\(self.partyModel.chatGroupId).")
                    //: self.func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                    self.iconThemeBringBackPlace(showMsg: (String(main_radioMsg.prefix(7)) + String(main_animaRemainValue)).localized)
                    //: self.voiceRoom_releaseAllResource()
                    self.turnChoose()
                }
            }
        }
    }
}

// MARK: - Public Event

//: extension TalkingVoiceRoomManager {
extension LanceManager {
    /// 最小化语聊房
    //: func voiceRoom_mini() {
    func firstMini() {
        //: isSmallMode = true
        isSmallMode = true
        //: miniView.isHidden = false
        miniView.isHidden = false
        //: voiceRoomVC?.popCurrentViewController()
        voiceRoomVC?.tinkle()
    }

    /// 返回语聊房
    //: func voiceRoom_goback() {
    func media() {
        //: isSmallMode = false
        isSmallMode = false
        //: miniView.isHidden = true
        miniView.isHidden = true
        //: guard voiceRoomVC != nil else { return }
        guard voiceRoomVC != nil else { return }
        //: guard let currentVC = currentViewController() else { return }
        guard let currentVC = springEquinox() else { return }
        // 如果顶部视图是私信页，先返回列表，避免多个私信页消息同步问题
        //: if currentVC.isKind(of: TalkingPrivateChatController.self) {
        if currentVC.isKind(of: PurchaseViewController.self) {
            //: if var vcArr = currentVC.navigationController?.viewControllers {
            if var vcArr = currentVC.navigationController?.viewControllers {
                //: vcArr.removeLast()
                vcArr.removeLast()
                //: vcArr.append(voiceRoomVC!)
                vcArr.append(voiceRoomVC!)
                //: currentVC.navigationController?.setViewControllers(vcArr, animated: true)
                currentVC.navigationController?.setViewControllers(vcArr, animated: true)
            }
            //: } else {
        } else {
            //: currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
            currentVC.navigationController?.pushViewController(voiceRoomVC!, animated: true)
        }
    }

    /// 销毁语聊房所有资源
    //: func voiceRoom_releaseAllResource() {
    func turnChoose() {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.isParty = false
            self.isParty = false // 有通知监听，需置为false
            //: UIApplication.shared.isIdleTimerDisabled = false
            UIApplication.shared.isIdleTimerDisabled = false // 恢复息屏
            //: self.miniView.removeFromSuperview()
            self.miniView.removeFromSuperview() // 移除小窗口
            //: self.voiceRoomVC?.releaseAllResourceAndPop()
            self.voiceRoomVC?.mp() // 将语聊房视图从栈中移除并释放资源
            //: if self.partyModel.position >= 0 {
            if self.partyModel.position >= 0 { // 退房下麦
                //: self.voiceRoom_reqChangeMike(type: 2, position: self.partyModel.position)
                self.quantifiability(type: 2, position: self.partyModel.position)
            }
            //: ZegoExpressEngine.shared().logoutRoom()
            ZegoExpressEngine.shared().logoutRoom() // 退出房间
            //: ZegoExpressEngine.destroy(nil)
            ZegoExpressEngine.destroy(nil) // 销毁引擎
            //: TalkingVoiceRoomManager.destroy()
            LanceManager.dialogContrast() // 销毁当前单例
        }
    }
}

// MARK: - Private Event

//: extension TalkingVoiceRoomManager {
extension LanceManager {
    /// 创建语聊房
    //: private func voiceRoom_create() {
    private func weightBridge() {
        // 创建引擎
        //: let profile = ZegoEngineProfile()
        let profile = ZegoEngineProfile()
        //: profile.appID = CreditsThen.getVoiceRoomAppId()
        profile.appID = CreditsThen.topHolderEach()
        //: profile.scenario = .standardChatroom
        profile.scenario = .standardChatroom
        //: ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        ZegoExpressEngine.createEngine(with: profile, eventHandler: self)
        // 进阶配置，进入房间
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: let user = ZegoUser(userID: AdministratorThen.share.loginUid)
        let user = ZegoUser(userID: AdministratorThen.share.loginUid)
        //: ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
        ZegoExpressEngine.shared().loginRoom(partyModel.roomId, user: user, config: config)
    }

    /// 切换语聊房
    /// - Parameter beforeRoomId: 上一房间Id
    //: private func voiceRoom_switch(beforeRoomId: String) {
    private func forestFire(beforeRoomId: String) {
        //: let config = ZegoRoomConfig()
        let config = ZegoRoomConfig()
        //: config.token = partyModel.token
        config.token = partyModel.token
        //: ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
        ZegoExpressEngine.shared().switchRoom(beforeRoomId, toRoomID: partyModel.roomId, config: config)
    }
}

// MARK: - ZegoEventHandler

//: extension TalkingVoiceRoomManager: ZegoEventHandler {
extension LanceManager: ZegoEventHandler {
    // MARK: - 进房/切房 监听回调

    /// 进房/切房 回调
    //: func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
    func onRoomStateChanged(_ reason: ZegoRoomStateChangedReason, errorCode: Int32, extendedData: [AnyHashable: Any], roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: if errorCode == 0, reason == .logined {
        if errorCode == 0, reason == .logined { // 进房成功
            // 启动声浪等级监控
            //: ZegoExpressEngine.shared().startSoundLevelMonitor()
            ZegoExpressEngine.shared().startSoundLevelMonitor()
            // 开启音量稳定控制
            //: ZegoExpressEngine.shared().enableAEC(true)
            ZegoExpressEngine.shared().enableAEC(true)
            // 获取嘉宾列表接口
            //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
            OnReqTool.bringForthCompletion(roomId: partyModel.roomId) { list in
                // 保存麦位列表信息
                //: for (key, value) in list.enumerated() {
                for (key, value) in list.enumerated() {
                    //: self.modifyMikePosition(key: key, model: value)
                    self.midnight(key: key, model: value)
                }
                // 刷新麦位列表UI
                //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                self.voiceRoomVC?.mikePositionView.romaineLettuce()
                // 房主首次创建房间，主动上麦
                //: if self.partyModel.userRole == VoiceRoomRole.host.rawValue, self.partyModel.isCreate == true {
                if self.partyModel.userRole == LackNameConvertible.host.rawValue, self.partyModel.isCreate == true {
                    //: self.voiceRoom_reqChangeMike(type: 1, position: 0)
                    self.quantifiability(type: 1, position: 0)
                }
            }

            //: } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
        } else if reason == .logining || reason == .reconnectFailed || reason == .reconnecting || reason == .reconnected {
            //: if errorCode == 0, reason == .reconnected {
            if errorCode == 0, reason == .reconnected { // 重连成功
                //: TalkingVoiceRoomReqTool.req_getMikeList(roomId: partyModel.roomId) { list in
                OnReqTool.bringForthCompletion(roomId: partyModel.roomId) { list in
                    // 更新麦位列表信息
                    //: for (key, value) in list.enumerated() {
                    for (key, value) in list.enumerated() {
                        //: self.modifyMikePosition(key: key, model: value)
                        self.midnight(key: key, model: value)
                        //: if String(value.uid) == AdministratorThen.share.loginUid {
                        if String(value.uid) == AdministratorThen.share.loginUid {
                            //: if value.mikeStatus == 1 {
                            if value.mikeStatus == 1 { // 房主闭麦
                                //: ZegoExpressEngine.shared().stopPublishingStream()
                                ZegoExpressEngine.shared().stopPublishingStream()
                                //: } else {
                            } else {
                                //: let streamID = self.voiceRoom_getStreamID()
                                let streamID = self.fact()
                                //: ZegoExpressEngine.shared().startPublishingStream(streamID)
                                ZegoExpressEngine.shared().startPublishingStream(streamID)
                                //: let isMute = value.mikeStatus == 0 ? true : false
                                let isMute = value.mikeStatus == 0 ? true : false
                                //: ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                                ZegoExpressEngine.shared().mutePublishStreamAudio(isMute)
                            }
                        }
                    }
                    // 刷新麦位列表UI
                    //: self.voiceRoomVC?.mikePositionView.refreshAllMikeView()
                    self.voiceRoomVC?.mikePositionView.romaineLettuce()
                }
                //: } else if errorCode != 0 {
            } else if errorCode != 0 {
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                k_hugeName.assetClothe(eventID: show_eventId, parameterStr: [String(bytes: mainSubmitMessage.map{addressFore(raw: $0)}, encoding: .utf8)!: String(errorCode)])
            }

            //: } else {
        } else { // 进房失败
            //: if errorCode == 1002055 || errorCode == 1002050 {
            if errorCode == 1_002_055 || errorCode == 1_002_050 { // 用户退出 || 被踢出房间
                //: let json = JSON(extendedData)
                let json = JSON(extendedData)
                //: let value = json["custom_kickout_message"].stringValue
                let value = json[(String(notiButteIdent) + String(mTickName))].stringValue
                //: if value == "closeRoom" || value == "adminCloseRoom" { // 关闭房间
                if value == (String(showPersonGuiltyPath.suffix(7)) + kLetterLeadingId.replacingOccurrences(of: "resource", with: "m")) || value == (dreamPlanSupportiveFormat.replacingOccurrences(of: "gift", with: "a") + String(noti_staffId.prefix(3)) + String(m_componentIdent.prefix(6))) { // 关闭房间
                    //: voiceRoom_releaseAllResource()
                    turnChoose()
                    //: let config = ShowAlertConfig()
                    let config = GainGageAlertConfig()
                    //: config.alignment = .center
                    config.alignment = .center
                    //: TalkingAlertShow.customAlert(message: "The room is closed, please go to another room".localized, rightBtnTitle: "OK".localized, rightBlock: {
                    GentleAlertShow.begNativeConfig(message: String(bytes: showDenyKey.map{egoForward(closed: $0)}, encoding: .utf8)!.localized, rightBtnTitle: "OK".localized, rightBlock: {
                        // 跳转到首页tab-party
                        //: NotificationCenter.default.post(name: SWITCH_TABBAR_HOME_PARTY_NOTIFICATION, object: nil, userInfo: nil)
                        NotificationCenter.default.post(name: m_endLimitMsg, object: nil, userInfo: nil)
                        //: }, config: config)
                    }, config: config)

                    //: } else {
                } else { // 被踢出房间
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked out of the room".localized)
                    iconThemeBringBackPlace(showMsg: String(bytes: noti_additionalName.map{dynamicsRole(interrupt: $0)}, encoding: .utf8)!.localized)
                }

                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "VOICEROOM：进房失败，roomID: \(roomID), error: \(errorCode), reason: \(reason), extendedData: \(extendedData).")
                ProblemBenchPoundThen.panelDetect(msg: (showBuryStackText.replacingOccurrences(of: "clothe", with: "O") + String(kLiteralContent)) + "\(roomID)" + (String(mWitchData) + String(noti_siblingValue.prefix(6))) + "\(errorCode)" + (String(k_okRiverValue) + String(mRitePleaseValue)) + "\(reason)" + (String(dream_signalIdent) + String(main_patientConFormat.prefix(4)) + "ata: ") + "\(extendedData).")
                //: func__showStatusBarErrorMsg(showMsg: "Party room exception".localized)
                iconThemeBringBackPlace(showMsg: (String(main_radioMsg.prefix(7)) + String(main_animaRemainValue)).localized)
                // 进房失败埋点
                //: uploadRecord.uploadRecordEvent(eventID: click_partyTab_creParty_fail, parameterStr: ["errCode": String(errorCode)])
                k_hugeName.assetClothe(eventID: show_eventId, parameterStr: [String(bytes: mainSubmitMessage.map{addressFore(raw: $0)}, encoding: .utf8)!: String(errorCode)])
            }
            //: voiceRoom_releaseAllResource()
            turnChoose()
        }
    }

    // MARK: - 推流回调

    /// 当前用户推流状态回调
    //: func onPublisherStateUpdate(_ state: ZegoPublisherState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPublisherStateUpdate(_: ZegoPublisherState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 || errorCode == 1003020 || errorCode == 1002055 else {
        guard errorCode == 0 || errorCode == 1_003_020 || errorCode == 1_002_055 else { // 推流失败，用户下麦
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 当前用户推流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            ProblemBenchPoundThen.panelDetect(msg: (user_soundId.replacingOccurrences(of: "role", with: "O") + "OOM:" + noti_motivationData.capitalized + app_textName + "流失败，" + noti_lieFormat.replacingOccurrences(of: "stand", with: "o") + "ID: ") + "\(partyModel.roomId)" + (String(notiSoleMsg.prefix(4)) + "rorCo" + String(dream_aliveUrl.prefix(4))) + "\(errorCode)" + (String(mMaximumUrl.suffix(6)) + String(app_okName)) + "\(streamID).")
            //: func__showStatusBarErrorMsg(showMsg: "An error occurred, please try again".localized)
            iconThemeBringBackPlace(showMsg: String(bytes: userWeightContent.map{$0^181}, encoding: .utf8)!.localized)
            //: let dict = getAllMikePositions()
            let dict = buttonPositions()
            //: for (position, model) in dict where String(model.uid) == AdministratorThen.share.loginUid {
            for (position, model) in dict where String(model.uid) == AdministratorThen.share.loginUid {
                //: voiceRoom_reqChangeMike(type: 2, position: position)
                quantifiability(type: 2, position: position)
                //: resetMikePosition(position: position, model: model)
                result(position: position, model: model)
            }
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                turnChoose()
            }
            //: return
            return
        }
    }

    // MARK: - 拉流回调

    /// 当前用户拉流状态回调
    //: func onPlayerStateUpdate(_ state: ZegoPlayerState, errorCode: Int32, extendedData: [AnyHashable: Any]?, streamID: String) {
    func onPlayerStateUpdate(_: ZegoPlayerState, errorCode: Int32, extendedData _: [AnyHashable: Any]?, streamID: String) {
        //: guard errorCode == 0 else {
        guard errorCode == 0 else { // 拉流失败
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 拉流失败，roomID: \(partyModel.roomId), errorCode: \(errorCode), streamID: \(streamID).")
            ProblemBenchPoundThen.panelDetect(msg: (appAmberMessage.replacingOccurrences(of: "tolerance", with: "IC") + "EROO" + String(appResponseKey.prefix(6)) + "败，ro" + String(kHoweverId.suffix(4)) + String(kCurrentlyMsg)) + "\(partyModel.roomId)" + (String(notiSoleMsg.prefix(4)) + "rorCo" + String(dream_aliveUrl.prefix(4))) + "\(errorCode)" + (String(mMaximumUrl.suffix(6)) + String(app_okName)) + "\(streamID).")
            //: if errorCode == 1002078 {
            if errorCode == 1_002_078 { // token过期，关闭房间
                //: voiceRoom_releaseAllResource()
                turnChoose()
            }

            //: return
            return
        }
    }

    // MARK: - 流更新回调

    /// 流更新回调
    //: func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData: [AnyHashable: Any]?, roomID: String) {
    func onRoomStreamUpdate(_ updateType: ZegoUpdateType, streamList: [ZegoStream], extendedData _: [AnyHashable: Any]?, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: switch updateType {
        switch updateType {
        //: case .add:
        case .add:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 开始拉流
                //: ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().startPlayingStream(zego.streamID)
            }

        //: case .delete:
        case .delete:
            //: streamList.forEach { zego in
            streamList.forEach { zego in
                // 停止拉流
                //: ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
                ZegoExpressEngine.shared().stopPlayingStream(zego.streamID)
            }

        //: @unknown default:
        @unknown default:
            //: fatalError()
            fatalError()
        }
    }

    // MARK: - 声浪等级监听回调

    /// 本地声浪等级
    //: func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
    func onCapturedSoundLevelUpdate(_ soundLevel: NSNumber) {
        //: guard Int(truncating: soundLevel) > 0 else { return }
        guard Int(truncating: soundLevel) > 0 else { return }
        //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: partyModel.position, level: soundLevel)
        voiceRoomVC?.mikePositionView.ownInvitationWindow(position: partyModel.position, level: soundLevel)
    }

    /// 远端声浪等级
    //: func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
    func onRemoteSoundLevelUpdate(_ soundLevels: [String: NSNumber]) {
        //: let dict = getAllMikePositions()
        let dict = buttonPositions()
        //: for (streamID, level) in soundLevels {
        for (streamID, level) in soundLevels {
            //: let components = streamID.split(separator: "-").map(String.init)
            let components = streamID.split(separator: "-").map(String.init)
            //: guard components.count > 2 else { continue }
            guard components.count > 2 else { continue }
            //: if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
            if let position = dict.first(where: { String($0.value.uid) == components[1] })?.key {
                //: voiceRoomVC?.mikePositionView.refreshSoundLevelAnimation(position: position, level: level)
                voiceRoomVC?.mikePositionView.ownInvitationWindow(position: position, level: level)
            }
        }
    }

    // MARK: - 信令

    /// 上麦、下麦、锁麦、解锁麦、切麦操作会收到后端下发信令
    //: func onIMRecvCustomCommand(_ command: String, from fromUser: ZegoUser, roomID: String) {
    func onIMRecvCustomCommand(_ command: String, from _: ZegoUser, roomID: String) {
        //: guard roomID == partyModel.roomId else { return }
        guard roomID == partyModel.roomId else { return }
        //: guard let decodedStr = command.removingPercentEncoding else {
        guard let decodedStr = command.removingPercentEncoding else {
            //: UploadLogTool.writeLog(msg: "VOICEROOM: 信令解析失败，command: \(command), roomID: \(roomID).")
            ProblemBenchPoundThen.panelDetect(msg: (main_involveForestPath.replacingOccurrences(of: "conduct", with: "OI") + "OOM: 信令解" + String(userAcrossBalloonValue)) + "\(command)" + (String(notiLapKey.prefix(6)) + "ID: ") + "\(roomID).")
            //: return
            return
        }
        // 更新麦位信息，刷新列表UI
        //: let json = JSON(parseJSON: decodedStr)
        let json = JSON(parseJSON: decodedStr)
        //: let position = json["data"]["position"].intValue
        let position = json[(String(notiWifeStr))][(String(user_allIdent) + dream_arrowIdent.replacingOccurrences(of: "move", with: "n"))].intValue
        //: guard position < MIKE_SEAT_COUNT else { return }
        guard position < kViewDismissMarginPath else { return }
        //: guard let mikeModel = TalkingMikeListItemModel.deserialize(from: json["data"].dictionaryObject) else { return }
        guard let mikeModel = GentleItemModel.deserialize(from: json[(String(notiWifeStr))].dictionaryObject) else { return }
        //: printLog(message: "xxxx-- \(json.rawValue)")
        printLog(message: (String(dream_termsId.suffix(7))) + "\(json.rawValue)")
        //: if json["opType"].stringValue == "party:changeMike" { // 房主/观众 麦位操作信令
        if json[(String(app_rewardKey))].stringValue == (String(m_towerValue.prefix(6)) + "chan" + String(userSubmitDoinglyMessage.prefix(6))) { // 房主/观众 麦位操作信令
            //: let type = json["data"]["type"].intValue
            let type = json[(String(notiWifeStr))][(String(app_accountHeContent))].intValue
            //: switch type {
            switch type {
            //: case 1:
            case 1: // 上麦
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: position, model: mikeModel)
                seekScan(position: position, model: mikeModel)

            //: case 2:
            case 2: // 下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = trademark(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    result(position: position, model: oldModel)
                }

            //: case 3:
            case 3: // 切麦
                //: resetMikePosition(position: position, model: mikeModel)
                result(position: position, model: mikeModel)
                //: let toPosition = json["data"]["toPosition"].intValue
                let toPosition = json[(String(notiWifeStr))][(String(dreamWhichCommonTitle.prefix(3)) + noti_denyData.lowercased())].intValue
                //: mikeModel.needGift = true
                mikeModel.needGift = true
                //: updateMikePosition(position: toPosition, model: mikeModel)
                seekScan(position: toPosition, model: mikeModel)

            //: case 4, 5:
            case 4, 5: // 开麦、闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                seekScan(position: position, model: mikeModel)

            //: default:
            default:
                //: break
                break
            }
            // 刷新礼物面板头像
            //: if type == 1 || type == 2 || type == 3 {
            if type == 1 || type == 2 || type == 3 {
                //: voiceRoomVC?.bottomView.refreshGiftIcon()
                voiceRoomVC?.bottomView.mEnd()
            }

            //: } else if json["opType"].stringValue == "party:adminChangeMike" { // 房主对他人操作信令
        } else if json[(String(app_rewardKey))].stringValue == (k_completeUrl.replacingOccurrences(of: "window", with: "ar") + "y:adm" + String(app_cloudValue.prefix(4)) + k_makeStr.replacingOccurrences(of: "conversion", with: "an") + String(dreamKitPath.suffix(6))) { // 房主对他人操作信令
            //: switch json["data"]["type"].intValue {
            switch json[(String(notiWifeStr))][(String(app_accountHeContent))].intValue {
            //: case 1, 2:
            case 1, 2: // 锁麦、解锁麦
                //: updateMikePosition(position: position, model: mikeModel)
                seekScan(position: position, model: mikeModel)

            //: case 3:
            case 3: // 踢下麦
                //: if let oldModel = getMikePosition(key: position) {
                if let oldModel = trademark(key: position) {
                    //: resetMikePosition(position: position, model: oldModel)
                    result(position: position, model: oldModel)
                    // 刷新礼物面板头像
                    //: voiceRoomVC?.bottomView.refreshGiftIcon()
                    voiceRoomVC?.bottomView.mEnd()
                    //: guard String(oldModel.uid) == AdministratorThen.share.loginUid else { return }
                    guard String(oldModel.uid) == AdministratorThen.share.loginUid else { return }
                    //: ZegoExpressEngine.shared().stopPublishingStream()
                    ZegoExpressEngine.shared().stopPublishingStream()
                    //: func__showStatusBarErrorMsg(showMsg: "You were kicked off the seat".localized)
                    iconThemeBringBackPlace(showMsg: (String(main_bubbleKey.suffix(7)) + String(dream_candidateName) + String(showScaleId.suffix(6)) + "off th" + String(noti_identificationMessage.prefix(6))).localized)
                }

            //: case 4:
            case 4: // 开麦
                //: updateMikePosition(position: position, model: mikeModel)
                seekScan(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == AdministratorThen.share.loginUid else { return }
                guard String(mikeModel.uid) == AdministratorThen.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(false)
                ZegoExpressEngine.shared().mutePublishStreamAudio(false)

            //: case 5:
            case 5: // 闭麦
                //: updateMikePosition(position: position, model: mikeModel)
                seekScan(position: position, model: mikeModel)
                //: guard String(mikeModel.uid) == AdministratorThen.share.loginUid else { return }
                guard String(mikeModel.uid) == AdministratorThen.share.loginUid else { return }
                //: ZegoExpressEngine.shared().mutePublishStreamAudio(true)
                ZegoExpressEngine.shared().mutePublishStreamAudio(true)

            //: default:
            default:
                //: break
                break
            }

            //: } else if json["opType"].stringValue == "party:changeCoin" { // 更新用户麦位金币
        } else if json[(String(app_rewardKey))].stringValue == (String(kPoData) + String(user_grantProblemOurselvesStr)) { // 更新用户麦位金币
            //: TalkingVoiceRoomManager.shared().getAllMikePositions().forEach { position, model in
            LanceManager.doingResource().buttonPositions().forEach { position, model in
                //: if model.uid == mikeModel.uid {
                if model.uid == mikeModel.uid {
                    //: model.coin = mikeModel.coin
                    model.coin = mikeModel.coin
                    //: updateMikePosition(position: position, model: model)
                    seekScan(position: position, model: model)
                    //: return
                    return
                }
            }

            //: } else if json["opType"].stringValue == "party:closeStream" { // 推流失败
        } else if json[(String(app_rewardKey))].stringValue == (dreamComeCountHowFormat.lowercased() + "y:clos" + String(appPatentFormat.suffix(7))) { // 推流失败
            //: ZegoExpressEngine.shared().stopPublishingStream()
            ZegoExpressEngine.shared().stopPublishingStream()
        }
    }
}

// MARK: - 信令处理

//: extension TalkingVoiceRoomManager {
extension LanceManager {
    /// 更新麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func updateMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func seekScan(position: Int, model: GentleItemModel) {
        //: modifyMikePosition(key: position, model: model)
        midnight(key: position, model: model)
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.makeupLocal(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        port(uid: model.uid)
    }

    /// 重置麦位数据，刷新UI
    /// - Parameters:
    ///   - position: 麦位
    ///   - model: 数据
    //: private func resetMikePosition(position: Int, model: TalkingMikeListItemModel) {
    private func result(position: Int, model: GentleItemModel) {
        //: modifyMikePosition(key: position, model: TalkingMikeListItemModel())
        midnight(key: position, model: GentleItemModel())
        //: voiceRoomVC?.mikePositionView.refreshSingleMikeView(position: position)
        voiceRoomVC?.mikePositionView.makeupLocal(position: position)
        //: updateBottomMicStatusIfCurrentUser(uid: model.uid)
        port(uid: model.uid)
    }

    /// 更新本人麦位、底部麦克风状态
    /// - Parameter uid: 用户Id
    //: private func updateBottomMicStatusIfCurrentUser(uid: Int) {
    private func port(uid: Int) {
        //: guard String(uid) == AdministratorThen.share.loginUid else { return }
        guard String(uid) == AdministratorThen.share.loginUid else { return }
        //: let dict = getAllMikePositions()
        let dict = buttonPositions()
        //: if let position = dict.first(where: { $0.value.uid == uid })?.key {
        if let position = dict.first(where: { $0.value.uid == uid })?.key {
            //: partyModel.position = position
            partyModel.position = position
            //: } else {
        } else {
            //: partyModel.position = -1
            partyModel.position = -1
        }
        //: voiceRoomVC?.bottomView.refreshBottomMicStatus()
        voiceRoomVC?.bottomView.undersurface()
    }
}

// MARK: - 麦位字典安全操作

//: extension TalkingVoiceRoomManager {
extension LanceManager {
    /// 获取初始化模型
    //: private func initMikePosition() -> [Int: TalkingMikeListItemModel] {
    private func conductPosition() -> [Int: GentleItemModel] {
        //: for index in 0 ..< MIKE_SEAT_COUNT {
        for index in 0 ..< kViewDismissMarginPath {
            //: modifyMikePosition(key: index, model: TalkingMikeListItemModel())
            midnight(key: index, model: GentleItemModel())
        }
        //: return mikePositionDict
        return mikePositionDict
    }

    /// 添加/修改 麦位
    //: func modifyMikePosition(key: Int, model: TalkingMikeListItemModel) {
    func midnight(key: Int, model: GentleItemModel) {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: mikePositionDict[key] = model
        mikePositionDict[key] = model
    }

    /// 获取麦位
    //: func getMikePosition(key: Int) -> TalkingMikeListItemModel? {
    func trademark(key: Int) -> GentleItemModel? {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict[key]
        return mikePositionDict[key]
    }

    /// 获取所有麦位
    //: func getAllMikePositions() -> [Int: TalkingMikeListItemModel] {
    func buttonPositions() -> [Int: GentleItemModel] {
        //: mplock.lock()
        mplock.lock()
        //: defer { mplock.unlock() }
        defer { mplock.unlock() }
        //: return mikePositionDict
        return mikePositionDict
    }
}
