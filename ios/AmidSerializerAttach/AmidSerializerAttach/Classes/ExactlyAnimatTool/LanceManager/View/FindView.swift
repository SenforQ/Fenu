
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let user_errData:[UInt8] = [0x91,0x96,0x91,0x9c,0x50,0x8b,0x97,0x8c,0x8d,0x9a,0x62,0x51,0x48,0x90,0x89,0x9b,0x48,0x96,0x97,0x9c,0x48,0x8a,0x8d,0x8d,0x96,0x48,0x91,0x95,0x98,0x94,0x8d,0x95,0x8d,0x96,0x9c,0x8d,0x8c]

fileprivate func foreheadWithout(dream num: UInt8) -> UInt8 {
    let value = Int(num) - 40
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Followers" :*/
fileprivate let dream_quitId:String = "Follolabel wage self reply eastern"
fileprivate let noti_selectedIdent:String = "wcastrs"

/*: "Following" :*/
fileprivate let main_trainTitle:[Character] = ["F","o"]
fileprivate let appSensorId:[Character] = ["l","l","o","w","i","n","g"]

/*: "#E6E6E6" :*/
fileprivate let appAvailableName:String = "recording sometime#E6E6E6"

/*: "btn_message_more" :*/
fileprivate let k_jumpSpreadDistantPath:[Character] = ["b","t","n","_","m","e","s","s","a","g","e","_","m","o","r","e"]

/*: "UID:  :*/
fileprivate let dreamGoAnalysisMessage:String = "UID: against news"

/*: "level_ :*/
fileprivate let user_littleSheFinalMessage:String = "level_conversion find carrier"

/*: "Gift" :*/
fileprivate let dream_giftPath:[UInt8] = [0x10,0x3e,0x31,0x23]

/*: "Mike" :*/
fileprivate let m_blackMessage:[UInt8] = [0x3d,0x19,0x1b,0x15]

/*: "Follow" :*/
fileprivate let noti_countWrittenContent:[UInt8] = [0x77,0x6f,0x6c,0x6c,0x6f,0x46]

/*: "gift_voice_card" :*/
fileprivate let userSocietyValue:[Character] = ["g","i","f","t","_","v","o","i"]
fileprivate let mainCommentOwnData:[Character] = ["c","e","_","c","a","r","d"]

/*: "btn_party_voice_nor" :*/
fileprivate let kEveryoneIdent:[Character] = ["b","t","n","_","p","a","r","t","y","_","v","o","i","c","e"]
fileprivate let mGreenRemainMessage:String = "third light_nor"

/*: "btn_party_voice_pre" :*/
fileprivate let main_drawingName:String = "btn_unit background"
fileprivate let mWrittenContent:String = "advertising artifact establish traffic writey_voic"

/*: "attentionUid" :*/
fileprivate let k_withPath:String = "coordinatortent"

/*: "source" :*/
fileprivate let show_historyFormat:String = "sourcshare"

/*: "Kick out" :*/
fileprivate let k_iconKey:String = "clothe reserve communicationKick ou"
fileprivate let dream_fullStr:[Character] = ["t"]

/*: "Unmute" :*/
fileprivate let kConstraintId:String = "Unmuteicon lite"

/*: "Mute" :*/
fileprivate let app_calculateSubTitle:String = "Muteobserve depth resign dismiss"

/*: "Kick off" :*/
fileprivate let userNurseMessage:String = "Kick oauthority burn spring"
fileprivate let user_highlightStr:[Character] = ["f","f"]

/*: "Block" :*/
fileprivate let mBelowUrl:[Character] = ["B","l","o","c","k"]

/*: "Report" :*/
fileprivate let main_sameValue:String = "later likeReport"

/*: "#F6F6F6" :*/
fileprivate let noti_lookWomanFormat:String = "ref array transfer auto pin#F6F6F6"

/*: "Confirm to kick out the room?" :*/
fileprivate let show_prisonItTitle:[Character] = ["C","o","n"]
fileprivate let app_accelerateName:String = "who limited uniformfirm t"
fileprivate let show_originalMessage:String = "k outsuccess next"
fileprivate let m_tickValue:String = "load rear log weekoom?"

/*: "Cancel" :*/
fileprivate let dreamOverContent:String = "statement heavy err condition letCancel"

/*: "Sure" :*/
fileprivate let m_quietValue:String = "Surelip little sweet"

/*: "Are you sure you want to cancel the mute?" :*/
fileprivate let show_backData:[UInt8] = [0x8b,0xb8,0xaf,0xea,0xb3,0xa5,0xbf,0xea,0xb9,0xbf,0xb8,0xaf,0xea,0xb3,0xa5,0xbf,0xea,0xbd,0xab,0xa4,0xbe,0xea,0xbe,0xa5,0xea,0xa9,0xab,0xa4,0xa9,0xaf,0xa6,0xea,0xbe,0xa2,0xaf,0xea,0xa7,0xbf,0xbe,0xaf,0xf5]

private func sumerruptStopWhole(beyond num: UInt8) -> UInt8 {
    return num ^ 202
}

/*: "Confirm" :*/
fileprivate let mInstancePath:String = "occur"
fileprivate let mainArrayData:String = "olightfirm"

/*: "The mute has been lifted~" :*/
fileprivate let m_givenData:[Character] = ["T","h","e"," ","m","u","t","e"," ","h","a","s"," "]
fileprivate let notiHolderFormat:[Character] = ["b","e","e","n"," ","l","i"]
fileprivate let dream_amName:String = "fted~session retain delay answer between"

/*: "Confirm to kick off the seat?" :*/
fileprivate let showInsideCulturalName:[Character] = ["C","o","n","f","i","r","m"]
fileprivate let appStatUrl:String = " to index simultaneously track medium"
fileprivate let dream_ballotMsg:String = "interested salt successfuloff th"

/*: "Shielding Success" :*/
fileprivate let dreamPageName:String = "Shieldicap data"
fileprivate let showMeritTitle:String = "ng Susend talk mode"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FindView.swift
//  AbroadTalking
//

//: import UIKit
import UIKit

//: @objc protocol TalkingVoiceRoomCardViewDelegate: NSObjectProtocol {
@objc protocol SampleObjectProtocol: NSObjectProtocol {
    // @
    //: func func__atUserClick(uid: String, nickname: String)
    func bubbleFeedOn(uid: String, nickname: String)

    //: func func__giftUserClick(uid: String)
    func exhaustSystem(uid: String)

    //: func func__muteUserMickClick(uid: String, mikeStatus: Int, position: Int)
    func talkDown(uid: String, mikeStatus: Int, position: Int)

    //: func func__followUserSucess(uid: String)
    func system(uid: String)
}

//: class TalkingVoiceRoomCardView: UIView {
class FindView: UIView {
    //: var popView: TalkingPopView?
    var popView: AsideView?
    //: var data =  [String: Any]()
    var data = [String: Any]()
    //: var type: LiveUserCardType?
    var type: QuickStrideable?
    //: var uid = ""
    var uid = ""
    //: var liveUserId = String(TalkingVoiceRoomManager.shared().partyModel.streamerInfo.uid)
    var liveUserId = String(LanceManager.doingResource().partyModel.streamerInfo.uid) // 主播ID

    //: var userModel = TalkingUserInfoModel.init()
    var userModel = GatherTransformable()

    //: open weak var delegate: TalkingVoiceRoomCardViewDelegate?
    open weak var delegate: SampleObjectProtocol?

    //: init(frame: CGRect, uid: String) {
    init(frame: CGRect, uid: String) {
        //: super.init(frame: frame)
        super.init(frame: frame)

        //: if uid == AdministratorThen.share.loginUserMode.userID {
        if uid == AdministratorThen.share.loginUserMode.userID {
            //: self.type = .user_me
            self.type = .user_me
            //: } else {
        } else {
            //: self.type = .user_other
            self.type = .user_other
        }
        //: self.uid = uid
        self.uid = uid
        //: setupSubviews()
        borderSetup()
        //: setupSubViewsConstraint()
        extraOf()
        //: reqUserCardInfo()
        teratogen()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: user_errData.map{foreheadWithout(dream: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {}

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: return view
        return view
        //: }()
    }()

    //: lazy var uidLb: UILabel = {
    lazy var uidLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 12)
        label.font = .afterDisable(type: .Regular, fontSize: 12)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.fluent()
        //: return label
        return label
        //: }()
    }()

    //: lazy var topIcon: UIButton = {
    lazy var topIcon: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(iconBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(cameraBuild), for: .touchUpInside)
        //: btn.layer.cornerRadius = 40
        btn.layer.cornerRadius = 40
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.layer.borderWidth = 2
        btn.layer.borderWidth = 2
        //: btn.layer.borderColor = UIColor.white.cgColor
        btn.layer.borderColor = UIColor.white.cgColor
        //: return btn
        return btn

        //: }()
    }()

    //: lazy var nameLb: UILabel = {
    lazy var nameLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Semibold, fontSize: 15)
        label.font = .afterDisable(type: .Semibold, fontSize: 15)
        //: label.textColor = UIColor.appTitleColor()
        label.textColor = UIColor.manualColor()
        //: return label
        return label
        //: }()
    }()

    //: lazy var levelIcon: UIImageView = {
    lazy var levelIcon: UIImageView = {
        //: let imag = UIImageView.init()
        let imag = UIImageView()
        //: imag.contentMode = .scaleAspectFill
        imag.contentMode = .scaleAspectFill
        //: return imag
        return imag
        //: }()
    }()

    //: lazy var signLb: UILabel = {
    lazy var signLb: UILabel = {
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.font = .pingfangFont(type: .Regular, fontSize: 13)
        label.font = .afterDisable(type: .Regular, fontSize: 13)
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.fluent()
        //: label.textAlignment = .center
        label.textAlignment = .center
        //: label.numberOfLines = 2
        label.numberOfLines = 2
        //: return label
        return label
        //: }()
    }()

    //: private lazy var followersLab: UILabel = {
    private lazy var followersLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .fluent()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Followers".localized
        lab.text = (String(dream_quitId.prefix(5)) + noti_selectedIdent.replacingOccurrences(of: "cast", with: "e")).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.afterDisable(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followersNum: UILabel = {
    private lazy var followersNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .manualColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.afterDisable(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingLab: UILabel = {
    private lazy var followingLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appValueDetailColor()
        lab.textColor = .fluent()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Following".localized
        lab.text = (String(main_trainTitle) + String(appSensorId)).localized
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 12)
        lab.font = UIFont.afterDisable(type: .Semibold, fontSize: 12)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var followingNum: UILabel = {
    private lazy var followingNum: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .appTitleColor()
        lab.textColor = .manualColor()
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "0"
        lab.text = "0"
        //: lab.font = UIFont.pingfangFont(type: .Semibold, fontSize: 15)
        lab.font = UIFont.afterDisable(type: .Semibold, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var lineView: UIView = {
    private lazy var lineView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.init(hex: "#E6E6E6")
        view.backgroundColor = UIColor(hex: (String(appAvailableName.suffix(7))))
        //: return view
        return view
        //: }()
    }()

    //: private lazy var stackView: UIStackView = {
    private lazy var stackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .fill
        v.alignment = .fill
        //: v.distribution = .fillEqually
        v.distribution = .fillEqually
        //: return v
        return v
        //: }()
    }()

    //: private lazy var moreBtn: UIButton = {
    private lazy var moreBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.setImage(UIImage.BundleImageNamed(name: "btn_message_more"), for: .normal)
        btn.setImage(UIImage.scribeNumber(name: (String(k_jumpSpreadDistantPath))), for: .normal)
        //: btn.addTarget(self, action: #selector(moreBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(boardSystem), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var muteView: TalkingLiveRoomMuteView = {
    private lazy var muteView: SkipView = {
        //: let v = TalkingLiveRoomMuteView()
        let v = SkipView()
        //: v.viewType  = .voiceRoom
        v.viewType = .voiceRoom
        //: return v
        return v
        //: }()
    }()
}

//: extension TalkingVoiceRoomCardView {
extension FindView {
    //: func reqUserCardInfo() {
    func teratogen() {
        //: TalkingLiveManager.req__userCardInfo(uid: self.uid, streamerUid: liveUserId, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
        TapBecauseLiveManager.somebodyTap(uid: self.uid, streamerUid: liveUserId, roomId: LanceManager.doingResource().partyModel.roomId) { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }

            //: if let userModel = JSONDeserializer<TalkingUserInfoModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<GatherTransformable>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: self.userModel = userModel
                self.userModel = userModel
                //: self.setUserCardData()
                self.near()
            }
        }
    }

    //: func setUserCardData() {
    func near() {
        //: uidLb.text = "UID: \(self.userModel.uid)"
        uidLb.text = (String(dreamGoAnalysisMessage.prefix(5))) + "\(self.userModel.uid)"
        //: topIcon.setUrlImage(urlStr: self.userModel.headPic)
        topIcon.arm(urlStr: self.userModel.headPic)
        //: nameLb.text = self.userModel.nickname
        nameLb.text = self.userModel.nickname
        //: levelIcon.image = UIImage.BundleImageNamed(name: "level_\(self.userModel.level)")
        levelIcon.image = UIImage.scribeNumber(name: (String(user_littleSheFinalMessage.prefix(6))) + "\(self.userModel.level)")
        //: signLb.text = self.userModel.signature
        signLb.text = self.userModel.signature
        //: followersNum.text = self.userModel.attentionNum
        followersNum.text = self.userModel.attentionNum
        //: followingNum.text = self.userModel.fansNum
        followingNum.text = self.userModel.fansNum

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: setBottomView(sex: self.userModel.sex)
            sole(sex: self.userModel.sex)
        }
    }

    //: func setBottomView(sex: String) {
    func sole(sex _: String) {
        //: var dataSource = Array<String>()
        var dataSource = [String]()
        //: if self.userModel.isOnMike {
        if self.userModel.isOnMike {
            //: dataSource.append("Gift")
            dataSource.append(String(bytes: dream_giftPath.map{$0^87}, encoding: .utf8)!)
            //: if liveUserId == AdministratorThen.share.loginUserMode.userID && self.userModel.mikeStatus != 0  {
            if liveUserId == AdministratorThen.share.loginUserMode.userID, self.userModel.mikeStatus != 0 { // 当前用户是主播非自己闭麦  闭麦/开麦 按钮
                //: dataSource.append("Mike")
                dataSource.append(String(bytes: m_blackMessage.map{$0^112}, encoding: .utf8)!)
            }
        }
        //: dataSource.append("@")
        dataSource.append("@")

        //: if self.userModel.isAttention == false {
        if self.userModel.isAttention == false {
            //: dataSource.append("Follow")
            dataSource.append(String(bytes: noti_countWrittenContent.reversed(), encoding: .utf8)!)
        }

        //: for i in 0 ..< dataSource.count {
        for i in 0 ..< dataSource.count {
            //: let btnStr = dataSource[i]
            let btnStr = dataSource[i]
            //: let btn = UIButton()
            let btn = UIButton()
            //: btn.accessibilityLabel = btnStr
            btn.accessibilityLabel = btnStr
            //: btn.setTitleColor(UIColor.appThemeColor(), for: UIControl.State.normal)
            btn.setTitleColor(UIColor.fogColor(), for: UIControl.State.normal)
            //: if btnStr == "Gift" {
            if btnStr == String(bytes: dream_giftPath.map{$0^87}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "gift_voice_card"), for: .normal)
                btn.setImage(UIImage.scribeNumber(name: (String(userSocietyValue) + String(mainCommentOwnData))), for: .normal)
                //: }else if btnStr == "Mike"{
            } else if btnStr == String(bytes: m_blackMessage.map{$0^112}, encoding: .utf8)! {
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_nor"), for: .normal)
                btn.setImage(UIImage.scribeNumber(name: (String(kEveryoneIdent) + String(mGreenRemainMessage.suffix(4)))), for: .normal)
                //: btn.setImage(UIImage.BundleImageNamed(name: "btn_party_voice_pre"), for: .selected)
                btn.setImage(UIImage.scribeNumber(name: (String(main_drawingName.prefix(4)) + "part" + String(mWrittenContent.suffix(6)) + "e_pre")), for: .selected)
                //: if self.userModel.mikeStatus != 2 {
                if self.userModel.mikeStatus != 2 {
                    //: btn.isSelected = true
                    btn.isSelected = true
                }
                //: }else{
            } else {
                //: btn.titleLabel?.font = UIFont.pingfangMediumFont(fontSize: 15)
                btn.titleLabel?.font = UIFont.imitationClotheStatement(fontSize: 15)
                //: btn.setTitle(btnStr.localized, for: UIControl.State.normal)
                btn.setTitle(btnStr.localized, for: UIControl.State.normal)
            }
            //: btn.addTarget(self, action: #selector(itemTouch(sender:)), for: UIControl.Event.touchUpInside)
            btn.addTarget(self, action: #selector(dismissNotice(sender:)), for: UIControl.Event.touchUpInside)
            //: self.stackView.addArrangedSubview(btn)
            self.stackView.addArrangedSubview(btn)

            //: if i != 0 {
            if i != 0 {
                //: let lineView = UIView.init()
                let lineView = UIView()
                //: lineView.backgroundColor = UIColor.init(hex: "#E6E6E6")
                lineView.backgroundColor = UIColor(hex: (String(appAvailableName.suffix(7))))
                //: btn.addSubview(lineView)
                btn.addSubview(lineView)
                //: lineView.snp.makeConstraints { make in
                lineView.snp.makeConstraints { make in
                    //: make.top.equalTo(10)
                    make.top.equalTo(10)
                    //: make.leading.equalToSuperview()
                    make.leading.equalToSuperview()
                    //: make.width.equalTo(1)
                    make.width.equalTo(1)
                    //: make.height.equalTo(24)
                    make.height.equalTo(24)
                }
            }
        }
    }

    //: @objc func itemTouch(sender: UIButton) {
    @objc func dismissNotice(sender: UIButton) {
        //: let accessLabel = sender.accessibilityLabel ?? ""
        let accessLabel = sender.accessibilityLabel ?? ""
        //: switch accessLabel {
        switch accessLabel {
        //: case "Gift":
        case String(bytes: dream_giftPath.map{$0^87}, encoding: .utf8)!:
            //: self.delegate?.func__giftUserClick(uid: self.userModel.uid)
            self.delegate?.exhaustSystem(uid: self.userModel.uid)
            //: self.dismiss()
            self.enchiridion()
        //: case "Mike":
        case String(bytes: m_blackMessage.map{$0^112}, encoding: .utf8)!:
            //: self.delegate?.func__muteUserMickClick(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            self.delegate?.talkDown(uid: self.userModel.uid, mikeStatus: self.userModel.mikeStatus, position: self.userModel.mikePosition)
            //: self.dismiss()
            self.enchiridion()
        //: case "@":
        case "@":
            //: self.delegate?.func__atUserClick(uid: self.userModel.uid, nickname: self.userModel.nickname)
            self.delegate?.bubbleFeedOn(uid: self.userModel.uid, nickname: self.userModel.nickname)
            //: self.dismiss()
            self.enchiridion()
        //: case "Follow":
        case String(bytes: noti_countWrittenContent.reversed(), encoding: .utf8)!:
            //: freshfollowUser(sender: sender)
            someHandle(sender: sender)
        //: default: break
        default: break
        }
    }

    //: func freshfollowUser(sender: UIButton) {
    func someHandle(sender: UIButton) {
        //: req_followUser(uid: self.userModel.uid) { succeed in
        atFollow(uid: self.userModel.uid) { succeed in
            //: if succeed {
            if succeed {
                //: self.followingNum.text = "\((Int(self.userModel.fansNum ) ?? 0) + 1)"
                self.followingNum.text = "\((Int(self.userModel.fansNum) ?? 0) + 1)"
                //: self.stackView.removeArrangedSubview(sender)
                self.stackView.removeArrangedSubview(sender)
                //: sender.isHidden = true
                sender.isHidden = true
                //: self.delegate?.func__followUserSucess(uid: self.userModel.uid)
                self.delegate?.system(uid: self.userModel.uid)
            }
        }
    }

    //: func req_followUser(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    func atFollow(uid: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["attentionUid"] = uid
        dict[(k_withPath.replacingOccurrences(of: "coordinator", with: "at") + "ionUid")] = uid
        //: dict["source"] = "2"
        dict[(show_historyFormat.replacingOccurrences(of: "share", with: "e"))] = "2"
        //: TalkingChatRequestTool.req_atationTool(isAttention: true, params: dict) { succeed, result, errorModel in
        ShrinkReactiveCompatible.inmateButton(isAttention: true, params: dict) { succeed, _, _ in
            //: completion?(succeed)
            completion?(succeed)
        }
    }

    //: @objc private func iconBtnClick() {
    @objc private func cameraBuild() {
        //: DumpPushManager.share.func__pushToUserDetailVC(uid: self.userModel.uid)
        DumpPushManager.share.belowStreet(uid: self.userModel.uid)
        //: self.dismiss()
        self.enchiridion()
    }
}

//: extension TalkingVoiceRoomCardView {
extension FindView {
    //: @objc func moreBtnClick() {
    @objc func boardSystem() {
        //: var titleArray: [String] = []
        var titleArray: [String] = []
        /// 主播 & 用户在房间内
        //: if liveUserId == AdministratorThen.share.loginUserMode.userID && self.userModel.isInRoom {
        if liveUserId == AdministratorThen.share.loginUserMode.userID, self.userModel.isInRoom {
            //: titleArray.append("Kick out".localized)
            titleArray.append((String(k_iconKey.suffix(7)) + String(dream_fullStr)).localized)
            //: if self.userModel.isPartyMute {
            if self.userModel.isPartyMute {
                //: titleArray.append("Unmute".localized)
                titleArray.append((String(kConstraintId.prefix(6))).localized)
                //: }else {
            } else {
                //: titleArray.append("Mute".localized)
                titleArray.append((String(app_calculateSubTitle.prefix(4))).localized)
            }
            //: if self.userModel.isOnMike {
            if self.userModel.isOnMike {
                //: titleArray.append("Kick off".localized)
                titleArray.append((String(userNurseMessage.prefix(6)) + String(user_highlightStr)).localized)
            }
            //: if !self.userModel.inMyBlackList {
            if !self.userModel.inMyBlackList {
                //: titleArray.append("Block".localized)
                titleArray.append((String(mBelowUrl)).localized)
            }
        }
        //: titleArray.append("Report".localized)
        titleArray.append((String(main_sameValue.suffix(6))).localized)

        //: Config.MenuCellConfig.menuCellHeight = 26
        Config.MenuConfig.menuCellHeight = 26
        //: let dropMenu = DropDownMenuView.pullDropDrownMenu(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        let dropMenu = MenuReactiveCompatible.asStar(anchorView: moreBtn, titleArray: titleArray, imageArray: [])
        //: dropMenu.menuBgColor = UIColor.init(hex: "#F6F6F6")!
        dropMenu.menuBgColor = UIColor(hex: (String(noti_lookWomanFormat.suffix(7))))!
        //: dropMenu.menuDelegate = self
        dropMenu.menuDelegate = self
        //: dropMenu.menuStyle = .MenuCustStyle
        dropMenu.menuStyle = .MenuCustStyle
    }

    //: func show() {
    func fosterHome() {
        //: popView = TalkingPopView.init(frame: UIScreen.main.bounds)
        popView = AsideView(frame: UIScreen.main.bounds)
        //: self.frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        self.frame = CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage)
        //: popView?.initWithView(view: self)
        popView?.viewChin(view: self)
        //: popView?.showInView(view: self.currentViewController()?.view ?? CreditsThen.getWindow())
        popView?.validMake(view: self.springEquinox()?.view ?? CreditsThen.captain())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func enchiridion() {
        //: popView?.dismissView()
        popView?.theme()
        //: popView = nil
        popView = nil
    }
}

//: extension TalkingVoiceRoomCardView: DropDownMenuViewDelegate {
extension FindView: AlongViewDelegate {
    //: func didClickSelectedRow(index: Int, title: String) {
    func input(index _: Int, title: String) {
        //: switch title {
        switch title {
        //: case "Kick out".localized:  // 踢出房间
        case (String(k_iconKey.suffix(7)) + String(dream_fullStr)).localized: // 踢出房间
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick out the room?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            GentleAlertShow.aboveSixth(title: nil, message: (String(show_prisonItTitle) + String(app_accelerateName.suffix(6)) + "o kic" + String(show_originalMessage.prefix(5)) + " the r" + String(m_tickValue.suffix(4))).localized, leftBtnTitle: (String(dreamOverContent.suffix(6))).localized, rightBtnTitle: (String(m_quietValue.prefix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                VillageView.projectShow()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomKickout(targetUid: self.userModel.uid, roomId: TalkingVoiceRoomManager.shared().partyModel.roomId) { succeed, result, errorModel in
                OnReqTool.guide(targetUid: self.userModel.uid, roomId: LanceManager.doingResource().partyModel.roomId) { _, _, _ in
                    //: ProgressHUD.dismiss()
                    VillageView.statusDismiss()
                    //: self.dismiss()
                    self.enchiridion()
                }
            }
        //: case "Unmute".localized: // 解除禁言
        case (String(kConstraintId.prefix(6))).localized: // 解除禁言
            //: TalkingAlertShow.alert(title: nil, message: "Are you sure you want to cancel the mute?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Confirm".localized) {
            GentleAlertShow.aboveSixth(title: nil, message: String(bytes: show_backData.map{sumerruptStopWhole(beyond: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(dreamOverContent.suffix(6))).localized, rightBtnTitle: (mInstancePath.replacingOccurrences(of: "occur", with: "C") + mainArrayData.replacingOccurrences(of: "light", with: "n")).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: ProgressHUD.show()
                VillageView.projectShow()
                //: TalkingVoiceRoomReqTool.req_VoiceRoomUnMute(targetUid: self.userModel.uid, roomID:TalkingVoiceRoomManager.shared().partyModel.roomId, completion: { succeed, result, errorModel in
                OnReqTool.anyCleanPrevious(targetUid: self.userModel.uid, roomID: LanceManager.doingResource().partyModel.roomId, completion: { succeed, _, _ in
                    //: ProgressHUD.dismiss()
                    VillageView.statusDismiss()
                    //: if succeed {
                    if succeed {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "The mute has been lifted~".localized)
                        self.bringHomePublishGap(showMsg: (String(m_givenData) + String(notiHolderFormat) + String(dream_amName.prefix(5))).localized)
                    }
                    //: self.dismiss()
                    self.enchiridion()
                    //: })
                })
            }
        //: case "Mute".localized: // 设置禁言
        case (String(app_calculateSubTitle.prefix(4))).localized: // 设置禁言
            //: muteView.showView()
            muteView.circumferenceCur()
            //: muteView.targetUid = self.userModel.uid
            muteView.targetUid = self.userModel.uid
            //: self.dismiss()
            self.enchiridion()

        //: case "Kick off".localized: /// 踢下麦
        case (String(userNurseMessage.prefix(6)) + String(user_highlightStr)).localized: /// 踢下麦
            //: TalkingAlertShow.alert(title: nil, message: "Confirm to kick off the seat?".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Sure".localized) {
            GentleAlertShow.aboveSixth(title: nil, message: (String(showInsideCulturalName) + String(appStatUrl.prefix(4)) + "kick " + String(dream_ballotMsg.suffix(6)) + "e seat?").localized, leftBtnTitle: (String(dreamOverContent.suffix(6))).localized, rightBtnTitle: (String(m_quietValue.prefix(4))).localized) {
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingVoiceRoomManager.shared().voiceRoom_reqAdminChangeMike(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                LanceManager.doingResource().ballotDestroy(uid: self.userModel.uid.intValue, type: 3, position: self.userModel.mikePosition)
                //: self.dismiss()
                self.enchiridion()
            }

        //: case "Report".localized:
        case (String(main_sameValue.suffix(6))).localized:
            //: let reportView = TalkingReportAlertView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            let reportView = WingView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.userModel.uid)
            //: reportView.showReportViewIn(view: nil)
            reportView.smartPress(view: nil)
        //: case "Block".localized:
        case (String(mBelowUrl)).localized:
            //: TalkingAlertShow.alert(title: nil, message: kMessage_blocking, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized) {
            GentleAlertShow.aboveSixth(title: nil, message: show_bottomText, leftBtnTitle: (String(dreamOverContent.suffix(6))).localized, rightBtnTitle: "OK".localized) {
                //: TalkingAlertShow.hideAlert()
                GentleAlertShow.someButton()
                //: } rightBlock: {
            } rightBlock: {
                //: TalkingAlertShow.hideAlert()
                GentleAlertShow.someButton()
                //: TalkingUserRequestManager.func__pullBlackWithUserId(uid: self.userModel.uid, isBlack: true) { succeed, result, errorModel in
                AdvocateReactiveCompatible.faceBig(uid: self.userModel.uid, isBlack: true) { succeed, _, _ in
                    //: if succeed == true {
                    if succeed == true {
                        //: self.func__showStatusBarSuccessMsg(showMsg: "Shielding Success".localized)
                        self.bringHomePublishGap(showMsg: (String(dreamPageName.prefix(7)) + String(showMeritTitle.prefix(5)) + "ccess").localized)
                        //: self.dismiss()
                        self.enchiridion()
                    }
                }
            }
        //: default: break
        default: break
        }
    }
}

//: extension TalkingVoiceRoomCardView {
extension FindView {
    // 添加视图
    //: private func setupSubviews() {
    private func borderSetup() {
        //: self.addSubview(contentView)
        self.addSubview(contentView)
        //: self.addSubview(topIcon)
        self.addSubview(topIcon)
        //: contentView.addSubview(uidLb)
        contentView.addSubview(uidLb)
        //: contentView.addSubview(nameLb)
        contentView.addSubview(nameLb)
        //: contentView.addSubview(levelIcon)
        contentView.addSubview(levelIcon)
        //: contentView.addSubview(signLb)
        contentView.addSubview(signLb)
        //: contentView.addSubview(followersLab)
        contentView.addSubview(followersLab)
        //: contentView.addSubview(followersNum)
        contentView.addSubview(followersNum)
        //: contentView.addSubview(followingLab)
        contentView.addSubview(followingLab)
        //: contentView.addSubview(followingNum)
        contentView.addSubview(followingNum)

        //: contentView.layoutIfNeeded()
        contentView.layoutIfNeeded()

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: contentView.addSubview(lineView)
            contentView.addSubview(lineView)
            //: contentView.addSubview(stackView)
            contentView.addSubview(stackView)
            //: contentView.addSubview(moreBtn)
            contentView.addSubview(moreBtn)
        }
    }

    // 添加约束
    //: private func setupSubViewsConstraint() {
    private func extraOf() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-44)
            make.bottom.equalToSuperview().offset(-44)
            //: make.leading.equalTo(10)
            make.leading.equalTo(10)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: if self.type == .user_me {
            if self.type == .user_me {
                //: make.height.equalTo(192)
                make.height.equalTo(192)
                //: }else {
            } else {
                //: make.height.equalTo(233)
                make.height.equalTo(233)
            }
        }
        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(contentView).offset(-33)
            make.top.equalTo(contentView).offset(-33)
            //: make.width.height.equalTo(80)
            make.width.height.equalTo(80)
        }
        //: uidLb.snp.makeConstraints { make in
        uidLb.snp.makeConstraints { make in
            //: make.leading.top.equalTo(12)
            make.leading.top.equalTo(12)
            //: make.trailing.equalTo(topIcon.snp.leading).offset(-10)
            make.trailing.equalTo(topIcon.snp.leading).offset(-10)
        }
        //: nameLb.snp.makeConstraints { make in
        nameLb.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom).offset(12)
            make.top.equalTo(topIcon.snp.bottom).offset(12)
            //: make.centerX.equalToSuperview().offset(-12)
            make.centerX.equalToSuperview().offset(-12)
            //: make.width.lessThanOrEqualTo(ScreenWidth/2)
            make.width.lessThanOrEqualTo(noti_senseContent / 2)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: levelIcon.snp.makeConstraints { make in
        levelIcon.snp.makeConstraints { make in
            //: make.leading.equalTo(nameLb.snp.trailing).offset(5)
            make.leading.equalTo(nameLb.snp.trailing).offset(5)
            //: make.centerY.equalTo(nameLb)
            make.centerY.equalTo(nameLb)
            //: make.size.equalTo(CGSize(width: 32, height: 14))
            make.size.equalTo(CGSize(width: 32, height: 14))
        }
        //: signLb.snp.makeConstraints { make in
        signLb.snp.makeConstraints { make in
            //: make.top.equalTo(nameLb.snp.bottom).offset(12)
            make.top.equalTo(nameLb.snp.bottom).offset(12)
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.height.equalTo(38)
            make.height.equalTo(38)
        }
        //: followersNum.snp.makeConstraints { make in
        followersNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(-ScreenWidth/4+10)
            make.centerX.equalTo(contentView).offset(-noti_senseContent / 4 + 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followingNum.snp.makeConstraints { make in
        followingNum.snp.makeConstraints { make in
            //: make.top.equalTo(signLb.snp.bottom).offset(12)
            make.top.equalTo(signLb.snp.bottom).offset(12)
            //: make.centerX.equalTo(contentView).offset(ScreenWidth/4-10)
            make.centerX.equalTo(contentView).offset(noti_senseContent / 4 - 10)
            //: make.height.equalTo(18)
            make.height.equalTo(18)
        }
        //: followersLab.snp.makeConstraints { make in
        followersLab.snp.makeConstraints { make in
            //: make.top.equalTo(followersNum.snp.bottom).offset(5)
            make.top.equalTo(followersNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followersNum)
            make.centerX.equalTo(followersNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }
        //: followingLab.snp.makeConstraints { make in
        followingLab.snp.makeConstraints { make in
            //: make.top.equalTo(followingNum.snp.bottom).offset(5)
            make.top.equalTo(followingNum.snp.bottom).offset(5)
            //: make.centerX.equalTo(followingNum)
            make.centerX.equalTo(followingNum)
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: if self.type == .user_other {
        if self.type == .user_other {
            //: lineView.snp.makeConstraints { make in
            lineView.snp.makeConstraints { make in
                //: make.top.equalTo(followersLab.snp.bottom).offset(12)
                make.top.equalTo(followersLab.snp.bottom).offset(12)
                //: make.leading.equalTo(7)
                make.leading.equalTo(7)
                //: make.trailing.equalTo(-7)
                make.trailing.equalTo(-7)
                //: make.height.equalTo(1)
                make.height.equalTo(1)
            }
            //: stackView.snp.makeConstraints { make in
            stackView.snp.makeConstraints { make in
                //: make.top.equalTo(lineView)
                make.top.equalTo(lineView)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(44)
                make.height.equalTo(44)
            }
            //: moreBtn.snp.makeConstraints { make in
            moreBtn.snp.makeConstraints { make in
                //: make.trailing.equalToSuperview()
                make.trailing.equalToSuperview()
                //: make.top.equalTo(12)
                make.top.equalTo(12)
                //: make.size.equalTo(CGSize(width: 44, height: 20))
                make.size.equalTo(CGSize(width: 44, height: 20))
            }
        }
    }

    //: override func touchesBegan(_ touches: Set<UITouch>, with event: UIEvent?) {
    override func touchesBegan(_ touches: Set<UITouch>, with _: UIEvent?) {
        //: let touch = touches.first!
        let touch = touches.first!
        //: var point = touch.location(in: self.currentViewController()?.view)
        var point = touch.location(in: self.springEquinox()?.view)
        //: point = contentView.layer.convert(point, from: self.currentViewController()?.view.layer)
        point = contentView.layer.convert(point, from: self.springEquinox()?.view.layer)
        //: if !contentView.layer.contains(point) {
        if !contentView.layer.contains(point) {
            //: self.dismiss()
            self.enchiridion()
        }
    }
}
