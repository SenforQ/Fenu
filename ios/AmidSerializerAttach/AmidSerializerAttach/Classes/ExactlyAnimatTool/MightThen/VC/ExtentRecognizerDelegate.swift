
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mCreationMsg:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "uid" :*/
fileprivate let kRapidlyData:[UInt8] = [0xe7,0xfb,0xf6]

private func hairOwn(press num: UInt8) -> UInt8 {
    return num ^ 146
}

/*: "headPic" :*/
fileprivate let showSecondPath:String = "dismiss quicklyheadPic"

/*: "nickname" :*/
fileprivate let userRemainingName:String = "monitorickmonitora"
fileprivate let dreamDoingerCustodyApplyPath:String = "forme"

/*: "birthday" :*/
fileprivate let appEgoId:String = "birtriver"
fileprivate let app_civilPath:[Character] = ["a","y"]

/*: "videoUrl" :*/
fileprivate let app_constraintValue:[Character] = ["v","i","d","e","o"]
fileprivate let noti_recordingName:String = "compute if sensorUrl"

/*: "cover" :*/
fileprivate let user_agencyId:String = "covround"

/*: "location" :*/
fileprivate let k_trustStr:String = "locsomebodyti"
fileprivate let mainWallValue:String = "ocos"

/*: "cmd" :*/
fileprivate let notiBodyTitle:[UInt8] = [0x64,0x6d,0x63]

/*: "refuseCall" :*/
fileprivate let dreamAwayText:[Character] = ["r","e","f","u","s"]
fileprivate let kDefinitionSinceCookingTitle:String = "following reduce cloud flow clickeCall"

/*: "data" :*/
fileprivate let notiBusyMsg:[UInt8] = [0xd7,0xd4,0xe7,0xd4]

fileprivate func accelerateLens(description num: UInt8) -> UInt8 {
    let value = Int(num) - 115
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Call interruption" :*/
fileprivate let user_tinName:String = "Call idismiss nigh satisfy salt"
fileprivate let app_selectedValue:[Character] = ["n","t","e","r","r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let appNetUrl:[Character] = ["s","t","o","p","C","a","l","l"]

/*: "logId" :*/
fileprivate let k_bottomFormat:[UInt8] = [0x4a,0x49,0x41,0x6f,0x42]

private func deleteShall(original num: UInt8) -> UInt8 {
    return num ^ 38
}

/*: "cancelCall" :*/
fileprivate let k_tagTitle:String = "cmonthnc"
fileprivate let m_elementStr:String = "requirerequire"

/*: "No answer" :*/
fileprivate let notiExistValue:[Character] = ["N","o"," ","a","n","s"]
fileprivate let noti_coordinatorUrl:[Character] = ["w","e","r"]

/*: "duration" :*/
fileprivate let showConceptLeaveTitle:[Character] = ["d"]
fileprivate let showFavoriteName:String = "uratioextended"

/*: "She has hung up" :*/
fileprivate let userProperlyFormat:String = "She hasubmit statement"
fileprivate let showListUrl:[Character] = ["s"," ","h","u","n","g"," ","u","p"]

/*: "cancelSex" :*/
fileprivate let k_workerPath:String = "cancbubble"
fileprivate let showDynamicsTitle:String = "missing protection scan defense loadSex"

/*: "He has hung up" :*/
fileprivate let user_clickData:[Character] = ["H","e"," ","h","a","s"," ","h","u","n","g"," ","u","p"]

/*: "endType" :*/
fileprivate let m_effectivePath:[Character] = ["e","n","d","T","y"]
fileprivate let show_birthdayIdent:String = "imitation"

/*: "stopSex" :*/
fileprivate let notiStarFormat:[Character] = ["s","t","o","p","S"]
fileprivate let k_askUrl:String = "EX"

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let noti_slowMessage:[UInt8] = [0xe4,0xc3,0xde,0xd8,0xcb,0xcb,0xc4,0xce,0xc4,0xc8,0xc3,0xd9,0x8d,0xca,0xc2,0xc1,0xc9,0x8d,0xce,0xc2,0xc4,0xc3,0xde,0x81,0x8d,0xce,0xcc,0xc1,0xc1,0x8d,0xc5,0xcc,0xde,0x8d,0xc8,0xc3,0xc9,0xc8,0xc9]

private func resourceState(sit num: UInt8) -> UInt8 {
    return num ^ 173
}

/*: "The other hung up the phone" :*/
fileprivate let userQuietKey:[Character] = ["T","h","e"," ","o","t","h","e","r"," ","h","u","n","g"," ","u","p"," ","t","h","e"]
fileprivate let main_customIdent:[Character] = [" ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let showArrayMpCombinedTitle:String = "tvide"

/*: "jumpUrl" :*/
fileprivate let user_readerWoodRatingPath:String = "function all skirt backgroundjumpUrl"

/*: "Cancel" :*/
fileprivate let m_powUrl:[Character] = ["C","a","n","c","e","l"]

/*: "Get gold coins" :*/
fileprivate let main_songContent:String = "lie physical letGet g"
fileprivate let show_participantUrl:[Character] = ["n","s"]

/*: "addBean" :*/
fileprivate let showNextValue:String = "addBeanresolution listener beautiful"

/*: "refuseSex" :*/
fileprivate let dreamImplementName:String = "refuplatform"
fileprivate let show_fadeUrl:String = "may lap sustain breakeSex"

/*: "onAcceptCall" :*/
fileprivate let app_destinationName:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let userWarnGestureIdent:[Character] = ["a","c","c","e","p","t","C"]
fileprivate let dreamMiracleMsg:[Character] = ["a","l","l"]

/*: "Calls are cancelled" :*/
fileprivate let appPriceBroadStr:[Character] = ["C","a","l","l","s"," ","a","r","e"," ","c","a","n","c","e"]
fileprivate let showFillFirmFormat:[Character] = ["l","l","e","d"]

/*: "The call has ended" :*/
fileprivate let show_retirementTitle:String = "going jump usedThe ca"
fileprivate let noti_creativeName:String = "factor tumble aid safe speaker ende"
fileprivate let showAlongBronzeUrl:String = "near"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ExtentRecognizerDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/8.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let videoCallHelper = CXCallObserver()
let mButtonNumberId = CXCallObserver()
//: class TalkingVideoChatViewController: TalkingBaseViewController {
class ExtentRecognizerDelegate: FlexibleViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ChatModel? // 视频通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: LogPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVideoMiniView?
    private var miniView: HotfootView? // 视频通话小窗口
    //: private var isFromMiniView = false
    private var isFromMiniView = false // 是否点击小窗口进入
    //: private var reportView: TalkingVideoFloatScreenView?
    private var reportView: PoliticianReactiveCompatible? // 视频举报弹窗

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        AdviceLandscapeReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        AdviceLandscapeReactiveCompatible.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mCreationMsg.reversed(), encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! MarkHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.disableTip()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVideoAuth()
            lapAuth()
            //: } else if self.chatModel?.videoStage == .Calling {
        } else if self.chatModel?.videoStage == .Calling { // 直接创建通话中的界面（包括重连）
            // 通话小窗口进入无需重复创建
            //: guard isFromMiniView == false else { return }
            guard isFromMiniView == false else { return }
            //: createCallingRoom(logId: self.chatModel?.logId)
            renameAwayWithinSawLog(logId: self.chatModel?.logId)
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currFindVC = nil
            AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.downData()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        thresholdRemote()
        //: setupSubViewsConstraint()
        prevalence()
        //: initNotificationObserver()
        rootScope()
        //: req_callGetUserInfo()
        lease()
        //: timeoutRules()
        adRules()
        //: setUpBGMPlayer()
        installBack()
        //: if self.chatModel?.videoStage != .Recieved {
        if self.chatModel?.videoStage != .Recieved {
            //: self.startPreview()
            self.electronicSignal()
        }
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AdministratorThen.share.appConfigMode.disableShootScreen {
            if AdministratorThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = GentleReactiveCompatible(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isFind = false
        AdviceLandscapeReactiveCompatible.shared.isFind = false
        AdviceLandscapeReactiveCompatible.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.borderCollectionActivties()
    }

    // MARK: - Lazy Load

    /// 视频通话背景
    //: private lazy var bgVideoView: TalkingBGVideoView = {
    private lazy var bgVideoView: CompensationView = {
        //: let v = TalkingBGVideoView()
        let v = CompensationView()
        //: v.showCoverView(show: self.chatModel?.videoStage != .Calling)
        v.sinceThumbShow(show: self.chatModel?.videoStage != .Calling)
        //: return v
        return v
        //: }()
    }()

    /// 视频通话操作按钮
    //: private lazy var interactionView: Talking1v1VideoInteractionView = {
    private lazy var interactionView: TwoPlayerDelegate = {
        //: let v = Talking1v1VideoInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = TwoPlayerDelegate(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    // 视频通话小窗口
    //: private lazy var windowView: TalkingVideoWindowView = {
    private lazy var windowView: GenerateReactiveCompatible = {
        //: let v = TalkingVideoWindowView()
        let v = GenerateReactiveCompatible()
        //: var x = ScreenWidth-15-actualWidth(w: 125)
        var x = noti_senseContent - 15 - actualWidth(w: 125)
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PrintingReactiveCompatible.shared.direction == .rightToLeft {
            //: x = 15
            x = 15
        }
        //: v.frame = CGRect(x: x, y: StatusBarHeight+actualHeight(h: 25)+26+35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        v.frame = CGRect(x: x, y: mNameKey + actualHeight(h: 25) + 26 + 35, width: actualWidth(w: 125), height: actualHeight(h: 170))
        //: v.isHidden = self.chatModel?.videoStage != .Calling
        v.isHidden = self.chatModel?.videoStage != .Calling
        //: v.block = { [weak self] in
        v.block = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.changeRenderView()
            self.createerDescription()
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: MightThen = {
        //: let m = TalkingVideoChatManager()
        let m = MightThen()
        //: m.infoModel = self.chatModel
        m.infoModel = self.chatModel
        // 视图传递给manager
        //: m.bgVideoView = self.bgVideoView.renderView
        m.bgVideoView = self.bgVideoView.renderView
        //: m.bgView = self.bgVideoView.bgView
        m.bgView = self.bgVideoView.bgView
        //: m.windowVideoView = self.windowView.renderView
        m.windowVideoView = self.windowView.renderView
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request && 刷新UI

//: extension TalkingVideoChatViewController {
extension ExtentRecognizerDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func lease() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        MightThen.tick(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: kRapidlyData.map{hairOwn(press: $0)}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(showSecondPath.suffix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(userRemainingName.replacingOccurrences(of: "monitor", with: "n") + dreamDoingerCustodyApplyPath.replacingOccurrences(of: "form", with: "m"))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(appEgoId.replacingOccurrences(of: "river", with: "hd") + String(app_civilPath))].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(app_constraintValue) + String(noti_recordingName.suffix(3)))].stringValue
            //: self.chatModel?.cover = json["cover"].stringValue
            self.chatModel?.cover = json[(user_agencyId.replacingOccurrences(of: "round", with: "er"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(k_trustStr.replacingOccurrences(of: "somebody", with: "a") + mainWallValue.replacingOccurrences(of: "cos", with: "n"))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.bindFresh()
            //: self.videoBG_refreshViewWithCameraStatus()
            self.successfullyStatus()
        }
    }

    /// 根据摄像头是否开启状态，刷新视频通话背景
    //: private func videoBG_refreshViewWithCameraStatus() {
    private func successfullyStatus() {
        //: var bgHeadPic: String?
        var bgHeadPic: String?
        //: var bgShowName: String?
        var bgShowName: String?
        //: var bgOpenCamera = VideoCameraStatus.Open
        var bgOpenCamera = StopCameraStatus.Open
        //: var windowHeadPic: String?
        var windowHeadPic: String?
        //: var windowOpenCamera = VideoCameraStatus.Open
        var windowOpenCamera = StopCameraStatus.Open

        //: if self.chatModel?.renderPostion == .SelfOnWindow {
        if self.chatModel?.renderPostion == .SelfOnWindow { // 自己在窗口
            //: bgHeadPic = self.chatModel?.pairHeadPic
            bgHeadPic = self.chatModel?.pairHeadPic
            //: bgShowName = self.chatModel?.pairNickName
            bgShowName = self.chatModel?.pairNickName
            //: bgOpenCamera = self.videoManager.config.remoteCameraStatus
            bgOpenCamera = self.videoManager.config.remoteCameraStatus
            //: windowHeadPic = self.chatModel?.iHeadPic
            windowHeadPic = self.chatModel?.iHeadPic
            //: windowOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            windowOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed

            //: } else {
        } else { // 自己在大背景
            //: bgHeadPic = self.chatModel?.iHeadPic
            bgHeadPic = self.chatModel?.iHeadPic
            //: bgShowName = self.chatModel?.iNickName
            bgShowName = self.chatModel?.iNickName
            //: bgOpenCamera = self.videoManager.config.openCamera ? .Open:.Closed
            bgOpenCamera = self.videoManager.config.openCamera ? .Open : .Closed
            //: windowHeadPic = self.chatModel?.pairHeadPic
            windowHeadPic = self.chatModel?.pairHeadPic
            //: windowOpenCamera = self.videoManager.config.remoteCameraStatus
            windowOpenCamera = self.videoManager.config.remoteCameraStatus
        }

        //: self.bgVideoView.refreshBgView(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        self.bgVideoView.bound(urlStr: bgHeadPic, showName: bgShowName, isOpenCamera: bgOpenCamera)
        //: self.windowView.refreshBgView(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
        self.windowView.countoCamera(urlStr: windowHeadPic, isOpenCamera: windowOpenCamera)
    }
}

// MARK: - Event

//: extension TalkingVideoChatViewController {
extension ExtentRecognizerDelegate {
    /// 权限校验，播放BGM
    //: private func checkVideoAuth() {
    private func lapAuth() {
        //: TalkingPermissionTool.checkCameraAndMicrophone { isOpen in
        AsidePermissionTool.opticalLensOn { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.iconThemeBringBackPlace(showMsg: dream_topKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (String(dreamAwayText) + String(kDefinitionSinceCookingTitle.suffix(5))), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: kRapidlyData.map{hairOwn(press: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.popOut()
                //: return
                return
            }

            //: self.startPreview()
            self.electronicSignal()
            //: self.stopBGM()
            self.timeBgm()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = LogPlayListener.sink(type: .Response)
            //: self.startBGM()
            self.disableTip()
        }
    }

    /// 预览视频画面
    //: private func startPreview() {
    private func electronicSignal() {
        //: self.videoManager.startPreview()
        self.videoManager.likeFrom()
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func borderCollectionActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.timeBgm()
        //: TalkingVideoDanmuManager.danmu_releaseAllResource()
        FindGentleDecisionMakerReactiveCompatible.drawn()
        //: TalkingSocketManager.shared.callerDelegate = nil
        AdviceLandscapeReactiveCompatible.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        AdviceLandscapeReactiveCompatible.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func popOut(animated: Bool = true) {
        //: self.stopBGM()
        self.timeBgm()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 视频通话双方窗口转换
    //: private func changeRenderView() {
    private func createerDescription() {
        //: if self.chatModel?.renderPostion == .selfOnBg {
        if self.chatModel?.renderPostion == .selfOnBg {
            //: self.chatModel?.renderPostion = .SelfOnWindow
            self.chatModel?.renderPostion = .SelfOnWindow
            //: } else if self.chatModel?.renderPostion == .SelfOnWindow {
        } else if self.chatModel?.renderPostion == .SelfOnWindow {
            //: self.chatModel?.renderPostion = .selfOnBg
            self.chatModel?.renderPostion = .selfOnBg
        }

        //: self.videoManager.changeRenderView()
        self.videoManager.jump()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.successfullyStatus()
    }

    /// 创建视频通话房间
    //: private func createTalkRoom() {
    private func isFill() {
        //: self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        self.windowView.isHidden = self.chatModel?.videoStage != .Calling
        //: self.videoManager.createRoomAndStartPublish()
        self.videoManager.explainSame()
        //: self.videoBG_refreshViewWithCameraStatus()
        self.successfullyStatus()
        // 展示投诉说明飘屏（男性）
        //: self.show_videoFloatScreenAnimation()
        self.mutualAnimation()
    }

    /// 展示投诉说明飘屏（男性）
    //: private func show_videoFloatScreenAnimation() {
    private func mutualAnimation() {
        //: guard AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue else { return }
        guard AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue else { return }
        //: guard reportView == nil else { return }
        guard reportView == nil else { return }

        //: reportView = TalkingVideoFloatScreenView()
        reportView = PoliticianReactiveCompatible()
        //: view.addSubview(reportView!)
        view.addSubview(reportView!)
        //: reportView?.show_animation()
        reportView?.computeRate()
    }
}

// MARK: - 通知

//: extension TalkingVideoChatViewController {
extension ExtentRecognizerDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func rootScope() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(volition(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 监听 对方摄像头是否可用
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(updateCameraStatus(notification:)),
                                               selector: #selector(localAlongside(notification:)),
                                               //: name: VIDEO_CHAT_OTHER_CAMERA_AVAILABLE_NOTIFICATION,
                                               name: userSessionPath,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(changedNote(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: user_barPath,
                                               //: object: nil)
                                               object: nil)

        //: videoCallHelper.setDelegate(self, queue: DispatchQueue.main)
        mButtonNumberId.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func volition(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            popOut()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func changedNote(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard AdviceLandscapeReactiveCompatible.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        AdviceLandscapeReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        AdviceLandscapeReactiveCompatible.shared.errorDelegate = self
        //: self.videoManager.restartTalkWhenDisconnectedAndNetAvailable()
        self.videoManager.restart()
    }

    /// 根据对方摄像头状态更新UI
    /// - Parameter notification: 通知
    //: @objc private func updateCameraStatus(notification: NSNotification) {
    @objc private func localAlongside(notification _: NSNotification) {
        //: self.videoBG_refreshViewWithCameraStatus()
        self.successfullyStatus()
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVideoChatViewController: CXCallObserverDelegate {
extension ExtentRecognizerDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption")
            self.iconThemeBringBackPlace(showMsg: (String(user_tinName.prefix(6)) + String(app_selectedValue)))
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (String(appNetUrl)), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: k_bottomFormat.map{deleteShall(original: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (k_tagTitle.replacingOccurrences(of: "month", with: "a") + "elCa" + m_elementStr.replacingOccurrences(of: "require", with: "l")), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: k_bottomFormat.map{deleteShall(original: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (String(dreamAwayText) + String(kDefinitionSinceCookingTitle.suffix(5))), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: kRapidlyData.map{hairOwn(press: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.doWhen()
                //: TalkingSocketManager.shared.currFindVC = nil
                AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
            }
            //: popCurrentViewController()
            popOut()
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVideoChatViewController {
extension ExtentRecognizerDelegate {
    /// 初始化视频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func installBack() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? WarningSignedNumeric.Call : WarningSignedNumeric.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = LogPlayListener.sink(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func disableTip() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.descale()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func timeBgm() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.shareOfSession()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVideoChatViewController {
extension ExtentRecognizerDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func adRules() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? mSearchObjectKey : self.chatModel?.creativePerson()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            justDate()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.justDate()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func justDate() {
        //: guard !TalkingSocketManager.shared.isFind else { return }
        guard !AdviceLandscapeReactiveCompatible.shared.isFind else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.iconThemeBringBackPlace(showMsg: (String(notiExistValue) + String(noti_coordinatorUrl)).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (k_tagTitle.replacingOccurrences(of: "month", with: "a") + "elCa" + m_elementStr.replacingOccurrences(of: "require", with: "l")), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: k_bottomFormat.map{deleteShall(original: $0)}, encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (String(dreamAwayText) + String(kDefinitionSinceCookingTitle.suffix(5))), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: kRapidlyData.map{hairOwn(press: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: socketDict)
        //: popCurrentViewController()
        popOut()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVideoChatViewController {
extension ExtentRecognizerDelegate {
    /// 恢复视频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func drawPing(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: k_bottomFormat.map{deleteShall(original: $0)}, encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(String(showConceptLeaveTitle) + showFavoriteName.replacingOccurrences(of: "extended", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.maximal(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.weight()
            //: createTalkRoom()
            isFill()
            //: self.stopBGM()
            self.timeBgm()
        }
    }

    /// 创建通话中房间
    /// - Parameter logId: 通话记录Id
    //: private func createCallingRoom(logId: Int?) {
    private func renameAwayWithinSawLog(logId: Int?) {
        //: guard let logId = logId else { return }
        guard let logId = logId else { return }

        //: self.chatModel?.logId = logId
        self.chatModel?.logId = logId
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.videoManager.changeRenderView()
        self.videoManager.jump()
        //: self.bgVideoView.showCoverView(show: false)
        self.bgVideoView.sinceThumbShow(show: false)
        //: self.stopBGM()
        self.timeBgm()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.weight()
        //: self.createTalkRoom()
        self.isFill()
    }
}

// MARK: - BecauseObjectProtocol【拨打方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCallerDelegate {
extension ExtentRecognizerDelegate: BecauseObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func pointBlank(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userProperlyFormat.prefix(6)) + String(showListUrl)).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(k_workerPath.replacingOccurrences(of: "bubble", with: "el") + String(showDynamicsTitle.suffix(3)))].stringValue == RecordNameLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(user_clickData)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.iconThemeBringBackPlace(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.doWhen()
            //: TalkingSocketManager.shared.currFindVC = nil
            AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
        }

        //: popCurrentViewController()
        popOut()
    }

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func willData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        renameAwayWithinSawLog(logId: json[String(bytes: k_bottomFormat.map{deleteShall(original: $0)}, encoding: .utf8)!].intValue)
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func freshData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(m_effectivePath) + show_birthdayIdent.replacingOccurrences(of: "imitation", with: "pe"))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(userProperlyFormat.prefix(6)) + String(showListUrl)).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(String(notiStarFormat) + k_askUrl.lowercased())].stringValue == RecordNameLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(user_clickData)).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: noti_slowMessage.map{resourceState(sit: $0)}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(userQuietKey) + String(main_customIdent)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.iconThemeBringBackPlace(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.doWhen()
            //: TalkingSocketManager.shared.currFindVC = nil
            AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.gutter()
        //: popCurrentViewController()
        popOut()
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func uniformAdd(msg: String, data: [String: Any]) {
        //: guard AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(showArrayMpCombinedTitle.replacingOccurrences(of: "vid", with: "yp"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
//            if self.interactionView.isShowRecharge {
//                return
//            }
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(user_readerWoodRatingPath.suffix(7)))] as? String else { return }
            //: let httpsUrl = AdministratorThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = AdministratorThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = MerelyFireConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: DumpPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            DumpPushManager.share.noneControlInstall(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            GentleAlertShow.aboveSixth(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(m_powUrl)).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(main_songContent.suffix(5)) + "old coi" + String(show_participantUrl)).localized)
            {
                //: TalkingAlertShow.hideAlert()
                GentleAlertShow.someButton()
                //: } rightBlock: {
            } rightBlock: {
                //: DumpPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                DumpPushManager.share.shapeStyle(clickEvent: notiNoContent, sufficient: false)
            }
        }
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func pictureLocation(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.awaited(giftArr: giftArr)
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func videoChild(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(showNextValue.prefix(7)))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.funPoint(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.access(point: addBean)
        }
    }
}

// MARK: - ExpectObjectProtocol【接收方】

//: extension TalkingVideoChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ExtentRecognizerDelegate: ExpectObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func miniskirt(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(userProperlyFormat.prefix(6)) + String(showListUrl)).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(dreamImplementName.replacingOccurrences(of: "platform", with: "s") + String(show_fadeUrl.suffix(4)))].stringValue == RecordNameLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(user_clickData)).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.iconThemeBringBackPlace(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.doWhen()
            //: TalkingSocketManager.shared.currFindVC = nil
            AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
        }

        //: popCurrentViewController()
        popOut()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func judge(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: createCallingRoom(logId: json["logId"].intValue)
        renameAwayWithinSawLog(logId: json[String(bytes: k_bottomFormat.map{deleteShall(original: $0)}, encoding: .utf8)!].intValue)
    }
}

// MARK: - TwelveNoonObjectProtocol【异常处理】

//: extension TalkingVideoChatViewController: TalkingSocketManagerErrorDelegate {
extension ExtentRecognizerDelegate: TwelveNoonObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func playerData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(dreamAwayText) + String(kDefinitionSinceCookingTitle.suffix(5))) { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.iconThemeBringBackPlace(showMsg: (String(notiExistValue) + String(noti_coordinatorUrl)).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.doWhen()
                //: TalkingSocketManager.shared.currFindVC = nil
                AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
            }
            //: popCurrentViewController()
            popOut()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(app_destinationName)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue else { return }
            //: DumpPushManager.share.func__jumpToWebRecharge(sufficient: false)
            DumpPushManager.share.shapeStyle(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(app_destinationName)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.iconThemeBringBackPlace(showMsg: msg)
            //: popCurrentViewController()
            popOut()
        }
    }
}

// MARK: - ToneCreateViewDelegate【通话按钮操作事件】

//: extension TalkingVideoChatViewController: Talking1v1VideoInteractionViewDelegate {
extension ExtentRecognizerDelegate: ToneCreateViewDelegate {
    /// 接听视频通话
    //: func interactionView_shouldAcceptCall() {
    func gen() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (String(userWarnGestureIdent) + String(dreamMiracleMsg)), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: kRapidlyData.map{hairOwn(press: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func shouldReport() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.iconThemeBringBackPlace(showMsg: (String(appPriceBroadStr) + String(showFillFirmFormat)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (k_tagTitle.replacingOccurrences(of: "month", with: "a") + "elCa" + m_elementStr.replacingOccurrences(of: "require", with: "l")), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: k_bottomFormat.map{deleteShall(original: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.iconThemeBringBackPlace(showMsg: (String(appPriceBroadStr) + String(showFillFirmFormat)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (String(dreamAwayText) + String(kDefinitionSinceCookingTitle.suffix(5))), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: kRapidlyData.map{hairOwn(press: $0)}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.iconThemeBringBackPlace(showMsg: (String(show_retirementTitle.suffix(6)) + "ll has" + String(noti_creativeName.suffix(5)) + showAlongBronzeUrl.replacingOccurrences(of: "near", with: "d")).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: notiBodyTitle.reversed(), encoding: .utf8)!: (String(appNetUrl)), String(bytes: notiBusyMsg.map{accelerateLens(description: $0)}, encoding: .utf8)!: [String(bytes: k_bottomFormat.map{deleteShall(original: $0)}, encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: popCurrentViewController()
        popOut()
    }

    /// 旋转摄像头
    //: func interactionView_shouldChangeCamera(isFront: Bool) {
    func broadcastLight(isFront: Bool) {
        //: self.videoManager.useFrontCamera(isFront)
        self.videoManager.licenseCamera(isFront)
    }

    /// 开启/关闭摄像头
    //: func interactionView_shouldForbiddenCamera(forbidden: Bool) {
    func adForbidden(forbidden: Bool) {
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved {
            //: self.bgVideoView.showCoverView(show: !forbidden)
            self.bgVideoView.sinceThumbShow(show: !forbidden)
        }
        //: self.videoManager.useCamera(!forbidden)
        self.videoManager.billValid(!forbidden)
        //: self.videoBG_refreshViewWithCameraStatus()
        self.successfullyStatus()
    }

    /// 展示视频通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func socialMp() {
        //: TalkingSocketManager.shared.currFindVC = self
        AdviceLandscapeReactiveCompatible.shared.currFindVC = self
        //: self.miniView = TalkingVideoMiniView.buildVideoMiniView()
        self.miniView = HotfootView.sumView()
        //: self.videoManager.smallRenderView = self.miniView?.renderView
        self.videoManager.smallRenderView = self.miniView?.renderView
        //: self.videoManager.enterSmallRenderMode()
        self.videoManager.getEnableCur()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.videoManager.exitSmallRenderMode()
            self.videoManager.nearbyTar()
            //: self.isFromMiniView = true
            self.isFromMiniView = true
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.springEquinox()?.navigationController?.pushViewController(self, animated: true)
        }
        //: popCurrentViewController()
        popOut()
    }

    /// 投诉成功
    //: func interactionView_reportSucceed() {
    func classAvesArteriaCentralisRetinaeStraddle() {
        //: if reportView?.superview != nil {
        if reportView?.superview != nil {
            //: reportView?.removeFromSuperview()
            reportView?.removeFromSuperview()
        }
    }
}

// MARK: - Layout

//: extension TalkingVideoChatViewController {
extension ExtentRecognizerDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func thresholdRemote() {
        //: self.view.layer.masksToBounds = true
        self.view.layer.masksToBounds = true
        //: self.view.addSubview(bgVideoView)
        self.view.addSubview(bgVideoView)
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
        //: self.interactionView.addWindowView(view: self.windowView)
        self.interactionView.quantityerpretation(view: self.windowView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func prevalence() {
        //: bgVideoView.snp.makeConstraints { make in
        bgVideoView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
