
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainCommitteeKey:[UInt8] = [0x3b,0x3c,0x3b,0x26,0x7a,0x31,0x3d,0x36,0x37,0x20,0x68,0x7b,0x72,0x3a,0x33,0x21,0x72,0x3c,0x3d,0x26,0x72,0x30,0x37,0x37,0x3c,0x72,0x3b,0x3f,0x22,0x3e,0x37,0x3f,0x37,0x3c,0x26,0x37,0x36]

/*: "uid" :*/
fileprivate let mainOwnerId:[UInt8] = [0x5b,0x47,0x4a]

/*: "headPic" :*/
fileprivate let m_creationFormat:String = "headPiclast bot"

/*: "nickname" :*/
fileprivate let noti_tooData:String = "nimarriedkn"
fileprivate let mainAssertName:[Character] = ["a","m","e"]

/*: "birthday" :*/
fileprivate let dreamTransitionUrl:String = "BIRTHDAY"

/*: "videoUrl" :*/
fileprivate let mainPairContent:String = "format float orange communicate minivideoUr"
fileprivate let m_kindPath:String = "scan"

/*: "location" :*/
fileprivate let user_illegalRejectStr:[Character] = ["l","o","c","a","t","i","o","n"]

/*: "cmd" :*/
fileprivate let show_oneTitle:[UInt8] = [0x62,0x6c,0x63]

fileprivate func earlyContext(appeal num: UInt8) -> UInt8 {
    let value = Int(num) - 255
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "refuseCall" :*/
fileprivate let k_floorId:[Character] = ["r","e"]
fileprivate let kDirectUrl:String = "fcooperativese"

/*: "data" :*/
fileprivate let appCloseContent:[UInt8] = [0xdb,0xde,0xcb,0xde]

private func mySegmentShow(cap num: UInt8) -> UInt8 {
    return num ^ 191
}

/*: "Call interruption" :*/
fileprivate let show_cooperativeData:[Character] = ["C","a","l","l"," ","i","n","t","e","r"]
fileprivate let notiWhetherPath:[Character] = ["r","u","p","t","i","o","n"]

/*: "stopCall" :*/
fileprivate let noti_electronicLowerHostValue:String = "stopCalluser transfer invitation coordinator take"

/*: "logId" :*/
fileprivate let app_minimizeFormat:[UInt8] = [0x64,0x49,0x67,0x6f,0x6c]

/*: "cancelCall" :*/
fileprivate let m_silentIdentificationStr:[Character] = ["c","a","n","c","e"]
fileprivate let noti_effectiveTitle:[Character] = ["l","C","a","l","l"]

/*: "No answer" :*/
fileprivate let user_fitMsg:[Character] = ["N","o"," ","a","n"]
fileprivate let mainSteerMessage:String = "swload"

/*: "duration" :*/
fileprivate let noti_failIdent:String = "duratioanimal"

/*: "She has hung up" :*/
fileprivate let show_factorStudyPrivateText:[Character] = ["S","h","e"," ","h","a","s"," ","h"]
fileprivate let user_begSiteDeadlineIdent:String = "U"
fileprivate let noti_republicUrl:String = "ng upwithout player argument moment"

/*: "cancelSex" :*/
fileprivate let m_missName:String = "cancelSeexport aid muse format"
fileprivate let user_slideText:String = "pin"

/*: "He has hung up" :*/
fileprivate let mainCompriseMsg:String = "He haback see"
fileprivate let user_careId:String = "alliance may highlight up"

/*: "endType" :*/
fileprivate let main_gainCreateRankId:[Character] = ["e","n","d","T","y","p","e"]

/*: "stopSex" :*/
fileprivate let appBringFluentTitle:String = "sauthorize"
fileprivate let user_deviceId:[Character] = ["o","p","S","e","x"]

/*: "Insufficient gold coins, call has ended" :*/
fileprivate let notiThumbText:[UInt8] = [0xfb,0xdc,0xc1,0xc7,0xd4,0xd4,0xdb,0xd1,0xdb,0xd7,0xdc,0xc6,0x92,0xd5,0xdd,0xde,0xd6,0x92,0xd1,0xdd,0xdb,0xdc,0xc1,0x9e,0x92,0xd1,0xd3,0xde,0xde,0x92,0xda,0xd3,0xc1,0x92,0xd7,0xdc,0xd6,0xd7,0xd6]

/*: "The other hung up the phone" :*/
fileprivate let notiMuseValue:String = "The otcombine fresh mp"
fileprivate let mainTodayCookieData:String = "hung upreturn no sale success alliance"
fileprivate let showForceUrl:[Character] = [" ","t","h","e"," ","p","h","o","n","e"]

/*: "type" :*/
fileprivate let k_ceasePath:String = "tformate"

/*: "jumpUrl" :*/
fileprivate let user_capOfflyContent:[Character] = ["j","u","m","p","U","r","l"]

/*: "Cancel" :*/
fileprivate let dreamWaveMessage:String = "Cancelprogram cause"

/*: "Get gold coins" :*/
fileprivate let noti_helloUrl:[Character] = ["G","e","t"," ","g","o","l","d"," "]
fileprivate let notiRegularIdent:String = "coinleaf"

/*: "addBean" :*/
fileprivate let app_reverseStreetUniteUrl:[Character] = ["a","d","d","B","e","a","n"]

/*: "refuseSex" :*/
fileprivate let appTenderUrl:String = "refwarms"

/*: "onAcceptCall" :*/
fileprivate let main_statementFormat:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "acceptCall" :*/
fileprivate let kCommitTalkAfterValue:String = "ACCEP"
fileprivate let app_viaTitle:String = "L"

/*: "Calls are cancelled" :*/
fileprivate let userAccountingRankReMessage:String = "whether vessel challengeCall"
fileprivate let dreamSpanHeadName:String = "e cancdisagree calculate called light mm"

/*: "The call has ended" :*/
fileprivate let kChooseIdent:[Character] = ["T","h","e"," ","c","a","l","l"," ","h","a","s"]
fileprivate let dream_priorityFormat:[Character] = [" ","e","n","d","e","d"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FuturismViewDelegate.swift
//  AbroadTalking
//
//  Created by young on 2022/11/11.
//

//: import CallKit.CXCallObserver
import CallKit.CXCallObserver
//: import UIKit
import UIKit

// 通话监听(必须为全局属性，否则无法接听)
//: let voiceCallHelper = CXCallObserver()
let noti_ofMessage = CXCallObserver()
//: class TalkingVoiceChatViewController: TalkingBaseViewController {
class FuturismViewDelegate: FlexibleViewController {
    //: var chatModel: TalkingVideoChatModel?
    var chatModel: ChatModel? // 语音通话模型
    //: private var player: TalkingCallBGMPlayer?
    private var player: LogPlayListener? // 拨打中背景音乐
    //: private var timeOutTimer: Timer?
    private var timeOutTimer: Timer? // 未接听超时定时器
    //: private var expiredDate = Date()
    private var expiredDate = Date() // 失效时间
    //: private var miniView: TalkingVoiceMiniView?
    private var miniView: TapReactiveCompatible? // 音频通话小窗口

    //: override init(nibName nibNameOrNil: String?, bundle nibBundleOrNil: Bundle?) {
    override init(nibName _: String?, bundle _: Bundle?) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        // 提前绑定delegate
        //: TalkingSocketManager.shared.callerDelegate = self
        AdviceLandscapeReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        AdviceLandscapeReactiveCompatible.shared.errorDelegate = self
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainCommitteeKey.map{$0^82}, encoding: .utf8)!)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: let nav = self.navigationController as! TalkingNavigationController
        let nav = self.navigationController as! MarkHasDelegate
        //: nav.protectedPushVc = nil
        nav.protectedPushVc = nil

        //: UIApplication.shared.isIdleTimerDisabled = true
        UIApplication.shared.isIdleTimerDisabled = true // 设备不会进入休眠状态
        //: self.startBGM()
        self.featureBindBgm()
        //: if self.chatModel?.videoStage == .Recieved {
        if self.chatModel?.videoStage == .Recieved { // 接收方验证权限
            //: checkVoiceAuth()
            totalerpret()
        }
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: if self.miniView != nil {
        if self.miniView != nil { // 移除小窗口
            //: self.miniView?.removeFromSuperview()
            self.miniView?.removeFromSuperview()
            //: self.miniView = nil
            self.miniView = nil
            //: TalkingSocketManager.shared.currFindVC = nil
            AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
        }
        //: interactionView.initCachEffectData()
        interactionView.busyNarrow()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true
        //: setupSubviews()
        installBySubviews()
        //: setupSubViewsConstraint()
        stopConstraint()
        //: initNotificationObserver()
        recapture()
        //: req_callGetUserInfo()
        cipher()
        //: timeoutRules()
        recentModeConversation()
        //: resumeTalk()
        qualityTalk()
        //: setUpBGMPlayer()
        counto()
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AdministratorThen.share.appConfigMode.disableShootScreen {
            if AdministratorThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = GentleReactiveCompatible(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: deinit {
    deinit {
        //: UIApplication.shared.isIdleTimerDisabled = false
        UIApplication.shared.isIdleTimerDisabled = false
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: TalkingSocketManager.shared.isFind = false
        AdviceLandscapeReactiveCompatible.shared.isFind = false
        AdviceLandscapeReactiveCompatible.shared.isCalling = false
        //: self.stopVCAllActivties()
        self.whoActivties()
    }

    // MARK: - Lazy Load

    /// 音频通话操作按钮
    //: private lazy var interactionView: Talking1v1VoiceInteractionView = {
    private lazy var interactionView: PosterReactiveCompatible = {
        //: let v = Talking1v1VoiceInteractionView(frame: .zero, chatModel: self.chatModel)
        let v = PosterReactiveCompatible(frame: .zero, chatModel: self.chatModel)
        //: v.delegate = self
        v.delegate = self
        //: return v
        return v
        //: }()
    }()

    //: private lazy var voiceManager: TalkingVoiceChatManager = {
    private lazy var voiceManager: RecordVoiceOverCloudDelegate = {
        //: let m = TalkingVoiceChatManager()
        let m = RecordVoiceOverCloudDelegate()
        //: m.handsfreeBtn = self.interactionView.handsfreeBtn
        m.handsfreeBtn = self.interactionView.handsfreeBtn
        //: return m
        return m
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingVoiceChatViewController {
extension FuturismViewDelegate {
    /// 查询接收者信息
    //: func req_callGetUserInfo() {
    func cipher() {
        //: guard let pairUid = chatModel?.pairUid else { return }
        guard let pairUid = chatModel?.pairUid else { return }
        //: TalkingVideoChatManager.req_callGetUserInfo(uid: pairUid) { [weak self] succeed, result, errorModel in
        MightThen.tick(uid: pairUid) { [weak self] succeed, result, _ in
            //: guard succeed == true else { return }
            guard succeed == true else { return }
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())

            //: self.chatModel?.pairUid = json["uid"].stringValue
            self.chatModel?.pairUid = json[String(bytes: mainOwnerId.map{$0^46}, encoding: .utf8)!].stringValue
            //: self.chatModel?.pairHeadPic = json["headPic"].stringValue
            self.chatModel?.pairHeadPic = json[(String(m_creationFormat.prefix(7)))].stringValue
            //: self.chatModel?.pairNickName = json["nickname"].stringValue
            self.chatModel?.pairNickName = json[(noti_tooData.replacingOccurrences(of: "married", with: "c") + String(mainAssertName))].stringValue
            //: self.chatModel?.pairBirthday = json["birthday"].stringValue
            self.chatModel?.pairBirthday = json[(dreamTransitionUrl.lowercased())].stringValue
            //: self.chatModel?.videoUrl = json["videoUrl"].stringValue
            self.chatModel?.videoUrl = json[(String(mainPairContent.suffix(7)) + m_kindPath.replacingOccurrences(of: "scan", with: "l"))].stringValue
            //: self.chatModel?.location = json["location"].stringValue
            self.chatModel?.location = json[(String(user_illegalRejectStr))].stringValue

            // 刷新通话视图
            //: self.interactionView.afterGetInfo()
            self.interactionView.transactionInfo()
        }
    }
}

// MARK: - Event

//: extension TalkingVoiceChatViewController {
extension FuturismViewDelegate {
    /// 权限校验，播放BGM
    //: private func checkVoiceAuth() {
    private func totalerpret() {
        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        AsidePermissionTool.openGold(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.iconThemeBringBackPlace(showMsg: dream_topKey)
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(k_floorId) + kDirectUrl.replacingOccurrences(of: "cooperative", with: "u") + "Call"), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: mainOwnerId.map{$0^46}, encoding: .utf8)!: self.chatModel?.pairUid]])
                //: self.popCurrentViewController()
                self.offeTimeAnimated()
                //: return
                return
            }

            //: self.stopBGM()
            self.box()
            //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: .Response)
            self.player = LogPlayListener.sink(type: .Response)
            //: self.startBGM()
            self.featureBindBgm()
        }
    }

    /// 释放当前页面所有资源
    //: private func stopVCAllActivties() {
    private func whoActivties() {
        //: if self.timeOutTimer != nil {
        if self.timeOutTimer != nil {
            //: self.timeOutTimer?.invalidate()
            self.timeOutTimer?.invalidate()
            //: self.timeOutTimer = nil
            self.timeOutTimer = nil
        }

        //: self.stopBGM()
        self.box()
        //: TalkingSocketManager.shared.callerDelegate = nil
        AdviceLandscapeReactiveCompatible.shared.callerDelegate = nil
        //: TalkingSocketManager.shared.calledPartyDelegate = nil
        AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = nil
        //: TalkingSocketManager.shared.errorDelegate = nil
        AdviceLandscapeReactiveCompatible.shared.errorDelegate = nil
    }

    // 将当前视图从栈中移除
    //: private func popCurrentViewController(animated: Bool = true) {
    private func offeTimeAnimated(animated: Bool = true) {
        //: self.stopBGM()
        self.box()

        //: if self.navigationController?.topViewController == self {
        if self.navigationController?.topViewController == self {
            //: self.navigationController?.popViewController(animated: animated)
            self.navigationController?.popViewController(animated: animated)
            //: } else {
        } else {
            //: if var vcArr = self.navigationController?.viewControllers {
            if var vcArr = self.navigationController?.viewControllers {
                //: if let index = vcArr.firstIndex(of: self) {
                if let index = vcArr.firstIndex(of: self) {
                    //: vcArr.remove(at: index)
                    vcArr.remove(at: index)
                    //: self.navigationController?.setViewControllers(vcArr, animated: false)
                    self.navigationController?.setViewControllers(vcArr, animated: false)
                }
            }
        }
    }

    /// 创建音频通话房间
    //: private func createTalkRoom() {
    private func succeedSegment() {
        //: self.voiceManager.createRoomAndStartPublish(roomId: self.chatModel!.logId)
        self.voiceManager.aliveId(roomId: self.chatModel!.logId)
    }

    /// 通话重连
    //: private func resumeTalk() {
    private func qualityTalk() {
        //: if self.chatModel?.resumeTalk == true {
        if self.chatModel?.resumeTalk == true {
            //: createTalkRoom()
            succeedSegment()
        }
    }
}

// MARK: - 通知

//: extension TalkingVoiceChatViewController {
extension FuturismViewDelegate {
    /// 初始化通知
    //: private func initNotificationObserver() {
    private func recapture() {
        // 监听 App 从后台切前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(appWillEnterForeground(notif:)),
                                               selector: #selector(enlargement(notif:)),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // socket断网重连监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(colorReachability(note:)),
                                               //: name: SOCKET_IS_OPEN_NOTIFICATION,
                                               name: user_barPath,
                                               //: object: nil)
                                               object: nil)

        //: voiceCallHelper.setDelegate(self, queue: DispatchQueue.main)
        noti_ofMessage.setDelegate(self, queue: DispatchQueue.main)
    }

    /// app进入前台
    //: @objc private func appWillEnterForeground(notif: Notification) {
    @objc private func enlargement(notif _: Notification) {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: let date = Date()
        let date = Date()
        //: let interval = date.timeIntervalSince(self.expiredDate)
        let interval = date.timeIntervalSince(self.expiredDate)
        //: if interval >= 0 {
        if interval >= 0 {
            //: popCurrentViewController()
            offeTimeAnimated()
        }
    }

    /// 断网重连socket
    //: @objc private func reachabilityChanged(note: Notification) {
    @objc private func colorReachability(note _: Notification) {
        //: guard TalkingSocketManager.shared.needReconnect == true else { return }
        guard AdviceLandscapeReactiveCompatible.shared.needReconnect == true else { return }
        //: TalkingSocketManager.shared.callerDelegate = self
        AdviceLandscapeReactiveCompatible.shared.callerDelegate = self
        //: TalkingSocketManager.shared.calledPartyDelegate = self
        AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = self
        //: TalkingSocketManager.shared.errorDelegate = self
        AdviceLandscapeReactiveCompatible.shared.errorDelegate = self
        //: self.voiceManager.restartTalkWhenDisconnectedAndNetAvailable(roomId: self.chatModel?.logId ?? 0)
        self.voiceManager.bringId(roomId: self.chatModel?.logId ?? 0)
    }
}

// MARK: - CXCallObserverDelegate【通话监听】

//: extension TalkingVoiceChatViewController: CXCallObserverDelegate {
extension FuturismViewDelegate: CXCallObserverDelegate {
    //: func callObserver(_ callObserver: CXCallObserver, callChanged call: CXCall) {
    func callObserver(_: CXCallObserver, callChanged call: CXCall) {
        //: if call.hasConnected == true {
        if call.hasConnected == true { // 通话中
            //: self.func__showStatusBarErrorMsg(showMsg: "Call interruption".localized)
            self.iconThemeBringBackPlace(showMsg: (String(show_cooperativeData) + String(notiWhetherPath)).localized)
            //: switch(self.chatModel?.videoStage) {
            switch self.chatModel?.videoStage {
            //: case .Calling:
            case .Calling:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
                AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(noti_electronicLowerHostValue.prefix(8))), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: app_minimizeFormat.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Waiting:
            case .Waiting:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
                AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(m_silentIdentificationStr) + String(noti_effectiveTitle)), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: app_minimizeFormat.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: case .Recieved:
            case .Recieved:
                //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
                AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(k_floorId) + kDirectUrl.replacingOccurrences(of: "cooperative", with: "u") + "Call"), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: mainOwnerId.map{$0^46}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: case .none:
            case .none:
                //: break
                break
            }

            //: if miniView != nil {
            if miniView != nil {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.doWhen()
                //: TalkingSocketManager.shared.currFindVC = nil
                AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
            }
            //: popCurrentViewController()
            offeTimeAnimated()

            //: } else if call.hasEnded == true || call.isOnHold == true {
        } else if call.hasEnded == true || call.isOnHold == true { // 电话挂断 || 无人接听挂断
            //: self.voiceManager.enableAudioModule(enable: true)
            self.voiceManager.gestureDoingAcceptGift(enable: true)
        }
    }
}

// MARK: - 通话音乐

//: extension TalkingVoiceChatViewController {
extension FuturismViewDelegate {
    /// 初始化音频通话背景音乐
    //: private func setUpBGMPlayer() {
    private func counto() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let type = self.chatModel!.isCaller == true ? CallBGMPlayerType.Call : CallBGMPlayerType.Response
        let type = self.chatModel!.isCaller == true ? WarningSignedNumeric.Call : WarningSignedNumeric.Response
        //: self.player = TalkingCallBGMPlayer.callBGMPlayer(type: type)
        self.player = LogPlayListener.sink(type: type)
    }

    /// 开始播放背景音乐
    //: private func startBGM() {
    private func featureBindBgm() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }
        //: guard self.chatModel?.resumeTalk != true else { return }
        guard self.chatModel?.resumeTalk != true else { return }

        //: self.player?.play()
        self.player?.descale()
    }

    /// 停止播放背景音乐
    //: private func stopBGM() {
    private func box() {
        //: guard self.player != nil else { return }
        guard self.player != nil else { return }
        //: self.player?.stop()
        self.player?.shareOfSession()
        //: self.player = nil
        self.player = nil
    }
}

// MARK: - 定时器

//: extension TalkingVoiceChatViewController {
extension FuturismViewDelegate {
    /// 超时定时器
    //: private func timeoutRules() {
    private func recentModeConversation() {
        //: guard self.chatModel?.videoStage != .Calling else { return }
        guard self.chatModel?.videoStage != .Calling else { return }

        //: let timeout = self.chatModel?.isCaller == true ? VIDEO_1v1_CALL_TIMEOUT : self.chatModel?.remainAnswerCallTime()
        let timeout = self.chatModel?.isCaller == true ? mSearchObjectKey : self.chatModel?.creativePerson()
        //: if timeout! <= 0 {
        if timeout! <= 0 {
            //: buildTalkTimeOutAction()
            downwardly()
        }
        //: self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] timer in
        self.timeOutTimer = Timer.scheduledTimer(withTimeInterval: timeout!, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.buildTalkTimeOutAction()
            self.downwardly()
            //: }, repeats: false)
        }, repeats: false)

        //: self.expiredDate = Date().addingTimeInterval(60)
        self.expiredDate = Date().addingTimeInterval(60)
    }

    /// 倒计时超时逻辑处理
    //: private func buildTalkTimeOutAction() {
    private func downwardly() {
        //: guard !TalkingSocketManager.shared.isFind else { return }
        guard !AdviceLandscapeReactiveCompatible.shared.isFind else { return }

        //: var socketDict: [String: Any]
        var socketDict: [String: Any]
        //: if self.chatModel?.isCaller == true {
        if self.chatModel?.isCaller == true { // 拨打方
            //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
            self.iconThemeBringBackPlace(showMsg: (String(user_fitMsg) + mainSteerMessage.replacingOccurrences(of: "load", with: "er")).localized)
            //: socketDict = ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]]
            socketDict = [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(m_silentIdentificationStr) + String(noti_effectiveTitle)), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: app_minimizeFormat.reversed(), encoding: .utf8)!: self.chatModel?.logId]]
            //: } else {
        } else { // 接收方
            //: socketDict = ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]]
            socketDict = [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(k_floorId) + kDirectUrl.replacingOccurrences(of: "cooperative", with: "u") + "Call"), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: mainOwnerId.map{$0^46}, encoding: .utf8)!: self.chatModel?.pairUid]]
        }
        //: TalkingSocketManager.shared.sendMessage(info: socketDict)
        AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: socketDict)
        //: popCurrentViewController()
        offeTimeAnimated()
    }
}

// MARK: - resume talk【恢复通话】

//: extension TalkingVoiceChatViewController {
extension FuturismViewDelegate {
    /// 恢复音频通话，更新chatModel
    /// - Parameter resumeData: 数据
    //: func updateInfoViaResumeTalkData(resumeData: [String: Any]) {
    func someIndex(resumeData: [String: Any]) {
        //: let json = JSON(resumeData)
        let json = JSON(resumeData)
        //: let logId = json["logId"].intValue
        let logId = json[String(bytes: app_minimizeFormat.reversed(), encoding: .utf8)!].intValue
        //: let duration = json["duration"].intValue
        let duration = json[(noti_failIdent.replacingOccurrences(of: "animal", with: "n"))].intValue
        //: let interval = Date().timeIntervalSince1970
        let interval = Date().timeIntervalSince1970
        //: let startTalkTime = floor(interval) - Double(duration)
        let startTalkTime = floor(interval) - Double(duration)
        //: self.chatModel?.startTime = startTalkTime
        self.chatModel?.startTime = startTalkTime
        //: self.interactionView.updateStartTime(startTime: startTalkTime)
        self.interactionView.indomitabilityHouse(startTime: startTalkTime)

        //: if self.chatModel?.videoStage != .Calling {
        if self.chatModel?.videoStage != .Calling {
            //: self.chatModel?.logId = logId
            self.chatModel?.logId = logId
            //: self.chatModel?.videoStage = .Calling
            self.chatModel?.videoStage = .Calling
            //: self.interactionView.showViewsViaStage()
            self.interactionView.characterActual()
            //: createTalkRoom()
            succeedSegment()
            //: self.stopBGM()
            self.box()
        }
    }
}

// MARK: - BecauseObjectProtocol【拨打方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCallerDelegate {
extension FuturismViewDelegate: BecauseObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {
    func pointBlank(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(show_factorStudyPrivateText) + user_begSiteDeadlineIdent.lowercased() + String(noti_republicUrl.prefix(5))).localized
            //: if json["cancelSex"].stringValue == Gender.male.rawValue {
            if json[(String(m_missName.prefix(8)) + user_slideText.replacingOccurrences(of: "pin", with: "x"))].stringValue == RecordNameLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(mainCompriseMsg.prefix(5)) + "s hung" + String(user_careId.suffix(3))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.iconThemeBringBackPlace(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.doWhen()
            //: TalkingSocketManager.shared.currFindVC = nil
            AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
        }

        //: popCurrentViewController()
        offeTimeAnimated()
    }

    /// 对方接受了来电，建立音频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func willData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: app_minimizeFormat.reversed(), encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow

        //: self.stopBGM()
        self.box()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.characterActual()
        //: self.createTalkRoom()
        self.succeedSegment()
    }

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {
    func freshData(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)

        //: let endType = json["endType"].intValue
        let endType = json[(String(main_gainCreateRankId))].intValue
        //: var typeStr = "She has hung up".localized
        var typeStr = (String(show_factorStudyPrivateText) + user_begSiteDeadlineIdent.lowercased() + String(noti_republicUrl.prefix(5))).localized
        //: if json["stopSex"].stringValue == Gender.male.rawValue {
        if json[(appBringFluentTitle.replacingOccurrences(of: "authorize", with: "t") + String(user_deviceId))].stringValue == RecordNameLiteral.male.rawValue {
            //: typeStr = "He has hung up".localized
            typeStr = (String(mainCompriseMsg.prefix(5)) + "s hung" + String(user_careId.suffix(3))).localized
        }

        //: if endType == 1 {
        if endType == 1 { // 付费方余额不足
            //: if AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue {
                //: typeStr = "Insufficient gold coins, call has ended".localized
                typeStr = String(bytes: notiThumbText.map{$0^178}, encoding: .utf8)!.localized
                //: } else {
            } else {
                //: typeStr = "The other hung up the phone".localized
                typeStr = (String(notiMuseValue.prefix(6)) + "her " + String(mainTodayCookieData.prefix(7)) + String(showForceUrl)).localized
            }
        }
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.iconThemeBringBackPlace(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.doWhen()
            //: TalkingSocketManager.shared.currFindVC = nil
            AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.detailGenderDismiss()
        //: self.popCurrentViewController()
        self.offeTimeAnimated()
        //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: noti_dismissText, object: nil)
    }

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {
    func uniformAdd(msg: String, data: [String: Any]) {
        //: guard AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
        guard AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue else { return }

        //: let index = data["type"] as? String ?? ""
        let index = data[(k_ceasePath.replacingOccurrences(of: "format", with: "yp"))] as? String ?? ""
        //: if index == "2" {
        if index == "2" {
            //: guard let jumpUrl = data["jumpUrl"] as? String  else { return }
            guard let jumpUrl = data[(String(user_capOfflyContent))] as? String else { return }
            //: let httpsUrl = AdministratorThen.share.appConfigMode.urlH5Domain+jumpUrl
            let httpsUrl = AdministratorThen.share.appConfigMode.urlH5Domain + jumpUrl
            //: var config = TalkingWebConfig.init()
            var config = MerelyFireConfig()
            //: config.isDiscountsView = true
            config.isDiscountsView = true
            //: DumpPushManager.share.func__pushToWebVC(urlStr: httpsUrl, webConfig: config)
            DumpPushManager.share.noneControlInstall(urlStr: httpsUrl, webConfig: config)
            //: } else {
        } else {
            //: TalkingAlertShow.alert(title: nil,
            GentleAlertShow.aboveSixth(title: nil,
                                        //: message: msg,
                                        message: msg,
                                        //: leftBtnTitle: "Cancel".localized,
                                        leftBtnTitle: (String(dreamWaveMessage.prefix(6))).localized,
                                        //: rightBtnTitle: "Get gold coins".localized) {
                                        rightBtnTitle: (String(noti_helloUrl) + notiRegularIdent.replacingOccurrences(of: "leaf", with: "s")).localized)
            {
                //: TalkingAlertShow.hideAlert()
                GentleAlertShow.someButton()
                //: } rightBlock: {
            } rightBlock: {
                //: DumpPushManager.share.func__jumpToWebRecharge(clickEvent: clickLess1minButton, sufficient: false)
                DumpPushManager.share.shapeStyle(clickEvent: notiNoContent, sufficient: false)
            }
        }
    }

    /// 女性通话积分激励动效展示
    //: func socket_videoChat_onCallMotivation(data: [String: Any]) {
    func videoChild(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: let addBean = json["addBean"].stringValue
        let addBean = json[(String(app_reverseStreetUniteUrl))].stringValue
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: interactionView.showPointAnimaData(point: addBean)
            interactionView.rateBit(point: addBean)
            //: } else {
        } else {
            //: self.miniView?.showPointAnimaData(point: addBean)
            self.miniView?.exhibit(point: addBean)
        }
    }
}

// MARK: - ExpectObjectProtocol【接收方】

//: extension TalkingVoiceChatViewController: SocketManagerVideoChatCalledPartyDelegate {
extension FuturismViewDelegate: ExpectObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func miniskirt(data: [String: Any]) {
        //: if self.miniView == nil {
        if self.miniView == nil {
            //: let json = JSON(data)
            let json = JSON(data)
            //: var typeStr = "She has hung up".localized
            var typeStr = (String(show_factorStudyPrivateText) + user_begSiteDeadlineIdent.lowercased() + String(noti_republicUrl.prefix(5))).localized
            //: if json["refuseSex"].stringValue == Gender.male.rawValue {
            if json[(appTenderUrl.replacingOccurrences(of: "warm", with: "u") + "eSex")].stringValue == RecordNameLiteral.male.rawValue {
                //: typeStr = "He has hung up".localized
                typeStr = (String(mainCompriseMsg.prefix(5)) + "s hung" + String(user_careId.suffix(3))).localized
            }
            //: self.func__showStatusBarErrorMsg(showMsg: typeStr)
            self.iconThemeBringBackPlace(showMsg: typeStr)
            //: } else {
        } else {
            //: self.miniView?.dismissWhenCallEnd()
            self.miniView?.doWhen()
            //: TalkingSocketManager.shared.currFindVC = nil
            AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
        }
        //: popCurrentViewController()
        offeTimeAnimated()
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {
    func judge(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.chatModel?.logId = json["logId"].intValue
        self.chatModel?.logId = json[String(bytes: app_minimizeFormat.reversed(), encoding: .utf8)!].intValue
        //: self.chatModel?.videoStage = .Calling
        self.chatModel?.videoStage = .Calling
        //: self.chatModel?.renderPostion = .SelfOnWindow
        self.chatModel?.renderPostion = .SelfOnWindow
        //: self.stopBGM()
        self.box()
        //: self.interactionView.showViewsViaStage()
        self.interactionView.characterActual()
        //: self.createTalkRoom()
        self.succeedSegment()
    }
}

// MARK: - TwelveNoonObjectProtocol【异常处理】

//: extension TalkingVoiceChatViewController: TalkingSocketManagerErrorDelegate {
extension FuturismViewDelegate: TwelveNoonObjectProtocol {
    // 处理error情况
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func playerData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "refuseCall" { // 对方拒绝了通话
        if cmd == (String(k_floorId) + kDirectUrl.replacingOccurrences(of: "cooperative", with: "u") + "Call") { // 对方拒绝了通话
            //: if self.miniView == nil {
            if self.miniView == nil {
                //: self.func__showStatusBarErrorMsg(showMsg: "No answer".localized)
                self.iconThemeBringBackPlace(showMsg: (String(user_fitMsg) + mainSteerMessage.replacingOccurrences(of: "load", with: "er")).localized)
                //: } else {
            } else {
                //: self.miniView?.dismissWhenCallEnd()
                self.miniView?.doWhen()
                //: TalkingSocketManager.shared.currFindVC = nil
                AdviceLandscapeReactiveCompatible.shared.currFindVC = nil
            }
            //: self.popCurrentViewController()
            self.offeTimeAnimated()

            //: } else if cmd == "onAcceptCall" && errorNo == 101 { // 接听通话时，余额不足
        } else if cmd == (String(main_statementFormat)) && errorNo == 101 { // 接听通话时，余额不足
            //: guard AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue else { return }
            //: DumpPushManager.share.func__jumpToWebRecharge(sufficient: false)
            DumpPushManager.share.shapeStyle(sufficient: false)

            //: } else if cmd == "onAcceptCall" && errorNo == 102 { // 对方已取消通话
        } else if cmd == (String(main_statementFormat)) && errorNo == 102 { // 对方已取消通话
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.iconThemeBringBackPlace(showMsg: msg)
            //: self.popCurrentViewController()
            self.offeTimeAnimated()
        }
    }
}

// MARK: - AdviceProcedureViewDelegate【通话按钮操作事件】

//: extension TalkingVoiceChatViewController: Talking1v1VoiceInteractionViewDelegate {
extension FuturismViewDelegate: AdviceProcedureViewDelegate {
    /// 接听音频通话
    //: func interactionView_shouldAcceptCall() {
    func appealActual() {
        //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "acceptCall", "data": ["uid": self.chatModel?.pairUid]])
        AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (kCommitTalkAfterValue.lowercased() + "tCal" + app_viaTitle.lowercased()), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: mainOwnerId.map{$0^46}, encoding: .utf8)!: self.chatModel?.pairUid]])
    }

    /// 拨打方结束通话
    //: func interactionView_shouldEndCall() {
    func create() {
        //: if self.chatModel?.videoStage == .Waiting {
        if self.chatModel?.videoStage == .Waiting { // 拨打方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.iconThemeBringBackPlace(showMsg: (String(userAccountingRankReMessage.suffix(4)) + "s ar" + String(dreamSpanHeadName.prefix(6)) + "elled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.chatModel?.logId]])
            AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(m_silentIdentificationStr) + String(noti_effectiveTitle)), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: app_minimizeFormat.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
            //: } else if self.chatModel?.videoStage == .Recieved {
        } else if self.chatModel?.videoStage == .Recieved { // 接收方取消【未通话】
            //: self.func__showStatusBarErrorMsg(showMsg: "Calls are cancelled".localized)
            self.iconThemeBringBackPlace(showMsg: (String(userAccountingRankReMessage.suffix(4)) + "s ar" + String(dreamSpanHeadName.prefix(6)) + "elled").localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "refuseCall", "data": ["uid": self.chatModel?.pairUid]])
            AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(k_floorId) + kDirectUrl.replacingOccurrences(of: "cooperative", with: "u") + "Call"), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: mainOwnerId.map{$0^46}, encoding: .utf8)!: self.chatModel?.pairUid]])
            //: } else {
        } else { // 【通话中】
            //: self.func__showStatusBarErrorMsg(showMsg: "The call has ended".localized)
            self.iconThemeBringBackPlace(showMsg: (String(kChooseIdent) + String(dream_priorityFormat)).localized)
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "stopCall", "data": ["logId": self.chatModel?.logId]])
            AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: show_oneTitle.map{earlyContext(appeal: $0)}, encoding: .utf8)!: (String(noti_electronicLowerHostValue.prefix(8))), String(bytes: appCloseContent.map{mySegmentShow(cap: $0)}, encoding: .utf8)!: [String(bytes: app_minimizeFormat.reversed(), encoding: .utf8)!: self.chatModel?.logId]])
        }

        //: self.interactionView.func_giftViewDismiss()
        self.interactionView.detailGenderDismiss()
        //: self.popCurrentViewController()
        self.offeTimeAnimated()
    }

    /// 是否静音
    //: func interactionView_shouldChangeMute(isMute: Bool) {
    func onAccessFog(isMute: Bool) {
        //: self.voiceManager.setMute(isMute: isMute)
        self.voiceManager.quantitativeRelation(isMute: isMute)
    }

    /// 是否免提
    //: func interactionView_shouldChangeSpeaker(isHandsfree: Bool) {
    func hair(isHandsfree: Bool) {
        //: self.voiceManager.setHandsfree(isHandsfree: isHandsfree)
        self.voiceManager.publishArrow(isHandsfree: isHandsfree)
    }

    /// 展示语音通话小窗口
    //: func interactionView_shouldDisplayMiniWindow() {
    func titleWarning() {
        //: TalkingSocketManager.shared.currFindVC = self
        AdviceLandscapeReactiveCompatible.shared.currFindVC = self
        //: self.miniView = TalkingVoiceMiniView.buildVoiceMiniView()
        self.miniView = TapReactiveCompatible.channelize()
        //: self.miniView?.tapGestureBlock = { [weak self] in
        self.miniView?.tapGestureBlock = { [weak self] in // 还原回通话界面
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.currentViewController()?.navigationController?.pushViewController(self, animated: true)
            self.springEquinox()?.navigationController?.pushViewController(self, animated: true)
        }
        //: let talkingTime = self.interactionView.getCurrentTalkingTime()
        let talkingTime = self.interactionView.container()
        //: self.miniView?.updateTalkingTime(talkingTime)
        self.miniView?.sampleRandom(talkingTime)
        //: popCurrentViewController()
        offeTimeAnimated()
    }

    /// 更新小窗口通话时间
    //: func interactionView_shouldUpdateTime(time: Double) {
    func dealRemind(time: Double) {
        //: guard self.miniView != nil else { return }
        guard self.miniView != nil else { return }
        //: self.miniView?.updateTalkingTime(time)
        self.miniView?.sampleRandom(time)
    }

    /// 收到礼物消息展示
    //: @objc func receiveGiftPlay(giftArr: Array<Any>) {
    @objc func norBuilder(giftArr: [Any]) {
        //: interactionView.addGiftEffectModelArr(giftArr: giftArr)
        interactionView.quantityeraction(giftArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingVoiceChatViewController {
extension FuturismViewDelegate {
    /// 添加视图
    //: private func setupSubviews() {
    private func installBySubviews() {
        //: self.view.addSubview(interactionView)
        self.view.addSubview(interactionView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func stopConstraint() {
        //: interactionView.snp.makeConstraints { make in
        interactionView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
    }
}
