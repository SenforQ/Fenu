
//: Declare String Begin

/*: "msgType" :*/
fileprivate let show_remainData:String = "msgTypeor video remind anniversary design"

/*: "tips" :*/
fileprivate let mainCapturePairValue:[Character] = ["t","i","p","s"]

/*: "extra" :*/
fileprivate let userAppNoseStr:String = "extwhether"

/*: "msgInfo" :*/
fileprivate let dream_scaleMsg:String = "miracle except missingmsgIn"
fileprivate let notiRenderName:[Character] = ["f","o"]

/*: "user" :*/
fileprivate let kPriceName:String = "usargument"

/*: "uid" :*/
fileprivate let k_promptKey:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let k_shrinkPartText:[UInt8] = [0x81,0x86,0x8c,0x84,0x81,0x8e,0x82,0x8a]

/*: "headPic" :*/
fileprivate let appFluentValue:[UInt8] = [0xf8,0xf5,0xf1,0xf4,0xc0,0xf9,0xf3]

private func demonstrateMore(fairly num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "age" :*/
fileprivate let appAdmitId:[UInt8] = [0x34,0x3a,0x38]

fileprivate func imaginaryBeing(under num: UInt8) -> UInt8 {
    let value = Int(num) + 45
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "gift" :*/
fileprivate let user_tabWeeklyHealthyValue:String = "gfractiont"

/*: "isGroup" :*/
fileprivate let showRefuseId:String = "opposite separate chip hi everyisGro"
fileprivate let dreamStillPreviousMsg:String = "UP"

/*: "WantGift" :*/
fileprivate let app_cornerThanksKey:String = "style compareWant"

/*: "txt" :*/
fileprivate let appDominantId:String = "arcxarc"

/*: "img" :*/
fileprivate let mainManeAdStr:[Character] = ["i","m","g"]

/*: "video" :*/
fileprivate let showDesignPath:[Character] = ["v","i","d","e","o"]

/*: "audio" :*/
fileprivate let k_processingTitle:String = "countdio"

/*: "messageType" :*/
fileprivate let k_broadcastStr:[Character] = ["m","e","s","s","a","g","e","T","y","p"]
fileprivate let mSmallStrikeValue:String = "beyond"

/*: "contentType" :*/
fileprivate let dream_arrowValue:[Character] = ["c","o"]
fileprivate let noti_bronzeContent:String = "ntvillagent"

/*: "ImgMsg" :*/
fileprivate let dreamAlwaysPrimaryText:String = "sale appropriateImgMsg"

/*: "imageUri" :*/
fileprivate let appLookIdent:[Character] = ["i","m","a","g","e","U"]
fileprivate let notiSkinData:[Character] = ["r","i"]

/*: "type" :*/
fileprivate let m_orangeUrl:String = "tywhole"

/*: "pic" :*/
fileprivate let dream_bindUrl:String = "pflame"

/*: "isBurn" :*/
fileprivate let m_medalMannerFormat:String = "I"
fileprivate let showLivingMsg:String = "why filesBurn"

/*: "giftId" :*/
fileprivate let main_adUrl:String = "dark"
fileprivate let userInjuryPath:[Character] = ["i","f","t","I","d"]

/*: "params" :*/
fileprivate let user_manageValue:String = "paramincrease"

/*: "audioLength" :*/
fileprivate let showSumTitle:String = "lust photo remarkaudioL"
fileprivate let user_quietStr:String = "ENGTH"

/*: "len" :*/
fileprivate let show_slatMsg:String = "leta"

/*: "audioSandbox" :*/
fileprivate let dreamPropertyIdent:String = "audioSevery plus properly conversation"

/*: "senderId" :*/
fileprivate let user_panStr:String = "senegativeder"
fileprivate let dream_tagStr:[Character] = ["I","d"]

/*: "AudioMsg" :*/
fileprivate let main_visitorValue:String = "consumer"
fileprivate let dreamSustainKey:[Character] = ["u","d","i","o","M","s","g"]

/*: "content" :*/
fileprivate let noti_coalId:String = "cfocus"
fileprivate let m_extraText:String = "NTENT"

/*: "TxtMsg" :*/
fileprivate let appAssertMessage:String = "TxtMsillegal accept"
fileprivate let userShadeFaintIdent:[Character] = ["g"]

/*: "message" :*/
fileprivate let noti_problemTitle:[Character] = ["m","e","s","s","a","g","e"]

/*: "quoteMsgId" :*/
fileprivate let showOuterIdent:String = "quoteMdraft scale"

/*: "quoteMsgInfo" :*/
fileprivate let main_boundaryTitle:[Character] = ["q","u","o","t","e","M","s","g","I","n","f"]
fileprivate let main_therePlatformMessage:[Character] = ["o"]

/*: "rYMsgType" :*/
fileprivate let show_customMsg:String = "effect computer ballot traffic seriousrYMsgTy"
fileprivate let app_mediaId:[Character] = ["p","e"]

/*: "GJ:Sess:AutoGreet:TxtCustom" :*/
fileprivate let app_statusKey:String = "thread instruction myGJ:Ses"
fileprivate let m_constantId:String = "toGreeafter trim support collect"
fileprivate let dream_tellFormat:String = "t:TxtCwed cycle month copy"

/*: "changeDirection" :*/
fileprivate let kMaleValue:String = "changattractive"
fileprivate let mParticipantText:String = "ctiosum"

/*: "system" :*/
fileprivate let dreamLateValue:[Character] = ["s","y","s","t","e","m"]

/*: "title" :*/
fileprivate let mainThinkReasonData:String = "titleffectively"

/*: "icon" :*/
fileprivate let show_culturalMsg:[Character] = ["i","c","o","n"]

/*: "all" :*/
fileprivate let notiYellowValue:[Character] = ["a","l","l"]

/*: "icon_chats_pcr" :*/
fileprivate let mainVisualUrl:[Character] = ["i","c","o","n","_","c","h","a","t"]
fileprivate let mGenderCooperativePath:String = "shall refs_pcr"

/*: "status" :*/
fileprivate let dream_constraintSupplyFormat:[Character] = ["s","t","a","t","u","s"]

/*: "msgIncome" :*/
fileprivate let k_paperUrl:String = "msgIncomethin weekly data reward commercial"

/*: "msgId" :*/
fileprivate let showUnitPath:String = "msgIdrunning agency unable storage"

/*: "PubliclyTransformable" :*/
fileprivate let main_lovelyContent:String = "merely aboveTMsgTr"
fileprivate let notiEconomicId:[Character] = ["a","t","e","S","t","a","t","u","s"]

/*: "TMsgTranslatedContent" :*/
fileprivate let mainSaveerAnnouncementKey:String = "TMsgTrdraw called"
fileprivate let notiFutureAboutPath:String = "tedCobeyond mystery home"

/*: "formatType" :*/
fileprivate let showWithinName:String = "ftumblerma"
fileprivate let kErrorCenterFormat:String = "tTypesquare separate scribe announcement"

/*: "plain" :*/
fileprivate let m_normallyValue:String = "pllancein"

/*: "imageData" :*/
fileprivate let k_attentionLeadingMsg:String = "imasmart"
fileprivate let mGivingMessage:String = "tight"

/*: "VideoMsg" :*/
fileprivate let dream_fractionName:String = "establish signal manageVideoM"
fileprivate let notiOppositionUrl:String = "senhance"

/*: "match" :*/
fileprivate let k_lightWalkName:String = "mfirech"

/*: "userIntro" :*/
fileprivate let app_pendingName:String = "userInexample earn"
fileprivate let mainBlueKey:[Character] = ["t","r","o"]

/*: "targetId" :*/
fileprivate let showMultipleId:[Character] = ["t","a","r","g","e","t","I","d"]

/*: "yyyy-MM-dd HH:mm:ss" :*/
fileprivate let k_chooseKey:String = "YYYY"
fileprivate let dream_statusFormat:String = "stroke background magnitude distribution minorHH:"
fileprivate let k_exhibitStr:String = "fiscal narrow begin king viewmm:ss"

/*: "sendTime" :*/
fileprivate let mGenTargetMessage:[Character] = ["s","e","n"]
fileprivate let noti_steadCommonTitle:String = "reply take across extension drawdTime"

/*: "isRead" :*/
fileprivate let dreamRawStarMessage:[Character] = ["i","s","R","e","a","d"]

/*: "audioData" :*/
fileprivate let dream_coalFollowPath:[Character] = ["a","u","d","i"]
fileprivate let mainEngagementUrl:String = "discount or typical since courseoData"

/*: "audioUri" :*/
fileprivate let app_dirtFormat:[Character] = ["a","u","d","i","o","U","r"]
fileprivate let mProcedureUrl:String = "identify"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AdvocatePatentThen.swift

//: import Foundation
import Foundation

// MARK: - 枚举定义

//: @objc public enum TXMessageContentType: UInt {
@objc public enum TaphouseUnsignedCount: UInt {
    //: case TXMessageContentType_text = 1
    case TXMessageContentType_text = 1
    //: case TXMessageContentType_picture
    case TXMessageContentType_picture
    //: case TXMessageContentType_gift
    case TXMessageContentType_gift
    //: case TXMessageContentType_askforGift
    case TXMessageContentType_askforGift
    //: case TXMessageContentType_voice
    case TXMessageContentType_voice
    //: case TXMessageContentType_local
    case TXMessageContentType_local
    //: case TXMessageContentType_tips
    case TXMessageContentType_tips
    //: case TXMessageContentType_userCard
    case TXMessageContentType_userCard
    //: case TXMessageContentType_video
    case TXMessageContentType_video
}

// MARK: - AdvocatePatentThen

//: @objcMembers public class TUISocialChatManager: NSObject {
@objcMembers public class AdvocatePatentThen: NSObject {
    // MARK: - 公共方法

    /// 获取消息插入的时间戳 毫秒
    //: public class func getMessageInsertTime() -> Int64 {
    public class func however() -> Int64 {
        //: let curDate = Date().timeIntervalSince1970
        let curDate = Date().timeIntervalSince1970
        //: let msInterval = Int64(floor(curDate * 1000))
        let msInterval = Int64(floor(curDate * 1000))
        //: var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime())
        var deltaTime = Int64(V2TIMManager.sharedInstance().getServerTime()) // 秒为单位
        //: if deltaTime < 1 {
        if deltaTime < 1 {
            //: deltaTime = 0
            deltaTime = 0
        }
        //: let timeStamp = msInterval - deltaTime
        let timeStamp = msInterval - deltaTime
        //: return timeStamp
        return timeStamp
    }

    // MARK: - 腾讯IM消息处理

    /// 腾讯IM消息插入
    //: public class func insertTXMessage(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ReconstructThen? {
    public class func countenance(withExtral extral: [String: Any], toUid: String, isSender: Bool, isTip: Bool) -> ReconstructThen? {
        //: let data = handleTXMessageSend(withExtral: extral, isSender: isSender, isTip: isTip)
        let data = likeRed(withExtral: extral, isSender: isSender, isTip: isTip)
        //: if let data = data {
        if let data = data {
            //: EdgeMsgListener.shared.func__handleMsg(targetId: toUid, msgCellData: data, isPrivete: true)
            EdgeMsgListener.shared.funcMethodDataCellOrganiseMark(targetId: toUid, msgCellData: data, isPrivete: true)
        }
        //: return data
        return data
    }

    /// 腾讯IM发送消息组装
    //: public class func handleTXMessageSend(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ReconstructThen? {
    public class func likeRed(withExtral extral: [String: Any], isSender: Bool, isTip: Bool) -> ReconstructThen? {
        //: if isTip {
        if isTip {
            //: var extralDict = extral
            var extralDict = extral
            //: extralDict["msgType"] = "tips"
            extralDict[(String(show_remainData.prefix(7)))] = (String(mainCapturePairValue))
            //: let extraDict = ["extra": extralDict]
            let extraDict = [(userAppNoseStr.replacingOccurrences(of: "whether", with: "ra")): extralDict]
            //: let extra = parseTXMessageJsonStr(data: extraDict)
            let extra = ownWith(data: extraDict)
            //: let data = TalkingChatTipsMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = DataGatherThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extra.data(using: .utf8)
            let cData = extra.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: data.innerMessage.isExcludedFromLastMessage = true
            data.innerMessage.isExcludedFromLastMessage = true
            //: let model = AbTalkingChatMsgBaseModel()
            let model = InformedConsentModel()
            //: if let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extralDict) {
            if let tipModel = try? TalkingSProspectModel(dictionary: extralDict) {
                //: model.tips = tipModel
                model.tips = tipModel
            }
            //: data.msgModel = model
            data.msgModel = model
            //: return data
            return data
        }

        //: var tempDict = extral
        var tempDict = extral
        //: guard let msgInfo = extral["msgInfo"] as? [String: Any] else {
        guard let msgInfo = extral[(String(dream_scaleMsg.suffix(5)) + String(notiRenderName))] as? [String: Any] else {
            //: return nil
            return nil
        }

        //: tempDict["user"] = [
        tempDict[(kPriceName.replacingOccurrences(of: "argument", with: "er"))] = [
            //: "uid": AdministratorThen.share.loginUserMode.userID,
            (String(k_promptKey)): AdministratorThen.share.loginUserMode.userID,
            //: "nickname": AdministratorThen.share.loginUserMode.nickname,
            String(bytes: k_shrinkPartText.map{$0^239}, encoding: .utf8)!: AdministratorThen.share.loginUserMode.nickname,
            //: "headPic": AdministratorThen.share.loginUserMode.headPic,
            String(bytes: appFluentValue.map{demonstrateMore(fairly: $0)}, encoding: .utf8)!: AdministratorThen.share.loginUserMode.headPic,
            //: "age": AdministratorThen.share.loginUserMode.age,
            String(bytes: appAdmitId.map{imaginaryBeing(under: $0)}, encoding: .utf8)!: AdministratorThen.share.loginUserMode.age,
        ]

        //: let extraDict = ["extra": tempDict]
        let extraDict = [(userAppNoseStr.replacingOccurrences(of: "whether", with: "ra")): tempDict]
        //: let extraString = parseTXMessageJsonStr(data: extraDict)
        let extraString = ownWith(data: extraDict)

        // 礼物
        //: if msgInfo["msgType"] as? String == "gift" {
        if msgInfo[(String(show_remainData.prefix(7)))] as? String == (user_tabWeeklyHealthyValue.replacingOccurrences(of: "fraction", with: "if")) {
            //: var data: ReconstructThen
            var data: ReconstructThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(showRefuseId.suffix(5)) + dreamStillPreviousMsg.lowercased())] as? String == "1" { // 群聊
                //: data = TalkingGroupChatGiftMsgCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = BuildCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = FireThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = FireThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? InformedConsentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 索要礼物
        //: if msgInfo["msgType"] as? String == "WantGift" {
        if msgInfo[(String(show_remainData.prefix(7)))] as? String == (String(app_cornerThanksKey.suffix(4)) + "Gift") {
            //: let data = LiteralCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = LiteralCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? InformedConsentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 文字
        //: if msgInfo["msgType"] as? String == "txt" {
        if msgInfo[(String(show_remainData.prefix(7)))] as? String == (appDominantId.replacingOccurrences(of: "arc", with: "t")) {
            //: var data: ReconstructThen
            var data: ReconstructThen
            //: if msgInfo["isGroup"] as? String == "1" {  // 群聊
            if msgInfo[(String(showRefuseId.suffix(5)) + dreamStillPreviousMsg.lowercased())] as? String == "1" { // 群聊
                //: data = ConnectThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = ConnectThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else {
                //: data = WarnThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                data = WarnThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? InformedConsentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 图片
        //: if msgInfo["msgType"] as? String == "img" {
        if msgInfo[(String(show_remainData.prefix(7)))] as? String == (String(mainManeAdStr)) {
            //: let data = GageBubbleCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = GageBubbleCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? InformedConsentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 视频
        //: if msgInfo["msgType"] as? String == "video" {
        if msgInfo[(String(show_remainData.prefix(7)))] as? String == (String(showDesignPath)) {
            //: let data = EnableicialCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = EnableicialCellData(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? InformedConsentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 语音
        //: if msgInfo["msgType"] as? String == "audio" {
        if msgInfo[(String(show_remainData.prefix(7)))] as? String == (k_processingTitle.replacingOccurrences(of: "count", with: "au")) {
            //: let data = HeapReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = HeapReactiveCompatible(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? InformedConsentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        // 打招呼
        //: if extral["gift"] != nil && (msgInfo["messageType"] as? Int) == 4 {
        if extral[(user_tabWeeklyHealthyValue.replacingOccurrences(of: "fraction", with: "if"))] != nil && (msgInfo[(String(k_broadcastStr) + mSmallStrikeValue.replacingOccurrences(of: "beyond", with: "e"))] as? Int) == 4 {
            //: let data = WarnThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            let data = WarnThen(direction: isSender ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: let cData = extraString.data(using: .utf8)
            let cData = extraString.data(using: .utf8)
            //: data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            data.innerMessage = V2TIMManager.sharedInstance().createCustomMessage(cData)
            //: if let model = try? AbTalkingChatMsgBaseModel(dictionary: tempDict) {
            if let model = try? InformedConsentModel(dictionary: tempDict) {
                //: data.msgModel = model
                data.msgModel = model
            }
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM重发消息组装 （只有私聊在用）
    //: public class func handleTXMessageReSend(withData cellData: ReconstructThen) -> ReconstructThen? {
    public class func windowWall(withData cellData: ReconstructThen) -> ReconstructThen? {
        // 图片
        //: if let reCellData = cellData as? GageBubbleCellData {
        if let reCellData = cellData as? GageBubbleCellData {
            //: let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            let paths = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true)
            //: var filePath = paths.first!
            var filePath = paths.first!
            //: filePath = (filePath as NSString).appendingPathComponent(CreditsThen.getImageFileName())
            filePath = (filePath as NSString).appendingPathComponent(CreditsThen.animalImage())
            //: filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((reCellData.imageUrl! as NSString).lastPathComponent)

            //: if !FileManager.default.fileExists(atPath: filePath) {
            if !FileManager.default.fileExists(atPath: filePath) {
                //: return nil
                return nil
            }

            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: msgInfo["contentType"] = "ImgMsg"
            msgInfo[(String(dream_arrowValue) + noti_bronzeContent.replacingOccurrences(of: "village", with: "e") + "Type")] = (String(dreamAlwaysPrimaryText.suffix(6)))
            //: msgInfo["msgType"] = "img"
            msgInfo[(String(show_remainData.prefix(7)))] = (String(mainManeAdStr))
            //: msgInfo["imageUri"] = filePath
            msgInfo[(String(appLookIdent) + String(notiSkinData))] = filePath
            //: dic["msgInfo"] = msgInfo
            dic[(String(dream_scaleMsg.suffix(5)) + String(notiRenderName))] = msgInfo

            //: params["type"] = "ImgMsg"
            params[(m_orangeUrl.replacingOccurrences(of: "whole", with: "pe"))] = (String(dreamAlwaysPrimaryText.suffix(6)))
            //: params["pic"] = filePath
            params[(dream_bindUrl.replacingOccurrences(of: "flame", with: "ic"))] = filePath

            // 重发私密照片
            //: let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            let isBurn = reCellData.msgModel.msgInfo.customSend.isBurn
            //: if !isBurn.isEmpty {
            if !isBurn.isEmpty {
                //: params["isBurn"] = isBurn
                params[(m_medalMannerFormat.lowercased() + String(showLivingMsg.suffix(5)))] = isBurn
                //: } else {
            } else {
                //: params["isBurn"] = "0"
                params[(m_medalMannerFormat.lowercased() + String(showLivingMsg.suffix(5)))] = "0"
            }

            //: let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            let giftId = reCellData.msgModel.msgInfo.customSend.giftId
            //: if !giftId.isEmpty {
            if !giftId.isEmpty {
                //: params["giftId"] = giftId
                params[(main_adUrl.replacingOccurrences(of: "dark", with: "g") + String(userInjuryPath))] = giftId
                //: } else {
            } else {
                //: params["giftId"] = "0"
                params[(main_adUrl.replacingOccurrences(of: "dark", with: "g") + String(userInjuryPath))] = "0"
            }

            //: dic["params"] = params
            dic[(user_manageValue.replacingOccurrences(of: "increase", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = likeRed(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 视频
        //: if let reCellData = cellData as? EnableicialCellData {
        if let reCellData = cellData as? EnableicialCellData {
            //: reCellData.status = .Msg_Status_Fail
            reCellData.status = .Msg_Status_Fail
            //: return reCellData
            return reCellData
        }

        // 语音
        //: if let reCellData = cellData as? HeapReactiveCompatible {
        if let reCellData = cellData as? HeapReactiveCompatible {
            //: let voiceModel = DBUserInfoManager.cache_getAudioDB(message: reCellData.innerMessage)
            let voiceModel = DecisionMakerReactiveCompatible.cleanMessage(message: reCellData.innerMessage)
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]

            //: if !voiceModel.db_audioLength.isEmpty {
            if !voiceModel.db_audioLength.isEmpty {
                //: msgInfo["audioLength"] = voiceModel.db_audioLength
                msgInfo[(String(showSumTitle.suffix(6)) + user_quietStr.lowercased())] = voiceModel.db_audioLength
                //: params["len"] = voiceModel.db_audioLength
                params[(show_slatMsg.replacingOccurrences(of: "ta", with: "n"))] = voiceModel.db_audioLength
            }
            //: if !voiceModel.db_filePath.isEmpty {
            if !voiceModel.db_filePath.isEmpty {
                //: msgInfo["audioSandbox"] = voiceModel.db_filePath
                msgInfo[(String(dreamPropertyIdent.prefix(6)) + "andbox")] = voiceModel.db_filePath
                //: params["audioSandbox"] = voiceModel.db_filePath
                params[(String(dreamPropertyIdent.prefix(6)) + "andbox")] = voiceModel.db_filePath
            }
            //: msgInfo["msgType"] = "audio"
            msgInfo[(String(show_remainData.prefix(7)))] = (k_processingTitle.replacingOccurrences(of: "count", with: "au"))
            //: msgInfo["senderId"] = AdministratorThen.share.loginUserMode.userID
            msgInfo[(user_panStr.replacingOccurrences(of: "negative", with: "n") + String(dream_tagStr))] = AdministratorThen.share.loginUserMode.userID
            //: dic["msgInfo"] = msgInfo
            dic[(String(dream_scaleMsg.suffix(5)) + String(notiRenderName))] = msgInfo

            //: params["type"] = "AudioMsg"
            params[(m_orangeUrl.replacingOccurrences(of: "whole", with: "pe"))] = (main_visitorValue.replacingOccurrences(of: "consumer", with: "A") + String(dreamSustainKey))
            //: dic["params"] = params
            dic[(user_manageValue.replacingOccurrences(of: "increase", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = likeRed(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        // 文本
        //: if let reCellData = cellData as? WarnThen {
        if let reCellData = cellData as? WarnThen {
            //: var dic: [String: Any] = [:]
            var dic: [String: Any] = [:]
            //: var params: [String: Any] = [:]
            var params: [String: Any] = [:]
            //: var msgInfo: [String: Any] = [:]
            var msgInfo: [String: Any] = [:]

            //: msgInfo["content"] = reCellData.contentStr ?? ""
            msgInfo[(noti_coalId.replacingOccurrences(of: "focus", with: "o") + m_extraText.lowercased())] = reCellData.contentStr ?? ""
            //: msgInfo["msgType"] = "txt"
            msgInfo[(String(show_remainData.prefix(7)))] = (appDominantId.replacingOccurrences(of: "arc", with: "t"))
            //: dic["msgInfo"] = msgInfo
            dic[(String(dream_scaleMsg.suffix(5)) + String(notiRenderName))] = msgInfo
            //: params["type"] = "TxtMsg"
            params[(m_orangeUrl.replacingOccurrences(of: "whole", with: "pe"))] = (String(appAssertMessage.prefix(5)) + String(userShadeFaintIdent))
            //: params["message"] = reCellData.contentStr ?? ""
            params[(String(noti_problemTitle))] = reCellData.contentStr ?? ""

            //: if reCellData.msgModel.quoteMsgInfo.isQuoteMsg() == true {
            if reCellData.msgModel.quoteMsgInfo.image() == true { // 引用消息重发
                //: params["quoteMsgId"] = reCellData.msgModel.quoteMsgInfo.msgId
                params[(String(showOuterIdent.prefix(6)) + "sgId")] = reCellData.msgModel.quoteMsgInfo.msgId
                //: dic["quoteMsgInfo"] = reCellData.msgModel.quoteMsgInfo
                dic[(String(main_boundaryTitle) + String(main_therePlatformMessage))] = reCellData.msgModel.quoteMsgInfo
                    //: .toDictionary()
                    .toDictionary()
            }

            //: dic["params"] = params
            dic[(user_manageValue.replacingOccurrences(of: "increase", with: "s"))] = params

            //: let data = handleTXMessageSend(withExtral: dic, isSender: true, isTip: false)
            let data = likeRed(withExtral: dic, isSender: true, isTip: false)
            //: data?.status = .Msg_Status_Fail
            data?.status = .Msg_Status_Fail
            //: return data
            return data
        }

        //: return nil
        return nil
    }

    /// 腾讯IM接收消息组装
    //: public class func handleTXReceiveMessage(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ReconstructThen? {
    public class func more(withExtral dic: [String: Any], cusData: Data, tempModel: V2TIMMessage) -> ReconstructThen? {
        //: var cellData: ReconstructThen?
        var cellData: ReconstructThen?
        //: let extraDict = dic["extra"] as? [String: Any]
        let extraDict = dic[(userAppNoseStr.replacingOccurrences(of: "whether", with: "ra"))] as? [String: Any]
        //: let msgInfoDict = extraDict?["msgInfo"] as? [String: Any]
        let msgInfoDict = extraDict?[(String(dream_scaleMsg.suffix(5)) + String(notiRenderName))] as? [String: Any]
        //: let user = dic["user"] as? [String: Any] ?? [:]
        let user = dic[(kPriceName.replacingOccurrences(of: "argument", with: "er"))] as? [String: Any] ?? [:]

        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: extraDict) else {
        guard let model = try? InformedConsentModel(dictionary: extraDict) else {
            //: return nil
            return nil
        }
        //: var isSelf = (model.user.uid == AdministratorThen.share.loginUserMode.userID.intValue)
        var isSelf = (model.user.uid == AdministratorThen.share.loginUserMode.userID.intValue)

        // 后台代发送的自动打招呼消息
        //: var isAutoSendMsg = false
        var isAutoSendMsg = false
        //: if model.msgInfo.uid.intValue == AdministratorThen.share.loginUserMode.userID.intValue &&
        if model.msgInfo.uid.intValue == AdministratorThen.share.loginUserMode.userID.intValue &&
            //: model.msgInfo.messageType.rawValue == 3 {
            model.msgInfo.messageType.rawValue == 3
        {
            //: isSelf = true
            isSelf = true
            //: isAutoSendMsg = true
            isAutoSendMsg = true
        }

        //: if dic["rYMsgType"] as? String == "GJ:Sess:AutoGreet:TxtCustom" {
        if dic[(String(show_customMsg.suffix(7)) + String(app_mediaId))] as? String == (String(app_statusKey.suffix(6)) + "s:Au" + String(m_constantId.prefix(6)) + String(dream_tellFormat.prefix(6)) + "ustom") {
            //: isSelf = msgInfoDict?["changeDirection"] as? Bool ?? false
            isSelf = msgInfoDict?[(kMaleValue.replacingOccurrences(of: "attractive", with: "e") + "Dire" + mParticipantText.replacingOccurrences(of: "sum", with: "n"))] as? Bool ?? false
        }

        //: let contentType = messageCellType(dic)
        let contentType = kindBirthType(dic)

        // 系统消息
        //: if extraDict?["msgType"] as? String == "system" ||
        if extraDict?[(String(show_remainData.prefix(7)))] as? String == (String(dreamLateValue)) ||
            //: contentType == .TXMessageContentType_tips {
            contentType == .TXMessageContentType_tips
        {
            //: let model = AbTalkingChatMsgBaseModel()
            let model = InformedConsentModel()
            //: let tipModel = try? AbTalkingChatMsgTipsModel(dictionary: extraDict)
            let tipModel = try? TalkingSProspectModel(dictionary: extraDict)
            //: cellData = TalkingChatTipsMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = DataGatherThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let content = dic["content"] as? String {
            if let content = dic[(noti_coalId.replacingOccurrences(of: "focus", with: "o") + m_extraText.lowercased())] as? String {
                //: tipModel?.content = content
                tipModel?.content = content
            }
            //: if tipModel != nil {
            if tipModel != nil {
                //: model.tips = tipModel!
                model.tips = tipModel!
            }
            //: cellData?.msgModel = model
            cellData?.msgModel = model
            //: return cellData
            return cellData
        }

        // 其他消息类型的创建...
        //: switch contentType {
        switch contentType {
        //: case .TXMessageContentType_picture:
        case .TXMessageContentType_picture:
            //: cellData = GageBubbleCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = GageBubbleCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_video:
        case .TXMessageContentType_video:
            //: cellData = EnableicialCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = EnableicialCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: case .TXMessageContentType_gift:
        case .TXMessageContentType_gift:
            //: if let groupID = tempModel.groupID, !groupID.isEmpty {
            if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊gift
                //: cellData = TalkingGroupChatGiftMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = BuildCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊gift
                //: cellData = FireThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = FireThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }

        //: case .TXMessageContentType_askforGift:
        case .TXMessageContentType_askforGift: // 索要礼物
            //: let tempDic = parseTXMessageData(data: cusData)
            let tempDic = textData(data: cusData)
            //: if let extraData = tempDic["extra"] as? [String: Any],
            if let extraData = tempDic[(userAppNoseStr.replacingOccurrences(of: "whether", with: "ra"))] as? [String: Any],
               //: let msgInfoData = extraData["msgInfo"] as? [String: Any],
               let msgInfoData = extraData[(String(dream_scaleMsg.suffix(5)) + String(notiRenderName))] as? [String: Any],
               //: !msgInfoData.isEmpty {
               !msgInfoData.isEmpty
            {
                //: if let info = try? AbTalkingChatMsgInfoModel(dictionary: msgInfoData) {
                if let info = try? TingModel(dictionary: msgInfoData) {
                    //: model.msgInfo = info
                    model.msgInfo = info
                }
            }
            //: cellData = LiteralCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = LiteralCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: cellData?.msgModel = model
            cellData?.msgModel = model

        //: case .TXMessageContentType_voice:
        case .TXMessageContentType_voice:
            //: cellData = HeapReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = HeapReactiveCompatible(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            //: if let audioCellData = cellData as? HeapReactiveCompatible {
            if let audioCellData = cellData as? HeapReactiveCompatible {
                //: if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                if audioCellData.voiceModel.db_voiceCacheWrap.msgId.isEmpty {
                    //: let mainKey = checkPrimaryKey(withMsgModel: tempModel)
                    let mainKey = withModel(withMsgModel: tempModel)
                    //: var cacheWrap = WCDBVoiceMsgTable.db_getVoiceMsg(with: mainKey)
                    var cacheWrap = FireMsgTable.windowTa(with: mainKey)
                    //: if cacheWrap == nil {
                    if cacheWrap == nil {
                        //: cacheWrap = WCDBVoiceMsgTable.db_inserVoiceMsgToDB(packageDict(withTXMessage: tempModel))
                        cacheWrap = FireMsgTable.tempPlatform(sinkMessage(withTXMessage: tempModel))
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        //: } else {
                    } else {
                        //: audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                        audioCellData.voiceModel.db_voiceCacheWrap = cacheWrap!
                    }
                }
            }

        //: case .TXMessageContentType_local:
        case .TXMessageContentType_local:
            //: cellData = TalkingChatMatchMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            cellData = CompartmentCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)

        //: default:
        default:
            //: let title = extraDict?["title"] as? String ?? ""
            let title = extraDict?[(mainThinkReasonData.replacingOccurrences(of: "effectively", with: "e"))] as? String ?? ""
            //: let content = extraDict?["content"] as? String ?? ""
            let content = extraDict?[(noti_coalId.replacingOccurrences(of: "focus", with: "o") + m_extraText.lowercased())] as? String ?? ""
            //: let img = extraDict?["img"] as? String ?? ""
            let img = extraDict?[(String(mainManeAdStr))] as? String ?? ""
            //: if !title.isEmpty || !content.isEmpty || !img.isEmpty {
            if !title.isEmpty || !content.isEmpty || !img.isEmpty { // 系统新消息
                //: cellData = TalkingChatSystemNotifJumpMsgCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = SampleAudioSystemCellData(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.extraDict = extraDict
                cellData?.extraDict = extraDict
                //: } else if let groupID = tempModel.groupID, !groupID.isEmpty {
            } else if let groupID = tempModel.groupID, !groupID.isEmpty { // 群聊text
                //: cellData = ConnectThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = ConnectThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: } else {
            } else { // 私聊text
                //: cellData = WarnThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                cellData = WarnThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
            }
        }

        // 设置用户信息
        //: if model.user.nickname.isEmpty {
        if model.user.nickname.isEmpty {
            //: var userDict = user
            var userDict = user
            //: userDict.merge(msgInfoDict ?? [:]) { (_, new) in new }
            userDict.merge(msgInfoDict ?? [:]) { _, new in new }
            //: if let userModel = try? AbTalkingChatMsgUserModel(dictionary: userDict) {
            if let userModel = try? LogModel(dictionary: userDict) {
                //: if userModel.uid == 0 {
                if userModel.uid == 0 {
                    //: userModel.uid = userDict["id"] as? Int ?? 0
                    userModel.uid = userDict["id"] as? Int ?? 0
                }
                //: if userModel.headPic.isEmpty == true {
                if userModel.headPic.isEmpty == true {
                    //: userModel.headPic = userDict["icon"] as? String ?? ""
                    userModel.headPic = userDict[(String(show_culturalMsg))] as? String ?? ""
                }

                //: if isAutoSendMsg {
                if isAutoSendMsg {
                    //: userModel.uid = model.msgInfo.toUid.intValue
                    userModel.uid = model.msgInfo.toUid.intValue
                    //: userModel.headPic =
                    userModel.headPic =
                        //: AdministratorThen.share.loginUserMode.headPic ?? ""
                        AdministratorThen.share.loginUserMode.headPic ?? ""
                }
                //: model.user = userModel
                model.user = userModel
            }
        }

        //: if model.msgInfo == nil {
        if model.msgInfo == nil {
            //: let msgInfoModel = AbTalkingChatMsgInfoModel()
            let msgInfoModel = TingModel()
            //: msgInfoModel.content = dic["content"] as? String ?? ""
            msgInfoModel.content = dic[(noti_coalId.replacingOccurrences(of: "focus", with: "o") + m_extraText.lowercased())] as? String ?? ""
            //: msgInfoModel.jumps = extraDict?["extra"] as? [Any] ?? []
            msgInfoModel.jumps = extraDict?[(userAppNoseStr.replacingOccurrences(of: "whether", with: "ra"))] as? [Any] ?? []
            //: model.msgInfo = msgInfoModel
            model.msgInfo = msgInfoModel
        }

        // 聊天室送礼消息
        //: if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
        if contentType == .TXMessageContentType_gift && tempModel.groupID != nil && !tempModel.groupID!.isEmpty {
            //: if model.toUser.uid.isEmpty {
            if model.toUser.uid.isEmpty { // 送礼所有人
                //: let toUserInfo = AbTalkingChatMsgToUserModel()
                let toUserInfo = LockModel()
                //: toUserInfo.uid = "all"
                toUserInfo.uid = (String(notiYellowValue))
                //: toUserInfo.nickname = model.gift.sendNum
                toUserInfo.nickname = model.gift.sendNum
                //: toUserInfo.headPic = "icon_chats_pcr"
                toUserInfo.headPic = (String(mainVisualUrl) + String(mGenderCooperativePath.suffix(5)))
                //: model.toUser = toUserInfo
                model.toUser = toUserInfo
            }
        }
        //: cellData?.msgModel = model
        cellData?.msgModel = model

        // 本地自定义字段处理
        //: let customStr = String(data: cusData, encoding: .utf8) ?? ""
        let customStr = String(data: cusData, encoding: .utf8) ?? ""
        //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
        let customDic = NSDictionary.engagement(withJsonString: customStr) ?? [:]
        //: let statusStr = customDic["status"] as? String ?? "0"
        let statusStr = customDic[(String(dream_constraintSupplyFormat))] as? String ?? "0"
        //: let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        let status = TMsgStatus(rawValue: UInt(statusStr) ?? 0) ?? .Msg_Status_Init
        //: cellData?.status = status
        cellData?.status = status
        //: cellData?.msgIncome = CGFloat(customDic["msgIncome"] as? Float ?? 0)
        cellData?.msgIncome = CGFloat(customDic[(String(k_paperUrl.prefix(9)))] as? Float ?? 0)
        //: cellData?.messageType = msgInfoDict?["messageType"] as? Int ?? 0
        cellData?.messageType = msgInfoDict?[(String(k_broadcastStr) + mSmallStrikeValue.replacingOccurrences(of: "beyond", with: "e"))] as? Int ?? 0

        //: if let msgId = customDic["msgId"] as? String {
        if let msgId = customDic[(String(showUnitPath.prefix(5)))] as? String {
            //: cellData?.msgModel.msgInfo.msgId = msgId
            cellData?.msgModel.msgInfo.msgId = msgId
        }

        // 文本翻译
        //: if let translateStatus = customDic["TMsgTranslateStatus"] as? Int {
        if let translateStatus = customDic[(String(main_lovelyContent.suffix(6)) + "ansl" + String(notiEconomicId))] as? Int {
            //: cellData?.translateStatus = TMsgTranslateStatus(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
            cellData?.translateStatus = PubliclyTransformable(rawValue: UInt(translateStatus)) ?? .MsgTranslate_init
        }
        //: if let translatedContent = customDic["TMsgTranslatedContent"] as? String {
        if let translatedContent = customDic[(String(mainSaveerAnnouncementKey.prefix(6)) + "ansla" + String(notiFutureAboutPath.prefix(5)) + "ntent")] as? String {
            //: cellData?.translatedContent = translatedContent
            cellData?.translatedContent = translatedContent
        }

        // 私密照片Cache
        //: if contentType == .TXMessageContentType_picture {
        if contentType == .TXMessageContentType_picture {
            //: let dict = AbTalkingChatMsgIntimatePhotoModel.loadLocalIntimatePhotoDict()
            let dict = EffectModel.shape()
            //: if let photoData = dict[tempModel.msgID as Any] as? String {
            if let photoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.intimatePhoto = AbTalkingChatMsgIntimatePhotoModel(string: photoData, error: nil)
                cellData?.msgModel.msgInfo.intimatePhoto = EffectModel(string: photoData, error: nil)
            }
        }

        // 私密视频Cache
        //: if contentType == .TXMessageContentType_video {
        if contentType == .TXMessageContentType_video {
            //: let dict = AbTalkingChatMsgVideoModel.loadLocalIntimateVideoDict()
            let dict = WantModel.clickAsk()
            //: if let videoData = dict[tempModel.msgID as Any] as? String {
            if let videoData = dict[tempModel.msgID as Any] as? String {
                //: cellData?.msgModel.msgInfo.video = AbTalkingChatMsgVideoModel(string: videoData, error: nil)
                cellData?.msgModel.msgInfo.video = WantModel(string: videoData, error: nil)
            }
        }

        // 索要礼物Cache
        //: if contentType == .TXMessageContentType_askforGift {
        if contentType == .TXMessageContentType_askforGift {
            //: let arr = WantGiftModel.loadAskForGiftArr()
            let arr = AcceptableQuarterModel.publication()
            //: if arr.contains(tempModel.msgID as Any) {
            if arr.contains(tempModel.msgID as Any) { // 已发送
                //: cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
                cellData?.msgModel.msgInfo.wantGift.sendStatus = 1
            }
        }

        //: return cellData
        return cellData
    }

    // MARK: - 辅助方法

    /// 根据字典获取消息类型
    //: public class func messageCellType(_ extraDic: [String: Any]) -> TXMessageContentType {
    public class func kindBirthType(_ extraDic: [String: Any]) -> TaphouseUnsignedCount {
        //: let dic = extraDic["extra"] as? [String: Any]
        let dic = extraDic[(userAppNoseStr.replacingOccurrences(of: "whether", with: "ra"))] as? [String: Any]
        //: let msgInfo = dic?["msgInfo"] as? [String: Any]
        let msgInfo = dic?[(String(dream_scaleMsg.suffix(5)) + String(notiRenderName))] as? [String: Any]
        //: guard let model = try? AbTalkingChatMsgBaseModel(dictionary: dic) else {
        guard let model = try? InformedConsentModel(dictionary: dic) else {
            //: return .TXMessageContentType_text
            return .TXMessageContentType_text
        }

        // 系统消息 官方小密
        //: if dic?["formatType"] as? String == "plain" {
        if dic?[(showWithinName.replacingOccurrences(of: "tumble", with: "o") + String(kErrorCenterFormat.prefix(5)))] as? String == (m_normallyValue.replacingOccurrences(of: "lance", with: "a")) {
            //: let imageUri = dic?["imageUri"] as? String ?? ""
            let imageUri = dic?[(String(appLookIdent) + String(notiSkinData))] as? String ?? ""
            //: let imageData = dic?["imageData"] as? String ?? ""
            let imageData = dic?[(k_attentionLeadingMsg.replacingOccurrences(of: "smart", with: "g") + "eDat" + mGivingMessage.replacingOccurrences(of: "tight", with: "a"))] as? String ?? ""
            //: let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            let isImageStyle = !imageData.isEmpty || !imageUri.isEmpty
            //: if isImageStyle {
            if isImageStyle {
                //: return .TXMessageContentType_picture
                return .TXMessageContentType_picture
            }
        }

        // 图片||私密照片
        //: if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
        if !model.msgInfo.imageUri.isEmpty || !model.msgInfo.imageData.isEmpty || !model.msgInfo.intimatePhoto.originUrl.isEmpty {
            //: return .TXMessageContentType_picture
            return .TXMessageContentType_picture
        }

        // 接收的视频消息 || 本人发送的视频消息
        //: if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == "VideoMsg" {
        if !model.msgInfo.video.videoUrl.isEmpty || model.msgInfo.contentType == (String(dream_fractionName.suffix(6)) + notiOppositionUrl.replacingOccurrences(of: "enhance", with: "g")) {
            //: return .TXMessageContentType_video
            return .TXMessageContentType_video
        }

        // 礼物，非打招呼消息
        //: if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
        if !model.gift.name.isEmpty && model.msgInfo.messageType.rawValue != 4 {
            //: return .TXMessageContentType_gift
            return .TXMessageContentType_gift
        }

        //: let isVoiceStyle = checkVoiceMsg(msginfo: msgInfo ?? [:])
        let isVoiceStyle = profileNight(msginfo: msgInfo ?? [:])
        //: if isVoiceStyle {
        if isVoiceStyle {
            //: return .TXMessageContentType_voice
            return .TXMessageContentType_voice
        }

        //: if (dic?["match"] as? Int) == 1 || (msgInfo?["match"] as? Int) == 1 {
        if (dic?[(k_lightWalkName.replacingOccurrences(of: "fire", with: "at"))] as? Int) == 1 || (msgInfo?[(k_lightWalkName.replacingOccurrences(of: "fire", with: "at"))] as? Int) == 1 {
            // 匹配消息
            //: return .TXMessageContentType_local
            return .TXMessageContentType_local
        }

        //: if dic?["msgType"] as? String == "tips" || (dic?["messageType"] as? Int) == 17 {
        if dic?[(String(show_remainData.prefix(7)))] as? String == (String(mainCapturePairValue)) || (dic?[(String(k_broadcastStr) + mSmallStrikeValue.replacingOccurrences(of: "beyond", with: "e"))] as? Int) == 17 {
            // 提示消息,包含超时未回复
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if (dic?["formatType"] as? String == "mf") && ((dic?["messageType"] as? Int) == 15 || (dic?["messageType"] as? Int) == 16) {
        if (dic?[(showWithinName.replacingOccurrences(of: "tumble", with: "o") + String(kErrorCenterFormat.prefix(5)))] as? String == "mf") && ((dic?[(String(k_broadcastStr) + mSmallStrikeValue.replacingOccurrences(of: "beyond", with: "e"))] as? Int) == 15 || (dic?[(String(k_broadcastStr) + mSmallStrikeValue.replacingOccurrences(of: "beyond", with: "e"))] as? Int) == 16) {
            // 提示消息，打招呼提示送出神秘礼物,对方设置价格消息提示
            //: return .TXMessageContentType_tips
            return .TXMessageContentType_tips
        }

        //: if dic?["msgType"] as? String == "userIntro" {
        if dic?[(String(show_remainData.prefix(7)))] as? String == (String(app_pendingName.prefix(6)) + String(mainBlueKey)) {
            //: return .TXMessageContentType_userCard
            return .TXMessageContentType_userCard
        }

        //: if msgInfo?["msgType"] as? String == "WantGift" || (msgInfo?["messageType"] as? Int) == 25 {
        if msgInfo?[(String(show_remainData.prefix(7)))] as? String == (String(app_cornerThanksKey.suffix(4)) + "Gift") || (msgInfo?[(String(k_broadcastStr) + mSmallStrikeValue.replacingOccurrences(of: "beyond", with: "e"))] as? Int) == 25 {
            //: return .TXMessageContentType_askforGift
            return .TXMessageContentType_askforGift
        }

        //: return .TXMessageContentType_text
        return .TXMessageContentType_text
    }

    // MARK: - 语音消息处理

    /// 从TX的model里提取参数
    //: public class func packageDict(withTXMessage message: V2TIMMessage) -> [String: Any] {
    public class func sinkMessage(withTXMessage message: V2TIMMessage) -> [String: Any] {
        //: let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        let extra = String(data: message.customElem.data, encoding: .utf8) ?? ""
        //: let dic = NSDictionary.dictionary(withJsonString: extra) ?? [:]
        let dic = NSDictionary.engagement(withJsonString: extra) ?? [:]
        //: let extraData = dic["extra"] as? [String: Any]
        let extraData = dic[(userAppNoseStr.replacingOccurrences(of: "whether", with: "ra"))] as? [String: Any]
        //: let msgInfo = extraData?["msgInfo"] as? [String: Any]
        let msgInfo = extraData?[(String(dream_scaleMsg.suffix(5)) + String(notiRenderName))] as? [String: Any]
        //: let user = extraData?["user"] as? [String: Any]
        let user = extraData?[(kPriceName.replacingOccurrences(of: "argument", with: "er"))] as? [String: Any]

        //: let senderId = msgInfo?["senderId"] as? String ?? ""
        let senderId = msgInfo?[(user_panStr.replacingOccurrences(of: "negative", with: "n") + String(dream_tagStr))] as? String ?? ""
        //: var info = msgInfo
        var info = msgInfo

        // 自己发送的和接收的数据结构不一样
        //: if senderId == AdministratorThen.share.loginUserMode.userID {
        if senderId == AdministratorThen.share.loginUserMode.userID {
            //: info?["senderId"] = senderId
            info?[(user_panStr.replacingOccurrences(of: "negative", with: "n") + String(dream_tagStr))] = senderId
            //: info?["targetId"] = message.userID ?? (user?["targetId"] as? String ?? "")
            info?[(String(showMultipleId))] = message.userID ?? (user?[(String(showMultipleId))] as? String ?? "")
            //: } else {
        } else {
            //: info?["senderId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(user_panStr.replacingOccurrences(of: "negative", with: "n") + String(dream_tagStr))] = "\(msgInfo?[(String(k_promptKey))] as? Int ?? 0)"
            //: info?["targetId"] = "\(msgInfo?["uid"] as? Int ?? 0)"
            info?[(String(showMultipleId))] = "\(msgInfo?[(String(k_promptKey))] as? Int ?? 0)"
        }

        //: if msgInfo?.keys.contains("msgId") ?? false {
        if msgInfo?.keys.contains((String(showUnitPath.prefix(5)))) ?? false {
            //: info?["msgId"] = "\(msgInfo?["msgId"] ?? "")"
            info?[(String(showUnitPath.prefix(5)))] = "\(msgInfo?[(String(showUnitPath.prefix(5)))] ?? "")"
            //: } else {
        } else {
            //: let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            let customStr = String(data: message.localCustomData, encoding: .utf8) ?? ""
            //: let customDic = NSDictionary.dictionary(withJsonString: customStr) ?? [:]
            let customDic = NSDictionary.engagement(withJsonString: customStr) ?? [:]
            //: if customDic["msgId"] != nil {
            if customDic[(String(showUnitPath.prefix(5)))] != nil {
                //: info?["msgId"] = "\(customDic["msgId"] ?? "")"
                info?[(String(showUnitPath.prefix(5)))] = "\(customDic[(String(showUnitPath.prefix(5)))] ?? "")"
                //: } else {
            } else {
                //: let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                let txMsgId = !message.msgID.isEmpty ? message.msgID : "\(Int64(Date().timeIntervalSince1970))"
                //: info?["msgId"] = txMsgId
                info?[(String(showUnitPath.prefix(5)))] = txMsgId
            }
        }

        //: if msgInfo?.keys.contains("audioSandbox") ?? false {
        if msgInfo?.keys.contains((String(dreamPropertyIdent.prefix(6)) + "andbox")) ?? false {
            //: info?["audioSandbox"] = "\(msgInfo?["audioSandbox"] ?? "")"
            info?[(String(dreamPropertyIdent.prefix(6)) + "andbox")] = "\(msgInfo?[(String(dreamPropertyIdent.prefix(6)) + "andbox")] ?? "")"
        }
        //: if msgInfo?.keys.contains("audioLength") ?? false {
        if msgInfo?.keys.contains((String(showSumTitle.suffix(6)) + user_quietStr.lowercased())) ?? false {
            //: info?["audioLength"] = "\(msgInfo?["audioLength"] ?? "")"
            info?[(String(showSumTitle.suffix(6)) + user_quietStr.lowercased())] = "\(msgInfo?[(String(showSumTitle.suffix(6)) + user_quietStr.lowercased())] ?? "")"
        }

        //: let formatter = DateFormatter()
        let formatter = DateFormatter()
        //: formatter.dateFormat = "yyyy-MM-dd HH:mm:ss"
        formatter.dateFormat = (k_chooseKey.lowercased() + "-MM-dd " + String(dream_statusFormat.suffix(3)) + String(k_exhibitStr.suffix(5)))
        //: info?["sendTime"] = formatter.string(from: message.timestamp ?? Date())
        info?[(String(mGenTargetMessage) + String(noti_steadCommonTitle.suffix(5)))] = formatter.string(from: message.timestamp ?? Date())
        //: info?["isRead"] = senderId == AdministratorThen.share.loginUserMode.userID ? "1" : "0"
        info?[(String(dreamRawStarMessage))] = senderId == AdministratorThen.share.loginUserMode.userID ? "1" : "0"

        //: return info ?? [:]
        return info ?? [:]
    }

    //: public class func checkPrimaryKey(withMsgModel tempModel: V2TIMMessage) -> String {
    public class func withModel(withMsgModel tempModel: V2TIMMessage) -> String {
        //: let v2Info = packageDict(withTXMessage: tempModel)
        let v2Info = sinkMessage(withTXMessage: tempModel)
        //: let key = "\(v2Info["msgId"] ?? "")"
        let key = "\(v2Info[(String(showUnitPath.prefix(5)))] ?? "")"
        //: print("key = \(key)")
        //: return key
        return key
    }

    // MARK: - JSON处理方法

    //: @objc public class func parseTXMessageData(data: Data) -> [String: Any] {
    @objc public class func textData(data: Data) -> [String: Any] {
        //: do {
        do {
            //: let dict = try JSONSerialization.jsonObject(
            let dict = try JSONSerialization.jsonObject(
                //: with: data, options: .mutableContainers)
                with: data, options: .mutableContainers
            )
            //: return dict as? [String: Any] ?? [:]
            return dict as? [String: Any] ?? [:]
            //: } catch {
        } catch {
            //: return [:]
            return [:]
        }
    }

    //: @objc public class func parseTXMessageJsonStr(data: [String: Any]) -> String {
    @objc public class func ownWith(data: [String: Any]) -> String {
        //: do {
        do {
            //: let jsonData = try JSONSerialization.data(
            let jsonData = try JSONSerialization.data(
                //: withJSONObject: data, options: [])
                withJSONObject: data, options: []
            )
            //: return String(data: jsonData, encoding: .utf8) ?? ""
            return String(data: jsonData, encoding: .utf8) ?? ""
            //: } catch {
        } catch {
            //: return ""
            return ""
        }
    }

    // MARK: - 语音消息检测

    //: @objc public class func checkVoiceMsg(msginfo: [String: Any]) -> Bool {
    @objc public class func profileNight(msginfo: [String: Any]) -> Bool {
        //: if let msgType = msginfo["msgType"] as? String, msgType == "audio" {
        if let msgType = msginfo[(String(show_remainData.prefix(7)))] as? String, msgType == (k_processingTitle.replacingOccurrences(of: "count", with: "au")) {
            //: return true
            return true
        }
        //: if let contentType = msginfo["contentType"] as? String,
        if let contentType = msginfo[(String(dream_arrowValue) + noti_bronzeContent.replacingOccurrences(of: "village", with: "e") + "Type")] as? String,
           //: contentType == "AudioMsg"
           contentType == (main_visitorValue.replacingOccurrences(of: "consumer", with: "A") + String(dreamSustainKey))
        {
            //: return true
            return true
        }
        //: if let audioData = msginfo["audioData"] as? String, !audioData.isEmpty {
        if let audioData = msginfo[(String(dream_coalFollowPath) + String(mainEngagementUrl.suffix(5)))] as? String, !audioData.isEmpty {
            //: return true
            return true
        }
        //: if let audioUri = msginfo["audioUri"] as? String, !audioUri.isEmpty {
        if let audioUri = msginfo[(String(app_dirtFormat) + mProcedureUrl.replacingOccurrences(of: "identify", with: "i"))] as? String, !audioUri.isEmpty {
            //: return true
            return true
        }
        //: return false
        return false
    }
}
