
//: Declare String Begin

/*: "IM退出登录失败，code: :*/
fileprivate let mRoseFormat:[Character] = ["I","M","退","出","登","录","失","败","，"]
fileprivate let main_drownFactorName:String = "code:simply radio insert choice timing"

/*: , desc: :*/
fileprivate let user_bouncePath:String = "processing would, desc:"

/*: "IM登录失败，code: :*/
fileprivate let noti_transportPeopleUrl:String = "IM登录失atm trade"
fileprivate let appArriveData:[Character] = ["d","e",":"]

/*: "IM获取未读消息总数失败，code: :*/
fileprivate let showNoValue:[Character] = ["I","M","获","取","未","\u{8bfb}","消"]
fileprivate let user_managerMobilePath:String = "息\u{603b}数失败，"

/*: "crushBadge" :*/
fileprivate let mainAgoStr:String = "cpairush"
fileprivate let k_profilePublicationPath:[Character] = ["B","a","d","g","e"]

/*: "badNumber" :*/
fileprivate let dream_parentDownStr:[UInt8] = [0xe3,0xe0,0xe5,0xcf,0xf4,0xec,0xe3,0xe4,0xf3]

private func commentBoundary(exit num: UInt8) -> UInt8 {
    return num ^ 129
}

/*: "plaza" :*/
fileprivate let userPatentName:[Character] = ["p","l","a","z","a"]

/*: "roomNum" :*/
fileprivate let showCropFormat:[Character] = ["r","o","o","m","N","u"]
fileprivate let m_directionValue:[Character] = ["m"]

/*: "num" :*/
fileprivate let m_bothPath:[Character] = ["n","u","m"]

/*: "onlyOneRoomId" :*/
fileprivate let main_colorCaptainPath:String = "resume implement pitchonlyOneR"
fileprivate let dream_dotDepthTitle:String = "feedback tooloomId"

/*: "LIVE-CHAT" :*/
fileprivate let app_executiveTitle:[Character] = ["L","I","V","E","-","C","H","A","T"]

/*: "PARTY-CHAT" :*/
fileprivate let app_statTitle:[Character] = ["P","A","R","T","Y","-","C","H","A","T"]

/*: "extra" :*/
fileprivate let k_tempPath:String = "EXTRA"

/*: "msgType" :*/
fileprivate let k_gradeMeritMsg:[Character] = ["m","s","g","T","y","p","e"]

/*: "newPkgGift" :*/
fileprivate let app_tumbleData:[Character] = ["n","e","w","P","k","g","G"]
fileprivate let dream_logName:String = "slimft"

/*: "MF:ChatMentionMsg" :*/
fileprivate let userStillMessage:[Character] = ["M","F",":","C","h","a","t","M","e","n","t","i","o","n"]
fileprivate let app_arrayKey:String = "anima container present distribution orangeMsg"

/*: "uid" :*/
fileprivate let notiHolderId:[UInt8] = [0x63,0x57,0x52]

fileprivate func implementCease(successfully num: UInt8) -> UInt8 {
    let value = Int(num) - 238
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onlineStatus" :*/
fileprivate let app_extentStr:[UInt8] = [0xd7,0xd6,0xd4,0xd1,0xd6,0xdd,0xeb,0xcc,0xd9,0xcc,0xcd,0xcb]

private func hemBreak(recording num: UInt8) -> UInt8 {
    return num ^ 184
}

/*: "isNewUser" :*/
fileprivate let k_admitText:[UInt8] = [0xe5,0xff,0xc2,0xe9,0xfb,0xd9,0xff,0xe9,0xfe]

private func postHeartEverybody(leaf num: UInt8) -> UInt8 {
    return num ^ 140
}

/*: "c2c_ :*/
fileprivate let show_meetingUrl:String = "authorize going muse representc2c_"

/*: "IM获取会话失败，convId: :*/
fileprivate let show_remainBeyondTitle:[Character] = ["I","M","获","取","会","话","失","败","\u{ff0c}","c","o"]
fileprivate let kColorfulText:String = "nvId:reader conduct"

/*: , code: :*/
fileprivate let mSurvivalName:[Character] = [","," "]
fileprivate let dreamAuthorizeKey:String = "pass equally sectioncode:"

/*: "IM插入消息失败，targetId: :*/
fileprivate let dreamUniversalNaturalFormat:[Character] = ["I","M","插","入","消","息","失","败"]
fileprivate let userFollowingIdent:[Character] = ["，","t","a","r","g","e","t","I","d",":"]

/*: "changeDirection" :*/
fileprivate let dreamCommentYouPath:[Character] = ["c","h","a","n","g","e"]
fileprivate let user_infoProjectDenyId:String = "prove application go candidateDirection"

/*: "rYMsgType" :*/
fileprivate let app_recoverThumbId:String = "faculty game wipe seek hisrY"
fileprivate let k_modelValue:String = "start important accord tail shortMsgType"

/*: "GJ:CallCustom" :*/
fileprivate let dream_manageTurnIdent:String = "ford make directionGJ:Call"
fileprivate let main_fillPath:[Character] = ["C","u","s","t","o","m"]

/*: "IM获取会话列表失败，code: :*/
fileprivate let k_twoFireKey:String = "IM获取会mine tail guilty carrier product"
fileprivate let show_operateYourselfStr:String = "medium eff pretty match败，co"
fileprivate let notiFeatureText:String = "de:observer gage bit freedom"

/*: "msgInfo" :*/
fileprivate let userIncreaseIdent:String = "msgInfolevel please ourselves"

/*: "needFold" :*/
fileprivate let noti_tipMessage:[Character] = ["n","e","e","d","F","o","l","d"]

/*: "group_ :*/
fileprivate let noti_memoryMessage:[Character] = ["g","r","o","u","p","_"]

/*: "IM删除会话失败，convID: :*/
fileprivate let app_equalPath:[Character] = ["I","M","\u{5220}","除","会","话","失","败","，","c","o","n","v","I","D",":"]

/*: "IM置顶会话失败，convId: :*/
fileprivate let userDoingTitle:[Character] = ["I","M","\u{7f6e}","顶","会","\u{8bdd}","失","败","\u{ff0c}","c"]
fileprivate let mainSkirtFormat:String = "proposal concerned execute commend acquireonvId:"

/*: "SDK 正在连接到服务器" :*/
fileprivate let m_woodIdent:String = "SDK 正estimated added see this"
fileprivate let appAutomaticallyValue:[Character] = ["\u{5728}","连","接","到","服","务","器"]

/*: "SDK 已经成功连接到服务器" :*/
fileprivate let noti_aideMsg:[Character] = ["S","D","K"," ","已","经","成"]
fileprivate let notiShadePromptMsg:[Character] = ["功","连","接","到","服","务","器"]

/*: "IM连接服务器失败，code: :*/
fileprivate let userThinkWeekName:String = "Ithan"
fileprivate let app_wouldStr:String = "连接服务器"

/*: , error: :*/
fileprivate let dream_statusValue:String = ", error:gain min"

/*: "IM账号被挤下线。" :*/
fileprivate let userCloudFormat:String = "IM账号被numb yourself"

/*: "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password." :*/
fileprivate let main_streamLeadingId:[UInt8] = []

private func rawMaster(eon num: UInt8) -> UInt8 {
    return num ^ 199
}

/*: "Offline Notification" :*/
fileprivate let userEndVillagePath:String = "Offlineequity message capture traffic"
fileprivate let kHeartData:[Character] = [" ","N","o","t","i","f","i","c","a","t","i","o","n"]

/*: "Cancel" :*/
fileprivate let app_onceWedTitle:[Character] = ["C","a","n","c","e","l"]

/*: "Re Login" :*/
fileprivate let show_ridSubjectName:String = "dark expressRe L"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  EdgeMsgListener.swift
//  AbroadTalking
//
//  Created by Hemming on 2023/3/7.
//

//: import UIKit
import UIKit

/// 视频通话【拨打方】代理
//: @objc public protocol IMManagerDelegate: NSObjectProtocol {
@objc public protocol ConfirmMerelyManagerDelegate: NSObjectProtocol {
    /// 会话列表更新
    //: @objc optional func onRefreshConversationList(data: [TalkingConversationModel])
    @objc optional func workFire(data: [ModelThen])

    /// 未读数更新
    //: @objc optional func onUnreadMsgCountChanged(count: Int)
    @objc optional func shareCount(count: Int)

    /// 收到新消息
    //: @objc optional func onRecvNewMessage(msg: V2TIMMessage)
    @objc optional func about(msg: V2TIMMessage)
}

/// 数据列表枚举
//: enum ChatListDataType {
enum DataType {
    //: case normalList
    case normalList // 默认列表
    //: case topList
    case topList // 置顶列表
    //: case moreList
    case moreList // 更多消息（超过5天未回复）
}

//: @objcMembers public class EdgeMsgListener: NSObject {
@objcMembers public class EdgeMsgListener: NSObject {
    //: public static let shared = EdgeMsgListener()
    public static let shared = EdgeMsgListener()
    //: public dynamic var isConnection = false
    public dynamic var isConnection = false
    //: var isRefreshSysMsg = false
    var isRefreshSysMsg = false
    //: var lastSysTime: TimeInterval = 0
    var lastSysTime: TimeInterval = 0

    //: var officialList: [TalkingConversationModel] = []
    var officialList: [ModelThen] = [] // 官方列表（like+系统通知+客服）
    //: var topConvList: [TalkingConversationModel] = []
    var topConvList: [ModelThen] = [] // 置顶列表
    //: var norConvList: [TalkingConversationModel] = []
    var norConvList: [ModelThen] = [] // 普通会话列表
    //: var chatRoomList: [TalkingConversationModel] = []
    var chatRoomList: [ModelThen] = [] // 聊天室
    //: var moreMsgList: [TalkingConversationModel] = []
    var moreMsgList: [ModelThen] = [] // 更多消息（超过5天未回复）

    /// 消息列表数据分页
    //: private var nextSeq: UInt64 = 0
    private var nextSeq: UInt64 = 0
    /// 多个代理容器
    //: private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    private let multiDelegate: NSHashTable<AnyObject> = NSHashTable.weakObjects()
    /// 会话列表缓存数据（避免重复转换）
    //: private var convListCache = [String: TalkingConversationModel]()
    private var convListCache = [String: ModelThen]()

    //: override private init() {
    override private init() {
        //: super.init()
        super.init()
        //: self.initialization()
        self.pastConfirm()
    }

    // MARK: - SDK初始化

    //: func func__TXSDKInit() {
    func attentionGap() {
        //: let config = V2TIMSDKConfig()
        let config = V2TIMSDKConfig()
        //: config.logLevel = .LOG_DEBUG
        config.logLevel = .LOG_DEBUG
        //: V2TIMManager.sharedInstance().initSDK(TXIMAppID, config: config)
        V2TIMManager.sharedInstance().initSDK(main_errorSendMsg, config: config)
        //: V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        V2TIMManager.sharedInstance().addAdvancedMsgListener(listener: self)
        //: V2TIMManager.sharedInstance().addConversationListener(listener: self)
        V2TIMManager.sharedInstance().addConversationListener(listener: self)
        //: V2TIMManager.sharedInstance().add(self)
        V2TIMManager.sharedInstance().add(self)
    }

    // MARK: - 清除单例状态

    //: func initialization() {
    func pastConfirm() {
        //: func__removeAllDelegate()
        edit()
        //: self.isConnection = false
        self.isConnection = false
        //: self.nextSeq = 0
        self.nextSeq = 0
        //: self.lastSysTime = 0
        self.lastSysTime = 0
        //: self.func__adddofficialList()
        self.strip()
        //: self.convListCache.removeAll()
        self.convListCache.removeAll()
        //: self.norConvList.removeAll()
        self.norConvList.removeAll()
        //: self.topConvList.removeAll()
        self.topConvList.removeAll()
        //: self.moreMsgList.removeAll()
        self.moreMsgList.removeAll()
    }

    /// 添加官方数据模型
    //: func func__adddofficialList() {
    func strip() {
        //: self.officialList.removeAll()
        self.officialList.removeAll()
        // 添加官方占位model
        //: let systemModel = TalkingConversationModel.getPlaceholderModel(chatType: .system)
        let systemModel = ModelThen.moment(chatType: .system)
        //: let serviceModel = TalkingConversationModel.getPlaceholderModel(chatType: .service)
        let serviceModel = ModelThen.moment(chatType: .service)
        //: self.officialList = [systemModel, serviceModel]
        self.officialList = [systemModel, serviceModel]
    }

    /// 获取官方数据列表（在审核模式下过滤客服消息不展示）
    /// - Returns: 模型
    //: func getOfficialList() -> [TalkingConversationModel] {
    func serration() -> [ModelThen] {
        // 审核模式，在消息列表移除官方客服
        //: if AdministratorThen.share.appStatus == AppSkinStatus.special.rawValue {
        if AdministratorThen.share.appStatus == BecauseMultiplierTarget.special.rawValue {
            //: if let index = self.officialList.firstIndex(where: { $0.userID == CreditsThen.getCustomerServiceID() }) {
            if let index = self.officialList.firstIndex(where: { $0.userID == CreditsThen.tingId() }) {
                //: self.officialList.remove(at: index)
                self.officialList.remove(at: index)
            }
        }
        //: return self.officialList
        return self.officialList
    }
}

// MARK: - 管理多个代理

//: extension EdgeMsgListener {
extension EdgeMsgListener {
    //: func func__removeAllDelegate() {
    func edit() {
        //: self.multiDelegate.removeAllObjects()
        self.multiDelegate.removeAllObjects()
    }

    //: func func__addDelegate(_ delegate: IMManagerDelegate) {
    func requireExpected(_ delegate: ConfirmMerelyManagerDelegate) {
        //: self.multiDelegate.add(delegate)
        self.multiDelegate.add(delegate)
    }

    //: func func__removeDelegate(_ delegate: IMManagerDelegate) {
    func chipAway(_ delegate: ConfirmMerelyManagerDelegate) {
        //: self.func__invokeDelegate {
        self.skipExamineed {
            //: if $0 === delegate as AnyObject {
            if $0 === delegate as AnyObject {
                //: self.multiDelegate.remove($0)
                self.multiDelegate.remove($0)
            }
        }
    }

    //: private func func__invokeDelegate(_ invocation: (IMManagerDelegate) -> Void) {
    private func skipExamineed(_ invocation: (ConfirmMerelyManagerDelegate) -> Void) {
        //: for delegate in self.multiDelegate.allObjects.reversed() {
        for delegate in self.multiDelegate.allObjects.reversed() {
            //: invocation(delegate as! IMManagerDelegate)
            invocation(delegate as! ConfirmMerelyManagerDelegate)
        }
    }
}

// MARK: - IM登录态管理

//: extension EdgeMsgListener {
extension EdgeMsgListener {
    /// 退出登录IM
    //: func func__LogingOut() {
    func announcement() {
        //: V2TIMManager.sharedInstance().logout {} fail: { code, desc in
        V2TIMManager.sharedInstance().logout {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM退出登录失败，code:\(code), desc:\(String(describing: desc)).")
            ProblemBenchPoundThen.panelDetect(msg: (String(mRoseFormat) + String(main_drownFactorName.prefix(5))) + "\(code)" + (String(user_bouncePath.suffix(7))) + "\(String(describing: desc)).")
        }
        //: self.initialization()
        self.pastConfirm()
    }

    /// 登录IM
    //: func func__LogingIn(completion: ((_ succeed: Bool) -> Void)? = nil) {
    func run(completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: guard self.isConnection == false else { return }
        guard self.isConnection == false else { return }
        //: let uSigStr = AdministratorThen.share.appUserConfigMode.uSig
        let uSigStr = AdministratorThen.share.appUserConfigMode.uSig
        //: let uidStr = AdministratorThen.share.loginUserMode.userID
        let uidStr = AdministratorThen.share.loginUserMode.userID
        //: V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
        V2TIMManager.sharedInstance().login(uidStr, userSig: uSigStr) {
            //: self.isConnection = true
            self.isConnection = true
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: self.func__getUnreadMsgCount()
                self.attitudeCount()
            }
            //: completion?(true)
            completion?(true)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM登录失败，code:\(code), desc:\(String(describing: desc)).")
            ProblemBenchPoundThen.panelDetect(msg: (String(noti_transportPeopleUrl.prefix(5)) + "败，co" + String(appArriveData)) + "\(code)" + (String(user_bouncePath.suffix(7))) + "\(String(describing: desc)).")
            //: self.isConnection = false
            self.isConnection = false
            //: completion?(false)
            completion?(false)
        }
    }

    /// 检测是否登录
    //: func func__checkCanOperateList() -> Bool {
    func underside() -> Bool {
        //: let status = V2TIMManager.sharedInstance().getLoginStatus()
        let status = V2TIMManager.sharedInstance().getLoginStatus()
        //: if self.isConnection == false || status != .STATUS_LOGINED || AdministratorThen.share.networkStatus == .Unavailable {
        if self.isConnection == false || status != .STATUS_LOGINED || AdministratorThen.share.networkStatus == .Unavailable {
            //: self.func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            self.iconThemeBringBackPlace(showMsg: dream_micData)
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - 获取未读消息数、wholikeme消息角标、聊天室信息

//: extension EdgeMsgListener {
extension EdgeMsgListener {
    /// 获取未读消息数
    //: func func__getUnreadMsgCount() {
    func attitudeCount() {
        //: V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
        V2TIMManager.sharedInstance().getTotalUnreadMessageCount { totalCount in
            //: self.onTotalUnreadMessageCountChanged(totalCount)
            self.onTotalUnreadMessageCountChanged(totalCount)
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取未读消息总数失败，code:\(code), desc:\(String(describing: desc)).")
            ProblemBenchPoundThen.panelDetect(msg: (String(showNoValue) + user_managerMobilePath + "code:") + "\(code)" + (String(user_bouncePath.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 刷新wholikeme消息角标
    //: func func__refreshSysMsg() {
    func unswerving() {
        //: let nowTs = NSDate.timeIntervalSinceReferenceDate
        let nowTs = NSDate.timeIntervalSinceReferenceDate
        //: if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
        if nowTs - self.lastSysTime < 10 || self.isRefreshSysMsg {
            //: return
            return
        }
        //: self.lastSysTime = nowTs
        self.lastSysTime = nowTs
        //: self.isRefreshSysMsg = true
        self.isRefreshSysMsg = true

        //: TalkingChatRequestTool.req_sysMessageList { _, result in
        ShrinkReactiveCompatible.extended { _, result in
            //: self.isRefreshSysMsg = false
            self.isRefreshSysMsg = false
            //: guard let dict = result as? [String: Any] else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let number = dict["crushBadge"] as! Int
            let number = dict[(mainAgoStr.replacingOccurrences(of: "pair", with: "r") + String(k_profilePublicationPath))] as! Int
            //: NotificationCenter.default.post(name: WHOLIKEME_BADGENUMBER_NOTIF, object: self, userInfo: ["badNumber": number])
            NotificationCenter.default.post(name: showMarginPackageStr, object: self, userInfo: [String(bytes: dream_parentDownStr.map{commentBoundary(exit: $0)}, encoding: .utf8)!: number])
        }
    }

    /// 聊天室信息
    /// - Parameter completion: 回调
    //: func getChatRoomData(completion: @escaping (_ succeed: Bool) -> Void) {
    func statReject(completion: @escaping (_ succeed: Bool) -> Void) {
        //: chatRoomList.removeAll()
        chatRoomList.removeAll()
        //: TalkingChatRequestTool.req_getChatRoomInfo { succeed, result, _ in
        ShrinkReactiveCompatible.dateAlongside { succeed, result, _ in
            //: guard succeed,
            guard succeed,
                  //: let data = result as? [String: Any],
                  let data = result as? [String: Any],
                  //: let plaza = data["plaza"] as? [String: Any] else {
                  let plaza = data[(String(userPatentName))] as? [String: Any]
            else {
                //: completion(false)
                completion(false)
                //: return
                return
            }

            //: if plaza["roomNum"] as? Int ?? 0 > 0 {
            if plaza[(String(showCropFormat) + String(m_directionValue))] as? Int ?? 0 > 0 {
                //: let model = TalkingConversationModel.getPlaceholderModel(chatType: .group)
                let model = ModelThen.moment(chatType: .group)
                //: model.num = plaza["num"] as? Int ?? 0
                model.num = plaza[(String(m_bothPath))] as? Int ?? 0
                //: model.onlyOneRoomId = plaza["onlyOneRoomId"] as? String ?? ""
                model.onlyOneRoomId = plaza[(String(main_colorCaptainPath.suffix(8)) + String(dream_dotDepthTitle.suffix(5)))] as? String ?? ""
                //: self.chatRoomList.append(model)
                self.chatRoomList.append(model)
            }
            //: completion(true)
            completion(true)
        }
    }
}

// MARK: - V2TIMAdvancedMsgListener

//: extension EdgeMsgListener: V2TIMAdvancedMsgListener {
extension EdgeMsgListener: V2TIMAdvancedMsgListener {
    /// 接收到新消息
    /// - Parameter msg: 消息数据
    //: public func onRecvNewMessage(_ msg: V2TIMMessage!) {
    public func onRecvNewMessage(_ msg: V2TIMMessage!) {
        //: if msg.groupID != nil && (msg.groupID.contains("LIVE-CHAT") || msg.groupID.contains("PARTY-CHAT")) { // 开播/语聊房 弹幕提醒
        if msg.groupID != nil && (msg.groupID.contains((String(app_executiveTitle))) || msg.groupID.contains((String(app_statTitle)))) { // 开播/语聊房 弹幕提醒
            //: TalkingDanmuManager.shared().onRecvDanmuNewMsg(msg: msg)
            ManageressReactiveCompatible.preserve().chinPolicy(msg: msg)
            //: return
            return
        }
        //: if msg.sender == AdministratorThen.share.loginUserMode.userID { return }
        if msg.sender == AdministratorThen.share.loginUserMode.userID { return }

        //: let dic = ManagerPubliclyThen.parseTXMessageData(data: msg.customElem.data)
        let dic = ManagerPubliclyThen.recover(data: msg.customElem.data)
        //: let extraDic = dic["extra"] as! [AnyHashable: Any]
        let extraDic = dic[(k_tempPath.lowercased())] as! [AnyHashable: Any]
        //: let msgModel = try? AbTalkingChatMsgBaseModel(dictionary: extraDic)
        let msgModel = try? InformedConsentModel(dictionary: extraDic)

        //: NotificationCenter.default.post(name: CHAT_NEW_MSG_NOTIFICATION, object: msg)
        NotificationCenter.default.post(name: showDetailContent, object: msg)

        //: if let type = extraDic["msgType"] {
        if let type = extraDic[(String(k_gradeMeritMsg))] {
            //: if type as? String == "newPkgGift" { // 刷新礼物背包
            if type as? String == (String(app_tumbleData) + dream_logName.replacingOccurrences(of: "slim", with: "i")) { // 刷新礼物背包
                //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: appPingFormat, object: nil)
            }
        }

        //: if msg.userID != nil && msg.userID.count > 0 {
        if msg.userID != nil && msg.userID.count > 0 {
            //: if ManagerPubliclyThen.checkVoiceMessage(message: msg) {
            if ManagerPubliclyThen.perpendicularMessage(message: msg) { // 语音消息
                //: let dic = TUISocialChatManager.packageDict(withTXMessage: msg)
                let dic = AdvocatePatentThen.sinkMessage(withTXMessage: msg)
                //: WCDBVoiceMsgTable.db_inserVoiceMsgToDB(dic)
                FireMsgTable.tempPlatform(dic)
            }
            //: self.judgeisHaveIntimate(msg: msg, dic: dic)
            self.have(msg: msg, dic: dic)
            // 处理礼物消息
            //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            GazetteAnimatTool.shared.destinationHave(giftMessageDic: dic as NSDictionary)
        }
        //: if msgModel?.msgInfo.messageType == .MessageTypeGift {
        if msgModel?.msgInfo.messageType == .MessageTypeGift { // 群聊收礼
            //: AbTalkingPrivateChatAnimatTool.shared.chatRoomDisposeReceiveGiftMsg(giftMessageDic: dic as NSDictionary)
            GazetteAnimatTool.shared.clickIgnore(giftMessageDic: dic as NSDictionary)
        }
        //: if msg.customElem.extension == "MF:ChatMentionMsg" { // 群聊@ 消息
        if msg.customElem.extension == (String(userStillMessage) + String(app_arrayKey.suffix(3))) { // 群聊@ 消息
            //: TalkingGroupChatManager.share.receivedMentionMsg(msg: msg, extraInfo: extraDic)
            PoorReactiveCompatible.share.messageFrame(msg: msg, extraInfo: extraDic)
        }

        //: if msgModel?.gift.unlockMsgId.count ?? 0 > 0 {
        if msgModel?.gift.unlockMsgId.count ?? 0 > 0 { // 记录解锁礼物消息
            //: var array: Array = UserDefaults.standard.object(forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            var array: Array = UserDefaults.standard.object(forKey: PlumpDownReadingManageressChatManager.share.UnlockMsgIdArrayKey) as? [String] ?? [String]()
            //: array.append(msgModel!.gift.unlockMsgId)
            array.append(msgModel!.gift.unlockMsgId)
            //: UserDefaults.standard.set(array, forKey: TalkingPrivateChatManager.share.UnlockMsgIdArrayKey)
            UserDefaults.standard.set(array, forKey: PlumpDownReadingManageressChatManager.share.UnlockMsgIdArrayKey)
        }
        //: if msg.sender != CreditsThen.getXiaoMiID() && (msg.userID != nil && msg.userID.count > 0) {
        if msg.sender != CreditsThen.rowId() && (msg.userID != nil && msg.userID.count > 0) { // 过滤系统通知、群聊
            // delegate转发
            //: self.func__invokeDelegate { $0.onRecvNewMessage?(msg: msg) }
            self.skipExamineed { $0.about?(msg: msg) }
            // 处理消息通知
            //: TalkingMessageNotificationManager.shared.onRecvNewMessage(msg: msg, dataDict: dic)
            SuiteNotificationManager.shared.oppositeness(msg: msg, dataDict: dic)
            // 更新消息列表用户为在线状态
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
                //: let userInfo = ["uid": msg.userID ?? "",
                let userInfo = [String(bytes: notiHolderId.map{implementCease(successfully: $0)}, encoding: .utf8)!: msg.userID ?? "",
                                //: "onlineStatus": 1,
                                String(bytes: app_extentStr.map{hemBreak(recording: $0)}, encoding: .utf8)!: 1,
                                //: "isNewUser": msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                                String(bytes: k_admitText.map{postHeartEverybody(leaf: $0)}, encoding: .utf8)!: msgModel?.msgInfo.isNewUser ?? false] as [String: Any]
                //: NotificationCenter.default.post(name: MSGLIST_UPDATE_ONLINESTATUS_NOTIFICATION,
                NotificationCenter.default.post(name: mMinFormat,
                                                //: object: nil,
                                                object: nil,
                                                //: userInfo: userInfo)
                                                userInfo: userInfo)
            }
        }
    }

    /// C2C 收到对端用户会话已读通知
    //: public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
    public func onRecvC2CReadReceipt(_ receiptList: [V2TIMMessageReceipt]!) {
        //: for receipt in receiptList {
        for receipt in receiptList {
            // 更新数据库和缓存
            //: DBUserInfoManager.cache_updateReadReceiptTime(with: receipt.userID, time: receipt.timestamp)
            DecisionMakerReactiveCompatible.readingTo(with: receipt.userID, time: receipt.timestamp)
            //: TalkingPrivateChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            PlumpDownReadingManageressChatManager.share.readReceiptDict[receipt.userID] = receipt.timestamp
            // 发送已读回执通知
            //: NotificationCenter.default.post(name: CHAT_MSG_READ_RECEIPT_NOTIFICATION, object: receipt)
            NotificationCenter.default.post(name: noti_barData, object: receipt)
        }
    }

    //: @objc public func func__handleMsg(targetId: String, msgCellData: ReconstructThen, isPrivete: Bool) {
    @objc public func funcMethodDataCellOrganiseMark(targetId: String, msgCellData: ReconstructThen, isPrivete: Bool) {
        //: if let imMsg = msgCellData.innerMessage {
        if let imMsg = msgCellData.innerMessage {
            //: self.func__insertMsgToLocalStorage(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
            self.inviteVid(targetId: targetId, imMsg: imMsg, isPrivete: isPrivete)
        }
        //: var convId = ""
        var convId = ""
        //: if isPrivete {
        if isPrivete {
            //: convId = "c2c_\(targetId)"
            convId = (String(show_meetingUrl.suffix(4))) + "\(targetId)"
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1) {
            //: V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
            V2TIMManager.sharedInstance().getConversation(convId) { [weak self] conv in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if conv != nil {
                if conv != nil {
                    //: let list = self.func__handleConversationList(convList: [conv!])
                    let list = self.unify(convList: [conv!])
                    //: self.func__updateConversationList(conVersationList: list)
                    self.coalTarBetween(conVersationList: list)
                }
                //: } fail: { code, desc in
            } fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM获取会话失败，convId:\(convId), code:\(code), desc:\(String(describing: desc)).")
            }
        }
    }

    //: func func__insertMsgToLocalStorage(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
    func inviteVid(targetId: String, imMsg: V2TIMMessage, isPrivete: Bool) {
        //: let senderStr = AdministratorThen.share.loginUserMode.userID
        let senderStr = AdministratorThen.share.loginUserMode.userID
        //: if isPrivete {
        if isPrivete {
            //: V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
            V2TIMManager.sharedInstance().insertC2CMessage(toLocalStorage: imMsg, to: targetId, sender: senderStr) {} fail: { code, desc in
                //: UploadLogTool.writeLog(msg: "IM插入消息失败，targetId:\(targetId), code:\(code), desc:\(String(describing: desc)).")
                ProblemBenchPoundThen.panelDetect(msg: (String(dreamUniversalNaturalFormat) + String(userFollowingIdent)) + "\(targetId)" + (String(mSurvivalName) + String(dreamAuthorizeKey.suffix(5))) + "\(code)" + (String(user_bouncePath.suffix(7))) + "\(String(describing: desc)).")
            }
        }
    }

    //: func judgeisHaveIntimate(msg: V2TIMMessage, dic: [String: Any]) {
    func have(msg: V2TIMMessage, dic: [String: Any]) {
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let isSelf = jsonDic["changeDirection"].boolValue
        let isSelf = jsonDic[(String(dreamCommentYouPath) + String(user_infoProjectDenyId.suffix(9)))].boolValue
        //: if jsonDic["rYMsgType"] == "GJ:CallCustom" && isSelf && DumpPushManager.share.func__getCurrentActivityVC()!.isKind(of: TalkingPrivateChatController.self) == false {
        if jsonDic[(String(app_recoverThumbId.suffix(2)) + String(k_modelValue.suffix(7)))].stringValue == (String(dream_manageTurnIdent.suffix(7)) + String(main_fillPath)) && isSelf && DumpPushManager.share.talkVc()!.isKind(of: PurchaseViewController.self) == false {
            //: var intimate: [String: Any] = UserDefaults.standard.object(forKey: isConversationIntimateCacheKey) as? [String: Any] ?? Dictionary()
            var intimate: [String: Any] = UserDefaults.standard.object(forKey: user_topIdent) as? [String: Any] ?? Dictionary()
            //: intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            intimate.updateValue(msg.userID ?? "", forKey: msg.userID)
            //: UserDefaults.standard.set(intimate, forKey: isConversationIntimateCacheKey)
            UserDefaults.standard.set(intimate, forKey: user_topIdent)
        }
    }
}

// MARK: - V2TIMConversationListener【会话、未读数】

//: extension EdgeMsgListener: V2TIMConversationListener {
extension EdgeMsgListener: V2TIMConversationListener {
    /// 有新的会话
    //: public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
    public func onNewConversation(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.unify(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.coalTarBetween(conVersationList: list)
    }

    /// 某些会话的关键信息发生变化
    //: public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
    public func onConversationChanged(_ conversationList: [V2TIMConversation]!) {
        //: let list = self.func__handleConversationList(convList: conversationList)
        let list = self.unify(convList: conversationList)
        //: self.func__updateConversationList(conVersationList: list)
        self.coalTarBetween(conVersationList: list)
    }

    /// 会话未读总数变更通知
    //: public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
    public func onTotalUnreadMessageCountChanged(_ totalUnreadCount: UInt64) {
        //: AdministratorThen.share.unreadMessageNum = Int(totalUnreadCount)
        AdministratorThen.share.unreadMessageNum = Int(totalUnreadCount)
        //: UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        UIApplication.shared.applicationIconBadgeNumber = Int(totalUnreadCount)
        //: self.func__invokeDelegate {
        self.skipExamineed {
            //: $0.onUnreadMsgCountChanged?(count: Int(totalUnreadCount))
            $0.shareCount?(count: Int(totalUnreadCount))
        }
    }

    /// 批量更新会话列表
    //: func func__updateConversationList(conVersationList: [TalkingConversationModel]) {
    func coalTarBetween(conVersationList: [ModelThen]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            // 防止刚启动APP，消息列表还未初始化，先缓存数据，找合适时机批量请求
            //: if DBUserInfoManager.shared().needReqUidsSet != nil {
            if DecisionMakerReactiveCompatible.libraryCounterruptFamily().needReqUidsSet != nil {
                // 提取非空 userId 并去重
                //: let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                let set = Set(conVersationList.compactMap { $0.userID.isEmpty ? nil : $0.userID })
                //: DBUserInfoManager.shared().needReqUidsSet!.formUnion(set)
                DecisionMakerReactiveCompatible.libraryCounterruptFamily().needReqUidsSet!.formUnion(set)
            }
            //: self.func__invokeDelegate {
            self.skipExamineed {
                //: $0.onRefreshConversationList?(data: conVersationList)
                $0.workFire?(data: conVersationList)
            }
        }
    }

    //: func func__handleTXConversationList(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func small(listArr: [V2TIMConversation], completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: var convList: [TalkingConversationModel] = []
        var convList: [ModelThen] = []

        // V2TIMConversation->ModelThen
        //: for conversation in listArr {
        for conversation in listArr {
            //: let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            let targetId = (conversation.userID != nil && conversation.userID.count > 0) ? conversation.userID : conversation.groupID
            //: var tempConvModel = self.convListCache[targetId!]
            var tempConvModel = self.convListCache[targetId!]
            //: if tempConvModel == nil {
            if tempConvModel == nil {
                //: tempConvModel = TalkingConversationModel(conv: conversation)
                tempConvModel = ModelThen(conv: conversation)
            }
            //: tempConvModel?.func__updateLastConvModel(conv: conversation)
            tempConvModel?.drop(conv: conversation)
            //: convList.append(tempConvModel!)
            convList.append(tempConvModel!)
        }

        // 增量
        //: var resultList: [TalkingConversationModel] = []
        var resultList: [ModelThen] = []
        //: let dbModels = WCDBUserInfoTable.db_getUserInfos(with: Array(self.convListCache.keys))
        let dbModels = GatherInfoTable.show(with: Array(self.convListCache.keys))
        //: let dbIdSet = Set(dbModels.map { $0.uid })
        let dbIdSet = Set(dbModels.map { $0.uid })
        //: for conversation in convList {
        for conversation in convList {
            // 添加db中是否有数据判断，防止刚启动APP，Delegate方法还未实现
            //: if self.func__addConvList(convModel: conversation, insertTop: false) ||
            if self.sincePrivacy(convModel: conversation, insertTop: false) ||
                //: (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
                (conversation.userID.count > 0 && !dbIdSet.contains(conversation.targetId))
            {
                //: resultList.append(conversation)
                resultList.append(conversation)
            }
        }
        //: let leftMore = (resultList.count > 0)
        let leftMore = (resultList.count > 0)
        //: completion(resultList, leftMore)
        completion(resultList, leftMore)
    }
}

// MARK: - 会话排序、删除、置顶

//: extension EdgeMsgListener {
extension EdgeMsgListener {
    /// 获取消息列表数据
    /// - Parameter completion: 回调
    //: func func__getConversationList(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
    func us(completion: @escaping (_ arr: [Any], _ leftMore: Bool) -> Void) {
        //: V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
        V2TIMManager.sharedInstance().getConversationList(self.nextSeq, count: 40) { list, nextSeq, _ in
            //: self.nextSeq = nextSeq
            self.nextSeq = nextSeq
            //: self.func__handleTXConversationList(listArr: list!) { arr, leftMore in
            self.small(listArr: list!) { arr, leftMore in
                //: self.func__updateConversationList(conVersationList: arr as? [TalkingConversationModel] ?? [])
                self.coalTarBetween(conVersationList: arr as? [ModelThen] ?? [])
                //: completion(arr, leftMore)
                completion(arr, leftMore)
            }
            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM获取会话列表失败，code:\(code), desc:\(String(describing: desc)).")
        }
    }

    /// 从缓存会话中获取模型
    //: func func__getCacheConversationModel(targetId: String) -> TalkingConversationModel? {
    func rowlock(targetId: String) -> ModelThen? {
        //: return self.convListCache[targetId]
        return self.convListCache[targetId]
    }

    //: func func__handleConversationList(convList: [V2TIMConversation]) -> [TalkingConversationModel] {
    func unify(convList: [V2TIMConversation]) -> [ModelThen] {
        //: var tempArr: [TalkingConversationModel] = []
        var tempArr: [ModelThen] = []
        //: for conv in convList {
        for conv in convList {
            //: let targetId = conv.userID ?? conv.groupID
            let targetId = conv.userID ?? conv.groupID
            //: var convModel = self.convListCache[targetId ?? ""]
            var convModel = self.convListCache[targetId ?? ""]
            //: if convModel == nil {
            if convModel == nil {
                //: convModel = TalkingConversationModel(conv: conv)
                convModel = ModelThen(conv: conv)
            }
            // 更新会话
            //: convModel?.func__updateLastConvModel(conv: conv)
            convModel?.drop(conv: conv)
            //: if self.func__addConvList(convModel: convModel!, insertTop: true) {
            if self.sincePrivacy(convModel: convModel!, insertTop: true) {
                //: tempArr.append(convModel!)
                tempArr.append(convModel!)
            }
        }
        //: return tempArr
        return tempArr
    }

    /// 会话列表新增会话
    /// - Parameters:
    ///   - convModel: 会话模型
    ///   - insertTop: 是否插入顶部
    /// - Returns: 是否添加成功
    //: func func__addConvList(convModel: TalkingConversationModel, insertTop: Bool) -> Bool {
    func sincePrivacy(convModel: ModelThen, insertTop: Bool) -> Bool {
        //: if convModel.chatType == .unDefined {
        if convModel.chatType == .unDefined {
            //: return false
            return false
        }
        //: if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }
        if !convModel.groupID.isEmptyString && convModel.chatType == .group { return false }

        //: let targetId = convModel.targetId
        let targetId = convModel.targetId
        // 系统消息，无需重新排序
        //: if convModel.chatType == .system || convModel.chatType == .service {
        if convModel.chatType == .system || convModel.chatType == .service {
            //: self.convListCache[targetId] = convModel
            self.convListCache[targetId] = convModel
            //: if self.officialList.count == 0 {
            if self.officialList.count == 0 {
                //: self.func__adddofficialList()
                self.strip()
            }
            //: if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
            if let index = officialList.firstIndex(where: { $0.userID == convModel.userID }) {
                //: let data = self.officialList[index]
                let data = self.officialList[index]
                //: convModel.showName = data.showName
                convModel.showName = data.showName
                //: self.officialList[index] = convModel
                self.officialList[index] = convModel
            }
            //: return true
            return true
        }

        // 普通消息
        //: var dataType: ChatListDataType = .normalList
        var dataType: DataType = .normalList
        //: var listArr = self.norConvList
        var listArr = self.norConvList
        //: let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - AdministratorThen.share.appConfigMode.moreMessageTimestamp
        let timeInterval = Int(convModel.listShowMessage?.timestamp.timeIntervalSince1970 ?? 0) - AdministratorThen.share.appConfigMode.moreMessageTimestamp
        // 是否建立会话
        //: var haveSeSSion = false
        var haveSeSSion = false
        //: if let userModel = DBUserInfoManager.cache_getCachedUserInfo(targetId: convModel.targetId) {
        if let userModel = DecisionMakerReactiveCompatible.clout(targetId: convModel.targetId) {
            //: haveSeSSion = userModel.isHaveSession
            haveSeSSion = userModel.isHaveSession
        }
        // 是否需要折叠
        //: var needFold = false
        var needFold = false
        //: if let elemData = convModel.listShowMessage?.customElem.data {
        if let elemData = convModel.listShowMessage?.customElem.data {
            //: let dict = ManagerPubliclyThen.parseTXMessageData(data: elemData)
            let dict = ManagerPubliclyThen.recover(data: elemData)
            //: let json = JSON(dict)
            let json = JSON(dict)
            //: needFold = json["extra"]["msgInfo"]["needFold"].boolValue
            needFold = json[(k_tempPath.lowercased())][(String(userIncreaseIdent.prefix(7)))][(String(noti_tipMessage))].boolValue
        }
        // 非置顶，非自己发送，超出时间 || 未建立会话，需要折叠
        //: if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
        if (convModel.isPinned == false && convModel.listShowMessage?.isSelf == false && timeInterval <= 0) ||
            //: (!haveSeSSion && needFold && convModel.isPinned == false)
            (!haveSeSSion && needFold && convModel.isPinned == false)
        {
            //: dataType = .moreList
            dataType = .moreList
            //: listArr = self.moreMsgList
            listArr = self.moreMsgList
            // 移除重复数据
            //: self.topConvList.removeAll { $0.userID == convModel.userID }
            self.topConvList.removeAll { $0.userID == convModel.userID }
            //: self.norConvList.removeAll { $0.userID == convModel.userID }
            self.norConvList.removeAll { $0.userID == convModel.userID }

            //: } else {
        } else {
            // 移除更多消息
            //: self.moreMsgList.removeAll { $0.userID == convModel.userID }
            self.moreMsgList.removeAll { $0.userID == convModel.userID }
            // 置顶消息
            //: if convModel.isPinned {
            if convModel.isPinned {
                //: dataType = .topList
                dataType = .topList
                //: listArr = self.topConvList
                listArr = self.topConvList
            }
        }

        //: let tempConvModel = self.convListCache[targetId]
        let tempConvModel = self.convListCache[targetId]
        //: if tempConvModel != nil {
        if tempConvModel != nil {
            //: if insertTop && listArr.first != tempConvModel {
            if insertTop && listArr.first != tempConvModel {
                //: if let index = listArr.firstIndex(of: tempConvModel!) {
                if let index = listArr.firstIndex(of: tempConvModel!) {
                    //: listArr.remove(at: index)
                    listArr.remove(at: index)
                }
                //: listArr.append(tempConvModel!)
                listArr.append(tempConvModel!)
                //: self.func__sortConVList(convList: listArr, dataType: dataType)
                self.playList(convList: listArr, dataType: dataType)
                //: return true
                return true
            }
            //: return false
            return false
        }

        //: self.convListCache[targetId] = convModel
        self.convListCache[targetId] = convModel
        //: listArr.append(convModel)
        listArr.append(convModel)
        //: self.func__sortConVList(convList: listArr, dataType: dataType)
        self.playList(convList: listArr, dataType: dataType)
        //: return true
        return true
    }

    /// 数据排序
    /// - Parameters:
    ///   - convList: 会话列表
    ///   - dataType: 数据类型
    //: func func__sortConVList(convList: [TalkingConversationModel], dataType: ChatListDataType) {
    func playList(convList: [ModelThen], dataType: DataType) {
        //: switch dataType {
        switch dataType {
        //: case .topList:
        case .topList:
            //: self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.topConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .normalList:
        case .normalList:
            //: self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }
            self.norConvList = convList.sorted { $0.orderKey > $1.orderKey }

        //: case .moreList:
        case .moreList:
            //: self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
            self.moreMsgList = convList.sorted { $0.orderKey > $1.orderKey }
        }
    }

    /// 删除会话
    /// - Parameters:
    ///   - targetId: 会话Id
    ///   - dataType: 会话类型
    //: func func__removeConversation(targetId: String, dataType: ChatListDataType) {
    func assemblage(targetId: String, dataType: DataType) {
        //: guard !targetId.isEmpty else { return }
        guard !targetId.isEmpty else { return }
        //: guard let tempConvModel = self.convListCache[targetId] else { return }
        guard let tempConvModel = self.convListCache[targetId] else { return }
        //: let convID = (tempConvModel.chatType == .private ? "c2c_\(targetId)" : "group_\(targetId)")
        let convID = (tempConvModel.chatType == .private ? (String(show_meetingUrl.suffix(4))) + "\(targetId)" : (String(noti_memoryMessage)) + "\(targetId)")
        //: V2TIMManager.sharedInstance().deleteConversation(convID) {
        V2TIMManager.sharedInstance().deleteConversation(convID) {
            // 移除db
            //: DBUserInfoManager.db_removeRecord(with: targetId)
            DecisionMakerReactiveCompatible.customerPackage(with: targetId)
            //: WCDBVoiceMsgTable.db_deleteVoiceMsg(AdministratorThen.share.loginUserMode.userID, toUid: targetId)
            FireMsgTable.communication(AdministratorThen.share.loginUserMode.userID, toUid: targetId)

            //: self.convListCache.removeValue(forKey: targetId)
            self.convListCache.removeValue(forKey: targetId)
            //: switch dataType {
            switch dataType {
            //: case .topList:
            case .topList:
                //: if let index = self.topConvList.firstIndex(of: tempConvModel) {
                if let index = self.topConvList.firstIndex(of: tempConvModel) {
                    //: self.topConvList.remove(at: index)
                    self.topConvList.remove(at: index)
                }
            //: case .normalList:
            case .normalList:
                //: if let index = self.norConvList.firstIndex(of: tempConvModel) {
                if let index = self.norConvList.firstIndex(of: tempConvModel) {
                    //: self.norConvList.remove(at: index)
                    self.norConvList.remove(at: index)
                }
            //: case .moreList:
            case .moreList:
                //: if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                if let index = self.moreMsgList.firstIndex(of: tempConvModel) {
                    //: self.moreMsgList.remove(at: index)
                    self.moreMsgList.remove(at: index)
                }
            }

            //: let unreadCount = tempConvModel.unreadCount
            let unreadCount = tempConvModel.unreadCount
            //: if unreadCount > 0 {
            if unreadCount > 0 {
                //: tempConvModel.unreadCount = 0
                tempConvModel.unreadCount = 0
                //: tempConvModel.isShowMsgUnread = false
                tempConvModel.isShowMsgUnread = false
            }
            //: self.func__getUnreadMsgCount()
            self.attitudeCount()

            //: } fail: { code, desc in
        } fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM删除会话失败，convID:\(convID), code:\(code), desc:\(String(describing: desc)).")
            ProblemBenchPoundThen.panelDetect(msg: (String(app_equalPath)) + "\(convID)" + (String(mSurvivalName) + String(dreamAuthorizeKey.suffix(5))) + "\(code)" + (String(user_bouncePath.suffix(7))) + "\(String(describing: desc)).")
        }
    }

    /// 置顶会话
    //: func func__setConversationTop(convModel: TalkingConversationModel, isTop: Bool) {
    func treetop(convModel: ModelThen, isTop: Bool) {
        //: V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
        V2TIMManager.sharedInstance().pinConversation(convModel.conversationID, isPinned: isTop) {} fail: { code, desc in
            //: UploadLogTool.writeLog(msg: "IM置顶会话失败，convId:\(convModel.conversationID), code:\(code), desc:\(String(describing: desc)).")
        }
        //: convModel.isPinned = isTop
        convModel.isPinned = isTop
        //: if isTop {
        if isTop {
            //: self.topConvList.insert(convModel, at: 0)
            self.topConvList.insert(convModel, at: 0)
            //: if let index = self.norConvList.firstIndex(of: convModel) {
            if let index = self.norConvList.firstIndex(of: convModel) {
                //: self.norConvList.remove(at: index)
                self.norConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.topConvList, dataType: .topList)
            self.playList(convList: self.topConvList, dataType: .topList)
            //: } else {
        } else {
            //: self.norConvList.insert(convModel, at: 0)
            self.norConvList.insert(convModel, at: 0)
            //: if let index = self.topConvList.firstIndex(of: convModel) {
            if let index = self.topConvList.firstIndex(of: convModel) {
                //: self.topConvList.remove(at: index)
                self.topConvList.remove(at: index)
            }
            //: self.func__sortConVList(convList: self.norConvList, dataType: .normalList)
            self.playList(convList: self.norConvList, dataType: .normalList)
        }
    }
}

// MARK: - V2TIMSDKListener

//: extension EdgeMsgListener: V2TIMSDKListener {
extension EdgeMsgListener: V2TIMSDKListener {
    //: public func onConnecting() {
    public func onConnecting() {
        //: printLog(message: "SDK 正在连接到服务器")
        printLog(message: (String(m_woodIdent.prefix(5)) + String(appAutomaticallyValue)))
    }

    //: public func onConnectSuccess() {
    public func onConnectSuccess() {
        //: printLog(message: "SDK 已经成功连接到服务器")
        printLog(message: (String(noti_aideMsg) + String(notiShadePromptMsg)))
    }

    //: public func onConnectFailed(_ code: Int32, err: String!) {
    public func onConnectFailed(_ code: Int32, err: String!) {
        //: UploadLogTool.writeLog(msg: "IM连接服务器失败，code:\(code), error:\(err!).")
        ProblemBenchPoundThen.panelDetect(msg: (userThinkWeekName.replacingOccurrences(of: "than", with: "M") + app_wouldStr.capitalized + "失败，code:") + "\(code)" + (String(dream_statusValue.prefix(8))) + "\(err!).")
    }

    //: public func onKickedOffline() {
    public func onKickedOffline() {
        //: UploadLogTool.writeLog(msg: "IM账号被挤下线。")
        ProblemBenchPoundThen.panelDetect(msg: (String(userCloudFormat.prefix(5)) + "挤下线。"))
        // socket断开连接
        //: TalkingSocketManager.shared.closeWebSocket(userLoginOut: true)
        AdviceLandscapeReactiveCompatible.shared.coverProperty(userLoginOut: true)
        //: EdgeMsgListener.shared.isConnection = false
        EdgeMsgListener.shared.isConnection = false
        // 销毁上传busy状态定时器
        //: (TalkingApplication.shared as! TalkingApplication).destroy()
        (ApplicationKingfisherCompatible.shared as! ApplicationKingfisherCompatible).borderPermission()

        //: let msg = "Your account is logged in on another device and you are forced to go offline! If it is not done by me, the password may have been leaked, and it is recommended to change the password.".localized
        let msg = String(bytes: main_streamLeadingId.map{rawMaster(eon: $0)}, encoding: .utf8)!.localized
        //: let config = ShowAlertConfig()
        let config = GainGageAlertConfig()
        //: if TalkingRequestAddrTool.share.interfaceLang == LangType.ar.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue ||
        if BubbleVisitorReactiveCompatible.share.interfaceLang == StatusExpressionConvertible.ar.rawValue || BubbleVisitorReactiveCompatible.share.interfaceLang == StatusExpressionConvertible.es.rawValue ||
            //: TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue
            BubbleVisitorReactiveCompatible.share.interfaceLang == StatusExpressionConvertible.pt.rawValue
        {
            //: config.width = 340
            config.width = 340
            //: config.rightFont = UIFont.pingfangMediumFont(fontSize: 13)
            config.rightFont = UIFont.imitationClotheStatement(fontSize: 13)
        }

        //: TalkingAlertShow.alert()
        GentleAlertShow.aboveSixth()
        //: TalkingAlertShow.alert(title: "Offline Notification".localized, message: msg, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Re Login".localized, leftBlock: {
        GentleAlertShow.aboveSixth(title: (String(userEndVillagePath.prefix(7)) + String(kHeartData)).localized, message: msg, leftBtnTitle: (String(app_onceWedTitle)).localized, rightBtnTitle: (String(show_ridSubjectName.suffix(4)) + "ogin").localized, leftBlock: {
            // 销毁直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            TapBecauseLiveManager.resolutionShared().areaLive()
            // 销毁语聊房
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            LanceManager.doingResource().turnChoose()
            // 调用退出登录接口
            //: TalkingLoginRequestTool.req_loginOut { t in
            InviteeRequestTool.executeValue { t in
                //: ProgressHUD.dismiss()
                VillageView.statusDismiss()
                //: if t {
                if t {
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: appVoiceUnderText, object: nil, userInfo: nil)
                }
            }
            //: }, rightBlock: {
        }, rightBlock: {
            //: TalkingLoginRequestTool.func__requestReloginUserInfo { t in
            InviteeRequestTool.called { t in
                //: if t {
                if t {
                    //: EdgeMsgListener.shared.func__LogingIn { _ in
                    EdgeMsgListener.shared.run { _ in
                        /// 直播弹幕重连
                        //: if TalkingLiveManager.shared().liveRoomModel.chatGroupId.count > 0 {
                        if TapBecauseLiveManager.resolutionShared().liveRoomModel.chatGroupId.count > 0 {
                            //: V2TIMManager.sharedInstance().joinGroup(TalkingLiveManager.shared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            V2TIMManager.sharedInstance().joinGroup(TapBecauseLiveManager.resolutionShared().liveRoomModel.chatGroupId, msg: nil) {} fail: { _, _ in
                            }
                        }
                    }
                    // socket重连
                    //: TalkingSocketManager.shared.updateWebSocket()
                    AdviceLandscapeReactiveCompatible.shared.tapSocket()
                }
            }
            //: }, config: config)
        }, config: config)
    }
}
