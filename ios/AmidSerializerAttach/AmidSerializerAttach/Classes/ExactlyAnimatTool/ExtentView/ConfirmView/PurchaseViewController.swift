
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let userManualData:[UInt8] = [0x3,0x4,0x3,0x1e,0x42,0x9,0x5,0xe,0xf,0x18,0x50,0x43,0x4a,0x2,0xb,0x19,0x4a,0x4,0x5,0x1e,0x4a,0x8,0xf,0xf,0x4,0x4a,0x3,0x7,0x1a,0x6,0xf,0x7,0xf,0x4,0x1e,0xf,0xe]

private func postPlan(snap num: UInt8) -> UInt8 {
    return num ^ 106
}

/*: "F5F5F5" :*/
fileprivate let mMakeupTitle:String = "f"
fileprivate let userStudyTrustName:[Character] = ["5","F","5","F","5"]

/*: "#128CFF" :*/
fileprivate let kBaseballName:String = "forward digital#128CFF"

/*: "Feedback" :*/
fileprivate let appOrdinaryKey:String = "Feedsize commercial"

/*: "#8370FF" :*/
fileprivate let userAgencyValue:String = "#8370FFhesitate secret background peer"

/*: "Quick Greeting" :*/
fileprivate let mainAttitudeStr:String = "eff autoQuick Gre"
fileprivate let dream_everybodyName:String = "etclearlyg"

/*: "Beauty Photos" :*/
fileprivate let app_amIdent:[Character] = ["B","e","a","u"]
fileprivate let noti_developIdent:String = "height representativety P"

/*: "url" :*/
fileprivate let main_wantMsg:[Character] = ["u","r","l"]

/*: "unlockGift" :*/
fileprivate let dreamColumnName:String = "bounce feed ease selectionunlockGi"
fileprivate let m_donStartPresentationTitle:String = "rite"

/*: "isBurn" :*/
fileprivate let main_typicalStr:[UInt8] = [0xbe,0xc8,0x97,0xca,0xc7,0xc3]

fileprivate func lightSpeed(mic num: UInt8) -> UInt8 {
    let value = Int(num) - 85
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftId" :*/
fileprivate let mainThanName:String = "record tone minimum envelope playgiftId"

/*: "rYMsgType" :*/
fileprivate let noti_lowMessage:String = "will mayrYMsg"
fileprivate let app_modifyDepthDeclineName:String = "side draftType"

/*: "GJ:CallCustom" :*/
fileprivate let mainFirstUrl:[Character] = ["G","J",":","C","a","l","l","C","u","s","t","o"]
fileprivate let mainArcMessage:String = "automatically"

/*: "changeDirection" :*/
fileprivate let app_wantContent:[Character] = ["c","h","a","n","g","e","D","i","r","e","c","t"]
fileprivate let noti_patientId:[Character] = ["i","o","n"]

/*: "extra" :*/
fileprivate let user_goldenData:[Character] = ["e","x","t","r","a"]

/*: "msgInfo" :*/
fileprivate let noti_peerActiveUrl:[Character] = ["m","s","g","I","n"]
fileprivate let m_runningYourText:String = "fya"

/*: "toUid" :*/
fileprivate let showCompareIdent:String = "yet midnight converttoUid"

/*: "wantGift" :*/
fileprivate let mainWordStr:[Character] = ["w","a","n","t","G","i","f","t"]

/*: "jumpKey" :*/
fileprivate let user_epicId:String = "arrow plain scanjumpKey"

/*: "truePersonAuth" :*/
fileprivate let app_othersText:String = "truePestrip tight landmark drag"
fileprivate let mainBuryMergeData:String = "rtitleon"

/*: "logType" :*/
fileprivate let mExistSlidePath:[Character] = ["l","o","g","T","y","p","e"]

/*: "msgType" :*/
fileprivate let notiEffectTitle:[UInt8] = [0x65,0x70,0x79,0x54,0x67,0x73,0x6d]

/*: "txt" :*/
fileprivate let appRequireTightValue:[Character] = ["t","x","t"]

/*: "type" :*/
fileprivate let dream_lanceData:String = "punishype"

/*: "QuickMsg" :*/
fileprivate let appCurNearFormat:String = "civil imitation boy budget givingQuickMsg"

/*: "params" :*/
fileprivate let m_behindSecondFormat:[UInt8] = [0x39,0x2a,0x3b,0x2a,0x36,0x3c]

fileprivate func importantOning(still num: UInt8) -> UInt8 {
    let value = Int(num) - 201
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let k_sinkData:[UInt8] = [0xa,0x16,0x15,0x1b,0xc,0x15,0x1b]

fileprivate func basicWhite(calculation num: UInt8) -> UInt8 {
    let value = Int(num) + 89
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "txtId" :*/
fileprivate let userStepPath:String = "txtIdrem feminist boundary weight cause"

/*: "message" :*/
fileprivate let noti_boaBindFormat:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let k_wholeId:[Character] = ["T","x","t","M","s","g"]

/*: "Voice hello download failed. Procedure" :*/
fileprivate let kDetermineStr:[UInt8] = [0xcc,0xe5,0xdf,0xd9,0xdb,0x96,0xde,0xdb,0xe2,0xe2,0xe5,0x96,0xda,0xe5,0xed,0xe4,0xe2,0xe5,0xd7,0xda,0x96,0xdc,0xd7,0xdf,0xe2,0xdb,0xda,0xa4,0x96,0xc6,0xe8,0xe5,0xd9,0xdb,0xda,0xeb,0xe8,0xdb]

fileprivate func houseEffect(when num: UInt8) -> UInt8 {
    let value = Int(num) - 118
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "length" :*/
fileprivate let appParentMsg:[Character] = ["l","e","n","g","t","h"]

/*: "audio" :*/
fileprivate let app_identityMessage:[Character] = ["a","u","d","i","o"]

/*: "audioId" :*/
fileprivate let kSendUrl:[Character] = ["a","u"]
fileprivate let dreamSometimeId:[Character] = ["d","i","o","I","d"]

/*: "audioLength" :*/
fileprivate let mainEndMsg:[UInt8] = [0xfa,0xe,0xfd,0x2,0x8,0xe5,0xfe,0x7,0x0,0xd,0x1]

fileprivate func slimImplement(anyone num: UInt8) -> UInt8 {
    let value = Int(num) - 153
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "audioSandbox" :*/
fileprivate let mWireFormat:String = "audioSandeveryone nobody particle fail find"
fileprivate let noti_lensMsg:[Character] = ["b","o","x"]

/*: "senderId" :*/
fileprivate let mCaptainBlankId:[UInt8] = [0x2a,0x1c,0x25,0x1b,0x1c,0x29,0x0,0x1b]

fileprivate func enhancePost(audience num: UInt8) -> UInt8 {
    let value = Int(num) + 73
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "AudioMsg" :*/
fileprivate let m_methodDominantUrl:String = "Audrelation fog follow"
fileprivate let notiShirtMsg:[Character] = ["i","o","M","s","g"]

/*: "Can't send blank message" :*/
fileprivate let dream_constantMessage:[Character] = ["C","a","n","\'","t"]
fileprivate let dreamPepUrl:[Character] = [" ","s","e","n"]
fileprivate let mProfessionalPath:String = "d blaanyone accord man boundary expression"
fileprivate let appLayParticipantUrl:String = "SSAGE"

/*: "quoteMsgId" :*/
fileprivate let mClothesPlanData:String = "prepare"
fileprivate let main_digitalText:String = "oteMsgIdread epic user less ordinary"

/*: "quoteMsgInfo" :*/
fileprivate let app_argumentKey:[Character] = ["q","u","o","t","e"]
fileprivate let noti_takeData:String = "MsgInfomotivation form asset square powder"

/*: "contentType" :*/
fileprivate let m_wallFormat:[UInt8] = [0xa3,0xaf,0xae,0xb4,0xa5,0xae,0xb4,0x94,0xb9,0xb0,0xa5]

fileprivate func sampleView(above num: UInt8) -> UInt8 {
    let value = Int(num) - 64
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "ImgMsg" :*/
fileprivate let dream_importantValue:[Character] = ["I","m","g","M","s","g"]

/*: "img" :*/
fileprivate let dream_providerTitle:String = "IMG"

/*: "imageUri" :*/
fileprivate let main_siteName:[UInt8] = [0xb,0xf,0x3,0x5,0x7,0x37,0x10,0xb]

private func alongPrice(tin num: UInt8) -> UInt8 {
    return num ^ 98
}

/*: "customSend" :*/
fileprivate let showStrikeKey:[UInt8] = [0xce,0xd8,0xde,0xd9,0xc2,0xc0,0xfe,0xc8,0xc3,0xc9]

private func fieldAbove(minor num: UInt8) -> UInt8 {
    return num ^ 173
}

/*: "pic" :*/
fileprivate let dream_differenceMessage:String = "lifestyleic"

/*: "VideoMsg" :*/
fileprivate let main_taText:String = "object living statement paperVid"
fileprivate let dreamNumberroStr:[Character] = ["e","o","M","s","g"]

/*: "video" :*/
fileprivate let dreamFiniteId:String = "vdailydeo"

/*: "coverImg" :*/
fileprivate let m_instanceUrl:[UInt8] = [0x38,0x34,0x2d,0x3e,0x29,0x12,0x36,0x3c]

private func totalGenerate(prompt num: UInt8) -> UInt8 {
    return num ^ 91
}

/*: "videoUrl" :*/
fileprivate let dreamBrowIdent:[UInt8] = [0x75,0x68,0x63,0x64,0x6e,0x54,0x71,0x6b]

fileprivate func decideLeaf(ready num: UInt8) -> UInt8 {
    let value = Int(num) - 255
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "duration" :*/
fileprivate let showBeforeUrl:[UInt8] = [0x18,0x29,0x26,0x15,0x28,0x1d,0x23,0x22]

fileprivate func shrinkPublish(fruit num: UInt8) -> UInt8 {
    let value = Int(num) + 76
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "len" :*/
fileprivate let showNumbererruptStr:[UInt8] = [0x34,0x3d,0x36]

private func reloadThanks(same num: UInt8) -> UInt8 {
    return num ^ 88
}

/*: "Documents/User/voice/" :*/
fileprivate let app_foreIdent:String = "why instructionDocuments"
fileprivate let dreamWavePath:String = "/Userunknown learn"

/*: "Documents/User/record/" :*/
fileprivate let show_joinId:String = "may last floorDocum"
fileprivate let noti_recordData:String = "shared engagement/Use"
fileprivate let app_onUrl:String = "r/recglass am mic top"

/*: "msgIncome" :*/
fileprivate let kMeetName:String = "msgIncomestill gravity lat child properly"

/*: "mfBean" :*/
fileprivate let noti_alwaysMsg:[UInt8] = [0x5b,0x50,0x74,0x53,0x57,0x58]

/*: "messageType" :*/
fileprivate let main_gainNumberroTitle:[Character] = ["m","e","s","s","a","g","e","T","y","p","e"]

/*: "WantGift" :*/
fileprivate let show_optimisticStr:String = "WantGifriver failure edge"
fileprivate let user_heDeliveryMessage:[Character] = ["t"]

/*: _PrivateChatTipsTitle_ :*/
fileprivate let app_holdIdent:String = "_Pridit depth empty finger"
fileprivate let app_fieldPath:String = "hatTihour king properly arrive"
fileprivate let showJoinAgoText:String = "psTimeet play greet"

/*: "msgId" :*/
fileprivate let mainImplementData:String = "msgIdyellow whole overlook clock"

/*: "totalMfCoin" :*/
fileprivate let dream_appearancePanTelevisionName:String = "alwaysoalwaysal"
fileprivate let k_recommendMsg:String = "assert properly bot strokeMfCoin"

/*: "attachSysMsg" :*/
fileprivate let userListValue:String = "aglobalglobalach"

/*: "intimatePhoto" :*/
fileprivate let user_economicIdent:[Character] = ["i","n","t","i","m"]
fileprivate let dreamTenderMessage:[Character] = ["a"]
fileprivate let noti_duringStr:[Character] = ["t","e","P","h","o","t","o"]

/*: "totalIntimate" :*/
fileprivate let dreamSmoothIdent:String = "lose blindtotalI"

/*: "uid" :*/
fileprivate let userEntryProjectName:[UInt8] = [0x94,0x88,0x85]

private func socialUniform(stat num: UInt8) -> UInt8 {
    return num ^ 225
}

/*: "intimate" :*/
fileprivate let dream_woodKey:[UInt8] = [0x65,0x74,0x61,0x6d,0x69,0x74,0x6e,0x69]

/*: "Can't use the function due to your lack of intimacy level." :*/
fileprivate let notiListenKey:[UInt8] = [0xe3,0x1,0xe,0xc7,0x14,0xc0,0x15,0x13,0x5,0xc0,0x14,0x8,0x5,0xc0,0x6,0x15,0xe,0x3,0x14,0x9,0xf,0xe,0xc0,0x4,0x15,0x5,0xc0,0x14,0xf,0xc0,0x19,0xf,0x15,0x12,0xc0,0xc,0x1,0x3,0xb,0xc0,0xf,0x6,0xc0,0x9,0xe,0x14,0x9,0xd,0x1,0x3,0x19,0xc0,0xc,0x5,0x16,0x5,0xc,0xce]

fileprivate func textPrompt(remove num: UInt8) -> UInt8 {
    let value = Int(num) + 96
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "giftNum" :*/
fileprivate let showCutIdent:String = "river birthday your naturalgiftNum"

/*: "pkgItemsetId" :*/
fileprivate let k_throughData:String = "behavior dismiss replacementpkgItem"
fileprivate let userPageData:[Character] = ["s","e","t","I","d"]

/*: "from" :*/
fileprivate let main_ceremonyStr:String = "frocute"

/*: "wantGiftId" :*/
fileprivate let userPositiveKey:String = "wthan"
fileprivate let kNearUrl:String = "tGiftIdenough representation old spread grade"

/*: "WantGiftMsg" :*/
fileprivate let dream_automaticFormat:[Character] = ["W","a"]
fileprivate let mainMatchStr:[Character] = ["n","t","G","i","f","t","M","s","g"]

/*: "gift" :*/
fileprivate let showModifyName:[Character] = ["g","i","f","t"]

/*: "View info" :*/
fileprivate let mainBrightId:[Character] = ["V","i","e","w"," ","i","n","f","o"]

/*: "Notes" :*/
fileprivate let dream_siteMsg:String = "chin insideNotes"

/*: "Report" :*/
fileprivate let showTransformUrl:String = "Reportrecommend interested except"

/*: "View read receipts?" :*/
fileprivate let user_atmId:String = "Viewparent forth"
fileprivate let mMysteryMsg:String = "final miss receip"
fileprivate let dreamRankValue:[Character] = ["t","s","?"]

/*: "She" :*/
fileprivate let showDepthDevelopName:String = "display thumbShe"

/*: "%@ is using too low a version" :*/
fileprivate let showPolicyName:[Character] = ["%","@"," ","i","s"," ","u","s","i","n","g"," ","t","o","o"," ","l","o","w"]
fileprivate let m_trackPath:[Character] = [" ","a"," ","v","e","r","s","i","o","n"]

/*: "Shoot video at least 5 seconds" :*/
fileprivate let mExplainName:String = "Shoosuspicion unite remind pending"
fileprivate let userDominantInsideKey:String = "every removal compriseo at "
fileprivate let user_maxFormat:String = "repeat index equity difference thant 5 s"

/*: "Upload failed" :*/
fileprivate let appNextScaleEmptyData:[Character] = ["U","p","l","o","a","d"," ","f","a","i","l","e","d"]

/*: "播放状态: :*/
fileprivate let appIndividualFormat:[Character] = ["播","放","状","态",":"]

/*: "c2c_" :*/
fileprivate let m_advertisingEveryBalanceStr:String = "c2c_mane makeup care"

/*: "msg" :*/
fileprivate let kQuietValue:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let dream_makerMsg:String = "TIPS"

/*: "You can use the video chat feature to give %@ a call" :*/
fileprivate let notiExhibitKey:[UInt8] = [0x4a,0x7c,0x66,0x33,0x70,0x72,0x7d,0x33,0x66,0x60,0x76,0x33,0x67,0x7b,0x76,0x33,0x65,0x7a,0x77,0x76,0x7c,0x33,0x70,0x7b,0x72,0x67,0x33,0x75,0x76,0x72,0x67,0x66,0x61,0x76,0x33,0x67,0x7c,0x33,0x74,0x7a,0x65,0x76,0x33,0x36,0x53,0x33,0x72,0x33,0x70,0x72,0x7f,0x7f]

private func opParent(earn num: UInt8) -> UInt8 {
    return num ^ 19
}

/*: "cellData" :*/
fileprivate let userReaderWillAideIdent:[Character] = ["c","e","l","l","D","a","t","a"]

/*: "System notification" :*/
fileprivate let dreamConcentrationKey:[Character] = ["S","y","s","t","e","m"," ","n","o","t","i","f","i","c"]
fileprivate let dream_exactlyWeeklyCarrierId:[Character] = ["a","t","i","o","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  PurchaseViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/9.
//

//: import AVFAudio
import AVFAudio
//: import Photos
import Photos
//: import UIKit
import UIKit

/// 私聊入口枚举
//: enum PrivateChatIsFromEnum {
enum AdvocateFromEnum {
    //: case Normal
    case Normal // 默认入口
    //: case LiveRoom
    case LiveRoom // 来自直播间（半屏展示）
}

//: class TalkingPrivateChatController: TalkingBaseViewController {
class PurchaseViewController: FlexibleViewController {
    //: static var replyLatestTimes: Date = Date(timeIntervalSince1970: 0)
    static var replyLatestTimes: Date = .init(timeIntervalSince1970: 0) // 最新回复时间
    //: var targetId = ""                                           // 接收方UserId
    var targetId = "" // 接收方UserId
    //: var needShowRating = false
    var needShowRating = false // 是否展示app store评分
    //: var quickGreetingTextDict: [String: Any]?
    var quickGreetingTextDict: [String: Any]? // 一键打招呼文本消息
    //: var quickGreetingAudioDict: [String: Any]?
    var quickGreetingAudioDict: [String: Any]? // 一键打招呼语音消息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var conversationType: TalkingIMChatType = .private
    private var conversationType: TalkingIMChatType = .private
    //: private var userWrap = MerelyMeasurable()
    private var userWrap = MerelyMeasurable() // chatInfo模型
    //: private var isFrom = PrivateChatIsFromEnum.Normal
    private var isFrom = AdvocateFromEnum.Normal // 私聊入口

    //: private var isShowIntimateView = false
    private var isShowIntimateView = false // 顶部是否已经展示了亲密度头像
    //: var QuickEndBlock: ((_ isSuccess: Bool) -> Void)?
    var QuickEndBlock: ((_ isSuccess: Bool) -> Void)? /// 1.7.8快速打招呼完成后

    //: init(chatID: String, isFrom: PrivateChatIsFromEnum = .Normal, userWrap: MerelyMeasurable = MerelyMeasurable()) {
    init(chatID: String, isFrom: AdvocateFromEnum = .Normal, userWrap: MerelyMeasurable = MerelyMeasurable()) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: targetId = chatID
        targetId = chatID
        //: self.userWrap = userWrap
        self.userWrap = userWrap

        //: TalkingPrivateChatController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        PurchaseViewController.replyLatestTimes = Date(timeIntervalSince1970: 0)
        //: TalkingPrivateChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        PlumpDownReadingManageressChatManager.share.readReceiptDict[targetId] = self.userWrap.readReceiptTime
        //: self.isFrom = isFrom
        self.isFrom = isFrom
        //: if targetId == CreditsThen.getXiaoMiID() {
        if targetId == CreditsThen.rowId() { // 系统消息
            //: conversationType = .system
            conversationType = .system
            //: hideNavi = false
            hideNavi = false
            //: } else if targetId == CreditsThen.getCustomerServiceID() {
        } else if targetId == CreditsThen.tingId() { // 官方客服
            //: conversationType = .service
            conversationType = .service
            //: hideNavi = true
            hideNavi = true

            //: } else {
        } else {
            //: hideNavi = true
            hideNavi = true
        }
        //: msgView.targetID = targetId
        msgView.targetID = targetId
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: userManualData.map{postPlan(snap: $0)}, encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AdministratorThen.share.appConfigMode.disableShootScreen {
            if AdministratorThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = GentleReactiveCompatible(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: half_showViewAnimation()
        throughAnimation()
        //: createUI()
        flag()
        //: createUIConstraints()
        resign()
        //: bindInteraction()
        fileTargetQuantityeraction()
        //: addTapGestureRecognizer()
        donkeyEngine()
        //: func__requestChatInfo()
        privacyInfo()
        //: func__getFillDraftText()
        componentPart()
        //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
        SuggestReactiveCompatible.shared.darkAges()
        //: func__addVoiceNotification()
        straightAndNarrow()
        //: addReadPeceiptNotification()
        detailFor()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            full()
            //: func__TipsNotification()
            tentNotification()
            //: if AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue {
            if AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue {
                //: func__setReplyTipsView()
                superpose()
            }
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        boxAdd(isAdd: true)
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        boxAdd(isAdd: false)
        //: if conversationType == .private {
        if conversationType == .private {
            //: func__saveFillDraftText()
            explainContainer()
        }
    }

    //: override func viewDidDisappear(_ animated: Bool) {
    override func viewDidDisappear(_ animated: Bool) {
        //: super.viewDidDisappear(animated)
        super.viewDidDisappear(animated)
        // 展示App Store评分
        //: if needShowRating {
        if needShowRating {
            //: AppStoreRatingManager.shared.requestQuickRating()
            PickRatingManager.shared.agree()
        }
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    // MARK: - Lazy Load

    //: private lazy var contentView: UIView = {
    private lazy var contentView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .white
        v.backgroundColor = .white
        //: return v
        return v
        //: }()
    }()

    //: lazy var msgView: TUIMessageController = {
    lazy var msgView: MessageViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MessageViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: if conversationType == .private {
        if conversationType == .private {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor.clear
            msgVC.tableView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: msgVC.view.backgroundColor = UIColor.clear
            msgVC.view.backgroundColor = UIColor.clear
            //: msgVC.tableView.backgroundColor = UIColor(hex: "F5F5F5")
            msgVC.tableView.backgroundColor = UIColor(hex: (mMakeupTitle.uppercased() + String(userStudyTrustName)))
        }
        //: msgVC.tableView.register(TalkingChatTextMsgCell.self, forCellReuseIdentifier: TalkingChatTextMsgCell.className())
        msgVC.tableView.register(ConfirmView.self, forCellReuseIdentifier: ConfirmView.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(TapViewCell.self, forCellReuseIdentifier: TapViewCell.className())
        //: msgVC.tableView.register(TalkingChatVideoMsgCell.self, forCellReuseIdentifier: TalkingChatVideoMsgCell.className())
        msgVC.tableView.register(RoundingAgencyViewCell.self, forCellReuseIdentifier: RoundingAgencyViewCell.className())
        //: msgVC.tableView.register(TalkingChatGiftMsgCell.self, forCellReuseIdentifier: TalkingChatGiftMsgCell.className())
        msgVC.tableView.register(ReadingReactiveCompatible.self, forCellReuseIdentifier: ReadingReactiveCompatible.className())
        //: msgVC.tableView.register(TalkingChatAskForGiftMsgCell.self, forCellReuseIdentifier: TalkingChatAskForGiftMsgCell.className())
        msgVC.tableView.register(UnwantedMsgCell.self, forCellReuseIdentifier: UnwantedMsgCell.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ExtentView.self, forCellReuseIdentifier: ExtentView.className())
        //: msgVC.tableView.register(TalkingChatMatchMsgCell.self, forCellReuseIdentifier: TalkingChatMatchMsgCell.className())
        msgVC.tableView.register(ReadingViewCell.self, forCellReuseIdentifier: ReadingViewCell.className())
        //: msgVC.tableView.register(TalkingChatAudioMsgCell.self, forCellReuseIdentifier: TalkingChatAudioMsgCell.className())
        msgVC.tableView.register(MovieMsgCell.self, forCellReuseIdentifier: MovieMsgCell.className())
        //: msgVC.tableView.register(TalkingChatSystemNotifJumpMsgCell.self, forCellReuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
        msgVC.tableView.register(GatherReactiveCompatible.self, forCellReuseIdentifier: GatherReactiveCompatible.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: AutoloadingReactiveCompatible.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(LackMessageCell.self, forCellReuseIdentifier: AutoloadingReactiveCompatible.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType

        //: return msgVC
        return msgVC
        //: }()
    }()

    //: lazy var bgImgeView: UIImageView = {
    lazy var bgImgeView: UIImageView = {
        //: let imgView = UIImageView.init()
        let imgView = UIImageView()
        //: imgView.backgroundColor = .clear
        imgView.backgroundColor = .clear
        //: imgView.contentMode = .scaleAspectFill
        imgView.contentMode = .scaleAspectFill
        //: imgView.clipsToBounds = true
        imgView.clipsToBounds = true
        //: return imgView
        return imgView
        //: }()
    }()

    //: lazy var coverView: UIVisualEffectView = {
    lazy var coverView: UIVisualEffectView = {
        //: let effect = UIBlurEffect.init(style: .dark)
        let effect = UIBlurEffect(style: .dark)
        //: let effectView = UIVisualEffectView.init(effect: effect)
        let effectView = UIVisualEffectView(effect: effect)
        //: effectView.alpha = 0.45
        effectView.alpha = 0.45
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var bgView: UIView = {
    lazy var bgView: UIView = {
        //: let view = UIImageView.init()
        let view = UIImageView()
        //: view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        view.backgroundColor = UIColor.black.withAlphaComponent(0.75)
        //: return view
        return view
        //: }()
    }()

    //: private lazy var videoCallManager: TalkingCallMenuManager = {
    private lazy var videoCallManager: EntitleMenuManager = {
        //: let m = TalkingCallMenuManager()
        let m = EntitleMenuManager()
        //: m.uid = targetId
        m.uid = targetId
        //: m.onReqestCallErrorBlock = { [weak self] errNo in
        m.onReqestCallErrorBlock = { [weak self] errNo in
            //: guard let self = self,errNo == 101 else { return }
            guard let self = self, errNo == 101 else { return }
            //: self.msgInputView.setHidenFreeMsgTimesView()
            self.msgInputView.hem()
        }
        //: return m
        return m
        //: }()
    }()

    //: lazy var msgInputView: TalkingChatInputView = {
    lazy var msgInputView: VillageThen = {
        //: var type: InputViewType = .PrivateChat
        var type: FireLiteralViewType = .PrivateChat
        //: if conversationType == .service {
        if conversationType == .service {
            //: type = .CustomerChat
            type = .CustomerChat
            //: } else if self.isFrom == .LiveRoom {
        } else if self.isFrom == .LiveRoom {
            //: type = .LiveRoom
            type = .LiveRoom
        }

        //: let view = TalkingChatInputView(type: type)
        let view = VillageThen(type: type)
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedView: UIView = {
    lazy var feedView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var feedbackBtn: UIButton = {
    lazy var feedbackBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: let attributes = [.foregroundColor: UIColor(hex: "#128CFF")!, .font: UIFont.pingfangFont(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor(hex: (String(kBaseballName.suffix(7))))!, .font: UIFont.afterDisable(type: .Medium, fontSize: 16), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Feedback".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(appOrdinaryKey.prefix(4)) + "back").localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(feedbackBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(trackClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    lazy var giftEffectView: CandidateAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = CandidateAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: lazy var navView: TalkingChatNavTitleView = {
    lazy var navView: AdviceReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = AdviceReactiveCompatible(type: conversationType)
        //: return navView
        return navView
        //: }()
    }()

    //: lazy var sendGreetBtn: UIButton = {
    lazy var sendGreetBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.afterDisable(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(userAgencyValue.prefix(7)))), for: .normal)
        //: btn.setTitle("Quick Greeting".localized, for: .normal)
        btn.setTitle((String(mainAttitudeStr.suffix(9)) + dream_everybodyName.replacingOccurrences(of: "clearly", with: "in")).localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.fogColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(sendGreetBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(silky), for: .touchUpInside)
        //: btn.isHidden = !AdministratorThen.share.appUserConfigMode.showDirectGreet || AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue || AdministratorThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = !AdministratorThen.share.appUserConfigMode.showDirectGreet || AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue || AdministratorThen.share.appStatus == BecauseMultiplierTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var replyTipsView: TalkingPrivateChatReplyTipsView? = {
    private lazy var replyTipsView: NativeReactiveCompatible? = {
        //: let view = TalkingPrivateChatReplyTipsView.init()
        let view = NativeReactiveCompatible()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    // 文本输入翻译检测
    //: private lazy var selectTranView: TalkingSelectTranslationView = {
    private lazy var selectTranView: AssagaiView = {
        //: let v = TalkingSelectTranslationView()
        let v = AssagaiView()
        //: v.isHidden = true
        v.isHidden = true
        //: v.transBlock = { [weak self] succeed, text in
        v.transBlock = { [weak self] succeed, text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            // 展示翻译结果视图
            //: self.view.endEditing(true)
            self.view.endEditing(true)
            //: self.transResultView.showTranslationResultView(text)
            self.transResultView.counteractionTranslation(text)
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var transResultView: TalkingTranslationResultView = {
    private lazy var transResultView: LimbThen = {
        //: let v = TalkingTranslationResultView()
        let v = LimbThen()
        //: v.isHidden = true
        v.isHidden = true
        //: v.sendBlock = { [weak self] text in
        v.sendBlock = { [weak self] text in
            //: guard let self = self else { return }
            guard let self = self else { return }
            // 发送翻译内容, 清空输入框
            //: self.func__sendTextMsg(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            self.cap(msgStr: text, quoteModel: self.msgInputView.quoteModel)
            //: self.msgInputView.inputTextView.text = ""
            self.msgInputView.inputTextView.text = ""
            //: self.msgInputView.quoteModel = nil
            self.msgInputView.quoteModel = nil
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var beautyPhotosBtn: UIButton = {
    private lazy var beautyPhotosBtn: UIButton = {
        //: let btn = UIButton.init(type: UIButton.ButtonType.custom)
        let btn = UIButton(type: UIButton.ButtonType.custom)
        //: btn.backgroundColor = .white
        btn.backgroundColor = .white
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        btn.titleLabel?.font = UIFont.afterDisable(type: .Medium, fontSize: 15)
        //: btn.setTitleColor(UIColor(hex: "#8370FF"), for: .normal)
        btn.setTitleColor(UIColor(hex: (String(userAgencyValue.prefix(7)))), for: .normal)
        //: btn.setTitle("Beauty Photos".localized, for: .normal)
        btn.setTitle((String(app_amIdent) + String(noti_developIdent.suffix(4)) + "hotos").localized, for: .normal)
        //: btn.layer.cornerRadius = 15
        btn.layer.cornerRadius = 15
        //: btn.layer.borderColor = UIColor.appThemeColor().cgColor
        btn.layer.borderColor = UIColor.fogColor().cgColor
        //: btn.layer.borderWidth = 1
        btn.layer.borderWidth = 1
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        //: btn.addTarget(self, action: #selector(beautyPhotosBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(threeClick), for: .touchUpInside)
        //: btn.isHidden = AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue || AdministratorThen.share.appStatus == AppSkinStatus.special.rawValue
        btn.isHidden = AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue || AdministratorThen.share.appStatus == BecauseMultiplierTarget.special.rawValue
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var gamesView: TalkingLiveRoomGamesView = {
    private lazy var gamesView: MildReactiveCompatible = {
        //: let v = TalkingLiveRoomGamesView()
        let v = MildReactiveCompatible()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var bannerView: TalkingPrivateBannerView = {
    private lazy var bannerView: RetirementDataSource = {
        //: let view = TalkingPrivateBannerView.init()
        let view = RetirementDataSource()
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var floatScreenView: TalkingGameFloatingScreenView = {
    private lazy var floatScreenView: GatherThen = { // 飘屏
        //: let v = TalkingGameFloatingScreenView()
        let v = GatherThen()
        //: v.isHidden = (AdministratorThen.share.appStatus == AppSkinStatus.special.rawValue)
        v.isHidden = (AdministratorThen.share.appStatus == BecauseMultiplierTarget.special.rawValue)
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: v.endPlayBlock = { [weak self] in
        v.endPlayBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: v.isUserInteractionEnabled = false
            v.isUserInteractionEnabled = false
        }
        //: return v
        return v
        //: }()
    }()

    //: private lazy var notesTopView: PrivateChatNotesTopView = {
    private lazy var notesTopView: ApplicationThen = { // 备注置顶
        //: let v = PrivateChatNotesTopView()
        let v = ApplicationThen()
        //: v.tapBlock = { [weak self] in
        v.tapBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.jumpToNotesView()
            self.headCon()
        }
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()
}

// MARK: - banner

//: extension TalkingPrivateChatController: PrivateBannerDelegate {
extension PurchaseViewController: DetectReactiveCompatible {
    //: func changeBannerViewFream(isMiniView: Bool) {
    func foremost(isMiniView: Bool) {
        //: guard self.isFrom == .Normal else { return }
        guard self.isFrom == .Normal else { return }
        //: UIView.animate(withDuration: 0.3) {
        UIView.animate(withDuration: 0.3) {
            //: if isMiniView {
            if isMiniView {
                //: if AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue {
                if AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                    }

                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        //: make.width.equalTo(89)
                        make.width.equalTo(89)
                        //: make.trailing.equalTo(-4)
                        make.trailing.equalTo(-4)
                        //: make.height.equalTo(36)
                        make.height.equalTo(36)
                    }
                }
                //: } else {
            } else {
                //: if AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue {
                if AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: if self.replyTipsView != nil {
                        if self.replyTipsView != nil {
                            //: make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            make.bottom.equalTo(self.replyTipsView!.snp.top).offset(-8)
                            //: } else {
                        } else {
                            //: make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                            make.bottom.equalTo(self.msgInputView.snp.top).offset(-8)
                        }
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                    //: } else {
                } else {
                    //: self.bannerView.snp.remakeConstraints { make in
                    self.bannerView.snp.remakeConstraints { make in
                        //: make.trailing.equalToSuperview()
                        make.trailing.equalToSuperview()
                        //: make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        make.bottom.equalTo(self.beautyPhotosBtn.snp.top)
                        //: make.height.equalTo(106)
                        make.height.equalTo(106)
                        //: make.width.equalTo(93)
                        make.width.equalTo(93)
                    }
                }
            }
            //: self.bannerView.layoutIfNeeded()
            self.bannerView.layoutIfNeeded()
            //: self.bannerView.resetView(isMiniView: isMiniView)
            self.bannerView.manner(isMiniView: isMiniView)
        }
    }
}

// MARK: - 聊天相关

//: extension TalkingPrivateChatController: TMessageControllerDelegate {
extension PurchaseViewController: StudyHypothesisDelegate {
    /// 一键打招呼
    //: @objc func sendGreetBtnClick() {
    @objc func silky() {
        //: TalkingPrivateChatManager.chat_sendQuickGreeting(toUserId: self.targetId) { errorCode, errorMsg, textDict, audioDict in
        PlumpDownReadingManageressChatManager.block(toUserId: self.targetId) { _, errorMsg, textDict, audioDict in
            //: guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
            guard (textDict?.count ?? 0 > 0) || (audioDict?.count ?? 0 > 0) else {
                //: if errorMsg?.count ?? 0 > 0 {
                if errorMsg?.count ?? 0 > 0 {
                    //: self.func__showStatusBarErrorMsg(showMsg: errorMsg!)
                    self.iconThemeBringBackPlace(showMsg: errorMsg!)
                }
                //: return
                return
            }

            // 发送一键打招呼消息
            //: self.func_sendQuickGreetingMsg(textDict: textDict, audioDict: audioDict)
            self.maxThin(textDict: textDict, audioDict: audioDict)
        }
    }

    /// 图片打招呼
    //: @objc func beautyPhotosBtnClick() {
    @objc func threeClick() {
        //: TalkingPrivateChatManager.chat_sendBeautyPhotos(toUserId: self.targetId) { isPushSet, dict in
        PlumpDownReadingManageressChatManager.tarLiveHandler(toUserId: self.targetId) { isPushSet, dict in
            //: if isPushSet {
            if isPushSet {
                //: let vc = TalkingGreetPhotoVC.init()
                let vc = AttemptViewController()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)
                //: return
                return
            }
            //: if dict != nil {
            if dict != nil {
                //: UIImageView().setUrlImage(urlStr: dict?["url"] as? String ?? "") {[weak self] _, image in
                UIImageView().refreshHistory(urlStr: dict?[(String(main_wantMsg))] as? String ?? "") { [weak self] _, image in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: if image != nil {
                    if image != nil {
                        //: let unlockGift = dict?["unlockGift"] as? Dictionary<String, Any>
                        let unlockGift = dict?[(String(dreamColumnName.suffix(8)) + m_donStartPresentationTitle.replacingOccurrences(of: "rite", with: "ft"))] as? [String: Any]
                        //: let sendModel = AbTalkingChatMsgCustomSendModel()
                        let sendModel = ThrowModel()
                        //: sendModel.isBurn = String.init(format: "%d", dict?["isBurn"] as? Int ?? 0)
                        sendModel.isBurn = String(format: "%d", dict?[String(bytes: main_typicalStr.map{lightSpeed(mic: $0)}, encoding: .utf8)!] as? Int ?? 0)
                        //: sendModel.giftId = unlockGift?["giftId"] as? String ?? "0"
                        sendModel.giftId = unlockGift?[(String(mainThanName.suffix(6)))] as? String ?? "0"
                        //: self.func__sendPictureMsg(img: image!, sendModel: sendModel)
                        self.ready(img: image!, sendModel: sendModel)
                        //: return
                        return
                    }
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ReconstructThen! {
    func change(_: MessageViewController!, counterest data: V2TIMMessage!) -> ReconstructThen! {
        //: let dic = ManagerPubliclyThen.parseTXMessageData(data: data.customElem.data)
        let dic = ManagerPubliclyThen.recover(data: data.customElem.data)
        //: var cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        var cellData = AdvocatePatentThen.more(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        //: if data.isKind(of: TalkingChatMatchMsgCellData.self), self.userWrap.uid.count > 0 {
        if data.isKind(of: CompartmentCellData.self), self.userWrap.uid.count > 0 {
            //: cellData?.msgModel.userInfoModel = self.userWrap
            cellData?.msgModel.userInfoModel = self.userWrap
        }

        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: if jsonDic["rYMsgType"].stringValue == "GJ:CallCustom" { // 音视频通话消息
        if jsonDic[(String(noti_lowMessage.suffix(5)) + String(app_modifyDepthDeclineName.suffix(4)))].stringValue == (String(mainFirstUrl) + mainArcMessage.replacingOccurrences(of: "automatically", with: "m")) { // 音视频通话消息
            //: let isSelf = jsonDic["changeDirection"].boolValue
            let isSelf = jsonDic[(String(app_wantContent) + String(noti_patientId))].boolValue
            //: if isSelf == true {
            if isSelf == true { // 消息需要翻转方向
                // 更正当前用户uid
                //: cellData?.msgModel.user.uid = jsonDic["extra"]["msgInfo"]["toUid"].intValue
                cellData?.msgModel.user.uid = jsonDic[(String(user_goldenData))][(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o"))][(String(showCompareIdent.suffix(5)))].intValue
                // 更正消息展示方向
                //: let msgModel = cellData!.msgModel
                let msgModel = cellData!.msgModel
                //: cellData = WarnThen(direction: isSelf ? .MsgDirectionOutgoing:.MsgDirectionIncoming)
                cellData = WarnThen(direction: isSelf ? .MsgDirectionOutgoing : .MsgDirectionIncoming)
                //: cellData?.msgModel = msgModel
                cellData?.msgModel = msgModel
            }
        }
        //: if cellData?.isKind(of: HeapReactiveCompatible.self) == true ||
        if cellData?.isKind(of: HeapReactiveCompatible.self) == true ||
            //: cellData?.isKind(of: GageBubbleCellData.self) == true ||
            cellData?.isKind(of: GageBubbleCellData.self) == true ||
            //: cellData?.isKind(of: WarnThen.self) == true ||
            cellData?.isKind(of: WarnThen.self) == true ||
            //: cellData?.isKind(of: EnableicialCellData.self) == true {
            cellData?.isKind(of: EnableicialCellData.self) == true
        {
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
        }

        //: if let cellData = cellData {
        if let cellData = cellData {
            // 接收解锁 私密消息（照片、视频）礼物，刷新本地消息状态
            //: TalkingPrivateChatManager.chat_onNewMessage_refreshLocalIntimateMsgStatus(cellData: cellData, msgView: self.msgView)
            PlumpDownReadingManageressChatManager.spread(cellData: cellData, msgView: self.msgView)
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(cellData.msgIncome, data.timestamp, msgView)
            PlumpDownReadingManageressChatManager.layer(cellData.msgIncome, data.timestamp, msgView)
        }

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ReconstructThen!) -> AutoloadingReactiveCompatible! {
    func message(_: MessageViewController!, observer data: ReconstructThen!) -> AutoloadingReactiveCompatible! {
        // 更新聊天头像，不会更新腾讯数据库
        //: let data = changeChatHeadPic(data: data)
        let data = humanFace(data: data)

        //: if data.isKind(of: WarnThen.self) {
        if data.isKind(of: WarnThen.self) {
            //: let cell = TalkingChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingChatTextMsgCell.className())
            let cell = ConfirmView(style: .default, reuseIdentifier: ConfirmView.className())
            //: let cellData = data as! WarnThen
            let cellData = data as! WarnThen

            //: if cellData.msgModel.quoteMsgInfo.isQuoteMsg() {
            if cellData.msgModel.quoteMsgInfo.image() {
                //: if cellData.msgModel.quoteMsgInfo.uid == AdministratorThen.share.loginUserMode.userID {
                if cellData.msgModel.quoteMsgInfo.uid == AdministratorThen.share.loginUserMode.userID {
                    //: cellData.msgModel.quoteMsgInfo.sendName = AdministratorThen.share.loginUserMode.nickname ?? ""
                    cellData.msgModel.quoteMsgInfo.sendName = AdministratorThen.share.loginUserMode.nickname ?? ""
                    //: }else{
                } else {
                    //: cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                    cellData.msgModel.quoteMsgInfo.sendName = self.userWrap.nickname
                }
            }
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
//            cell.transBlock = { [weak self] in
//                guard let self = self else { return }
//                self.msgView.translateMsg(with: cellData)
//            }
            //: return cell
            return cell
        }

        //: if data.isKind(of: GageBubbleCellData.self) {
        if data.isKind(of: GageBubbleCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = TapViewCell(style: .default, reuseIdentifier: TapViewCell.className())
            //: let cellData = data as! GageBubbleCellData
            let cellData = data as! GageBubbleCellData
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: EnableicialCellData.self) {
        if data.isKind(of: EnableicialCellData.self) {
            //: let cell = TalkingChatVideoMsgCell.init(style: .default, reuseIdentifier: TalkingChatVideoMsgCell.className())
            let cell = RoundingAgencyViewCell(style: .default, reuseIdentifier: RoundingAgencyViewCell.className())
            //: let cellData = data as! EnableicialCellData
            let cellData = data as! EnableicialCellData
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: FireThen.self) {
        if data.isKind(of: FireThen.self) {
            //: let cell = TalkingChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatGiftMsgCell.className())
            let cell = ReadingReactiveCompatible(style: .default, reuseIdentifier: ReadingReactiveCompatible.className())
            //: let cellData = data as! FireThen
            let cellData = data as! FireThen
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }
        //: if data.isKind(of: LiteralCellData.self) {
        if data.isKind(of: LiteralCellData.self) {
            //: let cell = TalkingChatAskForGiftMsgCell.init(style: .default, reuseIdentifier: TalkingChatAskForGiftMsgCell.className())
            let cell = UnwantedMsgCell(style: .default, reuseIdentifier: UnwantedMsgCell.className())
            //: let cellData = data as! LiteralCellData
            let cellData = data as! LiteralCellData
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: cell.sendBlock = { [weak self] in
            cell.sendBlock = { [weak self] in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: var model = TalkingRoomGiftModel.init()
                var model = ApprovalModelType()
                //: model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                model.gid = cellData.msgModel.msgInfo.wantGift.giftId
                //: self.func__sendGiftMsg(giftModel: model, giftNum: "1", cellData: cellData, fromStr: "wantGift")
                self.permissionAttribute(giftModel: model, giftNum: "1", cellData: cellData, fromStr: (String(mainWordStr)))
            }
            //: return cell
            return cell
        }
        //: if data.isKind(of: HeapReactiveCompatible.self) {
        if data.isKind(of: HeapReactiveCompatible.self) {
            //: let cell = TalkingChatAudioMsgCell.init(style: .default, reuseIdentifier: TalkingChatAudioMsgCell.className())
            let cell = MovieMsgCell(style: .default, reuseIdentifier: MovieMsgCell.className())
            //: let cellData = data as! HeapReactiveCompatible
            let cellData = data as! HeapReactiveCompatible
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatMatchMsgCellData.self) {
        if data.isKind(of: CompartmentCellData.self) {
            //: let cell = TalkingChatMatchMsgCell.init(style: .default, reuseIdentifier: TalkingChatMatchMsgCell.className())
            let cell = ReadingViewCell(style: .default, reuseIdentifier: ReadingViewCell.className())
            //: let cellData = data as! TalkingChatMatchMsgCellData
            let cellData = data as! CompartmentCellData
            //: if self.userWrap.uid.count > 0 {
            if self.userWrap.uid.count > 0 {
                //: cellData.msgModel.userInfoModel = self.userWrap
                cellData.msgModel.userInfoModel = self.userWrap
            }
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: DataGatherThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ExtentView(style: .default, reuseIdentifier: ExtentView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! DataGatherThen
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatSystemNotifJumpMsgCellData.self) {
        if data.isKind(of: SampleAudioSystemCellData.self) {
            //: let cell = TalkingChatSystemNotifJumpMsgCell.init(style: .default, reuseIdentifier: TalkingChatSystemNotifJumpMsgCell.className())
            let cell = GatherReactiveCompatible(style: .default, reuseIdentifier: GatherReactiveCompatible.className())
            //: let cellData = data as! TalkingChatSystemNotifJumpMsgCellData
            let cellData = data as! SampleAudioSystemCellData
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: AutoloadingReactiveCompatible!) {
    func image(_: MessageViewController!, up cell: AutoloadingReactiveCompatible!) {
        //: guard self.isFrom != .LiveRoom,
        guard self.isFrom != .LiveRoom,
              //: conversationType != .system,
              conversationType != .system,
              //: String(cell.messageData?.msgModel.user.uid ?? 0) != CreditsThen.getCustomerServiceID() else { return }
              String(cell.messageData?.msgModel.user.uid ?? 0) != CreditsThen.tingId() else { return }

        //: if let uid = cell.messageData?.msgModel.user.uid {
        if let uid = cell.messageData?.msgModel.user.uid {
            //: DumpPushManager.share.func__pushToUserDetailVC(uid: String(uid))
            DumpPushManager.share.belowStreet(uid: String(uid))
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: AutoloadingReactiveCompatible!) {
    func orientationOrigin(_: MessageViewController!, makeup cell: AutoloadingReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TapViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! GageBubbleCellData
            let imageCellData = cell.messageData as! GageBubbleCellData
            //: if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 {
            if imageCellData.msgModel.msgInfo.intimatePhoto.previewUrl.count > 0 { // 是私密照片
                // 已销毁的图片不能点击
                //: guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                guard imageCellData.msgModel.msgInfo.intimatePhoto.burnStatus != 1 else { return }
                // 展示私密照片视图
                //: let vc = TalkingIntimatePhotoPreviewVC(imageCellData: imageCellData, toUid: self.targetId)
                let vc = CountimateTwo(imageCellData: imageCellData, toUid: self.targetId)
                // 更改本地私密照片状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimatePhotoMsg(imageCellData, photoModel: model)
                    self.msgView.off(imageCellData, billOfFareModel: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.beforeEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: appDepthGuideWarnValue)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.bringView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.history(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                thenController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 普通图片
                //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
                let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!

                //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
                var filePath = (libraryPath as NSString).appendingPathComponent(dream_acceptFormat)
                //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
                filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

                //: var model = TalkingMomentPhotoModel.init()
                var model = ExactlyHandyJSON()
                //: if FileManager.default.fileExists(atPath: filePath) {
                if FileManager.default.fileExists(atPath: filePath) {
                    //: let data = NSData.init(contentsOfFile: filePath)
                    let data = NSData(contentsOfFile: filePath)
                    //: model.imagePic = UIImage(data: data! as Data)
                    model.imagePic = UIImage(data: data! as Data)
                    //: } else {
                } else {
                    //: if imageCellData.imageData != nil {
                    if imageCellData.imageData != nil {
                        //: model.imagePic = imageCellData.imageData
                        model.imagePic = imageCellData.imageData
                        //: } else {
                    } else {
                        //: model.imgUrl =  imageCellData.imageUrl
                        model.imgUrl = imageCellData.imageUrl
                    }
                }
                //: var currenModel = TalkingMomentModel.init()
                var currenModel = LogHandyJSON()
                //: var tempArr: [TalkingMomentPhotoModel] = []
                var tempArr: [ExactlyHandyJSON] = []
                //: tempArr.append(model)
                tempArr.append(model)
                //: currenModel.pic = tempArr
                currenModel.pic = tempArr
                //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
                let vc = HaveViewController(momentModel: currenModel, index: 0, type: .normal)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                thenController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: RoundingAgencyViewCell.self) { // 视频
            //: let videoCellData = cell.messageData as! EnableicialCellData
            let videoCellData = cell.messageData as! EnableicialCellData
            //: let videoModel = videoCellData.msgModel.msgInfo.video
            let videoModel = videoCellData.msgModel.msgInfo.video
            //: if (videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0") { // 普通视频
            if videoModel.burnStatus == -1 && videoModel.lockStatus == -1 && videoModel.giftId == "0" { // 普通视频
                // 优先使用本地视频，没有再使用网络
                //: var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                var videoPath = videoCellData.msgModel.msgInfo.customSend.localVideoUrl
                //: if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                if videoPath.count == 0 || !FileManager.default.fileExists(atPath: videoPath) {
                    //: videoPath = videoModel.videoUrl
                    videoPath = videoModel.videoUrl
                }

                //: let vc = TalkingMomentVideoVC.init(videoPath: videoPath)
                let vc = InMigrationRecognizerDelegate(videoPath: videoPath)
                //: getNavigationController()?.pushViewController(vc, animated: true)
                thenController()?.pushViewController(vc, animated: true)

                //: } else {
            } else { // 私密视频
                // 已销毁的视频不能点击
                //: guard videoModel.burnStatus != 1 else { return }
                guard videoModel.burnStatus != 1 else { return }
                // 预览私密视频
                //: let vc = TalkingIntimateVideoPreviewVC(videoCellData: videoCellData, toUid: self.targetId)
                let vc = RacePlayerDelegate(videoCellData: videoCellData, toUid: self.targetId)
                // 更改本地私密视频状态
                //: vc.statusBlock = { (model) in
                vc.statusBlock = { model in
                    //: self.msgView.changeIntimateVideoMsg(videoCellData, videoModel: model)
                    self.msgView.notBuild(videoCellData, wall: model)
                }
                // 送礼效果在当前页面处理
                //: vc.sendGiftResultBlock = { succeed, result, errorModel in
                vc.sendGiftResultBlock = { succeed, result, errorModel in
                    //: guard succeed else {
                    guard succeed else {
                        //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickUnlockButton)
                        self.beforeEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: appDepthGuideWarnValue)
                        //: return
                        return
                    }

                    //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: false)
                    self.bringView(extral: result as! [String: Any?], isFromInputView: false)
                    //: let dataDict = result as! Dictionary<String, Any>
                    let dataDict = result as! [String: Any]
                    //: self.unlockPhotoOrVideoTool(dataDict: dataDict)
                    self.history(dataDict: dataDict)
                }
                //: getNavigationController()?.pushViewController(vc, animated: true)
                thenController()?.pushViewController(vc, animated: true)
            }

            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: MovieMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! MovieMsgCell
            //: func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: audioCell.cellData as! HeapReactiveCompatible, cellView: audioCell)
            priorityInView(cellData: audioCell.cellData as! HeapReactiveCompatible, cellView: audioCell)

            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ConfirmView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ConfirmView
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = ManagerPubliclyThen.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = ManagerPubliclyThen.recover(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(user_goldenData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(user_epicId.suffix(7)))) {
                //: let truePersonAuth = extraDic["jumpKey"] as? String ?? ""
                let truePersonAuth = extraDic[(String(user_epicId.suffix(7)))] as? String ?? ""
                //: if truePersonAuth == "truePersonAuth" {
                if truePersonAuth == (String(app_othersText.prefix(6)) + mainBuryMergeData.replacingOccurrences(of: "title", with: "s") + "Auth") {
                    //: DumpPushManager.share.func__pushUserVerifyController(toast: nil)
                    DumpPushManager.share.supra(toast: nil)
                    //: return
                    return
                }
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<WhistleTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: DumpPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    DumpPushManager.share.molarityEconomicTheoryCommend(jumpModel: jumpModel)
                }
            }

            //: let extraJson = JSON(extraDic)
            let extraJson = JSON(extraDic)
            //: if  dic["rYMsgType"] as? String == "GJ:CallCustom" {  // 音视频通话消息
            if dic[(String(noti_lowMessage.suffix(5)) + String(app_modifyDepthDeclineName.suffix(4)))] as? String == (String(mainFirstUrl) + mainArcMessage.replacingOccurrences(of: "automatically", with: "m")) { // 音视频通话消息
                //: if extraJson["msgInfo"]["logType"].intValue == 1 {
                if extraJson[(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o"))][(String(mExistSlidePath))].intValue == 1 {
                    //: checkAndCallVoice()
                    haveWant()
                    //: } else {
                } else {
                    //: checkAndCallVideo()
                    godspeed()
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ExtentView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ExtentView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = ManagerPubliclyThen.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = ManagerPubliclyThen.recover(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(user_goldenData))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains((String(user_epicId.suffix(7)))) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<WhistleTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: DumpPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    DumpPushManager.share.molarityEconomicTheoryCommend(jumpModel: jumpModel)
                }
            }
            //: } else if cell.isKind(of: TalkingChatMatchMsgCell.self) {
        } else if cell.isKind(of: ReadingViewCell.self) {
            //: DumpPushManager.share.func__pushToUserDetailVC(uid: targetId)
            DumpPushManager.share.belowStreet(uid: targetId)
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: AutoloadingReactiveCompatible!) {
    func identity(_: MessageViewController!, loadController cell: AutoloadingReactiveCompatible!) {
        //: var cellData: TalkingChatMsgBaseCellData?
        var cellData: MarkCellData?
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TapViewCell.self) {
            //: let imageCell = cell as! TalkingChatImageMsgCell
            let imageCell = cell as! TapViewCell
            //: cellData = imageCell.cellData as! GageBubbleCellData
            cellData = imageCell.cellData as! GageBubbleCellData
            //: } else if cell.isKind(of: TalkingChatVideoMsgCell.self) {
        } else if cell.isKind(of: RoundingAgencyViewCell.self) {
            //: let videoCell = cell as! TalkingChatVideoMsgCell
            let videoCell = cell as! RoundingAgencyViewCell
            //: cellData = videoCell.cellData as! EnableicialCellData
            cellData = videoCell.cellData as! EnableicialCellData
            //: } else if cell.isKind(of: TalkingChatAudioMsgCell.self) {
        } else if cell.isKind(of: MovieMsgCell.self) {
            //: let audioCell = cell as! TalkingChatAudioMsgCell
            let audioCell = cell as! MovieMsgCell
            //: cellData = audioCell.cellData as! HeapReactiveCompatible
            cellData = audioCell.cellData as! HeapReactiveCompatible
            //: } else if cell.isKind(of: TalkingChatTextMsgCell.self) {
        } else if cell.isKind(of: ConfirmView.self) {
            //: let textCell = cell as! TalkingChatTextMsgCell
            let textCell = cell as! ConfirmView
            //: cellData = textCell.cellData as! WarnThen
            cellData = textCell.cellData as! WarnThen
        }
        //: if cellData != nil {
        if cellData != nil {
            //: let reCellData: ReconstructThen? = TUISocialChatManager.handleTXMessageReSend(withData: cellData!)
            let reCellData: ReconstructThen? = AdvocatePatentThen.windowWall(withData: cellData!)
            //: if reCellData != nil {
            if reCellData != nil {
                //: cell.messageData?.msgModel = reCellData!.msgModel
                cell.messageData?.msgModel = reCellData!.msgModel
                //: cell.messageData?.status = .Msg_Status_Fail
                cell.messageData?.status = .Msg_Status_Fail
                //: if reCellData!.isKind(of: EnableicialCellData.self) {
                if reCellData!.isKind(of: EnableicialCellData.self) { // 视频重发
                    //: func__sendVideoMsg(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    model(msgData: cell.messageData, sendModel: cell.messageData!.msgModel.msgInfo.customSend)
                    //: } else {
                } else {
                    //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
                    funcSend(msgData: cell.messageData!, reSend: true)
                }
            }
        }
    }

    /// 回复消息
    //: func messageController(_ controller: TUIMessageController!, onReplyMessage cellData: ReconstructThen!) {
    func gesture(_: MessageViewController!, device cellData: ReconstructThen!) {
        //: msgInputView.quoteModel = TalkingPrivateChatManager.handleReplyMsg(cellData: cellData, targetId: userWrap.uid)
        msgInputView.quoteModel = PlumpDownReadingManageressChatManager.material(cellData: cellData, targetId: userWrap.uid)
    }

    /// 点击“引用”消息
    //: func messageController(_ controller: TUIMessageController!, onClickReplyMessage cellData: ReconstructThen!) {
    func gift(_: MessageViewController!, click cellData: ReconstructThen!) {
        //: TalkingPrivateChatManager.handleClickReplyMsg(cellData: cellData, targetId: userWrap.uid)
        PlumpDownReadingManageressChatManager.player(cellData: cellData, targetId: userWrap.uid)
    }

    /// 实时更新聊天双方头像
    /// - Parameter data: 腾讯数据
    /// - Returns: 腾讯数据
    //: private func changeChatHeadPic(data: ReconstructThen) -> ReconstructThen {
    private func humanFace(data: ReconstructThen) -> ReconstructThen {
        //: let isSelf = data.msgModel.user.uid == Int(AdministratorThen.share.loginUserMode.userID)
        let isSelf = data.msgModel.user.uid == Int(AdministratorThen.share.loginUserMode.userID)
        //: if isSelf {
        if isSelf {
            //: if AdministratorThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
            if AdministratorThen.share.loginUserMode.headPic?.count ?? 0 > 0 {
                //: data.msgModel.user.headPic = AdministratorThen.share.loginUserMode.headPic!
                data.msgModel.user.headPic = AdministratorThen.share.loginUserMode.headPic!
                //: data.msgModel.user.headPicFrame = AdministratorThen.share.loginUserMode.headPicFrame
                data.msgModel.user.headPicFrame = AdministratorThen.share.loginUserMode.headPicFrame
            }
            //: } else {
        } else {
            //: if self.userWrap.headPic.count > 0 {
            if self.userWrap.headPic.count > 0 {
                //: data.msgModel.user.headPic = self.userWrap.headPic
                data.msgModel.user.headPic = self.userWrap.headPic
                //: data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
                data.msgModel.user.headPicFrame = self.userWrap.headPicFrame
            }
        }
        //: return data
        return data
    }
}

// MARK: - 发送消息

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 发送快速打招呼消息
    //: func func_sendQuickGreetingMsg() {
    func diskQuote() {
        //: msgView.targetID = self.targetId
        msgView.targetID = self.targetId
        //: let msgInfo = ["msgType": "txt"]
        let msgInfo = [String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (String(appRequireTightValue))]
        //: let paramsDic = ["type": "QuickMsg"]
        let paramsDic = [(dream_lanceData.replacingOccurrences(of: "punish", with: "t")): (String(appCurNearFormat.suffix(8)))]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo, String(bytes: m_behindSecondFormat.map{importantOning(still: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
            //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
            self.funcSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送一键打招呼消息
    /// - Parameters:
    ///   - textDict: 文本消息
    ///   - audioDict: 语音消息
    //: private func func_sendQuickGreetingMsg(textDict: [String: Any]?, audioDict: [String: Any]?) {
    private func maxThin(textDict: [String: Any]?, audioDict: [String: Any]?) {
        //: if textDict?.count ?? 0 > 0 {
        if textDict?.count ?? 0 > 0 { // 发送文本消息
            //: let content = textDict?["content"]
            let content = textDict?[String(bytes: k_sinkData.map{basicWhite(calculation: $0)}, encoding: .utf8)!]
            //: let greetId = textDict?["id"]
            let greetId = textDict?["id"]
            //: let msgInfo = ["content": content, "txtId": greetId, "msgType": "txt"]
            let msgInfo = [String(bytes: k_sinkData.map{basicWhite(calculation: $0)}, encoding: .utf8)!: content, (String(userStepPath.prefix(5))): greetId, String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (String(appRequireTightValue))]
            //: let paramsDic = ["message": content, "txtId": greetId, "type": "TxtMsg"]
            let paramsDic = [String(bytes: noti_boaBindFormat.reversed(), encoding: .utf8)!: content, (String(userStepPath.prefix(5))): greetId, (dream_lanceData.replacingOccurrences(of: "punish", with: "t")): (String(k_wholeId))]
            //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
            let dic = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo, String(bytes: m_behindSecondFormat.map{importantOning(still: $0)}, encoding: .utf8)!: paramsDic]
            //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
            if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
                //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                self.funcSend(msgData: cellData, reSend: false)
            }
        }

        //: if audioDict?.count ?? 0 > 0 {
        if audioDict?.count ?? 0 > 0 { // 发送语音消息
            //: VoiceDownloadTaskManager.shared.addAudioGreetDownLoadTask(audioDict!) { model in
            SprechgesangObjectProtocol.shared.publishHandle(audioDict!) { model in
                //: if model.taskTypeId != .oneKeyGreet {
                if model.taskTypeId != .oneKeyGreet {
                    //: return
                    return
                }
                //: if model.taskStatus != .finish {
                if model.taskStatus != .finish {
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.func__showStatusBarErrorMsg(showMsg: "Voice hello download failed. Procedure")
                        self.iconThemeBringBackPlace(showMsg: String(bytes: kDetermineStr.map{houseEffect(when: $0)}, encoding: .utf8)!)
                        //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                        PlumpDownReadingManageressChatManager.nearRate(toUserId: self.targetId, isHave: false)
                    }
                    //: return
                    return
                }
                //: let greetId = audioDict?["id"]
                let greetId = audioDict?["id"]
                //: let length = audioDict?["length"]
                let length = audioDict?[(String(appParentMsg))]
                //: let msgInfo = ["msgType": "audio",
                let msgInfo = [String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (String(app_identityMessage)),
                               //: "audioId": greetId,
                               (String(kSendUrl) + String(dreamSometimeId)): greetId,
                               //: "audioLength": length,
                               String(bytes: mainEndMsg.map{slimImplement(anyone: $0)}, encoding: .utf8)!: length,
                               //: "audioSandbox": model.unZipFilePath,
                               (String(mWireFormat.prefix(9)) + String(noti_lensMsg)): model.unZipFilePath,
                               //: "senderId": AdministratorThen.share.loginUserMode.userID]
                               String(bytes: mCaptainBlankId.map{enhancePost(audience: $0)}, encoding: .utf8)!: AdministratorThen.share.loginUserMode.userID]

                //: let paramsDic = ["audioId": greetId,
                let paramsDic = [(String(kSendUrl) + String(dreamSometimeId)): greetId,
                                 //: "audioLength": length,
                                 String(bytes: mainEndMsg.map{slimImplement(anyone: $0)}, encoding: .utf8)!: length,
                                 //: "audioSandbox": model.unZipFilePath,
                                 (String(mWireFormat.prefix(9)) + String(noti_lensMsg)): model.unZipFilePath,
                                 //: "type": "AudioMsg"]
                                 (dream_lanceData.replacingOccurrences(of: "punish", with: "t")): (String(m_methodDominantUrl.prefix(3)) + String(notiShirtMsg))]
                //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
                let dic = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo, String(bytes: m_behindSecondFormat.map{importantOning(still: $0)}, encoding: .utf8)!: paramsDic]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
                    //: self.func__sendPriveteMsg(msgData: cellData, reSend: false)
                    self.funcSend(msgData: cellData, reSend: false)
                }
            }
        }
    }

    /// 发送文本消息
    /// - Parameter msgStr: 文本
    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func cap(msgStr: String, quoteModel: PreferConversationModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            iconThemeBringBackPlace(showMsg: (String(dream_constantMessage) + String(dreamPepUrl) + String(mProfessionalPath.prefix(5)) + "nk me" + appLayParticipantUrl.lowercased()).localized)
            //: return
            return
        }

        //: let msgInfo = ["content": temStr, "msgType": "txt"]
        let msgInfo = [String(bytes: k_sinkData.map{basicWhite(calculation: $0)}, encoding: .utf8)!: temStr, String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (String(appRequireTightValue))]
        //: var paramsDic = ["message": temStr, "type": "TxtMsg"]
        var paramsDic = [String(bytes: noti_boaBindFormat.reversed(), encoding: .utf8)!: temStr, (dream_lanceData.replacingOccurrences(of: "punish", with: "t")): (String(k_wholeId))]
        //: var dic:[String: Any] = ["msgInfo": msgInfo]
        var dic: [String: Any] = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo]

        //: if let tempQuoteModel = quoteModel{
        if let tempQuoteModel = quoteModel {
            //: paramsDic["quoteMsgId"] = tempQuoteModel.msgId
            paramsDic[(mClothesPlanData.replacingOccurrences(of: "prepare", with: "qu") + String(main_digitalText.prefix(8)))] = tempQuoteModel.msgId
            //: dic["quoteMsgInfo"] = tempQuoteModel.toDictionary()
            dic[(String(app_argumentKey) + String(noti_takeData.prefix(7)))] = tempQuoteModel.toDictionary()
        }
        //: dic["params"] = paramsDic
        dic[String(bytes: m_behindSecondFormat.map{importantOning(still: $0)}, encoding: .utf8)!] = paramsDic

        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
            //: cellData.msgModel.quoteMsgInfo = quoteModel ?? AbTalkingChatMsgQuoteModel()
            cellData.msgModel.quoteMsgInfo = quoteModel ?? PreferConversationModel()
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            funcSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameters:
    ///   - img: 图片
    ///   - sendModel: 本地数据模型
    //: private func func__sendPictureMsg(img: UIImage, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func ready(img: UIImage, sendModel: ThrowModel) {
        //: let imagePath = GageBubbleCellData.getChatImagePath(image: img)
        let imagePath = GageBubbleCellData.courseOfActionChatAuditoryImageGetVisualisation(image: img)
        // 缓存本地的消息
        //: let sendDict = sendModel.toDictionary() ?? [:]
        let sendDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath, "customSend": sendDict]
        let msgInfo: [String: Any] = [String(bytes: m_wallFormat.map{sampleView(above: $0)}, encoding: .utf8)!: (String(dream_importantValue)), String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (dream_providerTitle.lowercased()), String(bytes: main_siteName.map{alongPrice(tin: $0)}, encoding: .utf8)!: imagePath, String(bytes: showStrikeKey.map{fieldAbove(minor: $0)}, encoding: .utf8)!: sendDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(dream_differenceMessage.replacingOccurrences(of: "lifestyle", with: "p")): imagePath,
                                        //: "type": "ImgMsg",
                                        (dream_lanceData.replacingOccurrences(of: "punish", with: "t")): (String(dream_importantValue)),
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: main_typicalStr.map{lightSpeed(mic: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(mainThanName.suffix(6))): sendModel.giftId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo, String(bytes: m_behindSecondFormat.map{importantOning(still: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            funcSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送视频消息
    /// - Parameters:
    ///   - msgData: 发送视频cellData；nil：上传流程，有值 ：发送流程
    ///   - sendModel: 视频消息自定义本地模型
    //: private func func__sendVideoMsg(msgData: ReconstructThen?, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func model(msgData: ReconstructThen?, sendModel: ThrowModel) {
        // 缓存本地的消息
        //: let videoDict = sendModel.toDictionary() ?? [:]
        let videoDict = sendModel.toDictionary() ?? [:]
        //: let msgInfo: [String: Any] = ["contentType": "VideoMsg", "msgType": "video", "customSend": videoDict]
        let msgInfo: [String: Any] = [String(bytes: m_wallFormat.map{sampleView(above: $0)}, encoding: .utf8)!: (String(main_taText.suffix(3)) + String(dreamNumberroStr)), String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (dreamFiniteId.replacingOccurrences(of: "daily", with: "i")), String(bytes: showStrikeKey.map{fieldAbove(minor: $0)}, encoding: .utf8)!: videoDict]
        // 接口参数
        //: let paramsDic: [String: Any] = ["type": "VideoMsg",
        let paramsDic: [String: Any] = [(dream_lanceData.replacingOccurrences(of: "punish", with: "t")): (String(main_taText.suffix(3)) + String(dreamNumberroStr)),
                                        //: "coverImg": sendModel.coverImg,
                                        String(bytes: m_instanceUrl.map{totalGenerate(prompt: $0)}, encoding: .utf8)!: sendModel.coverImg,
                                        //: "videoUrl": sendModel.videoUrl,
                                        String(bytes: dreamBrowIdent.map{decideLeaf(ready: $0)}, encoding: .utf8)!: sendModel.videoUrl,
                                        //: "duration": sendModel.duration,
                                        String(bytes: showBeforeUrl.map{shrinkPublish(fruit: $0)}, encoding: .utf8)!: sendModel.duration,
                                        //: "isBurn": sendModel.isBurn,
                                        String(bytes: main_typicalStr.map{lightSpeed(mic: $0)}, encoding: .utf8)!: sendModel.isBurn,
                                        //: "giftId": sendModel.giftId]
                                        (String(mainThanName.suffix(6))): sendModel.giftId]
        //: let dict = ["msgInfo": msgInfo, "params": paramsDic]
        let dict = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo, String(bytes: m_behindSecondFormat.map{importantOning(still: $0)}, encoding: .utf8)!: paramsDic]

        // 压缩成功 ---> 创建cellData ---> 上传视频
        //: guard let cellData = msgData else {
        guard let cellData = msgData else {
            //: if let uploadCellData = TUISocialChatManager.handleTXMessageSend(withExtral: dict, isSender: true, isTip: false) {
            if let uploadCellData = AdvocatePatentThen.likeRed(withExtral: dict, isSender: true, isTip: false) {
                //: self.msgView.sendCustomMessage(uploadCellData)
                self.msgView.fill(uploadCellData)
                //: self.cos_UploadVideo(msgData: uploadCellData, sendModel: sendModel)
                self.noWeight(msgData: uploadCellData, sendModel: sendModel)
            }
            //: return
            return
        }

        // 失败重试，展示加载动画
        //: if cellData.status != .Msg_Status_Sending_2 {
        if cellData.status != .Msg_Status_Sending_2 {
            self.msgView.afterDistance(cellData, narrow: .Msg_Status_Sending_2)
        }

        // 失败重试上传视频流程
        //: guard sendModel.videoUrl.count > 0 else {
        guard sendModel.videoUrl.count > 0 else {
            //: self.cos_UploadVideo(msgData: cellData, sendModel: sendModel)
            self.noWeight(msgData: cellData, sendModel: sendModel)
            //: return
            return
        }

        // 修改本地消息数据，调用发送视频接口
        //: self.msgView.modifyCustomMessageData(withExtral: dict, cellData: cellData)
        self.msgView.nearStreetwise(dict, place: cellData)
        //: func__sendPriveteMsg(msgData: cellData, reSend: false)
        funcSend(msgData: cellData, reSend: false)
    }

    /// 发送语音
    /// - Parameters:
    ///   - voicePath: 语音路径
    ///   - voiceTime: 时间
    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {
    func requestBy(voicePath: String, voiceTime: Int) {
        //: let msgInfo = ["msgType": "audio",
        let msgInfo = [String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (String(app_identityMessage)),
                       //: "audioLength": "\(voiceTime)",
                       String(bytes: mainEndMsg.map{slimImplement(anyone: $0)}, encoding: .utf8)!: "\(voiceTime)",
                       //: "audioSandbox": voicePath,
                       (String(mWireFormat.prefix(9)) + String(noti_lensMsg)): voicePath,
                       //: "senderId": AdministratorThen.share.loginUserMode.userID]
                       String(bytes: mCaptainBlankId.map{enhancePost(audience: $0)}, encoding: .utf8)!: AdministratorThen.share.loginUserMode.userID]

        //: let paramsDic = ["audioSandbox": voicePath,
        let paramsDic = [(String(mWireFormat.prefix(9)) + String(noti_lensMsg)): voicePath,
                         //: "type": "AudioMsg",
                         (dream_lanceData.replacingOccurrences(of: "punish", with: "t")): (String(m_methodDominantUrl.prefix(3)) + String(notiShirtMsg)),
                         //: "len": "\(voiceTime)"]
                         String(bytes: showNumbererruptStr.map{reloadThanks(same: $0)}, encoding: .utf8)!: "\(voiceTime)"]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo, String(bytes: m_behindSecondFormat.map{importantOning(still: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            funcSend(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ReconstructThen, reSend: Bool) {
    private func funcSend(msgData: ReconstructThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            iconThemeBringBackPlace(showMsg: dream_micData)
            //: return
            return
        }

        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
        //: params["toUid"] = self.targetId
        params[(String(showCompareIdent.suffix(5)))] = self.targetId

        // 发送视频消息，索要礼物消息，快速打招呼消息，不在此处插入
        //: if msgData.isKind(of: EnableicialCellData.self) == false && msgData.isKind(of: LiteralCellData.self) == false && params["type"] as? String != "QuickMsg" {
        if msgData.isKind(of: EnableicialCellData.self) == false, msgData.isKind(of: LiteralCellData.self) == false, params[(dream_lanceData.replacingOccurrences(of: "punish", with: "t"))] as? String != (String(appCurNearFormat.suffix(8))) {
            //: self.msgView.sendCustomMessage(msgData)
            self.msgView.fill(msgData)
        }
        //: let recordTime = ManagerPubliclyThen.getMessageInsertTime()
        let recordTime = ManagerPubliclyThen.joinOrientation()

        //: if params.keys .contains("pic") { // 图片
        if params.keys.contains((dream_differenceMessage.replacingOccurrences(of: "lifestyle", with: "p"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(dream_differenceMessage.replacingOccurrences(of: "lifestyle", with: "p"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(dream_differenceMessage.replacingOccurrences(of: "lifestyle", with: "p"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }
        //: let type  = params["type"] as! String
        let type = params[(dream_lanceData.replacingOccurrences(of: "punish", with: "t"))] as! String
        //: if type == "AudioMsg" { // 语音
        if type == (String(m_methodDominantUrl.prefix(3)) + String(notiShirtMsg)) { // 语音
            //: if params.keys.contains("audioSandbox") {
            if params.keys.contains((String(mWireFormat.prefix(9)) + String(noti_lensMsg))) {
                //: var path =  params["audioSandbox"] as! String
                var path = params[(String(mWireFormat.prefix(9)) + String(noti_lensMsg))] as! String
                //: params.removeValue(forKey: "audioSandbox")
                params.removeValue(forKey: (String(mWireFormat.prefix(9)) + String(noti_lensMsg)))

                //: let theFileName = (path as NSString).lastPathComponent
                let theFileName = (path as NSString).lastPathComponent

                //: if path.contains("Documents/User/voice/") {
                if path.contains((String(app_foreIdent.suffix(9)) + String(dreamWavePath.prefix(5)) + "/voice/")) {
                    //: path = Talking_Chat_voice_SandboxPath + theFileName
                    path = user_productionStr + theFileName
                    //: } else if(path.contains("Documents/User/record/")) {
                } else if path.contains((String(show_joinId.suffix(5)) + "ents" + String(noti_recordData.suffix(4)) + String(app_onUrl.prefix(5)) + "ord/")) {
                    //: path = Talking_Chat_record_SandboxPath + theFileName
                    path = k_backgroundIdent + theFileName
                }

                //: let audioId = params["audioId"] ?? "0"
                let audioId = params[(String(kSendUrl) + String(dreamSometimeId))] ?? "0"
                //: if  (audioId as! NSString).integerValue == 0 {
                if (audioId as! NSString).integerValue == 0 {
                    //: do {
                    do {
                        //: let voiceData = try Data.init(contentsOf: URL.init(fileURLWithPath: path))
                        let voiceData = try Data(contentsOf: URL(fileURLWithPath: path))
                        //: params["audio"] = voiceData
                        params[(String(app_identityMessage))] = voiceData
                        //: } catch {
                    } catch {}
                }
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendPrivete(param: params, completion: { succeed, result, errorModel in
        ShrinkReactiveCompatible.flagEnable(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: if type == "QuickMsg" {
                if type == (String(appCurNearFormat.suffix(8))) {
                    //: self.QuickEndBlock?(false)
                    self.QuickEndBlock?(false)
                }
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.lastAll(msgData, barMouth: nil, frameChange: NSNumber(value: 0), exposureProcess: .Msg_Status_Fail)
                                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickMessagesendButton)
                self.beforeEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_displayMsg)
                //: if params.keys.contains("txtId") {
                if params.keys.contains((String(userStepPath.prefix(5)))) {
                    //: TalkingPrivateChatManager.chat_changeSaveGreetData(toUserId: self.targetId, isHave: false)
                    PlumpDownReadingManageressChatManager.nearRate(toUserId: self.targetId, isHave: false)
                }
                /// 打招呼达到设定次数失败时
                //: if errorModel!.errorCode == 10015 {
                if errorModel!.errorCode == 10015 {
                    //: self.msgView.deleteCustomMessage(msgData)
                    self.msgView.removeScreen(msgData)
                }
                //: return
                return
            }
            //: var updateMsgData = msgData
            var updateMsgData = msgData
            //: let dataDict = result as! Dictionary<String, Any>
            let dataDict = result as! [String: Any]
            //: var msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
            var msgInfo = dataDict[(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o"))] as! [String: Any]

            //: var msgIncome: NSNumber = NSNumber.init(value: 0)
            var msgIncome: NSNumber = .init(value: 0)
            //: if msgInfo.keys.contains("msgIncome") {
            if msgInfo.keys.contains((String(kMeetName.prefix(9)))) {
                //: msgIncome = msgInfo["msgIncome"] as! NSNumber
                msgIncome = msgInfo[(String(kMeetName.prefix(9)))] as! NSNumber
            }

            // 直播时回复消息，积分更改
            //: if TalkingLiveManager.shared().isLive && msgIncome.floatValue > 0 {
            if TapBecauseLiveManager.resolutionShared().isLive && msgIncome.floatValue > 0 {
                //: let mfBean = Double(truncating: msgIncome)
                let mfBean = Double(truncating: msgIncome)
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean])
                NotificationCenter.default.post(name: mainScaleText, object: nil, userInfo: [String(bytes: noti_alwaysMsg.map{$0^54}, encoding: .utf8)!: mfBean])
            }
            /// 亲密度
            //: self.sendMsgIntimateTool(msgInfo: msgInfo)
            self.implement(msgInfo: msgInfo)

            /// 索要礼物，快速打招呼
            //: let messageType = msgInfo["messageType"] as? Int
            let messageType = msgInfo[(String(main_gainNumberroTitle))] as? Int
            //: if messageType == 25 || type == "QuickMsg" {
            if messageType == 25 || type == (String(appCurNearFormat.suffix(8))) {
                //: if messageType == 25 {
                if messageType == 25 {
                    //: msgInfo["msgType"] = "WantGift"
                    msgInfo[String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!] = (String(show_optimisticStr.prefix(7)) + String(user_heDeliveryMessage))
                    //: uploadRecord.uploadRecordEvent(eventID: clickMessageDetailGiftPopAskBtnSusType)
                    k_hugeName.hour(eventID: mTotalText)
                    //: } else if type == "QuickMsg" {
                } else if type == (String(appCurNearFormat.suffix(8))) {
                    //: msgInfo["msgType"] = "txt"
                    msgInfo[String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!] = (String(appRequireTightValue))
                    //: self.QuickEndBlock?(true)
                    self.QuickEndBlock?(true)
                }
                //: let dic = ["msgInfo": msgInfo]
                let dic = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo]
                //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
                if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
                    //: updateMsgData = cellData
                    updateMsgData = cellData
                    //: self.msgView.sendCustomMessage(updateMsgData)
                    self.msgView.fill(updateMsgData)
                }
            }

            // 更新提示文案
            //: self.userWrap.existSess = true
            self.userWrap.existSess = true
            //: let key =  "\(AdministratorThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
            let key = "\(AdministratorThen.share.loginUserMode.userID)" + (String(app_holdIdent.prefix(4)) + "vateC" + String(app_fieldPath.prefix(5)) + String(showJoinAgoText.prefix(4)) + "tle_") + "\(self.targetId)"
            //: UserDefaults.standard.set(true, forKey: key)
            UserDefaults.standard.set(true, forKey: key)
            //: if AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue && type != "QuickMsg" {
            if AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue, type != (String(appCurNearFormat.suffix(8))) {
                //: self.removeReplyTipsView()
                self.hidden()
            }
            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(updateMsgData, msgId: msgInfo["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
            self.msgView.lastAll(updateMsgData, barMouth: msgInfo[(String(mainImplementData.prefix(5)))] as? String, frameChange: msgIncome, exposureProcess: .Msg_Status_Succ)
            
            // 女性记录最新回复时间, 刷新表格
            //: TalkingPrivateChatManager.updateReplyLatestTimeAndReload(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)
            PlumpDownReadingManageressChatManager.layer(CGFloat(msgIncome.floatValue), updateMsgData.innerMessage.timestamp, self.msgView)

            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((dream_appearancePanTelevisionName.replacingOccurrences(of: "always", with: "t") + String(k_recommendMsg.suffix(6)))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(dream_appearancePanTelevisionName.replacingOccurrences(of: "always", with: "t") + String(k_recommendMsg.suffix(6)))] as! NSNumber
                //: AdministratorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                AdministratorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }

            /// 免费消息提示
            //: if dataDict.keys.contains("attachSysMsg") {
            if dataDict.keys.contains((userListValue.replacingOccurrences(of: "global", with: "t") + "SysMsg")) {
                //: let attachSysMsg: String = dataDict["attachSysMsg"] as? String ?? ""
                let attachSysMsg: String = dataDict[(userListValue.replacingOccurrences(of: "global", with: "t") + "SysMsg")] as? String ?? ""
                //: if attachSysMsg.count>0 {
                if attachSysMsg.count > 0 {
                    //: self.func__privateAttachSys_insertTipMsg(str: attachSysMsg)
                    self.guideWith(str: attachSysMsg)
                }
            }

            //: if msgInfo.keys.contains("intimatePhoto") { // 发送成功私密照片 => model，更新本地消息
            if msgInfo.keys.contains((String(user_economicIdent) + String(dreamTenderMessage) + String(noti_duringStr))) { // 发送成功私密照片 => model，更新本地消息
                //: let intimatePhotoDict = msgInfo["intimatePhoto"] as! [String: Any]
                let intimatePhotoDict = msgInfo[(String(user_economicIdent) + String(dreamTenderMessage) + String(noti_duringStr))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgIntimatePhotoModel(dictionary: intimatePhotoDict) {
                if let model = try? EffectModel(dictionary: intimatePhotoDict) {
                    //: updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    updateMsgData.msgModel.msgInfo.intimatePhoto = model
                    //: self.msgView.changeIntimatePhotoMsg(updateMsgData, photoModel: model)
                    self.msgView.off(updateMsgData, billOfFareModel: model)
                }

                //: } else if msgInfo.keys.contains("video") { // 发送成功私密视频 => model，更新本地消息
            } else if msgInfo.keys.contains((dreamFiniteId.replacingOccurrences(of: "daily", with: "i"))) { // 发送成功私密视频 => model，更新本地消息
                //: let intimateVideoDict = msgInfo["video"] as! [String: Any]
                let intimateVideoDict = msgInfo[(dreamFiniteId.replacingOccurrences(of: "daily", with: "i"))] as! [String: Any]
                //: if let model = try? AbTalkingChatMsgVideoModel(dictionary: intimateVideoDict) {
                if let model = try? WantModel(dictionary: intimateVideoDict) {
                    //: updateMsgData.msgModel.msgInfo.video = model
                    updateMsgData.msgModel.msgInfo.video = model
                    //: self.msgView.changeIntimateVideoMsg(updateMsgData, videoModel: model)
                    self.msgView.notBuild(updateMsgData, wall: model)
                }
            }
            //: })
        })

        // 判断是否需要插入音视频提示消息
        //: func__privateChatVideo_insertTipMsg()
        date()

        //: updateSession()
        scheduleShare()
    }

    /// 建立会话标识，本地更新数据和会话列表
    //: func updateSession() {
    func scheduleShare() {
        //: DBUserInfoManager.cache_updateSessionAndPostNotif(with: targetId)
        DecisionMakerReactiveCompatible.body(with: targetId)
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool, clickEvent: String) {
    private func beforeEvent(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool, clickEvent: String) {
        //: if errorCode == 50203 {
        if errorCode == 50203 { // 余额不足
            //: guard AdministratorThen.share.loginUserMode.status != 1 else {
            guard AdministratorThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    iconThemeBringBackPlace(showMsg: errorStr)
                }
                //: return
                return
            }

            //: DumpPushManager.share.func__jumpToWebRecharge(clickEvent: clickEvent, sufficient: false)
            DumpPushManager.share.shapeStyle(clickEvent: clickEvent, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.forefrontCurrent()
            //: AdministratorThen.share.loginUserMode.freeMsgTimes = ""
            AdministratorThen.share.loginUserMode.freeMsgTimes = ""
            //: msgInputView.setHidenFreeMsgTimesView()
            msgInputView.hem()
//            insertTipMsg_Nobalance(tipStr: errorStr)
            //: } else if errorCode == 10888 {
        } else if errorCode == 10888 { // vip订阅弹窗
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ConsumerPopView(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage))
            //: view.show()
            view.bill()
            //: } else if errorCode == 60003, !AdministratorThen.share.loginUserMode.isNaUser {
        } else if errorCode == 60003, !AdministratorThen.share.loginUserMode.isNaUser {
            /// 真人认证提示弹窗
            //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
            ShrinkWindowManager.shared.pinLap()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                iconThemeBringBackPlace(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 亲密度

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 解锁图片视频送礼亲密度
    //: private func unlockPhotoOrVideoTool(dataDict: Dictionary<String, Any>) {
    private func history(dataDict: [String: Any]) {
        //: let msgInfo = dataDict["msgInfo"] as! Dictionary<String, Any>
        let msgInfo = dataDict[(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o"))] as! [String: Any]
        /// 亲密度
        //: self.sendMsgIntimateTool(msgInfo: msgInfo)
        self.implement(msgInfo: msgInfo)
    }

    /// 初次展示导航栏的亲密度头像
    //: private func showFirstIntimateView() {
    private func beforeEnable() {
        //: if self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.chatTitleEffect && conversationType == .private  {
        if self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.chatTitleEffect, conversationType == .private {
            //: self.isShowIntimateView = true
            self.isShowIntimateView = true
            //: navView.private_updateIntimateNavView(userModel: userWrap)
            navView.shrinkBright(userModel: userWrap)
            /// 初次展示动效
            //: let key = "\(AdministratorThen.share.loginUserMode.userID)_\(self.targetId)"
            let key = "\(AdministratorThen.share.loginUserMode.userID)_\(self.targetId)"
            //: var intimateCacheData = (Defaults.array(forKey: TalkingFirstShowIntimateGuide)) as? [String] ?? []
            var intimateCacheData = (noti_foundData.array(forKey: mBuildKey)) as? [String] ?? []
            //: if !intimateCacheData.contains(key) {
            if !intimateCacheData.contains(key) {
                //: navView.playAnimation()
                navView.quantityensityLevel()
                //: intimateCacheData.append(key)
                intimateCacheData.append(key)
                //: Defaults.set(intimateCacheData, forKey: TalkingFirstShowIntimateGuide)
                noti_foundData.set(intimateCacheData, forKey: mBuildKey)
            }
        }
    }

    /// 收到亲密度消息
    //: @objc func newMsgIntimate(notification: NSNotification) {
    @objc func generalUntested(notification: NSNotification) {
        //: let data = notification.object as! V2TIMMessage
        let data = notification.object as! V2TIMMessage
        //: let dic = ManagerPubliclyThen.parseTXMessageData(data: data.customElem.data)
        let dic = ManagerPubliclyThen.recover(data: data.customElem.data)
        //: let jsonDic = JSON(dic)
        let jsonDic = JSON(dic)
        //: let extra = jsonDic["extra"].dictionary
        let extra = jsonDic[(String(user_goldenData))].dictionary
        //: let msgInfo = extra?["msgInfo"]?.dictionary
        let msgInfo = extra?[(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o"))]?.dictionary
        //: if data.sender == self.targetId {
        if data.sender == self.targetId {
            //: if let totalIntimate = msgInfo?["totalIntimate"]?.int {
            if let totalIntimate = msgInfo?[(String(dreamSmoothIdent.suffix(6)) + "ntimate")]?.int {
                //: if self.conversationType == .private {
                if self.conversationType == .private {
                    //: let time = data.timestamp.timeIntervalSince1970
                    let time = data.timestamp.timeIntervalSince1970
                    //: self.intimateTool(currenIntimate: totalIntimate, time: Int(time))
                    self.totalimate(currenIntimate: totalIntimate, time: Int(time))
                }
            }
        }
    }

    /// 发送消息时
    //: private func sendMsgIntimateTool(msgInfo: Dictionary<String, Any>) {
    private func implement(msgInfo: [String: Any]) {
        //: if msgInfo.keys.contains("totalIntimate") && self.conversationType == .private {
        if msgInfo.keys.contains((String(dreamSmoothIdent.suffix(6)) + "ntimate")), self.conversationType == .private {
            //: let totalIntimacy = msgInfo["totalIntimate"] as? Int ?? 0
            let totalIntimacy = msgInfo[(String(dreamSmoothIdent.suffix(6)) + "ntimate")] as? Int ?? 0
            //: self.intimateTool(currenIntimate: totalIntimacy)
            self.totalimate(currenIntimate: totalIntimacy)
        }
    }

    /// 亲密度操作
    //: func intimateTool(currenIntimate: Int, time: Int = 0) {
    func totalimate(currenIntimate: Int, time: Int = 0) {
        //: guard currenIntimate > 0 else { return }
        guard currenIntimate > 0 else { return }
        //: let model = DBUserInfoManager.cache_getCachedUserInfo(targetId: self.targetId)
        let model = DecisionMakerReactiveCompatible.clout(targetId: self.targetId)
        //: self.userWrap.intimate = currenIntimate
        self.userWrap.intimate = currenIntimate
        /// 满足最低条件，展示页面
        //: if currenIntimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
        if currenIntimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.chatTitleEffect {
            /// 亲密度增加
            //: if !self.isShowIntimateView {
            if !self.isShowIntimateView {
                //: self.isShowIntimateView = true
                self.isShowIntimateView = true
                //: navView.private_updateIntimateNavView(userModel: self.userWrap)
                navView.shrinkBright(userModel: self.userWrap)
            }
            //: if currenIntimate - (model?.intimate ?? 0) > 0 {
            if currenIntimate - (model?.intimate ?? 0) > 0 {
                //: self.navView.playIntimateAnim(intimate: currenIntimate - (model?.intimate ?? 0))
                self.navView.tillNearby(intimate: currenIntimate - (model?.intimate ?? 0))
                /// 播放完毕
                //: self.navView.animEndBlock = { [weak self] in
                self.navView.animEndBlock = { [weak self] in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: self.navView.setAllData(intimate: currenIntimate)
                    self.navView.repudiate(intimate: currenIntimate)
                }
                //: } else {
            } else {
                //: self.navView.setAllData(intimate: currenIntimate)
                self.navView.repudiate(intimate: currenIntimate)
            }
        }

        //: let currTime = time > 0 ? time: Int(Date().timeIntervalSince1970)
        let currTime = time > 0 ? time : Int(Date().timeIntervalSince1970)
        //: DBUserInfoManager.cache_updateIntimate(with: targetId, intimate: currenIntimate, currTime: currTime)
        DecisionMakerReactiveCompatible.convertTime(with: targetId, intimate: currenIntimate, currTime: currTime)
        //: let userInfo = ["uid": self.targetId,
        let userInfo = [String(bytes: userEntryProjectName.map{socialUniform(stat: $0)}, encoding: .utf8)!: self.targetId,
                        //: "intimate": currenIntimate] as [String : Any]
                        String(bytes: dream_woodKey.reversed(), encoding: .utf8)!: currenIntimate] as [String: Any]
        //: NotificationCenter.default.post(name: MSGLIST_UPDATE_INTIMATE_NOTIFICATION,
        NotificationCenter.default.post(name: showValueTitle,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: userInfo)
                                        userInfo: userInfo)
    }
}

// MARK: - 输入框

//: extension TalkingPrivateChatController: ChatInputViewDelegate {
extension PurchaseViewController: ShrinkViewDelegate {
    /// 检测是否需要展示翻译入口
    //: func func_inputContentIntervalOneSecond() {
    func sign() {
        //: guard AdministratorThen.share.loginUserMode.sex == Gender.female.rawValue else { return }
        guard AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.female.rawValue else { return }
        //: let isEnglish = selectTranView.checkInputLanguage(self.msgInputView.inputTextView.text)
        let isEnglish = selectTranView.countervalInputSmooth(self.msgInputView.inputTextView.text)
        //: self.selectTranView.isHidden = isEnglish
        self.selectTranView.isHidden = isEnglish
    }

    //: func func__selectMentionUser(range: NSRange) {}
    func night(range _: NSRange) {}

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func mightHaveBeen(actionType: MightComparable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.sendImg {
                if self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.sendImg {
                    //: func__choicePhotos()
                    paper()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    bringHome()
                }
                //: } else {
            } else {
                //: func__choicePhotos()
                paper()
            }
        //: case .VideoCall:
        case .VideoCall: // 音/视频通话
            //: var videoStr: String?
            var videoStr: String?
            //: var voiceStr: String?
            var voiceStr: String?
            //: if AdministratorThen.share.loginUserMode.sex == Gender.female.rawValue {
            if AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.female.rawValue {
                //: videoStr = self.userWrap.videoBean
                videoStr = self.userWrap.videoBean
                //: voiceStr = self.userWrap.voiceBean
                voiceStr = self.userWrap.voiceBean
                //: } else {
            } else {
                //: if AdministratorThen.share.loginUserMode.loungePlus {
                if AdministratorThen.share.loginUserMode.loungePlus { // 男性会员
                    //: videoStr = self.userWrap.videoVIPPrice
                    videoStr = self.userWrap.videoVIPPrice
                    //: voiceStr = self.userWrap.voiceVIPPrice
                    voiceStr = self.userWrap.voiceVIPPrice
                    //: } else {
                } else {
                    //: videoStr = self.userWrap.videoPrice
                    videoStr = self.userWrap.videoPrice
                    //: voiceStr = self.userWrap.voicePrice
                    voiceStr = self.userWrap.voicePrice
                }
            }
            //: if self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.requestCall {
            if self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.requestCall {
                //: videoCallManager.showVideoCallMenu(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                videoCallManager.inciteVisitor(videoPrice: videoStr, voicePrice: voiceStr, vipPrompt: self.userWrap.prompt)
                //: } else {
            } else {
                //: showIntimateErrorStr()
                bringHome()
            }

        //: case .ShortVideo:
        case .ShortVideo: // 短视频
            //: if self.conversationType == .private {
            if self.conversationType == .private {
                //: if self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.sendVideo {
                if self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.sendVideo {
                    //: func__sendShortVideo()
                    tabVideo()
                    //: } else {
                } else {
                    //: showIntimateErrorStr()
                    bringHome()
                }
                //: } else {
            } else {
                //: func__sendShortVideo()
                tabVideo()
            }

        //: case .Gift: break
        case .Gift: break

        //: case .Game:
        case .Game: // 游戏
            //: gamesView.showView(from: .PrivateChat)
            gamesView.plainspoken(from: .PrivateChat)
        }
    }

    //: private func showIntimateErrorStr() {
    private func bringHome() {
        //: self.func__showStatusBarErrorMsg(showMsg: "Can't use the function due to your lack of intimacy level.".localized)
        self.iconThemeBringBackPlace(showMsg: String(bytes: notiListenKey.map{textPrompt(remove: $0)}, encoding: .utf8)!.localized)
    }

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {}
    func duringSequence(giftModel _: ApprovalModelType, giftNum _: String, model _: LanceTransformable) {}

    // 聊天面板送礼
    /// - Parameters:
    ///   - giftModel: 礼物模型
    ///   - giftNum: 礼物数量
    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func up(giftModel: ApprovalModelType, giftNum: String) {
        //: func__sendGiftMsg(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
        permissionAttribute(giftModel: giftModel, giftNum: giftNum, cellData: nil, fromStr: nil)
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, cellData: LiteralCellData?, fromStr: String?) {
    func permissionAttribute(giftModel: ApprovalModelType, giftNum: String, cellData: LiteralCellData?, fromStr: String?) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            iconThemeBringBackPlace(showMsg: dream_micData)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] = self.targetId
        params[(String(showCompareIdent.suffix(5)))] = self.targetId
        //: params["giftId"] = giftModel.gid
        params[(String(mainThanName.suffix(6)))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(showCutIdent.suffix(7)))] = giftNum
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(String(k_throughData.suffix(7)) + String(userPageData))] = giftModel.pkgItemsetId
        }
        //: if fromStr != nil {
        if fromStr != nil {
            //: params["from"] = fromStr
            params[(main_ceremonyStr.replacingOccurrences(of: "cute", with: "m"))] = fromStr
        }
        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftPrivete(param: params, completion: { succeed, result, errorModel in
        ShrinkReactiveCompatible.boxThirdDescription(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: clickSendingiftsButton)
                self.beforeEvent(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false, clickEvent: user_firstConnectMsg)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.beforeAgent(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, isFromInputView: true)
            self.bringView(extral: result as! [String: Any?], isFromInputView: true)
            //: self.updateSession()
            self.scheduleShare()
            /// 亲密度
            //: let extralInfo = result as! Dictionary<String, Any?>
            let extralInfo = result as! [String: Any?]
            //: let msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
            let msgInfoDic: [String: Any] = extralInfo[(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o"))] as! [String: Any]
            //: if msgInfoDic.keys.contains("totalIntimate") && self.conversationType == .private {
            if msgInfoDic.keys.contains((String(dreamSmoothIdent.suffix(6)) + "ntimate")), self.conversationType == .private {
                //: let totalIntimacy = msgInfoDic["totalIntimate"] as? Int ?? 0
                let totalIntimacy = msgInfoDic[(String(dreamSmoothIdent.suffix(6)) + "ntimate")] as? Int ?? 0
                //: self.intimateTool(currenIntimate: totalIntimacy)
                self.totalimate(currenIntimate: totalIntimacy)
            }
            /// 索要礼物送礼
            //: if cellData != nil {
            if cellData != nil {
                // 修改状态
                //: self.msgView.changeIntimateGiftMsg(cellData)
                self.msgView.streetwise(cellData)
                //: uploadRecord.uploadRecordEvent(eventID: ClickmessageDetailaskMsgsendBtn)
                k_hugeName.hour(eventID: mainDomainStorageIdent)
            }
            //: })
        })
    }

    /// 索要礼物
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func sensorNum(giftModel: ApprovalModelType, giftNum _: String) {
        //: uploadRecord.uploadRecordEvent(eventID: ClickMessageDetailGiftPopAskBtnAllType)
        k_hugeName.hour(eventID: showLengthRateProductionKey)
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            iconThemeBringBackPlace(showMsg: dream_micData)
            //: return
            return
        }
        //: guard self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.wantGift else {
        guard self.userWrap.intimate >= AdministratorThen.share.appUserConfigMode.intimateLimit.wantGift else {
            //: showIntimateErrorStr()
            bringHome()
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["wantGiftId"] = giftModel.gid
        params[(userPositiveKey.replacingOccurrences(of: "than", with: "an") + String(kNearUrl.prefix(7)))] = giftModel.gid
        //: params["type"] = "WantGiftMsg"
        params[(dream_lanceData.replacingOccurrences(of: "punish", with: "t"))] = (String(dream_automaticFormat) + String(mainMatchStr))

        //: let msgInfo = ["msgType": "WantGift"]
        let msgInfo = [String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (String(show_optimisticStr.prefix(7)) + String(user_heDeliveryMessage))]
        //: let dic = ["msgInfo": msgInfo, "params": params]
        let dic = [(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o")): msgInfo, String(bytes: m_behindSecondFormat.map{importantOning(still: $0)}, encoding: .utf8)!: params]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            funcSend(msgData: cellData, reSend: false)
        }
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func debased(heightToBottom: CGFloat) {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.top.equalTo(HalfViewTopMargin-heightToBottom)
                make.top.equalTo(appOkPostUrl - heightToBottom)
            }
        }
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.toBottom(true)
            self.msgView.toBottom(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func wainscot(height: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.toBottom(true)
            self.msgView.toBottom(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func sequence(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.sequence(tap)
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: let location = tap.location(ofTouch: 0, in: self.contentView)
            let location = tap.location(ofTouch: 0, in: self.contentView)
            //: if !contentView.point(inside: location, with: nil) {
            if !contentView.point(inside: location, with: nil) {
                // 点击列表以外区域，移除视图（包括聊天列表）
                //: half_dismissViewAnimation(true)
                missFloutHalfLight(true)
            }
        }
    }

    /// 开始录音操作
    //: func func__starRecordBtnDown() {
    func match() {
        //: if TalkingVoiceMsgPlayManager.shared.playStatus == .Playing {
        if SuggestReactiveCompatible.shared.playStatus == .Playing {
            //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
            SuggestReactiveCompatible.shared.audioWith()
        }
    }
}

// MARK: - 动效

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, isFromInputView: Bool) {
    func bringView(extral: [String: Any?], isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o"))] as! [String: Any]
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!] = (String(showModifyName))
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(noti_peerActiveUrl) + m_runningYourText.replacingOccurrences(of: "ya", with: "o"))] = msgInfoDic

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = AdvocatePatentThen.likeRed(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.fill(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(kMeetName.prefix(9)))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(kMeetName.prefix(9)))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.lastAll(cellData, barMouth: msgInfoDic[(String(mainImplementData.prefix(5)))] as? String, frameChange: msgIncome, exposureProcess: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any])
        level(giftDic: extral as [String: Any])
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((dream_appearancePanTelevisionName.replacingOccurrences(of: "always", with: "t") + String(k_recommendMsg.suffix(6)))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(dream_appearancePanTelevisionName.replacingOccurrences(of: "always", with: "t") + String(k_recommendMsg.suffix(6)))] as! NSNumber
            //: AdministratorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            AdministratorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: AdministratorThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.stooper(needReload: false, mf_coin: AdministratorThen.share.loginUserMode.mf_coin)
        }
    }

    //: func initCachEffectData() {
    func full() {
        //: giftEffectView.initCachAnimatData(tagetID: targetId)
        giftEffectView.cookie(tagetID: targetId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>) {
    func level(giftDic: [String: Any]) {
        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.impression(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func grapnel(giftArr: [Any]) {
        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.endemic(modelArr: giftArr)
    }

    //: @objc func getChatVCTargetId() ->String {
    @objc func ratingProgramId() -> String {
        //: return targetId
        return targetId
    }
}

// MARK: - 音视频通话Event

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 检查权限后发起音频通话
    //: func checkAndCallVoice() {
    func haveWant() {
        //: videoCallManager.checkAndCallVoice {[weak self] succeed in
        videoCallManager.vocalizationSet { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.scheduleShare()
        }
    }

    /// 检测权限后发起视频通话
    //: func checkAndCallVideo() {
    func godspeed() {
        //: videoCallManager.checkAndCallVideo {[weak self] succeed in
        videoCallManager.extendedGenerateComplection { [weak self] succeed in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard succeed else { return }
            guard succeed else { return }
            //: self.updateSession()
            self.scheduleShare()
        }
    }
}

// MARK: - Event

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    //: @objc private func feedbackBtnClick() {
    @objc private func trackClick() {
        //: let vc = TalkingFeedbackVC.init()
        let vc = LiteralFindFeedbackVc()
        //: getNavigationController()?.pushViewController(vc, animated: true)
        thenController()?.pushViewController(vc, animated: true)
    }

    /// 更多按钮事件
    //: private func moreBtnClick() {
    private func dataConverterLevelCell() {
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ChromosomeMappingView(frame: self.view.frame)
        //: vc.initwithList(cellTitleList: ["View info".localized, "Notes".localized, "Report".localized])
        vc.aList(cellTitleList: [(String(mainBrightId)).localized, (String(dream_siteMsg.suffix(5))).localized, (String(showTransformUrl.prefix(6))).localized])
        //: vc.cancelColor = .appTitleColor()
        vc.cancelColor = .manualColor()
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: DumpPushManager.share.func__pushToUserDetailVC(uid: self.targetId)
                DumpPushManager.share.belowStreet(uid: self.targetId)

                //: } else if index == 1 {
            } else if index == 1 { // 备注
                //: jumpToNotesView()
                headCon()

                //: } else if index == 2 {
            } else if index == 2 { // 举报
                //: self.msgInputView.showOtherInputView = true
                self.msgInputView.showOtherInputView = true
                //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                let reportView = WingView(frame: UIScreen.main.bounds, type: .reportUserType, rId: self.targetId)
                //: reportView.showReportViewIn(view: nil)
                reportView.smartPress(view: nil)
                //: reportView.reportHideBlock = { _ in
                reportView.reportHideBlock = { _ in
                    //: self.msgInputView.showOtherInputView = false
                    self.msgInputView.showOtherInputView = false
                }
            }
        }
    }

    /// 跳转到备注页面
    //: private func jumpToNotesView() {
    private func headCon() {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let view = PrivateChatNotesView()
        let view = SampleThen()
        //: view.show(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        view.everyLast(self.userWrap.content, isTop: self.userWrap.top, toUid: self.targetId)
        //: view.dismissBlock = { content, isTop in
        view.dismissBlock = { content, isTop in
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: self.userWrap.content = content
            self.userWrap.content = content
            //: self.userWrap.top = isTop
            self.userWrap.top = isTop
            //: self.notesTopView.refresh(self.userWrap)
            self.notesTopView.attentionScale(self.userWrap)
        }
    }

    /// 返回按钮点击事件
    //: private func backButtonClick() {
    private func equalCompute() {
        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: half_dismissViewAnimation(false)
            missFloutHalfLight(false)
            //: } else {
        } else {
            //: getNavigationController()?.popViewController(animated: true)
            thenController()?.popViewController(animated: true)
        }
    }

    /// 展示飘屏内容
    //: @objc private func showFloatScreen(notification: NSNotification) {
    @objc private func mpNotification(notification: NSNotification) {
        //: if conversationType == .private {
        if conversationType == .private {
            //: let dataInfo = notification.userInfo as! [String: AnyObject]
            let dataInfo = notification.userInfo as! [String: AnyObject]
            //: if let model = TalkingGameFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
            if let model = AdviceFloatingScreenModel.deserialize(from: dataInfo as? Dictionary) {
                //: self.floatScreenView.isUserInteractionEnabled = true
                self.floatScreenView.isUserInteractionEnabled = true
                //: self.floatScreenView.didReceive(model)
                self.floatScreenView.naturalize(model)
            }
        }
    }
}

// MARK: - VIP消息已读回执

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 添加通知
    //: private func addReadPeceiptNotification() {
    private func detailFor() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__msgReadReceipt(notification:)),
                                               selector: #selector(draft(notification:)),
                                               //: name: CHAT_MSG_READ_RECEIPT_NOTIFICATION,
                                               name: noti_barData,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(func__refreshMsgList),
                                               selector: #selector(compartment),
                                               //: name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION,
                                               name: dream_tableUrl,
                                               //: object: nil)
                                               object: nil)
    }

    /// 收到对端已读消息回执通知 -> 刷新列表
    //: @objc func func__msgReadReceipt(notification: NSNotification) {
    @objc func draft(notification: NSNotification) {
        //: let obj = notification.object as! V2TIMMessageReceipt
        let obj = notification.object as! V2TIMMessageReceipt
        //: guard obj.userID == targetId else { return }
        guard obj.userID == targetId else { return }
        //: guard AdministratorThen.share.loginUserMode.loungePlus == true else { return }
        guard AdministratorThen.share.loginUserMode.loungePlus == true else { return }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 订阅成功 -> 更改订阅状态 -> 刷新列表
    //: @objc private func func__refreshMsgList() {
    @objc private func compartment() {
        //: AdministratorThen.share.loginUserMode.loungePlus = true
        AdministratorThen.share.loginUserMode.loungePlus = true
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.msgView.tableView.reloadData()
            self.msgView.tableView.reloadData()
        }
    }

    /// 判断是否插入已读回执引导充值VIP提示消息
    //: func func__privateVIPReadReceipt_insertTipsMsg() {
    func meet() {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.4) {
            //: guard let lastCellData = self.msgView.uiMsgs.lastObject as? ReconstructThen else { return }
            guard let lastCellData = self.msgView.uiMsgs.lastObject as? ReconstructThen else { return }
            //: if TalkingPrivateChatManager.needInsertVIPReadReceiptMsg(cellData: lastCellData) {
            if PlumpDownReadingManageressChatManager.yaup(cellData: lastCellData) {
                // 只做内存插入展示
                //: self.insertTipMsg_Nobalance(tipStr: "View read receipts?".localized, needInsertLocal: false)
                self.across(tipStr: (String(user_atmId.prefix(4)) + " read" + String(mMysteryMsg.suffix(7)) + String(dreamRankValue)).localized, needInsertLocal: false)
            }
        }
    }
}

// MARK: - 选择图片

//: extension TalkingPrivateChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension PurchaseViewController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func paper() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        AsidePermissionTool.replaceDown(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = GagePickTool.examine(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: self.present(vc, animated: true)
                self.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.showUsePhoto(img: photos![0])
                            self.good(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func birth() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        AsidePermissionTool.goopBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.showUsePhoto(img: photo)
                self.good(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }

    /// 展示阅后即焚+私密照片选项
    /// - Parameter img: 图片资源
    //: private func showUsePhoto(img: UIImage) {
    private func good(img: UIImage) {
        //: guard conversationType == .private &&
        guard conversationType == .private &&
            //: AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue else {
            AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue
        else { // 非私聊 && 非审核模式
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ThrowModel()
            //: sendModel.isBurn = "0"
            sendModel.isBurn = "0"
            //: sendModel.giftId = "0"
            sendModel.giftId = "0"
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.ready(img: img, sendModel: sendModel)
            //: return
            return
        }

        //: let vc = TalkingMomentPhotosVC(images: [img], index: 0, type: .sendIntimatePhoto)
        let vc = HaveViewController(images: [img], index: 0, type: .sendIntimatePhoto)
        //: vc.sendIntimatePhotoBlock = {[weak self] isBurn, giftId in
        vc.sendIntimatePhotoBlock = { [weak self] isBurn, giftId in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ThrowModel()
            //: sendModel.isBurn = isBurn
            sendModel.isBurn = isBurn
            //: sendModel.giftId = giftId
            sendModel.giftId = giftId
            //: self.func__sendPictureMsg(img: img, sendModel: sendModel)
            self.ready(img: img, sendModel: sendModel)
        }
        //: getNavigationController()?.pushViewController(vc, animated: true)
        thenController()?.pushViewController(vc, animated: true)
    }
}

// MARK: - 选择视频

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 发送短视频
    //: private func func__sendShortVideo() {
    private func tabVideo() {
        // 检测用户短视频版本是否可用
        //: guard TalkingPrivateChatManager.checkShortVideo(userVersion: self.userWrap.version) else {
        guard PlumpDownReadingManageressChatManager.finish(userVersion: self.userWrap.version) else {
            //: let gender = self.userWrap.sex == Gender.male.rawValue ? "He".localized:"She".localized
            let gender = self.userWrap.sex == RecordNameLiteral.male.rawValue ? "He".localized : (String(showDepthDevelopName.suffix(3))).localized
            //: self.func__showStatusBarErrorMsg(showMsg: "%@ is using too low a version".localizedArguments(gender))
            self.iconThemeBringBackPlace(showMsg: (String(showPolicyName) + String(m_trackPath)).innerArguments(gender))
            //: return
            return
        }

        //: self.openVideoPicker()
        self.receiveTo()
    }

    /// 打开视频相册
    //: private func openVideoPicker() {
    private func receiveTo() {
        //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: false, allowVideo: true)
        let vc = GagePickTool.examine(maxCount: 1, allowPhoto: false, allowVideo: true)
        //: vc.modalPresentationStyle = .fullScreen
        vc.modalPresentationStyle = .fullScreen
        //: self.present(vc, animated: true)
        self.present(vc, animated: true)
        //: vc.didFinishPickingVideoHandle = { [weak self] ( coverImage: UIImage?, assets: PHAsset?)  in
        vc.didFinishPickingVideoHandle = { [weak self] (_: UIImage?, assets: PHAsset?) in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingImagePickTool.getVideoPath(asset: assets) { filePath in
            GagePickTool.spreadInput(asset: assets) { filePath in
                //: guard filePath != nil else { return }
                guard filePath != nil else { return }
                //: let asset = AVURLAsset.init(url: filePath!)
                let asset = AVURLAsset(url: filePath!)
                //: let time = asset.duration
                let time = asset.duration
                //: let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                let seconds = Int(roundf(Float(CMTimeGetSeconds(time))))
                //: if seconds >= 5 {
                if seconds >= 5 { // 压缩视频
                    //: self.CompressedVideo(url: filePath!)
                    self.louveredWindow(url: filePath!)
                    //: } else {
                } else {
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) { // 视频压缩取消
                        //: self.func__showStatusBarErrorMsg(showMsg: "Shoot video at least 5 seconds".localized)
                        self.iconThemeBringBackPlace(showMsg: (String(mExplainName.prefix(4)) + "t vide" + String(userDominantInsideKey.suffix(5)) + "leas" + String(user_maxFormat.suffix(5)) + "econds").localized)
                    }
                }
            }
        }
    }

    /// 视频压缩
    /// - Parameter url: 本地路径
    //: private func CompressedVideo(url: URL) {
    private func louveredWindow(url: URL) {
        //: let style: TalkingVideoEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        let style: VisitorEditorStyle = (conversationType == .private) ? .intimateVideo : .normal
        //: let videoVC = TalkingVideoEditorController(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
        let videoVC = PerusalThen(style: style, maxDuration: 60.0, videoURL: url) { videoInfo in
            //: guard let videoInfo = videoInfo else { return }
            guard let videoInfo = videoInfo else { return }
            // 压缩成功，实例化模型
            //: let sendModel = AbTalkingChatMsgCustomSendModel()
            let sendModel = ThrowModel()
            //: sendModel.giftId = videoInfo.giftId
            sendModel.giftId = videoInfo.giftId
            //: sendModel.isBurn = videoInfo.isBurn
            sendModel.isBurn = videoInfo.isBurn
            //: sendModel.localCoverImg = videoInfo.coverPath
            sendModel.localCoverImg = videoInfo.coverPath
            //: sendModel.localVideoUrl = videoInfo.videoPath
            sendModel.localVideoUrl = videoInfo.videoPath
            //: sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            sendModel.duration = Int(roundf(Float(videoInfo.duration)))
            // 发送视频消息
            //: self.func__sendVideoMsg(msgData: nil, sendModel: sendModel)
            self.model(msgData: nil, sendModel: sendModel)
        }

        //: ProgressHUD.dismiss()
        VillageView.statusDismiss()
        //: getNavigationController()?.pushViewController(videoVC, animated: true)
        thenController()?.pushViewController(videoVC, animated: true)
    }

    /// 上传视频
    /// - Parameters:
    ///   - msgData: cellData，此处只数据传递
    ///   - sendModel: 消息自定义本地模型
    //: private func cos_UploadVideo(msgData: ReconstructThen, sendModel: AbTalkingChatMsgCustomSendModel) {
    private func noWeight(msgData: ReconstructThen, sendModel: ThrowModel) {
        //: TalkingMomentVideoManager.shared.cos_uploadVideo(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
        ManagerRoundingPinpointTvReactiveCompatible.shared.doingExist(type: 3, coverPath: sendModel.localCoverImg, videoPath: sendModel.localVideoUrl) { coverUrl, videoUrl, succeed in
            //: guard succeed else {
            guard succeed else {
                //: self.func__showStatusBarErrorMsg(showMsg: "Upload failed".localized)
                self.iconThemeBringBackPlace(showMsg: (String(appNextScaleEmptyData)).localized)
                // 更改消息状态为失败
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.lastAll(msgData, barMouth: nil, frameChange: NSNumber(value: 0), exposureProcess: .Msg_Status_Fail)
                //: return
                return
            }

            /// 上传成功，刷新本地视频消息，调用发消息接口
            //: sendModel.coverImg = coverUrl
            sendModel.coverImg = coverUrl
            //: sendModel.videoUrl = videoUrl
            sendModel.videoUrl = videoUrl
            //: self.func__sendVideoMsg(msgData: msgData, sendModel: sendModel)
            self.model(msgData: msgData, sendModel: sendModel)
        }
    }
}

// MARK: - 语音消息

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    //: func func__addVoiceNotification() {
    func straightAndNarrow() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidBecomeActive),
        NotificationCenter.default.addObserver(self, selector: #selector(rootExit),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(appDidEnterBack),
        NotificationCenter.default.addObserver(self, selector: #selector(sumrude),
                                               //: name: UIApplication.didEnterBackgroundNotification,
                                               name: UIApplication.didEnterBackgroundNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(audioSessionInterrupted(notification:)),
                                               selector: #selector(sumForm(notification:)),
                                               //: name: AVAudioSession.interruptionNotification,
                                               name: AVAudioSession.interruptionNotification,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(insertGiftMsgNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(texture(notification:)),
                                               //: name: CHAT_ADD_CALL_RECORD_MSG_NOTIFICATION,
                                               name: show_searchName,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(newMsgIntimate(notification:)),
                                               selector: #selector(generalUntested(notification:)),
                                               //: name: CHAT_NEW_MSG_NOTIFICATION,
                                               name: showDetailContent,
                                               //: object: nil)
                                               object: nil)
    }

    //: func func__addOrRrmoveObserverForMsgPlayer(isAdd: Bool) {
    func boxAdd(isAdd: Bool) {
        //: if isAdd {
        if isAdd {
            //: TalkingVoiceMsgPlayManager.shared.addDaskManagerDelegate()
            SuggestReactiveCompatible.shared.darkAges()
            //: } else {
        } else {
            //: TalkingVoiceMsgPlayManager.shared.removeDaskManagerDelegate()
            SuggestReactiveCompatible.shared.slowDelegate()
        }
    }

    //: @objc func appDidBecomeActive() {
    @objc func rootExit() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
        boxAdd(isAdd: true)
    }

    //: @objc func appDidEnterBack() {
    @objc func sumrude() {
        //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
        boxAdd(isAdd: false)
    }

    //: @objc func audioSessionInterrupted(notification: NSNotification) {
    @objc func sumForm(notification: NSNotification) {
        //: guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        guard let userInfo = notification.userInfo, let reasonValue = userInfo[AVAudioSessionInterruptionTypeKey] as? UInt else { return }
        //: switch reasonValue {
        switch reasonValue {
        //: case AVAudioSession.InterruptionType.began.rawValue:
        case AVAudioSession.InterruptionType.began.rawValue: // Began
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: false)
            boxAdd(isAdd: false)
        //: break
        //: case AVAudioSession.InterruptionType.ended.rawValue:
        case AVAudioSession.InterruptionType.ended.rawValue: // End
            //: func__addOrRrmoveObserverForMsgPlayer(isAdd: true)
            boxAdd(isAdd: true)
        //: break
        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__DidTouchVoiceCellNeedPlayWithCurrentModel(cellData: HeapReactiveCompatible, cellView: TalkingChatAudioMsgCell) {
    func priorityInView(cellData: HeapReactiveCompatible, cellView: MovieMsgCell) {
        //: guard TalkingSocketManager.shared.isFind == false else {
        guard AdviceLandscapeReactiveCompatible.shared.isFind == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.iconThemeBringBackPlace(showMsg: m_buildNearbyData)
            //: return
            return
        }

        //: let message = cellData.innerMessage
        let message = cellData.innerMessage

        //: for indexModel in self.msgView.uiMsgs {
        for indexModel in self.msgView.uiMsgs {
            //: if (indexModel as? ReconstructThen)!.isKind(of: HeapReactiveCompatible.self) == false {
            if (indexModel as? ReconstructThen)!.isKind(of: HeapReactiveCompatible.self) == false {
                //: continue
                continue
            }
            //: let audioModel = indexModel as! HeapReactiveCompatible
            let audioModel = indexModel as! HeapReactiveCompatible
            //: audioModel.voiceModel.isPlayingStatus = 2
            audioModel.voiceModel.isPlayingStatus = 2
            //: audioModel.voiceModel.activityShowStatus = 2
            audioModel.voiceModel.activityShowStatus = 2
            // 红点提示隐藏
            //: if (audioModel.isEqual(cellData)) {
            if audioModel.isEqual(cellData) {
                //: cellView.voiceTrackPoint.isHidden = true
                cellView.voiceTrackPoint.isHidden = true
            }
        }
        //: if message != nil {
        if message != nil {
            //: cellData.voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: message!)
            cellData.voiceModel.db_voiceCacheWrap = DecisionMakerReactiveCompatible.cleanMessage(message: message!)
        }
        //: TalkingVoiceMsgPlayManager.shared.stopAudioPlayer()
        SuggestReactiveCompatible.shared.audioWith()

        //: if TalkingVoiceMsgPlayManager.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
        if SuggestReactiveCompatible.shared.clickCellId == cellData.voiceModel.db_voiceCacheWrap.msgId {
            //: printLog(message: "播放状态:\(TalkingVoiceMsgPlayManager.shared.playStatus)")
            printLog(message: (String(appIndividualFormat)) + "\(SuggestReactiveCompatible.shared.playStatus)")
            //: if TalkingVoiceMsgPlayManager.shared.playStatus != .Unknown {
            if SuggestReactiveCompatible.shared.playStatus != .Unknown {
                //: TalkingVoiceMsgPlayManager.shared.playStatus = .Unknown
                SuggestReactiveCompatible.shared.playStatus = .Unknown
                //: return
                return
            }
        }
        // 指定播放的cell
        //: TalkingVoiceMsgPlayManager.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        SuggestReactiveCompatible.shared.clickCellId = cellData.voiceModel.db_voiceCacheWrap.msgId
        //: TalkingVoiceMsgPlayManager.shared.playStatus = .Waiting
        SuggestReactiveCompatible.shared.playStatus = .Waiting
        //: var waitArray = [cellData.voiceModel]
        var waitArray = [cellData.voiceModel]
        //: if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
        if cellData.voiceModel.db_voiceCacheWrap.db_isRead == "0" {
            //: let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            let currentIndex = self.msgView.uiMsgs.index(of: cellData)
            //: for index in currentIndex+1 ..< self.msgView.uiMsgs.count {
            for index in currentIndex + 1 ..< self.msgView.uiMsgs.count {
                //: let subModel: ReconstructThen = self.msgView.uiMsgs.object(at: index) as! ReconstructThen
                let subModel: ReconstructThen = self.msgView.uiMsgs.object(at: index) as! ReconstructThen
                //: let subMessage = subModel.innerMessage
                let subMessage = subModel.innerMessage
                //: if subModel.isKind(of: HeapReactiveCompatible.self) {
                if subModel.isKind(of: HeapReactiveCompatible.self) {
                    //: let audioModel = subModel as! HeapReactiveCompatible
                    let audioModel = subModel as! HeapReactiveCompatible
                    //: let voiceModel = audioModel.voiceModel
                    let voiceModel = audioModel.voiceModel
                    //: if subMessage != nil {
                    if subMessage != nil {
                        //: voiceModel.db_voiceCacheWrap = DBUserInfoManager.cache_getAudioDB(message: subMessage!)
                        voiceModel.db_voiceCacheWrap = DecisionMakerReactiveCompatible.cleanMessage(message: subMessage!)
                    }
                    //: if (voiceModel.db_voiceCacheWrap.db_isRead == "0") {
                    if voiceModel.db_voiceCacheWrap.db_isRead == "0" {
                        //: waitArray.append(voiceModel)
                        waitArray.append(voiceModel)
                    }
                    //: cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    cellData.voiceModel.db_voiceCacheWrap.db_isRead = "1"
                    //: cellView.voiceTrackPoint.isHidden = true
                    cellView.voiceTrackPoint.isHidden = true
                    //: WCDBVoiceMsgTable.db_updateVoiceMsg(cellData.voiceModel.db_voiceCacheWrap)
                    FireMsgTable.flush(cellData.voiceModel.db_voiceCacheWrap)
                }
            }
        }
        //: TalkingVoiceMsgPlayManager.shared.starPlayMsg(msgArr: waitArray)
        SuggestReactiveCompatible.shared.knapContainer(msgArr: waitArray)
    }
}

// MARK: - 用户相关

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 请求聊天用户信息 V1.8.0 提前到进入页面之前，先判断是否封号
    //: func func__requestChatInfo() {
    func privacyInfo() {
        //: guard conversationType == .private || conversationType == .service else { return }
        guard conversationType == .private || conversationType == .service else { return }

        //: self.func__updateUIWithUserWrap()
        self.talkMotivation()
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        // 判断是否需要插入一键打招呼消息
        //: if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
        if (self.quickGreetingTextDict?.count ?? 0 > 0) || (self.quickGreetingAudioDict?.count ?? 0 > 0) {
            //: self.func_sendQuickGreetingMsg(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
            self.maxThin(textDict: self.quickGreetingTextDict, audioDict: self.quickGreetingAudioDict)
        }
        //: if self.msgView.uiMsgs.count > 0 && self.userWrap.existSess == false {
        if self.msgView.uiMsgs.count > 0, self.userWrap.existSess == false {
            //: for model in self.msgView.uiMsgs {
            for model in self.msgView.uiMsgs {
                //: let indexModel = model as? ReconstructThen
                let indexModel = model as? ReconstructThen
                //: if indexModel?.isKind(of: HeapReactiveCompatible.self) == true || indexModel?.isKind(of: GageBubbleCellData.self) == true || indexModel?.isKind(of: WarnThen.self) == true || indexModel?.isKind(of: EnableicialCellData.self) == true {
                if indexModel?.isKind(of: HeapReactiveCompatible.self) == true || indexModel?.isKind(of: GageBubbleCellData.self) == true || indexModel?.isKind(of: WarnThen.self) == true || indexModel?.isKind(of: EnableicialCellData.self) == true {
                    //: self.userWrap.existSess = true
                    self.userWrap.existSess = true
                    //: break
                    break
                }
            }
        }
        // 判断是否需要插入音视频提示消息
        //: self.func__privateChatVideo_insertTipMsg()
        self.date()
        // 判断是否需要插入官方客服提示消息
        //: self.func__customerServiceCenter_insertTipMsg()
        self.reason()
        /// 判断是否插入已读回执引导充值VIP提示消息
        //: self.func__privateVIPReadReceipt_insertTipsMsg()
        self.meet()
    }

    /// 刷新UI
    //: func func__updateUIWithUserWrap() {
    func talkMotivation() {
        //: guard userWrap.uid.count > 0 else { return }
        guard userWrap.uid.count > 0 else { return }
        //: bgImgeView.setUrlImage(urlStr: userWrap.headPic)
        bgImgeView.overloadFinish(urlStr: userWrap.headPic)
        //: navView.private_updateNavView(userModel: userWrap, isFrom: self.isFrom)
        navView.orbicularMid(userModel: userWrap, isFrom: self.isFrom)
        /// 亲密度头像
        //: self.showFirstIntimateView()
        self.beforeEnable()

        //: if sendGreetBtn.isHidden == false {
        if sendGreetBtn.isHidden == false {
            //: sendGreetBtn.isHidden = userWrap.sex == AdministratorThen.share.loginUserMode.sex
            sendGreetBtn.isHidden = userWrap.sex == AdministratorThen.share.loginUserMode.sex
        }
        //: if beautyPhotosBtn.isHidden == false {
        if beautyPhotosBtn.isHidden == false {
            //: beautyPhotosBtn.isHidden = userWrap.sex == AdministratorThen.share.loginUserMode.sex
            beautyPhotosBtn.isHidden = userWrap.sex == AdministratorThen.share.loginUserMode.sex
        }
        // 备注置顶
        //: notesTopView.refresh(userWrap)
        notesTopView.attentionScale(userWrap)
    }

    //: func func__getFillDraftText() {
    func componentPart() {
        //: let userID = "c2c_"+targetId
        let userID = (String(m_advertisingEveryBalanceStr.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
        V2TIMManager.sharedInstance().getConversation(userID) { [weak self] conv in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: let draftText = conv?.draftText ?? ""
                let draftText = conv?.draftText ?? ""
                //: if !draftText.isEmptyString {
                if !draftText.isEmptyString {
                    //: self.msgInputView.inputTextView.text = conv?.draftText
                    self.msgInputView.inputTextView.text = conv?.draftText
                    //: self.msgInputView.inputTextView.becomeFirstResponder()
                    self.msgInputView.inputTextView.becomeFirstResponder()
                    //: self.func_inputContentIntervalOneSecond()
                    self.sign()
                }
            }
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }

    //: func func__saveFillDraftText() {
    func explainContainer() {
        //: let userID = "c2c_"+targetId
        let userID = (String(m_advertisingEveryBalanceStr.prefix(4))) + targetId
        //: V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
        V2TIMManager.sharedInstance().setConversationDraft(userID, draftText: msgInputView.inputTextView.text!) {
            //: } fail: { code, desc in
        } fail: { _, _ in
        }
    }
}

// MARK: - 提示消息

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    //: func func__TipsNotification() {
    func tentNotification() {
        //: NotificationCenter.default.addObserver(self, selector: #selector(insertTipsNotif(notification:)),
        NotificationCenter.default.addObserver(self, selector: #selector(gapAcrossAttitude(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kExplainUrl,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) -> Void {
    @objc func gapAcrossAttitude(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: DataGatherThen = userinfo[(String(kQuietValue))] as! DataGatherThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.toBottom(true)
        self.msgView.toBottom(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func across(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo: [String: Any] = ["content": tipStr,
        let msgInfo: [String: Any] = [String(bytes: k_sinkData.map{basicWhite(calculation: $0)}, encoding: .utf8)!: tipStr,
                                      //: "msgType": "tips"]
                                      String(bytes: notiEffectTitle.reversed(), encoding: .utf8)!: (dream_makerMsg.lowercased())]
        //: var message: ReconstructThen?
        var message: ReconstructThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: message = TUISocialChatManager.insertTXMessage(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            message = AdvocatePatentThen.countenance(withExtral: msgInfo, toUid: targetId, isSender: true, isTip: true)
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = AdvocatePatentThen.likeRed(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.toBottom(true)
        self.msgView.toBottom(true)
    }

    /// 官方客服插入系统提示消息
    //: func func__customerServiceCenter_insertTipMsg() {
    func reason() {
        //: guard conversationType == .service else { return }
        guard conversationType == .service else { return }
        //: TalkingPrivateChatManager.chat_customerServiceCenter_insertTipMsg { tipsStr in
        PlumpDownReadingManageressChatManager.recentPush { tipsStr in
            //: guard tipsStr != nil else { return }
            guard tipsStr != nil else { return }
            // 客服提示消息，只做内存插入展示
            //: self.insertTipMsg_Nobalance(tipStr: tipsStr!, needInsertLocal: false)
            self.across(tipStr: tipsStr!, needInsertLocal: false)
        }
    }

    /// 判断是否需要插入免费提示消息
    //: func func__privateAttachSys_insertTipMsg(str: String) {
    func guideWith(str: String) {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: self.insertTipMsg_Nobalance(tipStr: str)
        self.across(tipStr: str)
    }

    /// 判断是否需要插入音视频通话提示消息
    //: func func__privateChatVideo_insertTipMsg() {
    func date() {
        //: guard conversationType == .private else { return }
        guard conversationType == .private else { return }
        //: TalkingPrivateChatManager.chat_privateChatVideo_insertTipMsg(toUid: self.userWrap.uid, msgView: self.msgView) {
        PlumpDownReadingManageressChatManager.untilCity(toUid: self.userWrap.uid, msgView: self.msgView) {
            // 插入音视频提示消息
            //: let str = self.userWrap.nickname
            let str = self.userWrap.nickname
            //: self.insertTipMsg_Nobalance(tipStr: "You can use the video chat feature to give %@ a call".localizedArguments(str))
            self.across(tipStr: String(bytes: notiExhibitKey.map{opParent(earn: $0)}, encoding: .utf8)!.innerArguments(str))
        }
    }

    /// 通话礼物消息
    //: @objc func insertGiftMsgNotif(notification: NSNotification) {
    @objc func texture(notification: NSNotification) {
        //: let dict = notification.userInfo
        let dict = notification.userInfo
        //: let cellData: ReconstructThen = dict?["cellData"] as! ReconstructThen
        let cellData: ReconstructThen = dict?[(String(userReaderWillAideIdent))] as! ReconstructThen
        //: self.msgView.uiMsgs.add(cellData)
        self.msgView.uiMsgs.add(cellData)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.toBottom(true)
        self.msgView.toBottom(true)
    }
}

// MARK: - 回复文案

//: extension TalkingPrivateChatController: PrivateChatReplyTipsProtocol {
extension PurchaseViewController: LackReactiveCompatible {
    //: func func__setReplyTipsView() {
    func superpose() {
        //: let key =  "\(AdministratorThen.share.loginUserMode.userID)_PrivateChatTipsTitle_\(self.targetId)"
        let key = "\(AdministratorThen.share.loginUserMode.userID)" + (String(app_holdIdent.prefix(4)) + "vateC" + String(app_fieldPath.prefix(5)) + String(showJoinAgoText.prefix(4)) + "tle_") + "\(self.targetId)"
        //: let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        let isShow: Bool = UserDefaults.standard.object(forKey: key) as? Bool ?? false
        //: if !isShow && AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue {
        if !isShow && AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue {
            //: view.addSubview(replyTipsView!)
            view.addSubview(replyTipsView!)
            //: replyTipsView?.snp.makeConstraints { make in
            replyTipsView?.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(50)
                make.height.equalTo(50)
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(replyTipsView!.snp.top)
                make.bottom.equalTo(replyTipsView!.snp.top)
            }
            //: self.replyTipsView?.setTipsStrView()
            self.replyTipsView?.tips()
            // 修改banner位置
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            foremost(isMiniView: self.bannerView.isMiniView)
            //: } else {
        } else {
            //: removeReplyTipsView()
            hidden()
        }
    }

    //: func seleteReplyTipsMessage(str: String) {
    func ask(str: String) {
        //: self.userWrap.existSess = true
        self.userWrap.existSess = true
        //: func__sendTextMsg(msgStr: str, quoteModel: nil)
        cap(msgStr: str, quoteModel: nil)
        //: removeReplyTipsView()
        hidden()
    }

    //: func removeReplyTipsView() {
    func hidden() {
        //: if conversationType == .private {
        if conversationType == .private {
            //: if self.replyTipsView != nil {
            if self.replyTipsView != nil {
                //: self.replyTipsView?.removeFromSuperview()
                self.replyTipsView?.removeFromSuperview()
                //: self.replyTipsView = nil
                self.replyTipsView = nil
            }
            //: msgView.view.snp.remakeConstraints { make in
            msgView.view.snp.remakeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: changeBannerViewFream(isMiniView: self.bannerView.isMiniView)
            foremost(isMiniView: self.bannerView.isMiniView)
        }
    }
}

// MARK: - 半屏页面展示/隐藏动画

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 半屏展示动画
    //: private func half_showViewAnimation() {
    private func throughAnimation() {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }
        //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_SHOW, object: nil, userInfo: nil)
        NotificationCenter.default.post(name: kTimeName, object: nil, userInfo: nil)
        //: self.view.frame = CGRect(x: 0, y: ScreenHeight-HalfViewTopMargin, width: ScreenWidth, height: ScreenHeight)
        self.view.frame = CGRect(x: 0, y: appMineMessage - appOkPostUrl, width: noti_senseContent, height: appMineMessage)
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: 0,
                                     y: 0,
                                     //: width: ScreenWidth,
                                     width: noti_senseContent,
                                     //: height: ScreenHeight)
                                     height: appMineMessage)
        }
    }

    /// 半屏页消失动画
    /// - Parameter removeChatList: 是否需要移除聊天列表
    //: private func half_dismissViewAnimation(_ removeChatList: Bool) {
    private func missFloutHalfLight(_ removeChatList: Bool) {
        //: guard self.isFrom == .LiveRoom else { return }
        guard self.isFrom == .LiveRoom else { return }

        //: self.view.endEditing(true)
        self.view.endEditing(true)
        // 移除聊天列表
        //: if removeChatList == true, let currentVC = self.currentViewController() {
        if removeChatList == true, let currentVC = self.springEquinox() {
            //: if TalkingVoiceRoomManager.shared().isParty == true {
            if LanceManager.doingResource().isParty == true {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: app_titleListName, object: nil, userInfo: nil)
            }
            //: for vc in currentVC.children {
            for vc in currentVC.children {
                //: if vc.isKind(of: TalkingChatListViewController.self) {
                if vc.isKind(of: ChatViewDelegate.self) {
                    //: vc.removeFromParent()
                    vc.removeFromParent()
                    //: vc.view.removeFromSuperview()
                    vc.view.removeFromSuperview()
                    //: break
                    break
                }
            }
        }

        // 移除私聊页
        //: UIView.animate(withDuration: 0.25) {
        UIView.animate(withDuration: 0.25) {
            //: self.view.frame = CGRect(x: 0,
            self.view.frame = CGRect(x: 0,
                                     //: y: ScreenHeight-HalfViewTopMargin,
                                     y: appMineMessage - appOkPostUrl,
                                     //: width: ScreenWidth,
                                     width: noti_senseContent,
                                     //: height: ScreenHeight)
                                     height: appMineMessage)
            //: } completion: { finish in
        } completion: { _ in
            //: self.removeFromParent()
            self.removeFromParent()
            //: self.view.removeFromSuperview()
            self.view.removeFromSuperview()
            //: if TalkingVoiceRoomManager.shared().isParty == false {
            if LanceManager.doingResource().isParty == false {
                //: NotificationCenter.default.post(name: LIVE_HALF_VIEW_DISMISS, object: nil, userInfo: nil)
                NotificationCenter.default.post(name: app_titleListName, object: nil, userInfo: nil)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingPrivateChatController {
extension PurchaseViewController {
    /// 创建UI
    //: private func createUI() {
    private func flag() {
        //: view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear:UIColor.white
        view.backgroundColor = (self.isFrom == .LiveRoom) ? UIColor.clear : UIColor.white
        //: view.addSubview(contentView)
        view.addSubview(contentView)

        //: if conversationType == .private {
        if conversationType == .private {
            //: contentView.addSubview(bgImgeView)
            contentView.addSubview(bgImgeView)
            //: contentView.addSubview(bgView)
            contentView.addSubview(bgView)
            //: contentView.addSubview(coverView)
            contentView.addSubview(coverView)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(beautyPhotosBtn)
            contentView.addSubview(beautyPhotosBtn)
            //: contentView.addSubview(sendGreetBtn)
            contentView.addSubview(sendGreetBtn)
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: contentView.addSubview(bannerView)
                contentView.addSubview(bannerView)
            }
            //: contentView.addSubview(floatScreenView)
            contentView.addSubview(floatScreenView)
            //: contentView.addSubview(giftEffectView)
            contentView.addSubview(giftEffectView)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: contentView.addSubview(notesTopView)
            contentView.addSubview(notesTopView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
            //: contentView.addSubview(navView)
            contentView.addSubview(navView)
            //: view.addSubview(msgInputView)
            view.addSubview(msgInputView)
            //: view.addSubview(selectTranView)
            view.addSubview(selectTranView)
            //: view.addSubview(transResultView)
            view.addSubview(transResultView)

            //: } else {
        } else {
            //: title = "System notification".localized
            title = (String(dreamConcentrationKey) + String(dream_exactlyWeeklyCarrierId)).localized
            //: contentView.addSubview(feedView)
            contentView.addSubview(feedView)
            //: feedView.addSubview(feedbackBtn)
            feedView.addSubview(feedbackBtn)
            //: self.addChild(msgView)
            self.addChild(msgView)
            //: contentView.addSubview(msgView.view)
            contentView.addSubview(msgView.view)
        }
    }

    /// 约束
    //: private func createUIConstraints() {
    private func resign() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.bottom.leading.trailing.equalToSuperview()
            make.bottom.leading.trailing.equalToSuperview()
            //: make.top.equalTo((self.isFrom == .LiveRoom) ? HalfViewTopMargin:0)
            make.top.equalTo((self.isFrom == .LiveRoom) ? appOkPostUrl : 0)
        }

        //: if self.isFrom == .LiveRoom {
        if self.isFrom == .LiveRoom {
            //: contentView.layoutIfNeeded()
            contentView.layoutIfNeeded()
            //: contentView.Corner(width: contentView.width,
            contentView.dimensionSixth(width: contentView.width,
                                       //: height: contentView.height,
                                       height: contentView.height,
                                       //: corners: [.topLeft, .topRight],
                                       corners: [.topLeft, .topRight],
                                       //: cornerRadii: .init(width: 8, height: 8))
                                       cornerRadii: .init(width: 8, height: 8))
        }

        //: if conversationType == .private {
        if conversationType == .private {
            //: bgView.snp.makeConstraints { make in
            bgView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: bgImgeView.snp.makeConstraints { make in
            bgImgeView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: coverView.snp.makeConstraints { make in
            coverView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLocationMessage : appServerSensePath)
            }
            //: notesTopView.snp.makeConstraints { make in
            notesTopView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalTo(36)
                make.height.equalTo(36)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: if beautyPhotosBtn.isHidden {
                if beautyPhotosBtn.isHidden {
                    //: make.bottom.equalTo(msgInputView.snp.top)
                    make.bottom.equalTo(msgInputView.snp.top)
                    //: } else {
                } else {
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top).offset(-5)
                }
            }
            //: giftEffectView.snp.makeConstraints { make in
            giftEffectView.snp.makeConstraints { make in
                //: make.edges.equalToSuperview()
                make.edges.equalToSuperview()
            }
            //: sendGreetBtn.snp.makeConstraints { make in
            sendGreetBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(beautyPhotosBtn)
                make.bottom.equalTo(beautyPhotosBtn)
                //: make.leading.equalTo(10)
                make.leading.equalTo(10)
                //: make.size.equalTo(beautyPhotosBtn)
                make.size.equalTo(beautyPhotosBtn)
            }
            //: beautyPhotosBtn.snp.makeConstraints { make in
            beautyPhotosBtn.snp.makeConstraints { make in
                //: make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                make.bottom.equalTo(msgInputView.snp.top).offset(-8)
                //: make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                make.leading.equalTo(sendGreetBtn.snp.trailing).offset(9)
                //: make.width.equalTo(134)
                make.width.equalTo(134)
                //: make.height.equalTo(30)
                make.height.equalTo(30)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + main_showUrl)
            }
            //: floatScreenView.snp.makeConstraints { make in
            floatScreenView.snp.makeConstraints { make in
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.height.equalTo(40)
                make.height.equalTo(40)
            }
            //: if self.isFrom == .Normal {
            if self.isFrom == .Normal {
                //: bannerView.snp.makeConstraints { make in
                bannerView.snp.makeConstraints { make in
                    //: make.trailing.equalToSuperview()
                    make.trailing.equalToSuperview()
                    //: make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    make.bottom.equalTo(beautyPhotosBtn.snp.top)
                    //: make.height.equalTo(106)
                    make.height.equalTo(106)
                    //: make.width.equalTo(93)
                    make.width.equalTo(93)
                }
            }

            //: } else if conversationType == .service {
        } else if conversationType == .service {
            //: navView.snp.makeConstraints { make in
            navView.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalToSuperview()
                make.leading.trailing.top.equalToSuperview()
                //: make.height.equalTo((self.isFrom == .LiveRoom) ? NavigationBarHeight:StatusBarNavigationBarHeight)
                make.height.equalTo((self.isFrom == .LiveRoom) ? kLocationMessage : appServerSensePath)
            }
            //: msgInputView.snp.makeConstraints { make in
            msgInputView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo(view)
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(msgInputView.BackViewHeight)
                make.height.equalTo(msgInputView.BackViewHeight)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.equalToSuperview()
                make.leading.trailing.equalToSuperview()
                //: make.top.equalTo(navView.snp.bottom)
                make.top.equalTo(navView.snp.bottom)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
            }
            //: selectTranView.snp.makeConstraints { make in
            selectTranView.snp.makeConstraints { make in
                //: make.leading.trailing.equalTo(msgInputView)
                make.leading.trailing.equalTo(msgInputView)
                //: make.bottom.equalTo(msgInputView.snp.top)
                make.bottom.equalTo(msgInputView.snp.top)
                //: make.height.equalTo(27)
                make.height.equalTo(27)
            }
            //: transResultView.snp.makeConstraints { make in
            transResultView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalToSuperview()
                make.leading.trailing.bottom.equalToSuperview()
                //: make.height.equalTo(206+kDeviceSafeBottomHeight)
                make.height.equalTo(206 + main_showUrl)
            }

            //: } else {
        } else {
            //: feedView.snp.makeConstraints { make in
            feedView.snp.makeConstraints { make in
                //: make.leading.trailing.bottom.equalTo((view))
                make.leading.trailing.bottom.equalTo(view)
                //: make.height.equalTo(56+kDeviceSafeBottomHeight)
                make.height.equalTo(56 + main_showUrl)
            }
            //: feedbackBtn.snp.makeConstraints { make in
            feedbackBtn.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo(feedView)
                make.leading.trailing.top.equalTo(feedView)
                //: make.height.equalTo(56)
                make.height.equalTo(56)
            }
            //: msgView.view.snp.makeConstraints { make in
            msgView.view.snp.makeConstraints { make in
                //: make.leading.trailing.top.equalTo((view))
                make.leading.trailing.top.equalTo(view)
                //: make.bottom.equalTo(feedView.snp.top)
                make.bottom.equalTo(feedView.snp.top)
            }
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func fileTargetQuantityeraction() {
        //: navView.backButton.rx.tap
        navView.backButton.rx.tap
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.backButtonClick()
                self.equalCompute()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.moreButton.rx.controlEvent(.touchUpInside)
        navView.moreButton.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.moreBtnClick()
                self.dataConverterLevelCell()
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
        //: navView.videoCallBtn.rx.controlEvent(.touchUpInside)
        navView.videoCallBtn.rx.controlEvent(.touchUpInside)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.view.endEditing(true)
                self.view.endEditing(true)
                //: self.func__selectMoreFunAction(actionType: .VideoCall)
                self.mightHaveBeen(actionType: .VideoCall)
                //: uploadRecord.uploadRecordEvent(eventID: ClickChatVideoButton, toUid: self.userWrap.uid )
                k_hugeName.hour(eventID: app_productionStr, toUid: self.userWrap.uid)
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)
    }
}
