
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let noti_rowMessage:[UInt8] = [0x64,0x65,0x74,0x6e,0x65,0x6d,0x65,0x6c,0x70,0x6d,0x69,0x20,0x6e,0x65,0x65,0x62,0x20,0x74,0x6f,0x6e,0x20,0x73,0x61,0x68,0x20,0x29,0x3a,0x72,0x65,0x64,0x6f,0x63,0x28,0x74,0x69,0x6e,0x69]

/*: "roomId" :*/
fileprivate let appLipValue:[Character] = ["r","o","o","m","I","d"]

/*: "type" :*/
fileprivate let mGatherValue:[UInt8] = [0xd9,0xd4,0xdd,0xc8]

private func engagementWhatRet(circle num: UInt8) -> UInt8 {
    return num ^ 173
}

/*: "extra" :*/
fileprivate let k_seriesTitle:[Character] = ["e","x","t","r","a"]

/*: "jumpKey" :*/
fileprivate let show_retainData:[UInt8] = [0x4f,0x50,0x48,0x55,0x6e,0x40,0x5c]

/*: "Send Gift" :*/
fileprivate let notiRoleMessage:String = "supposed"
fileprivate let appInsertName:[Character] = ["e","n","d"," ","G","i","f","t"]

/*: "View info" :*/
fileprivate let userHemValue:String = "View infogender till sound supportive"

/*: "Report" :*/
fileprivate let userCandidateKey:String = "Reportmodify thick abs fill"

/*: "uid" :*/
fileprivate let main_complexityMessage:[UInt8] = [0x7d,0x61,0x6c]

private func tipAttractive(off num: UInt8) -> UInt8 {
    return num ^ 8
}

/*: "name" :*/
fileprivate let m_externalFormat:[UInt8] = [0x5f,0x50,0x5c,0x54]

/*: "Can't send blank message" :*/
fileprivate let mainTireFormat:String = "Can\'"
fileprivate let dreamForeText:String = "d blaassert little end"
fileprivate let k_workerStaffTitle:String = "SSAGE"

/*: "user" :*/
fileprivate let dream_pathMessage:String = "pitcher"

/*: "content" :*/
fileprivate let dreamHelloContent:[UInt8] = [0x79,0x85,0x84,0x8a,0x7b,0x84,0x8a]

fileprivate func padReason(multiple num: UInt8) -> UInt8 {
    let value = Int(num) + 234
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "msgType" :*/
fileprivate let dreamSmoothName:String = "creation concerned what production exportmsgType"

/*: "txt" :*/
fileprivate let kCulturalName:[Character] = ["t","x","t"]

/*: "isGroup" :*/
fileprivate let app_smoothName:String = "calculate error your his entityisGroup"

/*: "jumps" :*/
fileprivate let app_woodRawIdent:String = "jumoperations"

/*: "message" :*/
fileprivate let main_overIdent:[UInt8] = [0x65,0x67,0x61,0x73,0x73,0x65,0x6d]

/*: "TxtMsg" :*/
fileprivate let showDirectionStr:[Character] = ["T","x","t","M","s","g"]

/*: "toUid" :*/
fileprivate let main_connectPath:[Character] = ["t","o","U","i","d"]

/*: "msgInfo" :*/
fileprivate let dream_whetherBeautyCommunicationContent:String = "accept it increasemsgInfo"

/*: "params" :*/
fileprivate let user_properlyUrl:[UInt8] = [0x12,0x3,0x14,0x3,0xf,0x15]

fileprivate func printingFlat(match num: UInt8) -> UInt8 {
    let value = Int(num) - 162
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "contentType" :*/
fileprivate let m_hiMsg:[UInt8] = [0x65,0x70,0x79,0x54,0x74,0x6e,0x65,0x74,0x6e,0x6f,0x63]

/*: "ImgMsg" :*/
fileprivate let appSkinPath:String = "pressure"
fileprivate let user_betweenMsg:String = "mgMsgbecome letter and moon base"

/*: "img" :*/
fileprivate let mainAvailableMsg:[Character] = ["i","m","g"]

/*: "imageUri" :*/
fileprivate let k_revenueOurId:[UInt8] = [0xeb,0xef,0xe3,0xe5,0xe7,0xd7,0xf0,0xeb]

private func laughPick(event num: UInt8) -> UInt8 {
    return num ^ 130
}

/*: "pic" :*/
fileprivate let user_inputIdent:String = "pbullet"

/*: "msgId" :*/
fileprivate let m_leaseIdent:[Character] = ["m","s","g","I","d"]

/*: "totalMfCoin" :*/
fileprivate let showStatMessage:String = "somethingosomethinga"
fileprivate let userWarningContent:[Character] = ["l","M","f","C","o","i","n"]

/*: "giftId" :*/
fileprivate let m_factMessage:[Character] = ["g","i","f","t","I"]
fileprivate let kDiscPeopleData:String = "calculation"

/*: "giftNum" :*/
fileprivate let dreamMagnitudeTitle:String = "giftNumreading cap will"

/*: "pkgItemsetId" :*/
fileprivate let mainShirtName:String = "info"
fileprivate let app_assetId:[Character] = ["k","g","I","t","e","m","s","e","t","I","d"]

/*: "msg" :*/
fileprivate let showLessStr:[Character] = ["m","s","g"]

/*: "tips" :*/
fileprivate let show_laterPath:String = "TIPS"

/*: "gift" :*/
fileprivate let mainSatisfyKey:String = "GIFT"

/*: "nickname" :*/
fileprivate let noti_motivationTeamText:[Character] = ["n","i","c","k","n"]
fileprivate let mAskTumbleMessage:[Character] = ["a","m","e"]

/*: "headPic" :*/
fileprivate let noti_sequenceUrl:[UInt8] = [0x15,0x18,0x1c,0x19,0x2d,0x14,0x1e]

/*: "All Numbers" :*/
fileprivate let mFocusResultMsg:String = "All Nutogether some you analyze in"

/*: "sendNum" :*/
fileprivate let app_alliancePath:String = "city max daily receivesendNum"

/*: "toUser" :*/
fileprivate let mVesselName:[Character] = ["t","o","U","s","e","r"]

/*: "msgIncome" :*/
fileprivate let m_shakeName:String = "leave inner additional lose wipemsgInco"
fileprivate let kShadowFormat:String = "temp"

/*: "isConnection" :*/
fileprivate let showParaValue:String = "input cookie sustain minuteisCon"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  RaceChatController.swift
//  AbroadTalking
//
//  Created by young on 2023/4/4.
//

//: import UIKit
import UIKit

//: class TalkingGroupChatController: TalkingBaseViewController {
class RaceChatController: FlexibleViewController {
    //: var infoModel = TalkingChatRoomInfoModel()
    var infoModel = AdaptHandyJSON() // 群聊信息
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var tipsTimer: Timer?
    private var tipsTimer: Timer? // 提示消息定时器
    //: private var conversationData = TalkingConversationModel()
    private var conversationData = ModelThen() // 会话cellData
    //: private var conversationType: TalkingIMChatType = .group
    private var conversationType: TalkingIMChatType = .group // 会话类型
    //: private var atFlag = false
    private var atFlag = false // 记录是否@

    //: init(groupType: String, infoData: [String: Any]) {
    init(groupType: String, infoData: [String: Any]) {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
        //: hideNavi = true
        hideNavi = true
        //: if let model = TalkingChatRoomInfoModel.deserialize(from: infoData) {
        if let model = AdaptHandyJSON.deserialize(from: infoData) {
            //: infoModel = model
            infoModel = model
        }
        //: conversationData.groupID = infoModel.roomId
        conversationData.groupID = infoModel.roomId
        //: conversationData.groupType = groupType
        conversationData.groupType = groupType
        //: if groupType == GroupType_Meeting {
        if groupType == GroupType_Meeting {
            //: conversationType = .meeting
            conversationType = .meeting
        }
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: noti_rowMessage.reversed(), encoding: .utf8)!)
    }

    //: override func loadView() {
    override func loadView() {
        //: if #available(iOS 13.2, *) {
        if #available(iOS 13.2, *) {
            //: if AdministratorThen.share.appConfigMode.disableShootScreen {
            if AdministratorThen.share.appConfigMode.disableShootScreen {
                //: super.loadView()
                super.loadView()
                //: } else {
            } else {
                //: let bgView = MAScreenShieldView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
                let bgView = GentleReactiveCompatible(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage))
                //: self.view = bgView
                self.view = bgView
            }
            //: } else {
        } else {
            //: super.loadView()
            super.loadView()
        }
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: createUI()
        appUi()
        //: createUIConstraints()
        masterAcross()
        //: bindInteraction()
        satisfy()
        //: addTapGestureRecognizer()
        donkeyEngine()
        //: func__chatRoom_insertTipMsg()
        colorCreate()
        //: chatSettings()
        speedy()

        //: if conversationType == .private {
        if conversationType == .private {
            //: initCachEffectData()
            connectData()
        }
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
        //: if self.atFlag {
        if self.atFlag {
            //: self.msgInputView.inputTextView.becomeFirstResponder()
            self.msgInputView.inputTextView.becomeFirstResponder()
            //: self.atFlag = false
            self.atFlag = false
        }
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: view.endEditing(true)
        view.endEditing(true)
    }

    //: deinit {
    deinit {
        //: if self.conversationData.groupType == GroupType_Meeting {
        if self.conversationData.groupType == GroupType_Meeting {
            //: V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
            V2TIMManager.sharedInstance().quitGroup(self.conversationData.groupID, succ: nil, fail: nil)
        }
        //: self.func__joinOrQuitRoom(join: false)
        self.sequenceContent(join: false)
        //: self.destroyTipsTimer()
        self.dismantle()
    }

    // MARK: - Lazy Load

    //: private lazy var msgView: TUIMessageController = {
    private lazy var msgView: MessageViewController = {
        //: let msgVC = TUIMessageController.init()
        let msgVC = MessageViewController()
        //: msgVC.delegate = self
        msgVC.delegate = self
        //: msgVC.view.backgroundColor = UIColor.clear
        msgVC.view.backgroundColor = UIColor.clear
        //: msgVC.tableView.backgroundColor = .clear
        msgVC.tableView.backgroundColor = .clear
        //: msgVC.tableView.register(TalkingGroupChatTextMsgCell.self, forCellReuseIdentifier: TalkingGroupChatTextMsgCell.className())
        msgVC.tableView.register(ToneViewCell.self, forCellReuseIdentifier: ToneViewCell.className())
        //: msgVC.tableView.register(TalkingChatImageMsgCell.self, forCellReuseIdentifier: TalkingChatImageMsgCell.className())
        msgVC.tableView.register(TapViewCell.self, forCellReuseIdentifier: TapViewCell.className())
        //: msgVC.tableView.register(TalkingGroupChatGiftMsgCell.self, forCellReuseIdentifier: TalkingGroupChatGiftMsgCell.className())
        msgVC.tableView.register(PositionThen.self, forCellReuseIdentifier: PositionThen.className())
        //: msgVC.tableView.register(TalkingChatTipsMsgCell.self, forCellReuseIdentifier: TalkingChatTipsMsgCell.className())
        msgVC.tableView.register(ExtentView.self, forCellReuseIdentifier: ExtentView.className())
        //: msgVC.tableView.register(TUISystemMessageCell.self, forCellReuseIdentifier: AutoloadingReactiveCompatible.TSystemMessageCell_ReuseId)
        msgVC.tableView.register(LackMessageCell.self, forCellReuseIdentifier: AutoloadingReactiveCompatible.TSystemMessageCell_ReuseId)
        //: msgVC.currentChatType = conversationType
        msgVC.currentChatType = conversationType
        //: return msgVC
        return msgVC
        //: }()
    }()

    //: private lazy var msgInputView: TalkingChatInputView = {
    private lazy var msgInputView: VillageThen = {
        //: let view = TalkingChatInputView(type: .GroupChat)
        let view = VillageThen(type: .GroupChat)
        //: view.chatRoomID = infoModel.roomId
        view.chatRoomID = infoModel.roomId
        //: view.delegate = self
        view.delegate = self
        //: return view
        return view
        //: }()
    }()

    //: private lazy var giftEffectView: TalkingPrivateChatAnimatView = {
    private lazy var giftEffectView: CandidateAnimatView = {
        //: let effectView = TalkingPrivateChatAnimatView.init()
        let effectView = CandidateAnimatView()
        //: effectView.contentMode = .scaleAspectFill
        effectView.contentMode = .scaleAspectFill
        //: return effectView
        return effectView
        //: }()
    }()

    //: private lazy var giftTrackView: TalkingGiftTrackView = {
    private lazy var giftTrackView: VillageReactiveCompatible = {
        //: let trackView = TalkingGiftTrackView.init()
        let trackView = VillageReactiveCompatible()
        //: trackView.isUserInteractionEnabled = false
        trackView.isUserInteractionEnabled = false
        //: return trackView
        return trackView
        //: }()
    }()

    //: private lazy var navView: TalkingChatNavTitleView = {
    private lazy var navView: AdviceReactiveCompatible = {
        //: let navView = TalkingChatNavTitleView(type: conversationType)
        let navView = AdviceReactiveCompatible(type: conversationType)
        //: navView.titleLabel.text = infoModel.name
        navView.titleLabel.text = infoModel.name
        //: return navView
        return navView
        //: }()
    }()

    //: private lazy var someoneView: TalkingSomeoneAtMeView = {
    private lazy var someoneView: TapView = {
        //: let v = TalkingSomeoneAtMeView()
        let v = TapView()
        //: v.isHidden = true
        v.isHidden = true
        //: return v
        return v
        //: }()
    }()

    //: private lazy var mentionInfoArr: [[String: Any]] = {
    private lazy var mentionInfoArr: [[String: Any]] = //: return [[String: Any]]()
        .init()
    //: }()
}

// MARK: - Load Data

//: extension TalkingGroupChatController {
extension RaceChatController {
    /// 群聊设置
    //: private func chatSettings() {
    private func speedy() {
        //: TalkingGroupChatManager.share.chatRoomVC = self
        PoorReactiveCompatible.share.chatRoomVC = self

        //: func__joinOrQuitRoom(join: true)
        sequenceContent(join: true)
    }

    /// 加入或退出聊天室
    /// - Parameter join: 是否加入
    //: private func func__joinOrQuitRoom(join: Bool) {
    private func sequenceContent(join: Bool) {
        //: let params = ["roomId": self.conversationData.groupID, "type": (join == true ? 1:2)] as [String: Any]
        let params = [(String(appLipValue)): self.conversationData.groupID, String(bytes: mGatherValue.map{engagementWhatRet(circle: $0)}, encoding: .utf8)!: join == true ? 1 : 2] as [String: Any]
        //: TalkingChatRequestTool.req_chatRoomJoinOrQuit(params: params) { succeed, result, errorModel in
        ShrinkReactiveCompatible.roundEx(params: params) { succeed, _, _ in
            //: guard succeed else { return }
            guard succeed else { return }
//            guard let data = result as? [String: Any] else {
//                return
//            }
//
//            if join {
//
//            }
        }
    }
}

// MARK: - StudyHypothesisDelegate

//: extension TalkingGroupChatController: TMessageControllerDelegate {
extension RaceChatController: StudyHypothesisDelegate {
    //: func messageController(_ controller: TUIMessageController!, onNewMessage data: V2TIMMessage!) -> ReconstructThen! {
    func change(_: MessageViewController!, counterest data: V2TIMMessage!) -> ReconstructThen! {
        //: let dic = ManagerPubliclyThen.parseTXMessageData(data: data.customElem.data)
        let dic = ManagerPubliclyThen.recover(data: data.customElem.data)
        //: let cellData = TUISocialChatManager.handleTXReceiveMessage(withExtral: dic, cusData: data.localCustomData, tempModel: data)
        let cellData = AdvocatePatentThen.more(withExtral: dic, cusData: data.localCustomData, tempModel: data)

        //: return cellData
        return cellData
    }

    //: func messageController(_ controller: TUIMessageController!, onShowMessageData data: ReconstructThen!) -> AutoloadingReactiveCompatible! {
    func message(_: MessageViewController!, observer data: ReconstructThen!) -> AutoloadingReactiveCompatible! {
        //: if data.isKind(of: ConnectThen.self) {
        if data.isKind(of: ConnectThen.self) {
            //: let cell = TalkingGroupChatTextMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatTextMsgCell.className())
            let cell = ToneViewCell(style: .default, reuseIdentifier: ToneViewCell.className())
            //: let cellData = data as! ConnectThen
            let cellData = data as! ConnectThen
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: cellData.atBlock = { [weak self] uid in
            cellData.atBlock = { [weak self] uid in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.menu_goToUserDetailController(uid: uid)
                self.menuJustController(uid: uid)
            }
            //: cell.transBlock = { [weak self] in
            cell.transBlock = { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.msgView.translateMsg(with: cellData)
                self.msgView.white(cellData)
            }

            //: return cell
            return cell
        }

        //: if data.isKind(of: GageBubbleCellData.self) {
        if data.isKind(of: GageBubbleCellData.self) {
            //: let cell = TalkingChatImageMsgCell.init(style: .default, reuseIdentifier: TalkingChatImageMsgCell.className())
            let cell = TapViewCell(style: .default, reuseIdentifier: TapViewCell.className())
            //: let cellData = data as! GageBubbleCellData
            let cellData = data as! GageBubbleCellData
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingGroupChatGiftMsgCellData.self) {
        if data.isKind(of: BuildCellData.self) {
            //: let cell = TalkingGroupChatGiftMsgCell.init(style: .default, reuseIdentifier: TalkingGroupChatGiftMsgCell.className())
            let cell = PositionThen(style: .default, reuseIdentifier: PositionThen.className())
            //: let cellData = data as! TalkingGroupChatGiftMsgCellData
            let cellData = data as! BuildCellData
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)

            //: return cell
            return cell
        }

        //: if data.isKind(of: TalkingChatTipsMsgCellData.self) {
        if data.isKind(of: DataGatherThen.self) {
            //: let cell = TalkingChatTipsMsgCell.init(style: .default, reuseIdentifier: TalkingChatTipsMsgCell.className())
            let cell = ExtentView(style: .default, reuseIdentifier: ExtentView.className())
            //: let cellData = data as! TalkingChatTipsMsgCellData
            let cellData = data as! DataGatherThen
            //: cell.fill(with: cellData)
            cell.fieldWith(with: cellData)
            //: return cell
            return cell
        }

        //: return nil
        return nil
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageAvatar cell: AutoloadingReactiveCompatible!) {
    func image(_: MessageViewController!, up cell: AutoloadingReactiveCompatible!) {
        //: if let userModel = cell.messageData?.msgModel.user {
        if let userModel = cell.messageData?.msgModel.user {
            //: if userModel.uid == Int(AdministratorThen.share.loginUid) {
            if userModel.uid == Int(AdministratorThen.share.loginUid) {
                //: menu_goToUserDetailController(uid: userModel.uid )
                menuJustController(uid: userModel.uid)
                //: }else{
            } else {
                //: menu_showMunuPopView(user: userModel)
                underCuttingEdge(user: userModel)
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onSelectMessageContent cell: AutoloadingReactiveCompatible!) {
    func orientationOrigin(_: MessageViewController!, makeup cell: AutoloadingReactiveCompatible!) {
        //: if cell .isKind(of: TalkingChatImageMsgCell.self) {
        if cell.isKind(of: TapViewCell.self) { // 图片
            //: let imageCellData = cell.messageData as! GageBubbleCellData
            let imageCellData = cell.messageData as! GageBubbleCellData
            //: let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            let libraryPath = NSSearchPathForDirectoriesInDomains(.cachesDirectory, .userDomainMask, true).first!
            //: var filePath = (libraryPath as NSString).appendingPathComponent(imageFileName)
            var filePath = (libraryPath as NSString).appendingPathComponent(dream_acceptFormat)
            //: filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)
            filePath = (filePath as NSString).appendingPathComponent((imageCellData.imageUrl! as NSString).lastPathComponent)

            //: var model = TalkingMomentPhotoModel.init()
            var model = ExactlyHandyJSON()
            //: if FileManager.default.fileExists(atPath: filePath) {
            if FileManager.default.fileExists(atPath: filePath) {
                //: let data = NSData.init(contentsOfFile: filePath)
                let data = NSData(contentsOfFile: filePath)
                //: model.imagePic = UIImage(data: data! as Data)
                model.imagePic = UIImage(data: data! as Data)
                //: } else {
            } else {
                //: if imageCellData.imageData != nil {
                if imageCellData.imageData != nil {
                    //: model.imagePic = imageCellData.imageData
                    model.imagePic = imageCellData.imageData
                    //: } else {
                } else {
                    //: model.imgUrl =  imageCellData.imageUrl
                    model.imgUrl = imageCellData.imageUrl
                }
            }
            //: var currenModel = TalkingMomentModel.init()
            var currenModel = LogHandyJSON()
            //: var tempArr: [TalkingMomentPhotoModel] = []
            var tempArr: [ExactlyHandyJSON] = []
            //: tempArr.append(model)
            tempArr.append(model)
            //: currenModel.pic = tempArr
            currenModel.pic = tempArr
            //: let vc = TalkingMomentPhotosVC.init(momentModel: currenModel, index: 0, type: .normal)
            let vc = HaveViewController(momentModel: currenModel, index: 0, type: .normal)
            //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
            self.springEquinox()?.navigationController?.pushViewController(vc, animated: true)

            //: } else if cell.isKind(of: TalkingGroupChatTextMsgCell.self) {
        } else if cell.isKind(of: ToneViewCell.self) {
            //: let textCell = cell as! TalkingGroupChatTextMsgCell
            let textCell = cell as! ToneViewCell
            //: let textData = textCell.messageData
            let textData = textCell.messageData
            //: let dic = ManagerPubliclyThen.parseTXMessageData(data: (textData?.innerMessage.customElem.data)!)
            let dic = ManagerPubliclyThen.recover(data: (textData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(k_seriesTitle))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: show_retainData.map{$0^37}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<WhistleTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: DumpPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    DumpPushManager.share.molarityEconomicTheoryCommend(jumpModel: jumpModel)
                }
            }

            //: } else if cell.isKind(of: TalkingChatTipsMsgCell.self) {
        } else if cell.isKind(of: ExtentView.self) {
            //: let tipCell = cell as! TalkingChatTipsMsgCell
            let tipCell = cell as! ExtentView
            //: let tipData = tipCell.messageData
            let tipData = tipCell.messageData
            //: let dic = ManagerPubliclyThen.parseTXMessageData(data: (tipData?.innerMessage.customElem.data)!)
            let dic = ManagerPubliclyThen.recover(data: (tipData?.innerMessage.customElem.data)!)
            //: guard let extraDic = dic["extra"] as? Dictionary<String, Any> else {
            guard let extraDic = dic[(String(k_seriesTitle))] as? [String: Any] else {
                //: return
                return
            }
            //: if extraDic.keys.contains("jumpKey") {
            if extraDic.keys.contains(String(bytes: show_retainData.map{$0^37}, encoding: .utf8)!) {
                //: if let jumpModel = JSONDeserializer<TalkingMessageJumpModel>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                if let jumpModel = JSONDeserializer<WhistleTransformable>.deserializeFrom(dict: extraDic, designatedPath: nil) {
                    //: DumpPushManager.share.func__actionPushForMessage(jumpModel: jumpModel)
                    DumpPushManager.share.molarityEconomicTheoryCommend(jumpModel: jumpModel)
                }
            }
        }
    }

    //: func messageController(_ controller: TUIMessageController!, onRetryMessageContent cell: AutoloadingReactiveCompatible!) {
    func identity(_: MessageViewController!, loadController cell: AutoloadingReactiveCompatible!) {
        //: func__sendPriveteMsg(msgData: cell.messageData!, reSend: true)
        threadMinimum(msgData: cell.messageData!, reSend: true)
    }
}

// MARK: - Event

//: extension TalkingGroupChatController {
extension RaceChatController {
    /// 销毁倒计时
    //: private func destroyTipsTimer() {
    private func dismantle() {
        //: if tipsTimer != nil {
        if tipsTimer != nil {
            //: tipsTimer?.invalidate()
            tipsTimer?.invalidate()
            //: tipsTimer = nil
            tipsTimer = nil
        }
    }

    /// 点击用户头像，展示操作面板
    /// - Parameter user: 用户模型
    //: private func menu_showMunuPopView(user: AbTalkingChatMsgUserModel) {
    private func underCuttingEdge(user: LogModel) {
        //: view.endEditing(true)
        view.endEditing(true)
        //: let vc = TalkingMunuPopView.init(frame: self.view.frame)
        let vc = ChromosomeMappingView(frame: self.view.frame)
        //: vc.title = user.nickname
        vc.title = user.nickname
        //: vc.initwithList(cellTitleList: ["Send Gift".localized, "@", "View info".localized, "Report".localized])
        vc.aList(cellTitleList: [(notiRoleMessage.replacingOccurrences(of: "supposed", with: "S") + String(appInsertName)).localized, "@", (String(userHemValue.prefix(9))).localized, (String(userCandidateKey.prefix(6))).localized])
        //: vc.munuBlock = { [weak self] index, str in
        vc.munuBlock = { [weak self] index, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: if index == 0 {
            if index == 0 {
                //: self.menu_sentGiftAction(user: user)
                self.m(user: user)
                //: } else if index == 1 {
            } else if index == 1 {
                //: self.menu_atTa(user: user)
                self.foreNoMouth(user: user)
                //: } else if index == 2 {
            } else if index == 2 {
                //: self.menu_goToUserDetailController(uid: user.uid)
                self.menuJustController(uid: user.uid)
                //: } else if index == 3 {
            } else if index == 3 {
                //: self.menu_reportAction(uid: String(user.uid))
                self.distinguishObserve(uid: String(user.uid))
            }
        }
    }

    /// 送礼
    /// - Parameter user: 用户模型
    //: private func menu_sentGiftAction(user: AbTalkingChatMsgUserModel) {
    private func m(user: LogModel) {
        //: self.msgInputView.clickgiftBtn()
        self.msgInputView.estimated()
        //: let model = TalkingChatRoomMemberModel()
        let model = LanceTransformable()
        //: model.uid = "\(user.uid)"
        model.uid = "\(user.uid)"
        //: model.nickname = user.nickname
        model.nickname = user.nickname
        //: model.headPic = user.headPic
        model.headPic = user.headPic
        //: model.sex = user.sex
        model.sex = user.sex
        //: model.age = Int(user.age) ?? 0
        model.age = Int(user.age) ?? 0
        //: self.msgInputView.giftView.didClickSelectedRow(model: model)
        self.msgInputView.giftView.resumeAnswer(model: model)
    }

    /// @Ta
    /// - Parameter user: 用户模型
    //: private func menu_atTa(user: AbTalkingChatMsgUserModel) {
    private func foreNoMouth(user: LogModel) {
        //: let dict: [String: Any] = ["uid": String(user.uid), "name": user.nickname]
        let dict: [String: Any] = [String(bytes: main_complexityMessage.map{tipAttractive(off: $0)}, encoding: .utf8)!: String(user.uid), String(bytes: m_externalFormat.map{$0^49}, encoding: .utf8)!: user.nickname]
        //: self.msgInputView.receiveAtInfo(range: nil, dict: dict)
        self.msgInputView.memory(range: nil, dict: dict)
        //: self.msgInputView.inputTextView.becomeFirstResponder()
        self.msgInputView.inputTextView.becomeFirstResponder()
    }

    /// 访问用户详情
    /// - Parameter uid: 用户Id
    //: private func menu_goToUserDetailController(uid: Int) {
    private func menuJustController(uid: Int) {
        //: DumpPushManager.share.func__pushToUserDetailVC(uid: String(uid))
        DumpPushManager.share.belowStreet(uid: String(uid))
    }

    /// 举报
    /// - Parameter uid: 用户Id
    //: private func menu_reportAction(uid: String) {
    private func distinguishObserve(uid: String) {
        //: self.msgInputView.showOtherInputView = true
        self.msgInputView.showOtherInputView = true
        //: let reportView = TalkingReportAlertView.init(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        let reportView = WingView(frame: UIScreen.main.bounds, type: .reportUserType, rId: uid)
        //: reportView.showReportViewIn(view: nil)
        reportView.smartPress(view: nil)
        //: reportView.reportHideBlock = { [weak self] _ in
        reportView.reportHideBlock = { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.msgInputView.showOtherInputView = false
            self.msgInputView.showOtherInputView = false
            //: return
        }
    }
}

// MARK: - 发送消息

//: extension TalkingGroupChatController {
extension RaceChatController {
    /// 发送文本消息
    /// - Parameter msgStr: 文本

    //: internal func func__sendTextMsg(msgStr: String, quoteModel: AbTalkingChatMsgQuoteModel?) {
    func cap(msgStr: String, quoteModel _: PreferConversationModel?) {
        //: let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        let temStr = msgStr.trimmingCharacters(in: CharacterSet.whitespacesAndNewlines)
        //: if temStr.count == 0 {
        if temStr.count == 0 {
            //: func__showStatusBarErrorMsg(showMsg: "Can't send blank message".localized)
            iconThemeBringBackPlace(showMsg: (mainTireFormat + "t sen" + String(dreamForeText.prefix(5)) + "nk me" + k_workerStaffTitle.lowercased()).localized)
            //: return
            return
        }

        //: let uidStr = NSMutableString()
        let uidStr = NSMutableString()
        //: var jumps = [[String: String]]()
        var jumps = [[String: String]]()
        //: if msgInputView.atArr.count > 0 {
        if msgInputView.atArr.count > 0 { // 有@ 消息
            //: msgInputView.atArr.forEach { uid in
            msgInputView.atArr.forEach { uid in
                //: let dict = ["jumpKey": "user", "uid": uid]
                let dict = [String(bytes: show_retainData.map{$0^37}, encoding: .utf8)!: (dream_pathMessage.replacingOccurrences(of: "pitch", with: "us")), String(bytes: main_complexityMessage.map{tipAttractive(off: $0)}, encoding: .utf8)!: uid]
                //: uidStr.append(uid+",")
                uidStr.append(uid + ",")
                //: jumps.append(dict)
                jumps.append(dict)
            }
            //: uidStr.deleteCharacters(in: NSRange(location: uidStr.length-1, length: 1))
            uidStr.deleteCharacters(in: NSRange(location: uidStr.length - 1, length: 1))
        }

        //: var msgInfo: [String: Any] = ["content": temStr, "msgType": "txt", "isGroup": "1"]
        var msgInfo: [String: Any] = [String(bytes: dreamHelloContent.map{padReason(multiple: $0)}, encoding: .utf8)!: temStr, (String(dreamSmoothName.suffix(7))): (String(kCulturalName)), (String(app_smoothName.suffix(7))): "1"]
        //: if jumps.count > 0 {
        if jumps.count > 0 {
            //: msgInfo["jumps"] = jumps
            msgInfo[(app_woodRawIdent.replacingOccurrences(of: "operation", with: "p"))] = jumps
        }

        // 接口参数
        //: var paramsDic: [String: Any] = ["message": temStr,
        var paramsDic: [String: Any] = [String(bytes: main_overIdent.reversed(), encoding: .utf8)!: temStr,
                                        //: "type": "TxtMsg",
                                        String(bytes: mGatherValue.map{engagementWhatRet(circle: $0)}, encoding: .utf8)!: (String(showDirectionStr)),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(appLipValue)): self.infoModel.roomId]
        //: if uidStr.length > 0 {
        if uidStr.length > 0 {
            //: paramsDic["toUid"] = uidStr
            paramsDic[(String(main_connectPath))] = uidStr
        }

        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dream_whetherBeautyCommunicationContent.suffix(7))): msgInfo, String(bytes: user_properlyUrl.map{printingFlat(match: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            threadMinimum(msgData: cellData, reSend: false)
        }
    }

    /// 发送图片消息
    /// - Parameter img: 图片
    //: private func func__sendPictureMsg(img: UIImage) {
    private func doingMy(img: UIImage) {
        //: let imagePath = GageBubbleCellData.getChatImagePath(image: img)
        let imagePath = GageBubbleCellData.courseOfActionChatAuditoryImageGetVisualisation(image: img)
        //: let msgInfo: [String: Any] = ["contentType": "ImgMsg", "msgType": "img", "imageUri": imagePath]
        let msgInfo: [String: Any] = [String(bytes: m_hiMsg.reversed(), encoding: .utf8)!: (appSkinPath.replacingOccurrences(of: "pressure", with: "I") + String(user_betweenMsg.prefix(5))), (String(dreamSmoothName.suffix(7))): (String(mainAvailableMsg)), String(bytes: k_revenueOurId.map{laughPick(event: $0)}, encoding: .utf8)!: imagePath]
        // 接口参数
        //: let paramsDic: [String: Any] = ["pic": imagePath,
        let paramsDic: [String: Any] = [(user_inputIdent.replacingOccurrences(of: "bullet", with: "ic")): imagePath,
                                        //: "type": "ImgMsg",
                                        String(bytes: mGatherValue.map{engagementWhatRet(circle: $0)}, encoding: .utf8)!: (appSkinPath.replacingOccurrences(of: "pressure", with: "I") + String(user_betweenMsg.prefix(5))),
                                        //: "roomId": self.infoModel.roomId]
                                        (String(appLipValue)): self.infoModel.roomId]
        //: let dic = ["msgInfo": msgInfo, "params": paramsDic]
        let dic = [(String(dream_whetherBeautyCommunicationContent.suffix(7))): msgInfo, String(bytes: user_properlyUrl.map{printingFlat(match: $0)}, encoding: .utf8)!: paramsDic]
        //: if let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: dic, isSender: true, isTip: false) {
        if let cellData = AdvocatePatentThen.likeRed(withExtral: dic, isSender: true, isTip: false) {
            //: func__sendPriveteMsg(msgData: cellData, reSend: false)
            threadMinimum(msgData: cellData, reSend: false)
        }
    }

    /// 发送消息
    /// - Parameters:
    ///   - msgData: 消息数据结构
    ///   - reSend: 是否重发【目前未使用该字段】
    //: private func func__sendPriveteMsg(msgData: ReconstructThen, reSend: Bool) {
    private func threadMinimum(msgData: ReconstructThen, reSend _: Bool) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            iconThemeBringBackPlace(showMsg: dream_micData)
            //: return
            return
        }
        // 插入本地
        //: self.msgView.sendCustomMessage(msgData)
        self.msgView.fill(msgData)
        // 处理请求参数
        //: var params: Dictionary<String, Any> = msgData.msgModel.params as! Dictionary<String, Any>
        var params: [String: Any] = msgData.msgModel.params as! [String: Any]
//        params["toUid"] = self.targetId
        //: if params.keys.contains("pic") { // 图片
        if params.keys.contains((user_inputIdent.replacingOccurrences(of: "bullet", with: "ic"))) { // 图片
            //: let imageStr: String = params["pic"] as! String
            let imageStr: String = params[(user_inputIdent.replacingOccurrences(of: "bullet", with: "ic"))] as! String
            //: do {
            do {
                //: let data: Data = try Data.init(contentsOf: URL.init(fileURLWithPath: imageStr))
                let data: Data = try Data(contentsOf: URL(fileURLWithPath: imageStr))
                //: params["pic"] = data
                params[(user_inputIdent.replacingOccurrences(of: "bullet", with: "ic"))] = data
                //: } catch {
            } catch {
                //: printLog(message: error)
                printLog(message: error)
            }
        }

        // 发送消息接口
        //: TalkingChatRequestTool.req_SendRoomMsg(param: params, completion: { succeed, result, errorModel in
        ShrinkReactiveCompatible.directStrong(param: params, completion: { succeed, result, errorModel in
            //: if !succeed {
            if !succeed {
                //: self.msgView.changeCustomMsg(msgData, msgId: nil, msgIncome: NSNumber(value: 0), status: .Msg_Status_Fail)
                self.msgView.lastAll(msgData, barMouth: nil, frameChange: NSNumber(value: 0), exposureProcess: .Msg_Status_Fail)
                
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.tillOnd(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            //: let dataDict = result as! [String: Any]
            let dataDict = result as! [String: Any]
            //: let msgInfo = dataDict["msgInfo"] as! [String: Any]
            let msgInfo = dataDict[(String(dream_whetherBeautyCommunicationContent.suffix(7)))] as! [String: Any]

            // 更新本地消息信息
            //: self.msgView.changeCustomMsg(msgData, msgId: msgInfo["msgId"] as? String, msgIncome: NSNumber(value: 0), status: .Msg_Status_Succ)
            self.msgView.lastAll(msgData, barMouth: msgInfo[(String(m_leaseIdent))] as? String, frameChange: NSNumber(value: 0), exposureProcess: .Msg_Status_Succ)
            
            //: if dataDict.keys.contains("totalMfCoin") {
            if dataDict.keys.contains((showStatMessage.replacingOccurrences(of: "something", with: "t") + String(userWarningContent))) {
                //: let totalMfCoin: NSNumber = dataDict["totalMfCoin"] as! NSNumber
                let totalMfCoin: NSNumber = dataDict[(showStatMessage.replacingOccurrences(of: "something", with: "t") + String(userWarningContent))] as! NSNumber
                //: AdministratorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
                AdministratorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            }
            //: })
        })
    }

    /// 处理发送失败情况
    //: private func dealSendMsgError(errorCode: Int, errorStr: String, isGift: Bool, isResend: Bool) {
    private func tillOnd(errorCode: Int, errorStr: String, isGift _: Bool, isResend _: Bool) {
        //: if errorCode == 50203 {
        if errorCode == 50203 {
            //: guard AdministratorThen.share.loginUserMode.status != 1 else {
            guard AdministratorThen.share.loginUserMode.status != 1 else {
                //: if !errorStr.isEmptyString {
                if !errorStr.isEmptyString {
                    //: func__showStatusBarErrorMsg(showMsg: errorStr)
                    iconThemeBringBackPlace(showMsg: errorStr)
                }
                //: return
                return
            }

            //: DumpPushManager.share.func__jumpToWebRecharge(clickEvent: clickSendingiftsButton, sufficient: false)
            DumpPushManager.share.shapeStyle(clickEvent: user_firstConnectMsg, sufficient: false)
            //: msgInputView.giftView.dismissView()
            msgInputView.giftView.forefrontCurrent()
            //: insertTipMsg_Nobalance(tipStr: errorStr)
            destination(tipStr: errorStr)
            //: }else if errorCode == 10888 {
        } else if errorCode == 10888 {
            //: let view = TalkingLiveVipSubscribePopView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
            let view = ConsumerPopView(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage))
            //: view.show()
            view.bill()
            //: } else {
        } else {
            //: if !errorStr.isEmptyString {
            if !errorStr.isEmptyString {
                //: func__showStatusBarErrorMsg(showMsg: errorStr)
                iconThemeBringBackPlace(showMsg: errorStr)
            }
        }
    }
}

// MARK: - 输入框

//: extension TalkingGroupChatController: ChatInputViewDelegate {
extension RaceChatController: ShrinkViewDelegate {
    //: func func__askforGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {
    func sensorNum(giftModel _: ApprovalModelType, giftNum _: String) {}

    /// 输入框输入内容停顿超过1秒
    //: func func_inputContentIntervalOneSecond() {}
    func sign() {}

    /// 输入框@ 在线列表用户
    //: func func__selectMentionUser(range: NSRange) {
    func night(range: NSRange) {
        //: let vc = TalkingChatRoomOnlineListController()
        let vc = GageEraseReactiveCompatible()
        //: vc.roomID = self.infoModel.roomId
        vc.roomID = self.infoModel.roomId
        //: vc.titleType = .callNumber
        vc.titleType = .callNumber
        //: vc.selectedBlock = {[weak self] dict in
        vc.selectedBlock = { [weak self] dict in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.atFlag = true
            self.atFlag = true
            //: self.msgInputView.receiveAtInfo(range: range, dict: dict)
            self.msgInputView.memory(range: range, dict: dict)
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
        }
        //: self.navigationController?.pushViewController(vc, animated: true)
        self.navigationController?.pushViewController(vc, animated: true)
    }

    //: func func__selectMoreFunAction(actionType: MoreActionType) {
    func mightHaveBeen(actionType: MightComparable) {
        //: switch actionType {
        switch actionType {
        //: case .Photo:
        case .Photo:
            //: func__choicePhotos()
            funPhotos()

        //: default:
        default:
            //: break
            break
        }
    }

    //: func func__sendGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String) {}
    func up(giftModel _: ApprovalModelType, giftNum _: String) {}

    //: func func__sendChatRoomGiftMsg(giftModel: TalkingRoomGiftModel, giftNum: String, model: TalkingChatRoomMemberModel) {
    func duringSequence(giftModel: ApprovalModelType, giftNum: String, model: LanceTransformable) {
        //: let reachability = try? Reachability()
        let reachability = try? Reachability()
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
            iconThemeBringBackPlace(showMsg: dream_micData)
            //: return
            return
        }
        //: var params: Dictionary<String, Any> = [:]
        var params: [String: Any] = [:]
        //: params["toUid"] =  model.uid
        params[(String(main_connectPath))] = model.uid
        //: params["giftId"] = giftModel.gid
        params[(String(m_factMessage) + kDiscPeopleData.replacingOccurrences(of: "calculation", with: "d"))] = giftModel.gid
        //: params["giftNum"] = giftNum
        params[(String(dreamMagnitudeTitle.prefix(7)))] = giftNum
        //: params["roomId"] = infoModel.roomId
        params[(String(appLipValue))] = infoModel.roomId
        //: if giftModel.pkgItemsetId != nil {
        if giftModel.pkgItemsetId != nil {
            //: params["pkgItemsetId"] = giftModel.pkgItemsetId
            params[(mainShirtName.replacingOccurrences(of: "info", with: "p") + String(app_assetId))] = giftModel.pkgItemsetId
        }

        // 送礼接口
        //: TalkingChatRequestTool.req_SendGiftChatRoom(param: params, completion: { succeed, result, errorModel in
        ShrinkReactiveCompatible.switchly(param: params, completion: { succeed, result, errorModel in
            //: guard succeed else {
            guard succeed else {
                //: self.dealSendMsgError(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                self.tillOnd(errorCode: errorModel!.errorCode, errorStr: errorModel!.errorMsg, isGift: true, isResend: false)
                //: return
                return
            }

            // 更新本地礼物背包
            //: if giftModel.pkgItemsetId != nil {
            if giftModel.pkgItemsetId != nil {
                //: self.msgInputView.giftView.bags_removeGiftPackage(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
                self.msgInputView.giftView.beforeAgent(pkgItemsetId: giftModel.pkgItemsetId!, num: Int(giftNum) ?? 0)
            }
            //: self.func__insertGiftMessageWithExtral(extral: result as! Dictionary<String, Any?>, model: model, isFromInputView: true)
            self.calculateView(extral: result as! [String: Any?], model: model, isFromInputView: true)
            //: })
        })
    }

    //: func heightToBottomChanged(heightToBottom: CGFloat) {
    func debased(heightToBottom: CGFloat) {
        //: msgInputView.snp.updateConstraints { make in
        msgInputView.snp.updateConstraints { make in
            //: make.bottom.equalTo(self.view).offset(-heightToBottom)
            make.bottom.equalTo(self.view).offset(-heightToBottom)
        }
        //: someoneView.snp.updateConstraints { make in
        someoneView.snp.updateConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.toBottom(true)
            self.msgView.toBottom(true)
        }
    }

    //: func inputViewHeightChanged(height: CGFloat) {
    func wainscot(height: CGFloat) {
        //: self.msgInputView.snp.updateConstraints { make in
        self.msgInputView.snp.updateConstraints { make in
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: self.view.needsUpdateConstraints()
        self.view.needsUpdateConstraints()
        //: self.view.updateConstraintsIfNeeded()
        self.view.updateConstraintsIfNeeded()
        //: UIView.animate(withDuration: 0.3, animations: {
        UIView.animate(withDuration: 0.3, animations: {
            //: self.view.layoutIfNeeded()
            self.view.layoutIfNeeded()
            //: })
        })
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.3) {
            //: self.msgView.toBottom(true)
            self.msgView.toBottom(true)
        }
    }

    //: @objc override func func__TapGestureRecognizer(_ tap: UITapGestureRecognizer) {
    @objc override func sequence(_ tap: UITapGestureRecognizer) {
        //: super.func__TapGestureRecognizer(tap)
        super.sequence(tap)
    }

    //: func func__starRecordBtnDown() {}
    func match() {}

    //: func func__sendVoiceMsg(voicePath: String, voiceTime: Int) {}
    func requestBy(voicePath _: String, voiceTime _: Int) {}
}

// MARK: - 选择图片

//: extension TalkingGroupChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
extension RaceChatController: UIImagePickerControllerDelegate, UINavigationControllerDelegate {
    //: private func func__choicePhotos() {
    private func funPhotos() {
        //: TalkingPermissionTool.func__openAlbumServiceWithBlock(true) { (isOpen: Bool) in
        AsidePermissionTool.replaceDown(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let vc = TalkingImagePickTool.initImagePickerVc(maxCount: 1, allowPhoto: true, allowVideo: false)
                let vc = GagePickTool.examine(maxCount: 1, allowPhoto: true, allowVideo: false)
                //: vc.modalPresentationStyle = .fullScreen
                vc.modalPresentationStyle = .fullScreen
                //: DumpPushManager.share.func__getCurrentActivityVC()?.present(vc, animated: true)
                DumpPushManager.share.talkVc()?.present(vc, animated: true)
                //: vc.didFinishPickingPhotosHandle = { [weak self] ( photos: Array<UIImage>?,
                vc.didFinishPickingPhotosHandle = { [weak self] (photos: [UIImage]?,
                                                                 //: assets: Array<Any>?,
                                                                 _: [Any]?,
                                                                 //: isSelectOriginalPhoto: Bool?)  in
                                                                 _: Bool?) in
                        //: guard let self = self else {return}
                        guard let self = self else { return }
                        //: if photos!.count > 0 {
                        if photos!.count > 0 {
                            //: self.func__sendPictureMsg(img: photos![0])
                            self.doingMy(img: photos![0])
                        }
                }
            }
        }
    }

    //: private func func__camaraPhotos() {
    private func resignSecond() {
        //: TalkingPermissionTool.func__openCaptureDeviceServiceWithBlock(true) { (isOpen: Bool) in
        AsidePermissionTool.goopBlock(true) { (isOpen: Bool) in
            //: if isOpen {
            if isOpen {
                //: let imagePicker = UIImagePickerController()
                let imagePicker = UIImagePickerController()
                //: imagePicker.sourceType = .camera
                imagePicker.sourceType = .camera
                //: imagePicker.delegate = self
                imagePicker.delegate = self
                //: self.present(imagePicker, animated: true, completion: {})
                self.present(imagePicker, animated: true, completion: {})
            }
        }
    }

    //: func imagePickerController(_ picker: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
    func imagePickerController(_: UIImagePickerController, didFinishPickingMediaWithInfo info: [UIImagePickerController.InfoKey: Any]) {
        //: self.dismiss(animated: true) {
        self.dismiss(animated: true) {
            //: if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
            if let photo = info[UIImagePickerController.InfoKey.originalImage] as! UIImage? {
                //: self.func__sendPictureMsg(img: photo)
                self.doingMy(img: photo)
            }
        }
    }

    //: func imagePickerControllerDidCancel(_ picker: UIImagePickerController) {
    func imagePickerControllerDidCancel(_: UIImagePickerController) {
        //: self.dismiss(animated: true, completion: nil)
        self.dismiss(animated: true, completion: nil)
    }
}

// MARK: - 处理@ 消息

//: extension TalkingGroupChatController {
extension RaceChatController {
    /// 接收到群聊@ 消息
    /// - Parameter info: @消息数据
    //: func handleMentionMsg(info: [String: Any]) {
    func clean(info: [String: Any]) {
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard info["roomId"] as! String == self.conversationData.groupID else { return }
            guard info[(String(appLipValue))] as! String == self.conversationData.groupID else { return }
            //: if self.mentionInfoArr.count >= 5 {
            if self.mentionInfoArr.count >= 5 { // 只保留最新5条@ 消息
                //: self.mentionInfoArr.removeFirst()
                self.mentionInfoArr.removeFirst()
            }
            //: self.mentionInfoArr.append(info)
            self.mentionInfoArr.append(info)
            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
        }
    }
}

// MARK: - 提示消息

//: extension TalkingGroupChatController {
extension RaceChatController {
    //: func func__TipsNotification() {
    func background() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(insertTipsNotif(notification:)),
                                               selector: #selector(happening(notification:)),
                                               //: name: CHAT_TIPS_TEXT_NOTIFICATION,
                                               name: kExplainUrl,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func insertTipsNotif(notification: NSNotification) {
    @objc func happening(notification: NSNotification) {
        //: let userinfo = notification.userInfo as! [String: AnyObject]
        let userinfo = notification.userInfo as! [String: AnyObject]
        //: let celldata: TalkingChatTipsMsgCellData  = userinfo["msg"] as! TalkingChatTipsMsgCellData
        let celldata: DataGatherThen = userinfo[(String(showLessStr))] as! DataGatherThen

        //: self.msgView.uiMsgs.add(celldata)
        self.msgView.uiMsgs.add(celldata)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.toBottom(true)
        self.msgView.toBottom(true)
    }

    /// 插入提示消息
    /// - Parameters:
    ///   - tipStr: 提示消息文本
    ///   - needInsertLocal: 是否需要插入本地数据库（默认：是）
    //: func insertTipMsg_Nobalance(tipStr: String, needInsertLocal: Bool = true) {
    func destination(tipStr: String, needInsertLocal: Bool = true) {
        //: let msgInfo = ["content": tipStr, "msgType": "tips"]
        let msgInfo = [String(bytes: dreamHelloContent.map{padReason(multiple: $0)}, encoding: .utf8)!: tipStr, (String(dreamSmoothName.suffix(7))): (show_laterPath.lowercased())]
        //: var message: ReconstructThen?
        var message: ReconstructThen?
        //: if needInsertLocal == true {
        if needInsertLocal == true { // 需要插入本地
            //: } else {
        } else {
            //: message = TUISocialChatManager.handleTXMessageSend(withExtral: msgInfo, isSender: true, isTip: true)
            message = AdvocatePatentThen.likeRed(withExtral: msgInfo, isSender: true, isTip: true)
        }

        //: guard let message = message else { return }
        guard let message = message else { return }
        //: self.msgView.uiMsgs.add(message)
        self.msgView.uiMsgs.add(message)
        //: self.msgView.tableView.reloadData()
        self.msgView.tableView.reloadData()
        //: self.msgView.toBottom(true)
        self.msgView.toBottom(true)
    }

    /// 公共聊天室插入系统提示消息
    //: func func__chatRoom_insertTipMsg() {
    func colorCreate() {
        // 延时，内存插入
        //: tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: {[weak self] time in
        tipsTimer = Timer.scheduledTimer(withTimeInterval: 5, repeats: false, block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: guard AdministratorThen.share.appUserConfigMode.rTips.count > 0 else { return }
            guard AdministratorThen.share.appUserConfigMode.rTips.count > 0 else { return }
            //: self.insertTipMsg_Nobalance(tipStr: AdministratorThen.share.appUserConfigMode.rTips, needInsertLocal: false)
            self.destination(tipStr: AdministratorThen.share.appUserConfigMode.rTips, needInsertLocal: false)
            //: })
        })
    }
}

// MARK: - 动效

//: extension TalkingGroupChatController {
extension RaceChatController {
    /// 插入礼物消息，播放动效
    /// - Parameters:
    ///   - extral: 送礼接口返回结果
    ///   - isFromInputView: 是否来自输入框送礼入口
    //: func func__insertGiftMessageWithExtral(extral: Dictionary<String, Any?>, model: TalkingChatRoomMemberModel, isFromInputView: Bool) {
    func calculateView(extral: [String: Any?], model: LanceTransformable, isFromInputView: Bool) {
        //: var extralInfo = extral
        var extralInfo = extral
        //: var msgInfoDic: Dictionary<String, Any> = extralInfo["msgInfo"] as! Dictionary<String, Any>
        var msgInfoDic: [String: Any] = extralInfo[(String(dream_whetherBeautyCommunicationContent.suffix(7)))] as! [String: Any]
        /// 用于本地判断celldata类型
        //: msgInfoDic["isGroup"] = "1"
        msgInfoDic[(String(app_smoothName.suffix(7)))] = "1"
        //: msgInfoDic["msgType"] = "gift"
        msgInfoDic[(String(dreamSmoothName.suffix(7)))] = (mainSatisfyKey.lowercased())
        //: extralInfo["msgInfo"] = msgInfoDic
        extralInfo[(String(dream_whetherBeautyCommunicationContent.suffix(7)))] = msgInfoDic

        //: var toUser = ["uid": model.uid,
        var toUser = [String(bytes: main_complexityMessage.map{tipAttractive(off: $0)}, encoding: .utf8)!: model.uid,
                      //: "nickname": model.nickname,
                      (String(noti_motivationTeamText) + String(mAskTumbleMessage)): model.nickname,
                      //: "headPic": model.headPic]
                      String(bytes: noti_sequenceUrl.map{$0^125}, encoding: .utf8)!: model.headPic]

        //: if model.nickname == "All Numbers".localized {
        if model.nickname == (String(mFocusResultMsg.prefix(6)) + "mbers").localized {
            //: if extralInfo.keys.contains("gift") != false {
            if extralInfo.keys.contains((mainSatisfyKey.lowercased())) != false {
                //: let giftdic = extralInfo["gift"] as! Dictionary<String, Any>
                let giftdic = extralInfo[(mainSatisfyKey.lowercased())] as! [String: Any]
                //: toUser["nickname"] = "\(giftdic["sendNum"] ?? "1")"
                toUser[(String(noti_motivationTeamText) + String(mAskTumbleMessage))] = "\(giftdic[(String(app_alliancePath.suffix(7)))] ?? "1")"
            }
        }
        //: extralInfo["toUser"] = toUser
        extralInfo[(String(mVesselName))] = toUser

        //: let cellData = TUISocialChatManager.handleTXMessageSend(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        let cellData = AdvocatePatentThen.likeRed(withExtral: extralInfo as [String: Any], isSender: true, isTip: false)
        //: self.msgView.sendCustomMessage(cellData)
        self.msgView.fill(cellData)
        //: var msgIncome: NSNumber = NSNumber.init(floatLiteral: 0)
        var msgIncome = NSNumber(floatLiteral: 0)
        //: if msgInfoDic.keys.contains("msgIncome") {
        if msgInfoDic.keys.contains((String(m_shakeName.suffix(7)) + kShadowFormat.replacingOccurrences(of: "temp", with: "me"))) {
            //: msgIncome = msgInfoDic["msgIncome"] as! NSNumber
            msgIncome = msgInfoDic[(String(m_shakeName.suffix(7)) + kShadowFormat.replacingOccurrences(of: "temp", with: "me"))] as! NSNumber
        }
        //: self.msgView.changeCustomMsg(cellData, msgId: msgInfoDic["msgId"] as? String, msgIncome: msgIncome, status: .Msg_Status_Succ)
        self.msgView.lastAll(cellData, barMouth: msgInfoDic[(String(m_leaseIdent))] as? String, frameChange: msgIncome, exposureProcess: .Msg_Status_Succ)
        
        //: sendGiftByMeToEfffecct(giftDic: extral as [String: Any], model: model)
        object(giftDic: extral as [String: Any], model: model)
        //: if extralInfo.keys.contains("totalMfCoin") {
        if extralInfo.keys.contains((showStatMessage.replacingOccurrences(of: "something", with: "t") + String(userWarningContent))) {
            //: let totalMfCoin: NSNumber = extralInfo["totalMfCoin"] as! NSNumber
            let totalMfCoin: NSNumber = extralInfo[(showStatMessage.replacingOccurrences(of: "something", with: "t") + String(userWarningContent))] as! NSNumber
            //: AdministratorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
            AdministratorThen.share.loginUserMode.mf_coin = totalMfCoin.stringValue
        }
        //: if isFromInputView {
        if isFromInputView {
            //: msgInputView.giftView.updateGiftInfo(needReload: false, mf_coin: AdministratorThen.share.loginUserMode.mf_coin)
            msgInputView.giftView.stooper(needReload: false, mf_coin: AdministratorThen.share.loginUserMode.mf_coin)
        }
        //: msgInputView.giftView.saveRecentMembers()
        msgInputView.giftView.thinImage()
    }

    //: func initCachEffectData() {
    func connectData() {
        //: giftEffectView.initCachAnimatData(tagetID: infoModel.roomId)
        giftEffectView.cookie(tagetID: infoModel.roomId)
    }

    //: func sendGiftByMeToEfffecct(giftDic: Dictionary<String, Any>, model: TalkingChatRoomMemberModel) {
    func object(giftDic: [String: Any], model: LanceTransformable) {
        //: guard let giftModel = giftEffectView.chatRoomDisposeReceiveGiftMsg(giftMessageDic: (giftDic as NSDictionary) as! Dictionary<String, Any>, model: model) else { return }
        guard let giftModel = giftEffectView.direction(giftMessageDic: (giftDic as NSDictionary) as! [String: Any], model: model) else { return }
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in FlowEditionGiftManager.share.draftItems() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SprinklerSystemTransformable]
        }
        //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel)
        giftTrackView.fenceThanAfter(model: giftModel)

        //: giftEffectView.dealGiftAnimatData(dict: giftDic)
        giftEffectView.impression(dict: giftDic)
    }

    //: @objc func addGiftEffectModelArr(giftArr: Array<Any>) {
    @objc func componentYesterday(giftArr: [Any]) {
        //: if giftTrackView.giftSendItem.count == 0 {
        if giftTrackView.giftSendItem.count == 0 {
            //: let arrM = NSMutableArray.init()
            let arrM = NSMutableArray()
            //: for model in TalkingChatGiftManager.share.getSendingItems() {
            for model in FlowEditionGiftManager.share.draftItems() {
                //: arrM.add(model)
                arrM.add(model)
            }
            //: giftTrackView.giftSendItem = arrM as! [TalkingGiftNumArrModel]
            giftTrackView.giftSendItem = arrM as! [SprinklerSystemTransformable]
        }
        //: for i in 0..<giftArr.count {
        for i in 0 ..< giftArr.count {
            //: let giftModel = giftArr[i]
            let giftModel = giftArr[i]
            //: giftTrackView.func__didReceiveGiftMsgModel(model: giftModel as! TalkingGiftMsgAnimatModel)
            giftTrackView.fenceThanAfter(model: giftModel as! RaceMeasurable)
        }

        //: giftEffectView.addGiftAnimatModelArr(modelArr: giftArr)
        giftEffectView.endemic(modelArr: giftArr)
    }
}

// MARK: - Layout

//: extension TalkingGroupChatController {
extension RaceChatController {
    /// 创建UI
    //: private func createUI() {
    private func appUi() {
        //: self.addChild(msgView)
        self.addChild(msgView)
        //: view.addSubview(msgView.view)
        view.addSubview(msgView.view)
        //: view.addSubview(someoneView)
        view.addSubview(someoneView)
        //: view.addSubview(giftEffectView)
        view.addSubview(giftEffectView)
        //: view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        view.insertSubview(giftTrackView, belowSubview: giftEffectView)
        //: view.addSubview(msgInputView)
        view.addSubview(msgInputView)
        //: view.addSubview(navView)
        view.addSubview(navView)
        //: msgView.targetID = self.conversationData.groupID
        msgView.targetID = self.conversationData.groupID
    }

    /// 约束
    //: private func createUIConstraints() {
    private func masterAcross() {
        //: navView.snp.makeConstraints { make in
        navView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(view)
            make.leading.trailing.top.equalTo(view)
            //: make.height.equalTo(StatusBarNavigationBarHeight)
            make.height.equalTo(appServerSensePath)
        }
        //: msgInputView.snp.makeConstraints { make in
        msgInputView.snp.makeConstraints { make in
            //: make.leading.trailing.bottom.equalTo(view)
            make.leading.trailing.bottom.equalTo(view)
            //: make.height.equalTo(msgInputView.BackViewHeight)
            make.height.equalTo(msgInputView.BackViewHeight)
        }
        //: msgView.view.snp.makeConstraints { make in
        msgView.view.snp.makeConstraints { make in
            //: make.leading.trailing.equalTo(view)
            make.leading.trailing.equalTo(view)
            //: make.top.equalTo(StatusBarNavigationBarHeight)
            make.top.equalTo(appServerSensePath)
            //: make.bottom.equalTo(msgInputView.snp.top)
            make.bottom.equalTo(msgInputView.snp.top)
        }
        //: someoneView.snp.makeConstraints { make in
        someoneView.snp.makeConstraints { make in
            //: make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            make.bottom.equalTo(msgInputView.snp.top).offset(-103)
            //: make.trailing.equalToSuperview()
            make.trailing.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 130, height: 37))
            make.size.equalTo(CGSize(width: 130, height: 37))
        }
        //: giftEffectView.snp.makeConstraints { make in
        giftEffectView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(view)
            make.leading.trailing.top.bottom.equalTo(view)
        }
        //: giftTrackView.snp.makeConstraints { make in
        giftTrackView.snp.makeConstraints { make in
            //: make.leading.equalTo(view)
            make.leading.equalTo(view)
            //: make.height.equalTo(180)
            make.height.equalTo(180)
            //: make.bottom.equalTo(-actualHeight(h: 220)-kDeviceSafeBottomHeight)
            make.bottom.equalTo(-actualHeight(h: 220) - main_showUrl)
        }
    }

    /// 事件绑定
    //: func bindInteraction() {
    func satisfy() {
        // 返回
        //: navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
        navView.backButton.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.navigationController?.popViewController(animated: true)
            self.navigationController?.popViewController(animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 在线列表
        //: navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
        navView.onlineListBtn.rx.tap.subscribe(onNext: { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: let vc = TalkingChatRoomOnlineListController()
            let vc = GageEraseReactiveCompatible()
            //: vc.roomID = self.infoModel.roomId
            vc.roomID = self.infoModel.roomId
            //: vc.titleType = .normal
            vc.titleType = .normal
            //: self.navigationController?.pushViewController(vc, animated: true)
            self.navigationController?.pushViewController(vc, animated: true)
            //: }).disposed(by: disposeBag)
        }).disposed(by: disposeBag)

        // 群聊someone@ me 点击事件
        //: someoneView.mentionBtn.rx.tap
        someoneView.mentionBtn.rx.tap
            //: .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            .throttle(.milliseconds(1000), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] in
            .subscribe(onNext: { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                // 找到当前消息，滚动到指定位置
                //: guard let dict = self.mentionInfoArr.first else { return }
                guard let dict = self.mentionInfoArr.first else { return }
                //: for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                for (index, cellData) in self.msgView.uiMsgs.enumerated() {
                    //: if (cellData as! ReconstructThen).isKind(of: ConnectThen.self) {
                    if (cellData as! ReconstructThen).isKind(of: ConnectThen.self) {
                        //: let groupCellData = cellData as! ConnectThen
                        let groupCellData = cellData as! ConnectThen
                        //: if groupCellData.msgID == dict["msgId"] as! String {
                        if groupCellData.msgID == dict[(String(m_leaseIdent))] as! String {
                            //: self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            self.msgView.tableView.scroll(toRow: UInt(index), inSection: 0, at: .bottom, animated: true)
                            //: self.mentionInfoArr.removeFirst()
                            self.mentionInfoArr.removeFirst()
                            //: self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false:true
                            self.someoneView.isHidden = self.mentionInfoArr.count > 0 ? false : true
                        }
                    }
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 被挤下线时，退出群聊
        //: EdgeMsgListener.shared.rx
        EdgeMsgListener.shared.rx
            //: .observeWeakly(Bool.self, "isConnection")
            .observeWeakly(Bool.self, (String(showParaValue.suffix(5)) + "nection"))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: if value == false {
                if value == false {
                    //: self.navigationController?.popViewController(animated: true)
                    self.navigationController?.popViewController(animated: true)
                }
                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }
}
