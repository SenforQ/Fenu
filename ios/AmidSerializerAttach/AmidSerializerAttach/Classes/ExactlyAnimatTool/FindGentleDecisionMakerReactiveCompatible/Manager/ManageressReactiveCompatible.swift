
//: Declare String Begin

/*: " customElem.data is error" :*/
fileprivate let userReserveValue:[Character] = [" ","c","u","s","t","o","m","E"]
fileprivate let showConstraintTitle:[Character] = ["l","e","m"]
fileprivate let dreamBucketValue:String = "since replacement publish.dat"
fileprivate let k_rewardStr:String = "rrmerge"

/*: "extra" :*/
fileprivate let userCharacterUnableShrinkId:String = "liptra"

/*: "MF:PartyChatSysMsg" :*/
fileprivate let kFeedbackId:String = "place powderMF:Pa"
fileprivate let appInviteTitle:[Character] = ["r","t","y"]
fileprivate let kMysteryFeatureMessage:[Character] = ["C","h","a","t","S","y","s","M","s","g"]

/*: "opType" :*/
fileprivate let main_skinName:[Character] = ["o"]
fileprivate let appKeepKey:[Character] = ["p","T","y","p","e"]

/*: "roomLogout" :*/
fileprivate let user_wireFormat:String = "network if sixthroomLogo"
fileprivate let notiMouthHundredUrl:String = "UT"

/*: "MF:PartyChatWelMsg" :*/
fileprivate let notiVentValue:[Character] = ["M","F",":","P","a","r","t","y","C","h","a","t","W","e"]
fileprivate let mDeclineTaskMessage:String = "lMsgyou big factor less society"

/*: "startLive" :*/
fileprivate let showShrinkSleeveId:[Character] = ["s","t","a","r","t","L","i","v","e"]

/*: "floatingScreen" :*/
fileprivate let show_styleMsg:[UInt8] = [0xab,0xa1,0xa2,0xac,0xb9,0xa4,0xa3,0xaa,0x9e,0xae,0xbf,0xa8,0xa8,0xa3]

/*: "mute" :*/
fileprivate let dream_noMsg:String = "streette"

/*: "uid" :*/
fileprivate let user_orbitPath:String = "scand"

/*: "expireAt" :*/
fileprivate let dream_illegalMessage:String = "exartifact"
fileprivate let user_strengthKey:String = "ireAtoff response"

/*: "unmute" :*/
fileprivate let show_viaNaturalStr:String = "unmutchance"

/*: "MF:LiveChatGiftMsg" :*/
fileprivate let main_saleMessage:[Character] = ["M","F",":","L","i","v"]
fileprivate let mWhatMessage:String = "foster effeCha"
fileprivate let dream_originalStr:String = "attitude res after road waytGiftMsg"

/*: "MF:PartyChatGiftMsg" :*/
fileprivate let show_resistancePath:[Character] = ["M","F",":","P","a"]
fileprivate let app_constraintIdent:[Character] = ["r","t","y","C","h","a","t","G"]
fileprivate let main_minimumName:String = "iftMsghide pan star title"

/*: "gift" :*/
fileprivate let noti_warmStr:[Character] = ["g","i","f","t"]

/*: "imgPreview" :*/
fileprivate let notiTailId:[Character] = ["i","m","g","P","r","e","v","i","e","w"]

/*: "name" :*/
fileprivate let main_qualityId:String = "nampublisher"

/*: "num" :*/
fileprivate let app_ovalStr:String = "mobileum"

/*: "mfBean" :*/
fileprivate let kSingleMsg:[UInt8] = [0x92,0x99,0xbd,0x9a,0x9e,0x91]

private func possibilityRobot(treat num: UInt8) -> UInt8 {
    return num ^ 255
}

/*: "MF:LiveChatPrizeMsg" :*/
fileprivate let dreamSquareKey:[Character] = ["M","F",":","L","i","v","e","C","h","a","t","P","r","i"]
fileprivate let user_audienceValue:String = "ceremony primary linkzeMsg"

/*: "<at>@[\\S\\s]+?</at>" :*/
fileprivate let m_shapeChoicePath:[Character] = ["<","a","t",">","@","[","\\","S"]
fileprivate let appDynamicsContent:String = "\\s]"
fileprivate let main_domainUrl:String = "estimated my balance+?</at>"

/*: "加入弹幕房间" :*/
fileprivate let k_windIdent:String = "加入弹幕房间"

/*: "Failed to enter the chat room. Please try again later" :*/
fileprivate let noti_spotMsg:[UInt8] = [0x92,0xb5,0xbd,0xb8,0xb1,0xb0,0xf4,0xa0,0xbb,0xf4,0xb1,0xba,0xa0,0xb1,0xa6,0xf4,0xa0,0xbc,0xb1,0xf4,0xb7,0xbc,0xb5,0xa0,0xf4,0xa6,0xbb,0xbb,0xb9,0xfa,0xf4,0x84,0xb8,0xb1,0xb5,0xa7,0xb1,0xf4,0xa0,0xa6,0xad,0xf4,0xb5,0xb3,0xb5,0xbd,0xba,0xf4,0xb8,0xb5,0xa0,0xb1,0xa6]

/*: "live/sendMsg" :*/
fileprivate let mLessStr:String = "live/senothers illegal expression first above"
fileprivate let app_iconMessage:String = "dMsgbeginning media attribute"

/*: "groupId" :*/
fileprivate let dream_demonstrateValue:String = "forward spark value choose foodgroupId"

/*: "message" :*/
fileprivate let userFoodMsg:[Character] = ["m"]
fileprivate let app_maneText:String = "importantsage"

/*: "toUid" :*/
fileprivate let user_clotheStr:String = "toUidcategory regional don put"

/*: "party/sendMsg" :*/
fileprivate let show_assetPath:[Character] = ["p","a","r","t","y","/","s","e"]
fileprivate let noti_alterId:[Character] = ["n","d","M","s","g"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ManageressReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/7/7.
//

//: import UIKit
import UIKit

//: protocol TalkingDanmuManagerDelegate: NSObject {
protocol VisitorThen: NSObject {
    /// 弹幕消息
    //: func func__DanmuRecvNewModel(Msg: TalkingLiveRoomDanmuModel)
    func recvAcross(Msg: AccountMeasurable)
    /// 礼物动效
    //: func func__giftRecvNewModel(Msg: TalkingLiveRoomDanmuModel)
    func empty(Msg: AccountMeasurable)

    //: func func__actionUserNewModel(pushUid: String?)
    func timeConstant(pushUid: String?)

    //: func func__longTouchUserNewModel(nickName: String, atUid: String?)
    func join(nickName: String, atUid: String?)
    // 用户进入房间
    //: func func__userLogin()
    func injunction()
    // 用户退出房间
    //: func func__userLogout()
    func uniformPartner()
}

//: class TalkingDanmuManager: NSObject {
class ManageressReactiveCompatible: NSObject {
    //: private var maxJoinRoom = 1
    private var maxJoinRoom = 1 // 重试加入弹幕房间次数
    //: private static var _instance: TalkingDanmuManager?
    private static var _instance: ManageressReactiveCompatible? // singleton
    //: open weak var delegate: TalkingDanmuManagerDelegate?
    open weak var delegate: VisitorThen?

    //: override private init() {}
    override private init() {}
    //: class func shared() -> TalkingDanmuManager {
    class func preserve() -> ManageressReactiveCompatible {
        //: guard _instance != nil else {
        guard _instance != nil else {
            //: _instance = TalkingDanmuManager()
            _instance = ManageressReactiveCompatible()
            //: return _instance!
            return _instance!
        }
        //: return _instance!
        return _instance!
    }
}

// MARK: - 展示消息通知

//: extension TalkingDanmuManager {
extension ManageressReactiveCompatible {
    /// 公告消息
    //: func showAnnouncementMsg(str: String) {
    func contain(str: String) {
        //: var model = TalkingLiveRoomDanmuModel()
        var model = AccountMeasurable()
        //: var msgInfo = TalkingDanmuMsgInfo()
        var msgInfo = WaitPubMsgInfo()
        //: msgInfo.messageType = 1
        msgInfo.messageType = 1 /// (公告算是特殊的文本消息)
        //: msgInfo.content = str
        msgInfo.content = str
        //: model.msgInfo = msgInfo
        model.msgInfo = msgInfo
        //: let celldata = TalkingLiveRoomDammuCellData()
        let celldata = CompensationThen()
        //: model = celldata.showAnnouncementMsg(model: model)
        model = celldata.priority(model: model)
        //: delegate?.func__DanmuRecvNewModel(Msg: model)
        delegate?.recvAcross(Msg: model)
    }

    /// 处理接收到的弹幕消息，展示
    /// - Parameters:
    ///   - msg: V2TIMMessage
    //: func onRecvDanmuNewMsg(msg: V2TIMMessage) {
    func chinPolicy(msg: V2TIMMessage) {
        //: guard let extra = String(data: msg.customElem.data, encoding: .utf8) else {
        guard let extra = String(data: msg.customElem.data, encoding: .utf8) else {
            //: printLog(message: " customElem.data is error")
            printLog(message: (String(userReserveValue) + String(showConstraintTitle) + String(dreamBucketValue.suffix(4)) + "a is e" + k_rewardStr.replacingOccurrences(of: "merge", with: "or")))
            //: return
            return
        }
        //: let jsonData: Data = extra.data(using: .utf8)!
        let jsonData: Data = extra.data(using: .utf8)!
        //: let dict = try? JSONSerialization.jsonObject(with: jsonData, options: .mutableContainers) as? NSDictionary
        let dict = try? JSONSerialization.jsonObject(with: jsonData, options: .mutableContainers) as? NSDictionary
        //: let extraDic = dict?["extra"] as? [String: Any]
        let extraDic = dict?[(userCharacterUnableShrinkId.replacingOccurrences(of: "lip", with: "ex"))] as? [String: Any]
        //: let ext = msg.customElem.extension
        let ext = msg.customElem.extension

        //: if ext == "MF:PartyChatSysMsg", extraDic?["opType"] as? String == "roomLogout" { // 语聊房退房操作
        if ext == (String(kFeedbackId.suffix(5)) + String(appInviteTitle) + String(kMysteryFeatureMessage)), extraDic?[(String(main_skinName) + String(appKeepKey))] as? String == (String(user_wireFormat.suffix(8)) + notiMouthHundredUrl.lowercased()) { // 语聊房退房操作
            //: self.delegate?.func__userLogout()
            self.delegate?.uniformPartner()
            //: return
            return

                    //: } else if ext == "MF:PartyChatWelMsg" { // 语聊房进入欢迎消息
        } else if ext == (String(notiVentValue) + String(mDeclineTaskMessage.prefix(4))) { // 语聊房进入欢迎消息
            //: self.delegate?.func__userLogin()
            self.delegate?.injunction()

            //: } else if let opType = extraDic?["opType"], opType as? String == "startLive" {
        } else if let opType = extraDic?[(String(main_skinName) + String(appKeepKey))], opType as? String == (String(showShrinkSleeveId)) {
            //: NotificationCenter.default.post(name: LIVE_RECIVE_STARTLIVE_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: mCleanPath, object: nil)
            //: return
            return

                    //: } else if let opType = extraDic?["opType"], opType as? String == "floatingScreen" { // 飘屏
        } else if let opType = extraDic?[(String(main_skinName) + String(appKeepKey))], opType as? String == String(bytes: show_styleMsg.map{$0^205}, encoding: .utf8)! { // 飘屏
            //: if let dict = extraDic?["floatingScreen"] {
            if let dict = extraDic?[String(bytes: show_styleMsg.map{$0^205}, encoding: .utf8)!] {
                //: NotificationCenter.default.post(name: LIVE_RECIVE_FLOATSCREEN_NOTIFICATION, object: nil, userInfo: ["floatingScreen": dict])
                NotificationCenter.default.post(name: app_featureName, object: nil, userInfo: [String(bytes: show_styleMsg.map{$0^205}, encoding: .utf8)!: dict])
            }
            //: return
            return

                    //: } else if let opType = extraDic?["opType"], opType as? String == "mute" { // 禁言
        } else if let opType = extraDic?[(String(main_skinName) + String(appKeepKey))], opType as? String == (dream_noMsg.replacingOccurrences(of: "street", with: "mu")) { // 禁言
            //: if let uid = extraDic?["uid"] as? Int {
            if let uid = extraDic?[(user_orbitPath.replacingOccurrences(of: "scan", with: "ui"))] as? Int {
                //: if AdministratorThen.share.loginUserMode.userID == "\(uid)" {
                if AdministratorThen.share.loginUserMode.userID == "\(uid)" {
                    //: if ext == "MF:PartyChatSysMsg" { // 语聊房
                    if ext == (String(kFeedbackId.suffix(5)) + String(appInviteTitle) + String(kMysteryFeatureMessage)) { // 语聊房
                        //: TalkingVoiceRoomManager.shared().partyModel.muteExpireAt = extraDic?["expireAt"] as? Int ?? 0
                        LanceManager.doingResource().partyModel.muteExpireAt = extraDic?[(dream_illegalMessage.replacingOccurrences(of: "artifact", with: "p") + String(user_strengthKey.prefix(5)))] as? Int ?? 0
                        //: }else {
                    } else {
                        //: TalkingLiveManager.shared().liveRoomModel.muteExpireAt = extraDic?["expireAt"] as? Int ?? 0
                        TapBecauseLiveManager.resolutionShared().liveRoomModel.muteExpireAt = extraDic?[(dream_illegalMessage.replacingOccurrences(of: "artifact", with: "p") + String(user_strengthKey.prefix(5)))] as? Int ?? 0
                    }
                }
            }
            //: return
            return

                    //: } else if let opType = extraDic?["opType"], opType as? String == "unmute" { // 解禁
        } else if let opType = extraDic?[(String(main_skinName) + String(appKeepKey))], opType as? String == (show_viaNaturalStr.replacingOccurrences(of: "chance", with: "e")) { // 解禁
            //: if let uid = extraDic?["uid"] as? Int {
            if let uid = extraDic?[(user_orbitPath.replacingOccurrences(of: "scan", with: "ui"))] as? Int {
                //: if AdministratorThen.share.loginUserMode.userID == "\(uid)" {
                if AdministratorThen.share.loginUserMode.userID == "\(uid)" {
                    //: if AdministratorThen.share.loginUserMode.userID == "\(uid)" {
                    if AdministratorThen.share.loginUserMode.userID == "\(uid)" {
                        //: if ext == "MF:PartyChatSysMsg" { // 语聊房
                        if ext == (String(kFeedbackId.suffix(5)) + String(appInviteTitle) + String(kMysteryFeatureMessage)) { // 语聊房
                            //: TalkingVoiceRoomManager.shared().partyModel.muteExpireAt = 0
                            LanceManager.doingResource().partyModel.muteExpireAt = 0
                            //: }else {
                        } else {
                            //: TalkingLiveManager.shared().liveRoomModel.muteExpireAt = 0
                            TapBecauseLiveManager.resolutionShared().liveRoomModel.muteExpireAt = 0
                        }
                    }
                }
            }
            //: return
            return
        }

        //: if var model = TalkingLiveRoomDanmuModel.deserialize(from: extraDic as? Dictionary) {
        if var model = AccountMeasurable.deserialize(from: extraDic as? Dictionary) {
            //: model.MsgExtension = msg.customElem.extension
            model.MsgExtension = msg.customElem.extension

            //: if model.msgInfo?.messageType == 3, model.MsgExtension == "MF:LiveChatGiftMsg" || model.MsgExtension == "MF:PartyChatGiftMsg" {
            if model.msgInfo?.messageType == 3, model.MsgExtension == (String(main_saleMessage) + String(mWhatMessage.suffix(4)) + String(dream_originalStr.suffix(8))) || model.MsgExtension == (String(show_resistancePath) + String(app_constraintIdent) + String(main_minimumName.prefix(6))) {
                //: let dic: Dictionary? = extraDic?["gift"] as? [String: Any]
                let dic: Dictionary? = extraDic?[(String(noti_warmStr))] as? [String: Any]
                //: if dic == nil {
                if dic == nil {
                    //: return
                    return
                }
                //: model.gift?.fromUid = model.user?.uid ?? 0
                model.gift?.fromUid = model.user?.uid ?? 0
                //: model.gift?.fromNickname = model.user?.nickname ?? ""
                model.gift?.fromNickname = model.user?.nickname ?? ""
                //: model.gift?.fromHeadPic = model.user?.headPic ?? ""
                model.gift?.fromHeadPic = model.user?.headPic ?? ""
                //: model.gift?.pid = Int(dic?["id"] as! String)
                model.gift?.pid = Int(dic?["id"] as! String)
                //: model.gift?.giftPic = dic?["imgPreview"] as? String ?? ""
                model.gift?.giftPic = dic?[(String(notiTailId))] as? String ?? ""
                //: model.gift?.pname = dic?["name"] as! String
                model.gift?.pname = dic?[(main_qualityId.replacingOccurrences(of: "publisher", with: "e"))] as! String
                //: if model.gift?.animationTimes ?? 0 <= 0 {
                if model.gift?.animationTimes ?? 0 <= 0 {
                    //: model.gift?.animationTimes = dic?["num"] as? Int ?? 0
                    model.gift?.animationTimes = dic?[(app_ovalStr.replacingOccurrences(of: "mobile", with: "n"))] as? Int ?? 0
                }

                //: if TalkingLiveManager.shared().liveRoomModel.roomId.count > 0 {
                if TapBecauseLiveManager.resolutionShared().liveRoomModel.roomId.count > 0 { // 直播间弹幕礼物(包含男性用户)
                    //: if TalkingLiveManager.shared().config.isSmallMode {
                    if TapBecauseLiveManager.resolutionShared().config.isSmallMode {
                        //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveLiveRoomGiftMsg(giftMessageDic: dict! as NSDictionary)
                        GazetteAnimatTool.shared.inmateReceive(giftMessageDic: dict! as NSDictionary)
                        //: } else {
                    } else {
                        //: delegate?.func__giftRecvNewModel(Msg: model)
                        delegate?.empty(Msg: model)
                    }

                    //: } else if TalkingVoiceRoomManager.shared().isParty && model.gift?.isDisplay == true {
                } else if LanceManager.doingResource().isParty, model.gift?.isDisplay == true { // 语聊房弹幕礼物
                    //: if TalkingVoiceRoomManager.shared().isSmallMode {
                    if LanceManager.doingResource().isSmallMode {
                        //: AbTalkingPrivateChatAnimatTool.shared.disposeReceiveLiveRoomGiftMsg(giftMessageDic: dict! as NSDictionary)
                        GazetteAnimatTool.shared.inmateReceive(giftMessageDic: dict! as NSDictionary)
                        //: } else {
                    } else {
                        //: delegate?.func__giftRecvNewModel(Msg: model)
                        delegate?.empty(Msg: model)
                    }
                }

                //: let mfBean = model.msgInfo?.mfBean
                let mfBean = model.msgInfo?.mfBean
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": mfBean ?? 0.0])
                NotificationCenter.default.post(name: mainScaleText, object: nil, userInfo: [String(bytes: kSingleMsg.map{possibilityRobot(treat: $0)}, encoding: .utf8)!: mfBean ?? 0.0])

                //: if model.gift?.giftPic.count ?? 0 > 0 {
                if model.gift?.giftPic.count ?? 0 > 0 {
                    //: if let newStr = model.gift?.giftPic.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain) {
                    if let newStr = model.gift?.giftPic.replacingOccurrences(of: kKeyInsertValue.generalGame(), with: dreamVoiceMarginStr) {
                        //: URLSession.shared.dataTask(with: URL(string: newStr)!) { data, _, error in
                        URLSession.shared.dataTask(with: URL(string: newStr)!) { data, _, error in
                            //: DispatchQueue.main.async {
                            DispatchQueue.main.async {
                                //: guard let data = data, error == nil, let image = UIImage(data: data) else {
                                guard let data = data, error == nil, let image = UIImage(data: data) else {
                                    //: self.setLiveRoomDammuCellData(danmuModel: model)
                                    self.now(danmuModel: model)
                                    //: return
                                    return
                                }
                                //: model.gift?.giftImg = image
                                model.gift?.giftImg = image
                                //: self.setLiveRoomDammuCellData(danmuModel: model)
                                self.now(danmuModel: model)
                            }
                            //: }.resume()
                        }.resume()
                    }
                }
                //: return
                return
            }
            //: if AdministratorThen.share.appStatus == AppSkinStatus.special.rawValue, model.MsgExtension == "MF:LiveChatPrizeMsg" {
            if AdministratorThen.share.appStatus == BecauseMultiplierTarget.special.rawValue, model.MsgExtension == (String(dreamSquareKey) + String(user_audienceValue.suffix(5))) {
                //: return
                return
            }

            //: setLiveRoomDammuCellData(danmuModel: model)
            now(danmuModel: model)
        }
    }

    //: private func setLiveRoomDammuCellData(danmuModel: TalkingLiveRoomDanmuModel) {
    private func now(danmuModel: AccountMeasurable) {
        //: var model = danmuModel
        var model = danmuModel
        //: let celldata = TalkingLiveRoomDammuCellData()
        let celldata = CompensationThen()
        //: model = celldata.caculateMsgHeight(model: model)
        model = celldata.aboutInsideModel(model: model)
        //: celldata.actionUserBlock = { [weak self] atUid in
        celldata.actionUserBlock = { [weak self] atUid in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.delegate?.func__actionUserNewModel(pushUid: atUid)
            self.delegate?.timeConstant(pushUid: atUid)
        }
        //: celldata.longUserBlock = { [weak self] atUid in
        celldata.longUserBlock = { [weak self] atUid in
            //: guard let self = self else { return }
            guard let self = self else { return }

            //: var uid = String(model.user?.uid ?? 0)
            var uid = String(model.user?.uid ?? 0)
            //: var name = model.user?.nickname ?? ""
            var name = model.user?.nickname ?? ""
            //: if atUid != nil, model.msgInfo?.jumps.count ?? 0 > 0, atUid != AdministratorThen.share.loginUserMode.userID, atUid != String(model.user?.uid ?? 0) {
            if atUid != nil, model.msgInfo?.jumps.count ?? 0 > 0, atUid != AdministratorThen.share.loginUserMode.userID, atUid != String(model.user?.uid ?? 0) {
                //: uid = atUid!
                uid = atUid!
                //: let text = model.msgInfo?.content ?? ""
                let text = model.msgInfo?.content ?? ""
                //: let regex = try! NSRegularExpression(pattern: "<at>@[\\S\\s]+?</at>", options: [])
                let regex = try! NSRegularExpression(pattern: (String(m_shapeChoicePath) + appDynamicsContent + String(main_domainUrl.suffix(7))), options: [])
                //: let result = regex.matches(in: text, options: [], range: NSRange(location: 0, length: text.utf16.count))
                let result = regex.matches(in: text, options: [], range: NSRange(location: 0, length: text.utf16.count))
                //: let matchRange = result.first?.range
                let matchRange = result.first?.range
                //: if matchRange?.length ?? 0 > 10 {
                if matchRange?.length ?? 0 > 10 {
                    //: let subStr = String.substringByUnicodeIndex(str: text, from: (matchRange?.location ?? 0) + 5, to: (matchRange?.length ?? 0) - 5)
                    let subStr = String.pressurePoint(str: text, from: (matchRange?.location ?? 0) + 5, to: (matchRange?.length ?? 0) - 5)
                    //: name = subStr
                    name = subStr
                }
            }

            //: self.delegate?.func__longTouchUserNewModel(nickName: name, atUid: uid)
            self.delegate?.join(nickName: name, atUid: uid)
        }

        //: delegate?.func__DanmuRecvNewModel(Msg: model)
        delegate?.recvAcross(Msg: model)
    }
}

//: extension TalkingDanmuManager {
extension ManageressReactiveCompatible {
    /// 释放单利
    //: class func danmu_releaseAllResource() {
    class func operation() {
        //: if TalkingDanmuManager._instance != nil {
        if ManageressReactiveCompatible._instance != nil {
            //: TalkingDanmuManager._instance = nil
            ManageressReactiveCompatible._instance = nil
        }
    }

    /// 加入直播弹幕
    //: class func joinLiveRoomDanmu(groupId: String, announcementMsg: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    class func duringBalance(groupId: String, announcementMsg: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
        V2TIMManager.sharedInstance().joinGroup(groupId, msg: nil) {
            //: printLog(message: "加入弹幕房间")
            printLog(message: (k_windIdent.capitalized))
            //: if announcementMsg.count > 0 {
            if announcementMsg.count > 0 {
                //: TalkingDanmuManager.shared().showAnnouncementMsg(str: announcementMsg)
                ManageressReactiveCompatible.preserve().contain(str: announcementMsg)
            }
            //: completion?(true)
            completion?(true)

            //: } fail: { _, _ in
        } fail: { _, _ in
            //: if TalkingDanmuManager.shared().maxJoinRoom == 1 {
            if ManageressReactiveCompatible.preserve().maxJoinRoom == 1 {
                //: TalkingDanmuManager.shared().maxJoinRoom = 0
                ManageressReactiveCompatible.preserve().maxJoinRoom = 0
                //: joinLiveRoomDanmu(groupId: groupId, announcementMsg: "")
                duringBalance(groupId: groupId, announcementMsg: "")
                //: } else {
            } else {
                //: TalkingDanmuManager.shared().func__showStatusBarErrorMsg(showMsg: "Failed to enter the chat room. Please try again later".localized)
                ManageressReactiveCompatible.preserve().iconThemeBringBackPlace(showMsg: String(bytes: noti_spotMsg.map{$0^212}, encoding: .utf8)!.localized)
            }
            //: completion?(false)
            completion?(false)
        }
    }

    /// 退出群聊弹幕
    //: class func qiutGroupDanmu(groupId: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
    class func platform(groupId: String, completion: ((_ succeed: Bool) -> Void)? = nil) {
        //: V2TIMManager.sharedInstance().quitGroup(groupId, succ: {
        V2TIMManager.sharedInstance().quitGroup(groupId, succ: {
            //: completion?(true)
            completion?(true)
            //: }, fail: { _, _ in
        }, fail: { _, _ in
            //: completion?(false)
            completion?(false)
            //: })
        })
    }
}

// MARK: - 发送接口

//: extension TalkingDanmuManager {
extension ManageressReactiveCompatible {
    /// 发送文本弹幕消息
    /// - Parameters:
    ///   - groupId: 弹幕群组id
    ///   - message: 消息内容
    ///   - toUid:   @用户的uid
    ///   - completion: 回调
    //: class func uploadToTextMsg(groupId: String, message: String, toUid: String? = "", completion: FinishBlock? = nil) {
    class func paraDown(groupId: String, message: String, toUid: String? = "", completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "live/sendMsg"
        reqModel.requestPath = (String(mLessStr.prefix(8)) + String(app_iconMessage.prefix(4)))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["groupId"] = groupId
        dict[(String(dream_demonstrateValue.suffix(7)))] = groupId
        //: dict["message"] = message
        dict[(String(userFoodMsg) + app_maneText.replacingOccurrences(of: "important", with: "es"))] = message
        //: if toUid?.count ?? 0 > 0 {
        if toUid?.count ?? 0 > 0 {
            //: dict["toUid"] = toUid
            dict[(String(user_clotheStr.prefix(5)))] = toUid
        }
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion?(succeed, result, errorModel)
            completion?(succeed, result, errorModel)
        }
    }

    /// 语聊房发送文本弹幕消息
    /// - Parameters:
    ///   - groupId: 弹幕群组id
    ///   - message: 消息内容
    ///   - toUid:   @用户的uid
    ///   - completion: 回调
    //: class func voiceRoomSendTextMsg(groupId: String, message: String, toUid: String? = "", completion: FinishBlock? = nil) {
    class func priceCompletion(groupId: String, message: String, toUid: String? = "", completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "party/sendMsg"
        reqModel.requestPath = (String(show_assetPath) + String(noti_alterId))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: var dict = [String: Any]()
        var dict = [String: Any]()
        //: dict["groupId"] = groupId
        dict[(String(dream_demonstrateValue.suffix(7)))] = groupId
        //: dict["message"] = message
        dict[(String(userFoodMsg) + app_maneText.replacingOccurrences(of: "important", with: "es"))] = message
        //: if toUid?.count ?? 0 > 0 {
        if toUid?.count ?? 0 > 0 {
            //: dict["toUid"] = toUid
            dict[(String(user_clotheStr.prefix(5)))] = toUid
        }
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion?(succeed, result, errorModel)
            completion?(succeed, result, errorModel)
        }
    }
}
