
//: Declare String Begin

/*: "SOCKET_ADDR_KEY" :*/
fileprivate let notiFollowingMessage:[Character] = ["S","O","C","K","E","T","_"]
fileprivate let noti_healthyMsg:[Character] = ["A","D","D","R","_","K","E","Y"]

/*: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION" :*/
fileprivate let dream_knowPath:[UInt8] = [0x4e,0x52,0x5e,0x56,0x58,0x49,0x42,0x4b,0x54,0x59,0x58,0x52,0x5e,0x55,0x5c,0x49,0x42,0x4f,0x58,0x4c,0x48,0x58,0x4e,0x49,0x5e,0x5c,0x51,0x51,0x42,0x53,0x52,0x49,0x54,0x5b,0x54,0x5e,0x5c,0x49,0x54,0x52,0x53]

/*: "RECIVE_FLOATSCREEN_NOTIFICATION" :*/
fileprivate let main_haveStr:[UInt8] = [0x1d,0xa,0xc,0x6,0x19,0xa,0x10,0x9,0x3,0x0,0xe,0x1b,0x1c,0xc,0x1d,0xa,0xa,0x1,0x10,0x1,0x0,0x1b,0x6,0x9,0x6,0xc,0xe,0x1b,0x6,0x0,0x1]

private func supportPrompt(bottom num: UInt8) -> UInt8 {
    return num ^ 79
}

/*: "isCalling" :*/
fileprivate let app_positiveTitle:[Character] = ["i","s","C","a","l","l"]
fileprivate let show_spendUrl:[Character] = ["i","n","g"]
 
/*: "SOCKET 连接成功." :*/
fileprivate let noti_cooperativeMessage:String = "SOCKET superior vent fresh forehead"
fileprivate let m_itemText:String = "连接成功."

/*: "SOCKET 连接失败, error: :*/
fileprivate let noti_psychologicalTitle:[Character] = ["S","O","C","K","E","T"," "]
fileprivate let m_tagId:[Character] = ["连","接"]
fileprivate let mActiveData:String = "失\u{8d25}, er"

/*: "SOCKET 失败 :*/
fileprivate let app_barMessage:String = "rear"
fileprivate let kOptimisticStr:[Character] = ["O","C","K","E","T"," ","失","败"]

/*: 次, 切换过Socket域名重新尝试，地址： :*/
fileprivate let noti_simultaneouslyValue:String = "\u{6b21}, 切换过Soc"
fileprivate let appCompareDateStr:String = "typical master poseket域名"
fileprivate let user_pullIdent:String = "，\u{5730}址："

/*: "SOCKET 已关闭, code: :*/
fileprivate let m_pinKey:[Character] = ["S","O","C","K","E","T"," ","已","关","闭",","]
fileprivate let dreamGoodRailMessage:String = " code:gift portrait class secure daily"

/*: , reason: :*/
fileprivate let show_busyId:String = ", reasopeople merely plan language"
fileprivate let mainCombinedMuseText:String = "n:quickly location remote"

/*: /?sid= :*/
fileprivate let user_gravityChemistryStr:String = "/?sid=space wind custody not"

/*: &uid= :*/
fileprivate let kAccuracyUrl:[Character] = ["&","u","i","d","="]

/*: &platform=iphone&version= :*/
fileprivate let dreamCheekValue:String = "&plaframe print ego offer"
fileprivate let kCountryName:String = "signature memory witchm=ip"
fileprivate let app_marginColumnId:String = "ion=site street"

/*: &packageId= :*/
fileprivate let mainFoodPath:[Character] = ["&","p","a","c","k","a","g"]
fileprivate let user_travelName:String = "rear moment skirt image mineId="

/*: "1v1server. :*/
fileprivate let showEnablelyName:String = "1v1sersoul how explore"
fileprivate let dream_overallData:String = "ver.site thread style overall up"

/*: .com:7766" :*/
fileprivate let notiBoaIdent:String = "vent pose transform collect.com:"

/*: "43.129.185.194:7767" :*/
fileprivate let dreamOnicialIdent:String = "43.12"
fileprivate let appIndexData:String = "9.185."
fileprivate let mainBallotStr:String = "streamstream6stream"

/*: "SOCKET 网络reachabilityChanged监听，断网关闭." :*/
fileprivate let appKnockButtonName:[UInt8] = [0xeb,0xe7,0xdb,0xe3,0xdd,0xec,0xb8,0x7f,0x55,0x29,0x7f,0x53,0x34,0xa,0xfd,0xf9,0xfb,0x0,0xf9,0xfa,0x1,0x4,0x1,0xc,0x11,0xdb,0x0,0xf9,0x6,0xff,0xfd,0xfc,0x7f,0x33,0x29,0x7d,0x28,0x44,0x87,0x54,0x24,0x7e,0x2e,0x45,0x7f,0x55,0x29,0x7d,0x1d,0x4b,0x81,0x2f,0x45,0xc6]

fileprivate func shipWilling(grant num: UInt8) -> UInt8 {
    let value = Int(num) + 104
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "cmd" :*/
fileprivate let noti_yourselfIdent:String = "cmres"

/*: "ping" :*/
fileprivate let show_busyValue:String = "pifruit"

/*: "callHeartbeat" :*/
fileprivate let app_showWholeStr:String = "dominate chance patient landmarkcallHe"
fileprivate let dreamMaximumId:[Character] = ["a","r","t","b","e","a","t"]

/*: "recievedDate" :*/
fileprivate let main_poseId:String = "recieepic"
fileprivate let dream_woodAskText:[Character] = ["t","e"]

/*: "liveHeartBeat" :*/
fileprivate let main_motivationIdent:String = "liveHevine sale soil interaction judge"
fileprivate let user_pressureSimpleKey:[Character] = ["t"]

/*: "🔗 关闭 socket" :*/
fileprivate let user_filterFormat:[Character] = ["🔗"," ","关","闭"," "]
fileprivate let main_rawPath:String = "socmaximumet"

/*: "The user logs out" :*/
fileprivate let appOutputName:String = "The useek how attention cease jump"
fileprivate let show_fullVisualMessage:String = "gs outrobot cos original involve s"

/*: "SOCKET  :*/
fileprivate let appTickId:String = "SOCKpatentT"
fileprivate let k_satisfyUrl:[Character] = [" "]

/*: 重连. 重试次数： :*/
fileprivate let appEffObserverId:String = "\u{91cd}连. 重试"
fileprivate let main_emptyId:String = "次meet："

/*: "SOCKET 连接正常 不需要重新连接." :*/
fileprivate let show_automaticallySitValueKey:[Character] = ["S"]
fileprivate let show_makeupId:[Character] = ["O","C","K","E","T"," ","连","接","正","常"," ","不","需","\u{8981}","重","新","连","\u{63a5}","."]

/*: "SOCKET 正在连接 不需要重新连接." :*/
fileprivate let app_expressFormat:String = "SOCKEserver"
fileprivate let mSharedAlwaysTitle:[Character] = ["\u{63a5}"," ","\u{4e0d}","需","\u{8981}"]
fileprivate let dreamConceptIdent:[Character] = ["重","\u{65b0}","连","接","."]

/*: "&resume=1" :*/
fileprivate let userMinimizeMessage:String = "&resumconversion successfully mask encounter correct"
fileprivate let dreamOutsideData:[Character] = ["e","=","1"]

/*: "&lang= :*/
fileprivate let user_heartData:String = "&lang=selected command pan bind"

/*: "SOCKET 开始连接, 地址： :*/
fileprivate let show_thresholdName:String = "situation item cut product bitSOCKET 开始"
fileprivate let k_secretMsg:[Character] = ["连","接",","," ","地","址","："]

/*: "onNetDisabled" :*/
fileprivate let k_retirementId:String = "added writing howeveronNe"
fileprivate let user_programData:String = "bleorientation"

/*: "errno" :*/
fileprivate let notiWindData:String = "elikelihoodlikelihoodno"

/*: "data" :*/
fileprivate let appLoopValue:String = "dcancela"

/*: "SOCKET 连接保活cmd: ping." :*/
fileprivate let kSaveCircleName:String = "shipOCKE"
fileprivate let m_anotherTitle:[Character] = ["T"," ","连","接","\u{4fdd}","\u{6d3b}","c","m"]
fileprivate let kOppositeId:String = "disagree duty ban attractived: p"

/*: "SOCKET trtc保活cmd: callHeartbeat." :*/
fileprivate let mainTelevisionFormat:[UInt8] = [0xd4,0xc8,0xc4,0xcc,0xc2,0xd3,0xa7,0xf3,0xf5,0xf3,0xe4,0x63,0x38,0x1a,0x61,0x33,0x3c,0xe4,0xea,0xe3,0xbd,0xa7,0xe4,0xe6,0xeb,0xeb,0xcf,0xe2,0xe6,0xf5,0xf3,0xe5,0xe2,0xe6,0xf3,0xa9]

private func delayHe(spread num: UInt8) -> UInt8 {
    return num ^ 135
}

/*: "SOCKET live保活cmd: liveHeartBeat." :*/
fileprivate let m_restoreText:[UInt8] = [0x9a,0x96,0x8a,0x92,0x8c,0x9b,0x67,0xb3,0xb0,0xbd,0xac,0x2b,0x6,0xe4,0x2d,0xfb,0x2,0xaa,0xb4,0xab,0x81,0x67,0xb3,0xb0,0xbd,0xac,0x8f,0xac,0xa8,0xb9,0xbb,0x89,0xac,0xa8,0xbb,0x75]

fileprivate func accuracyBalloon(group num: UInt8) -> UInt8 {
    let value = Int(num) - 71
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "onP" :*/
fileprivate let noti_wouldIdent:String = "onPvessel treat net field rapidly"

/*: "msg" :*/
fileprivate let showBudgetHeavyMsg:[Character] = ["m","s","g"]

/*: "🔗 处理消息：cmdName: :*/
fileprivate let kNoTitle:[Character] = ["🔗"," ","处","理","消","\u{606f}","：","c","m","d","N"]
fileprivate let user_personMessage:String = "ame:char ion automatically"

/*: , errNo: :*/
fileprivate let main_contactMessage:[Character] = [","," ","e","r","r","N","o",":"]

/*: , msg: :*/
fileprivate let kRoundIdent:String = ", msg:resume party limited what support"

/*: , data: :*/
fileprivate let dream_needId:String = ", datainsert share"
fileprivate let m_enjoyValue:[Character] = [":"]

/*: "SOCKET 接收异常消息：cmdName: :*/
fileprivate let user_spanCoatFormat:String = "SOrich"
fileprivate let notiCosFormat:String = "收异常消"
fileprivate let main_listenEntityName:String = "息：cdescribe index"
fileprivate let noti_listenerData:String = "mdName:scene fact outside who mic"

/*: "onReceivedGift" :*/
fileprivate let notiInstallName:[Character] = ["o","n","R","e","c","e","i","v","e","d","G","i"]
fileprivate let mainSeparateStr:[Character] = ["f","t"]

/*: "onVideoMatch" :*/
fileprivate let main_equallyName:[Character] = ["o","n","V","i","d","e","o","M"]
fileprivate let notiToolData:[Character] = ["a","t","c","h"]

/*: "onRequestCall" :*/
fileprivate let notiFosterData:[Character] = ["o","n"]
fileprivate let userKindWelcomeName:String = "change seek edition currency overlookReques"

/*: "onCancelCall" :*/
fileprivate let notiWindUrl:String = "onCancenative rank expected front"
fileprivate let userPatentDuringFormat:String = "shirt flame visiblelCall"

/*: "onRefuseCall" :*/
fileprivate let show_productionBuryMessage:[Character] = ["o"]
fileprivate let mainInsideValue:String = "privacy barnRef"

/*: "onAcceptCall" :*/
fileprivate let mainTellKey:[Character] = ["o","n","A","c","c","e","p","t","C","a","l","l"]

/*: "onStopCall" :*/
fileprivate let mainFoodKey:String = "onStopspec traffic firm stare allow"
fileprivate let mainDefinitionIdent:[Character] = ["C","a","l","l"]

/*: "freeCallTimes" :*/
fileprivate let dreamContrastKey:[Character] = ["f","r","e","e","C","a"]
fileprivate let user_laterIdent:[Character] = ["l","l","T","i","m","e","s"]

/*: "mfCoin" :*/
fileprivate let dreamReplaceTitle:[Character] = ["m","f","C","o","i","n"]

/*: "onMissCoin" :*/
fileprivate let user_harassmentUrl:String = "owner observe preserveonMiss"

/*: "acceptCall" :*/
fileprivate let show_ionPath:String = "negativecce"

/*: "stopCall" :*/
fileprivate let dream_rutStr:String = "stocorrect"

/*: "requestCall" :*/
fileprivate let dream_suiteMsg:String = "reqtelevisiones"
fileprivate let notiScoreKey:String = "difference fostertCall"

/*: "[Invite you to a video call...]" :*/
fileprivate let dreamAbsoluteStr:[UInt8] = [0xed,0xff,0xd8,0xc0,0xdf,0xc2,0xd3,0x96,0xcf,0xd9,0xc3,0x96,0xc2,0xd9,0x96,0xd7,0x96,0xc0,0xdf,0xd2,0xd3,0xd9,0x96,0xd5,0xd7,0xda,0xda,0x98,0x98,0x98,0xeb]

private func conversationDisc(restriction num: UInt8) -> UInt8 {
    return num ^ 182
}

/*: "type" :*/
fileprivate let notiObjectMessage:String = "tytranslate"

/*: "[Invite you to a voice call...]" :*/
fileprivate let k_stoveName:[UInt8] = [0xbe,0xac,0x8b,0x93,0x8c,0x91,0x80,0xc5,0x9c,0x8a,0x90,0xc5,0x91,0x8a,0xc5,0x84,0xc5,0x93,0x8a,0x8c,0x86,0x80,0xc5,0x86,0x84,0x89,0x89,0xcb,0xcb,0xcb,0xb8]

private func mightTap(orbit num: UInt8) -> UInt8 {
    return num ^ 229
}

/*: "uid" :*/
fileprivate let notiHiddenPath:[Character] = ["u","i","d"]

/*: "nickname" :*/
fileprivate let noti_satisfyUrl:[Character] = ["n","i","c","k","n","a","m","e"]

/*: "headPic" :*/
fileprivate let appPowMsg:[Character] = ["h","e","a","d","P","i","c"]

/*: "onCallResume" :*/
fileprivate let showExhaustUrl:[Character] = ["o","n","C","a","l","l","R","e","s","u","m"]
fileprivate let showBronzeGainUrl:String = "identity"

/*: "cancelCall" :*/
fileprivate let main_mediaStr:[Character] = ["c","a","n","c","e","l","C","a","l","l"]

/*: "refuseCall" :*/
fileprivate let main_mePath:[Character] = ["r","e"]
fileprivate let noti_markValue:String = "what factorfuseCall"

/*: "onCallMotivation" :*/
fileprivate let noti_itStr:String = "onCaflame time"
fileprivate let userMakerSecondTitle:String = "checkat"
fileprivate let show_onceMsg:[Character] = ["i","o","n"]

/*: "onLiveWarning" :*/
fileprivate let dreamSuccessfulMessage:[Character] = ["o","n","L","i","v","e","W","a","r","n","i","n","g"]

/*: "reason" :*/
fileprivate let dreamOriginText:String = "reopson"

/*: "time" :*/
fileprivate let noti_boundaryId:[Character] = ["t","i","m","e"]

/*: "onStopLive" :*/
fileprivate let noti_remoteLowerIdent:String = "onStprotocol laugh"

/*: "onSendProgramGift" :*/
fileprivate let showRecentData:[Character] = ["o","n","S","e","n","d"]
fileprivate let noti_annualValue:[Character] = ["P","r","o","g","r","a"]
fileprivate let kProcedureIdent:[Character] = ["m","G","i","f","t"]

/*: "onGameFs" :*/
fileprivate let m_maleId:String = "path only the matchonGameF"
fileprivate let mainWedDonKey:[Character] = ["s"]

/*: "broadStatus" :*/
fileprivate let notiQuickName:[Character] = ["b","r"]
fileprivate let userAwayIdent:String = "fit again continue dynamics tempoadS"

/*: "onCallChatMsg" :*/
fileprivate let userThenSampleFormat:[Character] = ["o","n","C"]
fileprivate let show_goingStrongNoneFormat:String = "valid zone lockallCha"

/*: "onNotificationPopupWin" :*/
fileprivate let appPassTitle:[Character] = ["o","n","N","o","t","i","f","i","c","a","t","i","o","n","P","o","p","u","p","W","i","n"]

/*: "onUserBlock" :*/
fileprivate let user_innerMsg:String = "onUserdynamics wood opportunity problem"
fileprivate let main_redText:String = "Blocklock object"

/*: "freeCallWin" :*/
fileprivate let showCuteTitle:[Character] = ["f","r","e","e","C","a"]
fileprivate let dream_perfectUrl:String = "llWincommit help name stream color"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AdviceLandscapeReactiveCompatible.swift
//  AbroadTalking
//
//  Created by young on 2022/9/13.
//

//: import SocketRocket
import SocketRocket
//: import UIKit
import UIKit

// 保活心跳包ping的时间间隔
//: let SOCKET_PING_INTERVAL = 60.0
let userCollectionFlushStr = 60.0
// 视频通话心跳包时间间隔
//: let SOCKET_CALL_INTERVAL = 20.0
let dreamDismissId = 20.0
// 直播心跳包时间间隔
//: let SOCKET_LIVE_INTERVAL = 20.0
let notiTimeId = 20.0
// 失败重试次数
//: let SOCKET_ADDR_RETRY_COUNT = 3
let user_styleFormat = 3
// socket连接超时时间
//: let SOCKET_MANAGER_CONNECT_TIMEOUT = 30.0
let kFlushIdent = 30.0
// socket连接成功缓存key
//: let SOCKET_ADDR_KEY = "SOCKET_ADDR_KEY"
let mainCreateValue = (String(notiFollowingMessage) + String(noti_healthyMsg))

// 错误码
//: let SOCKET_MANAGER_UNCONNECTED_CODE = 5001
let app_noName = 5001
//: let SOCKET_MANAGER_SELF_BUSY_CODE   = 5002
let userImagePath = 5002
//: let SOCKET_WITHOUT_NET_CODE         = 5003
let dream_messageQueryText = 5003
//: let SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE = 1002
let mAskPath = 1002

// 接收方收到音视频通话通知
//: public let SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION = NSNotification.Name(rawValue: "SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION")
public let notiOkStr = NSNotification.Name(rawValue: String(bytes: dream_knowPath.map{$0^29}, encoding: .utf8)!)
// 游戏飘屏通知
//: public let RECIVE_FLOATSCREEN_NOTIFICATION = NSNotification.Name(rawValue: "RECIVE_FLOATSCREEN_NOTIFICATION")
public let noti_blockDescriptionName = NSNotification.Name(rawValue: String(bytes: main_haveStr.map{supportPrompt(bottom: $0)}, encoding: .utf8)!)

// MARK: - protocol

// MARK: - socket失败代理

/// socket收到失败消息
//: protocol TalkingSocketManagerErrorDelegate: NSObjectProtocol {
protocol TwelveNoonObjectProtocol: NSObjectProtocol {
    /// socket收到失败消息
    /// - Parameters:
    ///   - errorNo: 失败编码
    ///   - cmd: 命令
    ///   - msg: 失败信息
    ///  - data: 数据
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any])
    func playerData(errorNo: Int, cmd: String, msg: String, data: [String: Any])
}

// MARK: - 视频速配代理

/// 视频速配代理（男）
//: protocol SocketManagerVideoMatchDelegate: NSObjectProtocol {
protocol PickUpMatchDelegate: NSObjectProtocol {
    /// 视频速配成功
    /// - Parameter data: 速配数据
    //: func socket_match_onVideoMatch(data: [String: Any])
    func animalData(data: [String: Any])

    /// 匹配成功，可以拨打通话
    /// - Parameter data: 速配数据
    //: func socket_match_onRequestCall(data: [String: Any])
    func capAdmin(data: [String: Any])
}

// MARK: - 音视频通话代理

/// 视频通话【拨打方】代理
//: protocol SocketManagerVideoChatCallerDelegate: NSObjectProtocol {
protocol BecauseObjectProtocol: NSObjectProtocol {
    /// 接收方接受了来电
    /// - Parameter data: 数据
    //: func socket_videoChat_acceptCall(data: [String: Any])
    func willData(data: [String: Any])

    /// 接收方挂断了通话（通话中）
    /// - Parameter data: 数据
    //: func socket_videoChat_stopCall(data: [String: Any])
    func freshData(data: [String: Any])

    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_cancelCall(data: [String: Any])
    func pointBlank(data: [String: Any])

    /// 通话余额不足提醒
    /// - Parameters:
    ///   - msg: 提醒信息
    ///   - data: 数据
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any])
    func uniformAdd(msg: String, data: [String: Any])
}

/// 视频通话【接收方】代理
//: @objc protocol SocketManagerVideoChatCalledPartyDelegate: NSObjectProtocol {
@objc protocol ExpectObjectProtocol: NSObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    /// - Parameter data: 数据
    //: func socket_videoChat_refuseCall(data: [String: Any])
    func miniskirt(data: [String: Any])

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    /// - Parameter data: 数据
    //: func socket_videoChat_onAcceptCall(data: [String: Any])
    func judge(data: [String: Any])

    /// 女性通话积分激励动效展示
    /// - Parameters:
    ///   - data: 数据
    //: @objc optional func socket_videoChat_onCallMotivation(data: [String: Any])
    @objc optional func videoChild(data: [String: Any])
}

//: class TalkingSocketManager: NSObject {
class AdviceLandscapeReactiveCompatible: NSObject {
    //: enum SocketRetryType: String {
    enum EscapeType: String {
        //: case AppLogin = "用户登录"
        case AppLogin = "用户登录"
        //: case NetWork = "网络重连"
        case NetWork = "网络重连"
        //: case EnterForeground = "回到APP"
        case EnterForeground = "回到APP"
        //: case SocketFail = "连接失败"
        case SocketFail = "连接失败"
        //: case SocketClose = "连接关闭"
        case SocketClose = "连接关闭"
        //: case SocketSendMsg = "发送消息失败"
        case SocketSendMsg = "发送消息失败"
    }

    /// 音视频最小化
    //: var currFindVC: AnyObject?
    var currFindVC: AnyObject? // 当前正在通话的音视频视图
    //: var videoMiniPosition = CGPoint(
    var videoMiniPosition = CGPoint( // 音视频小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PrintingReactiveCompatible.shared.direction == .rightToLeft ? 0 : noti_senseContent,
        //: y: StatusBarHeight+190
        y: mNameKey + 190
    )
    //: var liveMiniPosition = CGPoint(
    var liveMiniPosition = CGPoint( // 直播小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PrintingReactiveCompatible.shared.direction == .rightToLeft ? 0 : noti_senseContent,
        //: y: StatusBarHeight+190
        y: mNameKey + 190
    )
    //: var partyMiniPosition = CGPoint(
    var partyMiniPosition = CGPoint( // 语聊房小窗口默认位置
        //: x: LanguageManager.shared.direction == .rightToLeft ? 0:ScreenWidth,
        x: PrintingReactiveCompatible.shared.direction == .rightToLeft ? 0 : noti_senseContent,
        //: y: StatusBarHeight+500
        y: mNameKey + 500
    )
    //: var containerView: UIView?
    var containerView: UIView? // 小窗口容器

    /// delegate
    //: weak var errorDelegate: TalkingSocketManagerErrorDelegate?
    weak var errorDelegate: TwelveNoonObjectProtocol? // error !=0 的异常处理
    //: weak var videoMatchDelegate: SocketManagerVideoMatchDelegate?
    weak var videoMatchDelegate: PickUpMatchDelegate? // 视频通话速配
    //: weak var callerDelegate: SocketManagerVideoChatCallerDelegate?
    weak var callerDelegate: BecauseObjectProtocol? // 视频通话【拨打方】
    //: weak var calledPartyDelegate: SocketManagerVideoChatCalledPartyDelegate?
    weak var calledPartyDelegate: ExpectObjectProtocol? // 视频通话【接收方】

    //: private var webSocket: SRWebSocket?
    private var webSocket: SRWebSocket?
    //: private var keepAliveTimer: Timer?
    private var keepAliveTimer: Timer? // socket保活心跳包
    //: private var connectUrlStr: String?
    private var connectUrlStr: String? // socket连接地址
    //: private var linkIndex = 0
    private var linkIndex = 0 // 当前连接地址索引
    //: private var linkAddrArr: [String]?
    private var linkAddrArr: [String]? // 连接地址数组
    //: private var failureCount = 0
    private var failureCount = 0 // 连接失败次数
    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()

    /// talking
    //: var needReconnect = false
    var needReconnect = false // 断网重连socket
    //: @objc dynamic var isTalking = false
    @objc dynamic var isFind = false // 是否视频通话中
    //: @objc dynamic var isCalling = false
    @objc dynamic var isCalling = false // 是否呼叫中
    //: private var talkTimer: Timer?
    private var talkTimer: Timer? // 视频通话心跳包
    //: private var liveTimer: Timer?
    private var liveTimer: Timer? // 直播心跳包
    //: private var unHandleCallData: [String: Any]?
    private var unHandleCallData: [String: Any]? // 在后台收到的接听消息
    //: private var cancelCall = false
    private var cancelCall = false // 拨打方是否停止呼叫（还未打通）
    //: var isFreeCall = false
    var isFreeCall = false // 是否发起免费通话

    // singleton
    //: static let shared = TalkingSocketManager()
    static let shared = AdviceLandscapeReactiveCompatible()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        // 网络状态监听
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(reachabilityChanged(note:)),
                                               selector: #selector(canMulti(note:)),
                                               //: name: .reachabilityChanged,
                                               name: .reachabilityChanged,
                                               //: object: nil)
                                               object: nil)

        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(checkWebsocket),
                                               selector: #selector(beforeAudience),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)

        // 音视频呼叫时
        //: self.rx
        self.rx
            //: .observeWeakly(Bool.self, "isCalling")
            .observeWeakly(Bool.self, (String(app_positiveTitle) + String(show_spendUrl)))
            //: .subscribe(onNext: {(value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ApplicationKingfisherCompatible.shared as! ApplicationKingfisherCompatible).wipeOutTop()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)

        // 音视频通话时
        //: self.rx
        self.rx
            .observeWeakly(Bool.self, "isFind")
            //: .subscribe(onNext: { (value) in
            .subscribe(onNext: { value in
                //: if value == true {
                if value == true {
                    // 上报女性用户busy状态
                    //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
                    (ApplicationKingfisherCompatible.shared as! ApplicationKingfisherCompatible).wipeOutTop()
                }

                //: }).disposed(by: disposeBag)
            }).disposed(by: disposeBag)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

// MARK: - SRWebSocketDelegate

//: extension TalkingSocketManager: SRWebSocketDelegate {
extension AdviceLandscapeReactiveCompatible: SRWebSocketDelegate {
    //: func webSocketDidOpen(_ webSocket: SRWebSocket!) {
    func webSocketDidOpen(_ webSocket: SRWebSocket!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: UploadLogTool.writeLog(msg: "SOCKET 连接成功.")
        ProblemBenchPoundThen.panelDetect(msg: (String(noti_cooperativeMessage.prefix(7)) + m_itemText.capitalized))
        //: sendPing()
        currentPing()
        //: NotificationCenter.default.post(name: SOCKET_IS_OPEN_NOTIFICATION,
        NotificationCenter.default.post(name: user_barPath,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
        // 还原断网重连状态
        //: self.needReconnect = false
        self.needReconnect = false

        //: self.failureCount = 0
        self.failureCount = 0

        // 保存成功连接的记录
        //: if self.linkIndex < self.linkAddrArr?.count ?? 0 {
        if self.linkIndex < self.linkAddrArr?.count ?? 0 {
            //: UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: SOCKET_ADDR_KEY)
            UserDefaults.standard.set(self.linkAddrArr?[self.linkIndex], forKey: mainCreateValue)
        }
        // 创建音视频最小化窗口容器
        //: mini_createContainerView()
        forefrontQuote()
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
    func webSocket(_ webSocket: SRWebSocket!, didFailWithError error: Error!) {
        //: UploadLogTool.writeLog(msg: "SOCKET 连接失败, error:\(error.localizedDescription).")
        ProblemBenchPoundThen.panelDetect(msg: (String(noti_psychologicalTitle) + String(m_tagId) + mActiveData + "ror:") + "\(error.localizedDescription).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: guard let status = AdministratorThen.share.reachability?.connection, status != .unavailable else {
        guard let status = AdministratorThen.share.reachability?.connection, status != .unavailable else {
            //: return
            return
        }
        //: if self.failureCount == SOCKET_ADDR_RETRY_COUNT {
        if self.failureCount == user_styleFormat {
            //: self.failureCount = 0
            self.failureCount = 0
            //: self.linkIndex = (self.linkIndex == 0) ? 1:0
            self.linkIndex = (self.linkIndex == 0) ? 1 : 0
            //: self.connectUrlStr = getWebSocketPath(isRetry: true)
            self.connectUrlStr = becomeRow(isRetry: true)
            //: UploadLogTool.writeLog(msg: "SOCKET 失败\(SOCKET_ADDR_RETRY_COUNT)次, 切换过Socket域名重新尝试，地址：\(connectUrlStr ?? "").")
            ProblemBenchPoundThen.panelDetect(msg: (app_barMessage.replacingOccurrences(of: "rear", with: "S") + String(kOptimisticStr)) + "\(user_styleFormat)" + (noti_simultaneouslyValue + String(appCompareDateStr.suffix(5)) + "\u{91cd}\u{65b0}尝试" + user_pullIdent) + "\(connectUrlStr ?? "").")
            //: } else {
        } else {
            //: self.failureCount += 1
            self.failureCount += 1
        }
        //: self.reconnectWebSocket(timeInterval: 2, type: .SocketFail)
        self.cornerInvitation(timeInterval: 2, type: .SocketFail)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
    func webSocket(_ webSocket: SRWebSocket!, didReceiveMessage message: Any!) {
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }
        //: self.handleReceiveMessage(message: message as? String)
        self.spanEdit(message: message as? String)
    }

    //: func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean: Bool) {
    func webSocket(_ webSocket: SRWebSocket!, didCloseWithCode code: Int, reason: String!, wasClean _: Bool) {
        //: UploadLogTool.writeLog(msg: "SOCKET 已关闭, code:\(code), reason:\(String(describing: reason)).")
        ProblemBenchPoundThen.panelDetect(msg: (String(m_pinKey) + String(dreamGoodRailMessage.prefix(6))) + "\(code)" + (String(show_busyId.prefix(7)) + String(mainCombinedMuseText.prefix(2))) + "\(String(describing: reason)).")
        //: guard self.webSocket == webSocket else { return }
        guard self.webSocket == webSocket else { return }

        //: self.closeWebSocket(userLoginOut: false)
        self.coverProperty(userLoginOut: false)
        //: if code == SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE {
        if code == mAskPath {
            //: return
            return
        }
        //: if let status = AdministratorThen.share.reachability?.connection, status != .unavailable {
        if let status = AdministratorThen.share.reachability?.connection, status != .unavailable {
            //: self.reconnectWebSocket(timeInterval: 5, type: .SocketClose)
            self.cornerInvitation(timeInterval: 5, type: .SocketClose)
        }
    }

    /// 获取socket地址
    //: func getWebSocketPath(isRetry: Bool) -> String? {
    func becomeRow(isRetry: Bool) -> String? {
        //: var linkAddr = ""
        var linkAddr = ""
        //: self.linkAddrArr = AdministratorThen.share.appConfigMode.ws
        self.linkAddrArr = AdministratorThen.share.appConfigMode.ws
        //: if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
        if self.linkAddrArr == nil || self.linkAddrArr?.count == 0 {
            //: self.linkAddrArr = defaultWSArr()
            self.linkAddrArr = accordArr()
        }
        //: let lastAddr = UserDefaults.standard.string(forKey: SOCKET_ADDR_KEY)
        let lastAddr = UserDefaults.standard.string(forKey: mainCreateValue)
        //: if lastAddr == nil || isRetry {
        if lastAddr == nil || isRetry {
            //: if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
            if let temLinkAddr = self.linkAddrArr?[safe: self.linkIndex] {
                //: linkAddr = temLinkAddr
                linkAddr = temLinkAddr
                //: } else {
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
            //: } else {
        } else {
            //: if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
            if let index = self.linkAddrArr!.firstIndex(of: lastAddr!) {
                //: self.linkIndex = index
                self.linkIndex = index
                //: linkAddr = lastAddr!
                linkAddr = lastAddr!
                //: }else{
            } else {
                //: self.linkIndex = 0
                self.linkIndex = 0
                //: linkAddr = self.linkAddrArr!.first!
                linkAddr = self.linkAddrArr!.first!
            }
        }

        //: let uid = AdministratorThen.share.loginUid
        let uid = AdministratorThen.share.loginUid
        //: let sid = AdministratorThen.share.loginSessionId
        let sid = AdministratorThen.share.loginSessionId
        //: let address = "ws://\(linkAddr)/?sid=\(sid)&uid=\(uid)&platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)"
        let address = "ws://\(linkAddr)" + (String(user_gravityChemistryStr.prefix(6))) + "\(sid)" + (String(kAccuracyUrl)) + "\(uid)" + (String(dreamCheekValue.prefix(4)) + "tfor" + String(kCountryName.suffix(4)) + "hone&vers" + String(app_marginColumnId.prefix(4))) + "\(user_dailyName)" + (String(mainFoodPath) + String(user_travelName.suffix(4))) + "\(dreamStreamKey)"
        //: return address
        return address
    }

    /// 默认地址
    //: func defaultWSArr() -> [String] {
    func accordArr() -> [String] {
        //: return Environment_Formal ? ["1v1server.\(ReplaceUrlDomain).com:7766"] : ["43.129.185.194:7767"]
        return notiLicenseServerFormat ? [(String(showEnablelyName.prefix(6)) + String(dream_overallData.prefix(4))) + "\(dreamVoiceMarginStr)" + (String(notiBoaIdent.suffix(5)) + "7766")] : [(dreamOnicialIdent.capitalized + appIndexData.capitalized + "194:" + mainBallotStr.replacingOccurrences(of: "stream", with: "7"))]
    }
}

// MARK: - NotificationCenter

//: extension TalkingSocketManager {
extension AdviceLandscapeReactiveCompatible {
    /// 网络监听通知
    //: @objc func reachabilityChanged(note: Notification) {
    @objc func canMulti(note: Notification) {
        //: let reachability = note.object as? Reachability
        let reachability = note.object as? Reachability
        //: if reachability?.connection == .unavailable {
        if reachability?.connection == .unavailable {
            // 断网, 关闭socket
            //: UploadLogTool.writeLog(msg: "SOCKET 网络reachabilityChanged监听，断网关闭.")
            ProblemBenchPoundThen.panelDetect(msg: String(bytes: appKnockButtonName.map{shipWilling(grant: $0)}, encoding: .utf8)!)
            //: self.closeWebSocket(userLoginOut: false)
            self.coverProperty(userLoginOut: false)
            //: } else {
        } else {
            // 网络原因重连
            //: self.reconnectWebSocket(timeInterval: 2.0, type: .NetWork)
            self.cornerInvitation(timeInterval: 2.0, type: .NetWork)
        }
    }

    /// 检测socket链接状态
    //: @objc func checkWebsocket() {
    @objc func beforeAudience() {
        //: self.reconnectWebSocket(timeInterval: 2.0, type: .EnterForeground)
        self.cornerInvitation(timeInterval: 2.0, type: .EnterForeground)
        // 处理通话逻辑
        //: self.handleCallData()
        self.cleanData()
    }
}

// MARK: - Event

//: extension TalkingSocketManager {
extension AdviceLandscapeReactiveCompatible {
    /// socket保活心跳包
    //: private func sendPing() {
    private func currentPing() {
        //: self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_PING_INTERVAL, block: { timer in
        self.keepAliveTimer = Timer.scheduledTimer(withTimeInterval: userCollectionFlushStr, block: { _ in
            //: self.sendMessage(info: ["cmd": "ping"])
            self.underAccelerate(info: [(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d")): (show_busyValue.replacingOccurrences(of: "fruit", with: "ng"))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 开启视频通话心跳包
    //: private func startTalkPing() {
    private func belowLength() {
        //: endTalkingPing()
        outTotal()

        //: self.talkTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_CALL_INTERVAL, block: {  timer in
        self.talkTimer = Timer.scheduledTimer(withTimeInterval: dreamDismissId, block: { _ in
            /// 不在通话中，不在音视频通话页，没有开启最小化时
            //: if !self.isFind || (self.currentViewController()?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currentViewController()?.isKind(of: TalkingVoiceChatViewController.self) == false ) && (self.currFindVC?.isKind(of: TalkingVideoChatViewController.self) == false  && self.currFindVC?.isKind(of: TalkingVoiceChatViewController.self) == false ) {
            if !self.isFind || (self.springEquinox()?.isKind(of: ExtentRecognizerDelegate.self) == false && self.springEquinox()?.isKind(of: FuturismViewDelegate.self) == false) && (self.currFindVC?.isKind(of: ExtentRecognizerDelegate.self) == false && self.currFindVC?.isKind(of: FuturismViewDelegate.self) == false) {
                //: return
                return
            }
            //: self.sendMessage(info: ["cmd": "callHeartbeat"])
            self.underAccelerate(info: [(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d")): (String(app_showWholeStr.suffix(6)) + String(dreamMaximumId))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束视频通话心跳包
    //: func endTalkingPing() {
    func outTotal() {
        //: if self.talkTimer != nil {
        if self.talkTimer != nil {
            //: self.talkTimer?.invalidate()
            self.talkTimer?.invalidate()
            //: self.talkTimer = nil
            self.talkTimer = nil
        }
    }

    /// 处理通话逻辑
    //: func handleCallData() {
    func cleanData() {
        //: if self.cancelCall == true {
        if self.cancelCall == true { // 拨打方已停止呼叫
            //: self.cancelCall = false
            self.cancelCall = false
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
            //: return
            return
        }

        //: if self.unHandleCallData != nil {
        if self.unHandleCallData != nil {
            //: let recievedTime = self.unHandleCallData!["recievedDate"] as! Date
            let recievedTime = self.unHandleCallData![(main_poseId.replacingOccurrences(of: "epic", with: "v") + "edDa" + String(dream_woodAskText))] as! Date
            //: let currentTime = Date()
            let currentTime = Date()
            //: if currentTime.timeIntervalSince(recievedTime) < VIDEO_1v1_RECIEVED_CALL_TIMEOUT {
            if currentTime.timeIntervalSince(recievedTime) < show_acceptPath {
                //: AdministratorThen.share.recieve1v1TalkRequest(info: self.unHandleCallData!)
                AdministratorThen.share.resultCustom(info: self.unHandleCallData!)
            }
            //: self.unHandleCallData = nil
            self.unHandleCallData = nil
        }
    }
}

// MARK: - 直播心跳包

//: extension TalkingSocketManager {
extension AdviceLandscapeReactiveCompatible {
    /// 开启直播心跳包
    //: func startLivePing() {
    func startDelete() {
        //: endLivePing()
        quantityrinsicFactor()
        //: self.liveTimer = Timer.scheduledTimer(withTimeInterval: SOCKET_LIVE_INTERVAL, block: { timer in
        self.liveTimer = Timer.scheduledTimer(withTimeInterval: notiTimeId, block: { _ in
            //: self.sendMessage(info: ["cmd": "liveHeartBeat"])
            self.underAccelerate(info: [(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d")): (String(main_motivationIdent.prefix(6)) + "artBea" + String(user_pressureSimpleKey))])
            //: }, repeats: true)
        }, repeats: true)
    }

    /// 结束直播心跳包
    //: func endLivePing() {
    func quantityrinsicFactor() {
        //: if self.liveTimer != nil {
        if self.liveTimer != nil {
            //: self.liveTimer?.invalidate()
            self.liveTimer?.invalidate()
            //: self.liveTimer = nil
            self.liveTimer = nil
        }
    }
}

// MARK: - 音视频窗口最小化

//: extension TalkingSocketManager {
extension AdviceLandscapeReactiveCompatible {
    /// 创建窗口最小化容器
    //: private func mini_createContainerView() {
    private func forefrontQuote() {
        //: guard self.containerView == nil else { return }
        guard self.containerView == nil else { return }

        //: let windowView = TalkingMiniContainerView()
        let windowView = ExtentEnableContainerView()
        //: let keyWindow = CreditsThen.getWindow()
        let keyWindow = CreditsThen.captain()
        //: windowView.frame = keyWindow.bounds
        windowView.frame = keyWindow.bounds
        //: windowView.backgroundColor = .clear
        windowView.backgroundColor = .clear
        //: keyWindow.addSubview(windowView)
        keyWindow.addSubview(windowView)
        //: self.containerView = windowView
        self.containerView = windowView
        //: self.containerView?.isHidden = !self.isFind
        self.containerView?.isHidden = !self.isFind
    }

    /// 移除窗口最小化容器
    //: private func mini_removeContainerView() {
    private func tillPlus() {
        //: guard self.containerView != nil else { return }
        guard self.containerView != nil else { return }
        // 防止抢登录时，用户还在直播中/语聊房
        //: guard TalkingLiveManager.shared().isLive == false else { return }
        guard TapBecauseLiveManager.resolutionShared().isLive == false else { return }
        //: guard TalkingVoiceRoomManager.shared().isParty == false else { return }
        guard LanceManager.doingResource().isParty == false else { return }

        //: self.containerView?.removeAllSubviews()
        self.containerView?.removeAllSubviews()
        //: self.containerView?.removeFromSuperview()
        self.containerView?.removeFromSuperview()
        //: self.containerView = nil
        self.containerView = nil
    }
}

// MARK: - Socket

//: extension TalkingSocketManager {
extension AdviceLandscapeReactiveCompatible {
    /// 关闭socket
    /// - Parameter userLoginOut: 是否是用户退出
    //: func closeWebSocket(userLoginOut: Bool) {
    func coverProperty(userLoginOut: Bool) {
        //: printLog(message: "🔗 关闭 socket")
        printLog(message: (String(user_filterFormat) + main_rawPath.replacingOccurrences(of: "maximum", with: "k")))
        //: if self.webSocket != nil {
        if self.webSocket != nil {
            //: self.webSocket?.delegate = nil
            self.webSocket?.delegate = nil
            //: if userLoginOut == true {
            if userLoginOut == true {
                //: self.webSocket?.close(withCode: SOCKET_MANAGER_USER_LOGOUT_CLOSED_CODE, reason: "The user logs out")
                self.webSocket?.close(withCode: mAskPath, reason: (String(appOutputName.prefix(5)) + "ser lo" + String(show_fullVisualMessage.prefix(6))))
                //: } else {
            } else {
                //: self.webSocket?.close()
                self.webSocket?.close()
            }
            //: self.webSocket = nil
            self.webSocket = nil
        }

        //: if self.keepAliveTimer != nil {
        if self.keepAliveTimer != nil {
            //: self.keepAliveTimer?.invalidate()
            self.keepAliveTimer?.invalidate()
            //: self.keepAliveTimer = nil
            self.keepAliveTimer = nil
        }

        //: endTalkingPing()
        outTotal()

        //: if userLoginOut == true {
        if userLoginOut == true {
            //: self.connectUrlStr = nil
            self.connectUrlStr = nil
            // 销毁最小化窗口
            //: mini_removeContainerView()
            tillPlus()
        }
    }

    /// 更新socket连接
    //: func updateWebSocket() {
    func tapSocket() {
        //: self.connectUrlStr = TalkingSocketManager.shared.getWebSocketPath(isRetry: false)
        self.connectUrlStr = AdviceLandscapeReactiveCompatible.shared.becomeRow(isRetry: false)
        //: self.reconnectWebSocket(timeInterval: 0, type: .AppLogin)
        self.cornerInvitation(timeInterval: 0, type: .AppLogin)
    }

    /// 重连socket
    //: private func reconnectWebSocket(timeInterval: TimeInterval, type: SocketRetryType) {
    private func cornerInvitation(timeInterval: TimeInterval, type: EscapeType) {
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + timeInterval) {
            //: UploadLogTool.writeLog(msg: "SOCKET \(type.rawValue)重连. 重试次数：\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            ProblemBenchPoundThen.panelDetect(msg: (appTickId.replacingOccurrences(of: "patent", with: "E") + String(k_satisfyUrl)) + "\(type.rawValue)" + (appEffObserverId + main_emptyId.replacingOccurrences(of: "meet", with: "数")) + "\(self.failureCount), 地址：\(self.connectUrlStr ?? "").")
            //: self.connectWebSocket(urlStr: self.connectUrlStr)
            self.cropStr(urlStr: self.connectUrlStr)
        }
    }

    /// 连接socket
    //: private func connectWebSocket(urlStr: String?) {
    private func cropStr(urlStr: String?) {
        //: guard urlStr?.isEmpty == false else { return }
        guard urlStr?.isEmpty == false else { return }

        //: if urlStr == self.connectUrlStr && self.webSocket != nil && (self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING) {
        if urlStr == self.connectUrlStr, self.webSocket != nil, self.webSocket?.readyState == .OPEN || self.webSocket?.readyState == .CONNECTING {
            //: if (self.webSocket?.readyState == .OPEN) {
            if self.webSocket?.readyState == .OPEN {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接正常 不需要重新连接.")
                ProblemBenchPoundThen.panelDetect(msg: (String(show_automaticallySitValueKey) + String(show_makeupId)))
                //: } else {
            } else {
                //: UploadLogTool.writeLog(msg: "SOCKET 正在连接 不需要重新连接.")
                ProblemBenchPoundThen.panelDetect(msg: (app_expressFormat.replacingOccurrences(of: "server", with: "T") + " 正在连" + String(mSharedAlwaysTitle) + String(dreamConceptIdent)))
            }
            //: return
            return
        }

        //: self.connectUrlStr = urlStr
        self.connectUrlStr = urlStr

        //: var newStr = urlStr!
        var newStr = urlStr!
        //: if self.isFind {
        if self.isFind {
            //: newStr = newStr.appending("&resume=1")
            newStr = newStr.appending((String(userMinimizeMessage.prefix(6)) + String(dreamOutsideData)))
        }
        //: newStr = newStr.appending("&lang=\(TalkingRequestAddrTool.share.interfaceLang)")
        newStr = newStr.appending((String(user_heartData.prefix(6))) + "\(BubbleVisitorReactiveCompatible.share.interfaceLang)")
        //: closeWebSocket(userLoginOut: false)
        coverProperty(userLoginOut: false)

        //: let url = URL(string: newStr)
        let url = URL(string: newStr)
        //: var request = URLRequest(url: url!)
        var request = URLRequest(url: url!)
        //: request.timeoutInterval = SOCKET_MANAGER_CONNECT_TIMEOUT
        request.timeoutInterval = kFlushIdent
        //: self.webSocket = SRWebSocket(urlRequest: request)
        self.webSocket = SRWebSocket(urlRequest: request)
        //: self.webSocket?.delegate = self
        self.webSocket?.delegate = self
        //: self.webSocket?.open()
        self.webSocket?.open()
        //: UploadLogTool.writeLog(msg: "SOCKET 开始连接, 地址：\(newStr).")
        ProblemBenchPoundThen.panelDetect(msg: (String(show_thresholdName.suffix(9)) + String(k_secretMsg)) + "\(newStr).")
    }

    /// 发送消息
    /// - Parameter info: 消息体
    /// - Returns: 发送结果
    //: @discardableResult
    @discardableResult
    //: func sendMessage(info: [String: Any]?) -> Bool {
    func underAccelerate(info: [String: Any]?) -> Bool {
        //: guard info != nil else { return false }
        guard info != nil else { return false }

        // 1. 判断是否有网络连接
        //: if AdministratorThen.share.reachability?.connection == nil || AdministratorThen.share.reachability?.connection == .unavailable {
        if AdministratorThen.share.reachability?.connection == nil || AdministratorThen.share.reachability?.connection == .unavailable {
            //: let info: [String: Any] = ["cmd": "onNetDisabled",
            let info: [String: Any] = [(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d")): (String(k_retirementId.suffix(4)) + "tDisa" + user_programData.replacingOccurrences(of: "orientation", with: "d")),
                                       //: "errno": "\(SOCKET_WITHOUT_NET_CODE)",
                                       (notiWindData.replacingOccurrences(of: "likelihood", with: "r")): "\(dream_messageQueryText)",
                                       //: "data": [:]]
                                       (appLoopValue.replacingOccurrences(of: "cancel", with: "at")): [:]]
            //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: handleReceiveMessage(message: message)
            spanEdit(message: message)
            //: return false
            return false
        }

        // 2. 尝试发送消息
        //: if self.webSocket?.readyState == SRReadyState.OPEN {
        if self.webSocket?.readyState == SRReadyState.OPEN {
            //: let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            let data = try? JSONSerialization.data(withJSONObject: info!, options: [])
            //: let message = String(data: data!, encoding: String.Encoding.utf8)
            let message = String(data: data!, encoding: String.Encoding.utf8)
            //: if info!["cmd"] as? String == "ping" {
            if info![(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d"))] as? String == (show_busyValue.replacingOccurrences(of: "fruit", with: "ng")) {
                //: UploadLogTool.writeLog(msg: "SOCKET 连接保活cmd: ping.")
                ProblemBenchPoundThen.panelDetect(msg: (kSaveCircleName.replacingOccurrences(of: "ship", with: "S") + String(m_anotherTitle) + String(kOppositeId.suffix(4)) + "ing."))
                //: } else if info!["cmd"] as? String == "callHeartbeat" {
            } else if info![(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d"))] as? String == (String(app_showWholeStr.suffix(6)) + String(dreamMaximumId)) {
                //: UploadLogTool.writeLog(msg: "SOCKET trtc保活cmd: callHeartbeat.")
                ProblemBenchPoundThen.panelDetect(msg: String(bytes: mainTelevisionFormat.map{delayHe(spread: $0)}, encoding: .utf8)!)
                //: } else if info!["cmd"] as? String == "liveHeartBeat" {
            } else if info![(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d"))] as? String == (String(main_motivationIdent.prefix(6)) + "artBea" + String(user_pressureSimpleKey)) {
                //: UploadLogTool.writeLog(msg: "SOCKET live保活cmd: liveHeartBeat.")
                ProblemBenchPoundThen.panelDetect(msg: String(bytes: m_restoreText.map{accuracyBalloon(group: $0)}, encoding: .utf8)!)
            }
            //: self.webSocket?.send(message)
            self.webSocket?.send(message)
            //: return true
            return true

            //: } else {
        } else {
            //: if info!["cmd"] as? String == "ping" {
            if info![(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d"))] as? String == (show_busyValue.replacingOccurrences(of: "fruit", with: "ng")) {
                //: let info: [String: Any] = ["cmd": "onP",
                let info: [String: Any] = [(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d")): (String(noti_wouldIdent.prefix(3))),
                                           //: "errno": "\(SOCKET_MANAGER_UNCONNECTED_CODE)",
                                           (notiWindData.replacingOccurrences(of: "likelihood", with: "r")): "\(app_noName)",
                                           //: "data": [:]]
                                           (appLoopValue.replacingOccurrences(of: "cancel", with: "at")): [:]]
                //: let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                let data = try? JSONSerialization.data(withJSONObject: info, options: [])
                //: let message = String(data: data!, encoding: String.Encoding.utf8)
                let message = String(data: data!, encoding: String.Encoding.utf8)
                //: handleReceiveMessage(message: message)
                spanEdit(message: message)
            }
            //: self.reconnectWebSocket(timeInterval: 5.0, type: .SocketSendMsg)
            self.cornerInvitation(timeInterval: 5.0, type: .SocketSendMsg)
            //: return false
            return false
        }
    }

    /// 处理接收到的消息
    /// - Parameter message: 消息体
    //: private func handleReceiveMessage(message: String?) {
    private func spanEdit(message: String?) {
        //: guard message != nil else { return }
        guard message != nil else { return }

        //: let json = JSON(parseJSON: message!)
        let json = JSON(parseJSON: message!)
        //: let cmdName = json["cmd"].stringValue
        let cmdName = json[(noti_yourselfIdent.replacingOccurrences(of: "res", with: "d"))].stringValue
        //: let errorNo = json["errno"].intValue
        let errorNo = json[(notiWindData.replacingOccurrences(of: "likelihood", with: "r"))].intValue
        //: let msg = json["msg"].stringValue
        let msg = json[(String(showBudgetHeavyMsg))].stringValue
        //: let data = json["data"].dictionaryObject ?? [:]
        let data = json[(appLoopValue.replacingOccurrences(of: "cancel", with: "at"))].dictionaryObject ?? [:]

        //: printLog(message: "🔗 处理消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data)")
        printLog(message: (String(kNoTitle) + String(user_personMessage.prefix(4))) + "\(cmdName)" + (String(main_contactMessage)) + "\(errorNo)" + (String(kRoundIdent.prefix(6))) + "\(msg)" + (String(dream_needId.prefix(6)) + String(m_enjoyValue)) + "\(data)")

        // 检测是否需要恢复直播
        //: check_stopOrRestoreLive(cmdName)
        methodOnly(cmdName)

        // 处理异常
        //: guard errorNo == 0 else {
        guard errorNo == 0 else {
            //: UploadLogTool.writeLog(msg: "SOCKET 接收异常消息：cmdName:\(cmdName), errNo:\(errorNo), msg:\(msg), data:\(data).")
            ProblemBenchPoundThen.panelDetect(msg: (user_spanCoatFormat.replacingOccurrences(of: "rich", with: "CK") + "ET 接" + notiCosFormat.capitalized + String(main_listenEntityName.prefix(3)) + String(noti_listenerData.prefix(7))) + "\(cmdName)" + (String(main_contactMessage)) + "\(errorNo)" + (String(kRoundIdent.prefix(6))) + "\(msg)" + (String(dream_needId.prefix(6)) + String(m_enjoyValue)) + "\(data).")
            //: if errorNo == 444 {
            if errorNo == 444 {
                //: AdministratorThen.share.loginUserMode.freeCallTimes = 0
                AdministratorThen.share.loginUserMode.freeCallTimes = 0
                //: } else if errorNo == 60003, !AdministratorThen.share.loginUserMode.isNaUser {
            } else if errorNo == 60003, !AdministratorThen.share.loginUserMode.isNaUser {
                /// 没真人认证时，需要弹窗提示
                //: TalkingPopupWindowManager.shared.faceVerificationPopUpWindow()
                ShrinkWindowManager.shared.pinLap()
                //: return
                return
            }
            //: self.errorDelegate?.socket_didRecieveError(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            self.errorDelegate?.playerData(errorNo: errorNo, cmd: cmdName, msg: msg, data: data)
            //: return
            return
        }

        // 处理正常接收的消息
        //: if cmdName == "onReceivedGift" { // 礼物
        if cmdName == (String(notiInstallName) + String(mainSeparateStr)) { // 礼物
            //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
            NotificationCenter.default.post(name: main_appFormat, object: data)

            //: } else if cmdName == "onVideoMatch" { // 视频速配
        } else if cmdName == (String(main_equallyName) + String(notiToolData)) { // 视频速配
            //: self.videoMatchDelegate?.socket_match_onVideoMatch(data: data)
            self.videoMatchDelegate?.animalData(data: data)

            //: } else if cmdName == "onRequestCall" { // 拨打方发出requestCall回执
        } else if cmdName == (String(notiFosterData) + String(userKindWelcomeName.suffix(6)) + "tCall") { // 拨打方发出requestCall回执
            //: self.isCalling = true
            self.isCalling = true
            // 视频速配 || 音视频通话
            //: self.videoMatchDelegate?.socket_match_onRequestCall(data: data)
            self.videoMatchDelegate?.capAdmin(data: data)

            //: } else if cmdName == "onCancelCall" { // 拨打方发出cancelCall回执，目前无需处理
        } else if cmdName == (String(notiWindUrl.prefix(7)) + String(userPatentDuringFormat.suffix(5))) { // 拨打方发出cancelCall回执，目前无需处理
            //: } else if cmdName == "onRefuseCall" { // 接收方发送refuseCall回执，目前无需处理
        } else if cmdName == (String(show_productionBuryMessage) + String(mainInsideValue.suffix(4)) + "useCall") { // 接收方发送refuseCall回执，目前无需处理
            //: } else if cmdName == "onAcceptCall" { // 接收方发送acceptCall回执
        } else if cmdName == (String(mainTellKey)) { // 接收方发送acceptCall回执
            //: self.isFind = true
            self.isFind = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            belowLength()
            //: self.calledPartyDelegate?.socket_videoChat_onAcceptCall(data: data)
            self.calledPartyDelegate?.judge(data: data)

            //: } else if cmdName == "onStopCall" { // 接收方发送stopCall回执
        } else if cmdName == (String(mainFoodKey.prefix(6)) + String(mainDefinitionIdent)) { // 接收方发送stopCall回执
            //: self.isFind = false
            self.isFind = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            outTotal()
            //: if let freeCallTimes = data["freeCallTimes"] as? Int {
            if let freeCallTimes = data[(String(dreamContrastKey) + String(user_laterIdent))] as? Int {
                //: AdministratorThen.share.loginUserMode.freeCallTimes = freeCallTimes
                AdministratorThen.share.loginUserMode.freeCallTimes = freeCallTimes
            }
            // 更新余额
            //: AdministratorThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            AdministratorThen.share.loginUserMode.mf_coin = json[(appLoopValue.replacingOccurrences(of: "cancel", with: "at"))][(String(dreamReplaceTitle))].stringValue
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: noti_dismissText, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ApplicationKingfisherCompatible.shared as! ApplicationKingfisherCompatible).minCharacter()
            //: } else if cmdName == "onMissCoin" { // 余额不足提醒
        } else if cmdName == (String(user_harassmentUrl.suffix(6)) + "Coin") { // 余额不足提醒
            //: self.callerDelegate?.socket_videoChat_onMissCoin(msg: msg, data: data)
            self.callerDelegate?.uniformAdd(msg: msg, data: data)

            //: } else if cmdName == "acceptCall" { // 接收方接受了来电，改变状态为通话中,开始推拉流
        } else if cmdName == (show_ionPath.replacingOccurrences(of: "negative", with: "a") + "ptCall") { // 接收方接受了来电，改变状态为通话中,开始推拉流
            //: self.isFind = true
            self.isFind = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            belowLength()
            //: self.callerDelegate?.socket_videoChat_acceptCall(data: data)
            self.callerDelegate?.willData(data: data)

            //: } else if cmdName == "stopCall" { // 接收方/拨打方 挂断了通话
        } else if cmdName == (dream_rutStr.replacingOccurrences(of: "correct", with: "p") + "Call") { // 接收方/拨打方 挂断了通话
            //: self.isFind = false
            self.isFind = false
            //: self.isCalling = false
            self.isCalling = false
            //: endTalkingPing()
            outTotal()
            //: self.callerDelegate?.socket_videoChat_stopCall(data: data)
            self.callerDelegate?.freshData(data: data)
            // 更新余额
            //: AdministratorThen.share.loginUserMode.mf_coin = json["data"]["mfCoin"].stringValue
            AdministratorThen.share.loginUserMode.mf_coin = json[(appLoopValue.replacingOccurrences(of: "cancel", with: "at"))][(String(dreamReplaceTitle))].stringValue

            //: isShowFreeStopCallPopUpView(data: data)
            commandData(data: data)
            //: NotificationCenter.default.post(name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: noti_dismissText, object: nil)
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: (TalkingApplication.shared as! TalkingApplication).femaleBusyStatusReport()
            (ApplicationKingfisherCompatible.shared as! ApplicationKingfisherCompatible).minCharacter()
            //: } else if cmdName == "requestCall" { // 接收方收到了拨打方的申请通话
        } else if cmdName == (dream_suiteMsg.replacingOccurrences(of: "television", with: "u") + String(notiScoreKey.suffix(5))) { // 接收方收到了拨打方的申请通话
            //: self.isCalling = true
            self.isCalling = true
            // 发送通知，告知外界处理逻辑
            //: NotificationCenter.default.post(name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: self)
            NotificationCenter.default.post(name: notiOkStr, object: self)

            //: if UIApplication.shared.applicationState != .background {
            if UIApplication.shared.applicationState != .background { // 应用在前台
                //: AdministratorThen.share.recieve1v1TalkRequest(info: data)
                AdministratorThen.share.resultCustom(info: data)
                //: } else {
            } else {
                //: self.cancelCall = false
                self.cancelCall = false
                //: self.unHandleCallData = data
                self.unHandleCallData = data
                //: self.unHandleCallData?["recievedDate"] = Date()
                self.unHandleCallData?[(main_poseId.replacingOccurrences(of: "epic", with: "v") + "edDa" + String(dream_woodAskText))] = Date()

                // 应用在后台，发送本地推送告知用户
                //: var body = "[Invite you to a video call...]".localized
                var body = String(bytes: dreamAbsoluteStr.map{conversationDisc(restriction: $0)}, encoding: .utf8)!.localized
                //: if json["data"]["type"].intValue == 1 { // 音频
                if json[(appLoopValue.replacingOccurrences(of: "cancel", with: "at"))][(notiObjectMessage.replacingOccurrences(of: "translate", with: "pe"))].intValue == 1 { // 音频
                    //: body = "[Invite you to a voice call...]".localized
                    body = String(bytes: k_stoveName.map{mightTap(orbit: $0)}, encoding: .utf8)!.localized
                }
                //: AppDelegateHelper.pushLocalNotification(uid: String(json["data"]["uid"].intValue),
                ApprovalThen.simultaneously(uid: String(json[(appLoopValue.replacingOccurrences(of: "cancel", with: "at"))][(String(notiHiddenPath))].intValue),
                                                 //: title: data["nickname"] as? String,
                                                 title: data[(String(noti_satisfyUrl))] as? String,
                                                 //: body: body,
                                                 body: body,
                                                 //: imageUrl: data["headPic"] as? String)
                                                 imageUrl: data[(String(appPowMsg))] as? String)
                // 播放来电音乐
                //: AppDelegateHelper.shared.startPlayBGM()
                ApprovalThen.shared.scale()
            }

            //: } else if cmdName == "onCallResume" { // 接收方/发送方 恢复通话中的页面，并开始推拉流
        } else if cmdName == (String(showExhaustUrl) + showBronzeGainUrl.replacingOccurrences(of: "identity", with: "e")) { // 接收方/发送方 恢复通话中的页面，并开始推拉流
            //: self.isFind = true
            self.isFind = true
            //: self.isCalling = false
            self.isCalling = false
            //: startTalkPing()
            belowLength()

            //: var resumeData = data
            var resumeData = data
            //: resumeData["onCallResume"] = "1"
            resumeData[(String(showExhaustUrl) + showBronzeGainUrl.replacingOccurrences(of: "identity", with: "e"))] = "1"
            //: AdministratorThen.share.recieve1v1TalkResumeRequest(info: resumeData)
            AdministratorThen.share.tumble(info: resumeData)
            //: (TalkingApplication.shared as! TalkingApplication).destroyTimer()
            (ApplicationKingfisherCompatible.shared as! ApplicationKingfisherCompatible).wipeOutTop()

            //: } else if cmdName == "cancelCall" { // 拨打方拒绝了通话，接收方收到该指令
        } else if cmdName == (String(main_mediaStr)) { // 拨打方拒绝了通话，接收方收到该指令
            //: self.isFind = false
            self.isFind = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.cancelCall = true
            self.cancelCall = true
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            outTotal()
            //: self.callerDelegate?.socket_videoChat_cancelCall(data: data)
            self.callerDelegate?.pointBlank(data: data)
            // 应用在后台
            //: if UIApplication.shared.applicationState == .background {
            if UIApplication.shared.applicationState == .background {
                // 移除来电本地推送
                //: AppDelegateHelper.local_removeDeliveredNotification(identifier: String(json["data"]["uid"].intValue))
                ApprovalThen.assets(identifier: String(json[(appLoopValue.replacingOccurrences(of: "cancel", with: "at"))][(String(notiHiddenPath))].intValue))
                // 停止播放来电音乐
                //: AppDelegateHelper.shared.stopPlayBGM()
                ApprovalThen.shared.controlHolder()
            }

            //: } else if cmdName == "refuseCall" { // 接收方拒绝了通话，拨打方收到该指令
        } else if cmdName == (String(main_mePath) + String(noti_markValue.suffix(8))) { // 接收方拒绝了通话，拨打方收到该指令
            //: self.isFind = false
            self.isFind = false
            //: self.isCalling = false
            self.isCalling = false
            //: self.isFreeCall = false
            self.isFreeCall = false
            //: endTalkingPing()
            outTotal()
            //: self.calledPartyDelegate?.socket_videoChat_refuseCall(data: data)
            self.calledPartyDelegate?.miniskirt(data: data)

            //: } else if cmdName == "onCallMotivation" { // 女性通话收到的积分（用于展示赚钱激励动效）
        } else if cmdName == (String(noti_itStr.prefix(4)) + "llMot" + userMakerSecondTitle.replacingOccurrences(of: "check", with: "iv") + String(show_onceMsg)) { // 女性通话收到的积分（用于展示赚钱激励动效）
            //: if AdministratorThen.share.loginUserMode.sex == Gender.female.rawValue {
            if AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.female.rawValue {
                //: self.calledPartyDelegate?.socket_videoChat_onCallMotivation?(data: data)
                self.calledPartyDelegate?.videoChild?(data: data)
            }

            //: } else if cmdName == "onLiveWarning" { // 直播违规警告
        } else if cmdName == (String(dreamSuccessfulMessage)) { // 直播违规警告
            //: TalkingLiveManager.live_showWarningView(content: msg, reason: json["data"]["reason"].stringValue, time: json["data"]["time"].stringValue)
            TapBecauseLiveManager.priceData(content: msg, reason: json[(appLoopValue.replacingOccurrences(of: "cancel", with: "at"))][(dreamOriginText.replacingOccurrences(of: "op", with: "a"))].stringValue, time: json[(appLoopValue.replacingOccurrences(of: "cancel", with: "at"))][(String(noti_boundaryId))].stringValue)

            //: } else if cmdName == "onStopLive" { // 停止直播
        } else if cmdName == (String(noti_remoteLowerIdent.prefix(4)) + "opLive") { // 停止直播
            //: TalkingLiveManager.shared().live_releaseAllResource()
            TapBecauseLiveManager.resolutionShared().areaLive()

            //: } else if cmdName == "onSendProgramGift" { // 视频通话节目送礼成功tost
        } else if cmdName == (String(showRecentData) + String(noti_annualValue) + String(kProcedureIdent)) { // 视频通话节目送礼成功tost
            //: self.func__showStatusBarSuccessMsg(showMsg: msg)
            self.bringHomePublishGap(showMsg: msg)

            //: } else if cmdName == "onGameFs" { // 游戏中奖飘屏
        } else if cmdName == (String(m_maleId.suffix(7)) + String(mainWedDonKey)) { // 游戏中奖飘屏
            //: let broadStatus = data["broadStatus"] as? Int ?? -1
            let broadStatus = data[(String(notiQuickName) + String(userAwayIdent.suffix(4)) + "tatus")] as? Int ?? -1
            //: if AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue {
                //: if broadStatus == 1 {
                if broadStatus == 1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: noti_blockDescriptionName, object: self, userInfo: data)
                    //: NotificationCenter.default.post(name: USER_RECIEVED_GIFT_NOTIFICATION, object: data)
                    NotificationCenter.default.post(name: main_appFormat, object: data)
                    //: } else if broadStatus == -1 {
                } else if broadStatus == -1 {
                    //: NotificationCenter.default.post(name: RECIVE_FLOATSCREEN_NOTIFICATION, object: self, userInfo: data)
                    NotificationCenter.default.post(name: noti_blockDescriptionName, object: self, userInfo: data)
                }
            }

            //: } else if cmdName == "onCallChatMsg" { // 收到视频通话中的聊天消息
        } else if cmdName == (String(userThenSampleFormat) + String(show_goingStrongNoneFormat.suffix(6)) + "tMsg") { // 收到视频通话中的聊天消息
            //: TalkingVideoDanmuManager.shared().onRecvDanmuNewMsg(msg: data)
            FindGentleDecisionMakerReactiveCompatible.actual().only(msg: data)

            //: } else if cmdName == "onNotificationPopupWin" { // 审核拒绝提示弹窗
        } else if cmdName == (String(appPassTitle)) { // 审核拒绝提示弹窗
            //: TalkingPopupWindowManager.shared.attestationRefusePopUpWindow(dic: data)
            ShrinkWindowManager.shared.serious(dic: data)

            //: } else if cmdName == "onUserBlock" { // 用户被封号
        } else if cmdName == (String(user_innerMsg.prefix(6)) + String(main_redText.prefix(5))) { // 用户被封号
            //: TalkingLiveManager.shared().live_releaseAllResource()
            TapBecauseLiveManager.resolutionShared().areaLive()
            //: TalkingVoiceRoomManager.shared().voiceRoom_releaseAllResource()
            LanceManager.doingResource().turnChoose()
            //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
            NotificationCenter.default.post(name: appVoiceUnderText, object: nil, userInfo: nil)
        }
    }
}

// MARK: - 处理业务逻辑

//: extension TalkingSocketManager {
extension AdviceLandscapeReactiveCompatible {
    /// 检测是否需要停止或恢复直播
    /// - Parameter cmdName: socket命令
    //: private func check_stopOrRestoreLive(_ cmdName: String) {
    private func methodOnly(_ cmdName: String) {
        // 【关闭直播】收到来电或主动通话时，关闭直播
        //: if cmdName == "requestCall" || cmdName == "onRequestCall" {
        if cmdName == (dream_suiteMsg.replacingOccurrences(of: "television", with: "u") + String(notiScoreKey.suffix(5))) || cmdName == (String(notiFosterData) + String(userKindWelcomeName.suffix(6)) + "tCall") {
            //: if TalkingLiveManager.shared().isLive == true {
            if TapBecauseLiveManager.resolutionShared().isLive == true {
                //: NEED_RESTORE_LIVE = true
                user_moviePath = true
                //: TalkingLiveManager.req_liveStop(type: 3)
                TapBecauseLiveManager.shackUp(type: 3)
                //: TalkingLiveManager.shared().live_releaseAllResource()
                TapBecauseLiveManager.resolutionShared().areaLive()
                //: return
                return
            }
        }

        // 【恢复直播】结束通话后，判断是否需要恢复直播
        //: guard NEED_RESTORE_LIVE == true else { return }
        guard user_moviePath == true else { return }
        //: if cmdName == "cancelCall" || cmdName == "onCancelCall" ||
        if cmdName == (String(main_mediaStr)) || cmdName == (String(notiWindUrl.prefix(7)) + String(userPatentDuringFormat.suffix(5))) ||
            //: cmdName == "refuseCall" || cmdName == "onRefuseCall" ||
            cmdName == (String(main_mePath) + String(noti_markValue.suffix(8))) || cmdName == (String(show_productionBuryMessage) + String(mainInsideValue.suffix(4)) + "useCall") ||
            //: cmdName == "stopCall" || cmdName == "onStopCall" {
            cmdName == (dream_rutStr.replacingOccurrences(of: "correct", with: "p") + "Call") || cmdName == (String(mainFoodKey.prefix(6)) + String(mainDefinitionIdent))
        {
            //: NEED_RESTORE_LIVE = false
            user_moviePath = false
            //: let alert = TalkingLiveAlertView(frame: .zero, type: .turnOnLive)
            let alert = ChiropteranAlertView(frame: .zero, type: .turnOnLive)
            //: alert.rightBlcok = {
            alert.rightBlcok = {
                //: TalkingLiveManager.shared().checkLiveAuthAndTurnOn()
                TapBecauseLiveManager.resolutionShared().andHandler()
            }
            //: alert.show()
            alert.skinAndPer()
        }
    }

    /// 免费1分钟通话结束弹窗
    //: private func isShowFreeStopCallPopUpView(data: [String: Any]) {
    private func commandData(data: [String: Any]) {
        //: if let freeCallTimes = data["freeCallTimes"] as? Int {
        if let freeCallTimes = data[(String(dreamContrastKey) + String(user_laterIdent))] as? Int {
            //: AdministratorThen.share.loginUserMode.freeCallTimes = freeCallTimes
            AdministratorThen.share.loginUserMode.freeCallTimes = freeCallTimes
        }
        //: let freeCallWin = data["freeCallWin"] as? Bool
        let freeCallWin = data[(String(showCuteTitle) + String(dream_perfectUrl.prefix(5)))] as? Bool
        //: if freeCallWin == true && AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue {
        if freeCallWin == true, AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue {
            //: let view = TalkingFreeStopCallPopUpView.init()
            let view = SuggestThen()
            //: view.show()
            view.living()
            //: view.setShowData(data: data)
            view.viewNetwork(data: data)
        }
    }
}
