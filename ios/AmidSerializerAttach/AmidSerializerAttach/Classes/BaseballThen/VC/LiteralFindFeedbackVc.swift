
//: Declare String Begin

/*: "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$" :*/
fileprivate let mQuantityryStr:[UInt8] = [0xcb,0xbd,0xce,0xf4,0xb8,0xef,0xd4,0xb8,0xcf,0xa5,0xb8,0xac,0xca,0xc9,0xbb,0xb8,0xc8,0xbe,0xbc,0xd5,0xbd,0xce,0xc9,0xf1,0xf4,0xb8,0xef,0xc9,0xbb,0xb8,0xc8,0xbe,0xbc,0xc9,0xbb,0xbd,0xce,0xf4,0xb8,0xef,0xc9,0xbb,0xc8,0xee,0xa7,0xb9,0xa3,0xe8,0xbc,0xb1]

private func gravityHello(gaze num: UInt8) -> UInt8 {
    return num ^ 149
}

/*: "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$" :*/
fileprivate let userLaunchKey:[UInt8] = [0x14,0xde,0xde,0xe7,0x11,0xe9,0xea,0xeb,0xed,0xee,0x13,0x11,0xe6,0xe3,0xef,0x13,0x31,0xef,0x33,0xdf,0x32,0xde,0xde,0xe6,0x12,0x1a,0x31,0xe8,0x33,0xe3,0x12,0x1a,0x31,0xee,0x33,0xdf,0x32,0xde,0xe6,0x12,0x1a,0x31,0xe9,0x33,0xe3,0x12,0x1a,0x31,0xed,0xe2,0xee,0x33,0xdf,0x32,0xde,0xe6,0x12,0x1a,0x31,0xe7,0xe6,0xe2,0xe7,0xe7,0x33,0xdf,0xdf,0xda]

fileprivate func aboveErr(equal num: UInt8) -> UInt8 {
    let value = Int(num) - 182
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)" :*/
fileprivate let showEnvironmentTitle:[UInt8] = [0x4b,0x15,0x48,0x49,0x62,0x21,0x32,0x1d,0x1d,0x1a,0x49,0x62,0x26,0x33,0x2e,0x22,0x2e,0x1a,0x47,0x4e,0x1a,0x67,0x1d,0x1a,0x26,0x4c,0x4a,0x68,0x1f,0x19,0x1e,0x1d,0x6a,0x11,0x16]

fileprivate func maneTicket(force num: UInt8) -> UInt8 {
    let value = Int(num) - 237
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "^[0-8]\\d{5}(?!\\d)$" :*/
fileprivate let kSplitUrl:[Character] = ["^","[","0","-","8","]","\\","d","{","5","}","(","?","!","\\"]
fileprivate let notiMysteryDemonstrateFormat:String = "timing succeed recentd)$"

/*: "^[a-zA-Z0-9]{6,20}+$" :*/
fileprivate let mainJudgeValue:[Character] = ["^","[","a","-","z","A","-","Z"]
fileprivate let k_identificationStr:String = "0-"
fileprivate let mBackMessage:[Character] = ["9","]","{","6",",","2","0","}","+","$"]

/*: "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$" :*/
fileprivate let notiConductData:[UInt8] = [0x24,0x3f,0x2f,0x5c,0x2a,0x29,0x2a,0x5d,0x2d,0x2e,0x5c,0x20,0x77,0x5c,0x2f,0x5c,0x5b,0x28,0x29,0x7d,0x36,0x2c,0x32,0x7b,0x5d,0x2e,0x5c,0x7a,0x2d,0x61,0x5b,0x28,0x2e,0x5c,0x29,0x2b,0x5d,0x2d,0x2e,0x5c,0x7a,0x2d,0x61,0x64,0x5c,0x5b,0x28,0x3f,0x29,0x2f,0x5c,0x2f,0x5c,0x3a,0x3f,0x73,0x70,0x74,0x74,0x68,0x28,0x5e]

/*: "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$" :*/
fileprivate let m_herMsg:[UInt8] = [0x55,0x23,0x34,0x31,0x23,0x34,0x31,0x39,0x3e,0x50,0x3b,0x26,0x3e,0x56,0x77,0x39,0x50,0x3b,0x26,0x3f,0x56,0x50,0x3b,0x26,0x32,0x56,0x77,0x50,0x3b,0x3a,0x56,0x34,0x50,0x3b,0x26,0x32,0x56,0x50,0x3b,0x26,0x32,0x56,0x34,0x22,0x57,0x25,0x22,0x2b,0x70,0x38,0x76,0x23,0x34,0x31,0x39,0x3e,0x50,0x3b,0x26,0x3e,0x56,0x77,0x39,0x50,0x3b,0x26,0x3f,0x56,0x50,0x3b,0x26,0x32,0x56,0x77,0x50,0x3b,0x3a,0x56,0x34,0x50,0x3b,0x26,0x32,0x56,0x50,0x3b,0x26,0x32,0x56,0x34,0x22,0x2f]

private func arrayTreat(enjoy num: UInt8) -> UInt8 {
    return num ^ 11
}

/*: "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)" :*/
fileprivate let dreamTemporaryIdent:[UInt8] = [0x2d,0x5b,0x5e,0x35,0x28,0x3c,0x58,0x7e,0x34,0x30,0x78,0x21,0x2c,0x79,0x2d,0x5e,0x35,0x28,0x3c,0x58,0x7e,0x34,0x32,0x78,0x2d,0x5e,0x35,0x28,0x3c,0x58,0x79,0x5e,0x7d,0x5d,0x58,0x2c,0x21,0x2c]

private func bitLoop(blue num: UInt8) -> UInt8 {
    return num ^ 5
}

/*: "SELF MATCHES %@" :*/
fileprivate let showAnimalUrl:[Character] = ["S","E","L","F"," ","M","A","T","C","H","E","S"]
fileprivate let show_patentIdent:String = " %environment"

/*: "Feedback" :*/
fileprivate let dreamChallengeKey:[Character] = ["F","e","e","d","b"]
fileprivate let userCookieMessage:[Character] = ["a","c","k"]

/*: "Enter your feedback…" :*/
fileprivate let show_rowColorUrl:String = "Enterballot project exception"
fileprivate let mSweetId:[Character] = ["r"," ","f","e"]
fileprivate let kConnectValue:String = "edback…old remind transaction app proposal"

/*: "#999999" :*/
fileprivate let user_againPath:[Character] = ["#","9","9","9","9","9"]
fileprivate let dream_listenerMessage:[Character] = ["9"]

/*: "Your email (Required) " :*/
fileprivate let mOptionTitle:String = "Your analyze golden tire source representative"
fileprivate let mSupplyData:String = "scribe handle blind (Re"
fileprivate let mListenerStr:String = "ed) spend what pattern"

/*: "icon_me_feelback_star" :*/
fileprivate let appLeafKingMessage:String = "icon_advice atm knock sink error"
fileprivate let m_locationValue:String = "star"
fileprivate let user_extraId:String = "eltapack"

/*: "#CCCCCC" :*/
fileprivate let m_whiteKey:[Character] = ["#","C","C","C","C","C","C"]

/*: "Send" :*/
fileprivate let main_grayDialogMsg:String = "Sendsmooth convert replace"

/*: "#D0D0D0" :*/
fileprivate let k_filterIdent:String = "#D0D0Dtopic vertical exposure"
fileprivate let kKeyUrl:[Character] = ["0"]

/*: "Please fill in the content" :*/
fileprivate let noti_suiteStr:String = "Please reward contract inner"
fileprivate let appHimValue:[Character] = ["f","i","l","l"," ","i","n"," "]
fileprivate let dream_mergeIdent:[Character] = ["t","h","e"," ","c","o","n","t","e","n","t"]

/*: "Please enter the correct email address" :*/
fileprivate let k_thenKey:[UInt8] = [0xb4,0xd0,0xc9,0xc5,0xd7,0xc9,0x84,0xc9,0xd2,0xd8,0xc9,0xd6,0x84,0xd8,0xcc,0xc9,0x84,0xc7,0xd3,0xd6,0xd6,0xc9,0xc7,0xd8,0x84,0xc9,0xd1,0xc5,0xcd,0xd0,0x84,0xc5,0xc8,0xc8,0xd6,0xc9,0xd7,0xd7]

fileprivate func standardSpec(forth num: UInt8) -> UInt8 {
    let value = Int(num) - 100
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "content" :*/
fileprivate let k_professionalText:String = "cpocket"
fileprivate let dreamBusinessPath:[Character] = ["n","t","e","n","t"]

/*: "contactWay" :*/
fileprivate let showSparkTitle:String = "contparty"

/*: "platform" :*/
fileprivate let appModelValue:String = "skinat"

/*: "iphone" :*/
fileprivate let showArrayName:[Character] = ["i","p","h","o","n","e"]

/*: "version" :*/
fileprivate let dream_attributePath:String = "vconstantrsion"

/*: "type" :*/
fileprivate let noti_banPath:String = "tactione"

/*: "Operation succeeded" :*/
fileprivate let main_siblingFormat:[Character] = ["O","p","e","r","a","t","i","o","n"," "]
fileprivate let userFoundationFormat:String = "succaddadddaddd"

/*: "Problem statements" :*/
fileprivate let showDisableText:String = "identification accord rewardProble"
fileprivate let showQuietStudyStr:String = "spark"
fileprivate let appMayLinkPath:String = "emestraights"

/*: "Feature advice" :*/
fileprivate let userLeadingStr:String = "gain under contact scenario menuFeatu"
fileprivate let main_regionUrl:String = "vienter"

/*: "Operation questions" :*/
fileprivate let mainBallotBottomUrl:[Character] = ["O","p","e","r","a","t","i","o","n"," "]
fileprivate let user_yellowMessage:String = "quplainst"

/*: "Others" :*/
fileprivate let appSendKey:[Character] = ["O","t","h","e","r","s"]

/*: "#EFEDFF" :*/
fileprivate let noti_pictureUrl:String = "condition intro execute#E"
fileprivate let noti_readerStr:[Character] = ["F","E","D","F","F"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  LiteralFindFeedbackVc.swift
//  AbroadTalking
//
//  Created by Charlotte on 2022/9/15.
//

//: import UIKit
import UIKit

//: enum Validate {
enum MightValidate {
    //: case email(_: String)
    case email(_: String) //  邮箱
    //: case phoneNumber(_: String)
    case phoneNumber(_: String) //  手机号
    //: case userName(_: String)
    case userName(_: String) //  用户名
    //: case password(_: String)
    case password(_: String) //  密码
    //: case nickName(_: String)
    case nickName(_: String) //  昵称
    //: case postalCode(_: String)
    case postalCode(_: String) //  邮编
    //: case URL(_: String)
    case URL(_: String) //  URL
    //: case IP(_: String)
    case IP(_: String) //  IP
    //: case isChinese(_: String)
    case isChinese(_: String) //  是否全是中文字符
    //: case idNumber(_: String)
    case idNumber(_: String) //  身份证号

    //: var isRight: Bool {
    var isRight: Bool {
        //: var predicateStr: String!
        var predicateStr: String!
        //: var currentObject: String!
        var currentObject: String!
        //: switch self {
        switch self {
        //: case let .email(str):
        case let .email(str):
            //: predicateStr = "^([a-zA-Z0-9_\\.-]+)@([\\da-z\\.-]+)\\.([a-z\\.]{2,6})$"
            predicateStr = String(bytes: mQuantityryStr.map{gravityHello(gaze: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .phoneNumber(str):
        case let .phoneNumber(str):
            //: predicateStr = "^((1[34578][0-9]{9})|((0\\d{2}-\\d{8})|(0\\d{3}-\\d{7,8})|(0\\d{10,11}))$"
            predicateStr = String(bytes: userLaunchKey.map{aboveErr(equal: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .userName(str):
        case let .userName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: showEnvironmentTitle.map{maneTicket(force: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .postalCode(str):
        case let .postalCode(str):
            //: predicateStr = "^[0-8]\\d{5}(?!\\d)$"
            predicateStr = (String(kSplitUrl) + String(notiMysteryDemonstrateFormat.suffix(3)))
            //: currentObject = str
            currentObject = str
        //: case let .password(str):
        case let .password(str):
            //: predicateStr = "^[a-zA-Z0-9]{6,20}+$"
            predicateStr = (String(mainJudgeValue) + k_identificationStr.capitalized + String(mBackMessage))
            //: currentObject = str
            currentObject = str
        //: case let .nickName(str):
        case let .nickName(str):
            //: predicateStr = "^([\\u4E00-\\u9FA5A-Za-z0-9_]{2,10}$)"
            predicateStr = String(bytes: showEnvironmentTitle.map{maneTicket(force: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .URL(str):
        case let .URL(str):
            //: predicateStr = "^(https?:\\/\\/)?([\\da-z\\.-]+)\\.([a-z\\.]{2,6})([\\/\\w \\.-]*)*\\/?$"
            predicateStr = String(bytes: notiConductData.reversed(), encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let .IP(str):
        case let .IP(str):
            //: predicateStr = "^(?:(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)\\.) {3}(?:25[0-5]|2[0-4][0-9]|[01]?[0-9][0-9]?)$"
            predicateStr = String(bytes: m_herMsg.map{arrayTreat(enjoy: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        //: case let . isChinese(str):
        case let .isChinese(str):
            //: predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            predicateStr = "(^[\u{4e00}-\u{9fa5}]$)"
            //: currentObject = str
            currentObject = str
        //: case let . idNumber(str):
        case let .idNumber(str):
            //: predicateStr = "(^[0-9]{15}$)|([0-9]{17}([0-9]|[xX])$)"
            predicateStr = String(bytes: dreamTemporaryIdent.map{bitLoop(blue: $0)}, encoding: .utf8)!
            //: currentObject = str
            currentObject = str
        }
        //: let predicate =  NSPredicate(format: "SELF MATCHES %@", predicateStr)
        let predicate = NSPredicate(format: (String(showAnimalUrl) + show_patentIdent.replacingOccurrences(of: "environment", with: "@")), predicateStr)
        //: return predicate.evaluate(with: currentObject)
        return predicate.evaluate(with: currentObject)
    }
}

//: class TalkingFeedbackVC: TalkingBaseViewController {
class LiteralFindFeedbackVc: FlexibleViewController {
    //: let limitCount = 300
    let limitCount = 300
    //: let EmialCount = 50
    let EmialCount = 50
    //: var seleteBtn: UIButton?
    var seleteBtn: UIButton?

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()

        //: self.title = "Feedback".localized
        self.title = (String(dreamChallengeKey) + String(userCookieMessage)).localized
        //: self.view.backgroundColor = .white
        self.view.backgroundColor = .white
        //: designView()
        optic()
        //: addTapGestureRecognizer()
        donkeyEngine()
        //: addKeyboardNotification()
        press()
    }

    // MARK: - Lazy load

    //: lazy var backView: UIScrollView = {
    lazy var backView: UIScrollView = {
        //: let view = UIScrollView.init()
        let view = UIScrollView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: view.showsHorizontalScrollIndicator = false
        view.showsHorizontalScrollIndicator = false
        //: view.showsVerticalScrollIndicator = false
        view.showsVerticalScrollIndicator = false
        //: return view
        return view
        //: }()
    }()

    //: lazy var contView: UIView = {
    lazy var contView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.backgroundColor = .white
        view.backgroundColor = .white
        //: return view
        return view
        //: }()
    }()

    //: lazy var inputTView: UIView = {
    lazy var inputTView: UIView = {
        //: let view = UIView.init()
        let view = UIView()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: view.layer.cornerRadius = 8
        view.layer.cornerRadius = 8
        //: view.backgroundColor = .appBgColor()
        view.backgroundColor = .landmarkBalance()
        //: return view
        return view
        //: }()
    }()

    //: lazy var textView: TalkingTextView = {
    lazy var textView: TwoHasDelegate = {
        //: let textView = TalkingTextView.init()
        let textView = TwoHasDelegate()
        //: textView.placeholder = "Enter your feedback…".localized
        textView.placeholder = (String(show_rowColorUrl.prefix(5)) + " you" + String(mSweetId) + String(kConnectValue.prefix(7))).localized
        //: textView.placeholderColor = UIColor.init(hex: "#999999")!
        textView.placeholderColor = UIColor(hex: (String(user_againPath) + String(dream_listenerMessage)))!
        //: textView.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        textView.font = UIFont.afterDisable(type: .Regular, fontSize: 15)
        //: textView.textColor = UIColor.appTitleColor()
        textView.textColor = UIColor.manualColor()
        //: textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)
        textView.PlaceholderInsets = UIEdgeInsets(top: 16, left: 16, bottom: 10, right: 16)

        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textContainer.lineBreakMode = .byTruncatingTail
        textView.textContainer.lineBreakMode = .byTruncatingTail
        //: textView.delegate = self
        textView.delegate = self
        //: textView.returnKeyType = UIReturnKeyType.done
        textView.returnKeyType = UIReturnKeyType.done
        //: textView.bounces = false
        textView.bounces = false
        //: textView.backgroundColor = .appBgColor()
        textView.backgroundColor = .landmarkBalance()
        //: numberLB.text =  "0/\(limitCount)"
        numberLB.text = "0/\(limitCount)"
        //: return textView
        return textView
        //: }()
    }()

    //: lazy var numberLB: UILabel = {
    lazy var numberLB: UILabel = {
        //: let LB = UILabel.init()
        let LB = UILabel()
        //: LB.textColor = UIColor.init(hex: "#999999")
        LB.textColor = UIColor(hex: (String(user_againPath) + String(dream_listenerMessage)))
        //: LB.font = UIFont.pingfangFont(type: .Regular, fontSize: 14)
        LB.font = UIFont.afterDisable(type: .Regular, fontSize: 14)
        //: LB.text = "0/\(limitCount)"
        LB.text = "0/\(limitCount)"
        //: LB.textAlignment = .right
        LB.textAlignment = .right
        //: return LB
        return LB
        //: }()
    }()

    //: lazy var emailTF: UITextField = {
    lazy var emailTF: UITextField = {
        //: let tf = UITextField.init()
        let tf = UITextField()
        //: tf.backgroundColor = .appBgColor()
        tf.backgroundColor = .landmarkBalance()
        //: tf.textColor = UIColor.appTitleColor()
        tf.textColor = UIColor.manualColor()
        //: tf.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        tf.font = UIFont.afterDisable(type: .Regular, fontSize: 16)
        //: tf.delegate = self
        tf.delegate = self
        //: tf.returnKeyType = UIReturnKeyType.done
        tf.returnKeyType = UIReturnKeyType.done
        //: tf.attributedPlaceholder = NSAttributedString.init(string: "Your email (Required) ".localized, attributes: [
        tf.attributedPlaceholder = NSAttributedString(string: (String(mOptionTitle.prefix(5)) + "email" + String(mSupplyData.suffix(4)) + "quir" + String(mListenerStr.prefix(4))).localized, attributes: [
            //: NSAttributedString.Key.foregroundColor: UIColor.init(hex: "#999999")!])
            NSAttributedString.Key.foregroundColor: UIColor(hex: (String(user_againPath) + String(dream_listenerMessage)))!])
        //: tf.clipsToBounds = true
        tf.clipsToBounds = true
        //: tf.layer.cornerRadius = 8
        tf.layer.cornerRadius = 8
        //: if LanguageManager.shared.direction == .rightToLeft {
        if PrintingReactiveCompatible.shared.direction == .rightToLeft {
            //: tf.textAlignment = .right
            tf.textAlignment = .right
        }
        //: let leftView = UIView()
        let leftView = UIView()
        //: leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))
        leftView.frame = CGRect(origin: .zero, size: CGSize(width: 30, height: 49))

        //: let leftImgView = UIImageView.init()
        let leftImgView = UIImageView()
        //: leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        leftImgView.frame = CGRect(x: 16, y: 20, width: 9, height: 9)
        //: leftImgView.image = UIImage.BundleImageNamed(name: "icon_me_feelback_star")
        leftImgView.image = UIImage.scribeNumber(name: (String(appLeafKingMessage.prefix(5)) + "me_f" + m_locationValue.replacingOccurrences(of: "star", with: "e") + user_extraId.replacingOccurrences(of: "tap", with: "b") + "_star"))
        //: leftView.addSubview(leftImgView)
        leftView.addSubview(leftImgView)
        //: tf.leftView = leftView
        tf.leftView = leftView
        //: tf.leftViewMode = .always
        tf.leftViewMode = .always
        //: return tf
        return tf
        //: }()
    }()

    //: lazy var doneBtn: UIButton = {
    lazy var doneBtn: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.setBackgroundColor(color: UIColor.init(hex: "#CCCCCC")!, forState: .disabled)
        btn.toastrackUpwardlyState(color: UIColor(hex: (String(m_whiteKey)))!, forState: .disabled)
        //: btn.setBackgroundColor(color: UIColor.appThemeColor(), forState: .normal)
        btn.toastrackUpwardlyState(color: UIColor.fogColor(), forState: .normal)
        //: btn.setTitle("Send".localized, for: .normal)
        btn.setTitle((String(main_grayDialogMsg.prefix(4))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        btn.titleLabel?.font = UIFont.afterDisable(type: .Medium, fontSize: 17)
        //: btn.addTarget(self, action: #selector(doneBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(refuse), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFeedbackVC {
extension LiteralFindFeedbackVc {
    //: @objc func TagBtnClick(sender: UIButton) {
    @objc func activeStill(sender: UIButton) {
        //: seleteBtn?.isSelected = false
        seleteBtn?.isSelected = false
        //: seleteBtn?.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
        seleteBtn?.layer.borderColor = UIColor(hex: (String(k_filterIdent.prefix(6)) + String(kKeyUrl)))?.cgColor
        //: sender.isSelected = !sender.isSelected
        sender.isSelected = !sender.isSelected
        //: seleteBtn = sender
        seleteBtn = sender
        //: seleteBtn!.layer.borderColor = UIColor.clear.cgColor
        seleteBtn!.layer.borderColor = UIColor.clear.cgColor
    }

    //: @objc func doneBtnClick() {
    @objc func refuse() {
        //: if seleteBtn == nil || textView.text.count<=0 || emailTF.text!.count<=0 {
        if seleteBtn == nil || textView.text.count <= 0 || emailTF.text!.count <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please fill in the content".localized)
            self.iconThemeBringBackPlace(showMsg: (String(noti_suiteStr.prefix(7)) + String(appHimValue) + String(dream_mergeIdent)).localized)
            //: return
            return
        }

        //: if LanguageManager.shared.direction == .leftToRight {
        if PrintingReactiveCompatible.shared.direction == .leftToRight {
            //: if !Validate.email(emailTF.text!).isRight {
            if !MightValidate.email(emailTF.text!).isRight {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please enter the correct email address".localized)
                self.iconThemeBringBackPlace(showMsg: String(bytes: k_thenKey.map{standardSpec(forth: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
        }

        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["content"] = textView.text
        dict[(k_professionalText.replacingOccurrences(of: "pocket", with: "o") + String(dreamBusinessPath))] = textView.text
        //: dict["contactWay"] = emailTF.text
        dict[(showSparkTitle.replacingOccurrences(of: "party", with: "a") + "ctWay")] = emailTF.text
        //: dict["platform"] = "iphone"
        dict[(appModelValue.replacingOccurrences(of: "skin", with: "pl") + "form")] = (String(showArrayName))
        //: dict["version"] = AppVersion
        dict[(dream_attributePath.replacingOccurrences(of: "constant", with: "e"))] = userAssetManagerPath
        //: dict["type"] = String((seleteBtn?.tag ?? 100)-100)
        dict[(noti_banPath.replacingOccurrences(of: "action", with: "yp"))] = String((seleteBtn?.tag ?? 100) - 100)

        //: ProgressHUD.show()
        VillageView.projectShow()

        //: TalkingMeRequestTool.req_Feedback(params: dict) { succeed, result, errorModel in
        MeThen.negativeFeedbackParamsProfileCompletion(params: dict) { succeed, _, _ in
            //: ProgressHUD.dismiss()
            VillageView.statusDismiss()
            //: if succeed {
            if succeed {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Operation succeeded".localized)
                self.bringHomePublishGap(showMsg: (String(main_siblingFormat) + userFoundationFormat.replacingOccurrences(of: "add", with: "e")).localized)
                //: self.navigationController?.popViewController(animated: true)
                self.navigationController?.popViewController(animated: true)
            }
        }
    }

    //: func addKeyboardNotification() {
    func press() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillShow(notification:)),
                                               selector: #selector(upon(notification:)),
                                               //: name: UIResponder.keyboardWillShowNotification,
                                               name: UIResponder.keyboardWillShowNotification,
                                               //: object: nil)
                                               object: nil)
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(keyboardWillHide(notification:)),
                                               selector: #selector(keyboardWithinLoad(notification:)),
                                               //: name: UIResponder.keyboardWillHideNotification,
                                               name: UIResponder.keyboardWillHideNotification,
                                               //: object: nil)
                                               object: nil)
    }

    //: @objc func keyboardWillShow(notification: Notification) {
    @objc func upon(notification: Notification) {
        //: guard emailTF.isFirstResponder else { return }
        guard emailTF.isFirstResponder else { return }
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        let value = info[UIResponder.keyboardFrameEndUserInfoKey] as? NSValue
        //: let frame = value?.cgRectValue ?? .zero
        let frame = value?.cgRectValue ?? .zero
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        //: guard (emailTF.bottom + StatusBarNavigationBarHeight) > frame.minY else { return }
        guard (emailTF.bottom + appServerSensePath) > frame.minY else { return }

        //: let y = (emailTF.bottom + StatusBarNavigationBarHeight) - frame.minY
        let y = (emailTF.bottom + appServerSensePath) - frame.minY
        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
            self.backView.transform = CGAffineTransform(translationX: 0, y: -y)
        }
    }

    //: @objc func keyboardWillHide(notification: Notification) {
    @objc func keyboardWithinLoad(notification: Notification) {
        //: guard let info = notification.userInfo else { return }
        guard let info = notification.userInfo else { return }
        //: let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25
        let duration = info[UIResponder.keyboardAnimationDurationUserInfoKey] as? Double ?? 0.25

        //: UIView.animate(withDuration: duration) {
        UIView.animate(withDuration: duration) {
            //: self.backView.transform = .identity
            self.backView.transform = .identity
        }
    }
}

// MARK: - UITextViewDelegate, UITextFieldDelegate

//: extension TalkingFeedbackVC: UITextViewDelegate, UITextFieldDelegate {
extension LiteralFindFeedbackVc: UITextViewDelegate, UITextFieldDelegate {
    //: public func textViewDidChange(_ textView: UITextView) {
    public func textViewDidChange(_ textView: UITextView) {
        //: if textView.text.count > limitCount {
        if textView.text.count > limitCount {
            // 获得已输出字数与正输入字母数
            //: let selectRange = textView.markedTextRange
            let selectRange = textView.markedTextRange

            // 获取高亮部分 － 如果有联想词则解包成功
            //: if let selectRange = selectRange {
            if let selectRange = selectRange {
                //: let position =  textView.position(from: (selectRange.start), offset: 0)
                let position = textView.position(from: selectRange.start, offset: 0)
                //: if (position != nil) {
                if position != nil {
                    //: return
                    return
                }
            }

            //: let textContent = textView.text ?? ""
            let textContent = textView.text ?? ""
            //: let textNum = textContent.count
            let textNum = textContent.count

            // 截取
            //: if textNum > limitCount && limitCount > 0 {
            if textNum > limitCount && limitCount > 0 {
                //: textView.text = string_prefix(index: limitCount, text: textContent)
                textView.text = fitAcross(index: limitCount, text: textContent)
            }
        }
        //: numberLB.text =  "\(textView.text.count)/\(limitCount)"
        numberLB.text = "\(textView.text.count)/\(limitCount)"
    }

    //: public func textViewShouldBeginEditing(_ textView: UITextView) -> Bool {
    public func textViewShouldBeginEditing(_: UITextView) -> Bool {
        //: numberLB.isHidden = false
        numberLB.isHidden = false
        //: return true
        return true
    }

    //: public func textViewDidEndEditing(_ textView: UITextView) {
    public func textViewDidEndEditing(_ textView: UITextView) {
        //: if textView.text.isEmpty {
        if textView.text.isEmpty {
            //: numberLB.isHidden = false
            numberLB.isHidden = false
            //: } else {
        } else {
            //: numberLB.isHidden = true
            numberLB.isHidden = true
        }
    }

    //: public func textView(_ textView: UITextView, shouldChangeTextIn range: NSRange, replacementText text: String) -> Bool {
    public func textView(_ textView: UITextView, shouldChangeTextIn _: NSRange, replacementText text: String) -> Bool {
        //: if textView.returnKeyType == .done {
        if textView.returnKeyType == .done {
            //: if text == "\n" {
            if text == "\n" {
                //: textView.resignFirstResponder()
                textView.resignFirstResponder()
            }
            //: return true
            return true
        }
        //: return true
        return true
    }

    // 字符串的截取 从头截取到指定index
    //: private func string_prefix(index: Int,text:String) -> String {
    private func fitAcross(index: Int, text: String) -> String {
        //: if text.count <= index {
        if text.count <= index {
            //: return text
            return text
            //: } else {
        } else {
            //: let index = text.index(text.startIndex, offsetBy: index)
            let index = text.index(text.startIndex, offsetBy: index)
            //: let str = text.prefix(upTo: index)
            let str = text.prefix(upTo: index)
            //: return String(str)
            return String(str)
        }
    }

    //: func textFieldShouldReturn(_ textField: UITextField) -> Bool {
    func textFieldShouldReturn(_ textField: UITextField) -> Bool {
        //: textField.resignFirstResponder()
        textField.resignFirstResponder()

        //: return true
        return true
    }

    //: func textField(_ textField: UITextField, shouldChangeCharactersIn range: NSRange, replacementString string: String) -> Bool {
    func textField(_ textField: UITextField, shouldChangeCharactersIn _: NSRange, replacementString string: String) -> Bool {
        //: if textField.text!.count+string.count>EmialCount || textField.text!.count+string.count <= 0 {
        if textField.text!.count + string.count > EmialCount || textField.text!.count + string.count <= 0 {
            //: return false
            return false
        }
        //: return true
        return true
    }
}

// MARK: - UI

//: extension TalkingFeedbackVC {
extension LiteralFindFeedbackVc {
    //: func designView() {
    func optic() {
        //: self.view.addSubview(backView)
        self.view.addSubview(backView)
        //: backView.snp.makeConstraints { make in
        backView.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
        }
        //: backView.addSubview(contView)
        backView.addSubview(contView)
        //: contView.snp.makeConstraints { make in
        contView.snp.makeConstraints { make in
            //: make.leading.trailing.top.bottom.equalTo(backView)
            make.leading.trailing.top.bottom.equalTo(backView)
        }

        //: contView.addSubview(inputTView)
        contView.addSubview(inputTView)
        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(contView).offset(128)
            make.top.equalTo(contView).offset(128)
            //: make.height.equalTo(210)
            make.height.equalTo(210)
        }

        //: inputTView.addSubview(textView)
        inputTView.addSubview(textView)
        //: inputTView.addSubview(numberLB)
        inputTView.addSubview(numberLB)
        //: textView.snp.makeConstraints { make in
        textView.snp.makeConstraints { make in
            //: make.leading.trailing.top.equalTo(inputTView)
            make.leading.trailing.top.equalTo(inputTView)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-40)
        }
        //: numberLB.snp.makeConstraints { make in
        numberLB.snp.makeConstraints { make in
            //: make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            make.trailing.equalTo(inputTView.snp.trailing).offset(-16)
            //: make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            make.bottom.equalTo(inputTView.snp.bottom).offset(-12)
            //: make.height.equalTo(20)
            make.height.equalTo(20)
        }
        //: contView.addSubview(emailTF)
        contView.addSubview(emailTF)
        //: emailTF.snp.makeConstraints { make in
        emailTF.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(15)
            make.leading.equalTo(contView).offset(15)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-15)
            make.trailing.equalTo(contView.snp.trailing).offset(-15)
            //: make.top.equalTo(inputTView.snp.bottom).offset(16)
            make.top.equalTo(inputTView.snp.bottom).offset(16)
            //: make.height.equalTo(49)
            make.height.equalTo(49)
        }

        //: contView.addSubview(doneBtn)
        contView.addSubview(doneBtn)
        //: doneBtn.snp.makeConstraints { make in
        doneBtn.snp.makeConstraints { make in
            //: make.leading.equalTo(contView).offset(54)
            make.leading.equalTo(contView).offset(54)
            //: make.trailing.equalTo(contView.snp.trailing).offset(-54)
            make.trailing.equalTo(contView.snp.trailing).offset(-54)
            //: make.top.equalTo(emailTF.snp.bottom).offset(111)
            make.top.equalTo(emailTF.snp.bottom).offset(111)
            //: make.height.equalTo(50)
            make.height.equalTo(50)
        }

        //: let arr = ["Problem statements".localized,
        let arr = [(String(showDisableText.suffix(6)) + "m sta" + showQuietStudyStr.replacingOccurrences(of: "spark", with: "t") + appMayLinkPath.replacingOccurrences(of: "straight", with: "nt")).localized,
                   //: "Feature advice".localized,
                   (String(userLeadingStr.suffix(5)) + "re ad" + main_regionUrl.replacingOccurrences(of: "enter", with: "ce")).localized,
                   //: "Operation questions".localized,
                   (String(mainBallotBottomUrl) + user_yellowMessage.replacingOccurrences(of: "plain", with: "e") + "ions").localized,
                   //: "Others".localized]
                   (String(appSendKey)).localized]

        //: for i in 0..<arr.count {
        for i in 0 ..< arr.count {
            //: let xz = 15
            let xz = 15
            //: let yz = 16
            let yz = 16
            //: let Width = (Int(ScreenWidth)-xz*3)/2
            let Width = (Int(noti_senseContent) - xz * 3) / 2
            //: let height = 38
            let height = 38
            //: let X = xz+(xz + Width)*(i%2)
            let X = xz + (xz + Width) * (i % 2)
            //: let Y = yz+(yz+height)*(i/2)
            let Y = yz + (yz + height) * (i / 2)

            //: let btn = UIButton.init()
            let btn = UIButton()
            //: btn.frame =  CGRect.init(x: X, y: Y , width: Width, height: height)
            btn.frame = CGRect(x: X, y: Y, width: Width, height: height)
            //: btn.setTitle(arr[i], for: .normal)
            btn.setTitle(arr[i], for: .normal)
            //: btn.setTitleColor(.black, for: .normal)
            btn.setTitleColor(.black, for: .normal)
            //: btn.setTitleColor(.appThemeColor(), for: .selected)
            btn.setTitleColor(.fogColor(), for: .selected)

            //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
            btn.titleLabel?.font = UIFont.afterDisable(type: .Regular, fontSize: 15)
            //: if TalkingRequestAddrTool.share.interfaceLang == LangType.es.rawValue || TalkingRequestAddrTool.share.interfaceLang == LangType.pt.rawValue {
            if BubbleVisitorReactiveCompatible.share.interfaceLang == StatusExpressionConvertible.es.rawValue || BubbleVisitorReactiveCompatible.share.interfaceLang == StatusExpressionConvertible.pt.rawValue {
                //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 13)
                btn.titleLabel?.font = UIFont.afterDisable(type: .Regular, fontSize: 13)
            }
            //: btn.layer.cornerRadius = 19
            btn.layer.cornerRadius = 19
            //: btn.layer.borderWidth = 1
            btn.layer.borderWidth = 1
            //: btn.layer.borderColor = UIColor.init(hex: "#D0D0D0")?.cgColor
            btn.layer.borderColor = UIColor(hex: (String(k_filterIdent.prefix(6)) + String(kKeyUrl)))?.cgColor
            //: btn.setBackgroundColor(color: UIColor.init(hex: "#EFEDFF")!, forState: .selected)
            btn.toastrackUpwardlyState(color: UIColor(hex: (String(noti_pictureUrl.suffix(2)) + String(noti_readerStr)))!, forState: .selected)
            //: btn.setBackgroundColor(color: .white, forState: .normal)
            btn.toastrackUpwardlyState(color: .white, forState: .normal)
            //: btn.tag = 100+i
            btn.tag = 100 + i
            //: btn.addTarget( self, action: #selector(TagBtnClick(sender:)), for: .touchUpInside)
            btn.addTarget(self, action: #selector(activeStill(sender:)), for: .touchUpInside)
            //: contView.addSubview(btn)
            contView.addSubview(btn)
        }

        //: contView.snp.remakeConstraints { make in
        contView.snp.remakeConstraints { make in
            //: make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            make.bottom.equalTo(doneBtn.snp.bottom).offset(50)
            //: make.width.equalTo(backView)
            make.width.equalTo(backView)
        }
    }
}
