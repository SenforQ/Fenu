
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dream_choiceWithinMsg:[UInt8] = [0x77,0x70,0x77,0x6a,0x36,0x7d,0x71,0x7a,0x7b,0x6c,0x24,0x37,0x3e,0x76,0x7f,0x6d,0x3e,0x70,0x71,0x6a,0x3e,0x7c,0x7b,0x7b,0x70,0x3e,0x77,0x73,0x6e,0x72,0x7b,0x73,0x7b,0x70,0x6a,0x7b,0x7a]

private func updatelyUpper(landmark num: UInt8) -> UInt8 {
    return num ^ 30
}

/*: "Voice greeting" :*/
fileprivate let mLockIdent:String = "Voiceteam accuracy problem sort"
fileprivate let mainAntiText:String = "etinactive"

/*: "Add" :*/
fileprivate let userRetirementContent:String = "Addarrive bucket hand"

/*: "type" :*/
fileprivate let showRiteFormat:String = "tenderype"

/*: "addStatus" :*/
fileprivate let mainStripQueryedMessage:[Character] = ["a"]
fileprivate let main_themStreetStr:String = "portion oneddSt"

/*: "list" :*/
fileprivate let user_calculationId:[Character] = ["l","i","s","t"]

/*: "audio" :*/
fileprivate let dream_administrativeData:String = "aspecdio"

/*: "length" :*/
fileprivate let noti_galleryTravelParticleIdent:String = "draft"
fileprivate let userWitchIdent:String = "recommendationgth"

/*: "url" :*/
fileprivate let appShowPalaceStr:[Character] = ["u","r","l"]

/*: "Successfully added, please wait for approval" :*/
fileprivate let dream_clubCircleTitle:[UInt8] = [0x6c,0x61,0x76,0x6f,0x72,0x70,0x70,0x61,0x20,0x72,0x6f,0x66,0x20,0x74,0x69,0x61,0x77,0x20,0x65,0x73,0x61,0x65,0x6c,0x70,0x20,0x2c,0x64,0x65,0x64,0x64,0x61,0x20,0x79,0x6c,0x6c,0x75,0x66,0x73,0x73,0x65,0x63,0x63,0x75,0x53]

/*: "Are you sure to delete this message？" :*/
fileprivate let showThickPrisonChinData:[UInt8] = [0xe9,0xda,0xcd,0x88,0xd1,0xc7,0xdd,0x88,0xdb,0xdd,0xda,0xcd,0x88,0xdc,0xc7,0x88,0xcc,0xcd,0xc4,0xcd,0xdc,0xcd,0x88,0xdc,0xc0,0xc1,0xdb,0x88,0xc5,0xcd,0xdb,0xdb,0xc9,0xcf,0xcd,0x47,0x14,0x37]

private func panRecover(phone num: UInt8) -> UInt8 {
    return num ^ 168
}

/*: "Cancel" :*/
fileprivate let mainMinuteButteTranslateIdent:[Character] = ["C","a","n","c","e","l"]

/*: "Delete" :*/
fileprivate let mainWallValue:String = "Deleteidentify flexible four family"

/*: "Please wait for the audit result" :*/
fileprivate let dreamBecomeMsg:[UInt8] = [0x68,0x54,0x5d,0x59,0x4b,0x5d,0x18,0x4f,0x59,0x51,0x4c,0x18,0x5e,0x57,0x4a,0x18,0x4c,0x50,0x5d,0x18,0x59,0x4d,0x5c,0x51,0x4c,0x18,0x4a,0x5d,0x4b,0x4d,0x54,0x4c]

private func singleFile(bright num: UInt8) -> UInt8 {
    return num ^ 56
}

/*: "The upper limit has been reached" :*/
fileprivate let userBoyFareMessage:[UInt8] = [0x77,0x4b,0x46,0x3,0x56,0x53,0x53,0x46,0x51,0x3,0x4f,0x4a,0x4e,0x4a,0x57,0x3,0x4b,0x42,0x50,0x3,0x41,0x46,0x46,0x4d,0x3,0x51,0x46,0x42,0x40,0x4b,0x46,0x47]

private func customList(permission num: UInt8) -> UInt8 {
    return num ^ 35
}

/*: "filePath" :*/
fileprivate let noti_boxRefuseIdent:[Character] = ["f","i","l","e","P"]
fileprivate let mPartyWeightTitle:String = "tenderh"

/*: "time" :*/
fileprivate let dream_networkMsg:[Character] = ["t","i","m","e"]

/*: "#FF506D" :*/
fileprivate let userRequestSubFormat:String = "#FF50stead colorful field given"
fileprivate let appBeautifulCircleNearMsg:String = "6Dfence po simple regular"

/*: "btn_message_delete_nor" :*/
fileprivate let user_captainName:[Character] = ["b","t","n","_","m","e","s","s","a","g","e","_","d"]
fileprivate let dream_wallDenyName:[Character] = ["e","l","e","t","e","_","n","o","r"]

/*: "Currently mute" :*/
fileprivate let show_likelihoodFormat:[Character] = ["C","u","r","r","e","n","t","l"]
fileprivate let kMutualFormat:[Character] = ["y"," ","m","u","t","e"]

/*: "Edit Title" :*/
fileprivate let appDisplayMsg:String = "Edit Titselected how"
fileprivate let m_asideDirectorName:[Character] = ["l","e"]

/*: "remark" :*/
fileprivate let notiRidIdent:String = "extent"
fileprivate let dreamWrapComplexityMessage:String = "eanniversaryrk"

/*: "Click the button below to add\ncontent" :*/
fileprivate let userKitMsg:[UInt8] = [0x76,0x59,0x5c,0x56,0x5e,0x15,0x41,0x5d,0x50,0x15,0x57,0x40,0x41,0x41,0x5a,0x5b,0x15,0x57,0x50,0x59,0x5a,0x42,0x15,0x41,0x5a,0x15,0x54,0x51,0x51,0x3f,0x56,0x5a,0x5b,0x41,0x50,0x5b,0x41]

private func structuralAnthropology(glass num: UInt8) -> UInt8 {
    return num ^ 53
}

/*: "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited." :*/
fileprivate let app_nobodyUrl:[UInt8] = []

private func fosterLancePose(record num: UInt8) -> UInt8 {
    return num ^ 17
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AudioViewController.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/2/9.
//

//: import UIKit
import UIKit

//: class TalkingGreetAudioVC: TalkingBaseViewController {
class AudioViewController: FlexibleViewController {
    //: var addStatus = -1
    var addStatus = -1 // 1：可以添加；2：已有正在审核中得音频，不能添加；3：已有20条审核通过的音频，不能添加；4：已有20条审核通过的文字，不能添加；5：已有20条审核中或审核通过的图片，不能添加；
    //: var recordView: TalkingGreetRecordAudioView?
    var recordView: InjuryReadingView?
    //: var isPlaying = false
    var isPlaying = false
    //: var playIndex: IndexPath?
    var playIndex: IndexPath?
    //: var player: TalkingAudioPlayTool?
    var player: SaveDetectReactiveCompatible?
    //: var isMute = false
    var isMute = false
    //: var currenCell: TalkingGreetAudioCell?
    var currenCell: FlexibleViewCell?
    //: init() {
    init() {
        //: super.init(nibName: nil, bundle: nil)
        super.init(nibName: nil, bundle: nil)
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dream_choiceWithinMsg.map{updatelyUpper(landmark: $0)}, encoding: .utf8)!)
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
    }

    //: override func viewDidAppear(_ animated: Bool) {
    override func viewDidAppear(_ animated: Bool) {
        //: super.viewDidAppear(animated)
        super.viewDidAppear(animated)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.title = "Voice greeting".localized
        self.title = (String(mLockIdent.prefix(5)) + " gre" + mainAntiText.replacingOccurrences(of: "active", with: "g")).localized
        //: self.view.backgroundColor = UIColor.appBgColor()
        self.view.backgroundColor = UIColor.landmarkBalance()
        //: designView()
        sumryIdentity()
        //: getAudioListData()
        tentData()
        //: setMutedDetection()
        holdfastDetection()
        //: NotificationCenter.default.addObserver(self, selector: #selector(cancelPlayerAudio(notification:)), name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(birthday(notification:)), name: notiOkStr, object: nil)

        //: NotificationCenter.default.addObserver(self, selector: #selector(interruptionChange(notification:)), name: AVAudioSession.interruptionNotification, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(warningDirection(notification:)), name: AVAudioSession.interruptionNotification, object: nil)

        // 添加接收到音视频通话通知
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(stopPlay),
                                               selector: #selector(backstop),
                                               //: name: SOCKET_VIDEOCHAT_REQUESTCALL_NOTIFICATION,
                                               name: notiOkStr,
                                               //: object: nil)
                                               object: nil)
    }

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: if isPlaying {
        if isPlaying {
            //: self.player?.stopPlay()
            self.player?.first()
        }
    }

    // MARK: - Lazy load

    //: private lazy var dataSourceArr: [TalkingGreetModel] = {
    private lazy var dataSourceArr: [RoundingRemovalModelType] = {
        //: var array = [TalkingGreetModel]()
        var array = [RoundingRemovalModelType]()
        //: return array
        return array
        //: }()
    }()

    //: lazy var MainTable: UITableView = {
    lazy var MainTable: UITableView = {
        //: let table = UITableView(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight-88), style: UITableView.Style.plain)
        let table = UITableView(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage - 88), style: UITableView.Style.plain)
        //: table.backgroundColor = UIColor.clear
        table.backgroundColor = UIColor.clear
        //: return table
        return table
        //: }()
    }()

    //: lazy var emptyView: EmptyView = {
    lazy var emptyView: EmptyView = {
        //: let view = EmptyView()
        let view = EmptyView()
        //: return view
        return view
        //: }()
    }()

    //: lazy var addButton: UIButton = {
    lazy var addButton: UIButton = {
        //: let btn = UIButton.init()
        let btn = UIButton()
        //: btn.setTitle("Add".localized, for: .normal)
        btn.setTitle((String(userRetirementContent.prefix(3))).localized, for: .normal)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.afterDisable(type: .Medium, fontSize: 18)
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        btn.setBackgroundImage(UIImage.tabOn(colors: UIColor.printColor(), size: CGSize(width: 267, height: 50)), for: .normal)
        //: btn.layer.cornerRadius = 25
        btn.layer.cornerRadius = 25
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(addButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(fastening), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Request & 数据处理

//: extension TalkingGreetAudioVC {
extension AudioViewController {
    //: func getAudioListData() {
    func tentData() {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(showRiteFormat.replacingOccurrences(of: "tender", with: "t"))] = 1
        //: ProgressHUD.show()
        VillageView.projectShow()
        //: TalkingMeRequestTool.req_GetGreetList(params: dict) {[weak self] succeed, result, errorModel in
        MeThen.unlessCompletion(params: dict) { [weak self] succeed, result, _ in

            //: ProgressHUD.dismiss()
            VillageView.statusDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if (self.MainTable.backgroundView == nil) {
            if self.MainTable.backgroundView == nil {
                //: self.setupEmptyDataView()
                self.candlelightView()
            }
            //: if succeed {
            if succeed {
                //: guard let dict = result as? Dictionary<String, Any> else {
                guard let dict = result as? [String: Any] else {
                    //: return
                    return
                }
                //: self.addStatus = dict["addStatus"] as! Int
                self.addStatus = dict[(String(mainStripQueryedMessage) + String(main_themStreetStr.suffix(4)) + "atus")] as! Int
                //: let array: Array =  dict["list"] as! Array<Any>
                let array: Array = dict[(String(user_calculationId))] as! [Any]
                //: var dataArr: [TalkingGreetModel] = []
                var dataArr: [RoundingRemovalModelType] = []
                //: for i in 0..<array.count {
                for i in 0 ..< array.count {
                    //: if let model = JSONDeserializer<TalkingGreetModel>.deserializeFrom(dict: array[i] as? Dictionary<String, Any>, designatedPath: nil) {
                    if let model = JSONDeserializer<RoundingRemovalModelType>.deserializeFrom(dict: array[i] as? [String: Any], designatedPath: nil) {
                        //: dataArr.append(model)
                        dataArr.append(model)
                    }
                }
                //: self.player = nil
                self.player = nil
                //: self.dataSourceArr = dataArr
                self.dataSourceArr = dataArr
                //: self.MainTable.reloadData()
                self.MainTable.reloadData()
            }
        }
    }

    //: func requestUploadAudioFileData(data: NSData, time: String) {
    func wallRequest(data: NSData, time: String) {
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["type"] = 1
        dict[(showRiteFormat.replacingOccurrences(of: "tender", with: "t"))] = 1
        //: dict["audio"] = data
        dict[(dream_administrativeData.replacingOccurrences(of: "spec", with: "u"))] = data
        //: dict["length"] = Int(time)
        dict[(noti_galleryTravelParticleIdent.replacingOccurrences(of: "draft", with: "l") + userWitchIdent.replacingOccurrences(of: "recommendation", with: "en"))] = Int(time)

        //: ProgressHUD.show()
        VillageView.projectShow()
        //: TalkingMeRequestTool.req_UploadGreetAdd(params: dict) {[weak self] succeed, result, errorModel in
        MeThen.previous(params: dict) { [weak self] _, result, _ in

            //: ProgressHUD.dismiss()
            VillageView.statusDismiss()
            //: guard let self = self else {return}
            guard let self = self else { return }

            //: guard let dict = result as? Dictionary<String, Any> else {
            guard let dict = result as? [String: Any] else {
                //: return
                return
            }
            //: let url: String = dict["url"] as! String
            let url: String = dict[(String(appShowPalaceStr))] as! String
            //: if !url.isEmptyString {
            if !url.isEmptyString {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Successfully added, please wait for approval".localized)
                self.bringHomePublishGap(showMsg: String(bytes: dream_clubCircleTitle.reversed(), encoding: .utf8)!.localized)
                //: self.getAudioListData()
                self.tentData()
                //: self.recordView?.dismiss()
                self.recordView?.server()
                //: self.recordView = nil
                self.recordView = nil
            }
        }
    }

    //: func shoWdeleteAlert(index: Int) {
    func someoneCapacity(index: Int) {
        //: let config = ShowAlertConfig()
        let config = GainGageAlertConfig()
        //: config.textFont = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        config.textFont = UIFont.afterDisable(type: .Medium, fontSize: 18)
        //: config.textlineBreakMode = .byCharWrapping
        config.textlineBreakMode = .byCharWrapping

        //: TalkingAlertShow.customAlert(message: "Are you sure to delete this message？".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "Delete".localized, leftBlock: {
        GentleAlertShow.begNativeConfig(message: String(bytes: showThickPrisonChinData.map{panRecover(phone: $0)}, encoding: .utf8)!.localized, leftBtnTitle: (String(mainMinuteButteTranslateIdent)).localized, rightBtnTitle: (String(mainWallValue.prefix(6))).localized, leftBlock: {
            //: TalkingAlertShow.hideAlert()
            GentleAlertShow.someButton()
            //: }, rightBlock: { [weak self] in
        }, rightBlock: { [weak self] in

            //: guard let self = self else {return}
            guard let self = self else { return }
            //: TalkingAlertShow.hideAlert()
            GentleAlertShow.someButton()
            //: self.stopPlay()
            self.backstop()

            //: let model: TalkingGreetModel = self.dataSourceArr[index]
            let model: RoundingRemovalModelType = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: TalkingMeRequestTool.req_DeleteAudio(params: dict) { succeed, result, errorModel in
            MeThen.numerate(params: dict) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: self.getAudioListData()
                self.tentData()
            }
            //: }, config: config)
        }, config: config)
    }
}

// MARK: - Event

//: extension TalkingGreetAudioVC {
extension AudioViewController {
    //: @objc func cancelPlayerAudio(notification: NSNotification) -> Void {
    @objc func birthday(notification _: NSNotification) {
        //: stopPlay()
        backstop()
    }

    /// 播放过程中中断
    //: @objc func interruptionChange(notification: NSNotification) -> Void {
    @objc func warningDirection(notification: NSNotification) {
        //: let dic = notification.userInfo as![String: AnyObject]
        let dic = notification.userInfo as! [String: AnyObject]

        //: let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        let changeReason = dic[AVAudioSessionInterruptionTypeKey]
        //: if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
        if changeReason?.intValue ?? 0 == AVAudioSession.InterruptionType.began.rawValue {
            //: stopPlay()
            backstop()
        }
    }

    //: @objc func addButtonClick() {
    @objc func fastening() {
        //: guard TalkingSocketManager.shared.isFind == false else {
        guard AdviceLandscapeReactiveCompatible.shared.isFind == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.iconThemeBringBackPlace(showMsg: m_buildNearbyData)
            //: return
            return
        }

        //: TalkingPermissionTool.func__openRecordServiceWithBlock(true) { isOpen in
        AsidePermissionTool.openGold(true) { isOpen in
            //: guard isOpen == true else {
            guard isOpen == true else {
                //: self.func__showStatusBarErrorMsg(showMsg: kMessage_permissions_error)
                self.iconThemeBringBackPlace(showMsg: dream_topKey)
                //: return
                return
            }
            //: if self.addStatus == 2 {
            if self.addStatus == 2 {
                //: self.func__showStatusBarErrorMsg(showMsg: "Please wait for the audit result".localized)
                self.iconThemeBringBackPlace(showMsg: String(bytes: dreamBecomeMsg.map{singleFile(bright: $0)}, encoding: .utf8)!.localized)
                //: return
                return
                    //: }else if (self.addStatus == 3) {
            } else if self.addStatus == 3 {
                //: self.func__showStatusBarErrorMsg(showMsg: "The upper limit has been reached".localized)
                self.iconThemeBringBackPlace(showMsg: String(bytes: userBoyFareMessage.map{customList(permission: $0)}, encoding: .utf8)!.localized)
                //: return
                return
            }
            //: self.stopPlay()
            self.backstop()
            //: self.recordView = TalkingGreetRecordAudioView.init(frame: self.view.frame)
            self.recordView = InjuryReadingView(frame: self.view.frame)
            //: self.recordView?.show()
            self.recordView?.havePost()
            //: self.recordView?.audioFileUpload = {[weak self] dict in
            self.recordView?.audioFileUpload = { [weak self] dict in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: let data = NSData.init(contentsOfFile: dict["filePath"] as! String)
                let data = NSData(contentsOfFile: dict[(String(noti_boxRefuseIdent) + mPartyWeightTitle.replacingOccurrences(of: "tender", with: "at"))] as! String)
                //: guard data != nil else {
                guard data != nil else {
                    //: return
                    return
                }
                //: self.requestUploadAudioFileData(data: data!, time: dict["time"] as! String)
                self.wallRequest(data: data!, time: dict[(String(dream_networkMsg))] as! String)
            }
        }
    }
}

//: extension TalkingGreetAudioVC {
extension AudioViewController {
    //: func setMutedDetection() {
    func holdfastDetection() {
        //: RBDMuteSwitch.shared.detectMuteSwitch()
        StatusThen.shared.year()
        //: RBDMuteSwitch.shared.isMutedBlock = { [weak self] mute in
        StatusThen.shared.isMutedBlock = { [weak self] mute in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.isMute = mute
            self.isMute = mute
        }
    }
}

// MARK: - UITableViewDelegate, UITableViewDataSource

//: extension TalkingGreetAudioVC: UITableViewDelegate, UITableViewDataSource {
extension AudioViewController: UITableViewDelegate, UITableViewDataSource {
    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return 1
        return 1
    }

    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        MainTable.backgroundView?.isHidden = dataSourceArr.count > 0
        //: MainTable.bounces = dataSourceArr.count > 0
        MainTable.bounces = dataSourceArr.count > 0
        //: return dataSourceArr.count
        return dataSourceArr.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return 101
        return 101
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let identifier = TalkingGreetAudioCell.className()
        let identifier = FlexibleViewCell.className()
        //: let cell: TalkingGreetAudioCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! TalkingGreetAudioCell
        let cell: FlexibleViewCell = tableView.dequeueReusableCell(withIdentifier: identifier, for: indexPath) as! FlexibleViewCell

        //: cell.setGreetAudioCell(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count-1)
        cell.equityWho(model: self.dataSourceArr[indexPath.section], row: indexPath.section, isLast: indexPath.section == self.dataSourceArr.count - 1)

        //: if isPlaying && playIndex?.row == indexPath.section {
        if isPlaying && playIndex?.row == indexPath.section {
            //: cell.syncPlayState(isPlaying: true)
            cell.atAdjustPlaying(isPlaying: true)
            //: }else {
        } else {
            //: cell.syncPlayState(isPlaying: false)
            cell.atAdjustPlaying(isPlaying: false)
        }

        //: cell.audioPlayActionBlock = { [weak self] in
        cell.audioPlayActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: RBDMuteSwitch.shared.detectMuteSwitch()
            StatusThen.shared.year()
            //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
            DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                //: self.playAudio(index: indexPath)
                self.botIndex(index: indexPath)
            }
        }
        //: cell.editActionBlock = { [weak self] in
        cell.editActionBlock = { [weak self] in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.stopPlay()
            self.backstop()
            //: self.editTitle(index: indexPath.section)
            self.isAdd(index: indexPath.section)
        }
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_: UITableView, didSelectRowAt _: IndexPath) {}

    // ios11以前
    //: func tableView(_ tableView: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
    func tableView(_: UITableView, editActionsForRowAt indexPath: IndexPath) -> [UITableViewRowAction]? {
        //: let deleteBtn = UITableViewRowAction(style: .normal, title: "Delete".localized) { action, indexP in
        let deleteBtn = UITableViewRowAction(style: .normal, title: (String(mainWallValue.prefix(6))).localized) { _, _ in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.someoneCapacity(index: indexPath.section)
        }
        //: deleteBtn.backgroundColor = UIColor(hex: "#FF506D")
        deleteBtn.backgroundColor = UIColor(hex: (String(userRequestSubFormat.prefix(5)) + String(appBeautifulCircleNearMsg.prefix(2))))

        //: return [deleteBtn]
        return [deleteBtn]
    }

    //: @available(iOS 11.0, *)
    @available(iOS 11.0, *)
    //: func tableView(_ tableView: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
    func tableView(_: UITableView, trailingSwipeActionsConfigurationForRowAt indexPath: IndexPath) -> UISwipeActionsConfiguration? {
        //: let deleteBtn = UIContextualAction(style: .normal, title: nil) { action, view, completionHandler in
        let deleteBtn = UIContextualAction(style: .normal, title: nil) { _, _, completionHandler in

            //: self.shoWdeleteAlert(index: indexPath.section)
            self.someoneCapacity(index: indexPath.section)
            //: completionHandler(false)
            completionHandler(false)
        }

        //: if let deleteBtnTrashcan =  UIImage.BundleImageNamed(name: "btn_message_delete_nor").cgImage {
        if let deleteBtnTrashcan = UIImage.scribeNumber(name: (String(user_captainName) + String(dream_wallDenyName))).cgImage {
            //: deleteBtn.image = ImageWithoutRender(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
            deleteBtn.image = ConcentrationImage(cgImage: deleteBtnTrashcan, scale: UIScreen.main.nativeScale, orientation: .up)
        }
        //: deleteBtn.backgroundColor = .white
        deleteBtn.backgroundColor = .white
        //: let actions = [deleteBtn]
        let actions = [deleteBtn]
        //: let config = UISwipeActionsConfiguration(actions: actions)
        let config = UISwipeActionsConfiguration(actions: actions)
        //: config.performsFirstActionWithFullSwipe = false
        config.performsFirstActionWithFullSwipe = false
        //: return config
        return config
    }

    //: func playAudio(index: IndexPath) {
    func botIndex(index: IndexPath) {
        //: guard !TalkingSocketManager.shared.isFind else {
        guard !AdviceLandscapeReactiveCompatible.shared.isFind else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.iconThemeBringBackPlace(showMsg: m_buildNearbyData)
            //: return
            return
        }
        //: if isPlaying && playIndex == index {
        if isPlaying && playIndex == index {
            //: isPlaying = false
            isPlaying = false
            //: MainTable.reloadData()
            MainTable.reloadData()
            //: self.player?.stopPlay()
            self.player?.first()
            //: return
            return
        }
        //: if isPlaying {
        if isPlaying {
            //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
            currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? FlexibleViewCell
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.atAdjustPlaying(isPlaying: false)
            }
        }

        //: playIndex = index
        playIndex = index
        //: let model = self.dataSourceArr[index.section]
        let model = self.dataSourceArr[index.section]

        //: if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
        if isMute || AVAudioSession.sharedInstance().outputVolume <= 0 {
            //: self.func__showStatusBarErrorMsg(showMsg: "Currently mute".localized)
            self.iconThemeBringBackPlace(showMsg: (String(show_likelihoodFormat) + String(kMutualFormat)).localized)
            //: return
            return
        }
        //: if self.player == nil {
        if self.player == nil {
            //: self.player = TalkingAudioPlayTool.shared
            self.player = SaveDetectReactiveCompatible.shared
            //: self.player?.delegate = self
            self.player?.delegate = self
        }
        //: self.player?.stopPlay()
        self.player?.first()
        //: self.player?.play(urlStr: model.content)
        self.player?.bubble(urlStr: model.content)
        //: isPlaying = true
        isPlaying = true
        //: currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? TalkingGreetAudioCell
        currenCell = MainTable.cellForRow(at: playIndex ?? IndexPath()) as? FlexibleViewCell
        //: if currenCell != nil {
        if currenCell != nil {
            //: currenCell?.syncPlayState(isPlaying: true)
            currenCell?.atAdjustPlaying(isPlaying: true)
        }
    }

    //: func editTitle(index: Int) {
    func isAdd(index: Int) {
        //: let view = TalkingEditAlertView.init(title: "Edit Title".localized, leftBtnTitle: "Cancel".localized, rightBtnTitle: "OK".localized, config: nil)
        let view = CompensationReactiveCompatible(title: (String(appDisplayMsg.prefix(8)) + String(m_asideDirectorName)).localized, leftBtnTitle: (String(mainMinuteButteTranslateIdent)).localized, rightBtnTitle: "OK".localized, config: nil)
        //: view.show()
        view.advanced()

        //: view.rightBlock = { [weak self] text in
        view.rightBlock = { [weak self] text in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: ProgressHUD.show()
            VillageView.projectShow()

            //: var model: TalkingGreetModel = self.dataSourceArr[index]
            var model: RoundingRemovalModelType = self.dataSourceArr[index]
            //: var dict = Dictionary<String, Any>()
            var dict = [String: Any]()
            //: dict["id"] = model.greetId
            dict["id"] = model.greetId
            //: dict["remark"] = text
            dict[(notiRidIdent.replacingOccurrences(of: "extent", with: "r") + dreamWrapComplexityMessage.replacingOccurrences(of: "anniversary", with: "ma"))] = text

            //: TalkingMeRequestTool.req_UpdateAudioRemark(params: dict) { succeed, result, errorModel in
            MeThen.wad(params: dict) { succeed, _, _ in
                //: ProgressHUD.dismiss()
                VillageView.statusDismiss()
                //: if succeed {
                if succeed {
                    //: model.remark = text
                    model.remark = text
                    //: self.dataSourceArr[index] = model
                    self.dataSourceArr[index] = model
                    //: self.MainTable.reloadData()
                    self.MainTable.reloadData()
                }
            }
        }
    }

    //: @objc func stopPlay() {
    @objc func backstop() {
        //: if isPlaying {
        if isPlaying {
            //: isPlaying = false
            isPlaying = false
            //: self.player?.stopPlay()
            self.player?.first()
            //: if currenCell != nil {
            if currenCell != nil {
                //: currenCell?.syncPlayState(isPlaying: false)
                currenCell?.atAdjustPlaying(isPlaying: false)
            }
        }
    }
}

//: extension TalkingGreetAudioVC: TalkingAudioPlayToolDelegate {
extension AudioViewController: SmoothenReactiveCompatible {
    //: func playerStatusChange(status: AudioPlayerStatus) {
    func anEvaluateSound(status: TwoTitleConvertible) {
        //: switch status {
        switch status {
        //: case .PlayerFinish:
        case .PlayerFinish:
            //: stopPlay()
            backstop()
        //: break
        //: case .PlayerInterruption:
        case .PlayerInterruption:
            //: stopPlay()
            backstop()
        //: break
        //: default:
        default:
            //: break
            break
        }
    }
}

// MARK: - Layout

//: extension TalkingGreetAudioVC {
extension AudioViewController {
    //: private func setupEmptyDataView() {
    private func candlelightView() {
        //: var style = EmptyStyle()
        var style = PickEmptyStyle()
        //: style.TipsTitle = "Click the button below to add\ncontent".localized
        style.TipsTitle = String(bytes: userKitMsg.map{structuralAnthropology(glass: $0)}, encoding: .utf8)!.localized
        //: emptyView = EmptyView.init(frame: self.view.frame, style: style)
        emptyView = EmptyView(frame: self.view.frame, style: style)
        //: emptyView.isHidden = true
        emptyView.isHidden = true
        //: emptyView.isScrollEnabled = false
        emptyView.isScrollEnabled = false
        //: emptyView.backgroundColor = .appBgColor()
        emptyView.backgroundColor = .landmarkBalance()
        //: MainTable.backgroundView = emptyView
        MainTable.backgroundView = emptyView

        //: emptyView.emptyBlock = { [weak self] in
        emptyView.emptyBlock = { [weak self] in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: self.MainTable.mj_header?.beginRefreshing()
            self.MainTable.mj_header?.beginRefreshing()
        }
    }

    //: private func designView() {
    private func sumryIdentity() {
        //: self.view.addSubview(addButton)
        self.view.addSubview(addButton)
        //: addButton.snp.makeConstraints { make in
        addButton.snp.makeConstraints { make in
            //: make.bottom.equalToSuperview().offset(-34)
            make.bottom.equalToSuperview().offset(-34)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: 267, height: 50))
            make.size.equalTo(CGSize(width: 267, height: 50))
        }
        //: self.view.addSubview(MainTable)
        self.view.addSubview(MainTable)
        //: MainTable.register(TalkingGreetAudioCell.self, forCellReuseIdentifier: TalkingGreetAudioCell.className())
        MainTable.register(FlexibleViewCell.self, forCellReuseIdentifier: FlexibleViewCell.className())
        //: MainTable.separatorStyle = .none
        MainTable.separatorStyle = .none
        //: MainTable.dataSource = self
        MainTable.dataSource = self
        //: MainTable.delegate = self
        MainTable.delegate = self
        //: MainTable.snp.makeConstraints { make in
        MainTable.snp.makeConstraints { make in
            //: make.edges.equalTo(self.view)
            make.edges.equalTo(self.view)
            //: make.bottom.equalTo(addButton.snp.top).offset(-16)
            make.bottom.equalTo(addButton.snp.top).offset(-16)
        }
        //: MainTable.tableFooterView = UIView.init()
        MainTable.tableFooterView = UIView()

        //: let headerView = UIView.init(frame: CGRect.init(x: 0, y: 0, width: ScreenWidth, height: 145))
        let headerView = UIView(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: 145))
        //: let label = UILabel.init()
        let label = UILabel()
        //: label.textColor = UIColor.appValueDetailColor()
        label.textColor = UIColor.fluent()
        //: label.font = UIFont.pingfangRugularFont(fontSize: 15)
        label.font = UIFont.soil(fontSize: 15)
        //: label.numberOfLines = 0
        label.numberOfLines = 0
        //: let string = "Add multiple voice greetings, and the system will send voice messages by turns;\nClick the bubble to play the voice, slide to the voice area to delete it, and the voice title can be edited.".localized
        let string = String(bytes: app_nobodyUrl.map{fosterLancePose(record: $0)}, encoding: .utf8)!.localized
        //: let attri = NSMutableAttributedString.init(string: string)
        let attri = NSMutableAttributedString(string: string)
        //: let style = NSMutableParagraphStyle.init()
        let style = NSMutableParagraphStyle()
        //: style.lineSpacing = 2
        style.lineSpacing = 2
        //: attri.yy_paragraphStyle = style
        attri.yy_paragraphStyle = style
        //: label.attributedText = attri
        label.attributedText = attri
        //: headerView.addSubview(label)
        headerView.addSubview(label)
        //: label.snp.makeConstraints { make in
        label.snp.makeConstraints { make in
            //: make.leading.equalTo(30)
            make.leading.equalTo(30)
            //: make.trailing.equalTo(-30)
            make.trailing.equalTo(-30)
            //: make.bottom.equalTo(-16)
            make.bottom.equalTo(-16)
            //: make.top.equalTo(12)
            make.top.equalTo(12)
        }
        //: MainTable.tableHeaderView = headerView
        MainTable.tableHeaderView = headerView
    }
}
