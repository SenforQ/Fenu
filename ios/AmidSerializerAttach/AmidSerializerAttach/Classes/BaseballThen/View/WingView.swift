
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dream_liveUrl:[UInt8] = [0xac,0xb1,0xac,0xb7,0x6b,0xa6,0xb2,0xa7,0xa8,0xb5,0x7d,0x6c,0x63,0xab,0xa4,0xb6,0x63,0xb1,0xb2,0xb7,0x63,0xa5,0xa8,0xa8,0xb1,0x63,0xac,0xb0,0xb3,0xaf,0xa8,0xb0,0xa8,0xb1,0xb7,0xa8,0xa7]

fileprivate func artifactShip(scan num: UInt8) -> UInt8 {
    let value = Int(num) + 189
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "Cancel" :*/
fileprivate let userTrailSmoothValue:String = "c"
fileprivate let kNobodyValue:[Character] = ["a","n","c","e","l"]

/*: "Submit" :*/
fileprivate let dreamMeetContent:String = "Submitend expected"

/*: "Please tell us your problems                   " :*/
fileprivate let notiMentionPath:[UInt8] = [0xd8,0xf4,0xed,0xe9,0xfb,0xed,0xa8,0xfc,0xed,0xf4,0xf4,0xa8,0xfd,0xfb,0xa8,0x1,0xf7,0xfd,0xfa,0xa8,0xf8,0xfa,0xf7,0xea,0xf4,0xed,0xf5,0xfb,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8,0xa8]

fileprivate func missingCycle(represent num: UInt8) -> UInt8 {
    let value = Int(num) + 120
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "You need to choose at least one." :*/
fileprivate let userDirectMsg:[UInt8] = [0xaf,0xc5,0xcb,0x76,0xc4,0xbb,0xbb,0xba,0x76,0xca,0xc5,0x76,0xb9,0xbe,0xc5,0xc5,0xc9,0xbb,0x76,0xb7,0xca,0x76,0xc2,0xbb,0xb7,0xc9,0xca,0x76,0xc5,0xc4,0xbb,0x84]

fileprivate func forwardTolerance(glass num: UInt8) -> UInt8 {
    let value = Int(num) - 86
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Please tell us your problems." :*/
fileprivate let k_ridThirdPleaseData:String = "Pleashold hundred difference recommendation"
fileprivate let kFrontSplitData:[Character] = ["e"," ","t","e","l","l"," ","u","s"," ","y","o","u","r"," "]
fileprivate let mainObjectPriorityId:String = "clothe period canproblems."

/*: "app/userReport" :*/
fileprivate let dreamLaunchFormat:[Character] = ["a","p","p","/","u","s","e","r","R","e","p"]
fileprivate let app_plainMsg:[Character] = ["o","r","t"]

/*: "reason" :*/
fileprivate let m_commandTransactionKey:String = "pan"
fileprivate let kConceptMessage:String = "ealsoon"

/*: "desc" :*/
fileprivate let app_interestName:[UInt8] = [0x94,0x95,0xa3,0x93]

fileprivate func mightCeremony(source num: UInt8) -> UInt8 {
    let value = Int(num) - 48
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "mf/moment/report" :*/
fileprivate let dream_frameIdent:[Character] = ["m","f","/","m","o"]
fileprivate let userCastControlFormat:String = "contentn"
fileprivate let dreamCurrencyContent:String = "t/reportsweet guidance"

/*: "momentId" :*/
fileprivate let showDueMsg:[UInt8] = [0x64,0x49,0x74,0x6e,0x65,0x6d,0x6f,0x6d]

/*: "mf/momentReply/report" :*/
fileprivate let mainMentionValue:String = "light encounter outputmf/m"
fileprivate let main_frameData:String = "tReplybridge gage prison boa fairly"
fileprivate let show_thinkTakeId:[Character] = ["/"]
fileprivate let m_ovalValue:[Character] = ["r","e","p","o","r","t"]

/*: "app/videoReport" :*/
fileprivate let k_himMsg:[Character] = ["a","p","p","/","v","i","d","e","o","R","e","p","o","r","t"]

/*: "logId" :*/
fileprivate let dreamFairPath:[UInt8] = [0xc6,0xc5,0xcd,0xe3,0xce]

private func wireHimScale(describe num: UInt8) -> UInt8 {
    return num ^ 170
}

/*: "Submitted successfully" :*/
fileprivate let appEaseUrl:[Character] = ["S","u","b","m","i","t","t","e"]
fileprivate let k_fiscalLayName:String = "draft sale returnd su"
fileprivate let dreamSampleName:String = "saccelerateully"

/*: "Nudity" :*/
fileprivate let mainListFormat:[Character] = ["N","u","d","i","t","y"]

/*: "Violence" :*/
fileprivate let user_whoMoveData:String = "Violestretch audience care"
fileprivate let showDetectStr:[Character] = ["n","c","e"]

/*: "Harassment" :*/
fileprivate let show_sometimeName:String = "Harasmemory tun trade"
fileprivate let noti_likeTitle:[Character] = ["s","m","e","n","t"]

/*: "Suicide or Self-injury" :*/
fileprivate let mSubmitAideContent:[Character] = ["S"]
fileprivate let noti_behaviorKey:String = "umidid"
fileprivate let k_remainingPath:String = " Selanswer priority openly net"

/*: "False Information" :*/
fileprivate let mBlueValue:String = "about waitFalse "
fileprivate let k_asternPath:String = "matillio"
fileprivate let user_partUrl:String = "amber"

/*: "Spam" :*/
fileprivate let notiPackagePath:[Character] = ["S","p","a","m"]

/*: "Hate Speech" :*/
fileprivate let showDrawTimeFormat:[Character] = ["H","a","t","e"," "]
fileprivate let k_littleFormat:String = "Speechcap edge motivation prompt execute"

/*: "Terrorism" :*/
fileprivate let mainVoiceTitle:String = "you fade stepTerr"

/*: "Sale of Illegal or Regulated Goods" :*/
fileprivate let user_policyStr:[UInt8] = [0xa9,0xb7,0xc2,0xbb,0x76,0xc5,0xbc,0x76,0x9f,0xc2,0xc2,0xbb,0xbd,0xb7,0xc2,0x76,0xc5,0xc8,0x76,0xa8,0xbb,0xbd,0xcb,0xc2,0xb7,0xca,0xbb,0xba,0x76,0x9d,0xc5,0xc5,0xba,0xc9]

fileprivate func currentLat(lust num: UInt8) -> UInt8 {
    let value = Int(num) - 86
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "Sending disgusting messages" :*/
fileprivate let userFutureData:[Character] = ["S","e","n","d","i"]
fileprivate let m_inmatePath:[Character] = ["n","g"," ","d","i","s","g","u","s","t","i","n","g"," ","m","e","s","s","a","g","e","s"]

/*: "Using photos of nudity or drugs" :*/
fileprivate let noti_wallName:[UInt8] = [0x73,0x67,0x75,0x72,0x64,0x20,0x72,0x6f,0x20,0x79,0x74,0x69,0x64,0x75,0x6e,0x20,0x66,0x6f,0x20,0x73,0x6f,0x74,0x6f,0x68,0x70,0x20,0x67,0x6e,0x69,0x73,0x55]

/*: "Harassing or sending ads" :*/
fileprivate let userTargetId:[Character] = ["H","a","r","a","s","s","i"]
fileprivate let app_presentationDoinglyMessage:String = "ng or sethen judicial makeup out harvest"
fileprivate let noti_limitValue:String = "nding adsappear wood"

/*: "Defraud or using fake info" :*/
fileprivate let dreamRichIdent:String = "Defraudrecommend portrait simply action"
fileprivate let user_sortUrl:[Character] = [" ","o","r"," ","u","s","i","n","g"," ","f","a","k","e"," ","i","n","f","o"]

/*: "Others" :*/
fileprivate let appBoardFormat:String = "edge"
fileprivate let user_monitorName:String = "easehers"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  WingView.swift
//  AbroadTalking
//
//  Created by Bolo on 2022/9/14.
//

//: import Then
import Then
//: import UIKit
import UIKit

//: enum TalkingReportAlertType: CaseIterable {
enum MisestimationCaseIterable: CaseIterable {
    //: case reportCommentType
    case reportCommentType // 举报动态
    //: case reportDynamicType
    case reportDynamicType
    //: case reportUserType
    case reportUserType // 举报用户
    //: case reportVideo
    case reportVideo // 举报视频通话
}

// 是否举报成功block
//: typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void
typealias ReportViewHideBlock = (_ isReportSucc: Bool) -> Void

//: class TalkingReportAlertView: UIView {
class WingView: UIView {
    //: var reportType: TalkingReportAlertType?
    var reportType: MisestimationCaseIterable?
    //: var dataArray: Array<TalkingReportModel> = []
    var dataArray: [PosterReportModel] = []
    //: var selectedModel = TalkingReportModel()
    var selectedModel = PosterReportModel()
    //: var tempId: String = ""
    var tempId: String = ""
    //: var reportHideBlock: ReportViewHideBlock?
    var reportHideBlock: ReportViewHideBlock?
    //: let disposeBag = DisposeBag()
    let disposeBag = DisposeBag()

    //: public init(frame: CGRect, type: TalkingReportAlertType?, rId: String) {
    public init(frame: CGRect, type: MisestimationCaseIterable?, rId: String) {
        //: tempId = rId
        tempId = rId
        //: reportType = type
        reportType = type
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        routeRender()
        //: setupSubViewsConstraint()
        bagRecent()
        //: setupReasons()
        laughAway()
        //: bindInteraction()
        alive()
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dream_liveUrl.map{artifactShip(scan: $0)}, encoding: .utf8)!)
    }

    // MARK: - Lazy load

    //: lazy var cancelButton = UIButton().then {
    lazy var cancelButton = UIButton().then {
        //: $0.setTitle("Cancel".localized, for: .normal)
        $0.setTitle((userTrailSmoothValue.uppercased() + String(kNobodyValue)).localized, for: .normal)
        //: $0.setTitleColor(UIColor.appThemeColor(), for: .normal)
        $0.setTitleColor(UIColor.fogColor(), for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.afterDisable(type: .Medium, fontSize: 17)
        //: $0.backgroundColor = UIColor.white
        $0.backgroundColor = UIColor.white
        //: $0.layer.borderWidth = 1
        $0.layer.borderWidth = 1
        //: $0.layer.borderColor = UIColor.appThemeColor().cgColor
        $0.layer.borderColor = UIColor.fogColor().cgColor
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerCancelAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(patentListener), for: .touchUpInside)
    }

    //: lazy var submitButton = UIButton().then {
    lazy var submitButton = UIButton().then {
        //: $0.setTitle("Submit".localized, for: .normal)
        $0.setTitle((String(dreamMeetContent.prefix(6))).localized, for: .normal)
        //: $0.setTitleColor(UIColor.white, for: .normal)
        $0.setTitleColor(UIColor.white, for: .normal)
        //: $0.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 17)
        $0.titleLabel?.font = UIFont.afterDisable(type: .Medium, fontSize: 17)
        //: $0.setBackgroundImage(UIImage.imageGradientColor(colors: UIColor.appGradientColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        $0.setBackgroundImage(UIImage.tabOn(colors: UIColor.printColor(), size: CGSize(width: 126, height: 50)), for: .normal)
        //: $0.layer.cornerRadius = 25
        $0.layer.cornerRadius = 25
        //: $0.clipsToBounds = true
        $0.clipsToBounds = true
        //: $0.addTarget(self, action: #selector(registerSubmitAction), for: .touchUpInside)
        $0.addTarget(self, action: #selector(reply), for: .touchUpInside)
    }

    //: lazy var tableFooterView = UIView().then {
    lazy var tableFooterView = UIView().then {
        //: $0.frame = CGRect(x: 0, y: 0, width: ScreenWidth-actualWidth(w: 108), height: actualHeight(h: 74))
        $0.frame = CGRect(x: 0, y: 0, width: noti_senseContent - actualWidth(w: 108), height: actualHeight(h: 74))
        //: $0.backgroundColor = UIColor.RGBA(245, 245, 248, 1)
        $0.backgroundColor = UIColor.fruitThumb(245, 245, 248, 1)
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 5
        $0.layer.cornerRadius = 5
    }

    //: lazy var otherTextView = PlaceholderTextView(placeholder: "Please tell us your problems                   ".localized, placeholderColor: UIColor.appValueDetailColor(), frame: CGRect(x: 10, y: 10, width: ScreenWidth-actualWidth(w: 128), height: actualHeight(h: 54))).then {
    lazy var otherTextView = InstanceNameView(placeholder: String(bytes: notiMentionPath.map{missingCycle(represent: $0)}, encoding: .utf8)!.localized, placeholderColor: UIColor.fluent(), frame: CGRect(x: 10, y: 10, width: noti_senseContent - actualWidth(w: 128), height: actualHeight(h: 54))).then {
        //: $0.backgroundColor = .clear
        $0.backgroundColor = .clear
        //: $0.placeholdertextView.backgroundColor = .clear
        $0.placeholdertextView.backgroundColor = .clear
        //: $0.limitWords = 200
        $0.limitWords = 200
        //: $0.isShowCountLabel = false
        $0.isShowCountLabel = false
        //: $0.isReturnHidden = true
        $0.isReturnHidden = true
        //: $0.plaleLabel.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.plaleLabel.font = .afterDisable(type: .Regular, fontSize: 16)
        //: $0.placeholdertextView.textColor = UIColor.appTitleColor()
        $0.placeholdertextView.textColor = UIColor.manualColor()
        //: $0.placeholdertextView.font = .pingfangFont(type: .Regular, fontSize: 16)
        $0.placeholdertextView.font = .afterDisable(type: .Regular, fontSize: 16)
    }

    //: lazy var bgView = UIView().then {
    lazy var bgView = UIView().then {
        //: $0.backgroundColor = UIColor.RGBA(0, 0, 0, 0.6)
        $0.backgroundColor = UIColor.fruitThumb(0, 0, 0, 0.6)
    }

    //: lazy var containerView = UIView().then {
    lazy var containerView = UIView().then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.layer.masksToBounds = true
        $0.layer.masksToBounds = true
        //: $0.layer.cornerRadius = 15
        $0.layer.cornerRadius = 15
    }

    //: lazy var tableView = UITableView.init(frame: .zero, style: .plain).then {
    lazy var tableView = UITableView(frame: .zero, style: .plain).then {
        //: $0.backgroundColor = .white
        $0.backgroundColor = .white
        //: $0.separatorStyle = .none
        $0.separatorStyle = .none
        //: $0.showsVerticalScrollIndicator = false
        $0.showsVerticalScrollIndicator = false
        //: $0.delegate = self
        $0.delegate = self
        //: $0.dataSource = self
        $0.dataSource = self
        //: if reportType != .reportCommentType, reportType != .reportVideo {
        if reportType != .reportCommentType, reportType != .reportVideo {
            //: $0.tableFooterView = self.tableFooterView
            $0.tableFooterView = self.tableFooterView
        }
        //: $0.register(TalkingReportViewCell.self, forCellReuseIdentifier: TalkingReportViewCell.className())
        $0.register(ApprovalView.self, forCellReuseIdentifier: ApprovalView.className())
    }
}

// MARK: - Bind && Event

//: extension TalkingReportAlertView {
extension WingView {
    //: @objc func func__TapGestureRecognizer() {
    @objc func nationalService() {
        //: self.endEditing(true)
        self.endEditing(true)
    }

    //: public func showReportViewIn(view: UIView?) {
    public func smartPress(view: UIView?) {
        //: if view == nil {
        if view == nil {
            //: let window = CreditsThen.getWindow()
            let window = CreditsThen.captain()
            //: window.addSubview(self)
            window.addSubview(self)
            //: return
            return
        }
        //: view?.addSubview(self)
        view?.addSubview(self)
    }

    /// 移除视图
    /// - Parameter succeed: 是否举报成功
    //: private func dismissReportView(succeed: Bool) {
    private func ban(succeed: Bool) {
        //: reportHideBlock?(succeed)
        reportHideBlock?(succeed)
        //: self.removeFromSuperview()
        self.removeFromSuperview()
    }

    /// 取消
    //: @objc func registerCancelAction() {
    @objc func patentListener() {
        //: self.dismissReportView(succeed: false)
        self.ban(succeed: false)
    }

    /// 提交举报内容
    //: @objc func registerSubmitAction() {
    @objc func reply() {
        //: if selectedModel.selected == false {
        if selectedModel.selected == false {
            //: self.func__showStatusBarErrorMsg(showMsg: "You need to choose at least one.".localized)
            self.iconThemeBringBackPlace(showMsg: String(bytes: userDirectMsg.map{forwardTolerance(glass: $0)}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
        if reportType == .reportUserType, otherTextView.placeholdertextView.text.isEmptyString {
            //: self.func__showStatusBarErrorMsg(showMsg: "Please tell us your problems.".localized)
            self.iconThemeBringBackPlace(showMsg: (String(k_ridThirdPleaseData.prefix(5)) + String(kFrontSplitData) + String(mainObjectPriorityId.suffix(9))).localized)
            //: return
            return
        }

        //: var param: [String: String] = [:]
        var param: [String: String] = [:]
        //: var path: String = ""
        var path = ""

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportUserType:
        case .reportUserType:
            //: path = "app/userReport"
            path = (String(dreamLaunchFormat) + String(app_plainMsg))
            //: param = ["id": tempId, "reason": selectedModel.type, "desc": otherTextView.placeholdertextView.text]
            param = ["id": tempId, (m_commandTransactionKey.replacingOccurrences(of: "pan", with: "r") + kConceptMessage.replacingOccurrences(of: "also", with: "as")): selectedModel.type, String(bytes: app_interestName.map{mightCeremony(source: $0)}, encoding: .utf8)!: otherTextView.placeholdertextView.text]

        //: case .reportDynamicType:
        case .reportDynamicType:
            //: path = "mf/moment/report"
            path = (String(dream_frameIdent) + userCastControlFormat.replacingOccurrences(of: "content", with: "me") + String(dreamCurrencyContent.prefix(8)))
            //: param = ["momentId": tempId, "reason": selectedModel.type]
            param = [String(bytes: showDueMsg.reversed(), encoding: .utf8)!: tempId, (m_commandTransactionKey.replacingOccurrences(of: "pan", with: "r") + kConceptMessage.replacingOccurrences(of: "also", with: "as")): selectedModel.type]

        //: case .reportCommentType:
        case .reportCommentType:
            //: path = "mf/momentReply/report"
            path = (String(mainMentionValue.suffix(4)) + "omen" + String(main_frameData.prefix(6)) + String(show_thinkTakeId) + String(m_ovalValue))
            //: param = ["id": tempId, "reason": selectedModel.type]
            param = ["id": tempId, (m_commandTransactionKey.replacingOccurrences(of: "pan", with: "r") + kConceptMessage.replacingOccurrences(of: "also", with: "as")): selectedModel.type]

        //: case .reportVideo:
        case .reportVideo:
            //: path = "app/videoReport"
            path = (String(k_himMsg))
            //: param = ["logId": tempId, "reason": selectedModel.type]
            param = [String(bytes: dreamFairPath.map{wireHimScale(describe: $0)}, encoding: .utf8)!: tempId, (m_commandTransactionKey.replacingOccurrences(of: "pan", with: "r") + kConceptMessage.replacingOccurrences(of: "also", with: "as")): selectedModel.type]

        //: default:
        default:
            //: break
            break
        }

        //: self.submitButton.isEnabled = false
        self.submitButton.isEnabled = false
        //: TalkingUserRequestManager.func__reportRequest(reqPath: path, reqParams: param) { succeed, result, errorModel in
        AdvocateReactiveCompatible.unprocessed(reqPath: path, reqParams: param) { succeed, _, _ in
            //: self.submitButton.isEnabled = true
            self.submitButton.isEnabled = true
            //: if succeed == true {
            if succeed == true {
                //: self.func__showStatusBarSuccessMsg(showMsg: "Submitted successfully".localized)
                self.bringHomePublishGap(showMsg: (String(appEaseUrl) + String(k_fiscalLayName.suffix(4)) + "cces" + dreamSampleName.replacingOccurrences(of: "accelerate", with: "f")).localized)
                //: self.dismissReportView(succeed: true)
                self.ban(succeed: true)
            }
        }
    }
}

// MARK: - Delegate

//: extension TalkingReportAlertView: UITableViewDelegate, UITableViewDataSource {
extension WingView: UITableViewDelegate, UITableViewDataSource {
    //: func numberOfSections(in tableView: UITableView) -> Int {
    func numberOfSections(in _: UITableView) -> Int {
        //: return 1
        return 1
    }

    //: func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
    func tableView(_: UITableView, numberOfRowsInSection _: Int) -> Int {
        //: return dataArray.count
        return dataArray.count
    }

    //: func tableView(_ tableView: UITableView, heightForRowAt indexPath: IndexPath) -> CGFloat {
    func tableView(_: UITableView, heightForRowAt _: IndexPath) -> CGFloat {
        //: return actualHeight(h: 40)
        return actualHeight(h: 40)
    }

    //: func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        //: let cell = tableView.dequeueReusableCell(withIdentifier: TalkingReportViewCell.className()) as! TalkingReportViewCell
        let cell = tableView.dequeueReusableCell(withIdentifier: ApprovalView.className()) as! ApprovalView
        //: let model = dataArray[indexPath.item]
        let model = dataArray[indexPath.item]
        //: cell.updateReportCell(model: model)
        cell.face(model: model)
        //: return cell
        return cell
    }

    //: func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
    func tableView(_ tableView: UITableView, didSelectRowAt indexPath: IndexPath) {
        //: for model in dataArray {
        for model in dataArray {
            //: model.selected = false
            model.selected = false
        }

        //: let itemModel = dataArray[indexPath.item]
        let itemModel = dataArray[indexPath.item]
        //: itemModel.selected = true
        itemModel.selected = true
        //: self.selectedModel = itemModel
        self.selectedModel = itemModel

        //: tableView.reloadData()
        tableView.reloadData()
    }
}

// MARK: - Layout

//: extension TalkingReportAlertView {
extension WingView {
    /// 添加视图
    //: private func setupSubviews() {
    private func routeRender() {
        //: addSubview(bgView)
        addSubview(bgView)
        //: addSubview(containerView)
        addSubview(containerView)
        //: containerView.addSubview(tableView)
        containerView.addSubview(tableView)
        //: containerView.addSubview(cancelButton)
        containerView.addSubview(cancelButton)
        //: containerView.addSubview(submitButton)
        containerView.addSubview(submitButton)
        //: tableFooterView.addSubview(otherTextView)
        tableFooterView.addSubview(otherTextView)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func bagRecent() {
        //: var height = actualHeight(h: 325)
        var height = actualHeight(h: 325)
        //: if reportType == .reportUserType {
        if reportType == .reportUserType {
            //: height = actualHeight(h: 375)
            height = actualHeight(h: 375)
            //: } else if reportType == .reportVideo {
        } else if reportType == .reportVideo {
            //: height = actualHeight(h: 338)
            height = actualHeight(h: 338)
        }
        //: let topY = (self.frame.height - height) / 2.0
        let topY = (self.frame.height - height) / 2.0

        //: bgView.snp.makeConstraints { make in
        bgView.snp.makeConstraints { make in
            //: make.leading.top.trailing.bottom.equalTo(0)
            make.leading.top.trailing.bottom.equalTo(0)
        }
        //: containerView.snp.makeConstraints { make in
        containerView.snp.makeConstraints { make in
            //: make.top.equalTo(topY)
            make.top.equalTo(topY)
            //: make.leading.equalTo(actualWidth(w: 40))
            make.leading.equalTo(actualWidth(w: 40))
            //: make.trailing.equalTo(actualWidth(w: -40))
            make.trailing.equalTo(actualWidth(w: -40))
            //: make.height.equalTo(height)
            make.height.equalTo(height)
        }
        //: tableView.snp.makeConstraints { make in
        tableView.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.top.equalTo(actualHeight(h: 36))
            make.top.equalTo(actualHeight(h: 36))
            //: if reportType == .reportVideo {
            if reportType == .reportVideo {
                //: make.bottom.equalTo(actualHeight(h: -62))
                make.bottom.equalTo(actualHeight(h: -62))
                //: } else {
            } else {
                //: make.bottom.equalTo(actualHeight(h: -82))
                make.bottom.equalTo(actualHeight(h: -82))
            }
        }
        //: cancelButton.snp.makeConstraints { make in
        cancelButton.snp.makeConstraints { make in
            //: make.leading.equalTo(actualWidth(w: 14))
            make.leading.equalTo(actualWidth(w: 14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
        //: submitButton.snp.makeConstraints { make in
        submitButton.snp.makeConstraints { make in
            //: make.trailing.equalTo(actualWidth(w: -14))
            make.trailing.equalTo(actualWidth(w: -14))
            //: make.bottom.equalTo(actualHeight(h: -16))
            make.bottom.equalTo(actualHeight(h: -16))
            //: make.size.equalTo(CGSize(width: 126, height: 50))
            make.size.equalTo(CGSize(width: 126, height: 50))
        }
    }

    /// 添加数据
    //: private func setupReasons() {
    private func laughAway() {
        //: var reasonArray: [String] = []
        var reasonArray: [String] = []
        //: var type = 0
        var type = 0

        //: switch self.reportType {
        switch self.reportType {
        //: case .reportDynamicType, .reportCommentType:
        case .reportDynamicType, .reportCommentType:
            //: type = 6
            type = 6
            //: reasonArray = ["Nudity".localized,
            reasonArray = [(String(mainListFormat)).localized,
                           //: "Violence".localized,
                           (String(user_whoMoveData.prefix(5)) + String(showDetectStr)).localized,
                           //: "Harassment".localized,
                           (String(show_sometimeName.prefix(5)) + String(noti_likeTitle)).localized,
                           //: "Suicide or Self-injury".localized,
                           (String(mSubmitAideContent) + noti_behaviorKey.replacingOccurrences(of: "mid", with: "ic") + "e or" + String(k_remainingPath.prefix(4)) + "f-injury").localized,
                           //: "False Information".localized,
                           (String(mBlueValue.suffix(6)) + "Infor" + k_asternPath.replacingOccurrences(of: "till", with: "t") + user_partUrl.replacingOccurrences(of: "amber", with: "n")).localized,
                           //: "Spam".localized,
                           (String(notiPackagePath)).localized,
                           //: "Hate Speech".localized,
                           (String(showDrawTimeFormat) + String(k_littleFormat.prefix(6))).localized,
                           //: "Terrorism".localized,
                           (String(mainVoiceTitle.suffix(4)) + "orism").localized,
                           //: "Sale of Illegal or Regulated Goods".localized]
                           String(bytes: user_policyStr.map{currentLat(lust: $0)}, encoding: .utf8)!.localized]

        //: case .reportUserType:
        case .reportUserType:
            //: type = 1
            type = 1
            //: reasonArray = ["Sending disgusting messages".localized,
            reasonArray = [(String(userFutureData) + String(m_inmatePath)).localized,
                           //: "Using photos of nudity or drugs".localized,
                           String(bytes: noti_wallName.reversed(), encoding: .utf8)!.localized,
                           //: "Harassing or sending ads".localized,
                           (String(userTargetId) + String(app_presentationDoinglyMessage.prefix(8)) + String(noti_limitValue.prefix(9))).localized,
                           //: "Defraud or using fake info".localized,
                           (String(dreamRichIdent.prefix(7)) + String(user_sortUrl)).localized,
                           //: "Others".localized]
                           (appBoardFormat.replacingOccurrences(of: "edge", with: "O") + user_monitorName.replacingOccurrences(of: "ease", with: "t")).localized]

        //: case .reportVideo:
        case .reportVideo:
            //: if let reason = AdministratorThen.share.appConfigMode.videoReport["reason"] {
            if let reason = AdministratorThen.share.appConfigMode.videoReport[(m_commandTransactionKey.replacingOccurrences(of: "pan", with: "r") + kConceptMessage.replacingOccurrences(of: "also", with: "as"))] {
                //: reasonArray = reason as! [String]
                reasonArray = reason as! [String]
            }

        //: default:
        default:
            //: type = 0
            type = 0
            //: reasonArray = []
            reasonArray = []
        }

        //: for i in 0..<reasonArray.count {
        for i in 0 ..< reasonArray.count {
            //: let model = TalkingReportModel()
            let model = PosterReportModel()
            //: model.reason = reasonArray[i]
            model.reason = reasonArray[i]
            //: if self.reportType == .reportVideo {
            if self.reportType == .reportVideo {
                //: model.type = "\(i)"
                model.type = "\(i)"
                //: } else {
            } else {
                //: model.type = "\(i+type)"
                model.type = "\(i + type)"
            }
            //: dataArray.append(model)
            dataArray.append(model)
        }

        //: tableView.reloadData()
        tableView.reloadData()
    }

    /// 添加事件
    //: private func bindInteraction() {
    private func alive() {
        //: let tapGesture = UITapGestureRecognizer()
        let tapGesture = UITapGestureRecognizer()
        //: bgView.addGestureRecognizer(tapGesture)
        bgView.addGestureRecognizer(tapGesture)
        //: tapGesture.rx.event.bind { [weak self] recognizer in
        tapGesture.rx.event.bind { [weak self] _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: self.endEditing(true)
            self.endEditing(true)
        }
        //: .disposed(by: disposeBag)
        .disposed(by: disposeBag)
    }
}
