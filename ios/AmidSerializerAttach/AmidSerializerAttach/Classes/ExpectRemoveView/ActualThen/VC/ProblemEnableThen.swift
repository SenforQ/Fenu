
//: Declare String Begin

/*: "cmd" :*/
fileprivate let mainSavePadData:[UInt8] = [0xfc,0xf2,0xfb]

private func simultaneouslyAreaSingle(transition num: UInt8) -> UInt8 {
    return num ^ 159
}

/*: "cancelCall" :*/
fileprivate let app_properlyMessage:String = "whonc"
fileprivate let showMassiveUrl:String = "willing shirtelCall"

/*: "data" :*/
fileprivate let dream_whiteName:[UInt8] = [0x8f,0x8c,0x9f,0x8c]

fileprivate func civilExclusive(handle num: UInt8) -> UInt8 {
    let value = Int(num) + 213
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "logId" :*/
fileprivate let mForestUrl:[Character] = ["l","o","g","I","d"]

/*: "#322870" :*/
fileprivate let kBelowData:String = "#322870"

/*: "Tap to start video chatting" :*/
fileprivate let mReminderId:[Character] = ["T","a","p"," ","t"]
fileprivate let kSourceInstallId:[Character] = ["o"," ","s","t","a","r","t"," ","v","i","d","e","o"," ","c","h","a","t","t","i","n","g"]

/*: "icon_close_nor" :*/
fileprivate let kQuicklyFormat:String = "likecon"
fileprivate let k_removalText:String = "_cloidentify distance end ease"

/*: "icon_views_number" :*/
fileprivate let app_movieUrl:String = "icon_case continue square drop"
fileprivate let app_fosterMsg:String = "mviewr"

/*: "%@ chatting" :*/
fileprivate let kRewardIdent:[Character] = ["%","@"," ","c"]
fileprivate let main_bassMessage:[Character] = ["h","a","t","t","i","n","g"]

/*: "icon_lounge" :*/
fileprivate let show_sixthStr:String = "detailed"
fileprivate let dream_songPath:String = "con_alongside week"

/*: "icon_rank_coin" :*/
fileprivate let appRewardExecuteTitle:String = "clothe trim fill pack reduceicon_"
fileprivate let userOnlyText:[Character] = ["c","o","i","n"]

/*: "vip_btn_top" :*/
fileprivate let m_ventIdent:String = "alongside root will cultural explorevip_"

/*: "btn_nearby_settings_nor" :*/
fileprivate let app_decreaseHairPlusName:[Character] = ["b","t","n","_","n","e","a","r","b","y","_","s","e","t","t"]
fileprivate let showMessageVineName:[Character] = ["i"]
fileprivate let mainUpperUrl:String = "photo outer constraintngs_nor"

/*: "%@ Coins/min" :*/
fileprivate let main_wedName:String = "background expected along calculate%@ Co"

/*: "get json error" :*/
fileprivate let show_tinMessage:String = "link chance waveget "
fileprivate let mainExamineedTitle:[Character] = ["r"]

/*: "matchId" :*/
fileprivate let userAttitudeStr:String = "matchIdpresent link behind period"

/*: "errno" :*/
fileprivate let mStreetInsideUniversalPath:[UInt8] = [0x4,0x13,0x13,0xf,0xe]

private func directionHistory(ya num: UInt8) -> UInt8 {
    return num ^ 97
}

/*: "VIPFee" :*/
fileprivate let dreamJoinUrl:String = "false heart theory visitor decentVIPFee"

/*: "Matching now" :*/
fileprivate let userAskMeritUrl:[Character] = ["M","a","t","c","h","i","n","g"," ","n","o","w"]

/*: "userTouch" :*/
fileprivate let showBlindIdent:String = "userTouavailable shirt failure balloon"
fileprivate let appIllegalText:String = "calso"

/*: "mf/freeCall/getVideoInfo" :*/
fileprivate let show_playUrl:[Character] = ["m","f","/","f","r","e","e","C","a","l","l","/","g","e","t","V","i","d"]
fileprivate let m_professionalFromStr:String = "daily monitoreoInfo"

/*: "fromType" :*/
fileprivate let m_scoreMessage:[UInt8] = [0xc9,0xdd,0xc0,0xc2,0xfb,0xd6,0xdf,0xca]

private func viewAll(less num: UInt8) -> UInt8 {
    return num ^ 175
}

/*: "You've been barred from receiving calls" :*/
fileprivate let kMouthName:[UInt8] = [0xf,0x39,0x23,0x71,0x20,0x33,0x76,0x34,0x33,0x33,0x38,0x76,0x34,0x37,0x24,0x24,0x33,0x32,0x76,0x30,0x24,0x39,0x3b,0x76,0x24,0x33,0x35,0x33,0x3f,0x20,0x3f,0x38,0x31,0x76,0x35,0x37,0x3a,0x3a,0x25]

/*: "source" :*/
fileprivate let main_executiveText:[UInt8] = [0x74,0x68,0x72,0x75,0x64,0x62]

private func trailSmall(merely num: UInt8) -> UInt8 {
    return num ^ 7
}

/*: "type" :*/
fileprivate let user_anonymousMessage:[UInt8] = [0xef,0xe2,0xeb,0xfe]

private func guiltyDecide(highlight num: UInt8) -> UInt8 {
    return num ^ 155
}

/*: "uid" :*/
fileprivate let mBothValue:[UInt8] = [0xe9,0xdd,0xd8]

fileprivate func districtManagerField(anima num: UInt8) -> UInt8 {
    let value = Int(num) - 116
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "fromFreeCall" :*/
fileprivate let noti_naturalIdent:[Character] = ["f","r","o","m","F"]
fileprivate let app_betweenPath:[Character] = ["r","e","e","C","a","l","l"]

/*: "requestCall" :*/
fileprivate let k_liePath:[Character] = ["r","e","q","u","e","s","t","C","a","l","l"]

/*: "onVideoMatch" :*/
fileprivate let showDutyUrl:String = "onViballot advanced stretch state work"
fileprivate let showInvitationName:String = "ctell"

/*: "[Automatic video] Receive video call incoming" :*/
fileprivate let userEasternFormat:[UInt8] = [0xb2,0xa8,0x9c,0x9d,0x86,0x84,0x88,0x9d,0x80,0x8a,0xc9,0x9f,0x80,0x8d,0x8c,0x86,0xb4,0xc9,0xbb,0x8c,0x8a,0x8c,0x80,0x9f,0x8c,0xc9,0x9f,0x80,0x8d,0x8c,0x86,0xc9,0x8a,0x88,0x85,0x85,0xc9,0x80,0x87,0x8a,0x86,0x84,0x80,0x87,0x8e]

private func gradeWalk(temporary num: UInt8) -> UInt8 {
    return num ^ 233
}

/*: "refuseCall" :*/
fileprivate let show_shirtPath:String = "automatic sitrefuseCal"
fileprivate let app_environmentUrl:[Character] = ["l"]

/*: "onCancelCall" :*/
fileprivate let mEnhanceMessage:[Character] = ["o","n","C","a","n","c","e"]
fileprivate let kPermitValue:[Character] = ["l","C","a","l","l"]

/*: "retryAfter" :*/
fileprivate let show_allowEditionMsg:[Character] = ["r","e","t","r","y","A","f","t","e"]
fileprivate let showProductLayUrl:[Character] = ["r"]

/*: "onRequestCall" :*/
fileprivate let userResignStr:String = "ourselves rem agent eye gradeonRe"
fileprivate let app_handMeritTitle:String = "qenhancest"

/*: "failType" :*/
fileprivate let userDarkTreatStr:[Character] = ["f","a","i","l","T","y"]
fileprivate let appFogTitle:String = "center"

/*: "callWaitingTime" :*/
fileprivate let dreamExplainValue:String = "courselves"
fileprivate let dreamViolationId:[Character] = ["l","W","a","i","t","i","n","g","T","i","m","e"]

/*: "toUid" :*/
fileprivate let mainPreserveMessage:String = "toUidfamily blade"

/*: "Free 1 min" :*/
fileprivate let appAnimaMsg:[Character] = ["F","r","e","e"," ","1"]
fileprivate let noti_republicUrl:String = " minwhat opposition chance"

/*: "%@/min" :*/
fileprivate let kUponUrl:[Character] = ["%"]
fileprivate let noti_commendMessage:[Character] = ["@","/","m","i","n"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  ProblemEnableThen.swift
//  AbroadTalking
//
//  Created by young on 2023/8/22.
//

//: import UIKit
import UIKit

//: class TalkingRandomVideoViewController: TalkingBaseViewController {
class ProblemEnableThen: FlexibleViewController {
    //: var fee = 0
    var fee = 0 // 原价
    //: var vipFee = 0
    var vipFee = 0 // VIP价格
    //: var chattingNum = 0
    var chattingNum = 0 // 当前匹配人数
    //: private var matchId: Int = 0
    private var matchId: Int = 0 // 速配批次Id
    //: private var waitingTimer: Timer?
    private var waitingTimer: Timer? // 呼叫等待定时（等待15s）
    //: private var retryAfterTimer: Timer?
    private var retryAfterTimer: Timer? // 匹配失败重试定时器
    //: private var logId = 0
    private var logId = 0 // 通话id
    //: private var isShowFreeVideo = false
    private var isShowFreeVideo = false // 是否已展示免费视频挽留弹窗
    //: var beginTime: TimeInterval?
    var beginTime: TimeInterval?
    //: private var dispatchWorkItem: DispatchWorkItem?
    private var dispatchWorkItem: DispatchWorkItem?
    //: private var closeBtnWorkItem: DispatchWorkItem?
    private var closeBtnWorkItem: DispatchWorkItem?
    //: var isFirstRandow = true
    var isFirstRandow = true
    //: var isBeginRand = false
    var isBeginRand = false

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 1.0) {
            //: if !self.isFirstRandow {
            if !self.isFirstRandow {
                //: self.refreshVideoCallPrice()
                self.incomePrivacy()
            }
        }
        //: self.resetCamera()
        self.scriptBold()
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: self.hideNavi = true
        self.hideNavi = true

        //: setupSubviews()
        formatSubviews()
        //: setupSubViewsConstraint()
        sinceEvaluate()
        //: bindInteraction()
        permissionBeg()
        //: videoManager.startPreview(isMatch: true)
        videoManager.likeFrom(isMatch: true)
        //: setPlayer(isRandon: false)
        linksman(isRandon: false)
        //: if isBeginRand {
        if isBeginRand {
            //: start_randomVideo()
            startOf()
        }
    }

    //: deinit {
    deinit {
        //: if waitingTimer != nil {
        if waitingTimer != nil {
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": logId]])
            AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: mainSavePadData.map{simultaneouslyAreaSingle(transition: $0)}, encoding: .utf8)!: (app_properlyMessage.replacingOccurrences(of: "who", with: "ca") + String(showMassiveUrl.suffix(6))), String(bytes: dream_whiteName.map{civilExclusive(handle: $0)}, encoding: .utf8)!: [(String(mForestUrl)): logId]])
        }
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
        //: destroyWaitingTimer()
        comprehensive()
        //: if TalkingSocketManager.shared.isFind == false {
        if AdviceLandscapeReactiveCompatible.shared.isFind == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            AdviceLandscapeReactiveCompatible.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            AdviceLandscapeReactiveCompatible.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            AdviceLandscapeReactiveCompatible.shared.errorDelegate = nil
        }
    }

    // MARK: - Lazy Load

    //: private lazy var videoManager: TalkingVideoChatManager = {
    private lazy var videoManager: MightThen = {
        //: let m = TalkingVideoChatManager()
        let m = MightThen()
        //: let info = TalkingVideoChatModel()
        let info = ChatModel()
        //: info.videoStage = .Calling
        info.videoStage = .Calling
        //: info.renderPostion = .selfOnBg
        info.renderPostion = .selfOnBg
        //: m.infoModel = info
        m.infoModel = info
        // 视图传递给manager
        //: m.bgVideoView = self.renderView
        m.bgVideoView = self.renderView
        //: return m
        return m
        //: }()
    }()

    // 视频画面渲染视图
    //: private lazy var renderView: UIView = {
    private lazy var renderView: UIView = {
        //: let v = UIView()
        let v = UIView()
        //: v.backgroundColor = .appTitleColor()
        v.backgroundColor = .manualColor()
        //: return v
        return v
        //: }()
    }()

    //: private lazy var gradientBtn: UIButton = {
    private lazy var gradientBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.addTarget(self, action: #selector(start_randomVideo), for: .touchUpInside)
        btn.addTarget(self, action: #selector(startOf), for: .touchUpInside)
        //: btn.layer.masksToBounds = true
        btn.layer.masksToBounds = true
        // 渐变
        //: let layer = CAGradientLayer()
        let layer = CAGradientLayer()
        //: layer.frame = CGRect(x: 0, y: 0, width: ScreenWidth+1, height: ScreenHeight+1)
        layer.frame = CGRect(x: 0, y: 0, width: noti_senseContent + 1, height: appMineMessage + 1)
        //: layer.startPoint = CGPoint(x: 0.5, y: 0)
        layer.startPoint = CGPoint(x: 0.5, y: 0)
        //: layer.endPoint = CGPoint(x: 0.5, y: 1)
        layer.endPoint = CGPoint(x: 0.5, y: 1)
        //: layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: "#322870")!.cgColor]
        layer.colors = [UIColor(white: 0, alpha: 0.15).cgColor, UIColor(hex: (kBelowData.capitalized))!.cgColor]
        //: layer.locations = [0, 1]
        layer.locations = [0, 1]
        //: btn.layer.addSublayer(layer)
        btn.layer.addSublayer(layer)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var svgaPlayer: SVGAPlayer = {
    private lazy var svgaPlayer: SVGAPlayer = {
        //: let player = SVGAPlayer.init()
        let player = SVGAPlayer()
        //: player.loops = 0
        player.loops = 0
        //: player.clearsAfterStop = false
        player.clearsAfterStop = false
        //: player.isUserInteractionEnabled = false
        player.isUserInteractionEnabled = false
        //: player.contentMode = .scaleAspectFill
        player.contentMode = .scaleAspectFill

        //: return player
        return player
        //: }()
    }()

    //: private lazy var tipsLab: UILabel = {
    private lazy var tipsLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.boldSystemFont(ofSize: 20)
        lab.font = UIFont.boldSystemFont(ofSize: 20)
        //: lab.textAlignment = .center
        lab.textAlignment = .center
        //: lab.text = "Tap to start video chatting".localized
        lab.text = (String(mReminderId) + String(kSourceInstallId)).localized
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton()
        let btn = UIButton()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_close_nor"), for: .normal)
        btn.setImage(UIImage.scribeNumber(name: (kQuicklyFormat.replacingOccurrences(of: "like", with: "i") + String(k_removalText.prefix(4)) + "se_nor")), for: .normal)
        //: btn.addTarget(self, action: #selector(closeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(suspendNative), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var chattingNumBtn: TalkingButton = {
    private lazy var chattingNumBtn: WordOfAdviceThen = {
        //: let btn = TalkingButton()
        let btn = WordOfAdviceThen()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .normal)
        btn.setImage(UIImage.scribeNumber(name: (String(app_movieUrl.prefix(5)) + "views_nu" + app_fosterMsg.replacingOccurrences(of: "view", with: "be"))), for: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_views_number"), for: .disabled)
        btn.setImage(UIImage.scribeNumber(name: (String(app_movieUrl.prefix(5)) + "views_nu" + app_fosterMsg.replacingOccurrences(of: "view", with: "be"))), for: .disabled)
        //: btn.spaceBetweenTitleAndImage = 5
        btn.spaceBetweenTitleAndImage = 5
        //: btn.isEnabled = false
        btn.isEnabled = false
        //: btn.setTitleColor(UIColor.white, for: .normal)
        btn.setTitleColor(UIColor.white, for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        btn.titleLabel?.font = UIFont.afterDisable(type: .Regular, fontSize: 15)
        //: btn.setTitle("%@ chatting".localizedArguments(chattingNum), for: .normal)
        btn.setTitle((String(kRewardIdent) + String(main_bassMessage)).innerArguments(chattingNum), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var priceStackView: UIStackView = {
    private lazy var priceStackView: UIStackView = {
        //: let v = UIStackView()
        let v = UIStackView()
        //: v.axis = .horizontal
        v.axis = .horizontal
        //: v.alignment = .center
        v.alignment = .center
        //: v.spacing = 4
        v.spacing = 4
        //: v.isUserInteractionEnabled = false
        v.isUserInteractionEnabled = false
        //: return v
        return v
        //: }()
    }()

    //: private lazy var vipIcon: UIImageView = {
    private lazy var vipIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_lounge")
        img.image = UIImage.scribeNumber(name: (show_sixthStr.replacingOccurrences(of: "detailed", with: "i") + String(dream_songPath.prefix(4)) + "lounge"))
        //: img.contentMode = .scaleAspectFill
        img.contentMode = .scaleAspectFill
        //: return img
        return img
        //: }()
    }()

    //: private lazy var goldCoinIcon: UIImageView = {
    private lazy var goldCoinIcon: UIImageView = {
        //: let img = UIImageView()
        let img = UIImageView()
        //: img.image = UIImage.BundleImageNamed(name: "icon_rank_coin")
        img.image = UIImage.scribeNumber(name: (String(appRewardExecuteTitle.suffix(5)) + "rank_" + String(userOnlyText)))
        //: return img
        return img
        //: }()
    }()

    //: private lazy var priceLab: UILabel = {
    private lazy var priceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .white
        lab.textColor = .white
        //: lab.font = UIFont.pingfangFont(type: .Regular, fontSize: 15)
        lab.font = UIFont.afterDisable(type: .Regular, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var vipTopImgView: UIImageView = {
    private lazy var vipTopImgView: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.isHidden = true
        imgV.isHidden = true
        //: imgV.image  = UIImage.BundleImageNamed(name: "vip_btn_top")
        imgV.image = UIImage.scribeNumber(name: (String(m_ventIdent.suffix(4)) + "btn_top"))
        //: return imgV
        return imgV
        //: }()
    }()

    // 会员折扣价
    //: private lazy var vipPriceBtn: TalkingButton = {
    private lazy var vipPriceBtn: WordOfAdviceThen = {
        //: let btn = TalkingButton()
        let btn = WordOfAdviceThen()
        //: btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        btn.backgroundColor = UIColor.white.withAlphaComponent(0.1)
        //: btn.layer.cornerRadius = actualWidth(w: 27)
        btn.layer.cornerRadius = actualWidth(w: 27)
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(termsClick), for: .touchUpInside)
        //: btn.isHidden = true
        btn.isHidden = true
        //: btn.setBackgroundImage(UIImage.BundleImageNamed(name: "btn_nearby_settings_nor"), for: .normal)
        btn.setBackgroundImage(UIImage.scribeNumber(name: (String(app_decreaseHairPlusName) + String(showMessageVineName) + String(mainUpperUrl.suffix(7)))), for: .normal)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var vipPriceImg: UIImageView = {
    private lazy var vipPriceImg: UIImageView = {
        //: let imgV = UIImageView()
        let imgV = UIImageView()
        //: imgV.image  = UIImage.BundleImageNamed(name: "icon_rank_coin")
        imgV.image = UIImage.scribeNumber(name: (String(appRewardExecuteTitle.suffix(5)) + "rank_" + String(userOnlyText)))
        //: return imgV
        return imgV
        //: }()
    }()

    //: private lazy var vipPriceLab: UILabel = {
    private lazy var vipPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: lab.textColor = .userVipColor()
        lab.textColor = .processCur()
        //: lab.text = "%@ Coins/min".localizedArguments(vipFee)
        lab.text = (String(main_wedName.suffix(5)) + "ins/min").innerArguments(vipFee)
        //: lab.font = UIFont.pingfangFont(type: .Medium, fontSize: 15)
        lab.font = UIFont.afterDisable(type: .Medium, fontSize: 15)
        //: return lab
        return lab
        //: }()
    }()

    //: private lazy var norPriceLab: UILabel = {
    private lazy var norPriceLab: UILabel = {
        //: let lab = UILabel()
        let lab = UILabel()
        //: return lab
        return lab
        //: }()
    }()

    // 免费视频挽留弹窗
    //: private lazy var freeVideoAlert: TalkingLiveAlertView = {
    private lazy var freeVideoAlert: ChiropteranAlertView = //: return ChiropteranAlertView(frame: .zero, type: .freeVideo)
        .init(frame: .zero, type: .freeVideo)
    //: }()
}

// MARK: - Request & 数据处理

//: extension TalkingRandomVideoViewController {
extension ProblemEnableThen {
    //: private func setPlayer(isRandon: Bool) {
    private func linksman(isRandon: Bool) {
        //: let url = SVGAEffectTool.default.getZipEffectPath(type: isRandon ? .Random_video_bg : .Random_video_bg_nor )
        let url = SampleEffectTool.default.joinWeight(type: isRandon ? .Random_video_bg : .Random_video_bg_nor)
        //: do {
        do {
            //: let data = try Data(contentsOf: url)
            let data = try Data(contentsOf: url)
            //: let parser = SVGAParser.init()
            let parser = SVGAParser()
            //: parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
            parser.parse(with: data, cacheKey: "") { [weak self] videoItem in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: svgaPlayer.videoItem = videoItem
                svgaPlayer.videoItem = videoItem
                //: svgaPlayer.startAnimation()
                svgaPlayer.startAnimation()
            }
            //: } catch {
        } catch {
            //: printLog(message: "get json error")
            printLog(message: (String(show_tinMessage.suffix(4)) + "json erro" + String(mainExamineedTitle)))
        }
    }

    /// 开始随机视频（男）
    /// - Parameters:
    ///   - cmd: 事件名
    ///   - errno: cmd的错误码
    //: private func req_randomVideoToMatch(cmd: String, errno: String) {
    private func toFront(cmd: String, errno: String) {
        //: beginTime = Date().timeIntervalSince1970
        beginTime = Date().timeIntervalSince1970
        //: let params = ["matchId": self.matchId,
        let params = [(String(userAttitudeStr.prefix(7))): self.matchId,
                      //: "cmd": cmd,
                      String(bytes: mainSavePadData.map{simultaneouslyAreaSingle(transition: $0)}, encoding: .utf8)!: cmd,
                      //: "errno": errno] as [String: Any]
                      String(bytes: mStreetInsideUniversalPath.map{directionHistory(ya: $0)}, encoding: .utf8)!: errno] as [String: Any]
        //: TalkingQuickVideoManager.req_randomVideoToMatch(params: params) { succeed, result, errorModel in
        ActualThen.backhandUnitedStatesOverlook(params: params) { succeed, result, _ in
            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.settle()
                //: return
                return
            }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(userAttitudeStr.prefix(7)))].intValue
        }
    }

    /// 更新VIP价格
    //: @objc private func req_updateVIPFee() {
    @objc private func limitedDit() {
        //: TalkingQuickVideoManager.req_videoMatchCheck() { succeed, result, errorModel in
        ActualThen.giveUp { succeed, result, _ in
            //: guard succeed else { return }
            guard succeed else { return }
            //: let json = JSON(result ?? [String: Any]())
            let json = JSON(result ?? [String: Any]())
            //: self.vipFee = json["VIPFee"].intValue
            self.vipFee = json[(String(dreamJoinUrl.suffix(6)))].intValue
            //: self.refreshVideoCallPrice()
            self.incomePrivacy()
        }
    }
}

// MARK: - Event

//: extension TalkingRandomVideoViewController {
extension ProblemEnableThen {
    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func termsClick() {
        //: DumpPushManager.share.func__pushToSubscribeAlert()
        DumpPushManager.share.underCharm()
    }

    /// 关闭按钮点击事件
    //: @objc func closeButtonClick() {
    @objc func suspendNative() {
        //: dispatchWorkItem?.cancel()
        dispatchWorkItem?.cancel()
        //: showCloseBtn()
        valid()
        //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCancelNoP)
        k_hugeName.hour(eventID: showTableKey)

        //: if AdministratorThen.share.appConfigMode.displayMatchV4PopUp, AdministratorThen.share.loginUserMode.freeCallTimes > 0,
        if AdministratorThen.share.appConfigMode.displayMatchV4PopUp, AdministratorThen.share.loginUserMode.freeCallTimes > 0,
           //: isShowFreeVideo == false {
           isShowFreeVideo == false
        {
            //: self.freeVideoAlert.show()
            self.freeVideoAlert.skinAndPer()
            //: isShowFreeVideo = true
            isShowFreeVideo = true
            //: return
            return
        }
        //: currentViewController()?.navigationController?.popViewController(animated: true)
        springEquinox()?.navigationController?.popViewController(animated: true)
    }

    /// 开始随机视频
    //: @objc private func start_randomVideo() {
    @objc private func startOf() {
        //: guard TalkingSocketManager.shared.isFind == false else {
        guard AdviceLandscapeReactiveCompatible.shared.isFind == false else {
            //: self.func__showStatusBarErrorMsg(showMsg: kMessage_videoTalking_limit)
            self.iconThemeBringBackPlace(showMsg: m_buildNearbyData)
            //: return
            return
        }
        //: guard gradientBtn.isEnabled == true else { return }
        guard gradientBtn.isEnabled == true else { return }
        //: afterShowClose()
        everyCost()
        //: self.matchId = 0
        self.matchId = 0 // 重置匹配池
        //: gradientBtn.isEnabled = false
        gradientBtn.isEnabled = false
        //: tipsLab.text = "Matching now".localized
        tipsLab.text = (String(userAskMeritUrl)).localized
        //: setPlayer(isRandon: true)
        linksman(isRandon: true)
        //: priceStackView.isHidden = true
        priceStackView.isHidden = true
        //: vipPriceBtn.isHidden = true
        vipPriceBtn.isHidden = true
        //: vipTopImgView.isHidden = true
        vipTopImgView.isHidden = true
        //: if AdministratorThen.share.loginUserMode.randomVideoType == 2 {
        if AdministratorThen.share.loginUserMode.randomVideoType == 2 {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRobotRandomButton)
            k_hugeName.hour(eventID: notiBeginId)
            //: req_getVideoInfo()
            wrap()
            //: } else {
        } else {
            //: uploadRecord.uploadRecordEvent(eventID: ClickRandomvideoCallNoP)
            k_hugeName.hour(eventID: user_assetUrl)

            //: TalkingSocketManager.shared.videoMatchDelegate = self
            AdviceLandscapeReactiveCompatible.shared.videoMatchDelegate = self
            //: TalkingSocketManager.shared.callerDelegate = self
            AdviceLandscapeReactiveCompatible.shared.callerDelegate = self
            //: TalkingSocketManager.shared.calledPartyDelegate = self
            AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = self
            //: TalkingSocketManager.shared.errorDelegate = self
            AdviceLandscapeReactiveCompatible.shared.errorDelegate = self
            //: restart_randomVideo()
            event()
        }
    }

    /// 重新开始视频速配
    /// - Parameters:
    ///   - cmd: 事件名（默认用户主动点击）
    ///   - errno: cmd的错误码
    //: private func restart_randomVideo(cmd: String = "userTouch", errno: String = "0") {
    private func event(cmd: String = (String(showBlindIdent.prefix(7)) + appIllegalText.replacingOccurrences(of: "also", with: "h")), errno: String = "0") {
        // 停止定时器
        //: destroyWaitingTimer()
        comprehensive()
        //: req_randomVideoToMatch(cmd: cmd, errno: errno)
        toFront(cmd: cmd, errno: errno)
    }

    /// 停止随机视频
    //: private func stop_randomVideo() {
    private func settle() {
        // 停止定时器
        //: destroyWaitingTimer()
        comprehensive()
        //: showCloseBtn()
        valid()

        //: gradientBtn.isEnabled = true
        gradientBtn.isEnabled = true
        //: tipsLab.text = "Tap to start video chatting".localized
        tipsLab.text = (String(mReminderId) + String(kSourceInstallId)).localized
        //: setPlayer(isRandon: false)
        linksman(isRandon: false)
        //: self.isFirstRandow = false
        self.isFirstRandow = false
        //: if TalkingSocketManager.shared.isFind == false {
        if AdviceLandscapeReactiveCompatible.shared.isFind == false {
            //: TalkingSocketManager.shared.videoMatchDelegate = nil
            AdviceLandscapeReactiveCompatible.shared.videoMatchDelegate = nil
            //: TalkingSocketManager.shared.callerDelegate = nil
            AdviceLandscapeReactiveCompatible.shared.callerDelegate = nil
            //: TalkingSocketManager.shared.calledPartyDelegate = nil
            AdviceLandscapeReactiveCompatible.shared.calledPartyDelegate = nil
            //: TalkingSocketManager.shared.errorDelegate = nil
            AdviceLandscapeReactiveCompatible.shared.errorDelegate = nil
        }
        //: refreshVideoCallPrice()
        incomePrivacy()
    }

    /// 销毁定时器
    //: private func destroyWaitingTimer() {
    private func comprehensive() {
        //: if self.waitingTimer != nil {
        if self.waitingTimer != nil {
            //: self.waitingTimer?.invalidate()
            self.waitingTimer?.invalidate()
            //: self.waitingTimer = nil
            self.waitingTimer = nil
        }

        //: if self.retryAfterTimer != nil {
        if self.retryAfterTimer != nil {
            //: self.retryAfterTimer?.invalidate()
            self.retryAfterTimer?.invalidate()
            //: self.retryAfterTimer = nil
            self.retryAfterTimer = nil
        }
    }

    /// 获取用户主动拨打视频信息
    //: @objc private func req_getVideoInfo() {
    @objc private func wrap() {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "mf/freeCall/getVideoInfo"
        reqModel.requestPath = (String(show_playUrl) + String(m_professionalFromStr.suffix(6)))
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.params = ["fromType": "1"]
        reqModel.params = [String(bytes: m_scoreMessage.map{viewAll(less: $0)}, encoding: .utf8)!: "1"]
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in

            //: guard succeed == true else {
            guard succeed == true else {
                //: self.stop_randomVideo()
                self.settle()
                //: if errorModel?.errorCode ?? 1 > 0 {
                if errorModel?.errorCode ?? 1 > 0 {
                    //: AdministratorThen.share.loginUserMode.randomVideoType = 1
                    AdministratorThen.share.loginUserMode.randomVideoType = 1
                    //: self.start_randomVideo()
                    self.startOf()
                    //: return
                    return
                }
                //: self.func__showStatusBarErrorMsg(showMsg: errorModel?.errorMsg ?? "")
                self.iconThemeBringBackPlace(showMsg: errorModel?.errorMsg ?? "")
                //: return
                return
            }
            //: guard let result = result as? Dictionary<String, Any> else {
            guard let result = result as? [String: Any] else {
                //: return
                return
            }
            //: if let model = TalkingVideoInitivCallModel.deserialize(from: result as? Dictionary) {
            if let model = FireLiteralModelType.deserialize(from: result as? Dictionary) {
                /// 预加载
                //: let  player = TalkingVideoPlayerManager.init()
                let player = BubblePlayerDirectorVideoPlayListener()
                //: player.setMute(bEnable: true)
                player.offdForDoing(bEnable: true)
                //: player.playerWithUrlAndVideoView(url: model.winVideoUrl, view: UIView.init())
                player.agreeSmart(url: model.winVideoUrl, view: UIView())

                //: TalkingVideoInitivCallManager.shared.videoCallModel = model
                TapThen.shared.videoCallModel = model

                //: TalkingPermissionTool.checkCameraAndMicrophone { [weak self] isOpen in
                AsidePermissionTool.opticalLensOn { [weak self] isOpen in
                    //: guard let self = self else { return }
                    guard let self = self else { return }
                    //: guard isOpen == true else { return }
                    guard isOpen == true else { return }

                    //: self.putThrough()
                    self.tidalRiverOurselves()
                }
            }
        }
    }

    /// 接通电话
    //: private func putThrough() {
    private func tidalRiverOurselves() {
        //: uploadRecord.uploadRecordEvent(eventID: clickVideoAcceptButton)
        k_hugeName.hour(eventID: dreamServerValue)
        /// 视频通话被禁用
        //: guard TalkingVideoInitivCallManager.shared.videoCallModel.isBan == 0 else {
        guard TapThen.shared.videoCallModel.isBan == 0 else {
            //: self.func__showStatusBarErrorMsg(showMsg: "You've been barred from receiving calls".localized)
            self.iconThemeBringBackPlace(showMsg: String(bytes: kMouthName.map{$0^86}, encoding: .utf8)!.localized)
            //: return
            return
        }
        //: self.dispatchWorkItem = DispatchWorkItem {
        self.dispatchWorkItem = DispatchWorkItem {
            //: TalkingVideoInitivCallManager.shared.req_userOperation(type: 1) { succeed, result, errorModel in
            TapThen.shared.ittyBitty(type: 1) { succeed, _, _ in
                //: guard succeed == true else {
                guard succeed == true else {
                    //: return
                    return
                }
                //: guard succeed == true else {
                guard succeed == true else {
                    //: self.stop_randomVideo()
                    self.settle()
                    //: return
                    return
                }
                //: let vc = TalkingVideoInitivCallVC()
                let vc = AdaptThen()
                //: vc.isRandomVCPush = true
                vc.isRandomVCPush = true
                //: self.currentViewController()?.navigationController?.pushViewController(vc, animated: true)
                self.springEquinox()?.navigationController?.pushViewController(vc, animated: true)
                //: self.stop_randomVideo()
                self.settle()
            }
        }
        // 添加延时任务
        //: DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
        DispatchQueue.main.asyncAfter(deadline: .now() + 3.0, execute: self.dispatchWorkItem!)
    }

    /// 匹配时x秒显示关闭按钮
    //: private func afterShowClose() {
    private func everyCost() {
        //: if AdministratorThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
        if AdministratorThen.share.appUserConfigMode.matchCancelBtnDelayTime > 0 {
            //: self.closeBtn.isHidden = true
            self.closeBtn.isHidden = true
            //: closeBtnWorkItem = DispatchWorkItem { [weak self] in
            closeBtnWorkItem = DispatchWorkItem { [weak self] in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.closeBtn.isHidden = false
                self.closeBtn.isHidden = false
            }
            //: DispatchQueue.main.asyncAfter(deadline: .now() + Double(AdministratorThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
            DispatchQueue.main.asyncAfter(deadline: .now() + Double(AdministratorThen.share.appUserConfigMode.matchCancelBtnDelayTime), execute: closeBtnWorkItem!)
        }
    }

    //: private func showCloseBtn() {
    private func valid() {
        //: closeBtnWorkItem?.cancel()
        closeBtnWorkItem?.cancel()
        //: self.closeBtn.isHidden = false
        self.closeBtn.isHidden = false
    }

    //: private func resetCamera() {
    private func scriptBold() {
        //: guard TalkingSocketManager.shared.isFind == false else {
        guard AdviceLandscapeReactiveCompatible.shared.isFind == false else {
            //: return
            return
        }
        //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
        DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2.0) {
            //: self.videoManager.infoModel?.videoStage = .Waiting
            self.videoManager.infoModel?.videoStage = .Waiting
            //: self.videoManager.config.openCamera = true
            self.videoManager.config.openCamera = true
            //: self.videoManager.isOpenTRTCPreview = false
            self.videoManager.isOpenTRTCPreview = false
            //: self.videoManager.startPreview()
            self.videoManager.likeFrom()
        }
    }
}

// MARK: - PickUpMatchDelegate

//: extension TalkingRandomVideoViewController: SocketManagerVideoMatchDelegate {
extension ProblemEnableThen: PickUpMatchDelegate {
    /// 视频速配成功
    //: func socket_match_onVideoMatch(data: [String: Any]) {
    func animalData(data: [String: Any]) {
        // 拨打视频通话
        //: guard let model = TalkingMatchResultModel.deserialize(from: data) else { return }
        guard let model = ConsistMeasurable.deserialize(from: data) else { return }
        //: self.matchId = model.matchId
        self.matchId = model.matchId
        //: var callData: [String: Any] = ["matchId": model.matchId,
        var callData: [String: Any] = [(String(userAttitudeStr.prefix(7))): model.matchId,
                                       //: "source": model.source,
                                       String(bytes: main_executiveText.map{trailSmall(merely: $0)}, encoding: .utf8)!: model.source,
                                       //: "type": model.type,
                                       String(bytes: user_anonymousMessage.map{guiltyDecide(highlight: $0)}, encoding: .utf8)!: model.type,
                                       //: "uid": model.uid]
                                       String(bytes: mBothValue.map{districtManagerField(anima: $0)}, encoding: .utf8)!: model.uid]
        //: let index = EnableFreeCallType.randow
        let index = GageType.randow
        //: if AdministratorThen.share.loginUserMode.freeCallTimes > 0 && AdministratorThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
        if AdministratorThen.share.loginUserMode.freeCallTimes > 0 && AdministratorThen.share.appUserConfigMode.enableFreeCallType & index.rawValue == index.rawValue {
            //: callData.updateValue(1, forKey: "fromFreeCall")
            callData.updateValue(1, forKey: (String(noti_naturalIdent) + String(app_betweenPath)))
            //: TalkingSocketManager.shared.isFreeCall = true
            AdviceLandscapeReactiveCompatible.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": callData]
        let info: [String: Any] = [String(bytes: mainSavePadData.map{simultaneouslyAreaSingle(transition: $0)}, encoding: .utf8)!: (String(k_liePath)), String(bytes: dream_whiteName.map{civilExclusive(handle: $0)}, encoding: .utf8)!: callData]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: info)
        // 15s未接听主动挂断
        //: waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] timer in
        waitingTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(15), block: { [weak self] _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: TalkingSocketManager.shared.sendMessage(info: ["cmd": "cancelCall", "data": ["logId": self.logId]])
            AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: [String(bytes: mainSavePadData.map{simultaneouslyAreaSingle(transition: $0)}, encoding: .utf8)!: (app_properlyMessage.replacingOccurrences(of: "who", with: "ca") + String(showMassiveUrl.suffix(6))), String(bytes: dream_whiteName.map{civilExclusive(handle: $0)}, encoding: .utf8)!: [(String(mForestUrl)): self.logId]])
            //: self.restart_randomVideo(cmd: "onVideoMatch")
            self.event(cmd: (String(showDutyUrl.prefix(4)) + "deoMat" + showInvitationName.replacingOccurrences(of: "tell", with: "h")))
            //: }, repeats: false)
        }, repeats: false)
    }

    //: func socket_match_onRequestCall(data: [String: Any]) {
    func capAdmin(data: [String: Any]) {
        //: let json = JSON(data)
        let json = JSON(data)
        //: self.logId = json["logId"].intValue
        self.logId = json[(String(mForestUrl))].intValue
    }
}

// MARK: - BecauseObjectProtocol【视频拨打方处理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCallerDelegate {
extension ProblemEnableThen: BecauseObjectProtocol {
    /// 拨打方拒绝了通话，接收方收到该指令（未通话）
    //: func socket_videoChat_cancelCall(data: [String: Any]) {}
    func pointBlank(data _: [String: Any]) {}

    /// 对方结束了通话（通话中）
    //: func socket_videoChat_stopCall(data: [String: Any]) {}
    func freshData(data _: [String: Any]) {}

    /// 余额不足提醒
    //: func socket_videoChat_onMissCoin(msg: String, data: [String: Any]) {}
    func uniformAdd(msg _: String, data _: [String: Any]) {}

    /// 对方接受了来电，建立视频通话
    //: func socket_videoChat_acceptCall(data: [String: Any]) {
    func willData(data: [String: Any]) {
        // 应用在后台，先提醒用户
        //: if UIApplication.shared.applicationState == .background {
        if UIApplication.shared.applicationState == .background {
            //: AppDelegateHelper.pushLocalNotification(body: "[Automatic video] Receive video call incoming".localized)
            ApprovalThen.simultaneously(body: String(bytes: userEasternFormat.map{gradeWalk(temporary: $0)}, encoding: .utf8)!.localized)
        }
        //: let model = TalkingMatchResultModel.deserialize(from: data)
        let model = ConsistMeasurable.deserialize(from: data)
        //: if let uid = model?.uid {
        if let uid = model?.uid {
            //: uploadSuccessTime(toUid: String(uid))
            thinUid(toUid: String(uid))
        }
        //: stop_randomVideo()
        settle()
        // 开启视频通话时先销毁当前预览画面
        //: videoManager.shouldReleaseAllResource()
        videoManager.constrictive()
        //: AdministratorThen.share.start1v1TalkCall(info: data, situation: .Calling)
        AdministratorThen.share.auspicate(info: data, situation: .Calling)
    }
}

// MARK: - ExpectObjectProtocol【接收到回执代理】

//: extension TalkingRandomVideoViewController: SocketManagerVideoChatCalledPartyDelegate {
extension ProblemEnableThen: ExpectObjectProtocol {
    /// 接收方拒绝了通话，拨打方收到该指令（未通话）
    //: func socket_videoChat_refuseCall(data: [String: Any]) {
    func miniskirt(data _: [String: Any]) {
        //: restart_randomVideo(cmd: "refuseCall")
        event(cmd: (String(show_shirtPath.suffix(9)) + String(app_environmentUrl)))
    }

    /// 接收方接受了通话，改变状态为通话中，acceptCall回执
    //: func socket_videoChat_onAcceptCall(data: [String: Any]) {}
    func judge(data _: [String: Any]) {}
}

// MARK: - TwelveNoonObjectProtocol

//: extension TalkingRandomVideoViewController: TalkingSocketManagerErrorDelegate {
extension ProblemEnableThen: TwelveNoonObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func playerData(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
        //: if cmd == "onCancelCall" {  // 取消通话异常
        if cmd == (String(mEnhanceMessage) + String(kPermitValue)) { // 取消通话异常
            //: stop_randomVideo()
            settle()
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 404 { // 匹配池用完
        if cmd == (String(showDutyUrl.prefix(4)) + "deoMat" + showInvitationName.replacingOccurrences(of: "tell", with: "h")), errorNo == 404 { // 匹配池用完
            //: stop_randomVideo()
            settle()
            //: let json = JSON(data)
            let json = JSON(data)
            //: self.matchId = json["matchId"].intValue
            self.matchId = json[(String(userAttitudeStr.prefix(7)))].intValue
            //: self.func__showStatusBarErrorMsg(showMsg: msg)
            self.iconThemeBringBackPlace(showMsg: msg)
            //: uploadFailTime(type: 2)
            sceneExecute(type: 2)
            //: return
            return
        }
        //: if cmd == "onVideoMatch", errorNo == 449 { // 匹配池用完，延时重试
        if cmd == (String(showDutyUrl.prefix(4)) + "deoMat" + showInvitationName.replacingOccurrences(of: "tell", with: "h")), errorNo == 449 { // 匹配池用完，延时重试
            //: let json = JSON(data)
            let json = JSON(data)
            //: retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json["retryAfter"].intValue), block: { [weak self] timer in
            retryAfterTimer = Timer.scheduledTimer(withTimeInterval: TimeInterval(json[(String(show_allowEditionMsg) + String(showProductLayUrl))].intValue), block: { [weak self] _ in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.matchId = json["matchId"].intValue
                self.matchId = json[(String(userAttitudeStr.prefix(7)))].intValue
                //: self.restart_randomVideo(cmd: cmd, errno: String(errorNo))
                self.event(cmd: cmd, errno: String(errorNo))
                //: }, repeats: false)
            }, repeats: false)
            //: return
            return
        }
        //: if cmd == "onRequestCall", errorNo == VideoChatErrorCode.MoneyLack.rawValue { // 余额不足
        if cmd == (String(userResignStr.suffix(4)) + app_handMeritTitle.replacingOccurrences(of: "enhance", with: "ue") + "Call"), errorNo == JawContentConvertible.MoneyLack.rawValue { // 余额不足
            //: stop_randomVideo()
            settle()
            //: guard AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
            guard AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue else { return }
            //: DumpPushManager.share.func__jumpToWebRecharge(clickEvent: clickRandomvideoButton, sufficient: false)
            DumpPushManager.share.shapeStyle(clickEvent: appSaltPath, sufficient: false)
            //: uploadFailTime(type: 1)
            sceneExecute(type: 1)
            //: return
            return
        }

        //: restart_randomVideo(cmd: cmd, errno: String(errorNo))
        event(cmd: cmd, errno: String(errorNo))
    }
}

// MARK: - 埋点

//: extension TalkingRandomVideoViewController {
extension ProblemEnableThen {
    //: func uploadFailTime(type: Int) {
    func sceneExecute(type: Int) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["failType"] = type
        parm[(String(userDarkTreatStr) + appFogTitle.replacingOccurrences(of: "center", with: "pe"))] = type
        //: parm["callWaitingTime"] = time
        parm[(dreamExplainValue.replacingOccurrences(of: "ourselves", with: "al") + String(dreamViolationId))] = time
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideoFailwaitTime, parameterStr: parm)
        k_hugeName.assetClothe(eventID: user_liveKey, parameterStr: parm)
    }

    //: func uploadSuccessTime(toUid: String) {
    func thinUid(toUid: String) {
        //: let nowtime = Date().timeIntervalSince1970
        let nowtime = Date().timeIntervalSince1970
        //: let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        let time = Int(nowtime - (beginTime ?? Date().timeIntervalSince1970))
        //: var parm = Dictionary<String, Any>()
        var parm = [String: Any]()
        //: parm["callWaitingTime"] = time
        parm[(dreamExplainValue.replacingOccurrences(of: "ourselves", with: "al") + String(dreamViolationId))] = time
        //: parm["toUid"] = toUid
        parm[(String(mainPreserveMessage.prefix(5)))] = toUid
        //: uploadRecord.uploadRecordEvent(eventID: RandomvideowaitTime, parameterStr: parm)
        k_hugeName.assetClothe(eventID: dreamNetworkAppName, parameterStr: parm)
    }
}

// MARK: - Layout

//: extension TalkingRandomVideoViewController {
extension ProblemEnableThen {
    /// 刷新视频通话价格
    //: @objc private func refreshVideoCallPrice() {
    @objc private func incomePrivacy() {
        //: guard AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue else {
        guard AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue else {
            //: priceStackView.isHidden = true
            priceStackView.isHidden = true
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true
            //: return
            return
        }

        //: vipIcon.isHidden = !AdministratorThen.share.loginUserMode.loungePlus
        vipIcon.isHidden = !AdministratorThen.share.loginUserMode.loungePlus
        //: priceStackView.isHidden = false
        priceStackView.isHidden = false
        //: let indexType = EnableFreeCallType.randow
        let indexType = GageType.randow
        //: if AdministratorThen.share.loginUserMode.freeCallTimes > 0 && AdministratorThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue {
        if AdministratorThen.share.loginUserMode.freeCallTimes > 0, AdministratorThen.share.appUserConfigMode.enableFreeCallType & indexType.rawValue == indexType.rawValue { // 有免费次数
            //: if AdministratorThen.share.appUserConfigMode.callBtnStyle == 1 {
            if AdministratorThen.share.appUserConfigMode.callBtnStyle == 1 {
                //: priceLab.text = "Free 1 min".localized
                priceLab.text = (String(appAnimaMsg) + String(noti_republicUrl.prefix(4))).localized
                //: } else {
            } else {
                //: priceStackView.isHidden = true
                priceStackView.isHidden = true
            }
            //: vipPriceBtn.isHidden = true
            vipPriceBtn.isHidden = true
            //: vipTopImgView.isHidden = true
            vipTopImgView.isHidden = true

            //: } else {
        } else { // 无免费次数, 展示价格
            //: let price = AdministratorThen.share.loginUserMode.loungePlus ? vipFee:fee
            let price = AdministratorThen.share.loginUserMode.loungePlus ? vipFee : fee
            //: priceLab.text = "%@/min".localizedArguments(price)
            priceLab.text = (String(kUponUrl) + String(noti_commendMessage)).innerArguments(price)

            //: let attr = NSMutableAttributedString(string: "%@/min".localizedArguments(price), attributes: [.strikethroughStyle : NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.pingfangMediumFont(fontSize: 10)])
            let attr = NSMutableAttributedString(string: (String(kUponUrl) + String(noti_commendMessage)).innerArguments(price), attributes: [.strikethroughStyle: NSUnderlineStyle.single.rawValue, .foregroundColor: UIColor.white.withAlphaComponent(0.5), .font: UIFont.imitationClotheStatement(fontSize: 10)])
            //: norPriceLab.attributedText = attr
            norPriceLab.attributedText = attr
            //: vipPriceBtn.isHidden = AdministratorThen.share.loginUserMode.loungePlus
            vipPriceBtn.isHidden = AdministratorThen.share.loginUserMode.loungePlus
            //: vipTopImgView.isHidden = AdministratorThen.share.loginUserMode.loungePlus
            vipTopImgView.isHidden = AdministratorThen.share.loginUserMode.loungePlus

            //: priceStackView.isHidden = !AdministratorThen.share.loginUserMode.loungePlus
            priceStackView.isHidden = !AdministratorThen.share.loginUserMode.loungePlus
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func formatSubviews() {
        //: view.addSubview(renderView)
        view.addSubview(renderView)
        //: view.addSubview(gradientBtn)
        view.addSubview(gradientBtn)
        //: view.addSubview(svgaPlayer)
        view.addSubview(svgaPlayer)
        //: view.addSubview(tipsLab)
        view.addSubview(tipsLab)
        //: view.addSubview(chattingNumBtn)
        view.addSubview(chattingNumBtn)
        //: view.addSubview(priceStackView)
        view.addSubview(priceStackView)
        //: priceStackView.addArrangedSubview(vipIcon)
        priceStackView.addArrangedSubview(vipIcon)
        //: priceStackView.addArrangedSubview(goldCoinIcon)
        priceStackView.addArrangedSubview(goldCoinIcon)
        //: priceStackView.addArrangedSubview(priceLab)
        priceStackView.addArrangedSubview(priceLab)
        //: view.addSubview(vipPriceBtn)
        view.addSubview(vipPriceBtn)
        //: view.addSubview(vipTopImgView)
        view.addSubview(vipTopImgView)
        //: vipPriceBtn.addSubview(vipPriceImg)
        vipPriceBtn.addSubview(vipPriceImg)
        //: vipPriceBtn.addSubview(vipPriceLab)
        vipPriceBtn.addSubview(vipPriceLab)
        //: vipPriceBtn.addSubview(norPriceLab)
        vipPriceBtn.addSubview(norPriceLab)

        //: view.addSubview(closeBtn)
        view.addSubview(closeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func sinceEvaluate() {
        //: renderView.snp.makeConstraints { make in
        renderView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: gradientBtn.snp.makeConstraints { make in
        gradientBtn.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: svgaPlayer.snp.makeConstraints { make in
        svgaPlayer.snp.makeConstraints { make in
            //: make.leading.equalTo(-8)
            make.leading.equalTo(-8)
            //: make.trailing.equalTo(8)
            make.trailing.equalTo(8)
            //: make.centerY.equalToSuperview()
            make.centerY.equalToSuperview()
            //: make.height.equalTo(actualWidth(w: 395))
            make.height.equalTo(actualWidth(w: 395))
        }
        //: tipsLab.snp.makeConstraints { make in
        tipsLab.snp.makeConstraints { make in
            //: make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            make.bottom.equalTo(svgaPlayer.snp.bottom).offset(-40)
            //: make.width.centerX.equalToSuperview()
            make.width.centerX.equalToSuperview()
        }
        //: chattingNumBtn.snp.makeConstraints { make in
        chattingNumBtn.snp.makeConstraints { make in
            //: make.top.equalTo(tipsLab.snp.bottom).offset(35)
            make.top.equalTo(tipsLab.snp.bottom).offset(35)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: priceStackView.snp.makeConstraints { make in
        priceStackView.snp.makeConstraints { make in
            //: make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            make.top.equalTo(chattingNumBtn.snp.bottom).offset(8)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(31)
            make.height.equalTo(31)
            //: make.trailing.lessThanOrEqualToSuperview()
            make.trailing.lessThanOrEqualToSuperview()
        }
        //: vipIcon.snp.makeConstraints { make in
        vipIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
        }
        //: goldCoinIcon.snp.makeConstraints { make in
        goldCoinIcon.snp.makeConstraints { make in
            //: make.width.height.equalTo(actualWidth(w: 15))
            make.width.height.equalTo(actualWidth(w: 15))
        }
        //: priceLab.snp.makeConstraints { make in
        priceLab.snp.makeConstraints { make in
            //: make.height.equalToSuperview()
            make.height.equalToSuperview()
        }

        //: vipPriceBtn.snp.makeConstraints { make in
        vipPriceBtn.snp.makeConstraints { make in
            //: make.top.equalTo(priceStackView.snp.bottom).offset(16)
            make.top.equalTo(priceStackView.snp.bottom).offset(16)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
            make.size.equalTo(CGSize(width: actualWidth(w: 240), height: actualWidth(w: 54)))
        }
        //: vipTopImgView.snp.makeConstraints { make in
        vipTopImgView.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            make.top.equalTo(vipPriceBtn.snp.top).offset(-12)
            //: make.leading.equalTo(vipPriceBtn.snp.leading)
            make.leading.equalTo(vipPriceBtn.snp.leading)
            //: make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
            make.size.equalTo(CGSize(width: actualWidth(w: 32), height: actualWidth(w: 24)))
        }

        //: vipPriceImg.snp.makeConstraints { make in
        vipPriceImg.snp.makeConstraints { make in
            //: make.top.equalToSuperview().offset(actualWidth(w: 12))
            make.top.equalToSuperview().offset(actualWidth(w: 12))
            //: make.width.height.equalTo(15)
            make.width.height.equalTo(15)
            //: make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
            make.trailing.equalTo(vipPriceLab.snp.leading).offset(-2)
        }

        //: vipPriceLab.snp.makeConstraints { make in
        vipPriceLab.snp.makeConstraints { make in
            //: make.centerY.equalTo(vipPriceImg)
            make.centerY.equalTo(vipPriceImg)
            //: make.centerX.equalToSuperview().offset(8)
            make.centerX.equalToSuperview().offset(8)
        }
        //: norPriceLab.snp.makeConstraints { make in
        norPriceLab.snp.makeConstraints { make in
            //: make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            make.top.equalTo(vipPriceImg.snp.bottom).offset(5)
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.top.equalTo(StatusBarHeight+6)
            make.top.equalTo(mNameKey + 6)
            //: make.trailing.equalTo(-10)
            make.trailing.equalTo(-10)
            //: make.width.height.equalTo(actualWidth(w: 44))
            make.width.height.equalTo(actualWidth(w: 44))
        }
    }

    /// 绑定事件
    //: private func bindInteraction() {
    private func permissionBeg() {
        // index/getconfig接口回调刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(refreshVideoCallPrice),
                                               selector: #selector(incomePrivacy),
                                               //: name: UPDATE_INDEX_GETCONFIG_NOTIFICATION,
                                               name: k_clickId,
                                               //: object: nil)
                                               object: nil)
        // 个人信息更新成功刷新UI
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(req_updateVIPFee),
                                               selector: #selector(limitedDit),
                                               //: name: USER_GETMYINFO_SUCCEED_NOTIFICATION,
                                               name: dream_pointTitle,
                                               //: object: nil)
                                               object: nil)
    }
}
