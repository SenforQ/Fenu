
//: Declare String Begin

/*: "setPassword" :*/
fileprivate let userReduceByName:String = "setPacreative domain"
fileprivate let noti_badIdent:String = "ssweditrd"

/*: "user/appleLogin" :*/
fileprivate let m_nothingPermitPath:String = "you"
fileprivate let notiModelId:String = "ser/ayellow listen lame"
fileprivate let app_segmentToolEntityTitle:String = "ogmystery"

/*: "user/googleLogin" :*/
fileprivate let app_girlDeviceStr:String = "USER"
fileprivate let kCorrectFormat:String = "GL"
fileprivate let user_featureUrl:String = "eLoginrunning sale interval member our"

/*: "user/smsLogin" :*/
fileprivate let m_adjustmentName:String = "user/task meeting forward"

/*: "user/emailVcodeLogin" :*/
fileprivate let user_leaveValue:[Character] = ["u","s","e","r","/","e","m","a","i","l","V","c","o","d"]
fileprivate let show_enhanceUrl:String = "broadcast"
fileprivate let showGivingStr:[Character] = ["L","o","g","i","n"]

/*: "user/pwdLogin" :*/
fileprivate let noti_informationId:String = "memory branch every root micuser/pwd"
fileprivate let dream_gazeStr:[Character] = ["L","o","g","i","n"]

/*: "user/emailPwdLogin" :*/
fileprivate let app_contractPath:String = "USER"
fileprivate let mainHundredMakerUrl:String = "normally party quietilP"
fileprivate let kHarassmentHairMsg:String = "together policywdLogin"

/*: "mobile" :*/
fileprivate let kFeedbackMsg:[UInt8] = [0x66,0x68,0x5b,0x62,0x65,0x5e]

fileprivate func letJoin(gold num: UInt8) -> UInt8 {
    let value = Int(num) + 7
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "areaCode" :*/
fileprivate let kParticleData:[UInt8] = [0xe5,0xf6,0xe1,0xe5,0xc7,0xeb,0xe0,0xe1]

private func directFood(approach num: UInt8) -> UInt8 {
    return num ^ 132
}

/*: "user/sendLoginSms" :*/
fileprivate let k_exampleTopicKey:[Character] = ["u","s","e","r","/","s","e","n","d","L","o","g","i","n","S","m","s"]

/*: "RSA加密失败" :*/
fileprivate let appMoreTitle:[Character] = ["R","S","A","加","\u{5bc6}","失","败"]

/*: "password" :*/
fileprivate let mUniversalFormat:[UInt8] = [0x8d,0x9c,0x8e,0x8e,0x8a,0x92,0x8f,0x99]

private func silentMode(colorful num: UInt8) -> UInt8 {
    return num ^ 253
}

/*: "vcode" :*/
fileprivate let show_rawComprehensiveIdent:[UInt8] = [0x65,0x64,0x6f,0x63,0x76]

/*: "user/modifyPassword" :*/
fileprivate let appTranslationValue:[Character] = ["u","s","e","r","/","m","o","d","i","f","y"]
fileprivate let k_handleUrl:[Character] = ["P","a"]
fileprivate let userMissingMessage:String = "capword"

/*: "user/sendModifyPwdSms" :*/
fileprivate let dream_generalEasternPublicationIdent:[Character] = ["u","s","e","r","/","s","e","n","d","M","o","d","i","f","y","P"]
fileprivate let noti_foreValue:String = "terms po civilwdSms"

/*: "user/sendModifyPwdEmail" :*/
fileprivate let main_followText:[Character] = ["u","s","e","r","/","s","e","n","d","M","o","d","i","f","y","P","w","d","E"]
fileprivate let show_missUrl:String = "mshipl"

/*: "mf/user/getMyInfo" :*/
fileprivate let mAspectFeeStr:String = "mf/uradio average"
fileprivate let show_frontName:String = "optimistic slimgetM"
fileprivate let m_feedbackAdmitMsg:[Character] = ["y","I","n","f","o"]

/*: "invite/bindInviter" :*/
fileprivate let userOffData:[Character] = ["i","n","v","i","t","e","/","b","i","n","d","I","n","v","i"]
fileprivate let user_splitKey:[Character] = ["t","e","r"]

/*: "invite/isBind" :*/
fileprivate let main_aliveMsg:String = "startnvstart"
fileprivate let dreamLimitKey:String = "row purchase capture selectionBind"

/*: "mf/user/CheckInviteCode" :*/
fileprivate let dream_normalFormat:[Character] = ["m","f","/","u","s","e","r","/","C","h","e","c","k","I","n","v","i","t","e","C","o"]
fileprivate let app_openlyMessage:String = "dgray"

/*: "mf/user/updateInfo" :*/
fileprivate let userPutStr:String = "angle pass am mid togethermf/u"
fileprivate let k_creativePath:[Character] = ["t","e","I","n","f","o"]

/*: "email" :*/
fileprivate let app_quietStr:[UInt8] = [0xda,0xd2,0xde,0xd6,0xd3]

/*: "user/getEmailBindVCode" :*/
fileprivate let mBasicPath:[Character] = ["u"]
fileprivate let userSufficientMsg:String = "thumb leave draw upser/g"
fileprivate let dream_remoteHerePath:String = "ailBincommunicate accounting gen"

/*: "user/sendLoginEmail" :*/
fileprivate let mCommentId:[Character] = ["u","s","e","r"]
fileprivate let mMechanicValue:[Character] = ["/","s","e","n","d","L","o","g","i","n","E","m","a","i","l"]

/*: "user/emailBind" :*/
fileprivate let kSimpleMessage:String = "user/eaway segment trigger"
fileprivate let showMatterName:String = "platformd"

/*: "user/logout" :*/
fileprivate let appIonMsg:String = "without"
fileprivate let userFlexibleId:String = "ser/lfloor announcement person mp"

/*: "app/countryList" :*/
fileprivate let show_distanceSwitchceName:[Character] = ["a","p","p","/","c"]
fileprivate let mainMaximumBeFormat:[Character] = ["o","u","n","t","r","y","L","i","s","t"]

/*: "user/getMobileBindVCode" :*/
fileprivate let dream_detailedMediumFormat:[Character] = ["u","s","e","r","/","g","e"]
fileprivate let noti_seriousKey:[Character] = ["t","M","o","b","i","l","e","B","i","n","d","V","C","o","d","e"]

/*: "user/mobileBind" :*/
fileprivate let k_twoItemText:String = "ussoon"
fileprivate let show_calendarId:String = "leBindpaper left retirement position actual"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InviteeRequestTool.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/29.
//

//: import UIKit
import UIKit

//: class TalkingLoginRequestTool: NSObject {
class InviteeRequestTool: NSObject {
    /// 第三方登录请求
    /// - Parameters:
    ///   - type: 登录类型
    ///   - content: 未加密手机号/邮箱
    ///   - params: 参数
    ///   - completion: 回调

    //: class func req_thirdLogin(type: LoginType, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
    class func cornerInputCompletion(type: EditionTableConvertible, content: String = "", params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = self.req_thirdLoginModel(type: type, params: params)
        let reqModel = self.package(type: type, params: params)
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: return
                return
            }
            // 跳转设置密码页
            //: let json = JSON(result ?? [:])
            let json = JSON(result ?? [:])
            //: if json["setPassword"].boolValue == true {
            if json[(String(userReduceByName.prefix(5)) + noti_badIdent.replacingOccurrences(of: "edit", with: "o"))].boolValue == true {
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
                //: DumpPushManager.share.func__pushToSetNewPasswordVC(type: type, content: content, isNewPwd: true)
                DumpPushManager.share.statementToPrompt(type: type, content: content, isNewPwd: true)
                //: return
                return
            }

            // 获取用户信息
            //: TalkingLoginRequestTool.req_LoginUserInfoData(type, completion: completion)
            InviteeRequestTool.lab(type, completion: completion)
        }
    }

    /// 第三方登录请求模型
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    /// - Returns: 请求模型
    //: private class func req_thirdLoginModel(type: LoginType, params: [String: String]) -> TalkingRequestModel {
    private class func package(type: EditionTableConvertible, params: [String: String]) -> DoingicialReactiveCompatible {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: switch type {
        switch type {
        //: case .AppleLogin:
        case .AppleLogin:
            //: reqModel.requestPath = "user/appleLogin"
            reqModel.requestPath = (m_nothingPermitPath.replacingOccurrences(of: "you", with: "u") + String(notiModelId.prefix(5)) + "ppleL" + app_segmentToolEntityTitle.replacingOccurrences(of: "mystery", with: "in"))
        //: case .GoogleLogin:
        case .GoogleLogin:
            //: reqModel.requestPath = "user/googleLogin"
            reqModel.requestPath = (app_girlDeviceStr.lowercased() + "/goo" + kCorrectFormat.lowercased() + String(user_featureUrl.prefix(6)))
        //: case .PhoneLogin:
        case .PhoneLogin:
            //: reqModel.requestPath = "user/smsLogin"
            reqModel.requestPath = (String(m_adjustmentName.prefix(5)) + "smsLogin")
        //: case .EmailLogin:
        case .EmailLogin:
            //: reqModel.requestPath = "user/emailVcodeLogin"
            reqModel.requestPath = (String(user_leaveValue) + show_enhanceUrl.replacingOccurrences(of: "broadcast", with: "e") + String(showGivingStr))
        //: case .PwdPhoneLogin:
        case .PwdPhoneLogin:
            //: reqModel.requestPath = "user/pwdLogin"
            reqModel.requestPath = (String(noti_informationId.suffix(8)) + String(dream_gazeStr))
        //: case .PwdEmailLogin:
        case .PwdEmailLogin:
            //: reqModel.requestPath = "user/emailPwdLogin"
            reqModel.requestPath = (app_contractPath.lowercased() + "/ema" + String(mainHundredMakerUrl.suffix(3)) + String(kHarassmentHairMsg.suffix(7)))
        }
        //: reqModel.params = params
        reqModel.params = params

        //: return reqModel
        return reqModel
    }

    /// 获取验证码
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_getPhoneCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func nonconductor(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: app_senseMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: kFeedbackMsg.map{letJoin(gold: $0)}, encoding: .utf8)!: reslutStr, String(bytes: kParticleData.map{directFood(approach: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = DoingicialReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/sendLoginSms"
            reqModel.requestPath = (String(k_exampleTopicKey))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appMoreTitle)))
        }
    }

    /// 密码登录
    /// - Parameters:
    ///   - phoneStr: 手机号登录
    ///   - areaStr: 区号
    ///   - pwd: 密码
    ///   - completion: 回调
    //: class func req_passwordLogin(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func mapCompletion(phoneStr: String, areaStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultPhoneStr = ""
        var resultPhoneStr = ""
        //: var resultCodeStr = ""
        var resultCodeStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: app_senseMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPhoneStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": resultPhoneStr, "areaCode": resultCodeStr, "password": resultPwdStr]
            let params = [String(bytes: kFeedbackMsg.map{letJoin(gold: $0)}, encoding: .utf8)!: resultPhoneStr, String(bytes: kParticleData.map{directFood(approach: $0)}, encoding: .utf8)!: resultCodeStr, String(bytes: mUniversalFormat.map{silentMode(colorful: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdPhoneLogin, params: params, completion: completion)
            InviteeRequestTool.cornerInputCompletion(type: .PwdPhoneLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appMoreTitle)))
        }
    }

    /// 修改密码
    /// - Parameters:
    ///   - pwd: 密码
    ///   - vcode: 验证码
    ///   - completion: 回调
    //: class func req_modifyPassword(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
    class func reading(pwd: String, vcode: String?, completion: @escaping FinishBlock) {
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: app_senseMessage)
            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: var params = ["password": resultPwdStr]
            var params = [String(bytes: mUniversalFormat.map{silentMode(colorful: $0)}, encoding: .utf8)!: resultPwdStr]

            //: if vcode?.count ?? 0 > 0 {
            if vcode?.count ?? 0 > 0 {
                //: params["vcode"] = vcode
                params[String(bytes: show_rawComprehensiveIdent.reversed(), encoding: .utf8)!] = vcode
            }

            //: let reqModel = TalkingRequestModel()
            let reqModel = DoingicialReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/modifyPassword"
            reqModel.requestPath = (String(appTranslationValue) + String(k_handleUrl) + userMissingMessage.replacingOccurrences(of: "cap", with: "ss"))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appMoreTitle)))
        }
    }

    /// 获取修改【手机号】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdSms(completion: @escaping FinishBlock) {
    class func cast(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdSms"
        reqModel.requestPath = (String(dream_generalEasternPublicationIdent) + String(noti_foreValue.suffix(5)))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取修改【邮箱】密码验证码
    /// - Parameter completion: 回调
    //: class func req_sendModifyPwdEmail(completion: @escaping FinishBlock) {
    class func matchCompletion(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestType = .GET
        reqModel.requestType = .GET
        //: reqModel.requestPath = "user/sendModifyPwdEmail"
        reqModel.requestPath = (String(main_followText) + show_missUrl.replacingOccurrences(of: "ship", with: "ai"))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 获取用户信息接口
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_LoginUserInfoData(_ type: LoginType, completion: FinishBlock? = nil) {
    class func lab(_ type: EditionTableConvertible, completion: FinishBlock? = nil) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(mAspectFeeStr.prefix(4)) + "ser/" + String(show_frontName.suffix(4)) + String(m_feedbackAdmitMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: return
                return
            }
            //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
            noti_foundData.set(result, forKey: show_numberValue)
            //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
            if let userModel = JSONDeserializer<RemovalReactiveCompatible>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                //: AdministratorThen.share.loginUserMode = userModel
                AdministratorThen.share.loginUserMode = userModel

                // 记录上次登录类型
                //: Defaults.set(type.rawValue, forKey: TalkingLastLoginTypeCacheKey)
                noti_foundData.set(type.rawValue, forKey: mRandomMessage)
                //: if AdministratorThen.share.loginUserMode.updateInfo == false {
                if AdministratorThen.share.loginUserMode.updateInfo == false { // 需要完善资料
                    //: completion?(false, nil, errorModel)
                    completion?(false, nil, errorModel)
                    //: DumpPushManager.share.func__pushToFillUserInfoVC()
                    DumpPushManager.share.imageForeVc()
                    //: return
                    return
                }

                //: completion?(succeed, result, errorModel)
                completion?(succeed, result, errorModel)
                //: NotificationCenter.default.post(name: DID_LOGIN_SUCCESS_NOTIFICATION, object: nil, userInfo: result as! Dictionary<String, Any>)
                NotificationCenter.default.post(name: dream_lessMessage, object: nil, userInfo: result as! [String: Any])

                //: if AdministratorThen.share.loginUserMode.remindBindEmail == true {
                if AdministratorThen.share.loginUserMode.remindBindEmail == true { // 需要绑定邮箱
                    //: DispatchQueue.main.asyncAfter(deadline: .now()+0.5) {
                    DispatchQueue.main.asyncAfter(deadline: .now() + 0.5) {
                        //: DumpPushManager.share.func__pushToLockUserEmailVC(isShowBack: false)
                        DumpPushManager.share.limitWith(isShowBack: false)
                    }
                }
            }
        }
    }

    /// 绑定邀请邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_inviteBindInviter(params: [String: String], completion: @escaping FinishBlock) {
    class func decide(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "invite/bindInviter"
        reqModel.requestPath = (String(userOffData) + String(user_splitKey))
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 是否已经绑定过邀请码
    /// - Parameters:
    ///   - params: 邀请码
    ///   - completion: 回调
    //: class func req_isBindInviter(completion: @escaping FinishBlock) {
    class func fill(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "invite/isBind"
        reqModel.requestPath = (main_aliveMsg.replacingOccurrences(of: "start", with: "i") + "te/is" + String(dreamLimitKey.suffix(4)))

        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 检测邀请码
    /// - Parameters:
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_CheckCodeData(params: [String: String], completion: @escaping FinishBlock) {
    class func pickOutCompletion(params: [String: String], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "mf/user/CheckInviteCode"
        reqModel.requestPath = (String(dream_normalFormat) + app_openlyMessage.replacingOccurrences(of: "gray", with: "e"))
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.params = params
        reqModel.params = params
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 更新用户信息
    /// - Parameters:
    ///   - type: 登录类型
    ///   - params: 参数
    ///   - completion: 回调
    //: class func req_updateUserInfo(params: [String: Any], completion: @escaping FinishBlock) {
    class func until(params: [String: Any], completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "mf/user/updateInfo"
        reqModel.requestPath = (String(userPutStr.suffix(4)) + "ser/upda" + String(k_creativePath))
        //: var temparams = params
        var temparams = params
        //: temparams += UIDevice.deviceInfoDic
        temparams += UIDevice.deviceInfoDic
        //: if let poStr = temparams["p0"] {
        if let poStr = temparams["p0"] {
            //: let token = NSDate.getCurrentTimeStamp()
            let token = NSDate.lieu()
            //: reqModel.addHeaderToken = token
            reqModel.addHeaderToken = token
            //: let key = token.appending(TokenSaltStr)
            let key = token.appending(main_localData)
            //: temparams["p0"] = (poStr as? String)?.encrypt(withKey: key)
            temparams["p0"] = (poStr as? String)?.doContrast(key)
        }
        //: reqModel.params = temparams
        reqModel.params = temparams
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// Email Code
    /// - Parameters:
    ///   - emailStr: Email
    ///   - emailType: Login or Bind
    //: class func req_getBindEmailCode( emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
    class func push(emailStr: String, isBind: Bool, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: app_senseMessage)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            //: let params = ["email": reslutStr]
            let params = [String(bytes: app_quietStr.map{$0^191}, encoding: .utf8)!: reslutStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = DoingicialReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: if isBind {
            if isBind {
                //: reqModel.requestPath = "user/getEmailBindVCode"
                reqModel.requestPath = (String(mBasicPath) + String(userSufficientMsg.suffix(5)) + "etEm" + String(dream_remoteHerePath.prefix(6)) + "dVCode")
                //: } else {
            } else {
                //: reqModel.requestPath = "user/sendLoginEmail"
                reqModel.requestPath = (String(mCommentId) + String(mMechanicValue))
            }
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appMoreTitle)))
        }
    }

    /// 绑定邮箱验证码
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_bindEmailCode( emailCode: String, completion: @escaping FinishBlock) {
    class func develop(emailCode: String, completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "user/emailBind"
        reqModel.requestPath = (String(kSimpleMessage.prefix(6)) + "mailB" + showMatterName.replacingOccurrences(of: "platform", with: "in"))
        //: reqModel.params = ["vcode": emailCode]
        reqModel.params = [String(bytes: show_rawComprehensiveIdent.reversed(), encoding: .utf8)!: emailCode]
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    //: class func req_passwordEmailLogin(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
    class func highlight(emailStr: String, pwd: String, completion: @escaping FinishBlock) {
        //: var resultEmailStr = ""
        var resultEmailStr = ""
        //: var resultPwdStr = ""
        var resultPwdStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: app_senseMessage)
            //: let clear = try ClearMessage(string: emailStr, using: .utf8)
            let clear = try ClearMessage(string: emailStr, using: .utf8)
            //: resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultEmailStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            let pwdClear = try ClearMessage(string: "\(pwd)", using: .utf8)
            //: resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            resultPwdStr = try pwdClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["email": resultEmailStr, "password": resultPwdStr]
            let params = [String(bytes: app_quietStr.map{$0^191}, encoding: .utf8)!: resultEmailStr, String(bytes: mUniversalFormat.map{silentMode(colorful: $0)}, encoding: .utf8)!: resultPwdStr]
            //: TalkingLoginRequestTool.req_thirdLogin(type: .PwdEmailLogin, params: params, completion: completion)
            InviteeRequestTool.cornerInputCompletion(type: .PwdEmailLogin, params: params, completion: completion)
            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appMoreTitle)))
        }
    }

    //: typealias RequestLoginOutBlock = (_ t: Bool) -> Void
    typealias RequestLoginOutBlock = (_ t: Bool) -> Void

    //: @objc class func req_loginOut(completion: @escaping RequestLoginOutBlock) {
    @objc class func executeValue(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "user/logout"
        reqModel.requestPath = (appIonMsg.replacingOccurrences(of: "without", with: "u") + String(userFlexibleId.prefix(5)) + "ogout")
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, _, _ in
            //: completion(succeed)
            completion(succeed)
        }
    }

    //: @objc class func func__requestReloginUserInfo(completion: @escaping RequestLoginOutBlock) {
    @objc class func called(completion: @escaping RequestLoginOutBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "mf/user/getMyInfo"
        reqModel.requestPath = (String(mAspectFeeStr.prefix(4)) + "ser/" + String(show_frontName.suffix(4)) + String(m_feedbackAdmitMsg))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, _ in
            //: if succeed {
            if succeed {
                //: Defaults.set(result, forKey: TalkingLoginUserInfoCacheKey)
                noti_foundData.set(result, forKey: show_numberValue)
                //: if let userModel = JSONDeserializer<LoginUserModel>.deserializeFrom(dict: result as? Dictionary<String, Any>, designatedPath: nil) {
                if let userModel = JSONDeserializer<RemovalReactiveCompatible>.deserializeFrom(dict: result as? [String: Any], designatedPath: nil) {
                    //: AdministratorThen.share.loginUserMode = userModel
                    AdministratorThen.share.loginUserMode = userModel
                }
            }
            //: completion(succeed)
            completion(succeed)
        }
    }

    /// 获取允许国家列表
    /// - Parameters:
    ///   - completion: 回调
    //: class func req_countryList( completion: @escaping FinishBlock) {
    class func text(completion: @escaping FinishBlock) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestPath = "app/countryList"
        reqModel.requestPath = (String(show_distanceSwitchceName) + String(mainMaximumBeFormat))
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: completion(succeed, result, errorModel)
            completion(succeed, result, errorModel)
        }
    }

    /// 绑定手机号获取验证码
    //: class func req_bindPhoneGetCode( phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
    class func phoneWill(phoneStr: String, areaStr: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: app_senseMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr]
            let params = [String(bytes: kFeedbackMsg.map{letJoin(gold: $0)}, encoding: .utf8)!: reslutStr, String(bytes: kParticleData.map{directFood(approach: $0)}, encoding: .utf8)!: reslutCodeStr]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = DoingicialReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/getMobileBindVCode"
            reqModel.requestPath = (String(dream_detailedMediumFormat) + String(noti_seriousKey))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appMoreTitle)))
        }
    }

    /// 绑定手机号
    //: class func req_MobileBind(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
    class func suspend(phoneStr: String, areaStr: String, vcode: String, completion: @escaping FinishBlock) {
        //: var reslutStr = ""
        var reslutStr = ""
        //: var reslutCodeStr = ""
        var reslutCodeStr = ""
        //: do {
        do {
            //: let rsa_publicKey = try PublicKey(pemEncoded: LoginRsaPublicKey)
            let rsa_publicKey = try PublicKey(pemEncoded: app_senseMessage)
            //: let clear = try ClearMessage(string: phoneStr, using: .utf8)
            let clear = try ClearMessage(string: phoneStr, using: .utf8)
            //: reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutStr = try clear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            let codeClear = try ClearMessage(string: "+\(areaStr)", using: .utf8)
            //: reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String
            reslutCodeStr = try codeClear.encrypted(with: rsa_publicKey, padding: .PKCS1).base64String

            //: let params = ["mobile": reslutStr, "areaCode": reslutCodeStr, "vcode": vcode]
            let params = [String(bytes: kFeedbackMsg.map{letJoin(gold: $0)}, encoding: .utf8)!: reslutStr, String(bytes: kParticleData.map{directFood(approach: $0)}, encoding: .utf8)!: reslutCodeStr, String(bytes: show_rawComprehensiveIdent.reversed(), encoding: .utf8)!: vcode]
            //: let reqModel = TalkingRequestModel.init()
            let reqModel = DoingicialReactiveCompatible()
            //: reqModel.requestType = .POST
            reqModel.requestType = .POST
            //: reqModel.requestPath = "user/mobileBind"
            reqModel.requestPath = (k_twoItemText.replacingOccurrences(of: "soon", with: "er") + "/mobi" + String(show_calendarId.prefix(6)))
            //: reqModel.params = params
            reqModel.params = params
            //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
            appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
                //: completion(succeed, result, errorModel)
                completion(succeed, result, errorModel)
            }

            //: } catch {
        } catch {
            //: printLog(message: "RSA加密失败")
            printLog(message: (String(appMoreTitle)))
        }
    }
}
