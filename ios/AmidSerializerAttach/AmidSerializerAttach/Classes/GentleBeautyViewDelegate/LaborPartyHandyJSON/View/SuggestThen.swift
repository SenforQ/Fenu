
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let mainPresentUrl:[UInt8] = [0x25,0x22,0x25,0x38,0x64,0x2f,0x23,0x28,0x29,0x3e,0x76,0x65,0x6c,0x24,0x2d,0x3f,0x6c,0x22,0x23,0x38,0x6c,0x2e,0x29,0x29,0x22,0x6c,0x25,0x21,0x3c,0x20,0x29,0x21,0x29,0x22,0x38,0x29,0x28]

private func captureWarn(when num: UInt8) -> UInt8 {
    return num ^ 76
}

/*: "icon_cover_toast" :*/
fileprivate let mainWomanFormat:String = "icodefine"
fileprivate let mNameurePath:[Character] = ["r","_","t","o","a","s","t"]

/*: "Free video time is over, you can continue to call her if you are interested" :*/
fileprivate let userEditNurseTitle:[UInt8] = [0x64,0x65,0x74,0x73,0x65,0x72,0x65,0x74,0x6e,0x69,0x20,0x65,0x72,0x61,0x20,0x75,0x6f,0x79,0x20,0x66,0x69,0x20,0x72,0x65,0x68,0x20,0x6c,0x6c,0x61,0x63,0x20,0x6f,0x74,0x20,0x65,0x75,0x6e,0x69,0x74,0x6e,0x6f,0x63,0x20,0x6e,0x61,0x63,0x20,0x75,0x6f,0x79,0x20,0x2c,0x72,0x65,0x76,0x6f,0x20,0x73,0x69,0x20,0x65,0x6d,0x69,0x74,0x20,0x6f,0x65,0x64,0x69,0x76,0x20,0x65,0x65,0x72,0x46]

/*: "#34C759" :*/
fileprivate let kSwitchingUrl:[Character] = ["#","3","4","C","7","5","9"]

/*: "icon_match_stopcall_pop" :*/
fileprivate let user_listenerKey:String = "icon_maquiet doc"
fileprivate let user_infoPermitId:[Character] = ["t","c","h","_","s"]
fileprivate let k_alwaysPath:[Character] = ["t","o","p","c","a","l","l","_","p","o","p"]

/*: "icon_lounge_big" :*/
fileprivate let m_rePath:[Character] = ["i","c","o","n","_"]
fileprivate let dream_fundamentalTalkData:String = "lorepublic"

/*: "#AB57F6" :*/
fileprivate let main_deliveryPath:[Character] = ["#","A"]
fileprivate let userActivityIdent:[Character] = ["B","5","7","F","6"]

/*: "#FC57B7" :*/
fileprivate let mainDocumentId:[Character] = ["#","F","C","5","7","B","7"]

/*: "#FE9074" :*/
fileprivate let kOfAfterId:[Character] = ["#","F","E","9","0","7","4"]

/*: "Maybe next time" :*/
fileprivate let user_soonData:String = "earn whileMaybe "
fileprivate let show_pitchStr:String = "tvisitore"

/*: "type" :*/
fileprivate let app_extraComprehensiveIdent:[UInt8] = [0x3a,0x37,0x3e,0x2b]

private func workerPose(representative num: UInt8) -> UInt8 {
    return num ^ 78
}

/*: "toUid" :*/
fileprivate let show_veryNothingMarkText:String = "toUidtrain length over make"

/*: "uid" :*/
fileprivate let noti_oppositeKey:[UInt8] = [0x64,0x69,0x75]

/*: "fromFreeCall" :*/
fileprivate let notiWillBassUrl:[Character] = ["f","r","o","m","F","r","e","e","C","a","l"]
fileprivate let mainMonthData:String = "L"

/*: "cmd" :*/
fileprivate let showCostName:[UInt8] = [0x9c,0xa6,0x9d]

fileprivate func jawChannel(valid num: UInt8) -> UInt8 {
    let value = Int(num) - 57
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "requestCall" :*/
fileprivate let noti_beanCourseId:[Character] = ["r","e","q","u","e"]
fileprivate let notiPurchaseSlideStr:[Character] = ["s","t","C","a","l","l"]

/*: "data" :*/
fileprivate let show_rootData:[UInt8] = [0x62,0x5f,0x72,0x5f]

fileprivate func slowPlayer(early num: UInt8) -> UInt8 {
    let value = Int(num) + 2
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

/*: "originalFee" :*/
fileprivate let mainRandomIdent:String = "orlimited"
fileprivate let mFeedStr:[Character] = ["g","i","n","a","l","F","e","e"]

/*: "VIPFee" :*/
fileprivate let k_walkCommercialUrl:[Character] = ["V","I","P","F","e","e"]

/*: "freeCallTimes" :*/
fileprivate let mainFollowingContent:String = "treat contact for going deadlinefreeC"
fileprivate let noti_animaId:String = "message occurallTimes"

/*: "onRequestCall" :*/
fileprivate let noti_retirementTitle:String = "write read exportonRe"
fileprivate let dream_propertyUrl:[Character] = ["l"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  SuggestThen.swift
//  AbroadTalking
//
//  Created by Charlotte on 2023/6/15.
//

//: import UIKit
import UIKit

//: class TalkingFreeStopCallPopUpView: UIView {
class SuggestThen: UIView {
    //: var popView: TalkingPopView?
    var popView: AsideView?
    //: var data = [String: Any]()
    var data = [String: Any]()

    //: override init(frame: CGRect) {
    override init(frame: CGRect) {
        //: super.init(frame: frame)
        super.init(frame: frame)
        //: setupSubviews()
        pass()
        //: setupSubViewsConstraint()
        totalerval()
    }

    //: @available(*, unavailable)
    @available(*, unavailable)
    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: mainPresentUrl.map{captureWarn(when: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: TalkingSocketManager.shared.errorDelegate = nil
        AdviceLandscapeReactiveCompatible.shared.errorDelegate = nil
    }

    // 懒加载初始化

    //: lazy var contentView: UIView = {
    lazy var contentView: UIView = {
        //: let view = UIView()
        let view = UIView()
        //: view.backgroundColor = UIColor.white
        view.backgroundColor = UIColor.white
        //: view.layer.cornerRadius = 16
        view.layer.cornerRadius = 16
        //: return view
        return view
        //: }()
    }()

    //: lazy var topIcon: UIImageView = {
    lazy var topIcon: UIImageView = {
        //: let iamg = UIImageView()
        let iamg = UIImageView()
        //: iamg.image = UIImage.BundleImageNamed(name: "icon_cover_toast")
        iamg.image = UIImage.scribeNumber(name: (mainWomanFormat.replacingOccurrences(of: "define", with: "n") + "_cove" + String(mNameurePath)))
        //: iamg.contentMode = .scaleAspectFill
        iamg.contentMode = .scaleAspectFill
        //: return iamg
        return iamg
        //: }()
    }()

    //: lazy var inputTView: UITextView = {
    lazy var inputTView: UITextView = {
        //: let textView = UITextView()
        let textView = UITextView()
        //: textView.font = UIFont.pingfangFont(type: .Regular, fontSize: 16)
        textView.font = UIFont.afterDisable(type: .Regular, fontSize: 16)
        //: textView.textColor = UIColor.appTitleColor()
        textView.textColor = UIColor.manualColor()
        //: textView.textContainer.maximumNumberOfLines = 0
        textView.textContainer.maximumNumberOfLines = 0
        //: textView.textAlignment = .center
        textView.textAlignment = .center
        //: textView.backgroundColor = .clear
        textView.backgroundColor = .clear
        //: textView.isEditable = false
        textView.isEditable = false
        //: textView.text = "Free video time is over, you can continue to call her if you are interested".localized
        textView.text = String(bytes: userEditNurseTitle.reversed(), encoding: .utf8)!.localized
        //: return textView
        return textView
        //: }()
    }()

    // 原价按钮
    //: private lazy var originalFeeBtn: TalkingButton = {
    private lazy var originalFeeBtn: WordOfAdviceThen = {
        //: let btn = TalkingButton()
        let btn = WordOfAdviceThen()
        //: btn.setBackgroundColor(color: UIColor(hex: "#34C759")!, forState: .normal)
        btn.toastrackUpwardlyState(color: UIColor(hex: (String(kSwitchingUrl)))!, forState: .normal)
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_match_stopcall_pop"), for: .normal)
        btn.setImage(UIImage.scribeNumber(name: (String(user_listenerKey.prefix(7)) + String(user_infoPermitId) + String(k_alwaysPath))), for: .normal)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Regular, fontSize: 18)
        btn.titleLabel?.font = UIFont.afterDisable(type: .Regular, fontSize: 18)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.spaceBetweenTitleAndImage = 6
        btn.spaceBetweenTitleAndImage = 6
        //: btn.layer.cornerRadius = 27
        btn.layer.cornerRadius = 27
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(originalFeeButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(reserve), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    // VIP价格
    //: private lazy var vipFeeBtn: TalkingButton = {
    private lazy var vipFeeBtn: WordOfAdviceThen = {
        //: let btn = TalkingButton()
        let btn = WordOfAdviceThen()
        //: btn.setImage(UIImage.BundleImageNamed(name: "icon_lounge_big"), for: .normal)
        btn.setImage(UIImage.scribeNumber(name: (String(m_rePath) + dream_fundamentalTalkData.replacingOccurrences(of: "republic", with: "un") + "ge_big")), for: .normal)
        //: btn.imageView?.contentMode = .scaleAspectFill
        btn.imageView?.contentMode = .scaleAspectFill
        //: btn.verticalAlignImageAndTitleWith(interval: 5, imageSize: CGSize(width: 28, height: 28), topOffset: 13)
        btn.localPrice(interval: 5, imageSize: CGSize(width: 28, height: 28), topOffset: 13)
        //: btn.titleLabel?.font = UIFont.pingfangFont(type: .Medium, fontSize: 18)
        btn.titleLabel?.font = UIFont.afterDisable(type: .Medium, fontSize: 18)
        //: btn.setTitleColor(.white, for: .normal)
        btn.setTitleColor(.white, for: .normal)
        //: btn.spaceBetweenTitleAndImage = 6
        btn.spaceBetweenTitleAndImage = 6
        //: let colors = [UIColor(hex: "#AB57F6")!.cgColor, UIColor(hex: "#FC57B7")!.cgColor, UIColor(hex: "#FE9074")!.cgColor]
        let colors = [UIColor(hex: (String(main_deliveryPath) + String(userActivityIdent)))!.cgColor, UIColor(hex: (String(mainDocumentId)))!.cgColor, UIColor(hex: (String(kOfAfterId)))!.cgColor]
        //: btn.setBackgroundImage(UIImage.imageGradientColor(colors: colors, size: CGSize(width: actualWidth(w: 257), height: actualWidth(w: 54))), for: .normal)
        btn.setBackgroundImage(UIImage.tabOn(colors: colors, size: CGSize(width: actualWidth(w: 257), height: actualWidth(w: 54))), for: .normal)
        //: btn.layer.cornerRadius = 27
        btn.layer.cornerRadius = 27
        //: btn.clipsToBounds = true
        btn.clipsToBounds = true
        //: btn.addTarget(self, action: #selector(maleVipPriceButtonClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(averageCost), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appValueDetailColor(), .font: UIFont.pingfangFont(type: .Regular, fontSize: 13), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.fluent(), .font: UIFont.afterDisable(type: .Regular, fontSize: 13), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Maybe next time".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(user_soonData.suffix(6)) + "next " + show_pitchStr.replacingOccurrences(of: "visitor", with: "im")).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: btn.addTarget(self, action: #selector(closeBtnClick), for: .touchUpInside)
        btn.addTarget(self, action: #selector(mainClick), for: .touchUpInside)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - Event

//: extension TalkingFreeStopCallPopUpView {
extension SuggestThen {
    /// 原价按钮拨打事件
    //: @objc func originalFeeButtonClick() {
    @objc func reserve() {
        //: let type = self.data["type"] as? Int ?? -1
        let type = self.data[String(bytes: app_extraComprehensiveIdent.map{workerPose(representative: $0)}, encoding: .utf8)!] as? Int ?? -1
        //: let toUid = self.data["toUid"] as? String ?? ""
        let toUid = self.data[(String(show_veryNothingMarkText.prefix(5)))] as? String ?? ""

        //: var data: [String: Any] = ["type": type,
        var data: [String: Any] = [String(bytes: app_extraComprehensiveIdent.map{workerPose(representative: $0)}, encoding: .utf8)!: type,
                                   //: "uid": toUid]
                                   String(bytes: noti_oppositeKey.reversed(), encoding: .utf8)!: toUid]
        //: if AdministratorThen.share.loginUserMode.freeCallTimes > 0, AdministratorThen.share.loginUserMode.sex == Gender.male.rawValue {
        if AdministratorThen.share.loginUserMode.freeCallTimes > 0, AdministratorThen.share.loginUserMode.sex == RecordNameLiteral.male.rawValue {
            //: data.updateValue(1, forKey: "fromFreeCall")
            data.updateValue(1, forKey: (String(notiWillBassUrl) + mainMonthData.lowercased()))
            //: TalkingSocketManager.shared.isFreeCall = true
            AdviceLandscapeReactiveCompatible.shared.isFreeCall = true
        }
        //: let info: [String: Any] = ["cmd": "requestCall", "data": data]
        let info: [String: Any] = [String(bytes: showCostName.map{jawChannel(valid: $0)}, encoding: .utf8)!: (String(noti_beanCourseId) + String(notiPurchaseSlideStr)), String(bytes: show_rootData.map{slowPlayer(early: $0)}, encoding: .utf8)!: data]
        //: TalkingSocketManager.shared.sendMessage(info: info)
        AdviceLandscapeReactiveCompatible.shared.underAccelerate(info: info)
        //: TalkingSocketManager.shared.errorDelegate = self
        AdviceLandscapeReactiveCompatible.shared.errorDelegate = self
        //: dismiss()
        backgroundDismiss()
    }

    /// 关闭按钮点击事件
    //: @objc func closeBtnClick() {
    @objc func mainClick() {
        //: dismiss()
        backgroundDismiss()
    }

    /// 展示
    //: func show() {
    func living() {
        //: popView = TalkingPopView(frame: UIScreen.main.bounds)
        popView = AsideView(frame: UIScreen.main.bounds)
        //: frame = CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight)
        frame = CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage)
        //: popView?.initWithView(view: self)
        popView?.viewChin(view: self)
        //: popView?.showInView(view: CreditsThen.getWindow())
        popView?.validMake(view: CreditsThen.captain())
        //: popView?.isRemoveTapGes = true
        popView?.isRemoveTapGes = true
    }

    //: @objc func dismiss() {
    @objc func backgroundDismiss() {
        //: popView?.dismissView()
        popView?.theme()
        //: popView = nil
        popView = nil
    }

    /// 更新数据
    //: func setShowData(data: [String: Any]) {
    func viewNetwork(data: [String: Any]) {
        //: self.data = data
        self.data = data
        //: let originalFee = data["originalFee"] as? Int ?? 0
        let originalFee = data[(mainRandomIdent.replacingOccurrences(of: "limited", with: "i") + String(mFeedStr))] as? Int ?? 0
        //: let vipFee = data["VIPFee"] as? Int ?? 0
        let vipFee = data[(String(k_walkCommercialUrl))] as? Int ?? 0
        //: let freeCallTimes = data["freeCallTimes"] as? Int
        let freeCallTimes = data[(String(mainFollowingContent.suffix(5)) + String(noti_animaId.suffix(8)))] as? Int
        //: AdministratorThen.share.loginUserMode.freeCallTimes = freeCallTimes ?? 0
        AdministratorThen.share.loginUserMode.freeCallTimes = freeCallTimes ?? 0
        //: originalFeeBtn.setTitle(String.setFreeStopPriceStr(originalFee: originalFee), for: .normal)
        originalFeeBtn.setTitle(String.societyVoiceCon(originalFee: originalFee), for: .normal)
        //: vipFeeBtn.setTitle(String.setFreeStopPriceStr(originalFee: originalFee, vipFee: vipFee), for: .normal)
        vipFeeBtn.setTitle(String.societyVoiceCon(originalFee: originalFee, vipFee: vipFee), for: .normal)
        //: updateSubViewsConstraint()
        remarkConstraint()
    }

    /// 男性侧vip价格按钮点击事件
    //: @objc private func maleVipPriceButtonClick() {
    @objc private func averageCost() {
        //: guard AdministratorThen.share.loginUserMode.loungePlus else {
        guard AdministratorThen.share.loginUserMode.loungePlus else {
            // 非会员 && 默认模式
            //: if AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue {
            if AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue {
                //: DumpPushManager.share.func__pushToSubscribeAlert()
                DumpPushManager.share.underCharm()
            }
            //: dismiss()
            backgroundDismiss()
            //: return
            return
        }

        //: originalFeeButtonClick()
        reserve()
    }
}

// MARK: - TwelveNoonObjectProtocol

//: extension TalkingFreeStopCallPopUpView: TalkingSocketManagerErrorDelegate {
extension SuggestThen: TwelveNoonObjectProtocol {
    //: func socket_didRecieveError(errorNo: Int, cmd: String, msg: String, data: [String: Any]) {
    func playerData(errorNo: Int, cmd: String, msg: String, data _: [String: Any]) {
        //: if cmd == "onRequestCall" {
        if cmd == (String(noti_retirementTitle.suffix(4)) + "questCal" + String(dream_propertyUrl)) {
            //: func__showStatusBarErrorMsg(showMsg: msg)
            iconThemeBringBackPlace(showMsg: msg)
            //: if errorNo == VideoChatErrorCode.MoneyLack.rawValue {
            if errorNo == JawContentConvertible.MoneyLack.rawValue {
                //: guard AdministratorThen.share.appStatus == AppSkinStatus.normal.rawValue else { return }
                guard AdministratorThen.share.appStatus == BecauseMultiplierTarget.normal.rawValue else { return }
                //: DumpPushManager.share.func__jumpToWebRecharge(clickEvent: clickFreepopupButton, sufficient: false)
                DumpPushManager.share.shapeStyle(clickEvent: mainSaveTitle, sufficient: false)
            }
        }
    }
}

// MARK: - Layout

//: extension TalkingFreeStopCallPopUpView {
extension SuggestThen {
    /// 更新约束
    //: private func updateSubViewsConstraint() {
    private func remarkConstraint() {
        //: if AdministratorThen.share.loginUserMode.loungePlus {
        if AdministratorThen.share.loginUserMode.loungePlus {
            //: originalFeeBtn.isHidden = true
            originalFeeBtn.isHidden = true
            //: contentView.snp.updateConstraints { make in
            contentView.snp.updateConstraints { make in
                //: make.height.equalTo(actualWidth(w: 255))
                make.height.equalTo(actualWidth(w: 255))
            }
        }
    }

    /// 添加视图
    //: private func setupSubviews() {
    private func pass() {
        //: addSubview(contentView)
        addSubview(contentView)
        //: contentView.addSubview(topIcon)
        contentView.addSubview(topIcon)
        //: contentView.addSubview(closeBtn)
        contentView.addSubview(closeBtn)
        //: contentView.addSubview(inputTView)
        contentView.addSubview(inputTView)
        //: contentView.addSubview(originalFeeBtn)
        contentView.addSubview(originalFeeBtn)
        //: contentView.addSubview(vipFeeBtn)
        contentView.addSubview(vipFeeBtn)
    }

    /// 添加约束
    //: private func setupSubViewsConstraint() {
    private func totalerval() {
        //: contentView.snp.makeConstraints { make in
        contentView.snp.makeConstraints { make in
            //: make.center.equalToSuperview()
            make.center.equalToSuperview()
            //: make.width.equalTo(actualWidth(w: 297))
            make.width.equalTo(actualWidth(w: 297))
            //: make.height.equalTo(actualWidth(w: 329))
            make.height.equalTo(actualWidth(w: 329))
        }

        //: topIcon.snp.makeConstraints { make in
        topIcon.snp.makeConstraints { make in
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.top.equalTo(actualWidth(w: -43))
            make.top.equalTo(actualWidth(w: -43))
            //: make.width.height.equalTo(actualWidth(w: 101))
            make.width.height.equalTo(actualWidth(w: 101))
        }

        //: inputTView.snp.makeConstraints { make in
        inputTView.snp.makeConstraints { make in
            //: make.top.equalTo(topIcon.snp.bottom)
            make.top.equalTo(topIcon.snp.bottom)
            //: make.leading.equalTo(20)
            make.leading.equalTo(20)
            //: make.trailing.equalTo(-20)
            make.trailing.equalTo(-20)
            //: make.height.equalTo(actualWidth(w: 103))
            make.height.equalTo(actualWidth(w: 103))
        }

        //: closeBtn.snp.makeConstraints { make in
        closeBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(actualWidth(w: -20))
            make.bottom.equalTo(actualWidth(w: -20))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
            //: make.height.equalTo(14)
            make.height.equalTo(14)
        }

        //: vipFeeBtn.snp.makeConstraints { make in
        vipFeeBtn.snp.makeConstraints { make in
            //: make.bottom.equalTo(closeBtn.snp.top).offset(actualWidth(w: -15))
            make.bottom.equalTo(closeBtn.snp.top).offset(actualWidth(w: -15))
            //: make.width.equalTo(actualWidth(w: 257))
            make.width.equalTo(actualWidth(w: 257))
            //: make.height.equalTo(actualWidth(w: 54))
            make.height.equalTo(actualWidth(w: 54))
            //: make.centerX.equalToSuperview()
            make.centerX.equalToSuperview()
        }

        //: originalFeeBtn.snp.makeConstraints { make in
        originalFeeBtn.snp.makeConstraints { make in
            //: make.centerX.width.height.equalTo(vipFeeBtn)
            make.centerX.width.height.equalTo(vipFeeBtn)
            //: make.bottom.equalTo(vipFeeBtn.snp.top).offset(actualWidth(w: -20))
            make.bottom.equalTo(vipFeeBtn.snp.top).offset(actualWidth(w: -20))
        }
    }
}
