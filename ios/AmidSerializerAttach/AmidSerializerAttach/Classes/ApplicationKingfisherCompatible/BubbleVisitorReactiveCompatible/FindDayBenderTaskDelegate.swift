
//: Declare String Begin

/*: ; build: :*/
fileprivate let dream_fingerStr:[Character] = [";"," ","b","u","i","l","d"]
fileprivate let kDayValue:[Character] = [":"]

/*: ; iOS  :*/
fileprivate let appConPath:String = "; iOS trace coordinate"

/*: "audio" :*/
fileprivate let mDoingceName:[Character] = ["a","u","d","i","o"]

/*: .wav" :*/
fileprivate let noti_clearlyStr:String = ".wavalongside arrive girl for"

/*: "audio/wav" :*/
fileprivate let showDrownPath:[Character] = ["a","u","d","i","o"]
fileprivate let notiFrameEonValue:String = "/wavsale media"

/*: .jpg" :*/
fileprivate let userAttributeKey:[Character] = [".","j","p","g"]

/*: "image/jpeg" :*/
fileprivate let k_mobileId:String = "image/jpetemp demonstrate signal"
fileprivate let mBurnMessage:[Character] = ["g"]

/*: "://app." :*/
fileprivate let mImpressionOutputPath:String = "group measure region sh writing://app."

/*: "data" :*/
fileprivate let kLivingGlobalId:[Character] = ["d","a","t","a"]

/*: ":null" :*/
fileprivate let showHideBoyMsg:String = ":nullactive item soul none healthy"

/*: "Request failed, Try again later" :*/
fileprivate let userColorfulStr:[UInt8] = [0xbf,0x88,0x9c,0x98,0x88,0x9e,0x99,0xcd,0x8b,0x8c,0x84,0x81,0x88,0x89,0xc1,0xcd,0xb9,0x9f,0x94,0xcd,0x8c,0x8a,0x8c,0x84,0x83,0xcd,0x81,0x8c,0x99,0x88,0x9f]

private func liteDominant(edition num: UInt8) -> UInt8 {
    return num ^ 237
}

/*: "json error" :*/
fileprivate let notiLevelBranchTitle:String = "json elet willing down once"
fileprivate let main_concentrationMessage:[Character] = ["r","r","o","r"]

/*: "platform=iphone&version= :*/
fileprivate let userViewPath:[Character] = ["p","l","a","t","f","o","r","m","=","i","p","h"]
fileprivate let user_contractIdent:String = "mouth cut you television combinedone&ve"

/*: &packageId= :*/
fileprivate let showBeanResentUrl:String = "prove income exclusive role below&packa"
fileprivate let app_floorPatentMessage:String = "geId=giving capable"

/*: &bundleId= :*/
fileprivate let notiPrivacyKey:String = "&bundleIbar instruction people data template"
fileprivate let userFiscalTemporaryUrl:String = "d=negative po"

/*: &lang= :*/
fileprivate let show_wantUrl:String = "&lang=low slide window sink"

/*: "/route/slowindex" :*/
fileprivate let notiAlsoFormat:[Character] = ["/","r","o","u","t","e","/","s"]
fileprivate let mainSiblingKey:[Character] = ["l","o","w"]
fileprivate let dream_contrastFormat:[Character] = ["i","n","d","e","x"]

/*: "/route/index" :*/
fileprivate let userPictureStr:String = "apply asset/r"
fileprivate let app_assetStr:[Character] = ["o","u","t","e","/","i","n","d","e","x"]

/*: "?path= :*/
fileprivate let dreamAirMsg:String = "?path=assert reader remark crop human"

/*: "&GJNonceStr= :*/
fileprivate let show_tunKey:String = "&GJNoafter have branch"
fileprivate let showPlotPath:String = "nceStr=full let atm pattern"

/*: "&params= :*/
fileprivate let showSoundFormat:String = "vine hidden club judge&params"
fileprivate let dream_viewFormat:[Character] = ["="]

/*: "token" :*/
fileprivate let notiComparePunishText:String = "tokrating"

/*: "请求成功 :*/
fileprivate let k_herId:String = "请求成功"

/*: "AAAAAAAA" :*/
fileprivate let notiMatchHemSucceedTitle:String = "relationrelationrelationrelationrelationrelationrelationrelation"

/*: "下载完成 -  :*/
fileprivate let mainCurId:String = "下载\u{5b8c}成 - "

/*: "/Documents/" :*/
fileprivate let main_adviceKey:String = "/Documevent upon retain scan going"

/*: ".mp4" :*/
fileprivate let show_phaseMsg:String = "republic from score dit hi.mp4"

/*: "移动地址: :*/
fileprivate let app_civilPath:[Character] = ["移","动","地","址",":"]

/*: "后台任务下载回来" :*/
fileprivate let m_compriseEhUrl:String = "后\u{53f0}\u{4efb}务下载回来"

/*: "下载成功 : :*/
fileprivate let userSuitePath:[Character] = ["下","载","成","功"," ",":"]

/*: "Net Error" :*/
fileprivate let mainPrepareValue:[Character] = ["N","e","t"," ","E","r","r","o","r"]

/*: "下载失败 : :*/
fileprivate let m_beyondStr:String = "下\u{8f7d}失败 :"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  FindDayBenderTaskDelegate.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/1/28.
//

//: import Alamofire
import Alamofire
//: import CoreMedia
import CoreMedia
//: import UIKit
import UIKit

// 默认请求超时时间
//: let REQUEST_TIMEOUT_INTERVAL = 10.0
let userViewName = 10.0

//: let userAgent = "\(AppName)/\(AppVersion) (\(AppBundle); build:\(AppBuildNumber); iOS \(UIDevice.current.systemVersion); \(UIDevice.modelName))"
let dream_displayStr = "\(noti_detailStr)/\(userAssetManagerPath) (\(mainSelectIdent)" + (String(dream_fingerStr) + String(kDayValue)) + "\(showReasonData)" + (String(appConPath.prefix(6))) + "\(UIDevice.current.systemVersion); \(UIDevice.modelName))"

//: typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: TalkingErrorResponse?) -> Void
typealias FinishBlock = (_ succeed: Bool, _ result: Any?, _ errorModel: FairlyAdaptErrorResponse?) -> Void

//: let GJ = TalkingRequestTool.init()
let appDepthCornerContent = FindDayBenderTaskDelegate()

//: @objc class TalkingRequestTool: NSObject, URLSessionDownloadDelegate {
@objc class FindDayBenderTaskDelegate: NSObject, URLSessionDownloadDelegate {
    //: public func startRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func wrinkle(model: DoingicialReactiveCompatible, completion: @escaping FinishBlock) {
        //: if model.paramsContainBinaryObject() {
        if model.theCivic() {
            //: self.startUploadDataRequest(model: model, completion: completion)
            self.allCompletion(model: model, completion: completion)
            //: } else {
        } else {
            //: self.startNormalRequest(model: model, completion: completion)
            self.information(model: model, completion: completion)
        }
    }

    //: public func startUploadDataRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func allCompletion(model: DoingicialReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.vociferator(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.straight(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: model.params, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    remoteDown(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.NetError.rawValue, errorMsg: dream_micData))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        iconThemeBringBackPlace(showMsg: dream_micData)
                    }

                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.upload(multipartFormData: { (multipartFormData) in
            AF.upload(multipartFormData: { multipartFormData in
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: if value is Data {
                    if value is Data {
                        //: if key == "audio" {
                        if key == (String(mDoingceName)) {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.getCurrentTimeStamp())+\(PackageID).wav", mimeType: "audio/wav")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(NSDate.lieu())+\(dreamStreamKey)" + (String(noti_clearlyStr.prefix(4))), mimeType: (String(showDrownPath) + String(notiFrameEonValue.prefix(4))))
                            //: } else {
                        } else {
                            //: multipartFormData.append(value as! Data, withName: key, fileName: "\(key).jpg", mimeType: "image/jpeg")
                            multipartFormData.append(value as! Data, withName: key, fileName: "\(key)" + (String(userAttributeKey)), mimeType: (String(k_mobileId.prefix(9)) + String(mBurnMessage)))
                        }
                        //: } else {
                    } else {
                        //: let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        let data_ = "\(value)".data(using: String.Encoding.utf8)!
                        //: multipartFormData.append(data_, withName: key)
                        multipartFormData.append(data_, withName: key)
                    }
                }
                //: }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
            }, to: serverUrl, method: .post, headers: headers).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    remoteDown(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.NetError.rawValue, errorMsg: dream_micData))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        iconThemeBringBackPlace(showMsg: dream_micData)
                    }

                    //: break
                }
            }
        }
    }

    //: public func startNormalRequest(model: TalkingRequestModel, completion: @escaping FinishBlock) {
    public func information(model: DoingicialReactiveCompatible, completion: @escaping FinishBlock) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.vociferator(model: model)
        //: let headers = self.getRequestHeader(model: model)
        let headers = self.straight(model: model)

        //: if model.requestType == .GET {
        if model.requestType == .GET {
            //: AF.request(serverUrl, method: .get, parameters: (Encryption_Request ? nil : model.params), headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .get, parameters: appCornerObjectGiftName ? nil : model.params, headers: headers, requestModifier: { $0.timeoutInterval = userViewName }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    remoteDown(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.NetError.rawValue, errorMsg: dream_micData))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        iconThemeBringBackPlace(showMsg: dream_micData)
                    }
                    //: break
                }
            }
            //: } else {
        } else {
            //: AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = REQUEST_TIMEOUT_INTERVAL }).responseData { [self] responseData in
            AF.request(serverUrl, method: .post, parameters: model.params, headers: headers, requestModifier: { $0.timeoutInterval = userViewName }).responseData { [self] responseData in
                //: switch responseData.result {
                switch responseData.result {
                //: case .success:
                case .success:
                    //: func__requestSucess(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                    remoteDown(model: model, response: responseData.response!, responseData: responseData.data!, completion: completion)
                //: break
                //: case .failure:
                case .failure:
                    //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: kNetErrorMsg))
                    completion(false, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.NetError.rawValue, errorMsg: dream_micData))
                    //: if model.showErrorStatusBar {
                    if model.showErrorStatusBar {
                        //: func__showStatusBarErrorMsg(showMsg: kNetErrorMsg)
                        iconThemeBringBackPlace(showMsg: dream_micData)
                    }

                    //: break
                }
            }
        }
    }

    //: func func__requestSucess(model: TalkingRequestModel, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
    func remoteDown(model: DoingicialReactiveCompatible, response: HTTPURLResponse, responseData: Data, completion: @escaping FinishBlock) {
        //: let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        let httpCookies = HTTPCookie.cookies(withResponseHeaderFields: response.allHeaderFields as! [String: String], for: response.url!)
        //: if httpCookies.count > 0 {
        if httpCookies.count > 0 {
            //: TalkingRequestAddrTool.share.func__updateTalkingCookies(cookies: httpCookies)
            BubbleVisitorReactiveCompatible.share.modifyDoingce(cookies: httpCookies)
        }

        //: var responseJson = String(data: responseData, encoding: .utf8)
        var responseJson = String(data: responseData, encoding: .utf8)

        //: if Encryption_Request, model.requestServer.contains("://app.") {
        if appCornerObjectGiftName, model.requestServer.contains((String(mImpressionOutputPath.suffix(7)))) {
            //: responseJson = responseJson?.aes256Decrypt(key: TalkingRequestAddrTool.share.encryKeyStr)
            responseJson = responseJson?.languageKey(key: BubbleVisitorReactiveCompatible.share.encryKeyStr)
        }

        //: responseJson = responseJson?.replacingOccurrences(of: "\"data\":null", with: "\"data\":{}")
        responseJson = responseJson?.replacingOccurrences(of: "\"" + (String(kLivingGlobalId)) + "\"" + (String(showHideBoyMsg.prefix(5))), with: "" + "\"" + (String(kLivingGlobalId)) + "\"" + ":{}")
        //: if !Encryption_Request {
        if !appCornerObjectGiftName {
            //: responseJson = responseJson?.replacingOccurrences(of: ":null", with: ":\"\"")
            responseJson = responseJson?.replacingOccurrences(of: (String(showHideBoyMsg.prefix(5))), with: ":" + "\"" + "\"")
        }
        //: if let responseModel = JSONDeserializer<TalkingBaseResponse>.deserializeFrom(json: responseJson) {
        if let responseModel = JSONDeserializer<DetectModelType>.deserializeFrom(json: responseJson) {
            //: if responseModel.errno == RequestResultCode.Normal.rawValue {
            if responseModel.errno == GatherStrideable.Normal.rawValue {
                //: completion(true, responseModel.data, nil)
                completion(true, responseModel.data, nil)
                //: } else {
            } else {
                //: completion(false, responseModel.data, TalkingErrorResponse.init(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                completion(false, responseModel.data, FairlyAdaptErrorResponse(errorCode: responseModel.errno, errorMsg: responseModel.msg ?? ""))
                //: if model.showErrorStatusBar {
                if model.showErrorStatusBar {
                    //: func__showStatusBarErrorMsg(showMsg: responseModel.msg ?? "Request failed, Try again later".localized)
                    iconThemeBringBackPlace(showMsg: responseModel.msg ?? String(bytes: userColorfulStr.map{liteDominant(edition: $0)}, encoding: .utf8)!.localized)
                }
                //: switch responseModel.errno {
                switch responseModel.errno {
                //: case RequestResultCode.NeedReLogin.rawValue:
                case GatherStrideable.NeedReLogin.rawValue:
                    //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION, object: nil, userInfo: nil)
                    NotificationCenter.default.post(name: appVoiceUnderText, object: nil, userInfo: nil)
                //: default:
                default:
                    //: break
                    break
                }
            }
            //: } else {
        } else {
            //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "json error".localized))
            completion(false, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.NetError.rawValue, errorMsg: (String(notiLevelBranchTitle.prefix(6)) + String(main_concentrationMessage)).localized))
        }
    }

    //: func buildServerUrl(model: TalkingRequestModel) -> String {
    func vociferator(model: DoingicialReactiveCompatible) -> String {
        //: var serverUrl: String = model.requestServer
        var serverUrl: String = model.requestServer

        //: var otherParams = "platform=iphone&version=\(AppNetVersion)&packageId=\(PackageID)&bundleId=\(AppBundle)&lang=\(TalkingRequestAddrTool.share.interfaceLang)"
        var otherParams = (String(userViewPath) + String(user_contractIdent.suffix(6)) + "rsion=") + "\(user_dailyName)" + (String(showBeanResentUrl.suffix(6)) + String(app_floorPatentMessage.prefix(5))) + "\(dreamStreamKey)" + (String(notiPrivacyKey.prefix(8)) + String(userFiscalTemporaryUrl.prefix(2))) + "\(mainSelectIdent)" + (String(show_wantUrl.prefix(6))) + "\(BubbleVisitorReactiveCompatible.share.interfaceLang)"
        //: if Encryption_Request, serverUrl.contains("://app.") {
        if appCornerObjectGiftName, serverUrl.contains((String(mImpressionOutputPath.suffix(7)))) {
            //: let allowCharSet: CharacterSet = CharacterSet.init(charactersIn: "?!@#$^&%*+,:;='\"`<>()[]{}/\\| ").inverted
            let allowCharSet = CharacterSet(charactersIn: "?!@#$^&%*+,:;='" + "\"" + "`<>()[]{}/\\| ").inverted
            //: if TalkingRequestAddrTool.share.slowPathArr.contains(model.requestPath) {
            if BubbleVisitorReactiveCompatible.share.slowPathArr.contains(model.requestPath) {
                //: serverUrl.append("/route/slowindex")
                serverUrl.append((String(notiAlsoFormat) + String(mainSiblingKey) + String(dream_contrastFormat)))
                //: } else {
            } else {
                //: serverUrl.append("/route/index") // 如果使用IP需要加上app/
                serverUrl.append((String(userPictureStr.suffix(2)) + String(app_assetStr))) // 如果使用IP需要加上app/
            }

            //: let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            let pathStr = model.requestPath.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
            //: serverUrl.append("?path=\(pathStr)")
            serverUrl.append((String(dreamAirMsg.prefix(6))) + "\(pathStr)")
            //: serverUrl.append("&GJNonceStr=\(TalkingRequestAddrTool.share.randomStr)")
            serverUrl.append((String(show_tunKey.prefix(5)) + String(showPlotPath.prefix(7))) + "\(BubbleVisitorReactiveCompatible.share.randomStr)")

            //: if model.requestType == .GET {
            if model.requestType == .GET {
                //: for (key, value) in model.params {
                for (key, value) in model.params {
                    //: otherParams.append("&\(key)=\(value)")
                    otherParams.append("&\(key)=\(value)")
                }
            }
            //: if let encryStr = otherParams.aes256Encrypt(key: TalkingRequestAddrTool.share.encryKeyStr) {
            if let encryStr = otherParams.positionToo(key: BubbleVisitorReactiveCompatible.share.encryKeyStr) {
                //: let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                let paramsStr = encryStr.addingPercentEncoding(withAllowedCharacters: allowCharSet)!
                //: serverUrl.append("&params=\(paramsStr)")
                serverUrl.append((String(showSoundFormat.suffix(7)) + String(dream_viewFormat)) + "\(paramsStr)")
            }
            //: printLog(message: serverUrl)
            printLog(message: serverUrl)
            //: } else {
        } else {
            //: if !model.requestPath.isEmptyString {
            if !model.requestPath.isEmptyString {
                //: serverUrl.append("/\(model.requestPath)")
                serverUrl.append("/\(model.requestPath)")
            }
            //: serverUrl.append("?\(otherParams)")
            serverUrl.append("?\(otherParams)")
        }
        //: return serverUrl
        return serverUrl
    }

    /// 获取请求头参数
    /// - Parameter model: 请求模型
    /// - Returns: 请求头参数
    //: func getRequestHeader(model: TalkingRequestModel) -> HTTPHeaders {
    func straight(model: DoingicialReactiveCompatible) -> HTTPHeaders {
        //: var headers = [HTTPHeader.userAgent(userAgent)]
        var headers = [HTTPHeader.userAgent(dream_displayStr)]
        //: if model.addHeaderToken.isEmpty == false {
        if model.addHeaderToken.isEmpty == false {
            //: let token = HTTPHeader(name: "token", value: model.addHeaderToken)
            let token = HTTPHeader(name: (notiComparePunishText.replacingOccurrences(of: "rating", with: "en")), value: model.addHeaderToken)
            //: headers.append(token)
            headers.append(token)
        }
        //: return HTTPHeaders(headers)
        return HTTPHeaders(headers)
    }

    // MARK: - 系统后台下载

    //: func urlSessionBackgroundDownLoad(model: TalkingRequestModel) {
    func fullRequire(model: DoingicialReactiveCompatible) {
        //: let serverUrl = self.buildServerUrl(model: model)
        let serverUrl = self.vociferator(model: model)
        //: URLSession.shared.dataTask(with: URL.init(string: serverUrl)!) {(data, response, error) in
        URLSession.shared.dataTask(with: URL(string: serverUrl)!) { _, response, error in
            //: if error == nil {
            if error == nil {
                //: printLog(message: "请求成功\(String(describing: response))" )
                printLog(message: (k_herId.capitalized) + "\(String(describing: response))")
            }
        }

//        let configuration1 = URLSessionConfiguration.default    // default模式下系统会创建一个持久化的缓存并在用户的钥匙串中存储证书
//        let configuration2 = URLSessionConfiguration.ephemeral  // 所有内容的生命周期都与session相同，当session无效时，所有内容自动释放。

        // 后台下载
        //: let configuration3 = URLSessionConfiguration.background(withIdentifier: "AAAAAAAA")  // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        let configuration3 = URLSessionConfiguration.background(withIdentifier: (notiMatchHemSucceedTitle.replacingOccurrences(of: "relation", with: "A"))) // 后台甚至APP已经关闭的时候仍然在传输数据的会话
        //: configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        configuration3.httpAdditionalHeaders = ["AA": "AA", "BB": "BB"] // 与请求一起发送的附加头文件的字典
        //: configuration3.networkServiceType = .default
        configuration3.networkServiceType = .default // 网络服务的类型
        //: configuration3.allowsCellularAccess = true
        configuration3.allowsCellularAccess = true // 一个布尔值，用于确定是否应通过蜂窝网络进行连接
        //: configuration3.timeoutIntervalForRequest = 15
        configuration3.timeoutIntervalForRequest = 15
        //: configuration3.timeoutIntervalForResource = 15
        configuration3.timeoutIntervalForResource = 15
        //: let session = URLSession.init(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        let session = URLSession(configuration: configuration3, delegate: self, delegateQueue: OperationQueue.main)
        //: session.downloadTask(with: URL.init(string: serverUrl)!).resume()
        session.downloadTask(with: URL(string: serverUrl)!).resume()
    }

    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didFinishDownloadingTo location: URL) {
        //: printLog(message: "下载完成 - \(location)")
        printLog(message: (mainCurId) + "\(location)")
        //: let locationPath = location.path
        let locationPath = location.path
        //: let documnets = NSHomeDirectory() + "/Documents/" + NSDate.getCurrentTimeStamp() + ".mp4"
        let documnets = NSHomeDirectory() + (String(main_adviceKey.prefix(6)) + "ents/") + NSDate.lieu() + (String(show_phaseMsg.suffix(4)))
        //: printLog(message: "移动地址:\(documnets)")
        printLog(message: (String(app_civilPath)) + "\(documnets)")
        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: do {
        do {
            //: try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            try fileManager.moveItem(atPath: locationPath, toPath: documnets)
            //: } catch {
        } catch {
            // catch 中默认提供error信息, 当序列化不成功是, 返回error
            //: printLog(message: error)
            printLog(message: error)
        }
    }

    // 监听下载进度
    //: func urlSession(_ session: URLSession, downloadTask: URLSessionDownloadTask, didWriteData bytesWritten: Int64, totalBytesWritten: Int64, totalBytesExpectedToWrite: Int64) {
    func urlSession(_: URLSession, downloadTask _: URLSessionDownloadTask, didWriteData _: Int64, totalBytesWritten _: Int64, totalBytesExpectedToWrite _: Int64) {}

    //: func urlSessionDidFinishEvents(forBackgroundURLSession session: URLSession) {
    func urlSessionDidFinishEvents(forBackgroundURLSession _: URLSession) {
        //: printLog(message: "后台任务下载回来")
        printLog(message: (m_compriseEhUrl))
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: guard let backgroundHandle = AppDelegateHelper.shared.backgroundSessionCompletionHandler else { return }
            guard let backgroundHandle = ApprovalThen.shared.backgroundSessionCompletionHandler else { return }
            //: backgroundHandle()
            backgroundHandle()
        }
    }

    // MARK: - 动画下载

    //: public func startGiftZipRequest(giftUrl: String, path: String, completion: @escaping FinishBlock) {
    public func signAtFord(giftUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }
        //: let giftUrlStr = giftUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let giftUrlStr = giftUrl.replacingOccurrences(of: kKeyInsertValue.generalGame(), with: dreamVoiceMarginStr)
        //: AF.download(giftUrlStr, to: destination).downloadProgress { speed in
        AF.download(giftUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(userSuitePath)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.Normal.rawValue, errorMsg: (String(mainPrepareValue)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.NetError.rawValue, errorMsg: (String(mainPrepareValue)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (m_beyondStr) + "\(errMsg)")
            }
        }
    }

    //: public func startVoiceRequest(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
    public func phonation(voiceUrl: String, path: String, completion: @escaping FinishBlock) {
        //: let destination: DownloadRequest.Destination = { _, _ in
        let destination: DownloadRequest.Destination = { _, _ in
            //: let pathUrl = URL(fileURLWithPath: path)
            let pathUrl = URL(fileURLWithPath: path)
            //: return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
            return (pathUrl, [.removePreviousFile, .createIntermediateDirectories])
        }

        //: let voiceUrlStr = voiceUrl.replacingOccurrences(of: UrlDomin.urlAes256Decrypt(), with: ReplaceUrlDomain)
        let voiceUrlStr = voiceUrl.replacingOccurrences(of: kKeyInsertValue.generalGame(), with: dreamVoiceMarginStr)
        //: AF.download(voiceUrlStr, to: destination).downloadProgress { speed in
        AF.download(voiceUrlStr, to: destination).downloadProgress { _ in
        }
        //: .responseData { response in
        .responseData { response in
            //: switch response.result {
            switch response.result {
            //: case .success(let json):
            case let .success(json):
                //: printLog(message: "下载成功 :\(json)")
                printLog(message: (String(userSuitePath)) + "\(json)")
                //: completion(true, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.Normal.rawValue, errorMsg: "Net Error".localized))
                completion(true, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.Normal.rawValue, errorMsg: (String(mainPrepareValue)).localized))
            //: break
            //: case .failure(let errMsg):
            case let .failure(errMsg):
                //: completion(false, nil, TalkingErrorResponse.init(errorCode: RequestResultCode.NetError.rawValue, errorMsg: "Net Error".localized))
                completion(false, nil, FairlyAdaptErrorResponse(errorCode: GatherStrideable.NetError.rawValue, errorMsg: (String(mainPrepareValue)).localized))

                //: printLog(message: "下载失败 :\(errMsg)")
                printLog(message: (m_beyondStr) + "\(errMsg)")
            }
        }
    }
}
