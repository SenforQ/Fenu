
//: Declare String Begin

/*: "http://" :*/
fileprivate let userComponentUrl:String = "http://manual background"

/*: "https://" :*/
fileprivate let dream_indicatorAgainstUrl:String = "accuracy"
fileprivate let notiSoundNightMessage:[Character] = ["t","t","p","s",":","/","/"]

/*: "http:// :*/
fileprivate let dreamLabTitle:[Character] = ["h","t","t","p",":","/","/"]

/*: "serverSelctIndex =  :*/
fileprivate let showIndicatorMessage:String = "serspeed"
fileprivate let kMatchIdent:String = "tire strikelctInd"
fileprivate let main_arrayData:String = " "

/*: "serverUrlStr =  :*/
fileprivate let dreamExposureStr:String = "seraide"
fileprivate let appWhetherData:[Character] = ["e","r","U","r","l","S","t","r"," ","="," "]

/*: "app. :*/
fileprivate let show_invitationFormat:String = "app.both list"

/*: .com" :*/
fileprivate let user_threadStr:[Character] = [".","c","o","m"]

/*: "mf/chatRoom/sendGift" :*/
fileprivate let appBottomName:String = "mf/chatask day analyze wave"
fileprivate let notiBillMsg:[Character] = ["t","R","o","o"]
fileprivate let showPresentationId:[Character] = ["m","/","s","e","n","d","G","i","f","t"]

/*: "recharge/applePayNotify" :*/
fileprivate let mainComputeIdent:String = "rewhich"
fileprivate let kSheIdent:String = "/applskip leading month"
fileprivate let dream_failMessage:[Character] = ["e","P","a","y","N"]
fileprivate let user_glassName:[Character] = ["o","t","i","f","y"]

/*: "user/appleLogin" :*/
fileprivate let k_attachId:String = "without patternuser/"
fileprivate let app_partyData:[Character] = ["o","g","i","n"]

/*: "IM/getMyBlackList" :*/
fileprivate let kMonitorKey:String = "announcement globalIM/g"
fileprivate let dreamMaxStr:String = "will status society adjustment remainingkList"

/*: "mf/business/sendMsg" :*/
fileprivate let k_databaseName:String = "area image ready secret ratemf/bus"
fileprivate let main_contractIdent:[Character] = ["i","n","e","s","s","/","s","e","n","d","M","s","g"]

/*: "mf/business/sendGift" :*/
fileprivate let noti_badStr:[Character] = ["m","f","/","b","u"]
fileprivate let m_visualMsg:[Character] = ["s","i","n","e","s","s","/","s","e","n","d","G","i","f","t"]

/*: "mf/chatRoom/sendMsg" :*/
fileprivate let noti_advancedArriveValue:[Character] = ["m","f","/","c","h","a","t","R","o","o","m","/","s","e","n","d","M","s"]
fileprivate let noti_availableText:String = "when"

/*: "GJ21FB" :*/
fileprivate let dream_productionMessage:[Character] = ["G","J","2","1","F","B"]

/*: "完成了检测,请求状态正常" :*/
fileprivate let show_portraitPath:String = "完partner了"
fileprivate let user_gageMessage:[Character] = ["检","测",",","\u{8bf7}","求","\u{72b6}","态","正","\u{5e38}"]

/*: "接口请求失败" :*/
fileprivate let showMeVerticalText:String = "接口report求失败"

/*: "完成了检测,需要更换server url" :*/
fileprivate let app_establishStr:[Character] = ["完","成","了","检","测",",","需","要","更","换","s","e","r","v","e","r"]
fileprivate let show_roleName:String = "admit definition agent url"

/*: "HTTPProxy" :*/
fileprivate let kProcedureUrl:String = "HTTfeature"
fileprivate let userNoteId:String = "second"

/*: "__SCOPED__" :*/
fileprivate let appIdentificationData:String = "__SCOPage price extent effective"

/*: "tap" :*/
fileprivate let dream_wifeFormat:String = "tafinger"

/*: "tun" :*/
fileprivate let notiConversionData:[Character] = ["t","u","n"]

/*: "ipsec" :*/
fileprivate let show_applyIdent:String = "ipsemedal"

/*: "ppp" :*/
fileprivate let appForwardStr:String = "memorymemorymemory"

/*: "func__updateTalkingCookies =  :*/
fileprivate let show_positionValue:[Character] = ["f","u","n","c","_","_","u","p","d","a","t","e","T","a","l","k","i","n","g","C"]
fileprivate let userBusinessUponKey:String = "scribe character accuracy road truthookies = "

/*: "https:// :*/
fileprivate let appQuicklyFormat:[Character] = ["h","t","t","p","s",":","/","/"]

/*: "func__deleteSameCookiesWithUrl =  :*/
fileprivate let user_coreUrl:[UInt8] = [0xeb,0xf8,0xe3,0xee,0xd2,0xd2,0xe9,0xe8,0xe1,0xe8,0xf9,0xe8,0xde,0xec,0xe0,0xe8,0xce,0xe2,0xe2,0xe6,0xe4,0xe8,0xfe,0xda,0xe4,0xf9,0xe5,0xd8,0xff,0xe1,0xad,0xb0,0xad]

private func piAllow(icon num: UInt8) -> UInt8 {
    return num ^ 141
}

/*: "UID" :*/
fileprivate let user_mediaText:String = "Uop"

/*: "PHPSESSID" :*/
fileprivate let m_generalIdent:String = "identity"
fileprivate let dream_dutyTitle:String = "PsegmentSSID"

/*: "func__saveAllCookies =  :*/
fileprivate let main_problemId:[Character] = ["f","u","n","c","_","_","s","a","v"]
fileprivate let main_weThanksFormat:String = "child"
fileprivate let show_colorfulIdent:[Character] = ["A","l","l","C","o","o","k","i","e","s"," ","="," "]

/*: "func__saveAllCookies  error" :*/
fileprivate let userGazeValue:String = "cell sort food netfunc__"
fileprivate let user_soilText:String = "llCookifill height row"
fileprivate let kRiseName:[Character] = ["e"]
fileprivate let k_ridName:[Character] = ["s"," "," ","e","r","r","o","r"]

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  BubbleVisitorReactiveCompatible.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/8/23.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: enum LangType: String {
enum StatusExpressionConvertible: String {
    //: case en = "en"
    case en
    //: case ar = "ar"
    case ar
    //: case es = "es"
    case es
    //: case pt = "pt"
    case pt
}

//: public class TalkingRequestAddrTool: NSObject {
public class BubbleVisitorReactiveCompatible: NSObject {
    //: @objc static public let share = TalkingRequestAddrTool()
    @objc public static let share = BubbleVisitorReactiveCompatible()
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: setUp()
        shipUpward()
    }

    //: private var timerDispose: Disposable?
    private var timerDispose: Disposable?

    //: var serverSelctIndex = 0
    var serverSelctIndex = 0
    //: var hasFinishCheck   = false
    var hasFinishCheck = false
    //: var hostCheckNum     = 0
    var hostCheckNum = 0

    //: var domainStr: String?
    var domainStr: String? // 用户登录的信息
    //: var _serverUrlStr: String = ""       //
    var _serverUrlStr: String = "" //

    //: @objc public var serverUrlStr: String {
    @objc public var serverUrlStr: String {
        //: get {
        get {
            //: return _serverUrlStr
            return _serverUrlStr
        }
        //: set {
        set {
            //: var newServerUrl: String = ""
            var newServerUrl: String = ""
            //: if newValue.hasPrefix("http://") || newValue.hasPrefix("https://") {
            if newValue.hasPrefix((String(userComponentUrl.prefix(7)))) || newValue.hasPrefix((dream_indicatorAgainstUrl.replacingOccurrences(of: "accuracy", with: "h") + String(notiSoundNightMessage))) {
                //: newServerUrl = newValue
                newServerUrl = newValue
                //: } else {
            } else {
                //: if Environment_Formal && !Encryption_Request {
                if notiLicenseServerFormat && !appCornerObjectGiftName {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(dreamLabTitle)) + "\(newValue)"
                    //: } else {
                } else {
                    //: newServerUrl = "http://\(newValue )"
                    newServerUrl = (String(dreamLabTitle)) + "\(newValue)"
                }
            }
            //: if NSURL.init(string: newServerUrl) == nil {
            if NSURL(string: newServerUrl) == nil {
                //: return
                return
            }
            //: let oldServerUrl: String = _serverUrlStr
            let oldServerUrl: String = _serverUrlStr

            //: if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
            if oldServerUrl.isEmptyString || oldServerUrl != newServerUrl {
                //: _serverUrlStr = newServerUrl
                _serverUrlStr = newServerUrl
                //: if oldServerUrl.count > 0 {
                if oldServerUrl.count > 0 {
                    //: let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: oldServerUrl)!)!
                    let oldCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: oldServerUrl)!)!
                    //: func__updateTalkingCookies(cookies: oldCookies)
                    modifyDoingce(cookies: oldCookies)
                }
            }
        }
    }

    //: func setUp() {
    func shipUpward() {
        //: serverSelctIndex = Defaults.integer(forKey: TalkingAddrServerUrlSelectIndexKey)
        serverSelctIndex = noti_foundData.integer(forKey: show_skipPath)
        //: printLog(message: "serverSelctIndex = \(serverSelctIndex)")
        printLog(message: (showIndicatorMessage.replacingOccurrences(of: "speed", with: "v") + "erSe" + String(kMatchIdent.suffix(6)) + "ex =" + main_arrayData.capitalized) + "\(serverSelctIndex)")
        //: let serverDomain = func__getInitialServerUrl()
        let serverDomain = yr()
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userComponentUrl.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if notiLicenseServerFormat && !appCornerObjectGiftName {
            //: serverPrefix = "http://"
            serverPrefix = (String(userComponentUrl.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
        //: printLog(message: "serverUrlStr = \(serverUrlStr)")
        printLog(message: (dreamExposureStr.replacingOccurrences(of: "aide", with: "v") + String(appWhetherData)) + "\(serverUrlStr)")
    }

    //: func func__getInitialServerUrl() -> String {
    func yr() -> String {
        //: var appServerUrl = ""
        var appServerUrl = ""
        //: appServerUrl = serverUrlArr[serverSelctIndex]
        appServerUrl = serverUrlArr[serverSelctIndex]
        //: return appServerUrl
        return appServerUrl
    }

    //: func func__saveNewTalkingServerUrl(serverStr: String) {
    func counterperson(serverStr: String) {
        //: serverUrlStr = serverStr
        serverUrlStr = serverStr
    }

    //: private lazy var serverUrlArr: Array = {
    private lazy var serverUrlArr: Array = //: return ["app.\(ReplaceUrlDomain).com", "app.\(ReplaceUrlDomain).com"]
        [(String(show_invitationFormat.prefix(4))) + "\(dreamVoiceMarginStr)" + (String(user_threadStr)), (String(show_invitationFormat.prefix(4))) + "\(dreamVoiceMarginStr)" + (String(user_threadStr))]
    //: }()

    //: lazy var slowPathArr: Array = {
    lazy var slowPathArr: Array = //: return ["mf/chatRoom/sendGift",
        [(String(appBottomName.prefix(6)) + String(notiBillMsg) + String(showPresentationId)),
         //: "recharge/applePayNotify",
         (mainComputeIdent.replacingOccurrences(of: "which", with: "ch") + "arge" + String(kSheIdent.prefix(5)) + String(dream_failMessage) + String(user_glassName)),
         //: "user/appleLogin"] + slowPathArr1
         (String(k_attachId.suffix(5)) + "appleL" + String(app_partyData))] + slowPathArr1
    //: }()

    //: private lazy var slowPathArr1 = {
    private lazy var slowPathArr1 = //: return ["IM/getMyBlackList",
        [(String(kMonitorKey.suffix(4)) + "etMyBlac" + String(dreamMaxStr.suffix(5))),
         //: "mf/business/sendMsg",
         (String(k_databaseName.suffix(6)) + String(main_contractIdent)),
         //: "mf/business/sendGift",
         (String(noti_badStr) + String(m_visualMsg)),
         //: "mf/chatRoom/sendMsg"]
         (String(noti_advancedArriveValue) + noti_availableText.replacingOccurrences(of: "when", with: "g"))]
    //: }()

    /// 获取接口语言
    //: lazy var interfaceLang: String = {
    lazy var interfaceLang: String = {
        //: let lang = CreditsThen.getSystemLangCode()
        let lang = CreditsThen.toppleForce()
        //: if ["en", "ar", "es", "pt"].contains(lang) {
        if ["en", "ar", "es", "pt"].contains(lang) {
            //: return lang
            return lang
        }
        //: return "en"
        return "en"
        //: }()
    }()

    //: lazy var randomStr: String = {
    lazy var randomStr: String = //: return String.randomString(length: 16)
        .invulnerability(length: 16)
    //: }()

    //: lazy var encryKeyStr: String = {
    lazy var encryKeyStr: String = {
        //: let sufStr = randomStr.suffix(6)
        let sufStr = randomStr.suffix(6)
        //: let reversedStr: String = String(sufStr.reversed())
        let reversedStr = String(sufStr.reversed())
        //: return "GJ21FB" + reversedStr
        return (String(dream_productionMessage)) + reversedStr
        //: }()
    }()

    //: deinit {
    deinit {
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }
}

//: extension TalkingRequestAddrTool {
extension BubbleVisitorReactiveCompatible {
    //: func checkServerUrl() {
    func arouse() {
        //: if AdministratorThen.share.networkStatus != .Unavailable {
        if AdministratorThen.share.networkStatus != .Unavailable {
            //: func_requestHost()
            statDay()
            //: } else {
        } else {
            //: func_starCodeTime()
            massive()
        }
    }

    //: func func_starCodeTime() {
    func massive() {
        //: if self.hasFinishCheck {
        if self.hasFinishCheck {
            //: return
            return
        }
        //: if timerDispose != nil {
        if timerDispose != nil {
            //: timerDispose?.dispose()
            timerDispose?.dispose()
        }
        //: timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
        timerDispose = Observable<Int>.interval(.seconds(3), scheduler: MainScheduler.instance)
            //: .subscribe(onNext: { [weak self] num in
            .subscribe(onNext: { [weak self] _ in
                //: guard let self = self else {return}
                guard let self = self else { return }
                //: self.func_requestHost()
                self.statDay()
                //: })
            })
    }

    //: func func_endCodeTime() {
    func everybody() {
        //: timerDispose?.dispose()
        timerDispose?.dispose()
    }

    //: func func_requestHost() {
    func statDay() {
        //: if AdministratorThen.share.networkStatus == .Unavailable {
        if AdministratorThen.share.networkStatus == .Unavailable {
            //: return
            return
        }
        //: func_endCodeTime()
        everybody()
        //: AppManagerRequest.func__initRequestHost { [weak self] succeed, result, errorModel in
        DrawReactiveCompatible.telecast { [weak self] succeed, _, _ in
            //: guard let self = self else {return}
            guard let self = self else { return }
            //: if succeed {
            if succeed {
                //: self.hasFinishCheck = true
                self.hasFinishCheck = true
                //: printLog(message: "完成了检测,请求状态正常")
                printLog(message: (show_portraitPath.replacingOccurrences(of: "partner", with: "成") + String(user_gageMessage)))
                //: } else {
            } else {
                //: printLog(message: "接口请求失败")
                printLog(message: (showMeVerticalText.replacingOccurrences(of: "report", with: "请")))
                //: if self.hasFinishCheck {return}
                if self.hasFinishCheck { return }
                //: self.hostCheckNum += 1
                self.hostCheckNum += 1
                //: if self.hostCheckNum < 3 {
                if self.hostCheckNum < 3 {
                    //: self.func_starCodeTime()
                    self.massive()
                    //: } else {
                } else {
                    //: printLog(message: "完成了检测,需要更换server url")
                    printLog(message: (String(app_establishStr) + String(show_roleName.suffix(4))))
                    //: self.func__changeHostUrl()
                    self.wood()
                }
            }
        }
    }

    //: func func__changeHostUrl() {
    func wood() {
        //: hostCheckNum = 0
        hostCheckNum = 0
        //: hasFinishCheck = true
        hasFinishCheck = true
        //: func_endCodeTime()
        everybody()

        //: serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        serverSelctIndex = (serverSelctIndex == 0) ? 1 : 0
        //: Defaults.set(serverSelctIndex, forKey: TalkingAddrServerUrlSelectIndexKey)
        noti_foundData.set(serverSelctIndex, forKey: show_skipPath)
        //: let serverDomain = serverUrlArr[serverSelctIndex]
        let serverDomain = serverUrlArr[serverSelctIndex]
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userComponentUrl.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if notiLicenseServerFormat, !appCornerObjectGiftName {
            //: serverPrefix = "http://"
            serverPrefix = (String(userComponentUrl.prefix(7)))
        }
        //: serverUrlStr = serverPrefix.appending(serverDomain)
        serverUrlStr = serverPrefix.appending(serverDomain)
    }
}

// MARK: - 判断用户设备是否开启VPN

//: extension TalkingRequestAddrTool {
public extension BubbleVisitorReactiveCompatible {
    /// 判断用户设备是否开启代理
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedProxy() -> Bool {
    class func scribeProxy() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let HTTPProxy = dict["HTTPProxy"] as? String else { return false }
        guard let HTTPProxy = dict[(kProcedureUrl.replacingOccurrences(of: "feature", with: "P") + "Prox" + userNoteId.replacingOccurrences(of: "second", with: "y"))] as? String else { return false }
        //: if (HTTPProxy.count > 0) {
        if HTTPProxy.count > 0 {
            //: return true
            return true
        }
        //: return false
        return false
    }

    /// 判断用户设备是否开启代理VPN
    /// - Returns: false: 未开启  true: 开启
    //: public class func isUsedVPN() -> Bool {
    class func toolRequest() -> Bool {
        //: guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        guard let proxy = CFNetworkCopySystemProxySettings()?.takeUnretainedValue() else { return false }
        //: guard let dict = proxy as? [String: Any] else { return false }
        guard let dict = proxy as? [String: Any] else { return false }
        //: guard let scopedDic = dict["__SCOPED__"] as? [String: Any] else { return false }
        guard let scopedDic = dict[(String(appIdentificationData.prefix(6)) + "ED__")] as? [String: Any] else { return false }
        //: for keyStr in scopedDic.keys {
        for keyStr in scopedDic.keys {
            //: if keyStr.contains("tap") || keyStr.contains("tun") || keyStr.contains("ipsec") || keyStr.contains("ppp"){
            if keyStr.contains((dream_wifeFormat.replacingOccurrences(of: "finger", with: "p"))) || keyStr.contains((String(notiConversionData))) || keyStr.contains((show_applyIdent.replacingOccurrences(of: "medal", with: "c"))) || keyStr.contains((appForwardStr.replacingOccurrences(of: "memory", with: "p"))) {
                //: return true
                return true
            }
        }
        //: return false
        return false
    }
}

//: extension TalkingRequestAddrTool {
extension BubbleVisitorReactiveCompatible {
    //: func func__updateTalkingCookies(cookies: Array<HTTPCookie>) {
    func modifyDoingce(cookies: [HTTPCookie]) {
        //: printLog(message: "func__updateTalkingCookies = \(cookies)")
        printLog(message: (String(show_positionValue) + String(userBusinessUponKey.suffix(9))) + "\(cookies)")
        //: var tempCookies: Array<HTTPCookie> = cookies
        var tempCookies: [HTTPCookie] = cookies
        //: if cookies.count == 0 {
        if cookies.count == 0 {
            //: tempCookies = func__getCookiesFromOldServerUrl()
            tempCookies = hostThread()
        }
        //: if tempCookies.count == 0 {
        if tempCookies.count == 0 {
            //: return
            return
        }
        //: var tempUrlArr: Array<URL> = []
        var tempUrlArr: [URL] = []

        //: if !self.serverUrlStr.isEmptyString {
        if !self.serverUrlStr.isEmptyString {
            //: tempUrlArr.append(URL.init(string: self.serverUrlStr)!)
            tempUrlArr.append(URL(string: self.serverUrlStr)!)
        }

        //: var urlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
        var urlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
        //: urlDomain = urlDomain.replacingOccurrences(of: "http://", with: "https://")
        urlDomain = urlDomain.replacingOccurrences(of: (String(userComponentUrl.prefix(7))), with: (dream_indicatorAgainstUrl.replacingOccurrences(of: "accuracy", with: "h") + String(notiSoundNightMessage)))
        //: if urlDomain.hasPrefix("https://") {
        if urlDomain.hasPrefix((dream_indicatorAgainstUrl.replacingOccurrences(of: "accuracy", with: "h") + String(notiSoundNightMessage))) {
            //: tempUrlArr.append(URL.init(string: urlDomain)!)
            tempUrlArr.append(URL(string: urlDomain)!)
            //: } else {
        } else {
            //: tempUrlArr.append(URL.init(string: "https://\(urlDomain)")!)
            tempUrlArr.append(URL(string: (String(appQuicklyFormat)) + "\(urlDomain)")!)
        }

        //: var addCookieArr: Array<HTTPCookie> = []
        var addCookieArr: [HTTPCookie] = []
        //: for cookies in tempCookies {
        for cookies in tempCookies {
            //: var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            var cookieInfo: [HTTPCookiePropertyKey: Any] = cookies.properties!
            //: for url in tempUrlArr {
            for url in tempUrlArr {
                //: cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                cookieInfo[HTTPCookiePropertyKey.domain] = url.host
                //: cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                cookieInfo.removeValue(forKey: HTTPCookiePropertyKey.discard)
                //: let newCookie = HTTPCookie.init(properties: cookieInfo)
                let newCookie = HTTPCookie(properties: cookieInfo)
                //: if newCookie != nil {
                if newCookie != nil {
                    //: addCookieArr.append(newCookie!)
                    addCookieArr.append(newCookie!)
                }
            }
        }
        //: for cookies in addCookieArr {
        for cookies in addCookieArr {
            //: HTTPCookieStorage.shared.setCookie(cookies)
            HTTPCookieStorage.shared.setCookie(cookies)
        }

        //: for url in tempUrlArr {
        for url in tempUrlArr {
            //: func__deleteSameCookiesWithUrl(url: url)
            pastDeadlineYield(url: url)
        }
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            WKWebsiteDataStore.default().fetchDataRecords(ofTypes: [WKWebsiteDataTypeCookies]) { _ in
            }
        }

        //: func__saveAllCookies()
        typical()
    }

    //: func func__deleteSameCookiesWithUrl(url: URL) {
    func pastDeadlineYield(url: URL) {
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var saveDic: Dictionary<String, String> = [:]
        var saveDic: [String: String] = [:]
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: if !cookies.name.isEmptyString {
            if !cookies.name.isEmptyString {
                //: let saveKey = cookies.name
                let saveKey = cookies.name
                //: if saveDic.keys.contains(saveKey) {
                if saveDic.keys.contains(saveKey) {
                    //: printLog(message: "func__deleteSameCookiesWithUrl = \(cookies.name)")
                    printLog(message: String(bytes: user_coreUrl.map{piAllow(icon: $0)}, encoding: .utf8)! + "\(cookies.name)")
                    //: HTTPCookieStorage.shared.deleteCookie(cookies)
                    HTTPCookieStorage.shared.deleteCookie(cookies)
                    //: } else {
                } else {
                    //: saveDic[saveKey] = "1"
                    saveDic[saveKey] = "1"
                }
            }
        }
    }

    //: func func__getCookiesFromOldServerUrl() -> Array<HTTPCookie> {
    func hostThread() -> [HTTPCookie] {
        //: var serverPrefix = "http://"
        var serverPrefix = (String(userComponentUrl.prefix(7)))
        //: if Environment_Formal && !Encryption_Request {
        if notiLicenseServerFormat, !appCornerObjectGiftName {
            //: serverPrefix = "https://"
            serverPrefix = (dream_indicatorAgainstUrl.replacingOccurrences(of: "accuracy", with: "h") + String(notiSoundNightMessage))
        }
        //: let fullDomain = serverPrefix.appending("app.\(ReplaceUrlDomain).com")
        let fullDomain = serverPrefix.appending((String(show_invitationFormat.prefix(4))) + "\(dreamVoiceMarginStr)" + (String(user_threadStr)))
        //: let oldCookies = HTTPCookieStorage.shared.cookies(for: URL.init(string: fullDomain)!)
        let oldCookies = HTTPCookieStorage.shared.cookies(for: URL(string: fullDomain)!)
        //: return oldCookies!
        return oldCookies!
    }

    //: @objc func cookieChanged(note: Notification) {
    @objc func pointOutElect(note _: Notification) {
        //: if AdministratorThen.share.loginUid.isEmptyString {
        if AdministratorThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: checkHtmlCookieIfNeedCurrentVersion()
            myBlade()
            //: } else {
        } else {
            //: checkHtmlCookieIfNeedEarlyVersion()
            nextCookie()
        }
    }

    //: func checkHtmlCookieIfNeedCurrentVersion() {
    func myBlade() {
        //: var htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain

        //: if !htmlDomain.isEmptyString {
        if !htmlDomain.isEmptyString {
            //: let url = URL.init(string: htmlDomain)
            let url = URL(string: htmlDomain)
            //: htmlDomain = url?.host ?? ""
            htmlDomain = url?.host ?? ""
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var appCookie: [String: HTTPCookie] = Dictionary()
            //: var h5Cookie: Dictionary<String, HTTPCookie> =  Dictionary()
            var h5Cookie: [String: HTTPCookie] = Dictionary()

            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr
                //: let temphtmlDomainStr = htmlDomain
                let temphtmlDomainStr = htmlDomain
                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (user_mediaText.replacingOccurrences(of: "op", with: "ID")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(user_mediaText.replacingOccurrences(of: "op", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))] = cookie
                    }

                    //: } else if temphtmlDomainStr.contains(cookie.domain) {
                } else if temphtmlDomainStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (user_mediaText.replacingOccurrences(of: "op", with: "ID")) {
                        //: h5Cookie["UID"] = cookie
                        h5Cookie[(user_mediaText.replacingOccurrences(of: "op", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE")) {
                        //: h5Cookie["PHPSESSID"] = cookie
                        h5Cookie[(m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))] = cookie
                    }
                }
                //: var cookieEqual = true
                var cookieEqual = true
                //: if appCookie.keys.count != h5Cookie.keys.count {
                if appCookie.keys.count != h5Cookie.keys.count {
                    //: cookieEqual = false
                    cookieEqual = false
                    //: } else {
                } else {
                    //: if appCookie.keys.contains("UID") && h5Cookie.keys.contains("UID") {
                    if appCookie.keys.contains((user_mediaText.replacingOccurrences(of: "op", with: "ID"))), h5Cookie.keys.contains((user_mediaText.replacingOccurrences(of: "op", with: "ID"))) {
                        //: let appUid: HTTPCookie = appCookie["UID"]!
                        let appUid: HTTPCookie = appCookie[(user_mediaText.replacingOccurrences(of: "op", with: "ID"))]!
                        //: let h5Uid: HTTPCookie = h5Cookie["UID"]!
                        let h5Uid: HTTPCookie = h5Cookie[(user_mediaText.replacingOccurrences(of: "op", with: "ID"))]!

                        //: if h5Uid.value != appUid.value {
                        if h5Uid.value != appUid.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                    //: if appCookie.keys.contains("PHPSESSID") && h5Cookie.keys.contains("PHPSESSID") {
                    if appCookie.keys.contains((m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))), h5Cookie.keys.contains((m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))) {
                        //: let appPHPSESSID: HTTPCookie = appCookie["PHPSESSID"]!
                        let appPHPSESSID: HTTPCookie = appCookie[(m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))]!
                        //: let h5PHPSESSID: HTTPCookie = h5Cookie["PHPSESSID"]!
                        let h5PHPSESSID: HTTPCookie = h5Cookie[(m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))]!
                        //: if appPHPSESSID.value != h5PHPSESSID.value {
                        if appPHPSESSID.value != h5PHPSESSID.value {
                            //: cookieEqual = false
                            cookieEqual = false
                        }
                    }
                }
                //: if !cookieEqual {
                if !cookieEqual {
                    //: for (key, _) in appCookie {
                    for (key, _) in appCookie {
                        //: let cookie: HTTPCookie = appCookie[key]!
                        let cookie: HTTPCookie = appCookie[key]!
                        //: var cookiePro: Dictionary<HTTPCookiePropertyKey, Any> = cookie.properties!
                        var cookiePro: [HTTPCookiePropertyKey: Any] = cookie.properties!
                        //: cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        cookiePro[HTTPCookiePropertyKey.domain] = htmlDomain
                        //: cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        cookiePro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                        //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: cookiePro)!)
                        HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: cookiePro)!)
                    }
                }
            }
        }
    }

    //: func checkHtmlCookieIfNeedEarlyVersion() {
    func nextCookie() {
        //: let htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
        let htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
        //: let url: URL = URL.init(string: htmlDomain)!
        let url = URL(string: htmlDomain)!
        //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies(for: url)!
        let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: url)!
        //: var hasUID = false
        var hasUID = false
        //: var hasPHPSSID = false
        var hasPHPSSID = false
        //: for cookie in cookieArr {
        for cookie in cookieArr {
            //: if cookie.name == "UID" && cookie.value == AdministratorThen.share.loginUserMode.userID {
            if cookie.name == (user_mediaText.replacingOccurrences(of: "op", with: "ID")), cookie.value == AdministratorThen.share.loginUserMode.userID {
                //: hasUID = true
                hasUID = true
            }
            //: if cookie.name == "PHPSESSID" {
            if cookie.name == (m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE")) {
                //: hasPHPSSID = true
                hasPHPSSID = true
            }
        }
        //: if !hasUID || !hasPHPSSID {
        if !hasUID || !hasPHPSSID {
            //: let cookieArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookieArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: var appCookie: Dictionary<String, Any> =  Dictionary()
            var appCookie: [String: Any] = Dictionary()
            //: for cookie in cookieArr {
            for cookie in cookieArr {
                //: let tempServerUrlStr = self.serverUrlStr
                let tempServerUrlStr = self.serverUrlStr

                //: if tempServerUrlStr.contains(cookie.domain) {
                if tempServerUrlStr.contains(cookie.domain) {
                    //: if cookie.name.uppercased() == "UID" {
                    if cookie.name.uppercased() == (user_mediaText.replacingOccurrences(of: "op", with: "ID")) {
                        //: appCookie["UID"] = cookie
                        appCookie[(user_mediaText.replacingOccurrences(of: "op", with: "ID"))] = cookie
                    }
                    //: if cookie.name.uppercased() == "PHPSESSID" {
                    if cookie.name.uppercased() == (m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE")) {
                        //: appCookie["PHPSESSID"] = cookie
                        appCookie[(m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))] = cookie
                    }
                }
                //: if !hasUID && appCookie.keys.contains("UID") {
                if !hasUID, appCookie.keys.contains((user_mediaText.replacingOccurrences(of: "op", with: "ID"))) {
                    //: let uidCookie: HTTPCookie = appCookie["UID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(user_mediaText.replacingOccurrences(of: "op", with: "ID"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
                //: if !hasPHPSSID && appCookie.keys.contains("PHPSESSID") {
                if !hasPHPSSID, appCookie.keys.contains((m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))) {
                    //: let uidCookie: HTTPCookie = appCookie["PHPSESSID"] as! HTTPCookie
                    let uidCookie: HTTPCookie = appCookie[(m_generalIdent.replacingOccurrences(of: "identity", with: "PH") + dream_dutyTitle.replacingOccurrences(of: "segment", with: "SE"))] as! HTTPCookie
                    //: var uidPro: Dictionary<HTTPCookiePropertyKey, Any> = uidCookie.properties!
                    var uidPro: [HTTPCookiePropertyKey: Any] = uidCookie.properties!
                    //: uidPro[HTTPCookiePropertyKey.domain] = url.host
                    uidPro[HTTPCookiePropertyKey.domain] = url.host
                    //: uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    uidPro.removeValue(forKey: HTTPCookiePropertyKey.discard)
                    //: HTTPCookieStorage.shared.setCookie(HTTPCookie.init(properties: uidPro)!)
                    HTTPCookieStorage.shared.setCookie(HTTPCookie(properties: uidPro)!)
                }
            }
        }
    }

    //: func func__saveAllCookies() {
    func typical() {
        //: do {
        do {
            //: let cookies: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
            let cookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
            //: printLog(message: "func__saveAllCookies = \(cookies)")
            printLog(message: (String(main_problemId) + main_weThanksFormat.replacingOccurrences(of: "child", with: "e") + String(show_colorfulIdent)) + "\(cookies)")
            //: let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            let cookieData = try NSKeyedArchiver.archivedData(withRootObject: cookies)
            //: Defaults.set(cookieData, forKey: TalkingUserCookiesKey)
            noti_foundData.set(cookieData, forKey: kUserName)
            //: } catch _ {
        } catch _ {
            //: printLog(message: "func__saveAllCookies  error")
            printLog(message: (String(userGazeValue.suffix(6)) + "saveA" + String(user_soilText.prefix(7)) + String(kRiseName) + String(k_ridName)))
        }
    }

    //: func func__setAppCookies() {
    func inactive() {
        //: let data = Defaults.object(forKey: TalkingUserCookiesKey)
        let data = noti_foundData.object(forKey: kUserName)
        //: if data != nil {
        if data != nil {
            //: let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            let data2: NSArray = NSKeyedUnarchiver.unarchiveObject(with: data as! Data) as! NSArray
            //: for cookie in data2 {
            for cookie in data2 {
                //: HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
                HTTPCookieStorage.shared.setCookie(cookie as! HTTPCookie)
            }
        }
    }

    //: func func__removeAllCookies() {
    func countAtm() {
        //: Defaults.removeObject(forKey: TalkingUserCookiesKey)
        noti_foundData.removeObject(forKey: kUserName)
        //: let cookiesArr: Array<HTTPCookie> = HTTPCookieStorage.shared.cookies!
        let cookiesArr: [HTTPCookie] = HTTPCookieStorage.shared.cookies!
        //: for cookies in cookiesArr {
        for cookies in cookiesArr {
            //: printLog(message: "🍪 删除\(cookies.name)")
            printLog(message: "🍪 删除\(cookies.name)")
            //: HTTPCookieStorage.shared.deleteCookie(cookies)
            HTTPCookieStorage.shared.deleteCookie(cookies)
        }
    }
}
