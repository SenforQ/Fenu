
//: Declare String Begin

/*: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION" :*/
fileprivate let main_observeId:[UInt8] = [0x4e,0x4f,0x49,0x54,0x41,0x43,0x49,0x46,0x49,0x54,0x4f,0x4e,0x5f,0x44,0x45,0x45,0x43,0x43,0x55,0x53,0x5f,0x59,0x41,0x50,0x5f,0x50,0x41,0x49,0x5f,0x45,0x4c,0x50,0x50,0x41]

/*: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION" :*/
fileprivate let mainMeritTitle:[UInt8] = [0xe4,0xf5,0xf5,0xe9,0xe0,0xfa,0xec,0xe4,0xf5,0xfa,0xf6,0xf0,0xe7,0xf6,0xe6,0xf7,0xec,0xe7,0xe0,0xfa,0xf6,0xf0,0xe6,0xe6,0xe0,0xe0,0xe1,0xfa,0xeb,0xea,0xf1,0xec,0xe3,0xec,0xe6,0xe4,0xf1,0xec,0xea,0xeb]

private func permissionProfessional(schedule num: UInt8) -> UInt8 {
    return num ^ 165
}

/*: "mf/recharge/createApplePay" :*/
fileprivate let dreamRiseTitle:String = "movie insertmf/re"
fileprivate let dreamBelowId:String = "ge/crechange decrease date supposed"
fileprivate let kStripValue:String = "file around personalpplePay"

/*: "productId" :*/
fileprivate let m_rearUrl:String = "hangr"
fileprivate let dreamWageUrl:[Character] = ["o","d","u","c","t","I","d"]

/*: "source" :*/
fileprivate let kPartyName:[Character] = ["s"]
fileprivate let k_yaName:[Character] = ["o","u","r","c","e"]

/*: "IAP充值创建订单失败: :*/
fileprivate let kSeriesEnterText:String = "IAP\u{5145}值创"

/*: "orderNum" :*/
fileprivate let appSpendName:String = "rounding overlook leading projectorderN"
fileprivate let show_patentData:[Character] = ["u","m"]

/*: "mf/recharge/applePayNotify" :*/
fileprivate let appAbleStr:[Character] = ["m","f","/","r","e","c","h","a"]
fileprivate let mainSeriesPath:String = "rocket aspect represent stat basicrge/"
fileprivate let showHugeKey:String = "ayNotifywhich director comprehensive"

/*: "IAP充值校验失败: :*/
fileprivate let k_numbName:[Character] = ["I","A","P","充","值"]
fileprivate let notiCareMessage:[Character] = ["\u{6821}","\u{9a8c}","失","败",":"]

/*: "transactionId" :*/
fileprivate let app_mobileFrameStr:[Character] = ["t","r","a","n","s","a","c","t"]
fileprivate let appBotPath:String = "serious opposition thationId"

/*: "reportMoney" :*/
fileprivate let notiImpressionId:String = "pending"
fileprivate let appTrafficStr:String = "portion doc minimaleportM"

/*: "mf/AutoSub/AppleCreateOrder" :*/
fileprivate let m_selectedFormat:String = "mf/Auhowever state app eff toward"
fileprivate let showOperationExactlyStr:String = "Applhello spring version purchase lock"
fileprivate let mainThroughDomainViewPath:String = "road plat pageteOrde"
fileprivate let showFeedbackValue:String = "date"

/*: "IAP订阅创建订单失败: :*/
fileprivate let mainSocialValue:[Character] = ["I","A","P","订","阅","创","建","订","单","失","败",":"]

/*: "orderId" :*/
fileprivate let mDeadlineName:[UInt8] = [0xcb,0xd6,0xc0,0xc1,0xd6,0xed,0xc0]

private func banNurse(quit num: UInt8) -> UInt8 {
    return num ^ 164
}

/*: "mf/AutoSub/ApplePaySuccess" :*/
fileprivate let notiWhenMidReportName:[Character] = ["m","f","/","A","u","t","o","S","u","b"]
fileprivate let notiComprehensiveIdent:String = "/Applplat no optimistic draft duty"
fileprivate let mPoseName:String = "Successmeet calculate"

/*: "IAP订阅校验失败: :*/
fileprivate let user_sinceId:String = "take project detailedIAP订阅"
fileprivate let userCaptainQualityBagValue:String = "校验失败:"

/*: "App" :*/
fileprivate let kOpportunityTitle:String = "now fore outcome openly magnitudeApp"

/*: "OrderTransactionInfo_ :*/
fileprivate let userAreaValue:String = "equally spec reminder week contrastOrde"
fileprivate let noti_customerName:String = "apriorityi"
fileprivate let main_gameKey:String = "fluent createfo_"

/*: "OrderTransactionInfo_Subscribe_ :*/
fileprivate let k_closedFormat:[UInt8] = [0x18,0x25,0x33,0x32,0x25,0x3,0x25,0x36,0x39,0x24,0x36,0x34,0x23,0x3e,0x38,0x39,0x1e,0x39,0x31,0x38,0x8,0x4,0x22,0x35,0x24,0x34,0x25,0x3e,0x35,0x32,0x8]

private func informationMonthCrush(regional num: UInt8) -> UInt8 {
    return num ^ 87
}

/*: "verifyData" :*/
fileprivate let app_kitFormatName:[UInt8] = [0x4b,0x58,0x4f,0x54,0x5b,0x44,0x79,0x5c,0x49,0x5c]

private func controlSkin(screen num: UInt8) -> UInt8 {
    return num ^ 61
}

/*: "IAP查询内购商品失败, productId: :*/
fileprivate let m_agreeName:String = "expected tail netIAP查询"
fileprivate let main_overlookMsg:[Character] = ["内","购","商","品","失","败",","," ","p","r","o","d","u","c","t","I","d",":"]

/*: "IAP查询内购商品失败, products.count为0." :*/
fileprivate let user_evaluatePath:String = "IAP查询内购his tick empty"
fileprivate let dream_securityValue:[Character] = ["商","\u{54c1}","失","败",","," ","p","r"]
fileprivate let notiActivityMessage:[Character] = ["o","d","u","c","t","s",".","c","o","u","n","t","为","0","."]

/*: "IAP查询内购商品失败, Error: :*/
fileprivate let userShallName:[Character] = ["I","A","P","查","询","内","\u{8d2d}","商","品","失","败",","," ","E","r","r","o","r",":"]

/*: "IAP-purchased: :*/
fileprivate let mainArmGatherFormat:[Character] = ["I","A","P","-","p","u","r","c","h","a","s","e","d"]
fileprivate let k_implementPath:String = ":"

/*: , 订单号: :*/
fileprivate let dreamStrengthIdent:String = ", committee单号"
fileprivate let appParaTitle:[Character] = [":"]

/*: , 商品Id: :*/
fileprivate let notiParentValue:[Character] = [","," ","\u{5546}","品","I","d",":"]

/*: , 苹果transId: :*/
fileprivate let k_fourIdent:[Character] = [","," ","苹","果","t","r","a","n","s","I"]
fileprivate let show_trustUrl:String = "d:wed our ship"

/*: "IAP-failed: :*/
fileprivate let dream_slowName:[Character] = ["I","A","P"]
fileprivate let mLeaseAlreadyBarIdent:String = "-failed:magnitude ballot"

/*: "IAP-deferred: :*/
fileprivate let m_seriesName:String = "IAP-defecurrent would patent"
fileprivate let mainMediaStr:[Character] = ["r","r","e","d",":"]

/*: "IAP未知交易类型: :*/
fileprivate let dreamTextSingleIdent:[Character] = ["I","A","P","\u{672a}","知","交","易","类","型",":"]

/*: " 未知的交易类型" :*/
fileprivate let user_renderFormat:String = " 未知的"

/*: "IAP获取本地收据数据失败, transactionId: :*/
fileprivate let k_permitMsg:[Character] = ["I","A","P","获","取"]
fileprivate let appStrongId:[Character] = ["本","地","收","据","\u{6570}","\u{636e}","失","败",","," ","t","r","a","n","s","a"]
fileprivate let user_habitAlwaysIdentifyMessage:String = "ctionId:carry through"

/*: , orderId: :*/
fileprivate let show_usedUrl:[Character] = [","," ","o","r","d","e","r","I","d"]
fileprivate let app_reasonUrl:String = "budget"

/*: , payType: :*/
fileprivate let kZoneHoneyStr:String = "date week, payTyp"
fileprivate let noti_hypothesisPath:String = "e:strong primary average"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  InjuryPubTransactionObserver.swift
//  AbroadTalking
//
//  Created by young on 2022/9/2.
//

//: import StoreKit
import StoreKit
//: import SwiftyJSON
import SwiftyJSON
//: import UIKit
import UIKit

// 苹果支付成功通知
//: public let APPLE_IAP_PAY_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_PAY_SUCCEED_NOTIFICATION")
public let kLikeValue = NSNotification.Name(rawValue: String(bytes: main_observeId.reversed(), encoding: .utf8)!)
// 苹果订阅成功通知
//: public let APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION = NSNotification.Name(rawValue: "APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION")
public let app_positionFeatureWithoutFormat = NSNotification.Name(rawValue: String(bytes: mainMeritTitle.map{permissionProfessional(schedule: $0)}, encoding: .utf8)!)
// 最大失败重试次数
//: let APPLE_IAP_MAX_RETRY_COUNT = 9
let mRouteUrl = 9

/// 支付类型
//: enum ApplePayType {
enum AsideType {
    //: case Pay
    case Pay // 支付
    //: case Subscribe
    case Subscribe // 订阅
}

/// 支付状态
//: enum AppleIAPStatus: String {
enum LogQuickLookable: String {
    //: case unknow            = "未知类型"
    case unknow = "未知类型"
    //: case createOrderFail   = "创建订单失败"
    case createOrderFail = "创建订单失败"
    //: case notArrow          = "设备不允许"
    case notArrow = "设备不允许"
    //: case noProductId       = "缺少产品Id"
    case noProductId = "缺少产品Id"
    //: case failed            = "交易失败/取消"
    case failed = "交易失败/取消"
    //: case restored          = "已购买过该商品"
    case restored = "已购买过该商品"
    //: case deferred          = "交易延期"
    case deferred = "交易延期"
    //: case verityFail        = "服务器验证失败"
    case verityFail = "服务器验证失败"
    //: case veritySucceed     = "服务器验证成功"
    case veritySucceed = "服务器验证成功"
    //: case renewSucceed      = "自动续订成功"
    case renewSucceed = "自动续订成功"
}

//: typealias IAPcompletionHandle = (AppleIAPStatus, Double, ApplePayType) -> Void
typealias IAPcompletionHandle = (LogQuickLookable, Double, AsideType) -> Void

//: class AppleIAPManager: NSObject {
class InjuryPubTransactionObserver: NSObject {
    //: var completionHandle: IAPcompletionHandle?
    var completionHandle: IAPcompletionHandle?
    //: private var productInfoReq: SKProductsRequest?
    private var productInfoReq: SKProductsRequest?
    //: private var reqRetryCountDict = [String: Int]()
    private var reqRetryCountDict = [String: Int]() // 记录每个交易请求重试次数
    //: private var payCacheList = [[String: String]]()
    private var payCacheList = [[String: String]]() // 【购买】缓存数据
    //: private var subscribeCacheList = [[String: String]]()
    private var subscribeCacheList = [[String: String]]() // 【订阅】缓存数据
    //: private var createOrderId: String?
    private var createOrderId: String? // 当前支付服务端创建的订单id
    //: private var currentPayType: ApplePayType = .Pay
    private var currentPayType: AsideType = .Pay // 当前支付类型

    // singleton
    //: static let shared = AppleIAPManager()
    static let shared = InjuryPubTransactionObserver()
    //: override func copy() -> Any { return self }
    override func copy() -> Any { return self }
    //: override func mutableCopy() -> Any { return self }
    override func mutableCopy() -> Any { return self }
    //: private override init() {
    override private init() {
        //: super.init()
        super.init()
        //: SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().add(self as SKPaymentTransactionObserver)
        // 监听应用将要销毁
        //: NotificationCenter.default.addObserver(self, selector: #selector(appWillTerminate),
        NotificationCenter.default.addObserver(self, selector: #selector(retired),
                                               //: name: UIApplication.willTerminateNotification,
                                               name: UIApplication.willTerminateNotification,
                                               //: object: nil)
                                               object: nil)
    }

    // MARK: - NotificationCenter

    //: @objc func appWillTerminate() {
    @objc func retired() {
        //: SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
        SKPaymentQueue.default().remove(self as SKPaymentTransactionObserver)
    }
}

// MARK: - 【苹果购买】业务接口

//: extension AppleIAPManager {
private extension InjuryPubTransactionObserver {
    /// 【购买】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_pay_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func civil(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/createApplePay"
        reqModel.requestPath = (String(dreamRiseTitle.suffix(5)) + "char" + String(dreamBelowId.prefix(6)) + "ateA" + String(kStripValue.suffix(7)))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(m_rearUrl.replacingOccurrences(of: "hang", with: "p") + String(dreamWageUrl))] = productId
        //: dict["source"] = source
        dict[(String(kPartyName) + String(k_yaName))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP充值创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                ProblemBenchPoundThen.panelDetect(msg: (kSeriesEnterText + "建订\u{5355}失败:") + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderNum"].string
            let orderId = json[(String(appSpendName.suffix(6)) + String(show_patentData))].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【购买】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_pay_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func upMinimize(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/recharge/applePayNotify"
        reqModel.requestPath = (String(appAbleStr) + String(mainSeriesPath.suffix(4)) + "appleP" + String(showHugeKey.prefix(8)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 2) {
                    //: UploadLogTool.writeLog(msg: "IAP充值校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    ProblemBenchPoundThen.panelDetect(msg: (String(k_numbName) + String(notiCareMessage)) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Pay)
                    self.reach(transactionId, .Pay)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newPayCacheList = self.payCacheList.filter({$0["transactionId"] != transactionId})
            let newPayCacheList = self.payCacheList.filter { $0[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))] != transactionId }
            //: let diskPath = self.getPayCachePath()
            let diskPath = self.sunnah()
            //: NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newPayCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_PAY_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: kLikeValue, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Pay)
            self.completionHandle?(.veritySucceed, result[(notiImpressionId.replacingOccurrences(of: "pending", with: "r") + String(appTrafficStr.suffix(6)) + "oney")].doubleValue, .Pay)
        }
    }
}

// MARK: - 【苹果订阅】业务接口

//: extension AppleIAPManager {
private extension InjuryPubTransactionObserver {
    /// 【订阅】创建业务订单
    /// - Parameters:
    ///   - productId: 产品Id
    ///   - block: 回调
    //: fileprivate func req_subscribe_createAppleOrder(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
    func doingOperation(productId: String, source: Int, handle: @escaping (String?, Bool) -> Void) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/AppleCreateOrder"
        reqModel.requestPath = (String(m_selectedFormat.prefix(5)) + "toSub/" + String(showOperationExactlyStr.prefix(4)) + "eCrea" + String(mainThroughDomainViewPath.suffix(6)) + showFeedbackValue.replacingOccurrences(of: "date", with: "r"))
        //: var dict = Dictionary<String, Any>()
        var dict = [String: Any]()
        //: dict["productId"] = productId
        dict[(m_rearUrl.replacingOccurrences(of: "hang", with: "p") + String(dreamWageUrl))] = productId
        //: dict["source"] = source
        dict[(String(kPartyName) + String(k_yaName))] = source
        //: reqModel.params = dict
        reqModel.params = dict
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true else {
            guard succeed == true else {
                //: UploadLogTool.writeLog(msg: "IAP订阅创建订单失败:\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                ProblemBenchPoundThen.panelDetect(msg: (String(mainSocialValue)) + "\(String(describing: errorModel?.errorMsg)), 参数:\(dict).")
                //: handle(nil, succeed)
                handle(nil, succeed)
                //: return
                return
            }

            //: let json = JSON(result!)
            let json = JSON(result!)
            //: let orderId = json["orderId"].string
            let orderId = json[String(bytes: mDeadlineName.map{banNurse(quit: $0)}, encoding: .utf8)!].string
            //: handle(orderId, succeed)
            handle(orderId, succeed)
        }
    }

    /// 【订阅】上传支付信息到服务器验证
    /// - Parameters:
    ///   - transaction: 交易信息
    ///   - params: 接口参数
    //: fileprivate func req_subscribe_uploadAppletransaction(_ transactionId: String, params: [String: String]) {
    func appletransaction(_ transactionId: String, params: [String: String]) {
        //: let reqModel = TalkingRequestModel.init()
        let reqModel = DoingicialReactiveCompatible()
        //: reqModel.requestType = .POST
        reqModel.requestType = .POST
        //: reqModel.requestPath = "mf/AutoSub/ApplePaySuccess"
        reqModel.requestPath = (String(notiWhenMidReportName) + String(notiComprehensiveIdent.prefix(5)) + "ePay" + String(mPoseName.prefix(7)))
        //: reqModel.params = params
        reqModel.params = params
        //: reqModel.showErrorStatusBar = false
        reqModel.showErrorStatusBar = false
        //: GJ.startRequest(model: reqModel) { succeed, result, errorModel in
        appDepthCornerContent.wrinkle(model: reqModel) { succeed, result, errorModel in
            //: guard succeed == true || errorModel?.errorCode == 405 else {
            guard succeed == true || errorModel?.errorCode == 405 else { // 验证接口失败，重试接口
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 3) {
                    //: UploadLogTool.writeLog(msg: "IAP订阅校验失败:\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    ProblemBenchPoundThen.panelDetect(msg: (String(user_sinceId.suffix(5)) + userCaptainQualityBagValue.capitalized) + "\(String(describing: errorModel?.errorCode)), 参数:\(params).")
                    //: self.transcationPurchasedToCheck(transactionId, .Subscribe)
                    self.reach(transactionId, .Subscribe)
                }
                //: return
                return
            }

            //: let result = JSON(result ?? [:])
            let result = JSON(result ?? [:])

            // 过滤已验证成功的订单数据
            //: let newSubscribeCacheList = self.subscribeCacheList.filter({$0["transactionId"] != transactionId})
            let newSubscribeCacheList = self.subscribeCacheList.filter { $0[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))] != transactionId }
            //: let diskPath = self.getSubscribeCachePath()
            let diskPath = self.monitorCalculate()
            //: NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)
            NSKeyedArchiver.archiveRootObject(newSubscribeCacheList, toFile: diskPath)

            // 成功通知
            //: NotificationCenter.default.post(name: APPLE_IAP_SUBSCRIBE_SUCCEED_NOTIFICATION, object: nil)
            NotificationCenter.default.post(name: app_positionFeatureWithoutFormat, object: nil)
            // 成功回调
            //: self.completionHandle?(.veritySucceed, result["reportMoney"].doubleValue, .Subscribe)
            self.completionHandle?(.veritySucceed, result[(notiImpressionId.replacingOccurrences(of: "pending", with: "r") + String(appTrafficStr.suffix(6)) + "oney")].doubleValue, .Subscribe)
        }
    }
}

// MARK: - Event

//: extension AppleIAPManager {
extension InjuryPubTransactionObserver {
    /// 初始化数据
    //: private func iap_initData() {
    private func disobligeWilling() {
        //: self.payCacheList = getLocalPayCacheList(payType: .Pay)
        self.payCacheList = editionOffce(payType: .Pay)
        //: self.subscribeCacheList = getLocalPayCacheList(payType: .Subscribe)
        self.subscribeCacheList = editionOffce(payType: .Subscribe)
        //: self.createOrderId = nil
        self.createOrderId = nil
    }

    /// 获取缓存列表
    /// - Parameter payType: 支付类型
    /// - Returns: 缓存列表
    //: private func getLocalPayCacheList(payType: ApplePayType) -> [[String: String]] {
    private func editionOffce(payType: AsideType) -> [[String: String]] {
        //: var list: [[String: String]]?
        var list: [[String: String]]?
        //: var diskPath = ""
        var diskPath = ""
        //: if payType == .Pay {
        if payType == .Pay {
            //: diskPath = getPayCachePath()
            diskPath = sunnah()
            //: } else {
        } else {
            //: diskPath = getSubscribeCachePath()
            diskPath = monitorCalculate()
        }

        //: if FileManager.default.fileExists(atPath: diskPath) {
        if FileManager.default.fileExists(atPath: diskPath) {
            //: list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            list = NSKeyedUnarchiver.unarchiveObject(withFile: diskPath) as? [[String: String]]
            //: if list == nil {
            if list == nil {
                //: try? FileManager.default.removeItem(atPath: diskPath)
                try? FileManager.default.removeItem(atPath: diskPath)
            }
        }
        //: if list == nil {
        if list == nil {
            //: list = [[String: String]]()
            list = [[String: String]]()
        }
        //: return list!
        return list!
    }

    /// 获取【购买】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getPayCachePath() -> String {
    private func sunnah() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(kOpportunityTitle.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = noti_foundData.string(forKey: mainFoundStr) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent((String(userAreaValue.suffix(4)) + "rTrans" + noti_customerName.replacingOccurrences(of: "priority", with: "ct") + "onIn" + String(main_gameKey.suffix(3))) + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取【订阅】缓存路径【和uid关联】
    /// - Returns: 缓存路径
    //: private func getSubscribeCachePath() -> String {
    private func monitorCalculate() -> String {
        //: let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        let documentDirectoryPath = NSSearchPathForDirectoriesInDomains(.documentDirectory, .userDomainMask, true).first ?? ""
        //: let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent("App")
        let appDirectoryPath = (documentDirectoryPath as NSString).appendingPathComponent((String(kOpportunityTitle.suffix(3))))

        //: let fileManager = FileManager.default
        let fileManager = FileManager.default
        //: if fileManager.fileExists(atPath: appDirectoryPath) == false {
        if fileManager.fileExists(atPath: appDirectoryPath) == false {
            //: try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
            try? fileManager.createDirectory(atPath: appDirectoryPath, withIntermediateDirectories: true)
        }

        //: let uid = Defaults.string(forKey: TalkingLoginUidCacheKey) ?? ""
        let uid = noti_foundData.string(forKey: mainFoundStr) ?? ""
        //: let filePath = (appDirectoryPath as NSString).appendingPathComponent("OrderTransactionInfo_Subscribe_\(uid)")
        let filePath = (appDirectoryPath as NSString).appendingPathComponent(String(bytes: k_closedFormat.map{informationMonthCrush(regional: $0)}, encoding: .utf8)! + "\(uid)")
        //: return filePath
        return filePath
    }

    /// 获取本地收据数据
    /// - Parameters:
    ///   - transactionId: 收据标识符
    ///   - payType: 支付类型
    /// - Returns: 收据数据
    //: fileprivate func getVerifyData(_ transactionId: String, _ payType: ApplePayType) -> String? {
    fileprivate func care(_ transactionId: String, _ payType: AsideType) -> String? {
        // 有未完成的订单，先取缓存
        //: var paramsArr = [[String: String]]()
        var paramsArr = [[String: String]]()
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.payCacheList.filter { $0[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))] == transactionId }
        //: case .Subscribe:
        case .Subscribe:
            //: paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            paramsArr = self.subscribeCacheList.filter { $0[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))] == transactionId }
        }
        //: if paramsArr.count > 0 && paramsArr.first!["verifyData"] != nil {
        if paramsArr.count > 0, paramsArr.first![String(bytes: app_kitFormatName.map{controlSkin(screen: $0)}, encoding: .utf8)!] != nil {
            //: return paramsArr.first!["verifyData"]
            return paramsArr.first![String(bytes: app_kitFormatName.map{controlSkin(screen: $0)}, encoding: .utf8)!]
        }

        // 取本地
        //: guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        guard let receiptUrl = Bundle.main.appStoreReceiptURL else { return nil }
        //: let data = NSData(contentsOf: receiptUrl)
        let data = NSData(contentsOf: receiptUrl)
        //: let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        let receiptStr = data?.base64EncodedString(options: NSData.Base64EncodingOptions(rawValue: 0))
        //: return receiptStr
        return receiptStr
    }
}

// MARK: - 失败重试流程

//: extension AppleIAPManager {
extension InjuryPubTransactionObserver {
    /// 检测未完成的苹果支付【只会重试当前登录用户】
    //: func iap_checkUnfinishedTransactions() {
    func perTransactions() {
        //: iap_initData()
        disobligeWilling()

        // 【购买】失败重试
        //: for dict in self.payCacheList {
        for dict in self.payCacheList {
            //: iap_failedRetry(dict["transactionId"], .Pay)
            fromOffLayer(dict[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))], .Pay)
        }

        // 【订阅】失败重试
        //: for dict in self.subscribeCacheList {
        for dict in self.subscribeCacheList {
            //: iap_failedRetry(dict["transactionId"], .Subscribe)
            fromOffLayer(dict[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))], .Subscribe)
        }
    }

    /// 失败重试
    /// - Parameters:
    ///   - transactionId: Id
    ///   - payType: 支付类型
    //: private func iap_failedRetry(_ transactionId: String?, _ payType: ApplePayType) {
    private func fromOffLayer(_ transactionId: String?, _ payType: AsideType) {
        //: guard let transactionId = transactionId else { return }
        guard let transactionId = transactionId else { return }
        // 初始化每个交易请求次数
        //: reqRetryCountDict[transactionId] = 0
        reqRetryCountDict[transactionId] = 0
        // 3. 服务端校验流程
        //: transcationPurchasedToCheck(transactionId, payType)
        reach(transactionId, payType)
    }
}

// MARK: - 苹果正常支付流程

//: extension AppleIAPManager {
extension InjuryPubTransactionObserver {
    /// 发起苹果支付【1.创建订单； 2.发起苹果支付； 3.服务端校验】
    /// - Parameters:
    ///   - purchID: 产品ID
    ///   - payType: 支付类型
    ///   - handle: 回调
    ///   - source: 0 常规充值 1 观看视频后充值或订阅
    //: func iap_startPurchase(productId: String, payType: ApplePayType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
    func titleShade(productId: String, payType: AsideType, source: Int = 0, handle: @escaping IAPcompletionHandle) {
        //: iap_initData()
        disobligeWilling()
        //: self.completionHandle = handle
        self.completionHandle = handle
        //: self.currentPayType = payType
        self.currentPayType = payType

        // 1. 根据类型创建订单
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: req_pay_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            civil(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Pay)
                    self.completionHandle?(.createOrderFail, 0, .Pay)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.anyComponentObserve(productId)
            }

        //: case .Subscribe:
        case .Subscribe:
            //: req_subscribe_createAppleOrder(productId: productId, source: source) { [weak self] orderId, succeed in
            doingOperation(productId: productId, source: source) { [weak self] orderId, succeed in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: guard succeed == true && orderId != nil else {
                guard succeed == true && orderId != nil else { // 订单创建失败
                    //: self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    self.completionHandle?(.createOrderFail, 0, .Subscribe)
                    //: return
                    return
                }

                //: self.createOrderId = orderId
                self.createOrderId = orderId
                //: self.requestProductInfo(productId)
                self.anyComponentObserve(productId)
            }
        }
    }

    // 2 发起苹果支付，查询apple内购商品
    //: fileprivate func requestProductInfo(_ productId: String) {
    fileprivate func anyComponentObserve(_ productId: String) {
        //: guard SKPaymentQueue.canMakePayments() else {
        guard SKPaymentQueue.canMakePayments() else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, productId:\(productId).")
            ProblemBenchPoundThen.panelDetect(msg: (String(m_agreeName.suffix(5)) + String(main_overlookMsg)) + "\(productId).")
            //: self.completionHandle?(.notArrow, 0, currentPayType)
            self.completionHandle?(.notArrow, 0, currentPayType)
            //: return
            return
        }

        // 销毁当前请求
        //: self.clearProductInfoRequest()
        self.pep()
        // 查询apple内购商品
        //: let identifiers: Set<String> = [productId]
        let identifiers: Set<String> = [productId]
        //: productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        productInfoReq = SKProductsRequest(productIdentifiers: identifiers)
        //: productInfoReq?.delegate = self
        productInfoReq?.delegate = self
        //: productInfoReq?.start()
        productInfoReq?.start()
    }

    // 销毁当前请求
    //: fileprivate func clearProductInfoRequest() {
    fileprivate func pep() {
        //: guard productInfoReq != nil else { return }
        guard productInfoReq != nil else { return }
        //: productInfoReq?.delegate = nil
        productInfoReq?.delegate = nil
        //: productInfoReq?.cancel()
        productInfoReq?.cancel()
        //: productInfoReq = nil
        productInfoReq = nil
    }
}

// MARK: - SKProductsRequestDelegate【商品查询】

//: extension AppleIAPManager: SKProductsRequestDelegate {
extension InjuryPubTransactionObserver: SKProductsRequestDelegate {
    // 查询apple内购商品成功回调
    //: func productsRequest(_ request: SKProductsRequest, didReceive response: SKProductsResponse) {
    func productsRequest(_: SKProductsRequest, didReceive response: SKProductsResponse) {
        //: guard response.products.count > 0 else {
        guard response.products.count > 0 else {
            //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, products.count为0.")
            ProblemBenchPoundThen.panelDetect(msg: (String(user_evaluatePath.prefix(7)) + String(dream_securityValue) + String(notiActivityMessage)))
            //: self.completionHandle?( .noProductId, 0, currentPayType)
            self.completionHandle?(.noProductId, 0, currentPayType)
            //: return
            return
        }

        //: let payment = SKPayment(product: response.products.first!)
        let payment = SKPayment(product: response.products.first!)
        //: SKPaymentQueue.default().add(payment)
        SKPaymentQueue.default().add(payment)
    }

    // 查询apple内购商品失败
    //: func request(_ request: SKRequest, didFailWithError error: Error) {
    func request(_: SKRequest, didFailWithError error: Error) {
        //: UploadLogTool.writeLog(msg: "IAP查询内购商品失败, Error:\(error).")
        ProblemBenchPoundThen.panelDetect(msg: (String(userShallName)) + "\(error).")
        //: self.completionHandle?( .noProductId, 0, currentPayType)
        self.completionHandle?(.noProductId, 0, currentPayType)
    }

    // 查询apple内购商品完成
    //: func requestDidFinish(_ request: SKRequest) {
    func requestDidFinish(_: SKRequest) {}
}

// MARK: - SKPaymentTransactionObserver【支付回调】

//: extension AppleIAPManager: SKPaymentTransactionObserver {
extension InjuryPubTransactionObserver: SKPaymentTransactionObserver {
    /// 2.2 apple内购完成回调
    //: func paymentQueue(_ queue: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
    func paymentQueue(_: SKPaymentQueue, updatedTransactions transactions: [SKPaymentTransaction]) {
        //: for transaction in transactions {
        for transaction in transactions {
            //: switch transaction.transactionState {
            switch transaction.transactionState {
            //: case .purchasing:
            case .purchasing: // 交易中
                //: break
                break
            //: case .purchased:
            case .purchased: // 交易成功
                /**
                 original.transactionIdentifier 首次订阅时为nil，transaction.transactionIdentifier有值；
                 后续自动订阅、续订时，original.transactionIdentifier为首次订阅时生成的transaction.transactionIdentifier，值固定不变；
                 每次订阅transaction.transactionIdentifier都不一样，为当前交易的标识；
                 */
                //: if transaction.original != nil && createOrderId == nil {
                if transaction.original != nil && createOrderId == nil { // 启动自动续订时，不需要调用服务端验证接口
                    //: self.completionHandle?(.renewSucceed, 0, currentPayType)
                    self.completionHandle?(.renewSucceed, 0, currentPayType)
                    //: } else {
                } else { // 普通购买和订阅
                    //: UploadLogTool.writeLog(msg: "IAP-purchased:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                    ProblemBenchPoundThen.panelDetect(msg: (String(mainArmGatherFormat) + k_implementPath.capitalized) + "\(currentPayType)" + (dreamStrengthIdent.replacingOccurrences(of: "committee", with: "订") + String(appParaTitle)) + "\(String(describing: createOrderId))" + (String(notiParentValue)) + "\(transaction.payment.productIdentifier)" + (String(k_fourIdent) + String(show_trustUrl.prefix(2))) + "\(String(describing: transaction.transactionIdentifier)).")
                    // 初始化每个交易请求次数
                    //: reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    reqRetryCountDict[transaction.transactionIdentifier!] = 0
                    // 3. 服务端校验流程
                    //: transcationPurchasedToCheck(transaction.transactionIdentifier!, self.currentPayType)
                    reach(transaction.transactionIdentifier!, self.currentPayType)
                }
                // 移除苹果支付系统缓存
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: createOrderId = nil
                createOrderId = nil
            //: case .failed:
            case .failed: // 交易失败/取消
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-failed:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                ProblemBenchPoundThen.panelDetect(msg: (String(dream_slowName) + String(mLeaseAlreadyBarIdent.prefix(8))) + "\(currentPayType)" + (dreamStrengthIdent.replacingOccurrences(of: "committee", with: "订") + String(appParaTitle)) + "\(String(describing: createOrderId))" + (String(notiParentValue)) + "\(transaction.payment.productIdentifier)" + (String(k_fourIdent) + String(show_trustUrl.prefix(2))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.failed, 0, currentPayType)
                self.completionHandle?(.failed, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .restored:
            case .restored: // 已购买过该商品
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.restored, 0, currentPayType)
                self.completionHandle?(.restored, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: case .deferred:
            case .deferred: // 交易延期
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: UploadLogTool.writeLog(msg: "IAP-deferred:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                ProblemBenchPoundThen.panelDetect(msg: (String(m_seriesName.prefix(8)) + String(mainMediaStr)) + "\(currentPayType)" + (dreamStrengthIdent.replacingOccurrences(of: "committee", with: "订") + String(appParaTitle)) + "\(String(describing: createOrderId))" + (String(notiParentValue)) + "\(transaction.payment.productIdentifier)" + (String(k_fourIdent) + String(show_trustUrl.prefix(2))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: self.completionHandle?(.deferred, 0, currentPayType)
                self.completionHandle?(.deferred, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
            //: @unknown default:
            @unknown default:
                //: SKPaymentQueue.default().finishTransaction(transaction)
                SKPaymentQueue.default().finishTransaction(transaction)
                //: self.completionHandle?(.unknow, 0, currentPayType)
                self.completionHandle?(.unknow, 0, currentPayType)
                //: createOrderId = nil
                createOrderId = nil
                //: UploadLogTool.writeLog(msg: "IAP未知交易类型:\(currentPayType), 订单号:\(String(describing: createOrderId)), 商品Id:\(transaction.payment.productIdentifier), 苹果transId:\(String(describing: transaction.transactionIdentifier)).")
                ProblemBenchPoundThen.panelDetect(msg: (String(dreamTextSingleIdent)) + "\(currentPayType)" + (dreamStrengthIdent.replacingOccurrences(of: "committee", with: "订") + String(appParaTitle)) + "\(String(describing: createOrderId))" + (String(notiParentValue)) + "\(transaction.payment.productIdentifier)" + (String(k_fourIdent) + String(show_trustUrl.prefix(2))) + "\(String(describing: transaction.transactionIdentifier)).")
                //: fatalError(" 未知的交易类型")
                fatalError((user_renderFormat.capitalized + "交易类型"))
            }
        }
    }

    /// 3. 服务端校验流程
    /// - Parameters:
    ///   - transactionId: 交易唯一标识符
    ///   - payType: 支付类型
    //: fileprivate func transcationPurchasedToCheck(_ transactionId: String, _ payType: ApplePayType) {
    fileprivate func reach(_ transactionId: String, _ payType: AsideType) {
        //: guard let receiptStr = getVerifyData(transactionId, payType) else {
        guard let receiptStr = care(transactionId, payType) else {
            //: UploadLogTool.writeLog(msg: "IAP获取本地收据数据失败, transactionId:\(transactionId), orderId:\(String(describing: createOrderId)), payType:\(payType).")
            ProblemBenchPoundThen.panelDetect(msg: (String(k_permitMsg) + String(appStrongId) + String(user_habitAlwaysIdentifyMessage.prefix(8))) + "\(transactionId)" + (String(show_usedUrl) + app_reasonUrl.replacingOccurrences(of: "budget", with: ":")) + "\(String(describing: createOrderId))" + (String(kZoneHoneyStr.suffix(8)) + String(noti_hypothesisPath.prefix(2))) + "\(payType).")
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 缓存支付成功信息，防止接口校验失败
        //: if createOrderId != nil {
        if createOrderId != nil { // 正常支付流程
            //: switch(payType) {
            switch payType {
            //: case .Pay:
            case .Pay:
                //: if self.payCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 {  // 防止重复添加缓存数据
                if self.payCacheList.filter({ $0[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))] == transactionId || $0[String(bytes: mDeadlineName.map{banNurse(quit: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(String(app_mobileFrameStr) + String(appBotPath.suffix(5))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: mDeadlineName.map{banNurse(quit: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: app_kitFormatName.map{controlSkin(screen: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.payCacheList.append(cacheDict)
                    self.payCacheList.append(cacheDict)
                    //: let diskPath = self.getPayCachePath()
                    let diskPath = self.sunnah()
                    //: NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.payCacheList, toFile: diskPath)
                }

            //: case .Subscribe:
            case .Subscribe:
                //: if self.subscribeCacheList.filter({$0["transactionId"] == transactionId || $0["orderId"] == createOrderId}).count == 0 { // 防止重复添加缓存数据
                if self.subscribeCacheList.filter({ $0[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))] == transactionId || $0[String(bytes: mDeadlineName.map{banNurse(quit: $0)}, encoding: .utf8)!] == createOrderId }).count == 0 { // 防止重复添加缓存数据
                    //: let cacheDict = ["transactionId": transactionId,
                    let cacheDict = [(String(app_mobileFrameStr) + String(appBotPath.suffix(5))): transactionId,
                                     //: "orderId": createOrderId!,
                                     String(bytes: mDeadlineName.map{banNurse(quit: $0)}, encoding: .utf8)!: createOrderId!,
                                     //: "verifyData": receiptStr]
                                     String(bytes: app_kitFormatName.map{controlSkin(screen: $0)}, encoding: .utf8)!: receiptStr]
                    //: self.subscribeCacheList.append(cacheDict)
                    self.subscribeCacheList.append(cacheDict)
                    //: let diskPath = self.getSubscribeCachePath()
                    let diskPath = self.monitorCalculate()
                    //: NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                    NSKeyedArchiver.archiveRootObject(self.subscribeCacheList, toFile: diskPath)
                }
            }
        }

        // 限制交易重试最大次数
        //: var reqCount = reqRetryCountDict[transactionId] ?? 0
        var reqCount = reqRetryCountDict[transactionId] ?? 0
        //: reqCount += 1
        reqCount += 1
        //: reqRetryCountDict[transactionId] = reqCount
        reqRetryCountDict[transactionId] = reqCount
        //: if reqCount > APPLE_IAP_MAX_RETRY_COUNT {
        if reqCount > mRouteUrl {
            //: self.completionHandle?(.verityFail, 0, payType)
            self.completionHandle?(.verityFail, 0, payType)
            //: return
            return
        }

        // 3.服务端校验，根据transactionId从缓存中取
        //: switch(payType) {
        switch payType {
        //: case .Pay:
        case .Pay:
            //: let paramsArr = self.payCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.payCacheList.filter { $0[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_pay_uploadAppletransaction(transactionId, params: paramsArr.first!)
            upMinimize(transactionId, params: paramsArr.first!)

        //: case .Subscribe:
        case .Subscribe:
            //: let paramsArr = self.subscribeCacheList.filter({$0["transactionId"] == transactionId})
            let paramsArr = self.subscribeCacheList.filter { $0[(String(app_mobileFrameStr) + String(appBotPath.suffix(5)))] == transactionId }
            //: guard paramsArr.count > 0 else { return }
            guard paramsArr.count > 0 else { return }
            //: req_subscribe_uploadAppletransaction(transactionId, params: paramsArr.first!)
            appletransaction(transactionId, params: paramsArr.first!)
        }
    }
}
