
//: Declare String Begin

/*: "init(coder:) has not been implemented" :*/
fileprivate let dreamThickPath:[UInt8] = [0xbc,0xbb,0xbc,0xa1,0xfd,0xb6,0xba,0xb1,0xb0,0xa7,0xef,0xfc,0xf5,0xbd,0xb4,0xa6,0xf5,0xbb,0xba,0xa1,0xf5,0xb7,0xb0,0xb0,0xbb,0xf5,0xbc,0xb8,0xa5,0xb9,0xb0,0xb8,0xb0,0xbb,0xa1,0xb0,0xb1]

private func findPocket(professional num: UInt8) -> UInt8 {
    return num ^ 213
}

/*: "Load..." :*/
fileprivate let notiContextFlowName:String = "Load...ego keep"

/*: "Skip" :*/
fileprivate let kCurrentValue:String = "eastern seriesSkip"

/*: "HttpTool.NativeToJs('recharge')" :*/
fileprivate let app_togetherStr:[UInt8] = [0xd3,0xef,0xef,0xeb,0xcf,0xf4,0xf4,0xf7,0xb5,0xd5,0xfa,0xef,0xf2,0xed,0xfe,0xcf,0xf4,0xd1,0xe8,0xb3,0xbc,0xe9,0xfe,0xf8,0xf3,0xfa,0xe9,0xfc,0xfe,0xbc,0xb2]

/*: "window.onPageShow&&onPageShow();" :*/
fileprivate let dream_foreTitle:[UInt8] = [0xa0,0x92,0x97,0x8d,0x98,0xa0,0x57,0x98,0x97,0x79,0x8a,0x90,0x8e,0x7c,0x91,0x98,0xa0,0x4f,0x4f,0x98,0x97,0x79,0x8a,0x90,0x8e,0x7c,0x91,0x98,0xa0,0x51,0x52,0x64]

fileprivate func radioSink(spark num: UInt8) -> UInt8 {
    let value = Int(num) - 41
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "jsEvent(onPageShow):  :*/
fileprivate let notiShallId:String = "jsEvtrace spread line"
fileprivate let app_quietMessage:[Character] = ["n","P","a","g","e","S","h"]
fileprivate let showLieIdent:String = "somebody behindow): "

/*: "window.onPageHide&&onPageHide();" :*/
fileprivate let user_wageData:[UInt8] = [0xf6,0xe8,0xef,0xe5,0xee,0xf6,0xaf,0xee,0xef,0xd1,0xe0,0xe6,0xe4,0xc9,0xe8,0xe5,0xe4,0xa7,0xa7,0xee,0xef,0xd1,0xe0,0xe6,0xe4,0xc9,0xe8,0xe5,0xe4,0xa9,0xa8,0xba]

/*: "jsEvent(onPageHide):  :*/
fileprivate let notiTailAdmitIdent:[Character] = ["j","s","E","v","e","n","t","(","o","n","P","a","g","e","H"]
fileprivate let kRutPath:[Character] = ["i","d","e",")",":"," "]

/*: "title" :*/
fileprivate let mainDeliveryMessage:[Character] = ["t","i","t","l","e"]

/*: "canGoBack" :*/
fileprivate let appRapidlyText:String = "invitation magazine fiscal librarycanG"
fileprivate let notiRateTitle:[Character] = ["k"]

/*: "PHPSESSID= :*/
fileprivate let kCommunicateData:String = "somebodyPS"
fileprivate let show_televisionId:[Character] = ["E","S","S","I","D","="]

/*: ";uid= :*/
fileprivate let mainContainerValue:[Character] = [";","u","i","d","="]

/*: "Cookie" :*/
fileprivate let mTheoryValue:[Character] = ["C","o","o","k","i","e"]

/*: "nav_back_black_nor" :*/
fileprivate let noti_statusAdmitKey:[Character] = ["n","a","v","_","b","a","c","k","_","b","l","a","c"]
fileprivate let show_readyTitle:String = "traffic host injury nativek_nor"

/*: "ico_intimate_list_sm" :*/
fileprivate let m_hideIdent:[Character] = ["i","c","o","_","i","n","t"]
fileprivate let k_accordName:[Character] = ["i","m","a","t","e","_","l","i","s","t","_","s","m"]

/*: "document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'" :*/
fileprivate let mainDrownStr:[UInt8] = [0xb0,0xbb,0xb7,0xa1,0xb9,0xb1,0xba,0xa0,0xfa,0xb3,0xb1,0xa0,0x91,0xb8,0xb1,0xb9,0xb1,0xba,0xa0,0xa7,0x96,0xad,0x80,0xb5,0xb3,0x9a,0xb5,0xb9,0xb1,0xfc,0xf3,0xb6,0xbb,0xb0,0xad,0xf3,0xfd,0x8f,0xe4,0x89,0xfa,0xa7,0xa0,0xad,0xb8,0xb1,0xfa,0xb6,0xb5,0xb7,0xbf,0xb3,0xa6,0xbb,0xa1,0xba,0xb0,0xe9,0xf3,0xa6,0xb3,0xb6,0xb5,0xfc,0xe4,0xf8,0xe4,0xf8,0xe4,0xf8,0xe4,0xfd,0xf3]

/*: "Alert" :*/
fileprivate let user_soulContent:[Character] = ["A","l","e","r","t"]

/*: "Cancle" :*/
fileprivate let showFoundationHidePaperMessage:String = "argument anniversary following leftCancle"

/*: "Done" :*/
fileprivate let show_signData:[Character] = ["D","o","n","e"]

/*: "js call method name =  :*/
fileprivate let showSupportiveTitle:String = "output miss suitejs c"
fileprivate let noti_electronicPath:[Character] = ["o","d"," ","n","a","m","e"," ","="," "]

/*: , message =  :*/
fileprivate let show_numbUrl:String = ", messagdrown pattern"
fileprivate let m_menuId:String = "e = back measure element except"

/*: "http://" :*/
fileprivate let main_insertValue:[Character] = ["h","t","t","p",":","/","/"]

/*: "https://" :*/
fileprivate let showHomeIdent:[Character] = ["h","t","t","p","s",":","/"]
fileprivate let notiPoseFormat:[Character] = ["/"]

/*: "var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n" :*/
fileprivate let m_argumentName:[UInt8] = [0x42,0x55,0x46,0x14,0x57,0x5b,0x5b,0x5f,0x5d,0x51,0x7a,0x55,0x59,0x51,0x47,0x14,0x9,0x14,0x50,0x5b,0x57,0x41,0x59,0x51,0x5a,0x40,0x1a,0x57,0x5b,0x5b,0x5f,0x5d,0x51,0x1a,0x47,0x44,0x58,0x5d,0x40,0x1c,0x13,0xf,0x14,0x13,0x1d,0x1a,0x59,0x55,0x44,0x1c,0x52,0x41,0x5a,0x57,0x40,0x5d,0x5b,0x5a,0x1c,0x57,0x5b,0x5b,0x5f,0x5d,0x51,0x1d,0x14,0x4f,0x14,0x46,0x51,0x40,0x41,0x46,0x5a,0x14,0x57,0x5b,0x5b,0x5f,0x5d,0x51,0x1a,0x47,0x44,0x58,0x5d,0x40,0x1c,0x13,0x9,0x13,0x1d,0x6f,0x4,0x69,0x14,0x49,0x14,0x1d,0xf,0x3e]

/*: "uid= :*/
fileprivate let user_seekFosterName:String = "gold fog advice decide penuid="

/*: ;domain= :*/
fileprivate let k_problemUrl:[Character] = [";","d","o","m","a","i","n","="]

/*: ;path=/" :*/
fileprivate let show_unlessValue:[Character] = [";","p"]
fileprivate let app_springPowKey:[Character] = ["a","t","h","=","/"]

/*: "document.cookie=' :*/
fileprivate let m_distinguishGlobalKey:[Character] = ["d","o","c","u","m","e","n"]
fileprivate let dreamCounselorContent:[Character] = ["t"]
fileprivate let mLabelId:[Character] = [".","c","o","o","k","i","e","=","\'"]

/*: ;domin= :*/
fileprivate let kMustCommendStr:String = ";"
fileprivate let noti_whetherAnswerFormat:[Character] = ["d","o","m","i","n","="]

/*: "if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie=' :*/
fileprivate let m_clearWithKey:[UInt8] = [0x51,0x5e,0x18,0x10,0x5b,0x57,0x57,0x53,0x51,0x5d,0x76,0x59,0x55,0x5d,0x4b,0x16,0x51,0x56,0x5c,0x5d,0x40,0x77,0x5e,0x10,0x1f,0x68,0x70,0x68,0x6b,0x7d,0x6b,0x6b,0x71,0x7c,0x1f,0x11,0x18,0x5,0x5,0x18,0x15,0x9,0x11,0x18,0x43,0x18,0x5c,0x57,0x5b,0x4d,0x55,0x5d,0x56,0x4c,0x16,0x5b,0x57,0x57,0x53,0x51,0x5d,0x5,0x1f]

private func blindCap(rocket num: UInt8) -> UInt8 {
    return num ^ 56
}

/*: "h5Domain =  :*/
fileprivate let kAspectThinUrl:String = "us pureh5Dom"
fileprivate let m_clearlyMessage:String = "=schedule"

/*: "getAllCookies =  :*/
fileprivate let mApprovalPath:String = "comprise shadow fare stepgetAll"
fileprivate let noti_hisId:String = "head conversationCookie"

/*: "PHPSESSID" :*/
fileprivate let user_containTitle:String = "PeyePSESSI"
fileprivate let noti_borderMsg:[Character] = ["D"]

/*: "uid" :*/
fileprivate let noti_fourQuoteKey:[Character] = ["u","i","d"]

/*: "appCookies =  :*/
fileprivate let showChanceContent:String = "appCpunish try date house"
fileprivate let userGravityKey:String = " "

/*: "UID" :*/
fileprivate let show_thousandValue:[Character] = ["U","I","D"]

/*: "navigator.userAgent" :*/
fileprivate let main_bottomKey:String = "navsomebody"
fileprivate let kSizeGageStr:String = "userAgeminute tun basic particle"
fileprivate let mFindStr:String = "narray"

/*: "xunwu_iphone" :*/
fileprivate let k_soilName:String = "comprise server window married rapidlyxunwu_i"
fileprivate let dream_hairValue:String = "phonstandard"

/*: " xunwu_device_id/ :*/
fileprivate let userRemarkKey:[Character] = [" ","x","u","n","w","u","_","d","e","v","i","c","e"]
fileprivate let mUniformId:String = "_id/button ourselves data"

/*: "xunwu_iphone xunwu_version/ :*/
fileprivate let noti_boyId:String = "xunpowder"
fileprivate let showVesselText:String = "ne xsh past"
fileprivate let app_indexPrepareMsg:String = "ecomesion"
fileprivate let notiIdentifyKey:String = "/"

/*:  xunwu_package/ :*/
fileprivate let notiNeedTitle:[Character] = [" ","x","u","n","w","u","_"]
fileprivate let mainTrustMsg:[Character] = ["p","a"]
fileprivate let userTapMsg:[Character] = ["c","k","a","g","e","/"]

/*:  xunwu_bundleId/ :*/
fileprivate let kMaterialText:String = "per low row security xunwu"
fileprivate let user_diskIdent:String = "Id/meet active suite dominant three"

/*:   webkit/1" :*/
fileprivate let app_leadingTitle:String = "  webktrust info"

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AsideViewController.swift
//  AbroadTalking
//
//  Created by Hemming on 2022/9/2.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

//: class TalkingWebViewController: TalkingBasePresentViewController {
class AsideViewController: PresentFindCreateLogVanguardRecognizerDelegate {
    //: typealias ExitWebAction = ( ) -> Void
    typealias ExitWebAction = () -> Void
    //: typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    typealias OpenWebSuccess = (_ openSucc: Bool) -> Void
    //: typealias HandleJSAction = (_ actionName: String) -> Void
    typealias HandleJSAction = (_ actionName: String) -> Void

    //: private let disposeBag = DisposeBag()
    private let disposeBag = DisposeBag()
    //: private var requestReady = false
    private var requestReady = false

    //: private var config: TalkingWebConfig!
    private var config: MerelyFireConfig!
    //: var isModal = false
    var isModal = false // 是否是模态弹出
    //: var urlStr: String!
    var urlStr: String!
    //: private var openBlock: OpenWebSuccess!
    private var openBlock: OpenWebSuccess!
    //: private var handleJSBlock: HandleJSAction!
    private var handleJSBlock: HandleJSAction!
    //: private var exitBlock: ExitWebAction!
    private var exitBlock: ExitWebAction!
    //: private var webUrlType: WebUrlType?
    private var webUrlType: ConcentrationTextLiteral?
    //: var isVideoCallRechargeHalfPage = false
    var isVideoCallRechargeHalfPage = false /// v1.5.8是否视频弹窗唤醒的半屏充值或订阅
    //: var isRechargeOrSubscribeChangeMatch = false
    var isRechargeOrSubscribeChangeMatch = false /// v1.6.0充值或订阅成功后，切换到底部match栏

    //: init(urlString: String, webViewType: WebUrlType?, webConfig: TalkingWebConfig) {
    init(urlString: String, webViewType: ConcentrationTextLiteral?, webConfig: MerelyFireConfig) {
        //: super.init()
        super.init()
        //: config = webConfig
        config = webConfig
        //: urlStr = urlString
        urlStr = urlString
        //: webUrlType = webViewType
        webUrlType = webViewType
    }

    //: required init?(coder: NSCoder) {
    required init?(coder _: NSCoder) {
        //: fatalError("init(coder:) has not been implemented")
        fatalError(String(bytes: dreamThickPath.map{findPocket(professional: $0)}, encoding: .utf8)!)
    }

    //: deinit {
    deinit {
        //: NSObject.cancelPreviousPerformRequests(withTarget: self)
        NSObject.cancelPreviousPerformRequests(withTarget: self)
        //: webView.stopLoading()
        webView.stopLoading()
        //: webView.uiDelegate = nil
        webView.uiDelegate = nil
        //: webView.navigationDelegate = nil
        webView.navigationDelegate = nil
        //: webView.removeFromSuperview()
        webView.removeFromSuperview()

        // 清缓存
        //: let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        let websiteDataTypes: Set<String> = [WKWebsiteDataTypeDiskCache]
        //: let dateForm = Date.init(timeIntervalSince1970: 0)
        let dateForm = Date(timeIntervalSince1970: 0)
        //: WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {
        WKWebsiteDataStore.default().removeData(ofTypes: websiteDataTypes, modifiedSince: dateForm) {}
    }

    //: override func viewWillAppear(_ animated: Bool) {
    override func viewWillAppear(_ animated: Bool) {
        //: super.viewWillAppear(animated)
        super.viewWillAppear(animated)
        //: jsEvent_onPageShow()
        mightHaveBeenProgram()
    }

    //: override func viewWillDisappear(_ animated: Bool) {
    override func viewWillDisappear(_ animated: Bool) {
        //: super.viewWillDisappear(animated)
        super.viewWillDisappear(animated)
        //: jsEvent_onPageHide()
        bridgeHide()
        /// 移除通知
        //: NotificationCenter.default.removeObserver(self)
        NotificationCenter.default.removeObserver(self)
    }

    //: override func viewDidLoad() {
    override func viewDidLoad() {
        //: super.viewDidLoad()
        super.viewDidLoad()
        //: view.clipsToBounds = true
        view.clipsToBounds = true
        //: self.title = "Load..."
        self.title = (String(notiContextFlowName.prefix(7)))
        //: addNotification()
        feeParent()
        //: func__checkConfig()
        doingly()
        //: func__setUpWebView()
        clubFuturism()
        //: func__registerObserver()
        eye()
        //: func__beginStartRequest()
        sourceAttentionFeed()
        //: func_checkAppleIAP()
        societyRunning()

        //: self.tranConfig.exitPrentView = { [weak self] in
        self.tranConfig.exitPrentView = { [weak self] in
            //: guard let self = self else {
            guard let self = self else {
                //: return
                return
            }
            //: if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
            if self.config.isHalfView && self.webUrlType == .RechargeHalfPage {
                //: NotificationCenter.default.post(name: RECHARGEHALFPAGE_END_CLOSE_NOTIFICATION, object: nil)
                NotificationCenter.default.post(name: mainFormatPath, object: nil)
            }
        }
    }

    // MARK: - Lazy load

    //: lazy var webView: WKWebView = {
    lazy var webView: WKWebView = {
        //: let webConfig = WKWebViewConfiguration.init()
        let webConfig = WKWebViewConfiguration()
        //: let preferences = WKPreferences.init()
        let preferences = WKPreferences()
        //: preferences.javaScriptEnabled = true
        preferences.javaScriptEnabled = true
        //: webConfig.preferences = preferences
        webConfig.preferences = preferences
        //: webConfig.allowsInlineMediaPlayback = true
        webConfig.allowsInlineMediaPlayback = true
        //: webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all
        webConfig.mediaTypesRequiringUserActionForPlayback = WKAudiovisualMediaTypes.all

        //: let userControl = WKUserContentController.init()
        let userControl = WKUserContentController()
        //: webConfig.userContentController = userControl
        webConfig.userContentController = userControl
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: func__checkWebCookiesBeforeRequest(webConfig: webConfig)
            primaryContainer(webConfig: webConfig)
        }
        //: let w = WKWebView.init(frame: .zero, configuration: webConfig)
        let w = WKWebView(frame: .zero, configuration: webConfig)
        //: w.uiDelegate = self
        w.uiDelegate = self
        //: w.navigationDelegate = self
        w.navigationDelegate = self
        //: w.allowsLinkPreview = false
        w.allowsLinkPreview = false
        //: w.allowsBackForwardNavigationGestures = false
        w.allowsBackForwardNavigationGestures = false
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: w.scrollView.contentInsetAdjustmentBehavior = .never
            w.scrollView.contentInsetAdjustmentBehavior = .never
        }
        //: return w
        return w
        //: }()
    }()

    //: private lazy var closeBtn: UIButton = {
    private lazy var closeBtn: UIButton = {
        //: let btn = UIButton(type: .custom)
        let btn = UIButton(type: .custom)
        //: let attributes = [.foregroundColor: UIColor.appThemeColor(), .font: UIFont.pingfangFont(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        let attributes = [.foregroundColor: UIColor.fogColor(), .font: UIFont.afterDisable(type: .Medium, fontSize: 22), .underlineStyle: 1] as [NSAttributedString.Key: Any]
        //: let attributeString = NSAttributedString(string: "Skip".localized, attributes: attributes)
        let attributeString = NSAttributedString(string: (String(kCurrentValue.suffix(4))).localized, attributes: attributes)
        //: btn.setAttributedTitle(attributeString, for: .normal)
        btn.setAttributedTitle(attributeString, for: .normal)
        //: return btn
        return btn
        //: }()
    }()
}

// MARK: - 【通知、js事件】

//: extension TalkingWebViewController {
extension AsideViewController {
    /// 添加通知
    //: private func addNotification() {
    private func feeParent() {
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(third_jsEvent_refreshCoin),
                                               selector: #selector(tertiarySame),
                                               //: name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION,
                                               name: m_thirdId,
                                               //: object: nil)
                                               object: nil)
        // 应用从后台切换到前台
        //: NotificationCenter.default.addObserver(self,
        NotificationCenter.default.addObserver(self,
                                               //: selector: #selector(jsEvent_onPageShow),
                                               selector: #selector(mightHaveBeenProgram),
                                               //: name: UIApplication.willEnterForegroundNotification,
                                               name: UIApplication.willEnterForegroundNotification,
                                               //: object: nil)
                                               object: nil)
    }

    /// 通知三方H5刷新金币
    //: @objc private func third_jsEvent_refreshCoin() {
    @objc private func tertiarySame() {
        //: self.webView.evaluateJavaScript("HttpTool.NativeToJs('recharge')") { data, error in
        self.webView.evaluateJavaScript(String(bytes: app_togetherStr.map{$0^155}, encoding: .utf8)!) { _, _ in
        }
    }

    /// js事件：网页展示
    //: @objc private func jsEvent_onPageShow() {
    @objc private func mightHaveBeenProgram() {
        //: self.webView.evaluateJavaScript("window.onPageShow&&onPageShow();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: dream_foreTitle.map{radioSink(spark: $0)}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageShow): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(notiShallId.prefix(4)) + "ent(o" + String(app_quietMessage) + String(showLieIdent.suffix(5))) + "\(String(describing: data))---\(String(describing: error))")
        }
    }

    /// js事件：网页消失
    //: private func jsEvent_onPageHide() {
    private func bridgeHide() {
        //: self.webView.evaluateJavaScript("window.onPageHide&&onPageHide();") { data, error in
        self.webView.evaluateJavaScript(String(bytes: user_wageData.map{$0^129}, encoding: .utf8)!) { data, error in
            //: printLog(message: "jsEvent(onPageHide): \(String(describing: data))---\(String(describing: error))")
            printLog(message: (String(notiTailAdmitIdent) + String(kRutPath)) + "\(String(describing: data))---\(String(describing: error))")
        }
    }
}

//: extension TalkingWebViewController {
extension AsideViewController {
    //: func func__registerObserver() {
    func eye() {
        //: webView.rx.observeWeakly(String.self, "title")
        webView.rx.observeWeakly(String.self, (String(mainDeliveryMessage)))
            //: .subscribe(onNext: { [weak self] (value) in
            .subscribe(onNext: { [weak self] value in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: self.title = value
                self.title = value
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: webView.rx.observeWeakly(String.self, "canGoBack")
        webView.rx.observeWeakly(String.self, (String(appRapidlyText.suffix(4)) + "oBac" + String(notiRateTitle)))
            //: .subscribe(onNext: { _ in
            .subscribe(onNext: { _ in
                //: })
            })
            //: .disposed(by: disposeBag)
            .disposed(by: disposeBag)

        //: NotificationCenter.default.addObserver(self, selector: #selector(closeDiscountsView), name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
        NotificationCenter.default.addObserver(self, selector: #selector(hesitateView), name: noti_dismissText, object: nil)
    }

    //: func func__setUpWebView() {
    func clubFuturism() {
        //: view.addSubview(webView)
        view.addSubview(webView)
        //: webView.snp.makeConstraints { make in
        webView.snp.makeConstraints { make in
            //: make.edges.equalToSuperview()
            make.edges.equalToSuperview()
        }
        //: func__addcustomUserAgent()
        ceremonyAgent()
        //: func__addBridgeMethod()
        gimpy()

        //: if config.clearBgColor {
        if config.clearBgColor {
            //: view.backgroundColor = .clear
            view.backgroundColor = .clear
            //: } else if config.isAlertType || config.showLoadingView {
        } else if config.isAlertType || config.showLoadingView {
            //: view.backgroundColor = .black.withAlphaComponent(0.4)
            view.backgroundColor = .black.withAlphaComponent(0.4)
            //: webView.backgroundColor = UIColor.clear
            webView.backgroundColor = UIColor.clear
            //: } else {
        } else {
            //: view.backgroundColor = UIColor.appBgColor()
            view.backgroundColor = UIColor.landmarkBalance()
        }
        //: if config.isHalfView {
        if config.isHalfView {
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
        }

        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: webView)
            VillageView.feeView(superView: webView)
        }

        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.addSubview(closeBtn)
            view.addSubview(closeBtn)
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
            //: closeBtn.isHidden = false
            closeBtn.isHidden = false
            //: closeBtn.snp.makeConstraints { make in
            closeBtn.snp.makeConstraints { make in
                //: make.height.width.equalTo(50)
                make.height.width.equalTo(50)
                //: make.trailing.equalTo(-10)
                make.trailing.equalTo(-10)
                //: make.height.equalToSuperview().offset(StatusBarHeight)
                make.height.equalToSuperview().offset(mNameKey)
            }
        }
    }

    //: func func__beginStartRequest() {
    func sourceAttentionFeed() {
        //: if !requestReady {
        if !requestReady {
            //: return
            return
        }
        //: if config.isAlertType {
        if config.isAlertType {
            //: DispatchQueue.main.asyncAfter(deadline: .now()+1) { [self] in
            DispatchQueue.main.asyncAfter(deadline: .now() + 1) { [self] in
                //: self.func__startRequest()
                self.meRefuse()
            }
            //: } else {
        } else {
            //: func__startRequest()
            meRefuse()
        }
    }

    //: func func__startRequest() {
    func meRefuse() {
        //: var urlRequest = URLRequest.init(url: URL.init(string: urlStr)!)
        var urlRequest = URLRequest(url: URL(string: urlStr)!)
        //: let uid = AdministratorThen.share.loginUid
        let uid = AdministratorThen.share.loginUid
        //: let ssid = AdministratorThen.share.loginSessionId
        let ssid = AdministratorThen.share.loginSessionId
        //: var cookieStr = "PHPSESSID=\(ssid)"
        var cookieStr = (kCommunicateData.replacingOccurrences(of: "somebody", with: "PH") + String(show_televisionId)) + "\(ssid)"
        //: if uid.count > 0 {
        if uid.count > 0 {
            //: cookieStr.append(";uid=\(uid)")
            cookieStr.append((String(mainContainerValue)) + "\(uid)")
        }
        //: urlRequest.addValue(cookieStr, forHTTPHeaderField: "Cookie")
        urlRequest.addValue(cookieStr, forHTTPHeaderField: (String(mTheoryValue)))
        //: urlRequest.httpShouldHandleCookies = false
        urlRequest.httpShouldHandleCookies = false
        //: webView.load(urlRequest)
        webView.load(urlRequest)
        //: func__clearJSBgColor()
        clearColor()
        //: func__initRightButton()
        nameureNor()
    }

    /// apple支付重试末完成的订单
    //: func func_checkAppleIAP() {
    func societyRunning() {
        //: if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
        if webUrlType == .RechargeFullPage || webUrlType == .RechargeHalfPage {
            //: AppleIAPManager.shared.completionHandle = { [weak self] status, reportMoney, payType in
            InjuryPubTransactionObserver.shared.completionHandle = { [weak self] status, reportMoney, payType in
                //: guard let self = self else {
                guard let self = self else {
                    //: return
                    return
                }
                //: if status == .veritySucceed {
                if status == .veritySucceed {
                    //: self.appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    self.noFeed(price: reportMoney, payType: payType)
                }
            }
            //: AppleIAPManager.shared.iap_checkUnfinishedTransactions()
            InjuryPubTransactionObserver.shared.perTransactions()
        }
    }

    //: func func__initLeftButton() {
    func perLeftAppropriateHoldfast() {
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            //: return
            return
        }
        //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "nav_back_black_nor"), style: .plain, target: self, action: #selector(func__closeAction(sender:)))
        let item = UIBarButtonItem(image: UIImage.scribeNumber(name: (String(noti_statusAdmitKey) + String(show_readyTitle.suffix(5)))), style: .plain, target: self, action: #selector(clotheSave(sender:)))
        //: item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        item.imageInsets = UIEdgeInsets(top: 0, left: -6, bottom: 0, right: 0)
        //: item.width = 34
        item.width = 34
        //: self.navigationItem.leftBarButtonItem = item
        self.navigationItem.leftBarButtonItem = item
    }

    //: func func__initRightButton() {
    func nameureNor() {
        //: self.navigationItem.rightBarButtonItem = nil
        self.navigationItem.rightBarButtonItem = nil
        //: if urlStr.contains(WebUrlType.Intimate.rawValue) {
        if urlStr.contains(ConcentrationTextLiteral.Intimate.rawValue) {
            //: let item = UIBarButtonItem.init(image: UIImage.BundleImageNamed(name: "ico_intimate_list_sm"), style: .plain, target: self, action: #selector(func__intimateAction))
            let item = UIBarButtonItem(image: UIImage.scribeNumber(name: (String(m_hideIdent) + String(k_accordName))), style: .plain, target: self, action: #selector(segment))
            //: self.navigationItem.rightBarButtonItem = item
            self.navigationItem.rightBarButtonItem = item
        }
    }

    //: @objc func func__closeAction(sender: UIBarButtonItem?) {
    @objc func clotheSave(sender: UIBarButtonItem?) {
        //: if webView.canGoBack && sender != nil {
        if webView.canGoBack, sender != nil {
            //: webView.goBack()
            webView.goBack()
            //: return
            return
        }
        //: if  exitBlock != nil {
        if exitBlock != nil {
            //: exitBlock()
            exitBlock()
        }
        //: self.tranConfig = nil
        self.tranConfig = nil
        //: func__removeBridgeMethod()
        diskRequest()
        //: if self.presentingViewController != nil {
        if self.presentingViewController != nil {
            // 当前页面dismiss后，下面还是网页时，手动调用viewDidAppear
            //: dismiss(animated: true) {
            dismiss(animated: true) {
                //: if let currentVC = self.currentViewController() {
                if let currentVC = self.springEquinox() {
                    //: if currentVC.isKind(of: TalkingWebViewController.self) &&
                    if currentVC.isKind(of: AsideViewController.self),
                       //: (currentVC as! TalkingWebViewController).isModal == true {
                       (currentVC as! AsideViewController).isModal == true
                    {
                        //: (currentVC as! TalkingWebViewController).jsEvent_onPageShow()
                        (currentVC as! AsideViewController).mightHaveBeenProgram()
                    }
                }
            }
            //: } else {
        } else {
            //: navigationController?.popViewController(animated: true)
            navigationController?.popViewController(animated: true)
        }
    }

    //: func func__checkConfig() {
    func doingly() {
        //: if config.isAlertType {
        if config.isAlertType {
            //: config.showLoadingView = true
            config.showLoadingView = true
            //: config.showCloseBtn = true
            config.showCloseBtn = true
        }
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: } else {
        } else {
            //: requestReady = true
            requestReady = true
        }
    }

    //: func func__hideLoadingBgViewAndCloseBtn(sucess: Bool) {
    func textSucess(sucess: Bool) {
        //: ProgressHUD.dismiss()
        VillageView.statusDismiss()
        //: UIView.animate(withDuration: 0.1) {
        UIView.animate(withDuration: 0.1) {
            //: self.view.backgroundColor = .clear
            self.view.backgroundColor = .clear
            //: if sucess {
            if sucess {
                //: self.closeBtn.isHidden = true
                self.closeBtn.isHidden = true
            }
        }
    }

    //: func func__clearJSBgColor() {
    func clearColor() {
        //: if config.clearBgColor {
        if config.clearBgColor {
            //: self.webView.evaluateJavaScript("document.getElementsByTagName('body')[0].style.background='rgba(0,0,0,0)'") { _, _  in
            self.webView.evaluateJavaScript(String(bytes: mainDrownStr.map{$0^212}, encoding: .utf8)!) { _, _ in
            }

            //: webView.backgroundColor = .clear
            webView.backgroundColor = .clear
            //: webView.scrollView.backgroundColor = .clear
            webView.scrollView.backgroundColor = .clear
            //: webView.scrollView.bounces = false
            webView.scrollView.bounces = false
            //: webView.isOpaque = false
            webView.isOpaque = false
        }
    }

    //: func openTourl(url: String) {
    func statementLimited(url: String) {
        //: if url.count > 0 {
        if url.count > 0 {
            //: DumpPushManager.share.func__pushToWebVC(urlStr: url)
            DumpPushManager.share.noneControlInstall(urlStr: url)
        }
    }

    //: func openToOutUrl(url: String) {
    func privacyUrl(url: String) {
        //: UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
        UIApplication.shared.open(URL(string: url)!, options: [:], completionHandler: nil)
    }

    //: @objc private func closeDiscountsView() {
    @objc private func hesitateView() {
        //: if config.isDiscountsView {
        if config.isDiscountsView {
            //: var root = self.presentingViewController
            var root = self.presentingViewController
            //: while root != nil  {
            while root != nil {
                //: root?.dismiss(animated: true)
                root?.dismiss(animated: true)
                //: root = root?.presentingViewController
                root = root?.presentingViewController
            }
            //: NotificationCenter.default.removeObserver(self, name: VIDEOCALL_END_CLOSEDISCOUNTS_NOTIFICATION, object: nil)
            NotificationCenter.default.removeObserver(self, name: noti_dismissText, object: nil)
        }
    }

    /// 亲密度说明
    //: @objc private func func__intimateAction() {
    @objc private func segment() {
        //: let view = TalkingIntimateInstructionsPopUpView.init(frame: CGRect(x: 0, y: 0, width: ScreenWidth, height: ScreenHeight))
        let view = UpwardsViewDelegate(frame: CGRect(x: 0, y: 0, width: noti_senseContent, height: appMineMessage))
        //: view.show()
        view.writtenUntilWho()
    }
}

//: extension TalkingWebViewController: WKNavigationDelegate, WKUIDelegate {
extension AsideViewController: WKNavigationDelegate, WKUIDelegate {
    //: func webView(_ webView: WKWebView, decidePolicyFor navigationAction: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
    func webView(_: WKWebView, decidePolicyFor _: WKNavigationAction, decisionHandler: @escaping (WKNavigationActionPolicy) -> Void) {
        //: decisionHandler(.allow)
        decisionHandler(.allow)
    }

    //: func webView(_ webView: WKWebView, didStartProvisionalNavigation navigation: WKNavigation!) {
    func webView(_: WKWebView, didStartProvisionalNavigation _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = true
        UIApplication.shared.isNetworkActivityIndicatorVisible = true
        //: if config.showLoadingView {
        if config.showLoadingView {
            //: ProgressHUD.show(superView: view)
            VillageView.feeView(superView: view)
        }
        //: if config.showCloseBtn {
        if config.showCloseBtn {
            //: view.bringSubviewToFront(closeBtn)
            view.bringSubviewToFront(closeBtn)
        }
    }

    //: func webView(_ webView: WKWebView, didFinish navigation: WKNavigation!) {
    func webView(_: WKWebView, didFinish _: WKNavigation!) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: true)
            textSucess(sucess: true)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(true)
            openBlock(true)
        }
        //: func__initLeftButton()
        perLeftAppropriateHoldfast()
        //: func__clearJSBgColor()
        clearColor()
    }

    //: func webView(_ webView: WKWebView, didFailProvisionalNavigation navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFailProvisionalNavigation _: WKNavigation!, withError _: Error) {
        //: UIApplication.shared.isNetworkActivityIndicatorVisible = false
        UIApplication.shared.isNetworkActivityIndicatorVisible = false
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            textSucess(sucess: false)
        }

        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didFail navigation: WKNavigation!, withError error: Error) {
    func webView(_: WKWebView, didFail _: WKNavigation!, withError _: Error) {
        //: if config.showCloseBtn && config.showLoadingView {
        if config.showCloseBtn && config.showLoadingView {
            //: func__hideLoadingBgViewAndCloseBtn(sucess: false)
            textSucess(sucess: false)
        }
        //: if openBlock != nil {
        if openBlock != nil {
            //: openBlock(false)
            openBlock(false)
        }
    }

    //: func webView(_ webView: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
    func webView(_: WKWebView, didReceive challenge: URLAuthenticationChallenge, completionHandler: @escaping (URLSession.AuthChallengeDisposition, URLCredential?) -> Void) {
        //: DispatchQueue.global().async {
        DispatchQueue.global().async {
            //: if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
            if challenge.protectionSpace.authenticationMethod == NSURLAuthenticationMethodServerTrust {
                //: if challenge.previousFailureCount == 0 {
                if challenge.previousFailureCount == 0 {
                    //: let credential = URLCredential.init(trust: challenge.protectionSpace.serverTrust!)
                    let credential = URLCredential(trust: challenge.protectionSpace.serverTrust!)
                    //: completionHandler(.useCredential, credential)
                    completionHandler(.useCredential, credential)
                    //: } else {
                } else {
                    //: completionHandler(.cancelAuthenticationChallenge, nil)
                    completionHandler(.cancelAuthenticationChallenge, nil)
                }
                //: } else {
            } else {
                //: completionHandler(.cancelAuthenticationChallenge, nil)
                completionHandler(.cancelAuthenticationChallenge, nil)
            }
        }
    }

//    func webView(_ webView: WKWebView, runJavaScriptAlertPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping () -> Void) {
//        let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
//        let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
//            completionHandler()
//        }
//        alertController.addAction(action)
//        present(alertController, animated: true)
//    }

    //: func webView(_ webView: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
    func webView(_: WKWebView, runJavaScriptConfirmPanelWithMessage message: String, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (Bool) -> Void) {
        //: let alertController = UIAlertController.init(title: "Alert", message: message, preferredStyle: .alert)
        let alertController = UIAlertController(title: (String(user_soulContent)), message: message, preferredStyle: .alert)
        //: let action =  UIAlertAction.init(title: "OK".localized, style: .default) { _ in
        let action = UIAlertAction(title: "OK".localized, style: .default) { _ in
            //: completionHandler(true)
            completionHandler(true)
        }
        //: let cancleAction =  UIAlertAction.init(title: "Cancle", style: .cancel) { _ in
        let cancleAction = UIAlertAction(title: (String(showFoundationHidePaperMessage.suffix(6))), style: .cancel) { _ in
            //: completionHandler(false)
            completionHandler(false)
        }
        //: alertController.addAction(cancleAction)
        alertController.addAction(cancleAction)
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }

    //: func webView(_ webView: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame frame: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
    func webView(_: WKWebView, runJavaScriptTextInputPanelWithPrompt prompt: String, defaultText: String?, initiatedByFrame _: WKFrameInfo, completionHandler: @escaping (String?) -> Void) {
        //: let alertController = UIAlertController.init(title: prompt, message: "", preferredStyle: .alert)
        let alertController = UIAlertController(title: prompt, message: "", preferredStyle: .alert)
        //: alertController.addTextField { textField in
        alertController.addTextField { textField in
            //: textField.text = defaultText
            textField.text = defaultText
        }
        //: let action =  UIAlertAction.init(title: "Done".localized, style: .default) { _ in
        let action = UIAlertAction(title: (String(show_signData)).localized, style: .default) { _ in
            //: completionHandler(alertController.textFields![0].text)
            completionHandler(alertController.textFields![0].text)
        }
        //: alertController.addAction(action)
        alertController.addAction(action)

        //: present(alertController, animated: true)
        present(alertController, animated: true)
    }
}

//: extension TalkingWebViewController: WKScriptMessageHandler {
extension AsideViewController: WKScriptMessageHandler {
    //: func userContentController(_ userContentController: WKUserContentController, didReceive message: WKScriptMessage) {
    func userContentController(_: WKUserContentController, didReceive message: WKScriptMessage) {
        //: printLog(message: "js call method name = \(message.name), message = \(message.body)")
        printLog(message: (String(showSupportiveTitle.suffix(4)) + "all meth" + String(noti_electronicPath)) + "\(message.name)" + (String(show_numbUrl.prefix(8)) + String(m_menuId.prefix(4))) + "\(message.body)")
        //: DispatchQueue.main.async {
        DispatchQueue.main.async {
            //: self.HandleScriptMessage(message)
            self.homeClosed(message)
            //: if self.handleJSBlock != nil {
            if self.handleJSBlock != nil {
                //: self.handleJSBlock(message.name)
                self.handleJSBlock(message.name)
            }
        }
    }

    //: func func__addBridgeMethod() {
    func gimpy() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in AttemptNameLiteral.allCases {
            //: ucController.add(self, name: method.rawValue)
            ucController.add(self, name: method.rawValue)
        }
    }

    //: func func__removeBridgeMethod() {
    func diskRequest() {
        //: let ucController = webView.configuration.userContentController
        let ucController = webView.configuration.userContentController
        //: for method in WebMethodType.allCases {
        for method in AttemptNameLiteral.allCases {
            //: ucController.removeScriptMessageHandler(forName: method.rawValue)
            ucController.removeScriptMessageHandler(forName: method.rawValue)
        }
    }
}

//: extension TalkingWebViewController {
extension AsideViewController {
    //: func func__addUserScript() {
    func acquire() {
        //: var htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
        var htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain

        //: if htmlDomain.hasPrefix("http://") {
        if htmlDomain.hasPrefix((String(main_insertValue))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "http://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(main_insertValue)), with: "")
        }
        //: if htmlDomain.hasPrefix("https://") {
        if htmlDomain.hasPrefix((String(showHomeIdent) + String(notiPoseFormat))) {
            //: htmlDomain = htmlDomain.replacingOccurrences(of: "https://", with: "")
            htmlDomain = htmlDomain.replacingOccurrences(of: (String(showHomeIdent) + String(notiPoseFormat)), with: "")
        }
        //: var cookiesStr = ""
        var cookiesStr = ""
        //: let tempUid: String = AdministratorThen.share.loginUserMode.userID
        let tempUid: String = AdministratorThen.share.loginUserMode.userID
        //: let tempSessionID: String = AdministratorThen.share.loginSessionId
        let tempSessionID: String = AdministratorThen.share.loginSessionId
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: cookiesStr.append("var cookieNames = document.cookie.split('; ').map(function(cookie) { return cookie.split('=')[0] } );\n")
            cookiesStr.append(String(bytes: m_argumentName.map{$0^52}, encoding: .utf8)!)
            //: var isfirst = true
            var isfirst = true
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: isfirst = false
                isfirst = false
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(user_seekFosterName.suffix(4))) + "\(tempUid)" + (String(k_problemUrl)) + "\(htmlDomain)" + (String(show_unlessValue) + String(app_springPowKey))
                //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                cookiesStr.append((String(m_distinguishGlobalKey) + String(dreamCounselorContent) + String(mLabelId)) + "\(tempStr)';\n")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "PHPSESSID=\(tempSessionID);domin=\(htmlDomain);path=/"
                let tempStr = (kCommunicateData.replacingOccurrences(of: "somebody", with: "PH") + String(show_televisionId)) + "\(tempSessionID)" + (kMustCommendStr.capitalized + String(noti_whetherAnswerFormat)) + "\(htmlDomain)" + (String(show_unlessValue) + String(app_springPowKey))
                //: if isfirst {
                if isfirst {
                    //: cookiesStr.append("document.cookie='\(tempStr)';\n")
                    cookiesStr.append((String(m_distinguishGlobalKey) + String(dreamCounselorContent) + String(mLabelId)) + "\(tempStr)';\n")
                    //: } else {
                } else {
                    //: cookiesStr.append("if (cookieNames.indexOf('PHPSESSID') == -1) { document.cookie='\(tempStr)'; };\n")
                    cookiesStr.append(String(bytes: m_clearWithKey.map{blindCap(rocket: $0)}, encoding: .utf8)! + "\(tempStr)'; };\n")
                }
            }
            //: } else {
        } else {
            //: if tempUid.count > 0 {
            if tempUid.count > 0 {
                //: let tempStr = "uid=\(tempUid);domain=\(htmlDomain);path=/"
                let tempStr = (String(user_seekFosterName.suffix(4))) + "\(tempUid)" + (String(k_problemUrl)) + "\(htmlDomain)" + (String(show_unlessValue) + String(app_springPowKey))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(m_distinguishGlobalKey) + String(dreamCounselorContent) + String(mLabelId)) + "\(tempStr)'")
            }
            //: if tempSessionID.count > 0 {
            if tempSessionID.count > 0 {
                //: let tempStr = "uid=\(tempSessionID);domain=\(htmlDomain);path=/"
                let tempStr = (String(user_seekFosterName.suffix(4))) + "\(tempSessionID)" + (String(k_problemUrl)) + "\(htmlDomain)" + (String(show_unlessValue) + String(app_springPowKey))
                //: cookiesStr.append("document.cookie='\(tempStr)'")
                cookiesStr.append((String(m_distinguishGlobalKey) + String(dreamCounselorContent) + String(mLabelId)) + "\(tempStr)'")
            }
        }

        //: if cookiesStr.count > 0 {
        if cookiesStr.count > 0 {
            //: let userScript = WKUserScript.init(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            let userScript = WKUserScript(source: cookiesStr, injectionTime: .atDocumentStart, forMainFrameOnly: false)
            //: webView.configuration.userContentController.addUserScript(userScript)
            webView.configuration.userContentController.addUserScript(userScript)
        }
    }

    //: func func__checkWebCookiesBeforeRequest(webConfig: WKWebViewConfiguration) {
    func primaryContainer(webConfig: WKWebViewConfiguration) {
        // iOS11+ 判断当前的WKWebViewCookie
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
            let cookieStore = webConfig.websiteDataStore.httpCookieStore
            //: let htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
            let htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
            //: let domain: String = URL.init(string: htmlDomain)?.host ?? ""
            let domain: String = URL(string: htmlDomain)?.host ?? ""
            //: printLog(message: "h5Domain = \(htmlDomain)")
            printLog(message: (String(kAspectThinUrl.suffix(5)) + "ain " + m_clearlyMessage.replacingOccurrences(of: "schedule", with: " ")) + "\(htmlDomain)")
            //: let userID: String = AdministratorThen.share.loginUserMode.userID
            let userID: String = AdministratorThen.share.loginUserMode.userID
            //: cookieStore.getAllCookies { [weak self] cookies in
            cookieStore.getAllCookies { [weak self] cookies in
                //: guard let self = self else { return }
                guard let self = self else { return }
                //: printLog(message: "getAllCookies = \(cookies)")
                printLog(message: (String(mApprovalPath.suffix(6)) + String(noti_hisId.suffix(6)) + "s = ") + "\(cookies)")
                //: var hasUidCookie = false
                var hasUidCookie = false
                //: var hasSessionCookie = false
                var hasSessionCookie = false
                //: for cookie in cookies {
                for cookie in cookies {
                    //: if domain.contains(cookie.domain) {
                    if domain.contains(cookie.domain) {
                        //: if cookie.name == "PHPSESSID" {
                        if cookie.name == (user_containTitle.replacingOccurrences(of: "eye", with: "H") + String(noti_borderMsg)) {
                            //: hasSessionCookie = true
                            hasSessionCookie = true
                            //: continue
                            continue
                        }
                        //: if cookie.name == "uid" && cookie.value == userID {
                        if cookie.name == (String(noti_fourQuoteKey)), cookie.value == userID {
                            //: hasUidCookie = true
                            hasUidCookie = true
                            //: continue
                            continue
                        }
                        //: if hasSessionCookie && hasUidCookie {
                        if hasSessionCookie, hasUidCookie {
                            //: break
                            break
                        }
                    }
                }
                /// 服务器有返回
                //: if hasUidCookie && hasSessionCookie {
                if hasUidCookie, hasSessionCookie {
                    //: self.requestReady = true
                    self.requestReady = true
                    //: self.func__beginStartRequest()
                    self.sourceAttentionFeed()
                    //: } else {
                } else {
                    //: self.func__insertWebCookies(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                    self.plumageYield(webConfig: webConfig, hasUid: hasUidCookie, hasSession: hasSessionCookie)
                }
            }
        }
    }

    //: func func__insertWebCookies(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool ) {
    func plumageYield(webConfig: WKWebViewConfiguration, hasUid: Bool, hasSession: Bool) {
        //: if #available(iOS 11.0, *) {
        if #available(iOS 11.0, *) {
            //: DispatchQueue.main.async {
            DispatchQueue.main.async {
                //: let cookieStore = webConfig.websiteDataStore.httpCookieStore
                let cookieStore = webConfig.websiteDataStore.httpCookieStore
                //: let htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
                let htmlDomain: String = AdministratorThen.share.appConfigMode.urlH5Domain
                //: let h5Domain: String = URL.init(string: htmlDomain)?.host ?? ""
                let h5Domain: String = URL(string: htmlDomain)?.host ?? ""
                //: printLog(message: "h5Domain = \(htmlDomain)")
                printLog(message: (String(kAspectThinUrl.suffix(5)) + "ain " + m_clearlyMessage.replacingOccurrences(of: "schedule", with: " ")) + "\(htmlDomain)")
                //: let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL.init(string: TalkingRequestAddrTool.share.serverUrlStr)!)!
                let appCookies: [HTTPCookie] = HTTPCookieStorage.shared.cookies(for: URL(string: BubbleVisitorReactiveCompatible.share.serverUrlStr)!)!
                //: var addCookies: [HTTPCookie] = []
                var addCookies: [HTTPCookie] = []

                //: printLog(message: "appCookies = \(appCookies)")
                printLog(message: (String(showChanceContent.prefix(4)) + "ookies =" + userGravityKey.capitalized) + "\(appCookies)")

                //: for cookies in appCookies {
                for cookies in appCookies {
                    //: if cookies.name.uppercased() == "UID" && !hasUid {
                    if cookies.name.uppercased() == (String(show_thousandValue)), !hasUid {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "uid"
                        temPro[HTTPCookiePropertyKey.name] = (String(noti_fourQuoteKey))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                    //: if cookies.name.uppercased() == "PHPSESSID" && !hasSession {
                    if cookies.name.uppercased() == (user_containTitle.replacingOccurrences(of: "eye", with: "H") + String(noti_borderMsg)), !hasSession {
                        //: var temPro: Dictionary<HTTPCookiePropertyKey, Any> = cookies.properties!
                        var temPro: [HTTPCookiePropertyKey: Any] = cookies.properties!
                        //: temPro[HTTPCookiePropertyKey.name] = "PHPSESSID"
                        temPro[HTTPCookiePropertyKey.name] = (user_containTitle.replacingOccurrences(of: "eye", with: "H") + String(noti_borderMsg))
                        //: temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        temPro[HTTPCookiePropertyKey.domain] = h5Domain
                        //: let temCookie: HTTPCookie = HTTPCookie.init(properties: temPro)!
                        let temCookie: HTTPCookie = .init(properties: temPro)!
                        //: addCookies.append(temCookie)
                        addCookies.append(temCookie)
                    }
                }
                //: for cookies in addCookies {
                for cookies in addCookies {
                    //: cookieStore.setCookie(cookies)
                    cookieStore.setCookie(cookies)
                }
                //: self.requestReady = true
                self.requestReady = true
                //: self.func__beginStartRequest()
                self.sourceAttentionFeed()
            }
        }
    }

    //: func func__addcustomUserAgent() {
    func ceremonyAgent() {
        //: self.webView.evaluateJavaScript("navigator.userAgent") { [weak self] (result, err) in
        self.webView.evaluateJavaScript((main_bottomKey.replacingOccurrences(of: "somebody", with: "ig") + "ator." + String(kSizeGageStr.prefix(7)) + mFindStr.replacingOccurrences(of: "array", with: "t"))) { [weak self] result, _ in
            //: guard let self = self else { return }
            guard let self = self else { return }
            //: printLog(message: "navigator.userAgent")
            printLog(message: (main_bottomKey.replacingOccurrences(of: "somebody", with: "ig") + "ator." + String(kSizeGageStr.prefix(7)) + mFindStr.replacingOccurrences(of: "array", with: "t")))
            //: var userAgent = result as? String ?? ""
            var userAgent = result as? String ?? ""
            //: if !userAgent.contains("xunwu_iphone") {
            if !userAgent.contains((String(k_soilName.suffix(7)) + dream_hairValue.replacingOccurrences(of: "standard", with: "e"))) {
                //: let str = getUserAgentString()
                let str = twine()
                //: userAgent.append(" \(str)")
                userAgent.append(" \(str)")
                //: DispatchQueue.main.async {
                DispatchQueue.main.async {
                    //: self.webView.customUserAgent = userAgent
                    self.webView.customUserAgent = userAgent
                }
            }
            //: UIDevice.getDeviceDeviceIdentifier { deviceId in
            UIDevice.artifactGuidance { deviceId in
                //: guard deviceId.count > 0 else { return }
                guard deviceId.count > 0 else { return }
                //: let str = " xunwu_device_id/\(deviceId)"
                let str = (String(userRemarkKey) + String(mUniformId.prefix(4))) + "\(deviceId)"
                //: if !userAgent.contains(str) {
                if !userAgent.contains(str) {
                    //: userAgent.append(" \(str)")
                    userAgent.append(" \(str)")
                    //: DispatchQueue.main.async {
                    DispatchQueue.main.async {
                        //: self.webView.customUserAgent = userAgent
                        self.webView.customUserAgent = userAgent
                    }
                }
            }
        }
    }

    //: func getUserAgentString() -> String {
    func twine() -> String {
        //: return "xunwu_iphone xunwu_version/\(AppNetVersion) xunwu_package/\(PackageID) xunwu_bundleId/\(AppBundle)  webkit/1"
        return (noti_boyId.replacingOccurrences(of: "powder", with: "wu") + "_ipho" + String(showVesselText.prefix(4)) + "unwu_v" + app_indexPrepareMsg.replacingOccurrences(of: "come", with: "r") + notiIdentifyKey.capitalized) + "\(user_dailyName)" + (String(notiNeedTitle) + String(mainTrustMsg) + String(userTapMsg)) + "\(dreamStreamKey)" + (String(kMaterialText.suffix(6)) + "_bundle" + String(user_diskIdent.prefix(3))) + "\(mainSelectIdent)" + (String(app_leadingTitle.prefix(6)) + "it/1")
    }
}
