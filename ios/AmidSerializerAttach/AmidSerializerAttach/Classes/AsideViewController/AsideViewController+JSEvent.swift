
//: Declare String Begin

/*: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION" :*/
fileprivate let dream_pastMsg:[UInt8] = [0x77,0x65,0x62,0x76,0x69,0x65,0x77,0x7f,0x74,0x68,0x69,0x72,0x64,0x7f,0x72,0x65,0x66,0x72,0x65,0x73,0x68,0x63,0x6f,0x69,0x6e,0x7f,0x6e,0x6f,0x74,0x69,0x66,0x69,0x63,0x61,0x74,0x69,0x6f,0x6e]

private func workThumb(artifact num: UInt8) -> UInt8 {
    return num ^ 32
}

/*: "goodsId" :*/
fileprivate let dreamGratedPath:String = "gevereverds"
fileprivate let noti_colorfulPrepareName:[Character] = ["I","d"]

/*: "source" :*/
fileprivate let user_livingData:[Character] = ["s","o","u","r","c","e"]

/*: "type" :*/
fileprivate let noti_effectData:String = "sectionype"

/*: "title" :*/
fileprivate let user_kingId:String = "titlfreedom"

/*: "url" :*/
fileprivate let dream_gameKey:[Character] = ["u","r","l"]

/*: "money" :*/
fileprivate let k_priceContent:[Character] = ["m","o","n","e","y"]

/*: "Other" :*/
fileprivate let kWarmShrinkValue:String = "tun processing sign contain annualOther"

/*: "scene" :*/
fileprivate let showConnectionIdent:String = "sclistnlist"

/*: "show" :*/
fileprivate let show_buttonData:[Character] = ["s","h","o","w"]

/*: "target" :*/
fileprivate let dream_comePath:String = "partyargeparty"

/*: "eventName" :*/
fileprivate let userBroadcastFormat:String = "evesupport"

/*: "jsonString" :*/
fileprivate let m_liteData:String = "jsonStrintolerance match end"
fileprivate let dreamDetectFormat:[Character] = ["g"]

/*: "coin" :*/
fileprivate let app_staffFormat:String = "thinin"

/*: "uid" :*/
fileprivate let notiSuperiorArenaMiracleId:String = "uirunning"

/*: "未实现的js事件： :*/
fileprivate let noti_failureUrl:[Character] = ["未","\u{5b9e}","现","的","j","s","\u{4e8b}","件","："]

/*: "PurchaseClick" :*/
fileprivate let user_museSectionText:String = "street clock sit beyond helpPurchase"
fileprivate let showLanceStr:[Character] = ["C","l","i","c","k"]

/*: "Retry After or Go to " :*/
fileprivate let k_boaKey:[Character] = ["R","e","t","r","y"," ","A","f"]
fileprivate let dreamTextMsg:[Character] = ["t","e","r"," ","o","r"," ","G","o"," ","t","o"," "]

/*: "Feedback" :*/
fileprivate let main_recordValue:String = "Feedbbullet record"
fileprivate let notiExampleKey:String = "componentck"

/*: " to contact us" :*/
fileprivate let showOriginUrl:String = " to conet city king"
fileprivate let appTwentyAddPath:String = "anyone edition seet us"

/*: "Apple" :*/
fileprivate let appDitUnlessId:[Character] = ["A","p","p","l","e"]

/*: " apple支付充值失败： :*/
fileprivate let app_harassmentMessage:[Character] = [""," ","a","p","p","l","e","支","付","充"]
fileprivate let mServiceName:String = "manage失败："

/*: "payResultCallback();" :*/
fileprivate let app_capableText:String = "send cast official giftpayRes"
fileprivate let app_lateMergeIdent:String = "afeministbac"

/*: "USD" :*/
fileprivate let userSecondMessage:[Character] = ["U","S","D"]

/*: "amount" :*/
fileprivate let dreamWhoIdent:[UInt8] = [0xf1,0xfd,0xff,0xe5,0xfe,0xe4]

private func fromComposition(fog num: UInt8) -> UInt8 {
    return num ^ 144
}

/*: "getSystemNotificationtStatus(true)" :*/
fileprivate let m_decideLittleOnPath:[UInt8] = [0xaf,0xad,0xbc,0x9b,0xc1,0xbb,0xbc,0xad,0xb5,0x96,0xb7,0xbc,0xb1,0xae,0xb1,0xab,0xa9,0xbc,0xb1,0xb7,0xb6,0xbc,0x9b,0xbc,0xa9,0xbc,0xbd,0xbb,0x70,0xbc,0xba,0xbd,0xad,0x71]

fileprivate func titleLine(root num: UInt8) -> UInt8 {
    let value = Int(num) - 72
    if value < 0 {
        return UInt8(value + 256)
    } else {
        return UInt8(value)
    }
}

/*: "getSystemNotificationtStatus(false)" :*/
fileprivate let userCropValue:[UInt8] = [0x97,0x95,0x84,0xa3,0x89,0x83,0x84,0x95,0x9d,0xbe,0x9f,0x84,0x99,0x96,0x99,0x93,0x91,0x84,0x99,0x9f,0x9e,0x84,0xa3,0x84,0x91,0x84,0x85,0x83,0xd8,0x96,0x91,0x9c,0x83,0x95,0xd9]

private func missNatural(tumble num: UInt8) -> UInt8 {
    return num ^ 240
}

/*: "mfBean" :*/
fileprivate let mTrimFormat:[UInt8] = [0x75,0x6e,0x4a,0x6d,0x69,0x76]

fileprivate func squareSlatSilent(sale num: UInt8) -> UInt8 {
    let value = Int(num) + 248
    if value > 255 {
        return UInt8(value - 256)
    } else {
        return UInt8(value)
    }
}

//: Declare String End

// __DEBUG__
// __CLOSE_PRINT__
//
//  AsideViewController+JSEvent.swift
//  AbroadTalking
//
//  Created by young on 2022/9/6.
//

//: import UIKit
import UIKit
//: import WebKit
import WebKit

// 通知三方H5刷新金币
//: public let WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION = NSNotification.Name(rawValue: "WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION")
public let m_thirdId = NSNotification.Name(rawValue: String(bytes: dream_pastMsg.map{workThumb(artifact: $0)}, encoding: .utf8)!)

//: extension TalkingWebViewController {
extension AsideViewController {
    /// 处理JS事件
    /// - Parameters:
    ///   - message: 消息
    //: func HandleScriptMessage(_ message: WKScriptMessage) {
    func homeClosed(_ message: WKScriptMessage) {
        //: let type = WebMethodType(rawValue: message.name)
        let type = AttemptNameLiteral(rawValue: message.name)
        //: let json = JSON(message.body)
        let json = JSON(message.body)
        //: switch type {
        switch type {
        //: case .MethodType_Exit:
        case .MethodType_Exit:
            //: self.func__closeAction(sender: nil)
            self.clotheSave(sender: nil)

        //: case .MethodType_needLogin:
        case .MethodType_needLogin:
            //: needLogin()
            grant()

        //: case .MethodType_apPay:
        case .MethodType_apPay:
            //: applePay(productId: json["goodsId"].stringValue, source: json["source"].intValue, payType: .Pay)
            rutType(productId: json[(dreamGratedPath.replacingOccurrences(of: "ever", with: "o") + String(noti_colorfulPrepareName))].stringValue, source: json[(String(user_livingData))].intValue, payType: .Pay)

        //: case .MethodType_apPay_subscribe:
        case .MethodType_apPay_subscribe:
            //: if json["type"].stringValue == "6" {
            if json[(noti_effectData.replacingOccurrences(of: "section", with: "t"))].stringValue == "6" {
                //: isVideoCallRechargeHalfPage = true
                isVideoCallRechargeHalfPage = true
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                dreamErrTitle = noti_allBarStr
                //: } else if json["type"].stringValue == "7" { // 点击订阅弹窗的订阅按钮
            } else if json[(noti_effectData.replacingOccurrences(of: "section", with: "t"))].stringValue == "7" { // 点击订阅弹窗的订阅按钮
                //: RECHARGE_CLICK_EVENT = subscribtionRecordButton
                dreamErrTitle = noti_allBarStr
            }
            //: applePay_subscribe(type: json["type"].stringValue, productId: json["goodsId"].stringValue, payType: .Subscribe)
            moreFollowing(type: json[(noti_effectData.replacingOccurrences(of: "section", with: "t"))].stringValue, productId: json[(dreamGratedPath.replacingOccurrences(of: "ever", with: "o") + String(noti_colorfulPrepareName))].stringValue, payType: .Subscribe)

        //: case .MethodType_changeMenuButton:
        case .MethodType_changeMenuButton:
            //: changeRightMenuButton(title: json["title"].stringValue, url: json["url"].stringValue)
            rubric(title: json[(user_kingId.replacingOccurrences(of: "freedom", with: "e"))].stringValue, url: json[(String(dream_gameKey))].stringValue)

        //: case .MethodType_refreshCoin:
        case .MethodType_refreshCoin:
            //: refreshCoin(price: Double(json["money"].stringValue) ?? 0, payMode: "Other", scene: json["scene"].stringValue)
            applyStart(price: Double(json[(String(k_priceContent))].stringValue) ?? 0, payMode: (String(kWarmShrinkValue.suffix(5))), scene: json[(showConnectionIdent.replacingOccurrences(of: "list", with: "e"))].stringValue)

        //: case .MethodType_appearsOnGiftBroadcast:
        case .MethodType_appearsOnGiftBroadcast:
            //: appearsOnGiftBroadcast(show: json["show"].boolValue)
            satisfyShow(show: json[(String(show_buttonData))].boolValue)

        //: case.MethodType_ToUrl:
        case .MethodType_ToUrl:
            //: openTourl(url: json.rawString() ?? "")
            statementLimited(url: json.rawString() ?? "")

        //: case .MethodType_customerService:
        case .MethodType_customerService:
            //: DumpPushManager.share.func__pushToPriveteChatVC(chatID: CreditsThen.getCustomerServiceID())
            DumpPushManager.share.estimatedHave(chatID: CreditsThen.tingId())

        //: case.setRightMenuButton:
        case .setRightMenuButton:
            //: break
            break

        //: case .MethodType_taskCenter:
        case .MethodType_taskCenter:
            //: let target = json["target"].intValue
            let target = json[(dream_comePath.replacingOccurrences(of: "party", with: "t"))].intValue
            //: self.reloadInputViews()
            self.reloadInputViews()
            //: switch target {
            switch target {
            //: case 1:
            case 1: /// 签到领金币页
                //: DumpPushManager.share.func__pushToSubscribePageWebVC()
                DumpPushManager.share.annualReward()

            //: case 2:
            case 2: /// 个人信息编辑页面
                //: let vc = TalkingEditProfilesVC.init()
                let vc = ProfilesVc()
                //: self.navigationController?.pushViewController(vc, animated: true)
                self.navigationController?.pushViewController(vc, animated: true)

            //: case 3:
            case 3: /// 真人认证页面
                //: verifyBtnClick()
                analysisSplit()
            //: case 4:
            case 4: /// 发布动态页面
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                pastConcerned(type: ExactlyNameConvertible.Moment.rawValue)
                /// 防止动态首页广播还没创建
                //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    //: NotificationCenter.default.post(name: FREEBTN_UPLOAD_POST_NOTIFICATION,
                    NotificationCenter.default.post(name: user_keyContent,
                                                    //: object: nil,
                                                    object: nil,
                                                    //: userInfo: nil)
                                                    userInfo: nil)
                }
            //: case 5:
            case 5: /// 我的页面
                //: tabberSelete(type: TabBarItemType.Account.rawValue)
                pastConcerned(type: ExactlyNameConvertible.Account.rawValue)
            //: case 6:
            case 6: /// 首页
                //: tabberSelete(type: TabBarItemType.Social.rawValue)
                pastConcerned(type: ExactlyNameConvertible.Social.rawValue)
            //: case 7:
            case 7: /// 消息列表页
                //: tabberSelete(type: TabBarItemType.Message.rawValue)
                pastConcerned(type: ExactlyNameConvertible.Message.rawValue)
            //: case 8:
            case 8: /// Greet setting页
                //: let vc = TalkingAutoGreetSettingVC()
                let vc = FlowViewController()
                //: navigationController?.pushViewController(vc, animated: true)
                navigationController?.pushViewController(vc, animated: true)
            //: case 9:
            case 9: /// 动态列表页
                //: tabberSelete(type: TabBarItemType.Moment.rawValue)
                pastConcerned(type: ExactlyNameConvertible.Moment.rawValue)
            //: default:
            default:
                //: break
                break
            }
        //: case .MethodType_systemNotification:
        case .MethodType_systemNotification:
            //: let type = json["type"].intValue
            let type = json[(noti_effectData.replacingOccurrences(of: "section", with: "t"))].intValue
            //: changeNotifaStatus(type: type)
            moderationType(type: type)

        //: case .MethodType_refreshPoint:
        case .MethodType_refreshPoint:
            //: refreshPoint()
            decree()

        //: case .MethodType_onEvent:
        case .MethodType_onEvent:
            //: reportEvent(eventName: json["eventName"].stringValue, jsonStr: json["jsonString"].stringValue)
            commonBean(eventName: json[(userBroadcastFormat.replacingOccurrences(of: "support", with: "n") + "tName")].stringValue, jsonStr: json[(String(m_liteData.prefix(9)) + String(dreamDetectFormat))].stringValue)

        //: case .MethodType_refreshCoinAll:
        case .MethodType_refreshCoinAll:
            //: req_refreshCoinAll(coin: json["coin"].stringValue)
            authority(coin: json[(app_staffFormat.replacingOccurrences(of: "thin", with: "co"))].stringValue)

        //: case .MethodType_refreshPackage:
        case .MethodType_refreshPackage:
            //: req_refreshPackage()
            totalmateAndPackage()

        //: case .MethodType_vibrate:
        case .MethodType_vibrate:
            //: phoneVibrate()
            representativeVibrate()

        //: case .MethodType_goFreeVideo:
        case .MethodType_goFreeVideo:
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
            DrawReactiveCompatible.reStart { _, _, _ in
                //: if AdministratorThen.share.loginUserMode.jumpType == 1 {
                if AdministratorThen.share.loginUserMode.jumpType == 1 {
                    //: self.func__closeAction(sender: nil)
                    self.clotheSave(sender: nil)
                    //: DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                    DispatchQueue.main.asyncAfter(deadline: DispatchTime.now() + 0.5) {
                        //: DumpPushManager.share.func__pushToRandomVideoVC(isBeginRand: false)
                        DumpPushManager.share.tagFeminist(isBeginRand: false)
                    }
                }
            }

        //: case .MethodType_goMessageDetail:
        case .MethodType_goMessageDetail:
            //: DumpPushManager.share.func__pushToPriveteChatVC(chatID: json["uid"].stringValue)
            DumpPushManager.share.estimatedHave(chatID: json[(notiSuperiorArenaMiracleId.replacingOccurrences(of: "running", with: "d"))].stringValue)

        //: case .MethodType_goPersonInfo:
        case .MethodType_goPersonInfo:
            //: DumpPushManager.share.func__pushToUserDetailVC(uid: json["uid"].stringValue)
            DumpPushManager.share.belowStreet(uid: json[(notiSuperiorArenaMiracleId.replacingOccurrences(of: "running", with: "d"))].stringValue)

        //: case .MethodType_showRechargeDialog:
        case .MethodType_showRechargeDialog:
            //: DumpPushManager.share.func__pushToHalfWebVC(webViewType: .RechargeHalfPage)
            DumpPushManager.share.attributeMove(webViewType: .RechargeHalfPage)

        //: case .OpenRetainPopup:
        case .OpenRetainPopup:
            //: self.func__closeAction(sender: nil)
            self.clotheSave(sender: nil)
        //: case .openBrowserWithUrl:
        case .openBrowserWithUrl:
            //: openToOutUrl(url: json.rawString() ?? "")
            privacyUrl(url: json.rawString() ?? "")
        //: case .none:
        case .none:
            //: printLog(message: "未实现的js事件：\(message.name)")
            printLog(message: (String(noti_failureUrl)) + "\(message.name)")
        }
    }

    // MARK: - Event

    /// 苹果订阅
    /// - Parameters:
    ///   - type: 订阅入口；1：首页banner，2：全屏充值页，3：半屏充值页，4：领取金币弹窗
    ///   - productId: 商品Id
    ///   - payType: 支付类型
    //: private func applePay_subscribe(type: String, productId: String, payType: ApplePayType) {
    private func moreFollowing(type _: String, productId: String, payType: AsideType) {
        //: applePay(productId: productId, payType: payType)
        rutType(productId: productId, payType: payType)
    }

    /// 苹果支付/订阅
    /// - Parameters:
    ///   - productId: productId: 商品Id
    ///   - source: 充值来源
    //: private func applePay(productId: String, source: Int = -1, payType: ApplePayType) {
    private func rutType(productId: String, source: Int = -1, payType: AsideType) {
        //: if AdministratorThen.share.loginUid.isEmptyString {
        if AdministratorThen.share.loginUid.isEmptyString {
            //: return
            return
        }
        //: FirebaseAnalyticsManager.share.logEvent(name: "PurchaseClick")
        GatherJawAnalyticsManager.share.earthyGroupAction(name: (String(user_museSectionText.suffix(8)) + String(showLanceStr)))

        //: ProgressHUD.show()
        VillageView.projectShow()
        //: var index = isVideoCallRechargeHalfPage ? 1: 0
        var index = isVideoCallRechargeHalfPage ? 1 : 0
        //: if source != -1 {
        if source != -1 {
            //: index = source
            index = source
        }
        //: AppleIAPManager.shared.iap_startPurchase(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
        InjuryPubTransactionObserver.shared.titleShade(productId: productId, payType: payType, source: index) { status, reportMoney, payType in
            //: ProgressHUD.dismiss()
            VillageView.statusDismiss()
            //: DispatchQueue.main.async { [self] in
            DispatchQueue.main.async { [self] in
                //: switch status {
                switch status {
                //: case .verityFail:
                case .verityFail:
                    //: ProgressHUD.toast( "Retry After or Go to \"Feedback\" to contact us".localized)
                    VillageView.asBalance((String(k_boaKey) + String(dreamTextMsg)) + "\"" + (String(main_recordValue.prefix(5)) + notiExampleKey.replacingOccurrences(of: "component", with: "a")) + "\"" + (String(showOriginUrl.prefix(6)) + "ntac" + String(appTwentyAddPath.suffix(4))).localized)
                //: case .veritySucceed:
                case .veritySucceed:
                    //: appleIAPVeritySucceed(price: reportMoney, payType: payType)
                    noFeed(price: reportMoney, payType: payType)
                //: case .renewSucceed:
                case .renewSucceed:
                    //: let scene = (payType == .Subscribe) ? "1":"2"
                    let scene = (payType == .Subscribe) ? "1" : "2"
                    //: refreshCoin(price: reportMoney, payMode: "Apple", scene: scene)
                    applyStart(price: reportMoney, payMode: (String(appDitUnlessId)), scene: scene)
                //: default:
                default:
                    //: printLog(message: " apple支付充值失败：\(status.rawValue)")
                    printLog(message: (String(app_harassmentMessage) + mServiceName.replacingOccurrences(of: "manage", with: "值")) + "\(status.rawValue)")
                }
            }
        }
    }

    /// 设置导航右侧按钮
    /// - Parameters:
    ///   - title: 标题
    ///   - url: 地址
    //: private func changeRightMenuButton(title: String, url: String) {
    private func rubric(title: String, url: String) {
        //: let rightBtn = TalkingButton()
        let rightBtn = WordOfAdviceThen()
        //: rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        rightBtn.titleLabel?.font = .systemFont(ofSize: 14)
        //: rightBtn.setTitle(title, for: .normal)
        rightBtn.setTitle(title, for: .normal)
        //: rightBtn.setTitleColor(.RGBA(51, 51, 51, 1), for: .normal)
        rightBtn.setTitleColor(.fruitThumb(51, 51, 51, 1), for: .normal)
        //: rightBtn.addTarget(self, action: #selector(rightButtonClick), for: .touchUpInside)
        rightBtn.addTarget(self, action: #selector(localMy), for: .touchUpInside)
        //: rightBtn.sizeToFit()
        rightBtn.sizeToFit()
        //: rightBtn.userInfo = ["url": url]
        rightBtn.userInfo = [(String(dream_gameKey)): url]
        //: self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
        self.navigationItem.rightBarButtonItem = UIBarButtonItem(customView: rightBtn)
    }

    //: @objc func rightButtonClick(button: TalkingButton) {
    @objc func localMy(button: WordOfAdviceThen) {
        //: guard let urlString = button.userInfo?["url"] as? String else {
        guard let urlString = button.userInfo?[(String(dream_gameKey))] as? String else {
            //: return
            return
        }

        //: self.urlStr = urlString
        self.urlStr = urlString
        //: func__startRequest()
        meRefuse()
    }

    /// 退出登录
    //: private func needLogin() {
    private func grant() {
        //: guard Int(AdministratorThen.share.loginUid) ?? 0 > 0 else {
        guard Int(AdministratorThen.share.loginUid) ?? 0 > 0 else {
            //: self.webView.reload()
            self.webView.reload()
            //: return
            return
        }

        //: NotificationCenter.default.post(name: DID_LOGIN_OUT_SUCCESS_NOTIFICATION,
        NotificationCenter.default.post(name: appVoiceUnderText,
                                        //: object: nil,
                                        object: nil,
                                        //: userInfo: nil)
                                        userInfo: nil)
    }

    /// 苹果 支付/订阅 成功验证通过
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payType: 支付类型
    //: func appleIAPVeritySucceed(price: Double, payType: ApplePayType) {
    func noFeed(price: Double, payType: AsideType) {
        //: self.webView.evaluateJavaScript("payResultCallback();") { data, error in
        self.webView.evaluateJavaScript((String(app_capableText.suffix(6)) + "ultC" + app_lateMergeIdent.replacingOccurrences(of: "feminist", with: "ll") + "k();")) { _, _ in
        }
        //: let scene = (payType == .Subscribe) ? "1":"2"
        let scene = (payType == .Subscribe) ? "1" : "2"
        //: refreshCoin(price: price, payMode: "Apple", scene: scene)
        applyStart(price: price, payMode: (String(appDitUnlessId)), scene: scene)
    }

    /// 支付/订阅 成功刷新个人信息【Apple/Other】
    /// - Parameters:
    ///   - price: 充值金额
    ///   - payMode: 类型
    ///   - scene: 1：订阅，2：内购
    //: private func refreshCoin(price: Double, payMode: String, scene: String) {
    private func applyStart(price: Double, payMode: String, scene: String) {
        //: NotificationCenter.default.post(name: UPDATE_USER_GETMYINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: mAccuracyName, object: nil)
        //: NotificationCenter.default.post(name: UPDATE_USER_GETINFOCOLUMN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: notiOfMsg, object: nil)
        //: NotificationCenter.default.post(name: REFRESH_INDEX_GETCONFIG_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: user_gameValue, object: nil)
        //: NotificationCenter.default.post(name: USER_UPDATE_LOUNGEPLUSINFO_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: dream_tableUrl, object: nil)
        // 成功埋点
        //: if scene == "1" {
        if scene == "1" {
            //: TalkingAdjustManager.share.addSubscribeEvent(price: price, currency: "USD")
            WingAdjustManager.share.circlet(price: price, currency: (String(userSecondMessage)))
            //: } else {
        } else {
            //: TalkingAdjustManager.share.addPurchasedEvent(price: price, currency: "USD")
            WingAdjustManager.share.invitationCurrency(price: price, currency: (String(userSecondMessage)))
        }
        //: FirebaseAnalyticsManager.share.trackPurchase(payType: payMode, price: price, currency: "USD")
        GatherJawAnalyticsManager.share.customAide(payType: payMode, price: price, currency: (String(userSecondMessage)))

        // 半屏充值成功埋点
        //: if RECHARGE_CLICK_EVENT.count > 0 {
        if dreamErrTitle.count > 0 {
            //: uploadRecord.uploadRecordEvent(eventID: RECHARGE_CLICK_EVENT, parameterStr: ["amount": price])
            k_hugeName.assetClothe(eventID: dreamErrTitle, parameterStr: [String(bytes: dreamWhoIdent.map{fromComposition(fog: $0)}, encoding: .utf8)!: price])
        }

        // 支付成功发送通知，调用三方js事件
        //: NotificationCenter.default.post(name: WEBVIEW_THIRD_REFRESHCOIN_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: m_thirdId, object: nil)
    }

    /// 广播设置
    /// - Parameter show: 是否上广播
    //: private func appearsOnGiftBroadcast(show: Bool) {
    private func satisfyShow(show _: Bool) {}

    //: private func verifyBtnClick() {
    private func analysisSplit() {
        //: DumpPushManager.share.func__pushUserVerifyController(toast: nil)
        DumpPushManager.share.supra(toast: nil)
    }

    /// 底部tabbar切换
    //: private func tabberSelete(type: Int) {
    private func pastConcerned(type: Int) {
        //: guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        guard type < self.tabBarController?.viewControllers?.count ?? 0 else { return }
        //: let vc = self.tabBarController?.viewControllers?[type]
        let vc = self.tabBarController?.viewControllers?[type]
        //: self.tabBarController?.tabBar.isHidden = false
        self.tabBarController?.tabBar.isHidden = false
        //: self.tabBarController?.selectedIndex = type
        self.tabBarController?.selectedIndex = type
        //: self.tabBarController?.selectedViewController = vc
        self.tabBarController?.selectedViewController = vc
        //: self.navigationController?.popViewController(animated: true)
        self.navigationController?.popViewController(animated: true)
    }

    /// 获取系统通知状态
    //: private func changeNotifaStatus(type: Int) {
    private func moderationType(type: Int) {
        //: if type == 0 {
        if type == 0 {
            //: if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
            if let url = URL(string: UIApplication.openSettingsURLString), UIApplication.shared.canOpenURL(url) {
                //: if #available(iOS 10.0, *) {
                if #available(iOS 10.0, *) {
                    //: UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    UIApplication.shared.open(url, options: [:], completionHandler: nil)
                    //: } else {
                } else {
                    //: if UIApplication.shared.canOpenURL(url) {
                    if UIApplication.shared.canOpenURL(url) {
                        //: UIApplication.shared.openURL(url)
                        UIApplication.shared.openURL(url)
                    }
                }
            }
            //: } else if type == 1 {
        } else if type == 1 {
            //: AppManagerRequest.func__requestUserConfig { succeed, result, errorModel in }
            DrawReactiveCompatible.bubbleQuote { _, _, _ in }
            //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in }
            DrawReactiveCompatible.reStart { _, _, _ in }
            //: TalkingPermissionTool.checkPushNotification { isShow in
            AsidePermissionTool.motivationNotification { isShow in
                //: DispatchQueue.main.sync {
                DispatchQueue.main.sync {
                    //: let str = isShow ? "getSystemNotificationtStatus(true)" : "getSystemNotificationtStatus(false)"
                    let str = isShow ? String(bytes: m_decideLittleOnPath.map{titleLine(root: $0)}, encoding: .utf8)! : String(bytes: userCropValue.map{missNatural(tumble: $0)}, encoding: .utf8)!
                    //: self.webView.evaluateJavaScript(str) { data, error in
                    self.webView.evaluateJavaScript(str) { _, _ in
                    }
                }
            }
        }
    }

    //: private func refreshPoint() {
    private func decree() {
        //: AppManagerRequest.func__requestUserInfo { succeed, result, errorModel in
        DrawReactiveCompatible.reStart { _, _, _ in
            //: if TalkingLiveManager.shared().isLive {
            if TapBecauseLiveManager.resolutionShared().isLive {
                //: NotificationCenter.default.post(name: LIVE_USER_POINT_CHANGE, object: nil, userInfo: ["mfBean": 0.0])
                NotificationCenter.default.post(name: mainScaleText, object: nil, userInfo: [String(bytes: mTrimFormat.map{squareSlatSilent(sale: $0)}, encoding: .utf8)!: 0.0])
            }
        }
    }

    /// 刷新金币
    /// - Parameter coin: 传' ', 刷新接口；传其他，进行相加（含负数））
    //: private func req_refreshCoinAll(coin: String) {
    private func authority(coin: String) {
        //: let value: Double = Double(coin) ?? 0
        let value = Double(coin) ?? 0
        //: if value == 0 {
        if value == 0 { // 刷新接口
            //: AppManagerRequest.func__getInfoColumn { succeed, result, errorModel in
            DrawReactiveCompatible.pan { _, _, _ in
            }
            //: } else {
        } else { // 加减
            //: let newCoin = Double(AdministratorThen.share.loginUserMode.mf_coin)! + value
            let newCoin = Double(AdministratorThen.share.loginUserMode.mf_coin)! + value
            //: AdministratorThen.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
            AdministratorThen.share.loginUserMode.mf_coin = String(format: "%.2f", newCoin)
        }
    }

    /// 刷新礼物背包
    //: private func req_refreshPackage() {
    private func totalmateAndPackage() {
        //: NotificationCenter.default.post(name: GIFT_REFRESHPACKAGE_NOTIFICATION, object: nil)
        NotificationCenter.default.post(name: appPingFormat, object: nil)
    }

    /// 事件上报
    /// - Parameters:
    ///   - eventName: 事件名称
    ///   - jsonStr: 事件参数
    //: private func reportEvent(eventName: String, jsonStr: String) {
    private func commonBean(eventName: String, jsonStr: String) {
        //: uploadRecord.uploadRecordEvent(eventID: eventName, toUid: "", jsonStr: jsonStr)
        k_hugeName.hour(eventID: eventName, toUid: "", jsonStr: jsonStr)
    }

    /// 手机震动
    //: private func phoneVibrate() {
    private func representativeVibrate() {
        //: AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
        AudioServicesPlaySystemSound(kSystemSoundID_Vibrate)
    }
}
